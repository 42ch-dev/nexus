{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/orchestration/capabilities/capability-info.schema.json",
  "schema_version": 1,
  "title": "Nexus CapabilityInfo",
  "description": "Description of a single registered capability (name + I/O schemas).",
  "type": "object",
  "required": ["name", "input_schema", "output_schema"],
  "properties": {
    "name": { "type": "string" },
    "input_schema": { "type": "string" },
    "output_schema": { "type": "string" },
    "origin": {
      "type": "string",
      "enum": ["builtin", "user", "peer"],
      "default": "builtin",
      "description": "Provenance of the capability (AR-40/AR-68): \"builtin\" ships with the engine, \"user\" is a locally-installed developer capability, \"peer\" is an admitted dialer tool."
    }
  },
  "additionalProperties": false
}
