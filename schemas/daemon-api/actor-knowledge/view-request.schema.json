{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/actor-knowledge/view-request.schema.json",
  "schema_version": 1,
  "title": "Nexus ActorKnowledgeViewRequest",
  "description": "Request body for POST /v1/daemon/actor-knowledge/view. actor_ref is admitted from stored owners; payload claims never establish scope. Character views require binding_id.",
  "type": "object",
  "required": ["actor_ref", "world_id"],
  "additionalProperties": false,
  "properties": {
    "actor_ref": {
      "$ref": "https://nexus42.invalid/schemas/domain/actor-ref.schema.json"
    },
    "world_id": {
      "$ref": "https://nexus42.invalid/schemas/common/common.schema.json#/definitions/WorldId"
    },
    "binding_id": {
      "$ref": "https://nexus42.invalid/schemas/common/common.schema.json#/definitions/ActorWorldBindingId",
      "description": "Required for Character actor_ref; must be the active binding of that Character in world_id."
    },
    "limit": { "type": "integer" },
    "cursor": {
      "type": "string",
      "description": "Opaque two-field keyset cursor from a previous page."
    }
  }
}
