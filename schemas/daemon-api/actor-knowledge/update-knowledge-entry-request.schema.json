{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/actor-knowledge/update-knowledge-entry-request.schema.json",
  "schema_version": 1,
  "title": "Nexus UpdateKnowledgeEntryRequest",
  "description": "Request body for PATCH /v1/daemon/characters/:character_id/knowledge/:entry_id. Only canonical_name and summary are mutable; null removes summary, omission keeps it.",
  "type": "object",
  "required": ["expected_revision"],
  "additionalProperties": false,
  "properties": {
    "expected_revision": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9223372036854775806
    },
    "canonical_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "summary": {
      "type": ["string", "null"],
      "maxLength": 65536,
      "description": "Plain UTF-8 summary stored in body.summary. Null removes the summary member; omission keeps it."
    }
  }
}
