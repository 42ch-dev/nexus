{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/creators/creator-detail.schema.json",
  "schema_version": 1,
  "title": "Nexus CreatorDetail",
  "description": "Response for GET /v1/daemon/creators/{creator_id}. holder_entry_id is the read-only service-managed holder projection of this already-authorized identity; there is no holder CRUD route.",
  "type": "object",
  "required": ["creator_id", "has_api_key", "has_cached_token", "is_active"],
  "properties": {
    "creator_id": { "type": "string" },
    "holder_entry_id": {
      "type": "string",
      "pattern": "^hld_[0-9a-f]{64}$",
      "description": "Read-only service-managed holder KnowledgeEntry id for this Creator (`hld_` namespace). Never accepted from a request body."
    },
    "handle": { "type": "string" },
    "display_name": { "type": "string" },
    "has_api_key": { "type": "boolean" },
    "has_cached_token": { "type": "boolean" },
    "is_active": { "type": "boolean" }
  },
  "additionalProperties": false
}
