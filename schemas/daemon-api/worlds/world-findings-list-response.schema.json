{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/worlds/world-findings-list-response.schema.json",
  "schema_version": 1,
  "title": "Nexus WorldFindingsListResponse",
  "description": "Read surface for world-attached check findings (V1.165 P1 T3 / DR-68, AR-3): GET /v1/daemon/worlds/{world_id}/findings. Items mirror the spoke Finding wire shape as inlined by check-response.schema.json (the check route returns the same shape verbatim); the projection converts the stored INTEGER Unix-epoch timestamps to RFC 3339 (spoke Timestamp = RFC 3339 date-time) and rehydrates the stored JSON columns (source_anchor / text_position / extensions). Severity/status are spoke vocabulary verbatim (info|warning|error / open|resolved|dismissed — AR-1: no nexus mapping on the world path). `truncated` is the honest flag for the 500-newest safety cap: true only when more rows exist than the cap. Owned world with zero findings → 200 + {\"findings\": [], \"truncated\": false} (PD-3).",
  "type": "object",
  "required": ["findings", "truncated"],
  "additionalProperties": false,
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "Spoke Finding wire shape (mirrored inline — same shape check-response.schema.json carries; epoch → RFC 3339 projection).",
        "required": ["finding_id", "schema_version", "title", "description", "severity", "status"],
        "additionalProperties": false,
        "properties": {
          "finding_id": { "type": "string", "description": "Stable finding id." },
          "schema_version": { "type": "integer", "minimum": 1, "description": "Wire schema version (integer >= 1)." },
          "title": { "type": "string", "description": "Short finding title." },
          "description": { "type": "string", "description": "Finding detail text." },
          "severity": { "type": "string", "description": "Open string. Core vocabulary (documented, not enforced): info, warning, error." },
          "status": { "type": "string", "description": "Open string. Core vocabulary (documented, not enforced): open, resolved, dismissed." },
          "kind": { "type": "string", "description": "Optional checker kind or category." },
          "target_entry_id": { "type": "string", "description": "Optional KnowledgeEntry the finding targets." },
          "source_anchor": {
            "type": "object",
            "description": "Spoke SourceAnchor wire shape (mirrored inline).",
            "required": ["source_id", "schema_version"],
            "additionalProperties": false,
            "properties": {
              "source_id": { "type": "string" },
              "schema_version": { "type": "integer", "minimum": 1 },
              "label": { "type": "string" },
              "mime_type": { "type": "string" },
              "span": {
                "type": "object",
                "required": ["start", "end"],
                "additionalProperties": false,
                "properties": {
                  "start": { "type": "number" },
                  "end": { "type": "number" }
                }
              },
              "extensions": { "type": "object", "additionalProperties": true }
            }
          },
          "suggested_fix": { "type": "string", "description": "Optional suggested remediation text." },
          "text_position": { "type": "object", "additionalProperties": true, "description": "Optional position hint within source text." },
          "created_at": { "type": "string", "format": "date-time", "description": "RFC 3339 UTC datetime string." },
          "updated_at": { "type": "string", "format": "date-time", "description": "RFC 3339 UTC datetime string." },
          "extensions": { "type": "object", "additionalProperties": true, "description": "Product namespace bag keyed by product-chosen ids matching ^[a-z][a-z0-9_-]*$. Values are opaque JSON objects." }
        }
      }
    },
    "truncated": {
      "type": "boolean",
      "description": "True when the 500-newest safety cap was exceeded and findings is the newest 500 (pagination lands with the Control Room panel — roadmap)."
    }
  }
}
