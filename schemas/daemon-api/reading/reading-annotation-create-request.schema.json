{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/reading/reading-annotation-create-request.schema.json",
  "schema_version": 1,
  "title": "Nexus ReadingAnnotationCreateRequest",
  "description": "Request body for POST /v1/daemon/reading/annotations. Creates a persistent highlight anchored by character offsets into the chapter body plain text. Creator scope is inferred from the active session.",
  "type": "object",
  "required": ["work_id", "chapter", "start_offset", "end_offset", "selected_text", "color"],
  "properties": {
    "work_id": { "type": "string" },
    "chapter": { "type": "integer", "minimum": 1 },
    "start_offset": {
      "type": "integer",
      "minimum": 0,
      "description": "Character offset into the current body plain text where the highlight begins (inclusive)."
    },
    "end_offset": {
      "type": "integer",
      "minimum": 0,
      "description": "Character offset into the current body plain text where the highlight ends (exclusive). Must be strictly greater than start_offset."
    },
    "selected_text": {
      "type": "string",
      "minLength": 1,
      "description": "The highlighted body text captured at creation time. Used for drift detection in the UI."
    },
    "color": {
      "type": "string",
      "enum": ["yellow", "blue", "green", "pink"],
      "description": "Highlight color. V1.89 enum: {yellow, blue, green, pink}."
    },
    "note": {
      "type": "string",
      "description": "Optional free-text note attached to the highlight."
    }
  },
  "additionalProperties": false
}
