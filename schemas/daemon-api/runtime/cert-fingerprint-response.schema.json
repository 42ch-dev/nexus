{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/daemon-api/runtime/cert-fingerprint-response.schema.json",
  "schema_version": 1,
  "title": "Nexus CertFingerprintResponse",
  "description": "Response for GET /v1/daemon/runtime/cert-fingerprint. Returns the SHA-256 fingerprint of the daemon's TLS certificate for TOFU pinning. No authentication required. When the daemon is loopback-only and has no TLS cert, the fingerprint field is an empty string and created_at is absent.",
  "type": "object",
  "required": ["fingerprint", "algorithm"],
  "properties": {
    "fingerprint": {
      "type": "string",
      "description": "SHA-256 fingerprint of the DER-encoded TLS certificate, colon-hex format (lowercase) with 'SHA256:' prefix. Example: 'SHA256:aa:bb:cc:dd:ee:ff:...'. Empty string when the daemon has no TLS cert (loopback-only mode)."
    },
    "algorithm": {
      "type": "string",
      "enum": ["sha256"],
      "description": "Hash algorithm used for the fingerprint. Always 'sha256' for the initial implementation."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the TLS certificate was generated or loaded at boot. Optional — present when the daemon has a cert; absent when loopback-only."
    }
  },
  "additionalProperties": false
}
