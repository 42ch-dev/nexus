{
 "$schema": "http://json-schema.org/draft-07/schema#",
 "$id": "https://nexus42.invalid/schemas/daemon-api/works/patch-work-request.schema.json",
 "schema_version": 2,
 "title": "Nexus PatchWorkRequest",
 "description": "Request body for PATCH /v1/daemon/works/{work_id}. `world_id` and `story_ref` are tri-state binding patches: omitted keeps the stored binding, `null` clears it to SQL NULL, a value sets it. Both fields are deliberately unrestricted at the JSON-Schema level: a typed nullable string (`anyOf`/`oneOf` [null, string]) collapses omission and null in the generated carrier, because typify resolves `anyOf` [null, T] to `Option<T>` (typify-impl `maybe_option`, enums.rs:27-52) and a non-required nullable property already carries the option (structs.rs:135-160). The Rust/TS boundary parser owns the three-state grammar and rejects every other value before any stored effect.",
 "type": "object",
 "properties": {
  "title": {
   "type": "string"
  },
  "long_term_goal": {
   "type": "string"
  },
  "creative_brief": {
   "type": "string"
  },
  "intake_status": {
   "type": "string"
  },
  "status": {
   "type": "string"
  },
  "world_id": {
   "description": "Tri-state World binding patch: omitted = keep the stored binding, null = clear it to SQL NULL, string = set it. Deliberately unrestricted so the generated carrier cannot flatten omission and null; the Rust/TS boundary parser owns the three-state grammar and rejects every other value before any stored effect."
  },
  "story_ref": {
   "description": "Tri-state story reference patch: omitted = keep the stored reference, null = clear it to SQL NULL, string = set it. Deliberately unrestricted so the generated carrier cannot flatten omission and null; the Rust/TS boundary parser owns the three-state grammar and rejects every other value before any stored effect."
  },
  "primary_preset_id": {
   "type": "string"
  },
  "current_stage": {
   "type": "string"
  },
  "stage_status": {
   "type": "string"
  },
  "force": {
   "type": "boolean"
  },
  "auto_review_master_on_timeout": {
   "type": "boolean"
  },
  "auto_chain_interrupted": {
   "type": "boolean"
  },
  "work_profile": {
   "type": "string"
  }
 },
 "additionalProperties": false
}
