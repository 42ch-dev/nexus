{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/orchestration/capabilities/list-capabilities-response.schema.json",
  "schema_version": 2,
  "title": "Nexus ListCapabilitiesResponse",
  "description": "Response for GET /v1/local/orchestration/capabilities (cursor-based pagination, F-P3). The array field is `items`; the legacy `capabilities` key was removed in `@42ch/nexus-contracts` 0.6.0.",
  "type": "object",
  "required": ["items", "pagination"],
  "properties": {
    "items": {
      "type": "array",
      "items": { "$ref": "https://nexus42.invalid/schemas/local-api/orchestration/capabilities/capability-info.schema.json" }
    },
    "pagination": { "$ref": "https://nexus42.invalid/schemas/local-api/kb/pagination-info.schema.json" }
  },
  "additionalProperties": false
}
