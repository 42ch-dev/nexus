{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/works/chapters/chapter-body.schema.json",
  "schema_version": 1,
  "title": "Nexus ChapterBody",
  "description": "Response body for GET /v1/local/works/{work_id}/chapters/{n}/body (V1.65 P0). Body is read-only through this surface.",
  "type": "object",
  "required": ["work_id", "chapter", "volume", "body_path", "content", "read_only", "updated_at"],
  "properties": {
    "work_id": { "type": "string" },
    "chapter": { "type": "integer", "minimum": 1 },
    "volume": { "type": "integer", "minimum": 1 },
    "body_path": { "type": "string" },
    "content": { "type": "string", "description": "Full body markdown content. Empty string if body file is missing or empty." },
    "frontmatter": {
      "type": "object",
      "description": "Parsed YAML frontmatter when available; omitted if not parsed by P0."
    },
    "read_only": { "type": "boolean", "description": "Always true in V1.65." },
    "updated_at": { "type": "string" }
  },
  "additionalProperties": false
}
