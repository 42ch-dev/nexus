{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/findings/list-findings-query.schema.json",
  "schema_version": 1,
  "title": "Nexus ListFindingsQuery",
  "description": "Query parameters for GET /v1/local/works/{work_id}/findings (cursor-based pagination, F-P2).",
  "type": "object",
  "properties": {
    "chapter": { "type": "integer" },
    "status": { "type": "string" },
    "severity": { "type": "string" },
    "limit": { "type": "integer" },
    "cursor": { "type": "string", "description": "Opaque pagination cursor returned by the previous response's `pagination.next_cursor`." }
  },
  "additionalProperties": false
}
