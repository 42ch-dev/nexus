{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/canvas/outline/outline-patch-chapter-request.schema.json",
  "schema_version": 1,
  "title": "Nexus OutlinePatchChapterRequest",
  "description": "Request body for POST /v1/local/works/{work_id}/chapters/{chapter_id}/patch (V1.72). Edits chapter-level metadata exposed on the outline canvas.",
  "type": "object",
  "required": ["work_id", "chapter_id", "base_revision", "set"],
  "properties": {
    "work_id": {
      "type": "string",
      "description": "Work identifier from the URL path. Must match the path parameter."
    },
    "chapter_id": {
      "type": "integer",
      "minimum": 1,
      "description": "Chapter number from the URL path. Must match the path parameter."
    },
    "base_revision": {
      "type": "integer",
      "minimum": 0,
      "description": "Revision observed by the client on the last canonical read."
    },
    "set": {
      "type": "object",
      "description": "Fields to update on the chapter. At least one property must be provided.",
      "minProperties": 1,
      "properties": {
        "title": {
          "type": "string",
          "description": "Display title for the chapter (UI-only; persisted in the work outline frontmatter)."
        },
        "slug": {
          "type": "string",
          "description": "Filename slug for the chapter."
        },
        "planned_word_count": {
          "type": "integer",
          "minimum": 0,
          "description": "Planned word count for the chapter."
        },
        "actual_word_count": {
          "type": "integer",
          "minimum": 0,
          "description": "Actual word count for the chapter, if known."
        },
        "volume": {
          "type": "integer",
          "minimum": 1,
          "description": "Volume binding for the chapter."
        },
        "status": {
          "type": "string",
          "enum": ["not_started", "outlined", "draft", "finalized", "published"],
          "description": "Lifecycle status of the chapter. published is read/protected in V1.72 patch routes unless explicitly authorized."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
