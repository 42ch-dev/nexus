{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/canvas/world-kb/world-kb-conflict-error.schema.json",
  "schema_version": 1,
  "title": "Nexus WorldKbConflictError",
  "description": "Structured detail placed inside the canonical ErrorResponse.details field when a World KB patch is rejected because expected_version is stale (HTTP 409). Per-row OCC on kb_key_blocks.revision / kb_extract_jobs.version (V1.73).",
  "type": "object",
  "required": ["current_version", "entity_id", "conflicting_path", "recovery_hint"],
  "properties": {
    "current_version": {
      "type": "integer",
      "minimum": 0,
      "description": "Current canonical per-row version of the entity or candidate at conflict time."
    },
    "entity_id": {
      "type": "string",
      "description": "Identifier of the key_block or candidate involved in the conflict."
    },
    "conflicting_path": {
      "type": "string",
      "description": "Dot-path or locator describing the row/version that changed underneath the client."
    },
    "recovery_hint": {
      "type": "string",
      "description": "Actionable hint for the client (e.g. 'refetch the graph and reapply')."
    }
  },
  "additionalProperties": false
}
