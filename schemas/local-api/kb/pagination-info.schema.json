{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/kb/pagination-info.schema.json",
  "schema_version": 1,
  "title": "Nexus PaginationInfo",
  "description": "Cursor-based pagination metadata.",
  "type": "object",
  "required": ["limit", "has_more"],
  "properties": {
    "limit": { "type": "integer" },
    "next_cursor": {
      "type": "string",
      "description": "Opaque cursor returned by the previous page. Clients MUST NOT parse it. Non-null only when another page exists."
    },
    "has_more": {
      "type": "boolean",
      "description": "True when the client may request another page (equivalent to `next_cursor` being non-null)."
    }
  },
  "additionalProperties": false
}
