{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/reading/reading-annotation-list-query.schema.json",
  "schema_version": 1,
  "title": "Nexus ReadingAnnotationListQuery",
  "description": "Query parameters for GET /v1/local/reading/annotations. Returns all annotations for the current creator on a given (work, chapter). Creator scope is inferred from the active session.",
  "type": "object",
  "required": ["work_id", "chapter"],
  "properties": {
    "work_id": { "type": "string" },
    "chapter": { "type": "integer", "minimum": 1 }
  },
  "additionalProperties": false
}
