{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/reading/reading-progress-request.schema.json",
  "schema_version": 1,
  "title": "Nexus ReadingProgressRequest",
  "description": "Request body for PUT /v1/local/reading/progress. Upserts persisted scroll position per (creator, work, chapter). Creator scope is inferred from the active session.",
  "type": "object",
  "required": ["work_id", "chapter", "scroll_progress"],
  "properties": {
    "work_id": { "type": "string" },
    "chapter": { "type": "integer", "minimum": 1 },
    "scroll_progress": {
      "type": "integer",
      "minimum": 0,
      "maximum": 10000,
      "description": "Current scroll position in thousandths (0–10000). Must be stored as-is; clients interpret the unit."
    }
  },
  "additionalProperties": false
}
