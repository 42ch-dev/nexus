{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/memory/delete-pending-review-response.schema.json",
  "schema_version": 1,
  "title": "Nexus DeletePendingReviewResponse",
  "description": "Response body for DELETE /v1/local/memory/pending-review/{id}. Echoes the path `pending_id`; `success` is `true` on deletion (a missing or non-owned row surfaces as an error envelope, not `success: false`).",
  "type": "object",
  "required": ["success", "pending_id"],
  "properties": {
    "success": { "type": "boolean" },
    "pending_id": { "type": "string" }
  },
  "additionalProperties": false
}
