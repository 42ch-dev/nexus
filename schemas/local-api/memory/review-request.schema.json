{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/local-api/memory/review-request.schema.json",
  "schema_version": 1,
  "title": "Nexus ReviewRequest",
  "description": "Request body for POST /v1/local/memory/review. Triggers the review/summarization pipeline for the active creator's entire pending queue. `creator_id` must match the active creator (config.toml), otherwise 403.",
  "type": "object",
  "required": ["creator_id"],
  "properties": {
    "creator_id": { "type": "string" }
  },
  "additionalProperties": false
}
