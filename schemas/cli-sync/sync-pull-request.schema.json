{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus.42ch.io/schemas/cli-sync/sync-pull-request.schema.json",
  "schema_version": 1,
  "title": "Nexus SyncPullRequest",
  "description": "Request body for POST /v1/sync/pull — incremental bundle fetch from the platform (CLI/daemon client contract).",
  "type": "object",
  "required": ["schema_version", "world_id"],
  "properties": {
    "schema_version": {
      "$ref": "https://nexus.42ch.io/schemas/common/common.schema.json#/definitions/SchemaVersion"
    },
    "world_id": {
      "$ref": "https://nexus.42ch.io/schemas/common/common.schema.json#/definitions/WorldId"
    },
    "after_confirmed_delta_sequence": {
      "type": "integer",
      "minimum": 0,
      "description": "Return bundles with server delta sequence strictly greater than this cursor (incremental pull)."
    }
  },
  "additionalProperties": false
}
