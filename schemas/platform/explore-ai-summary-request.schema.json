{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/platform/explore-ai-summary-request.schema.json",
  "schema_version": 1,
  "title": "Nexus ExploreAiSummaryRequest",
  "description": "Request body for Explore AI summarization over a world or manuscript (platform plan 19).",
  "type": "object",
  "required": ["schema_version", "scope", "entity_id"],
  "properties": {
    "schema_version": {
      "$ref": "https://nexus42.invalid/schemas/common/common.schema.json#/definitions/SchemaVersion"
    },
    "scope": {
      "type": "string",
      "enum": ["world", "manuscript"],
      "description": "Whether entity_id is a world or manuscript"
    },
    "entity_id": {
      "type": "string",
      "minLength": 4,
      "maxLength": 128,
      "description": "Target id (e.g. wld_*, stm_*) — validated per scope on the platform"
    },
    "max_length": {
      "type": "integer",
      "minimum": 128,
      "maximum": 16000,
      "description": "Soft cap on summary length in characters"
    }
  },
  "additionalProperties": false
}
