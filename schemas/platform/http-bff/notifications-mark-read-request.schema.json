{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/platform/http-bff/notifications-mark-read-request.schema.json",
  "schema_version": 1,
  "title": "Nexus NotificationsMarkReadRequest",
  "description": "Request body for marking notifications read (platform plan 20). Either pass explicit ids or mark_all.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": {
      "$ref": "https://nexus42.invalid/schemas/common/common.schema.json#/definitions/SchemaVersion"
    },
    "notification_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^ntf_[a-zA-Z0-9]+$"
      },
      "maxItems": 500,
      "description": "Ids to mark read; omit when using mark_all"
    },
    "mark_all": {
      "type": "boolean",
      "description": "When true, mark all visible notifications read for the principal"
    }
  },
  "additionalProperties": false
}
