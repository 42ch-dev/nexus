{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexus.42ch.io/schemas/platform/explore-search-request.schema.json",
  "schema_version": 1,
  "title": "Nexus ExploreSearchRequest",
  "description": "Request body for POST /v1/explore/search — read-only full-text style query.",
  "type": "object",
  "required": ["schema_version", "query"],
  "properties": {
    "schema_version": {
      "$ref": "https://nexus.42ch.io/schemas/common/common.schema.json#/definitions/SchemaVersion"
    },
    "query": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "description": "Search query string"
    },
    "cursor": {
      "type": "string",
      "description": "Opaque pagination cursor from a prior response"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "description": "Max hits to return (server may cap)"
    }
  },
  "additionalProperties": false
}
