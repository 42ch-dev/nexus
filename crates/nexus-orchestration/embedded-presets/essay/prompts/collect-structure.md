# Essay Init — Structure

Structure confirmed.

What short name (slug) should we use for this essay? This becomes the folder name under `Works/`. Use lowercase letters, hyphens, and numbers only.

Example: `why-we-write`
