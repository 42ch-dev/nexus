# Combat Engine — Advance Timeline

The combat cycle is complete. Record the battle events to the world's narrative timeline.

## Instructions
1. The compute module has already appended timeline events (combat actions, damage events, status changes).
2. Verify that all events have valid `event_type` (e.g., `state_update`, `story_advance`) and `status` (`provisional`).
3. The timeline position is automatically incremented by the daemon.

## Output
Confirm the number of timeline events created and the current timeline sequence number.
