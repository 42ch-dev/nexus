[package]
name = "nexus-storage-guard"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Audited SQLite FFI boundary for connection-local writer protocol scalar functions"

[lints.rust]
unsafe_code = "allow"

[dependencies]
sqlx = { workspace = true }
libsqlite3-sys = { version = "=0.30.1", features = ["bundled"] }
