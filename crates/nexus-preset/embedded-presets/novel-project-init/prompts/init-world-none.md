---
vars:
  initial_idea: { type: string, required: true }
max_tokens: 1000
---

# Novel Project Init — Worldless

The creator chose to keep the Work worldless (no World binding).

Optionally ask for a brief **setting note** (1-2 paragraphs) that will be included in the Work's README.md for LLM context.

Guidelines:
- This is optional — they can skip it
- If they provide a note, it will help the novel-writing preset maintain consistency
- Keep it to 1-2 paragraphs max
