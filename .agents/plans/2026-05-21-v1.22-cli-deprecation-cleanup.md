# V1.22 — CLI Deprecation Cleanup — Implementation Plan

**Plan ID**: `2026-05-21-v1.22-cli-deprecation-cleanup`
**Created**: 2026-05-21
**Status**: Draft
**Owner**: `@project-manager`
**Agents**: `@fullstack-dev`, `@qa-engineer`, `@qc-specialist`
**Iteration compass**: `.agents/iterations/v1.22-cli-deprecation-cleanup-delivery-compass-v1.md`
**Branch**: `feature/v1.22-cli-deprecation-cleanup` (single integration, PR to main)

---

## Scope

Remove all deprecated CLI commands, their implementations, deprecated runtime redirects, obsolete tests, and dead imports. Product is pre-release — no backward compatibility required.

**Complexity**: Medium — some deprecated modules contain shared utilities actively used by replacement commands. These must be extracted before deletion.

---

## Batch plan

### Batch A — Extract shared utilities from deprecated modules

**Goal**: Move shared functions out of `init.rs` and `schedule.rs` so those modules can be safely deleted.

| ID | Task | Detail |
|----|------|--------|
| A1 | Extract init utilities to `creator.rs` | Move `default_creative_root`, `validate_slug`, `materialize_adr014_workspace`, `persist_cli_workspace_selection` from `commands/init.rs` → `commands/creator.rs` (or a new `creator/workspace_utils.rs`). Update all imports in `creator.rs`. |
| A2 | Extract schedule runner to `daemon.rs` | Move `schedule::run()` logic into `commands/daemon.rs` directly (it's only called from there). Remove `commands/schedule.rs` import from `daemon.rs`. |
| A3 | Verify extraction | `cargo check --workspace` + `cargo test --workspace` pass. No behavioral change. |

**Verification**: `cargo check && cargo test`

### Batch B — Remove fully-deletable command modules

**Goal**: Delete modules that have NO cross-references from active code.

| ID | Task | Detail | Lines removed |
|----|------|--------|--------------|
| B1 | Delete `auth.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. | ~64 |
| B2 | Delete `db.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. | ~118 |
| B3 | Delete `debug.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. | ~308 |
| B4 | Delete `doctor.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. Remove `main.rs:49` redirect. | ~464 |
| B5 | Delete `explore.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. Remove `main.rs:58` redirect. | ~34 |
| B6 | Delete `config.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. | ~178 |
| B7 | Delete `identity.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. | ~513 |
| B8 | Delete `runtime_mode.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. | ~251 |
| B9 | Delete `context.rs` top-level | No cross-refs to command entry. Remove from `mod.rs`. Remove from `cli.rs`. Keep `context/` dir if summary.rs is still used. | ~794 |
| B10 | Delete `permission.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. Remove `main.rs:81` redirect. | ~607 |
| B11 | Delete `policy.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. Remove `main.rs:76` redirect. | ~154 |
| B12 | Delete `session.rs` | No cross-refs. Remove from `mod.rs`. Remove from `cli.rs`. Remove `main.rs:72` redirect. | ~245 |
| B13 | Delete `preset.rs` top-level | No cross-refs to command entry. Remove from `mod.rs`. Remove from `cli.rs`. Check if `system preset` has its own impl. | ~745 |
| B14 | Delete `agent.rs` | Remove from `mod.rs`. Remove from `cli.rs`. | ~? |
| B15 | Delete `init.rs` (post-extraction) | After A1, all shared functions moved. Remove from `mod.rs`. Remove from `cli.rs`. | ~318 |
| B16 | Delete `clone.rs` | `creator.rs` uses `CloneArgs` — move struct to `creator.rs` first. Then delete. | ~216 |
| B17 | Delete `schedule.rs` (post-extraction) | After A2, run logic moved to `daemon.rs`. Remove from `mod.rs`. Remove from `cli.rs`. | ~1027 |
| B18 | Delete `soul.rs` | Remove sub-command from `creator.rs`. Delete file. Remove from `mod.rs`. | ~228 |
| B19 | Delete `memory.rs` | Remove sub-command from `creator.rs`. Delete file. Remove from `mod.rs`. | ~337 |

**Verification**: `cargo check --workspace` (expect compile errors → fix in Batch C)

### Batch C — Clean up dependent code and imports

**Goal**: Fix all compile errors from Batch B deletions.

| ID | Task | Detail |
|----|------|--------|
| C1 | Clean `cli.rs` | Remove all `#[command(subcommand)]` entries for deleted commands. Remove `mod` references. |
| C2 | Clean `commands/mod.rs` | Remove `pub mod` declarations for deleted modules. |
| C3 | Clean `main.rs` | Remove all deprecated redirect `eprintln!` blocks (lines ~49, 58, 72, 76, 81). |
| C4 | Clean `creator.rs` | Remove `SoulCommand`/`MemoryCommand` sub-commands. Remove deleted imports. |
| C5 | Clean `acp.rs` | Remove deprecated `agent run` subcommand (line 838+). Remove `cmd_agent_use` fn and `agent_use_is_stub` test. |
| C6 | Fix `ContextSummaryBuilder::with_default_config` | Remove `#[deprecated]` annotation; if no call sites remain, delete function. |
| C7 | Fix `agent.rs` deprecation warning | Remove deprecation `eprintln!` in `commands/agent.rs:94` (wait — agent.rs is being deleted). Check `acp_worker/` instead. |
| C8 | Remove dead imports | `cargo check` and fix any remaining import errors. |

**Verification**: `cargo check --workspace` (zero errors, zero warnings)

### Batch D — Test cleanup and final verification

**Goal**: Remove tests for deleted code, run full verification.

| ID | Task | Detail |
|----|------|--------|
| D1 | Remove obsolete tests | Delete any `#[cfg(test)]` modules inside deleted files (already gone with B). Check for test helpers referencing deleted commands. |
| D2 | Run `cargo test --workspace` | All tests pass. |
| D3 | Run `cargo clippy --all -- -D warnings` | Zero warnings. |
| D4 | Run `cargo fmt --all` | Ensure formatting. |
| D5 | Verify zero deprecated markers | `rg 'deprecated' crates/nexus42/src/` returns only non-CLI matches (if any). |

**Verification**: Full CI suite passes.

---

## Acceptance criteria

1. `rg 'deprecated' crates/nexus42/src/cli.rs` → 0 matches
2. `rg 'deprecated' crates/nexus42/src/main.rs` → 0 matches
3. `rg '#\[deprecated\]' crates/nexus42/src/` → 0 matches (excluding generated)
4. No `commands/init.rs`, `commands/auth.rs`, `commands/db.rs`, `commands/debug.rs`, `commands/doctor.rs`, `commands/explore.rs`, `commands/clone.rs`, `commands/config.rs`, `commands/context.rs`, `commands/identity.rs`, `commands/runtime_mode.rs`, `commands/schedule.rs`, `commands/permission.rs`, `commands/policy.rs`, `commands/session.rs`, `commands/soul.rs`, `commands/memory.rs`, `commands/preset.rs` on disk (or only non-deprecated versions)
5. `cargo test --workspace` passes
6. `cargo clippy --all -- -D warnings` passes

---

## Dependencies

| Batch | Depends on |
|-------|-----------|
| A | None |
| B | A |
| C | B |
| D | C |

**Parallelism**: B tasks are independent of each other (can be split across subagents). C tasks may need sequential ordering.

---

## Risk

**Low** — pure deletion. Key risk is missing a cross-reference, caught by `cargo check`.

---

## Estimated batches

4 batches (A: extract, B: delete, C: clean, D: verify)

## Notes

- `context.rs` (794 lines) may contain shared types used by `platform.rs` — verify before deleting
- `preset.rs` (745 lines) — check if `system preset` reuses types from this module
- `schedule.rs` (1027 lines) — largest module; after extraction to daemon.rs, verify daemon tests
