# V1.6 WS-A — Residual Governance

**Plan ID**: `2026-04-20-v1.6-ws-a-residual-governance`
**Status**: Todo
**Compass (SSOT)**: [knowledge/v1.6-delivery-compass-v1.md](../knowledge/v1.6-delivery-compass-v1.md) §4 WS-A
**Milestone**: M1 — Residual governance complete
**Working branch**: `feature/v1.6`
**Dependencies**: none

**Goal**: Resolve all 4 V1.5 residuals targeting V1.6 (R1, R2, R3, R6) to zero open medium-severity items.

**Authoritative design**: Compass §4 WS-A + referenced knowledge docs (creator-schedule-and-core-context-v1.md, orchestration-engine-v1.md).

**Residual coverage** (4 items → all resolved):

| Residual | Title | Severity | Scope |
|----------|-------|----------|-------|
| R1 | cancel signal pause_schedule error silently ignored | medium | `nexus42d/.../schedules.rs` |
| R2 | resume_schedule TOCTOU race | medium | `nexus-orchestration/.../supervisor.rs` |
| R3 | Scheduler::tick() dead code | low | `nexus-orchestration/.../scheduler/mod.rs` |
| R6 | Recovered sessions lack runners (NoGraphLoaded) | low | `nexus-orchestration/.../engine.rs` |

**Tasks**:

- T1: R1 — Log `pause_schedule()` error at `warn!` level; fix both cancel-path pause (line 545) and standalone pause signal path (line 482).
- T2: R2 — Check `rows_affected()` after `resume_schedule()` UPDATE; return current status if 0.
- T3: R3 — Remove dead `Scheduler::tick()` query; delegate to `tick_clocked()`.
- T4: R6 — Reconstruct FlowRunner from preset_id + CapabilityRegistry + embedded preset loader + SqliteSessionStorage after session recovery. Verify V1.5 QC2 S2 (`apply_llm_summarize` hex string handling).

**Evidence**:
- Unit test: pause_schedule error path emits `warn!` log; cancel still succeeds.
- Unit test: concurrent resume_schedule calls produce correct serial outcome.
- Dead code removal: `rg` confirms no remaining reference.
- Integration test: daemon restart → recovered session resumes without NoGraphLoaded.
- `cargo test --workspace` green; `cargo clippy --all -- -D warnings` clean.

**Effort (agent-oriented)**: M — 1–2 agent sessions.

---

*Created: 2026-04-20.*
