# V1.2 Tech Debt Cleanup Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Address 21 warning-level and above residuals from V1.1 Tech Debt Mitigation and previous plans, ensuring production readiness and code quality.

**Source:** Consolidated from all plans' residual findings (warning, high, medium severity).

**Priority:** P1 — These are quality improvements identified during V1.1 development that should be addressed before V1.2.

---

## Overview

**Total Residuals**: 21 (2 HIGH + 8 MEDIUM + 11 WARNING)

**Categories**:
- HIGH priority (2): Critical code quality issues
- MEDIUM priority (8): Important improvements
- WARNING priority (11): Quality improvements from QC reviews

---

## Task Groups

### Task Group 1: HIGH Priority Residuals (2 tasks)

**Estimated Effort**: S (~1 session)

#### Task 1: CTX-R4 — WorldId format validation

**Context**: CLI doesn't validate WorldId format at input. Users could input malformed IDs.

**Files**:
- `crates/nexus42/src/commands/context.rs`

**Acceptance Criteria**:
- [ ] Add clap validator for `wld_` prefix pattern
- [ ] Add unit tests for validation
- [ ] Update error messages to be user-friendly

**Target**: V1.2

---

#### Task 2: CTX-R5 — UTF-8 truncation safety

**Context**: UTF-8 truncation in opening excerpt needs multi-byte safety tests.

**Files**:
- `crates/nexus42/src/context/summary.rs`

**Acceptance Criteria**:
- [ ] Add tests for multi-byte character boundaries
- [ ] Add tests for edge cases (emoji, CJK characters)
- [ ] Verify is_char_boundary usage is correct

**Target**: V1.2

---

### Task Group 2: MEDIUM Priority Residuals (8 tasks)

**Estimated Effort**: M (~2-3 sessions)

#### Task 3: CLI-R15 — Add tracing to daemon API handlers

**Context**: HTTP handlers lack tracing/logging for request tracking and debugging.

**Files**:
- `crates/nexus42d/src/api/handlers/*.rs`

**Acceptance Criteria**:
- [ ] Add `tracing::info` for request start/end
- [ ] Add `tracing::debug` for key operations
- [ ] Include request ID in traces

**Target**: V1.2

---

#### Task 4: SYNC-R7 — BundleBuilder monotonicity validation

**Context**: BundleBuilder missing monotonicity validation.

**Files**:
- `crates/nexus-sync/src/delta_bundle.rs`

**Acceptance Criteria**:
- [ ] Add optional `validate()` method to builder
- [ ] Validate timestamp ordering
- [ ] Add tests for validation

**Target**: V1.2

---

#### Task 5: SYNC-R8 — Schema allOf constraint verification

**Context**: Schema allOf may not enforce constraints correctly.

**Files**:
- `schemas/cli-sync/bundle.schema.json`
- Generated types

**Acceptance Criteria**:
- [ ] Verify codegen output handles allOf correctly
- [ ] Add inline constraints if needed
- [ ] Add test cases

**Target**: V1.2

---

#### Task 6: SYNC-R9 — SyncClient auth token validation

**Context**: SyncClient doesn't validate auth token format.

**Files**:
- `crates/nexus-sync/src/sync_client.rs`

**Acceptance Criteria**:
- [ ] Add basic format validation for auth tokens
- [ ] Add tests for validation
- [ ] Update documentation

**Target**: V1.2

---

#### Task 7: SYNC-R10 — Outbox schema migration documentation

**Context**: Outbox schema migration path not documented.

**Files**:
- `crates/nexus-sync/src/outbox.rs`

**Acceptance Criteria**:
- [ ] Add schema_version column design
- [ ] Document migration strategy
- [ ] Add migration examples

**Target**: V1.2

---

#### Task 8: SYNC-R13 — AutoReject confirmation prompt

**Context**: AutoReject resolution lacks safety mechanism.

**Files**:
- `crates/nexus-sync/src/conflict.rs`
- CLI commands

**Acceptance Criteria**:
- [ ] Add confirmation prompt for auto-reject
- [ ] Add `--force` flag to bypass prompt
- [ ] Add tests

**Target**: V1.2

---

#### Task 9: FH-R2 — Pool timeout configuration

**Context**: Pool timeout not explicitly configured (uses deadpool default 30s).

**Files**:
- `crates/nexus42d/src/db/pool.rs`

**Acceptance Criteria**:
- [ ] Add configurable timeout to DbPool::new()
- [ ] Document default timeout
- [ ] Add builder pattern for configuration

**Target**: V1.2

---

#### Task 10: FH-R4 — NexusApiError auth variants

**Context**: NexusApiError auth variants underspecified (only AuthRequired).

**Files**:
- `crates/nexus42d/src/api/errors.rs`

**Acceptance Criteria**:
- [ ] Add auth-specific error variants
- [ ] Update error handling in auth middleware
- [ ] Add tests

**Target**: V1.2

---

### Task Group 3: WARNING Priority Residuals (11 tasks)

**Estimated Effort**: M (~2-3 sessions)

These are quality improvements from QC reviews. Group by component:

#### Task 11: QC-W1 — Migration documentation placement

**Files**: `outbox.rs`
**Action**: Add migration notes to CHANGELOG.md or create MIGRATION-v1.1.md
**Target**: Before V1.2 release

---

#### Task 12: QC-W2 — HTTP body size error variant

**Files**: `sync_client.rs`, `errors.rs`
**Action**: Add dedicated `SyncError::BodySizeLimitExceeded` variant
**Target**: V1.2

---

#### Task 13: QC-W3 — Pool status monitoring

**Files**: `pool.rs`, `outbox.rs`
**Action**: Expose pool status via API endpoint
**Target**: V1.2

---

#### Task 14: QC-W4 — InvalidParameterName fix

**Files**: `pool.rs`
**Action**: Use appropriate rusqlite::Error variant
**Target**: V1.2

---

#### Task 15: QC-W5 — Streaming body reader

**Files**: `sync_client.rs`
**Action**: Implement streaming body reader to prevent OOM
**Target**: V1.2

---

#### Task 16: QC-W6 — Test helper cleanup

**Files**: `outbox.rs`
**Action**: Add cleanup policy for temp directories
**Target**: V1.2

---

#### Task 17: QC-W7 — Pool configuration

**Files**: `pool.rs`
**Action**: Add runtime pool configuration (env var or builder)
**Target**: V1.2

---

#### Task 18: QC-W8 — Test count fix

**Files**: `status.json`
**Action**: Update test count to 8
**Target**: Immediate

---

#### Task 19: QC-W9 — CI script extraction

**Files**: `.github/workflows/ci.yml`
**Action**: Extract to `tooling/check-schema-drift.sh`
**Target**: V1.2

---

#### Task 20: QC-W10 — TempDir type hint

**Files**: `test_utils.rs`
**Action**: Add newtype wrapper or compile-time hint
**Target**: V1.2

---

#### Task 21: QC-W11 — Schema version type

**Files**: CI, contracts, CLI, daemon
**Action**: Standardize schema version type (u32 vs &str)
**Target**: V1.2

---

## Execution Order

**Recommended Execution**:
1. **Immediate**: Task 18 (test count fix)
2. **Sprint 1**: Task Group 1 (HIGH priority, 2 tasks)
3. **Sprint 2**: Task Group 2 (MEDIUM priority, 8 tasks)
4. **Sprint 3**: Task Group 3 (WARNING priority, 11 tasks)

**Parallelization**: Task Groups 1-3 can run in parallel if resources allow.

---

## Validation Commands

**After each task group**:
```bash
cargo test --all
cargo clippy --all -- -D warnings
cargo +nightly fmt --all -- --check
```

---

## Plan Completion Criteria

- [ ] All 21 residuals addressed or explicitly waived
- [ ] All tests passing
- [ ] 0 clippy warnings
- [ ] Documentation updated
- [ ] Ready for V1.2 release

---

## Dependencies

- **External**: None
- **Internal**: V1.1 GA complete (confirmed)

---

## Notes

This plan consolidates all warning-level and above residuals from V1.1 development. LOW severity residuals remain tracked in status.json but are not blocking V1.2 release.

**Created**: 2026-04-09
**Status**: Todo