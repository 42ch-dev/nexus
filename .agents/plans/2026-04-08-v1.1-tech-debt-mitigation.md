# V1.1 Tech Debt Mitigation + Feature Gaps Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve CRITICAL contract drift, complete missing schemas, and address 27 V1.1 residuals to align nexus OSS with v1-spec requirements.

**Architecture:** Three-wave approach: Wave 1 (P0) fixes critical blockers immediately; Wave 2 (P1) delivers V1.1 core features; Wave 3 (P2) hardens reliability. Each wave produces working, testable software.

**Tech Stack:** Rust (nexus42, nexus42d, nexus-domain), JSON Schema (schemas/), TypeScript (packages/nexus-contracts), SQLite, ACP SDK

---

## Wave Overview

| Wave | Priority | Scope | Estimated Sessions |
|------|----------|-------|-------------------|
| Wave 1 | P0 (Critical) | DM-R5 MembershipRole enum fix + 3 missing schemas | ~2 sessions |
| Wave 2 | P1 (V1.1 Core) | ACP session/policy, Research extraction, Context Assembly, Manuscript ops | ~6-8 sessions |
| Wave 3 | P2 (Hardening) | DEBT-X1/X2/X3, SYNC-R4/R5, Foundation residuals | ~4-6 sessions |

---

## Wave 1: P0 Critical Blockers

### Task 1: Fix MembershipRole Enum Drift (DM-R5)

**Context:** Rust domain uses `Owner/Admin/Curator/Collaborator/Viewer`, but v1-spec defines `owner/maintainer/collaborator/official_creator`. This causes permission mapping errors.

**Files:**
- Modify: `crates/nexus-domain/src/world_membership.rs`
- Modify: `crates/nexus-domain/src/world_membership.rs` (tests)
- Regenerate: `packages/nexus-contracts/src/generated/` + `crates/nexus-contracts/src/generated/` (via codegen)
- Test: `cargo test --package nexus-domain`

**Spec Source:** `data-model-v1.md` §5.4, §7

**Schema Source:** `schemas/domain/world-membership.schema.json` (already correct: `owner/maintainer/collaborator/official_creator`)

**Acceptance Criteria:**
- [ ] Rust enum matches spec exactly: `Owner, Maintainer, Collaborator, OfficialCreator`
- [ ] All permission methods (`can_confirm_canon`, `can_sync_kb`, etc.) updated for new roles
- [ ] All tests pass with new enum values
- [ ] Codegen regenerated and committed

---

- [ ] **Step 1: Write failing test for new MembershipRole enum**

Create test file: `crates/nexus-domain/src/world_membership.rs` (append to existing test module)

```rust
#[cfg(test)]
mod enum_alignment_tests {
    use super::*;

    #[test]
    fn membership_role_matches_spec() {
        // Verify enum has exactly 4 values matching spec
        let roles = vec![
            MembershipRole::Owner,
            MembershipRole::Maintainer,
            MembershipRole::Collaborator,
            MembershipRole::OfficialCreator,
        ];
        
        // Verify no other values exist (compile-time check)
        // If there were Owner/Admin/Curator/Viewer, this wouldn't compile
        assert_eq!(roles.len(), 4);
    }

    #[test]
    fn official_creator_role_exists() {
        // This test will fail until OfficialCreator variant exists
        let role = MembershipRole::OfficialCreator;
        assert_eq!(role.to_string(), "official_creator");
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test --package nexus-domain enum_alignment_tests`
Expected: FAIL with "no variant named `OfficialCreator`"

- [ ] **Step 3: Fix MembershipRole enum definition**

Modify: `crates/nexus-domain/src/world_membership.rs` (replace enum definition)

```rust
/// MembershipRole enum - matches v1-spec §5.4, §7
/// Values: owner, maintainer, collaborator, official_creator
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, strum::Display)]
#[serde(rename_all = "snake_case")]
#[strum(serialize_all = "snake_case")]
pub enum MembershipRole {
    Owner,
    Maintainer,
    Collaborator,
    OfficialCreator,
}
```

- [ ] **Step 4: Update permission methods for new roles**

Modify: `crates/nexus-domain/src/world_membership.rs` (update `can_confirm_canon` and `can_sync_kb`)

```rust
impl MembershipRole {
    /// Check if role can confirm canon content
    /// Per spec: owner + maintainer + official_creator can confirm
    pub fn can_confirm_canon(&self) -> bool {
        matches!(
            self,
            MembershipRole::Owner | MembershipRole::Maintainer | MembershipRole::OfficialCreator
        )
    }

    /// Check if role can sync knowledge base
    /// Per spec: owner + maintainer + collaborator can sync
    pub fn can_sync_kb(&self) -> bool {
        matches!(
            self,
            MembershipRole::Owner | MembershipRole::Maintainer | MembershipRole::Collaborator
        )
    }
}
```

- [ ] **Step 5: Update all existing tests to use new enum values**

Search and replace in `crates/nexus-domain/src/world_membership.rs` and related test files:

```bash
# Replace Admin → Maintainer (semantic mapping)
# Replace Curator → Collaborator (if used in tests)
# Remove Viewer references (not in spec)
# Add OfficialCreator test cases
```

Example test update:

```rust
#[test]
fn owner_has_full_permissions() {
    let role = MembershipRole::Owner; // unchanged
    assert!(role.can_confirm_canon());
    assert!(role.can_sync_kb());
}

#[test]
fn maintainer_can_confirm_and_sync() {
    let role = MembershipRole::Maintainer; // was Admin
    assert!(role.can_confirm_canon());
    assert!(role.can_sync_kb());
}

#[test]
fn collaborator_can_sync_but_not_confirm() {
    let role = MembershipRole::Collaborator; // was Curator
    assert!(!role.can_confirm_canon());
    assert!(role.can_sync_kb());
}

#[test]
fn official_creator_can_confirm_but_not_sync() {
    let role = MembershipRole::OfficialCreator; // new variant
    assert!(role.can_confirm_canon());
    assert!(!role.can_sync_kb());
}
```

- [ ] **Step 6: Run tests to verify all pass**

Run: `cargo test --package nexus-domain`
Expected: All tests PASS

- [ ] **Step 7: Regenerate TypeScript contracts**

Run: `pnpm run codegen`
Verify: `packages/nexus-contracts/src/generated/domain/WorldMembership.ts` has correct enum

Expected output:

```typescript
export enum MembershipRole {
  owner = "owner",
  maintainer = "maintainer",
  collaborator = "collaborator",
  official_creator = "official_creator",
}
```

- [ ] **Step 8: Regenerate Rust contracts**

Verify: `crates/nexus-contracts/src/generated/domain/world_membership.rs` has correct enum

Run: `cargo test --package nexus-contracts` (verify generated types compile)

- [ ] **Step 9: Update status.json residual**

Mark DM-R5 as resolved in `.agents/status.json`:

```json
{
  "metadata": {
    "residual_findings": {
      "2025-04-05-domain-models": [
        // Remove DM-R5 from array or mark lifecycle: "resolved"
      ]
    }
  }
}
```

- [ ] **Step 10: Commit**

```bash
git add crates/nexus-domain/src/world_membership.rs
git add packages/nexus-contracts/src/generated/
git add crates/nexus-contracts/src/generated/
git add .agents/status.json
git commit -m "fix(domain): align MembershipRole enum with v1-spec (DM-R5)

- Replace Owner/Admin/Curator/Collaborator/Viewer with Owner/Maintainer/Collaborator/OfficialCreator
- Update permission methods: can_confirm_canon, can_sync_kb
- Regenerate TypeScript and Rust contracts
- Resolves DM-R5 CRITICAL drift"
```

---

### Task 2: Create delta.schema.json

**Context:** v1-spec §5.12 defines Delta structure, but nexus OSS lacks independent schema file. Delta is currently inlined in bundle.schema.json.

**Files:**
- Create: `schemas/domain/delta.schema.json`
- Modify: `schemas/domain/bundle.schema.json` (reference new delta schema)
- Modify: `schemas/cli-sync/bundle.schema.json` (reference new delta schema)
- Regenerate: via codegen

**Spec Source:** `data-model-v1.md` §5.12

**Schema Definition:**

```json
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/domain/delta.schema.json",
  "title": "Delta",
  "description": "A single atomic change to an entity in a manuscript world",
  "type": "object",
  "required": ["delta_type", "operation", "target_entity_type", "target_entity_id", "local_timestamp"],
  "properties": {
    "delta_type": {
      "type": "string",
      "enum": ["create", "update", "delete"],
      "description": "Type of change operation"
    },
    "operation": {
      "type": "string",
      "description": "Specific operation name (e.g., 'update_character_name')"
    },
    "target_entity_type": {
      "type": "string",
      "description": "Type of entity being modified"
    },
    "target_entity_id": {
      "type": "string",
      "description": "ID of the entity being modified"
    },
    "payload": {
      "type": "object",
      "description": "Operation-specific payload data"
    },
    "source_anchor": {
      "$ref": "https://nexus42.invalid/schemas/common/source-anchor.schema.json",
      "description": "Source reference for this delta"
    },
    "local_timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when delta was created locally"
    }
  }
}
```

---

- [ ] **Step 1: Create delta.schema.json**

Create: `schemas/domain/delta.schema.json` (use schema above)

- [ ] **Step 2: Validate schema**

Run: `pnpm run validate-schemas`
Expected: All 23 schemas valid (22 existing + 1 new)

- [ ] **Step 3: Update bundle.schema.json to reference delta**

Modify: `schemas/domain/bundle.schema.json`

```json
{
  "properties": {
    "deltas": {
      "type": "array",
      "items": {
        "$ref": "delta.schema.json"
      },
      "description": "List of deltas in this bundle"
    }
  }
}
```

- [ ] **Step 4: Update cli-sync bundle.schema.json**

Modify: `schemas/cli-sync/bundle.schema.json` (same reference change)

- [ ] **Step 5: Regenerate contracts**

Run: `pnpm run codegen`

Verify: `packages/nexus-contracts/src/generated/domain/Delta.ts` exists
Verify: `crates/nexus-contracts/src/generated/domain/delta.rs` exists

- [ ] **Step 6: Run tests**

Run: `cargo test --all`
Run: `pnpm run typecheck`

Expected: All tests PASS

- [ ] **Step 7: Commit**

```bash
git add schemas/domain/delta.schema.json
git add schemas/domain/bundle.schema.json
git add schemas/cli-sync/bundle.schema.json
git add packages/nexus-contracts/src/generated/
git add crates/nexus-contracts/src/generated/
git commit -m "feat(schema): add delta.schema.json per v1-spec §5.12

- Create independent Delta schema with delta_type/operation/payload/source_anchor
- Update bundle schemas to reference new delta schema
- Regenerate TypeScript and Rust contracts"
```

---

### Task 3: Create workspace-binding.schema.json

**Context:** v1-spec §5.14 defines WorkspaceBinding, but nexus OSS lacks schema file.

**Files:**
- Create: `schemas/domain/workspace-binding.schema.json`
- Regenerate: via codegen

**Spec Source:** `data-model-v1.md` §5.14

**Schema Definition:**

```json
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/domain/workspace-binding.schema.json",
  "title": "WorkspaceBinding",
  "description": "Binding between a local workspace and a remote world",
  "type": "object",
  "required": ["workspace_id", "local_root", "world_id", "creator_id", "binding_status"],
  "properties": {
    "workspace_id": {
      "type": "string",
      "description": "Unique workspace identifier"
    },
    "local_root": {
      "type": "string",
      "description": "Local filesystem path to workspace root"
    },
    "profile_name": {
      "type": "string",
      "description": "Optional profile name for this binding"
    },
    "world_id": {
      "type": "string",
      "description": "Remote world ID this workspace is bound to"
    },
    "creator_id": {
      "type": "string",
      "description": "Creator ID associated with this binding"
    },
    "binding_status": {
      "type": "string",
      "enum": ["active", "paused", "detached"],
      "description": "Current binding status"
    }
  }
}
```

---

- [ ] **Step 1: Create workspace-binding.schema.json**

Create: `schemas/domain/workspace-binding.schema.json` (use schema above)

- [ ] **Step 2: Validate schema**

Run: `pnpm run validate-schemas`
Expected: All 24 schemas valid

- [ ] **Step 3: Regenerate contracts**

Run: `pnpm run codegen`

Verify: `packages/nexus-contracts/src/generated/domain/WorkspaceBinding.ts` exists
Verify: `crates/nexus-contracts/src/generated/domain/workspace_binding.rs` exists

- [ ] **Step 4: Run tests**

Run: `cargo test --all`
Run: `pnpm run typecheck`

Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add schemas/domain/workspace-binding.schema.json
git add packages/nexus-contracts/src/generated/
git add crates/nexus-contracts/src/generated/
git commit -m "feat(schema): add workspace-binding.schema.json per v1-spec §5.14

- Define WorkspaceBinding with workspace_id/local_root/world_id/creator_id/binding_status
- Regenerate TypeScript and Rust contracts"
```

---

### Task 4: Create agent-profile.schema.json

**Context:** v1-spec §5.15 defines AgentProfile, but nexus OSS lacks schema file.

**Files:**
- Create: `schemas/domain/agent-profile.schema.json`
- Regenerate: via codegen

**Spec Source:** `data-model-v1.md` §5.15

**Schema Definition:**

```json
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://nexus42.invalid/schemas/domain/agent-profile.schema.json",
  "title": "AgentProfile",
  "description": "Configuration for an ACP agent in a workspace",
  "type": "object",
  "required": ["agent_profile_id", "workspace_id", "profile_kind", "selection_mode", "status"],
  "properties": {
    "agent_profile_id": {
      "type": "string",
      "description": "Unique profile identifier"
    },
    "workspace_id": {
      "type": "string",
      "description": "Workspace this profile belongs to"
    },
    "profile_kind": {
      "type": "string",
      "enum": ["primary", "specialist", "fallback"],
      "description": "Kind of agent profile"
    },
    "selection_mode": {
      "type": "string",
      "enum": ["explicit", "registry", "auto"],
      "description": "How agent is selected"
    },
    "registry_agent_id": {
      "type": "string",
      "description": "Agent ID from ACP Registry (if selection_mode=registry)"
    },
    "transport": {
      "type": "string",
      "enum": ["local", "http", "stdio"],
      "description": "Transport method for agent communication"
    },
    "default_output_manuscript": {
      "type": "string",
      "description": "Default manuscript for agent output"
    },
    "protocol_version": {
      "type": "string",
      "description": "ACP protocol version (e.g., '0.10.4')"
    },
    "status": {
      "type": "string",
      "enum": ["active", "inactive", "error"],
      "description": "Profile status"
    }
  }
}
```

---

- [ ] **Step 1: Create agent-profile.schema.json**

Create: `schemas/domain/agent-profile.schema.json` (use schema above)

- [ ] **Step 2: Validate schema**

Run: `pnpm run validate-schemas`
Expected: All 25 schemas valid

- [ ] **Step 3: Regenerate contracts**

Run: `pnpm run codegen`

Verify: `packages/nexus-contracts/src/generated/domain/AgentProfile.ts` exists
Verify: `crates/nexus-contracts/src/generated/domain/agent_profile.rs` exists

- [ ] **Step 4: Run tests**

Run: `cargo test --all`
Run: `pnpm run typecheck`

Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add schemas/domain/agent-profile.schema.json
git add packages/nexus-contracts/src/generated/
git add crates/nexus-contracts/src/generated/
git commit -m "feat(schema): add agent-profile.schema.json per v1-spec §5.15

- Define AgentProfile with profile_kind/selection_mode/transport/status
- Regenerate TypeScript and Rust contracts"
```

---

## Wave 1 Summary

**Deliverables:**
- MembershipRole enum aligned with v1-spec (DM-R5 resolved)
- 3 missing schemas created (delta, workspace-binding, agent-profile)
- Codegen regenerated for all new types
- All tests passing

**Evidence:**
- `cargo test --all` passes
- `pnpm run typecheck` passes
- `pnpm run validate-schemas` shows 25 valid schemas

---

## Wave 2: P1 V1.1 Core Features (Task Outline)

Wave 2 tasks will be detailed in subsequent plan sections during execution.

### Task Group 2.1: ACP Session & Permission Policy (T4)

**Tasks:**
- Task 5: Implement ACP session persistence (ACP-R6)
- Task 6: Implement permission policy engine (ACP-R7)
- Task 7: Implement daemon-mediated tool access (ACP-R8)

**Estimated Effort:** M-L (~3-4 sessions)

### Task Group 2.2: Research & Manuscript Operations (T1, T2)

**Tasks:**
- Task 8: Implement PDF/URL content extraction for Research (CLI-R8)
- Task 9: Add manuscript promote strict mode (CLI-R6)
- Task 10: Add manuscript verify content integrity (CLI-R7)

**Estimated Effort:** M (~2-3 sessions)

### Task Group 2.3: Context Assembly Enhancement (T3)

**Tasks:**
- Task 11: Add WorldId format validation (CTX-R4)
- Task 12: Fix UTF-8 truncation safety (CTX-R5)
- Task 13: Convert MemoryKind to enum (CTX-R6)
- Task 14: Add file size limit to summary generator (CTX-R2)
- Task 15: Add file I/O edge case tests (CTX-R7)

**Estimated Effort:** M (~2-3 sessions)

### Task Group 2.4: Multi-Creator Governance (T5)

**Tasks:**
- Task 16: Implement precheck auth match validation (SYNC-R6)
- Task 17: Honor retry_after from conflict response (SYNC-R11)
- Task 18: Persist partial apply retry state (SYNC-R12)

**Estimated Effort:** M (~2-3 sessions)

---

## Wave 3: P2 Reliability Hardening (Task Outline)

Wave 3 tasks address cross-cutting reliability issues.

### Task Group 3.1: SQLite Infrastructure (T7)

**Tasks:**
- Task 19: Add connection pooling to nexus-sync (DEBT-X1)
- Task 20: Deduplicate SQLite schema (DEBT-X2)
- Task 21: Standardize error propagation (DEBT-X3)

**Estimated Effort:** L (~3-5 sessions)

### Task Group 3.2: Sync Contract Hardening

**Tasks:**
- Task 22: Add outbox transaction atomicity tests (SYNC-R4)
- Task 23: Add HTTP body size limit to SyncClient (SYNC-R5)

**Estimated Effort:** M (~2 sessions)

### Task Group 3.3: Foundation Hardening Residuals

**Tasks:**
- Task 24: Resolve FH-R1 (schema drift detection)
- Task 25: Resolve FH-R3 (error code inconsistency)
- Task 26: Resolve FH-R6 (test helper duplication)

**Estimated Effort:** S-M (~1-2 sessions)

---

## Execution Order

**Recommended Execution:**
1. Complete Wave 1 (Tasks 1-4) first — these are P0 blockers
2. Wave 2 can be executed in parallel by multiple agents (Task Groups 2.1–2.4 are independent)
3. Wave 3 should start after Wave 2 Task Group 3.1 completes (depends on DEBT-X fixes)

**Parallel Execution Map:**
```
Wave 1 (Sequential): Task 1 → Task 2 → Task 3 → Task 4

Wave 2 (Parallel Groups):
  - Agent A: Task Group 2.1 (ACP session/policy)
  - Agent B: Task Group 2.2 (Research/Manuscript)
  - Agent C: Task Group 2.3 (Context Assembly)
  - Agent D: Task Group 2.4 (Multi-Creator)

Wave 3 (Sequential after Wave 2):
  Task Group 3.1 → Task Group 3.2 → Task Group 3.3
```

---

## Validation Commands

**After each wave:**
```bash
cargo test --all
cargo clippy --all -- -D warnings
cargo +nightly fmt --all -- --check
pnpm run validate-schemas
pnpm run codegen
git diff --exit-code packages/nexus-contracts/src/generated/ crates/nexus-contracts/src/generated/
pnpm run typecheck
```

**Evidence Required:**
- All tests passing
- 0 clippy warnings
- Schema validation success
- Codegen matches committed output
- TypeScript types compile

---

## Dependencies

**External Dependencies:**
- nexus-platform V1.0-phase2 must be complete (confirmed by user)
- ADR-002 domain model fixes must be stable (confirmed)
- ACP SDK v0.10.4 must be available (already integrated)

**Internal Dependencies:**
- Wave 2 Task Group 2.1 depends on Wave 1 Task 4 (AgentProfile schema)
- Wave 3 Task Group 3.1 depends on Wave 2 completion (to avoid conflicting changes)

---

## Plan Completion Criteria

**Wave 1 Complete:**
- [ ] DM-R5 resolved
- [ ] 3 schemas created and validated
- [ ] All tests passing
- [ ] Codegen regenerated

**Wave 2 Complete:**
- [ ] ACP session/policy implemented (ACP-R6/R7/R8)
- [ ] Research extraction working (CLI-R8)
- [ ] Manuscript strict mode + verify (CLI-R6/R7)
- [ ] Context Assembly enhanced (CTX-R2/R4/R6/R7)
- [ ] Precheck auth match (SYNC-R6)

**Wave 3 Complete:**
- [ ] DEBT-X1/X2/X3 resolved
- [ ] SYNC-R4/R5 resolved
- [ ] Foundation residuals addressed

**Overall V1.1 Ready:**
- [ ] All 27 residuals resolved or waived
- [ ] Contract drift eliminated
- [ ] All v1-spec §10 features implemented
- [ ] CI pipeline clean
- [ ] Ready for V1.1 GA release

---

## Next Steps

1. Execute Wave 1 (Tasks 1-4) sequentially ✅ COMPLETE
2. After Wave 1 complete, plan Wave 2 detailed tasks ✅ COMPLETE
3. Execute Wave 2 in parallel with multiple agents ✅ COMPLETE
4. After Wave 2 complete, plan Wave 3 detailed tasks ✅ COMPLETE
5. Execute Wave 3 sequentially ✅ COMPLETE (Batch A merged, Batch B complete)
6. Final validation and merge to main

---

## Execution Status

### Wave 1: P0 Critical Blockers ✅ COMPLETE (2026-04-08)

**Merged**: `32b8081` (7 commits, 59 files, 4102 insertions)

- [x] Task 1: Fix MembershipRole Enum Drift (DM-R5)
- [x] Task 2: Create delta.schema.json
- [x] Task 3: Create workspace-binding.schema.json
- [x] Task 4: Create agent-profile.schema.json

**QC Result**: APPROVE (0 Critical, 0 Warnings)

### Wave 2: P1 V1.1 Core Features ✅ COMPLETE (2026-04-08)

**Batch 1 Merged**: `4ac8fb6` (10 commits, 23 files, 3086 insertions)
**Batch 2 Merged**: `39793cd` (4 commits, 14 files, 1710 insertions)

**Task Group 2.1: ACP Session & Permission Policy**
- [x] Task 5: Implement ACP session persistence (ACP-R6)
- [x] Task 6: Implement permission policy engine (ACP-R7)
- [x] Task 7: Implement daemon-mediated tool access (ACP-R8)

**Task Group 2.2: Research & Manuscript Operations**
- [x] Task 8: Implement PDF/URL content extraction for Research (CLI-R8)
- [x] Task 9: Add manuscript promote strict mode (CLI-R6)
- [x] Task 10: Add manuscript verify content integrity (CLI-R7)

**Task Group 2.3: Context Assembly Enhancement**
- [x] Task 11: Add WorldId format validation (CTX-R4)
- [x] Task 12: Fix UTF-8 truncation safety (CTX-R5)
- [x] Task 13: Convert MemoryKind to enum (CTX-R6)
- [x] Task 14: Add file size limit to summary generator (CTX-R2)
- [x] Task 15: Add file I/O edge case tests (CTX-R7)

**Task Group 2.4: Multi-Creator Governance**
- [x] Task 16: Implement precheck auth match validation (SYNC-R6)
- [x] Task 17: Honor retry_after from conflict response (SYNC-R11)
- [x] Task 18: Persist partial apply retry state (SYNC-R12)

**QC Result**: APPROVE (0 Critical, 6 Warnings)

### Wave 3: P2 Reliability Hardening ✅ COMPLETE (2026-04-09)

**Batch A Merged**: `1d5877a` (1 commit + merge, 10 files, 1511 insertions)
**Batch B Merged**: `5132ec4` (merged to main)

**Task Group 3.1: SQLite Infrastructure**
- [x] Task 19: Add connection pooling to nexus-sync (DEBT-X1)
- [x] Task 20: Deduplicate SQLite schema (DEBT-X2)
- [x] Task 21: Standardize error propagation (DEBT-X3)

**Task Group 3.2: Sync Contract Hardening**
- [x] Task 22: Add outbox transaction atomicity tests (SYNC-R4)
- [x] Task 23: Add HTTP body size limit to SyncClient (SYNC-R5)

**Task Group 3.3: Foundation Hardening Residuals**
- [x] Task 24: Resolve FH-R1 (schema drift detection CI)
- [x] Task 25: Resolve FH-R3 (error code inconsistency docs)
- [x] Task 26: Resolve FH-R6 (test helper duplication)

**QC Result**: APPROVE (0 Critical, 3 Warnings, 6 Suggestions — unanimous)

---

## QC Residuals (Wave 3 Batch A)

The following residuals were identified during QC三审 for Wave 3 Batch A. These are non-blocking quality improvements tracked for V1.1+.

### Warnings (Should address before V1.1 GA)

| ID | Title | Severity | Scope | Target | Owner |
|----|-------|----------|-------|--------|-------|
| W-1 | Migration documentation placement | Warning | outbox.rs module header vs CHANGELOG.md | Before V1.1 release | @fullstack-dev |
| W-2 | HTTP body size uses Serialization error variant | Warning | sync_client.rs:263-267, 278-284 | V1.1+ | @fullstack-dev |
| W-3 | Pool status monitoring not exposed | Warning | pool.rs:59-61, outbox.rs | V1.1+ | @fullstack-dev |
| W-4 | InvalidParameterName misuse in pool error mapping | Warning | pool.rs:163-171 | Next batch | @fullstack-dev |
| W-5 | Body size enforcement reads full response before check | Warning | sync_client.rs:258-284 (when Content-Length absent) | V1.1+ (security-adjacent) | @fullstack-dev |
| W-6 | Test helper leaks temp directories | Warning | outbox.rs:204-213 (new_in_memory) | CI cleanup policy | @fullstack-dev |
| W-7 | Pool configuration not tunable at runtime | Warning | pool.rs:39-46 | V1.1+ | @fullstack-dev |
| W-8 | Test count discrepancy (7 vs 8 atomicity tests) | Warning | status.json vs outbox.rs | Before merge | @fullstack-dev |

### Suggestions (Optional improvements)

| ID | Title | Scope | Target |
|----|-------|-------|--------|
| S-1 | Add pool timeout configuration | pool.rs:40-45 | V1.1+ |
| S-2 | CI drift detection for schema patterns | Follow-up to DEBT-X2 | V1.1+ |
| S-3 | Test isolation for in-memory databases | outbox.rs:148-160 | Future |
| S-4 | Plan claims 24 error variants; impl has 22 | errors.rs | Doc update |
| S-5 | with_pool WAL initialization documentation | outbox.rs:191-200 | Doc refinement |
| S-6 | Pool exhaustion integration test | pool.rs | V1.2 |
| S-7 | Pool status observability | pool.rs:58-61 | V1.1+ |
| S-8 | Breaking change migration verification | manuscript.rs, research.rs | Before merge |

### Recommended Actions

**High Priority (before V1.1 GA)**:
1. W-1: Add migration notes to CHANGELOG.md or create MIGRATION-v1.1.md
2. W-5: Implement streaming body reader to prevent OOM with chunked responses
3. W-8: Update status.json test count (7 not 8)

**Medium Priority (V1.1+)**:
1. W-2: Add dedicated `SyncError::BodySizeLimitExceeded` variant
2. W-4: Use appropriate rusqlite::Error variant for pool panics
3. W-7: Add runtime pool configuration (env var or builder method)

**Low Priority (accept with documentation)**:
1. W-3, W-6: Nice-to-have for observability and test infrastructure

---

## Progress Summary

**Total Tasks**: 26
**Completed**: 26 (100%)
**Remaining**: 0

**Residuals Resolved**: 23 (all plan tasks)
- Wave 1: DM-R5, W1-C1 (2)
- Wave 2: ACP-R6/R7/R8, CLI-R6/R7/R8, CTX-R2/R4/R5/R6/R7, SYNC-R6/R11/R12 (14)
- Wave 3 Batch A: DEBT-X1/X2/X3, SYNC-R4/R5 (5)
- Wave 3 Batch B: FH-R1/R3/R6 (3)

**QC Residuals Added**: 11 Warnings + 11 Suggestions (Wave 3 Batch A: 8+8, Batch B: 3+6)

**Target**: V1.1 GA ready after Batch B completion