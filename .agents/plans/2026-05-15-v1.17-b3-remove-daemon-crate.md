# V1.17 Batch 3: Remove Old Daemon Crate

## Metadata

- **Plan ID**: `2026-05-15-v1.17-b3-remove-daemon-crate`
- **Status**: Todo
- **Owner**: `@project-manager`
- **Primary Spec**: `.agents/knowledge/specs/daemon-runtime-v1.md` (Section 3.3)
- **Task Category**: `logic` (cleanup + dependency removal)
- **Branch**: `feature/v1.17-b3-remove-daemon-crate`
- **Merge Target**: `main`
- **Blocked by**: `2026-05-15-v1.17-b2-single-binary-wiring`

## Objective

Remove `crates/nexus42d` from workspace members and all references. Clean build/test matrix to ensure single-binary operation.

## Scope

### In Scope

1. Remove `crates/nexus42d` from root `Cargo.toml` workspace members
2. Remove all `nexus42d` dependency references from other crates
3. Delete `crates/nexus42d/` directory
4. Update any documentation or config referencing `nexus42d`
5. Clean `.sqlx/` metadata if paths changed
6. Full workspace build and test verification

### Out of Scope

- Naming/log changes (Batch 4)
- Agent host work

## Tasks

| ID | Task | Depends on | Parallel | Owner (planned) | Done criteria |
|----|------|------------|----------|-----------------|---------------|
| T1 | Audit all references to `nexus42d` in codebase | — | no | @fullstack-dev | Complete reference list |
| T2 | Remove workspace member from root Cargo.toml | T1 | no | @fullstack-dev | Member removed |
| T3 | Remove `nexus42d` dep from all other crates | T1 | yes | @fullstack-dev | No remaining deps |
| T4 | Delete `crates/nexus42d/` directory | T2-T3 | no | @fullstack-dev | Directory removed |
| T5 | Update docs/config referencing `nexus42d` | T4 | no | @fullstack-dev | No stale references |
| T6 | Full workspace verification | T5 | no | @fullstack-dev | `cargo clippy --all && cargo test --all` clean |

## Acceptance Criteria

1. `crates/nexus42d/` no longer exists
2. `nexus42d` not in workspace members
3. No code references to `nexus42d` remain
4. `cargo check --workspace` passes
5. `cargo clippy --all -- -D warnings` passes
6. `cargo test --all` passes

## Sign-off

- [ ] Implementation complete
- [ ] QC tri-review passed
- [ ] QA verification passed
- [ ] status.json updated
