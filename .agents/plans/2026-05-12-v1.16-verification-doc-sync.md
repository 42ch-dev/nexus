# Plan: V1.16 Verification, Documentation, and Tracker Sync

**Plan ID**: `2026-05-12-v1.16-verification-doc-sync`  
**Status**: Todo  
**Working branch**: `feature/v1.16`  
**Theme**: B4 — Compatibility and verification  
**Milestone**: M4  
**Effort**: S  
**Depends on**:

- `2026-05-12-v1.16-system-platform-topology` — Done
- `2026-05-12-v1.16-trace-correlation` — Done
- `2026-05-12-v1.16-creator-identity-auth-compat` — Done

---

## Goal

Close V1.16 by verifying the Delivery Compass Done definition against the integration branch, synchronizing the knowledge/tracker assets, and leaving auditable evidence for PM/QA Done sign-off.

## Scope

This is a docs/verification plan. It should not introduce runtime behavior changes, new command behavior, schema changes, or broad refactors.

## Inputs checked during plan creation

- `.agents/knowledge/v1.16-delivery-compass-v1.md` defines the six minimum Done items.
- `.agents/knowledge/README.md` currently lists V1.16 as `Draft (active planning SSOT)` and still describes earlier tracker state in the current-focus notes.
- `.agents/status.json` currently has no `InReview` entries, but still contains Done V1.16 rows alongside this Todo row; this must be reconciled with the repository's Profile B compaction rule before final Done.
- Contract test inventory: `crates/nexus42/tests/command_surface_contract.rs` contains **23** `#[test]` entries, of which **3** are `#[ignore]` entries; default active contract test count is therefore **20** unless the ignored tests are intentionally included with `--include-ignored`.

## Done definition to verify

From `.agents/knowledge/v1.16-delivery-compass-v1.md` §5, V1.16 is Done only if all six items are verified:

1. `creator use <creator_id_or_handle>` persists a normalized `creator_id`.
2. `creator status/list` stably output `creator_id`, `handle`, and `display_name`.
3. `daemon orchestrate` and `acp` have no responsibility overlap.
4. ACP execution path coverage is 100%, with no bypass path.
5. `creator kb` default `work` is usable and `world` mapping is usable.
6. User-path / Creator-path auth behavior matches `auth-session-model-v1`.

## Tasks

| ID | Task | Depends on | Owner | Done criteria | Status |
|----|------|------------|-------|---------------|--------|
| T1 | Verify all six Delivery Compass Done items against current code and plan artifacts | — | @qa-engineer / @project-manager | Evidence table maps each Done item to implementation files, contract tests, and QC/QA reports; any gap is recorded as Blocked or residual before sign-off | [ ] |
| T2 | Run full test suite on the V1.16 integration branch | T1 | @qa-engineer | `cargo test --workspace --all-targets` passes, plus active contract tests pass; if `--include-ignored` is run, the 3 ignored contract tests are either passing or explicitly documented as intentional future-contract checks | [ ] |
| T3 | Update `.agents/knowledge/README.md` for V1.16 closure | T1, T2 | @project-manager / @architect | Current focus and V1.16 index row reflect delivered V1.16 artifacts, closure status, and any V1.17-gated follow-up; no stale “Draft” or obsolete tracker notes remain | [ ] |
| T4 | Final `status.json` and tracker cleanup | T1, T2, T3 | @project-manager | No stale `InReview` entries; Profile B compaction is respected; V1.16 Done snapshots/catalog entries are complete; residual findings target `v1.17` or backlog with reachable links | [ ] |
| T5 | Record final verification and commit evidence | T2, T3, T4 | @project-manager / @qa-engineer | Plan sign-off records exact commands, outcomes, commit hashes, residual summary, and final `git status --short` cleanliness before Done | [ ] |

## Verification commands

Run from repository root on `feature/v1.16` unless PM explicitly provides a different integration checkout.

```bash
git branch --show-current
git status --short
cargo test -p nexus42 --test command_surface_contract
cargo test -p nexus42 --test command_surface_contract -- --include-ignored
cargo test --workspace --all-targets
```

If full workspace tests expose already-accepted flaky tests, keep the V1.16 closeout evidence precise: record the failing test, matching residual ID if one exists, rerun/narrowing evidence, and whether it blocks the V1.16 Done definition.

## Status and tracker cleanup checklist

- [ ] Confirm `.agents/status.json` contains no `status: InReview` rows.
- [ ] Reconcile any Done rows remaining in `plans[]` with `.agents/AGENTS.md` Profile B rules.
- [ ] Confirm each closed V1.16 plan has an archived snapshot under `.agents/archived/plans/` and a catalog entry in `.agents/archived/plans-done.json`.
- [ ] Confirm open residual findings are intentional, severity/target fields are current, and tracking links are repo-relative and reachable.
- [ ] Update `.agents/knowledge/README.md` so V1.16 is no longer described as only draft planning if the verification evidence passes.

## Non-goals

- Adding new V1.16 runtime features.
- Editing generated contract files or JSON Schemas.
- Closing V1.17-targeted residuals inside V1.16 without a separate plan.
- Updating private `nexus-platform` files from this repository checkout.

## Handoff notes

- Treat this plan as the release-close gate for M4: `status.json` / tracker / knowledge index synchronized, then candidate for Done.
- The final status transition to `Done` remains PM/QA-owned under Morning Star rules.
- Preserve auditability: prefer a compact evidence table in this plan or a dedicated report under `.agents/plans/reports/2026-05-12-v1.16-verification-doc-sync/` over chat-only sign-off.
