# V1.6 WS-B — ACP SDK DTO Decoupling

**Plan ID**: `2026-04-20-v1.6-ws-b-acp-sdk-dto`
**Status**: Todo
**Compass (SSOT)**: [knowledge/v1.6-delivery-compass-v1.md](../knowledge/v1.6-delivery-compass-v1.md) §4 WS-B
**Milestone**: M2 — ACP preparation + creator tooling complete
**Working branch**: `feature/v1.6`
**Dependencies**: none (parallel with WS-A/C/D)

**Goal**: Create Nexus-owned DTO layer for `NexusAcpClient` trait; confine SDK types to `AcpSdkAdapter`.

**Authoritative design**: [acp-client-tech-spec-v2.md](../archived/knowledge/acp-client-tech-spec-v2.md) §1, §11 + [schemas-boundary-v1.md](../archived/knowledge/schemas-boundary-v1.md). DTO types → `crates/nexus-contracts/src/local/acp/`.

**Tasks**:

- T1: Define 16 Nexus-owned DTO types (NexusSessionId, NexusProtocolVersion, NexusInitializeRequest/Response, NexusPromptRequest, NexusPromptCompleted, NexusStopReason, NexusNewSessionRequest, NexusSessionCreated, NexusCancelResult, NexusAgentCapabilities, NexusAgentInfo, NexusAuthMethod, NexusSessionModeState, NexusContentBlock). Use newtype wrappers for simple cases.
- T2: Move `subscribe()` off `NexusAcpClient` trait or wrap return type in `NexusStreamEvent` + `NexusEventStream`. Document chosen approach.
- T3: Update `NexusAcpClient` trait signatures to use Nexus DTOs.
- T4: Update all consumers (orchestration engine, acp-worker, CLI agent run) to use new DTOs.
- T5: Verify consumers compile without `agent-client-protocol` in dependency tree.

**Evidence**:
- `cargo doc --package nexus-acp-host` shows no SDK types in trait signatures.
- `pub trait NexusAcpClient` block contains zero references to `agent_client_protocol` or `acp::`.
- Consumers compile without SDK dependency.
- `cargo test --workspace` green; clippy/fmt clean.

**Effort (agent-oriented)**: L — 2–3 agent sessions.

---

*Created: 2026-04-20.*
