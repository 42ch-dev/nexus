# V1.4 Delivery Compass v1

**Status**: Active (implementation-period SSOT)
**Scope**: V1.4 delivery planning and execution alignment for `nexus` OSS repository — Orchestration Track + `schemas/` boundary refactor
**Predecessor**: [v1.3-delivery-compass-v1.md](v1.3-delivery-compass-v1.md)
**Created**: 2026-04-17
**Wave-0 design inputs** (authoritative for this compass's scope claims):

- [orchestration-engine-v1.md](orchestration-engine-v1.md) — primary design (daemon engine + capability registry + preset bundle + `graph-flow` adoption)
- [daemon-lifecycle-api-v2.md](daemon-lifecycle-api-v2.md) — statig 6-state HSM (closes TD-9)
- [acp-client-tech-spec-v2.md](acp-client-tech-spec-v2.md) — worker-delegated ACP hosting amendment + `nexus-acp-host` crate extraction

## 1) Purpose and SSOT boundary

This document is the **implementation-period SSOT** for V1.4 planning decisions and execution guardrails for the `nexus` OSS repository. It translates the 2026-04-17 orchestration brainstorming outcomes into waves, milestones, acceptance criteria, and cross-cutting policy (including the long-deferred `schemas/` boundary refactor that V1.4 finally absorbs into a program track).

- Authoritative for: V1.4 scope classification, wave decomposition, milestone gates, minimum regression gate, cross-repo dependency boundaries, risk controls.
- Not authoritative for: field-level wire contracts, orchestration engine internals, or daemon lifecycle state semantics — those live in the wave-0 design inputs above.
- Conflict resolution:
  - Plan execution status → `status.json` in `{PLAN_DIR}` wins for machine state.
  - Architecture / contract semantics → v1-spec / ADR source files win.
  - Orchestration engine internals → wave-0 design inputs above win.

## 2) Confirmed decisions (locked)

Outputs of the 2026-04-17 orchestration brainstorming (see answer traces Q1–Q5):

- **Strategy shape**: hierarchical — outer long-task state machine + inner short DAG ("graph-of-graphs").
- **Daemon role**: `nexus42d` becomes **orchestration engine + capability registry**. Existing HTTP-era capabilities (sync, outbox, workspace ops, registry refresh) are reclassified as first-class registered capabilities; HTTP surface retreats to a trigger/query layer over the same engine.
- **ACP host locus**: Extract `crates/nexus-acp-host` (worker-only linkage); `nexus42d` never links `agent-client-protocol`; orchestration-driven ACP sessions run inside **per-creator long-lived `nexus42 acp-worker` subprocess**; interactive `nexus42 agent run` unchanged.
- **Daemon ↔ worker IPC**: parent↔child stdin/stdout JSON-RPC 2.0 (owned-pipe graceful shutdown).
- **Transition mechanism**: rule-first + LLM-judge fallback + user manual override (`nexus42 schedule advance`).
- **Preset physical form**: filesystem folder bundle (YAML manifest + Markdown prompt templates + optional local schemas).
- **Orchestration runtime library**: `graph-flow = "=0.2.3"` (rs-graph-llm) — both outer SM and inner DAG, with custom SQLite `SessionStorage`, behind an adapter trait; `rig` feature **not** enabled.
- **Daemon lifecycle library**: `statig = "0.3"` — 6-state HSM closing TD-9.
- **Libraries are orthogonal**: statig for compile-time daemon lifecycle; graph-flow for dynamic preset execution; **no overlap, no dep cycle**.
- **`schemas/` boundary refactor is V1.4 scope** (was "parallel small plan" C-track; now formally folded in as WS5 — see §4 below).
- **B-track (Schedules + Plan + stable core-context)** is **deferred** out of V1.4 to a follow-up release; V1.4 ships the engine + first-preset plumbing with `_system.maintenance` + `novel-writing` end-to-end, but not the multi-Schedule queueing / priority / seed-prompt-versioning layer.
- **Planning strategy**: waves ordered by dependency graph; WS5 (schemas refactor) parallelisable with WS2 (orchestration skeleton); WS4 (statig lifecycle) parallelisable with WS2.
- **Branch strategy**: `feature/v1.4-orchestration` from `main` as plan integration branch; per-wave topic branches off the integration branch when ≥2 concurrent writers.

## 3) Version reclassification compass (V1.4 in-tree)

This compass covers V1.4 only. V1.2 → [v1.2-delivery-compass-v1.md](v1.2-delivery-compass-v1.md); V1.3 → [v1.3-delivery-compass-v1.md](v1.3-delivery-compass-v1.md); cross-version mapping matrix → [v1.2-reclassification-matrix-v1.md](v1.2-reclassification-matrix-v1.md) (includes V1.4 column).

### V1.4 (Orchestration + schemas boundary)

In (this compass):

- **Orchestration foundation** — daemon engine + capability registry + worker model + preset bundle + first user-facing preset end-to-end.
- **TD-9 closure** — `statig` 6-state daemon lifecycle HSM + v2 `GET /v1/local/daemon/status` superset response.
- **`nexus-acp-host` crate extraction** — ACP client logic moves out of `crates/nexus42` into its own crate (worker-only linkage enforced by CI).
- **`schemas/` boundary refactor** — audit every `schemas/**/*.schema.json` file; keep only cross-process wire contracts in `schemas/` (subject to codegen + `verify-codegen` gate); move local-only domain representations into a Rust crate.
- **Rescoped ACP residuals** — ACP-R6 (session persistence), ACP-R7 plumbing (permission policy I/O path), ACP-R8 (daemon-mediated tool access, re-scoped), ACP-R11 (session modes) — status changes tracked in [acp-client-tech-spec-v2.md](acp-client-tech-spec-v2.md) Appendix B.
- **V1.3 backlog** — `V1.3 Creator Register — E2E Integration Tests (T6)` (currently the sole `Todo` in `status.json`) — executed in V1.4 as a non-orchestration lane (platform M10 dependency).

Out (deferred):

- **B-track** (creator Schedule concurrency, priority, preemption, `nexus42 schedule add/update/remove` full semantics, seed-prompt → stable core-context derivation and versioning) — see OQ-1…OQ-8 in [orchestration-engine-v1.md](orchestration-engine-v1.md) §11.
- **User-authored capabilities** (shell / WASM plugin ABI) — V1.5+.
- **Preset third-party registry / signing / publish** — V1.5+.
- **Multi-agent worker** (single worker hosting >1 ACP agent) — V1.5+.
- **ACP SDK migration** to `sacp` v1.0 — governed by adapter-trait policy; no forced move in V1.4.
- **Full ACP permission policy engine UI** — only plumbing in V1.4; UI + decision engine in V1.5.
- **Preset hot-reload with in-flight session migration** — V1.4 uses snapshot semantics (running sessions keep their preset snapshot; new sessions pick up the new preset).

Backlog (discovered during V1.4 brainstorming, park for later):

- System-managed multi-preset scheduler for `_system.maintenance` family (if V1.4 system Schedule grows beyond the embedded single preset).
- Capability schema registry sharing with platform (cross-repo capability catalog sync) — evaluate after V1.4 ships.

## 4) V1.4 workstreams (execution decomposition)

Mapped 1:1 to [orchestration-engine-v1.md](orchestration-engine-v1.md) §10 phases; WS5 is the compass's addition.

### WS1 — `nexus-acp-host` crate extraction (orchestration Phase 1)

- **Goal**: Extract ACP client logic out of `crates/nexus42/src/acp/` into new crate `crates/nexus-acp-host`; `nexus42d` crate never links it (CI-enforced linkage gate); hidden `nexus42 acp-worker` subcommand stub lands.
- **Authoritative design**: [acp-client-tech-spec-v2.md](acp-client-tech-spec-v2.md) §11.
- **Deliverables**: new crate scaffold; migrated source files; updated `Cargo.toml`s; re-exports where needed; CI `verify-no-acp-in-daemon` gate; hidden `acp-worker` subcommand returning initialize OK.
- **Evidence**: `cargo build --workspace` clean; `cargo test --workspace` green (existing ACP tests move with crate); interactive `nexus42 agent list/show/probe/run` outputs unchanged vs pre-migration; linkage gate CI job passes; `acp-worker --creator <id>` runs and exits cleanly.

### WS2 — Orchestration skeleton (orchestration Phase 2)

- **Goal**: Introduce the runtime plumbing of the engine + capability registry + Worker Manager + daemon↔worker IPC; register non-ACP built-in capabilities; land `_system.maintenance` hardcoded graph (not yet via file loader); add orchestration HTTP endpoints.
- **Authoritative design**: [orchestration-engine-v1.md](orchestration-engine-v1.md) §§3–6, §10.3.
- **Deliverables**: new crate `crates/nexus-orchestration`; `OrchestrationEngine` trait + `GraphFlowEngine` impl; `SqliteSessionStorage` + `orchestration_sessions` migration under `nexus-local-db`; `Capability` trait + registered built-ins (`sync.*`, `outbox.*`, `workspace.*`, `registry.refresh`, `creator.*` non-ACP); Worker Manager lifecycle; `worker/initialize`, `worker/health`, `worker/shutdown` IPC verbs; `/v1/local/orchestration/*` HTTP endpoints.
- **Evidence**: engine can create/step/pause/resume/cancel a hardcoded 3-state test graph; roundtrip test (start → kill daemon → restart → resume) passes; Worker Manager integration test with dummy worker script; `GET /v1/local/orchestration/sessions` returns `_system.maintenance` session when engine is running; clippy/fmt clean.

### WS3 — Preset loader + first user preset end-to-end (orchestration Phase 3)

- **Goal**: Enable filesystem preset bundles; register ACP-related capabilities (`acp.*`, `judge.llm`, `judge.rule`); implement `AcpPromptTask` dispatching through Worker Manager; ship embedded `novel-writing` preset as E2E demonstrator; add user-facing `nexus42 schedule start/status/advance` CLI (minimal, not B-track full).
- **Authoritative design**: [orchestration-engine-v1.md](orchestration-engine-v1.md) §§7–9, §10.4.
- **Deliverables**: `load_preset()` with §7.6 validation; bundled preset `embedded-presets/novel-writing/`; `AcpPromptTask` + `JudgeTask` + `InnerGraphTask` + `ManualWaitTask` implementations; CLI commands `schedule start` / `schedule status` / `schedule advance`; at least one tool-policy enforcement path (`auto_grant_read_only`).
- **Evidence**: `nexus42 schedule start novel-writing --creator <id>` returns a session id; mocked-agent E2E test advances through all 4 outer states and the `brainstorm_graph` inner DAG; daemon restart mid-`brainstorming` resumes from last completed inner-graph node; `nexus42 schedule advance` progresses from `outlining` → `drafting`.

### WS4 — Daemon lifecycle HSM (orchestration Phase 4) — **parallel with WS2**

- **Goal**: Replace the v1 "running-probe" shim with the full 6-state `statig` HSM, closing TD-9.
- **Authoritative design**: [daemon-lifecycle-api-v2.md](daemon-lifecycle-api-v2.md).
- **Deliverables**: new `crates/nexus42d/src/lifecycle/` module; `Lifecycle` trait + `StatigLifecycle` impl; entry/exit actions; SIGINT/SIGTERM integration; panic-hook bridge; v2 `GET /v1/local/daemon/status` (with `schema_version: 2`, `subsystems{}`, `degraded{}`); new `schemas/acp-runtime/daemon-status-v2.schema.json` (wire) + regenerated contracts.
- **Evidence**: integration test — cold start → `Running` within threshold; SIGTERM → `Stopping` → exit 0 within grace; induced subsystem failure → `Degraded` with populated fields; `FatalError` → non-zero exit; backward-compat test: v1 clients still get `lifecycle_state: "running"` on healthy daemon.

### WS5 — `schemas/` boundary refactor (**parallel with WS2; no dependency on WS1/WS3/WS4**)

- **Goal**: Enforce the rule that `schemas/` only hosts **cross-process wire contracts**; relocate local-only domain representations into a Rust crate so `pnpm run codegen` stops emitting TypeScript shapes nobody on the platform side consumes.
- **Motivation**: current `schemas/domain/**/*.schema.json` (20 files) contains a mix of wire-transmitted types (used in sync bundles / platform responses) and local-only types (only ever seen inside `nexus42` / `nexus42d`). The mix creates codegen noise in `@42ch/nexus-contracts` that platform consumers do not need, and obscures the "what is wire, what is local" invariant for future contributors.
- **Authoritative policy**:
  - **Wire criterion**: a schema is **wire** iff at least one of the following is true — (a) it appears (directly or via `$ref`) inside a transmitted bundle (`schemas/cli-sync/bundle.schema.json`, delta payloads, outbox envelopes), (b) it is consumed by an HTTP request/response pair that `nexus-platform` reads or writes, (c) it is consumed by an external public service (e.g. ACP registry on CDN), (d) it is the `daemon-status-v2` contract (new in WS4).
  - All other schemas are **local**.
  - **Local-only placement**: new home is `crates/nexus-domain/src/types/local/` (extend the existing `nexus-domain` crate; no new crate required); types are hand-coded in Rust with `serde` derives (no JSON Schema, no codegen). If test-time validation of persisted shapes is desired, add `schemars` derives later.
- **Deliverables**:
  - **Audit table** in this repo (committed under `.agents/plans/knowledge/schemas-boundary-audit-v1.md` — new knowledge doc produced by the WS5 plan) classifying every file under `schemas/**/*.schema.json` as `wire` or `local` with one-line justification and first-producer / first-consumer pointers.
  - **Moves for `local` classified schemas** — delete the JSON Schema file, add the hand-coded Rust type under `crates/nexus-domain/src/types/local/<name>.rs`, migrate any existing usages.
  - **Codegen exclusions updated** — `tooling/codegen/` dropped the removed schemas automatically (it iterates the dir); regenerate `packages/nexus-contracts/src/generated/*.ts` and `crates/nexus-contracts/src/generated/*.rs` + confirm `verify-codegen` clean.
  - **Npm package bump** — `@42ch/nexus-contracts` **minor** version bump (public TS surface shrinks; non-breaking if platform never used those types, but safer to signal via minor); release notes in package README.
  - **Platform coordination note** — one-page handoff confirming no `nexus-platform` code imported the removed TS types (run `rg` on `nexus-platform` before landing; pre-merge checklist per AGENTS.md root §"Pre-merge checklist").
- **Evidence**:
  - `cargo test --workspace` and `cargo clippy --all -- -D warnings` clean.
  - `pnpm run codegen` produces no diff post-regeneration; `pnpm run validate-schemas` passes.
  - `@42ch/nexus-contracts` public TypeScript export list diffs cleanly (only deletions expected).
  - Audit doc `schemas-boundary-audit-v1.md` covers 100% of `schemas/**/*.schema.json` files.
  - Platform-side `rg` on moved type names returns no active imports.

### WS6 — V1.3 backlog closure (non-orchestration lane)

- **Goal**: Close the single remaining V1.3 backlog item (`V1.3 Creator Register — E2E Integration Tests (T6)` per `.agents/plans/status.json`) opportunistically in V1.4.
- **Dependency**: platform M10 must be complete (creator register + verify API endpoints reachable in integration-test environment).
- **Deliverables**: integration tests in `crates/nexus42/tests/` with `wiremock` or platform test harness; covers token lookup → platform register → challenge solve → verify → credential store.
- **Evidence**: tests pass against mocked platform; residual row removed from `status.json.plans[]`.

## 5) Milestone gates (M1/M2/M3/M4)

### M1 — Foundation lands (`nexus-acp-host` + TD-9 closure)

- **Entry**: WS1 and WS4 design frozen (both landed in this compass's wave-0 inputs); scope confirmed.
- **Exit**: WS1 merged to `feature/v1.4-orchestration`; WS4 merged (parallel, independent); TD-9 row in [architecture-alignment-review-v1.md](architecture-alignment-review-v1.md) moves from "Partial" to "Resolved (v2)"; CI linkage gate (no ACP SDK in daemon) green.

### M2 — Engine skeleton + schemas refactor complete

- **Entry**: M1 done.
- **Exit**: WS2 merged (orchestration engine handles hardcoded graph end-to-end; persistence works across restart; `_system.maintenance` runs); WS5 merged (schemas audit doc committed; local types moved to `nexus-domain`; codegen + verify-codegen clean; npm minor bump released); daemon and engine interoperate under WS4 lifecycle.

### M3 — First user preset end-to-end

- **Entry**: M2 done.
- **Exit**: WS3 merged; `novel-writing` preset demonstrably runs through all states (automated E2E test against mock agent); CLI `schedule start/status/advance` usable; daemon restart mid-execution resumes correctly.

### M4 — V1.4 release gate

- **Entry**: M3 done.
- **Exit**: Minimum regression gate (§6) passes; WS6 V1.3 backlog closed or explicitly deferred with rationale; all open residuals triaged; `roadmap.md` in `nexus-platform` updated to reflect V1.4 completion per AGENTS.md root "Pre-merge checklist".

## 6) Minimum automated regression set (V1.4 hard gate)

| ID | Scope | Evidence |
|---|---|---|
| **R1** | `nexus42d` cold start → `Running` within threshold; HTTP `GET /v1/local/daemon/status` returns `lifecycle_state: "running"` with `schema_version: 2` | `crates/nexus42d/tests/lifecycle.rs` |
| **R2** | Graceful shutdown: SIGTERM → `Stopping` → exit 0 within grace period | same |
| **R3** | Subsystem failure → `Degraded` visible on status endpoint; recovery → `Running` | same |
| **R4** | Orchestration engine create → step → pause → restart daemon → resume → complete (durability across restart) | `crates/nexus-orchestration/tests/durability.rs` |
| **R5** | `novel-writing` preset end-to-end with mock ACP agent: gathering → brainstorming (inner graph) → outlining (manual advance) → drafting → done | `crates/nexus-orchestration/tests/e2e_novel_writing.rs` |
| **R6** | Interactive `nexus42 agent run` unchanged: list/show/probe/run with mock agent succeed (regression guard for WS1 crate extraction) | `crates/nexus42/tests/acp_smoke.rs` |
| **R7** | `schemas/` audit: every file under `schemas/**/*.schema.json` has an entry in `schemas-boundary-audit-v1.md` (CI check: count parity) | `.github/workflows/ci.yml` audit-coverage job |
| **R8** | `pnpm run codegen` + `verify-codegen` produce no diff post-WS5 (confirms moved schemas are cleanly removed from codegen output) | existing `verify-codegen` job |
| **R9** | CI linkage gate: `nexus42d/Cargo.toml` does NOT list `nexus-acp-host` or `agent-client-protocol` | `.github/workflows/ci.yml` `verify-no-acp-in-daemon` job |
| **R10** | Worker crash detection: killing worker PID marks corresponding Session `paused` with reason `worker_crash` | `crates/nexus-orchestration/tests/crash_recovery.rs` |

## 7) Cross-repo dependency compass

### Primary execution repo (current): `nexus` OSS

Core V1.4 implementation lands here. All WS1–WS5 deliverables are in-repo.

### Adjacent repo: `nexus-platform`

- **WS3 dependency (mock-only)**: orchestration E2E tests use mocked platform; no live platform dependency for V1.4 release gate.
- **WS5 coordination (read-only)**: before merging WS5, `rg` on `nexus-platform` for any imports of the TypeScript types we intend to remove; confirmation commit message in platform repo is not required but recommended as a cross-repo pre-merge check.
- **WS6 dependency**: V1.3 Creator Register E2E tests need platform M10 API endpoints reachable against a test environment (already available per V1.3 closure); if platform M10 has regressed, WS6 is deferred with a plan note.
- **Post-V1.4 coordination**: `@42ch/nexus-contracts` npm minor bump (WS5) — semver-respecting; platform can upgrade opportunistically, no coordinated breaking change.

### External public services

- **ACP Registry CDN** (`https://cdn.agentclientprotocol.com/...`) — unchanged consumer pattern.
- **External ACP agents** (npm / binary) — driven by WS3 via the registry; no schema evolution for V1.4.

### Dependency rule

- Any WS3 capability that would require live platform endpoints to reach Done **must** have a mock test path declared; live-platform coverage belongs in a post-V1.4 integration track.

## 8) Risk compass

### RISK-01 — Orchestration scope creep into B-track

- **Symptom**: tasks start depending on multi-Schedule concurrency / priority semantics that are explicitly deferred.
- **Control**: enforce §4 In/Out matrix at wave entry; reference OQ-1…OQ-8 in [orchestration-engine-v1.md](orchestration-engine-v1.md) §11 as "explicit B-track questions, not V1.4 gates".

### RISK-02 — `graph-flow` 0.2.x crate drift during V1.4 build

- **Symptom**: upstream releases a 0.3.x with API changes mid-implementation.
- **Control**: pin `=0.2.3` in `Cargo.toml`; `OrchestrationEngine` trait isolates callers; if upstream breaks, upgrade in a **separate plan** — V1.4 does not chase version churn.

### RISK-03 — LocalSet contagion into `nexus42d` runtime (regression)

- **Symptom**: some refactor accidentally links `agent-client-protocol` from `nexus42d` or a shared downstream crate, re-introducing `!Send` contagion.
- **Control**: CI `verify-no-acp-in-daemon` job (R9) runs on every PR; failing this gate blocks merge.

### RISK-04 — WS5 audit incompleteness

- **Symptom**: some `schemas/domain/*.schema.json` gets mis-classified as local and removed from `schemas/`, breaking platform.
- **Control**: WS5 publishes audit table for per-file review **before** any file move; `rg` on `nexus-platform` for each removal candidate; npm bump reviewed by at least one reviewer familiar with platform consumers; post-merge, watch `nexus-platform` CI for green status on the next contracts upgrade.

### RISK-05 — Worker IPC protocol instability

- **Symptom**: first-release IPC verbs undergo churn as WS3 integration surfaces edge cases; future presets / external contributors depend on an unstable shape.
- **Control**: document IPC catalogue in [orchestration-engine-v1.md](orchestration-engine-v1.md) §6.4 as v1 explicitly; breaking changes bump a minor in the daemon↔worker protocol version handshake declared on `worker/initialize` reply; no external-author exposure in V1.4.

### RISK-06 — V1.4 wall-clock drag if WS5 audit uncovers deep wire/local entanglement

- **Symptom**: some "obvious" local-only domain type turns out to be transmitted in an obscure bundle path, forcing redesign instead of simple move.
- **Control**: WS5 first task is **audit only** (no file moves); if the audit reveals >30% of `schemas/domain/*` needs to stay wire, WS5 is **rescoped mid-flight** to close only the clearly-local subset, with the remainder moved to a V1.5 successor plan.

### RISK-07 — TD-9 parity with cli-spec §10.1 drifts during implementation

- **Symptom**: implementer abbreviates the state graph or skips `Degraded` semantics for expediency.
- **Control**: [daemon-lifecycle-api-v2.md](daemon-lifecycle-api-v2.md) is wave-0 spec; QC triple-review must cite §2–§6 of that doc when validating WS4 PR.

### RISK-08 — Preset bundle format churn after first external author

- **Symptom**: once external authors try the YAML schema, corner cases (Handlebars edge escapes, variable scoping) force breaking bundle format changes.
- **Control**: V1.4 ships **only embedded presets** (`_system.maintenance`, `novel-writing`); no third-party distribution story; format is `preset.version: 1` — format `v2` can be introduced non-breakingly because loaders can dispatch by `version` key.

## 9) Plan landing strategy (`{PLAN_DIR}` and `knowledge/`)

- This document location: `.agents/plans/knowledge/v1.4-delivery-compass-v1.md`.
- Role: implementation-period SSOT for V1.4 planning semantics.
- Required companion updates during execution:
  - `{PLAN_DIR}/status.json` for machine state (new plan row(s) created per wave).
  - Related main plan `.md` file(s) under `{PLAN_DIR}/` for task-level progress (likely one per WS to keep QC triple-reviews scoped; or one umbrella plan if waves are short — decide when WS1 plan is authored).
  - Residual findings in `metadata.residual_findings` (open only).
  - `metadata.tech_debt_summary` refreshed when WS4 closes (TD-9 count drops).
- Update rule: if any confirmed decision in §2 changes, update this document and the corresponding section in [orchestration-engine-v1.md](orchestration-engine-v1.md); both must stay aligned.

## 10) Spec reference index (using `{v1-spec}` variable paths)

Internal (this repo):

- Orchestration engine: [orchestration-engine-v1.md](orchestration-engine-v1.md)
- Daemon lifecycle HSM: [daemon-lifecycle-api-v2.md](daemon-lifecycle-api-v2.md)
- ACP client spec v2: [acp-client-tech-spec-v2.md](acp-client-tech-spec-v2.md)
- Context assembly (unchanged in V1.4): [restructured-context-assembly-v1.md](restructured-context-assembly-v1.md)
- Local DB ownership (extended in WS2): [local-db-refactor-v1.md](local-db-refactor-v1.md)
- TD resolution matrix: [architecture-alignment-review-v1.md](architecture-alignment-review-v1.md) (TD-9 row updated at WS4 merge)

External (v1-spec, resolved via `.agents/local-paths.json` per AGENTS.md root):

- `{v1-spec}/architecture/v1.md` — base architecture; §2.3 informs WS1 amendment scope
- `{v1-spec}/cli-sync/cli-spec-v1.md` §10.1 — daemon lifecycle ground truth (WS4 implements this)
- `{v1-spec}/acp-runtime/acp-client-tech-spec-v1.md` — external-repo mirror of the OSS v1 (now archived); WS1 amendment needs the external spec to move in lockstep (coordination item)

Public external:

- `graph-flow` (rs-graph-llm) v0.2.3 — https://github.com/a-agmon/rs-graph-llm
- `statig` v0.3 — https://github.com/mdeloof/statig
- ACP protocol — https://agentclientprotocol.com/

## 11) Change control

When execution starts, each PM status update touching V1.4 should state whether this compass remains valid:

- "Compass valid: yes / no"
- If no, list changed sections and linked plan/status updates.

Common expected updates during V1.4 execution:

- M1 exit → append entry to `notes.json` (program timeline) confirming WS1/WS4 landed; update `architecture-alignment-review-v1.md` TD-9 row.
- M2 exit → same for WS2/WS5; add "schemas boundary audit" knowledge doc to the index; bump `@42ch/nexus-contracts` in `packages/nexus-contracts/package.json`.
- M3 exit → update `status.json` with the first user preset end-to-end evidence; Residual findings created for any known post-V1.4 polish items.
- M4 exit → mark V1.4 plan row(s) as `Done`; update `roadmap.md` in `nexus-platform` per AGENTS.md root "Pre-merge checklist"; archive this compass as `Superseded` once V1.5 compass is authored.
