# V1.28 Agent Host ACP Correctness — Locked Plan

**Plan ID**: `2026-05-25-v1.28-agent-host-acp-correctness`  
**Title**: V1.28 Agent Host Batch 1 — ACP permissions, capabilities, shutdown, admission, cross-platform probe  
**Created**: 2026-05-25  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`, `@qc-specialist`, `@qa-engineer`  
**Task category**: `logic`  
**Working branch**: `feature/v1.28-agent-host-acp-correctness`  
**Merge target**: `feature/v1.28-context-and-agent-host` (iteration integration branch; see compass §Branch policy)  
**Depends on**: none (parallel with context plan; **merge before** native-multiturn plan)  
**Primary spec**: [v1.28-context-and-agent-host-delivery-compass-v1.md](../iterations/v1.28-context-and-agent-host-delivery-compass-v1.md)  
**Spec refs**:

- [acp-client-tech-spec.md](../knowledge/specs/acp-client-tech-spec.md)
- [agent-host.md](../knowledge/specs/agent-host.md)
- [`crates/nexus-agent-host/src/providers/acp.rs`](../../crates/nexus-agent-host/src/providers/acp.rs)
- [`crates/nexus-agent-host/src/capability/model.rs`](../../crates/nexus-agent-host/src/capability/model.rs)
- [deferred-features-cross-version-tracker.md](../knowledge/deferred-features-cross-version-tracker.md) DF-19, DF-20, DF-24, DF-25, DF-26

---

## 1. Problem Statement

V1.18 deferred Agent Host hardening to a V1.19 batch that was never delivered as an iteration. Batch 1 items remain **Open** and block reliable local ACP sessions (permission hangs, orphan processes, admission not enforced, capability claims vs implementation mismatch, Unix-only probe).

V1.28 treats Agent Host as local product **P0–P1** (grill PD-04).

---

## 2. Goals

| Tracker ID | Deliverable | Acceptance |
| --- | --- | --- |
| **DF-19** | Handle `session/request_permission` in ACP provider path | Session no longer hangs/timeouts on permission RPC; test or documented manual repro fixed |
| **DF-20** | Align `set_model` / `set_mode` capability claims with `AcpProvider` behavior | Negotiation + descriptor honest; or implement handlers |
| **DF-24** | `HostManager::shutdown` invokes `ProviderAdapter::shutdown` | No orphan provider processes after daemon stop; unit/integration test |
| **DF-25** | Wire `AdmissionPolicy` into `create_session` and `exec` | Rejected sessions when policy denies; tests for allow/deny |
| **DF-26** | Cross-platform command probe (replace Unix-only `which`) | Works on macOS + Linux CI; Windows behavior documented if partial |

Move each row to **Shipped** in deferred tracker §4 when merged.

---

## 3. Non-goals

- **DF-18** native multi-turn (separate plan).
- **DF-21–28** Batch 2 (timeouts enforcement, risk classifier, streaming, API validation, path traversal).
- Orchestration `acp_prompt` / `acp_session_load` stubs (DF-35, DF-36).
- `nexus42 acp-worker` agent slot full connection (DF-41).

---

## 4. Gates

| Gate | Status | Evidence |
| --- | --- | --- |
| `specify` | pass | V1.28 compass + PD-04 |
| `clarify` | pass | Batch 1 only |
| `plan` | pass | This file |

---

## 5. Batch AH1 — DF-19 Permission (`@fullstack-dev`)

| ID | Task | Acceptance | Verify |
| --- | --- | --- | --- |
| AH1.1 | Implement permission request handler in ACP provider | Returns structured deny/allow/default | Unit test |
| AH1.2 | Timeout or cancel path documented | No infinite wait | Review + test |

---

## 6. Batch AH2 — DF-20 Capabilities (`@fullstack-dev`)

| ID | Task | Acceptance | Verify |
| --- | --- | --- | --- |
| AH2.1 | Audit `CapabilityDescriptor::acp_full()` vs provider | Claims match implementation | Test negotiation |
| AH2.2 | Fix or remove false `set_model` claim | Consistent with provider | `cargo test -p nexus-agent-host` |

---

## 7. Batch AH3 — DF-24 Shutdown (`@fullstack-dev`)

| ID | Task | Acceptance | Verify |
| --- | --- | --- | --- |
| AH3.1 | `HostManager::shutdown` calls provider shutdown | All active sessions cleaned | Integration test |
| AH3.2 | Remove or update stale `lifecycle/actions.rs` stub comments if touched | No misleading "stub" on shipped paths | `rg stub lifecycle` |

---

## 8. Batch AH4 — DF-25 Admission (`@fullstack-dev`)

| ID | Task | Acceptance | Verify |
| --- | --- | --- | --- |
| AH4.1 | Invoke admission policy on session create | Deny when over limit | Unit test |
| AH4.2 | Invoke on `exec` | Consistent policy | Unit test |

---

## 9. Batch AH5 — DF-26 Probe (`@fullstack-dev`)

| ID | Task | Acceptance | Verify |
| --- | --- | --- | --- |
| AH5.1 | Replace Unix-only `which` with cross-platform resolution | CI green on Linux/macOS | CI |
| AH5.2 | Document Windows limitation in agent-host.md if needed | Spec note | Review |

---

## 10. Verification

```bash
cd nexus
cargo test -p nexus-agent-host
cargo test -p nexus-daemon-runtime  # if lifecycle touched
rg 'DF-19|request_permission' crates/nexus-agent-host
```

---

## 11. QC / QA

- QC tri-review: security (admission, permission defaults), process cleanup on shutdown, no scope creep into orchestration.
- QA: smoke daemon start → agent-host session → clean shutdown (manual checklist in QA report).
