# V1.15 Orchestration-First Pipeline Execution Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. PM may later dispatch independent implementation tracks; this plan is the paper baseline and does not itself authorize subagent dispatch.

| Field | Value |
|---|---|
| `plan_id` | `2026-05-10-v1.15-orchestration-first-pipeline` |
| `status` | `Todo` |
| `created_at` | `2026-05-10T13:58:12Z` |
| `primary_spec` | `.agents/knowledge/v1.15-delivery-compass-v1.md` |
| `working_branch` | `feature/v1.15` |
| `task_category` | `docs` for this plan; implementation tasks are `logic` / `deep` by workstream |

**Goal:** Deliver V1.15 for the public `nexus` repository by moving writing/research/publish workflows from legacy CLI verbs to orchestration-first presets, embedded skills, ACP session bootstrapping, and the `novel-writing` workspace-to-platform sync story.

**Architecture:** Batch the release as B1 → B2 → B3. B1 establishes embedded skills and ACP session injection, B2 removes obsolete command groups after preset entrypoints exist, and B3 completes novel-writing filesystem policy plus sync contract/tests.

**Tech Stack:** Rust workspace crates (`nexus42`, `nexus42d`, `nexus-orchestration`, `nexus-acp-host`, `nexus-home-layout`, `nexus-local-db`, `nexus-contracts`), JSON Schema wire contracts as SSOT, cargo fmt/clippy/test, GitNexus impact/change analysis.

---

## Themes and Workstreams

### O1 — Embedded skills + preset/session integration

| Workstream | Focus | Primary scope | Priority |
|---|---|---|---|
| O1-A | Embedded skills authoring and packaging | `crates/nexus-orchestration/embedded-skills/` | P0 |
| O1-B | Install/upgrade sync to `$HOME/.nexus42/skills/` | `crates/nexus42`, `crates/nexus-home-layout` | P0 |
| O1-C | Preset schema `recommended_skills`; remove `recommended_models` | preset YAML/schema/loader paths | P0 |
| O1-D | ACP new-session skill injection and project-readable symlink/copy | `crates/nexus-acp-host`, orchestration bootstrap | P0 |

### O2 — Command surface convergence

| Workstream | Focus | Primary scope | Priority |
|---|---|---|---|
| O2-A | Remove `research`, `manuscript`, and `publish` CLI modules | `crates/nexus42` | P0 |
| O2-B | Preset/pipeline entrypoints and help text replace former workflows | CLI UX + orchestration entrypoints | P0 |
| O2-C | Update unit/e2e fixtures and help assertions | `crates/nexus42`, `crates/nexus-orchestration/tests` | P0 |

### O3 — `novel-writing` layout + sync module

| Workstream | Focus | Primary scope | Priority |
|---|---|---|---|
| O3-A | `Stories/<StoryRef>/<chapter-id>.md` policy | embedded `novel-writing` preset | P0 |
| O3-B | World/story mapping in local DB or creator config | `crates/nexus-local-db`, config/schema docs | P0 |
| O3-C | Sync module contract from workspace artifacts to platform publish/sync bundles | preset module + tests | P1 |

---

## Batching Strategy

| Batch | Covers | Dependency rule | Exit condition |
|---|---|---|---|
| **B1** | O1-A, O1-B, O1-C, O1-D | Starts first; all later batches depend on preset skills being locatable and ACP-injectable. | Embedded skills install/upgrade sync works; `recommended_skills` round-trips into ACP new-session context. |
| **B2** | O2-A, O2-B, O2-C | Starts after B1 exposes replacement preset entrypoints, so removed CLI groups do not leave dead-end workflows. | Removed CLI groups are absent from help/dispatch; replacement entrypoints and tests cover former workflows. |
| **B3** | O3-A, O3-B, O3-C | Starts after B1 skill/session path is available; may overlap with late B2 tests only if PM provides worktree isolation and non-overlapping files. | `novel-writing` produces `Stories/<StoryRef>/...`; sync module contract and tests are review-ready. |

**Parallelism note:** Within B1, O1-A and O1-B can be prepared in parallel only after PM defines worktree/topic-branch boundaries; O1-C and O1-D depend on the concrete skill location contract. B2 and B3 are planned as separate batches for review clarity, with full QC triple-review only after all B1–B3 implementation for this `plan_id` reaches `InReview`.

---

## File Structure and Responsibility Lock

- `crates/nexus-orchestration/embedded-skills/` — repository-versioned embedded skill payloads for preset bootstrapping.
- `crates/nexus-orchestration/` — preset loader, embedded preset behavior, and orchestration pipeline entrypoints.
- `crates/nexus42/` — CLI install/upgrade sync path, command removal, help text, and user-visible replacement entrypoints.
- `crates/nexus-acp-host/` — ACP client adapter and new-session skill injection boundary.
- `crates/nexus-home-layout/` — `$HOME/.nexus42/` path layout, including user-local skills destination.
- `crates/nexus-local-db/` — world/story mapping persistence when needed for `novel-writing`.
- `crates/nexus-contracts/` and `schemas/` — only touched if implementation proves a wire-contract change is unavoidable; default is no breaking wire change in this plan.
- `.agents/knowledge/v1.15-delivery-compass-v1.md` — authoritative scope input for this plan.
- `.agents/status.json` — machine state for this plan and later execution status.

---

## Tasks

| ID | Workstream | Batch | Description | Depends on | Parallel | Planned owner | Done criteria |
|---|---|---|---|---|---|---|---|
| T1 | O1-A | B1 | Author repository-versioned embedded skills under `crates/nexus-orchestration/embedded-skills/` with clear manifest/version boundaries. | — | yes, with T2 after PM worktree assignment | `fullstack-dev` | Embedded skill files are package-visible, documented, and covered by loader/package tests. |
| T2 | O1-B | B1 | Implement idempotent install/upgrade sync from embedded skills to `$HOME/.nexus42/skills/`, including conflict reporting. | T1 interface contract | yes, with T1 only after contract lock | `fullstack-dev-2` | Sync copies expected skills, preserves/flags user conflicts via hash or `.new` policy, and has focused tests. |
| T3 | O1-C | B1 | Replace active preset `recommended_models` path with validated `recommended_skills`. | T1 | no | `fullstack-dev` | Preset schema/loader accepts `recommended_skills`, rejects unreadable paths, and no active preset path depends on `recommended_models`. |
| T4 | O1-D | B1 | Inject recommended skills into ACP new-session context and ensure home-only skills become project-readable via symlink or fallback copy. | T2, T3 | no | `fullstack-dev` | ACP session bootstrap includes recommended skills; project skill root contains readable links/copies; path traversal is covered. |
| T5 | B1 gate | B1 | Run B1 integration verification and update plan/status notes for B1 evidence. | T1–T4 | no | `qa-engineer` / PM follow-up | Focused cargo tests for touched crates pass; `status.json` notes/gates can cite B1 evidence. |
| T6 | O2-A | B2 | Remove `research`, `manuscript`, and `publish` CLI command modules and dispatch wiring. | T5 | yes, with T7 if file ownership is split | `fullstack-dev` | Commands are not reachable by parser/dispatch and no dead module exports remain. |
| T7 | O2-B | B2 | Add or align preset/pipeline entrypoints and help text that cover former workflow intent without recreating removed groups. | T5 | yes, with T6 after CLI boundary lock | `fullstack-dev-2` | Help text guides users to preset/orchestration entrypoints; former workflows have replacement invocation docs/tests. |
| T8 | O2-C | B2 | Update CLI/orchestration tests and fixtures for removed commands and replacement entrypoints. | T6, T7 | no | `qa-engineer` / `fullstack-dev` | Help assertions prove removed groups are absent; replacement flow tests pass. |
| T9 | O3-A | B3 | Implement `novel-writing` story output policy: `Stories/<StoryRef>/<chapter-id>.md`. | T4 | yes, with T10 after mapping contract lock | `fullstack-dev` | Preset output writes to the agreed story path and does not add fixed workspace skeleton directories. |
| T10 | O3-B | B3 | Persist/resolve world-story association through local DB or creator config instead of inferring solely from paths. | T9 contract | yes, with T9 only if persistence/API boundary is split | `fullstack-dev-2` | Mapping is explicit, testable, and resilient to renamed folders. |
| T11 | O3-C | B3 | Define and test the `novel-writing` sync module contract for workspace-to-platform structured deltas/bundles. | T9, T10 | no | `architect` for contract, `fullstack-dev` for implementation | Contract is written in-repo, tests cover bundle shape, and platform companion gate is identified without adding duplicate DTO sources. |

---

## Cross-Repo Gates

| Gate | Nexus exit condition |
|---|---|
| **G1** | This V1.15 compass scope and the companion platform program compass scope are committed/aligned before implementation claims cross-repo readiness. |
| **G2** | B1 merged with green `cargo clippy` / `cargo test` evidence on touched crates. |
| **G3** | B2 and B3 complete; `.agents/status.json` and residual tracking reflect actual state. |
| **G4** | Tracker and deferred-tracker V1.15 horizon prose are aligned; platform CLI spec and ADR companion changes land before or with final V1.15 Done claim. |
| **G5** | QC ×3 plus QA runs on this registered `{PLAN_DIR}` plan diff range after the implementation reaches `InReview`. |

---

## Done Definition

1. No removed CLI group remains reachable (`nexus42 research *`, `nexus42 manuscript *`, `nexus42 publish *`).
2. `recommended_models` is absent from the active preset path.
3. `cargo clippy --all -- -D warnings` and `cargo test` evidence exists for touched workspace members.
4. Companion platform `cli-spec-v1.md` plus ADR-023/024 changes are merged before or with any final nexus PR that claims V1.15 `Done`.
5. `status.json`, this plan, and `.agents/knowledge/v1.15-delivery-compass-v1.md` remain mutually consistent.

---

## Risk Summary

| Rank | Risk | Mitigation |
|---|---|---|
| 1 | Skill sync overwrites user files. | Use content hashes or `.new` suffix policy, emit explicit operator report, and test conflict paths. |
| 2 | Symlink portability across OS/permissions. | Provide a fallback copy policy behind preset/config feature selection; test symlink target validation. |
| 3 | Sync module drifts from platform API. | Keep JSON Schema/contracts as SSOT, add contract tests, and enforce platform companion gate before Done. |

Additional plan-level risk: command removal may break undocumented local workflows. V1.15 is pre-release, so no compatibility layer is required, but help text and preset entrypoints must make the replacement path explicit.

---

## QC / QA Wave Plan

**When:** After B1, B2, and B3 implementation for this `plan_id` reaches `InReview`. Default is one full QC triple-review for the whole multi-batch plan, not one per batch.

| Reviewer | Focus |
|---|---|
| QC1 | Crate boundaries: orchestration vs CLI vs ACP host; DTO/schema single-source discipline. |
| QC2 | Safety: path traversal, symlink targets, user-local file conflict behavior, ACP session injection boundaries. |
| QC3 | Performance/reliability: skill sync I/O, session bootstrap overhead, test runtime, fallback behavior. |
| QA | Full `cargo test` / workspace checks on the PM-provided Review cwd; attach command log and verify help text/user-visible entrypoints. |

QC/QA assignments must share the same `plan_id`, `Review cwd`, `Working branch`, and `Review range / Diff basis` exactly. If implementation runs in multiple worktrees, PM must merge all review scope into one `HEAD` before dispatching QC.

---

## Verification Plan

- Before editing Rust symbols, implementers must run GitNexus impact analysis for the target symbol/file and report any HIGH/CRITICAL risk before proceeding.
- Before committing implementation work, run `gitnexus_detect_changes({"scope":"all","repo":"nexus"})` and confirm changed symbols match the assigned task scope.
- Required local commands for final Done candidate:
  - `cargo +nightly fmt --all -- --check`
  - `cargo clippy --all -- -D warnings`
  - `cargo test --workspace`
- If `schemas/` changes, run `pnpm run codegen` and commit regenerated output in the same implementation scope.

---

## Implementation Effort (agent-oriented)

- **Complexity:** L — three batches across CLI, orchestration, ACP host, path layout, local persistence, and sync contract surfaces.
- **Agent session band:** ~3–8 focused implementation/verification sessions, plus QC/QA gate sessions, assuming the compass remains locked and platform companion contracts are available when needed.

---

## Self-Review Checklist

- Spec coverage: O1-A..D map to T1–T5; O2-A..C map to T6–T8; O3-A..C map to T9–T11.
- Batching: B1 → B2 → B3 dependencies are explicit, with limited parallel flags and worktree caveats.
- No source-code changes are included in this plan-authoring commit.
- Placeholder scan: no TBD/TODO placeholders in task descriptions or done criteria.
