# V1.2 M3: Minimum Automated Regression Suite (R1–R5)

**Plan ID**: `2026-04-14-v1.2-regression-suite`
**Status**: Todo
**Workstream**: M3-gate
**Milestone**: M3
**Owner**: @project-manager
**Agents**: @qa-engineer
**Compass**: [v1.2-delivery-compass-v1](knowledge/v1.2-delivery-compass-v1.md)

## Scope

Implement the minimum automated regression set as V1.2 hard release gate. All 5 scenarios must pass before V1.2 release.

## Regression Scenarios

| ID | Description | Validates |
|----|-------------|-----------|
| R1 | `local_only` anonymous/temporary identity end-to-end | WS2a identity |
| R2 | `local_only` local persistent identity end-to-end | WS2a identity |
| R3 | Local truth core chain regression (SOUL/memory/KB/output) | WS3a + WS3b pipeline |
| R4 | Mode switch and degradation regression (`local_first`/`cloud_enhanced`) | WS4 compatibility |
| R5 | No-platform dependency guard for `local_only` path | WS2b runtime guards |

## Dependencies

- `2026-04-14-v1.2-local-memory-pipeline` (WS3b) — R3 requires full pipeline
- `2026-04-14-v1.2-runtime-compatibility` (WS4) — R4 requires mode switch

## Deliverables

- [ ] R1–R5 automated test implementations
- [ ] CI integration for regression gate
- [ ] Regression gate documentation
- [ ] All R1–R5 pass in automated run

## Acceptance Evidence (Compass §5 M3 + §6)

- Minimum automated regression set passes
- Batch E tracked as parallel non-blocking lane with explicit status

## Tasks

### Regression Scenarios

- [x] **R4**: Mode switch and degradation regression — verified in T6 QA (commit `1e63679`)
  - Evidence: `context_assemble_*` integration tests pass, degradation chain tests pass
  - Status: Complete (no additional implementation needed)

- [ ] **R1**: `local_only` anonymous/temporary identity end-to-end
  - File: `tests/regression/r1_local_only_anonymous_identity.rs`
  - Scope: Create anonymous identity → use for session → verify no platform dependency
  - Validates: WS2a identity dual path (anonymous mode)

- [ ] **R2**: `local_only` local persistent identity end-to-end
  - File: `tests/regression/r2_local_only_persistent_identity.rs`
  - Scope: Create local persistent identity → persist to config → use in multiple sessions
  - Validates: WS2a identity dual path (persistent mode)

- [ ] **R3**: Local truth core chain regression (SOUL/memory/KB/output)
  - File: `tests/regression/r3_local_truth_chain.rs`
  - Scope: Init SOUL → create memory → accumulate → assemble context → verify output
  - Validates: WS3a + WS3b full pipeline integration

- [ ] **R5**: No-platform dependency guard for `local_only` path
  - File: `tests/regression/r5_no_platform_guard.rs`
  - Scope: Set local_only mode → attempt platform operations → verify blocked
  - Validates: WS2b runtime_guard + OperationCategory checks

### CI Integration

- [ ] Create `tests/regression/mod.rs` with test harness
- [ ] Add regression test runner script
- [ ] Update CI workflow to run regression suite
- [ ] Document regression gate in `docs/regression-gate.md`

### Verification

- [ ] All 5 regression tests pass (`cargo test --test regression`)
- [ ] CI pipeline green on regression suite
- [ ] Regression gate documentation complete
