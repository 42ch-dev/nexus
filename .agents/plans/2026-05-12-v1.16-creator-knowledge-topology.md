# Plan: V1.16 Creator Identity and Knowledge Command Topology

**Plan ID**: `2026-05-12-v1.16-creator-knowledge-topology`
**Theme**: B3 — Creator knowledge model
**Milestone**: M2
**Effort**: M
**Working branch**: `feature/v1.16-creator-knowledge-topology` (from `feature/v1.16`)
**Depends on**: `2026-05-12-v1.16-command-surface-contract`

---

## Goal

Extend `creator` with soul/memory/kb/logout subcommands, migrate `init`/`clone` → `creator workspace init`/`creator workspace clone`, migrate `world` → `sync world`, migrate `schedule` → `daemon schedule`, and hide remaining legacy top-level commands. After this plan, only 6 V2 groups are visible.

## Scope

### In scope

1. **Extend `creator` subcommands**:
   - `creator soul` — migrated from `soul` (thin delegation)
   - `creator memory` — migrated from `memory` (thin delegation)
   - `creator kb` — new stub for future KB multi-scope
   - `creator logout` — new (clear creator credentials/session)
   - Extend `creator workspace` with `init` (from `init`), `clone` (from `clone`), `link`/`unlink`/`status` (stubs)

2. **Extend `sync` subcommands**:
   - `sync world` — migrated from `world` (thin delegation)
   - `sync retry` — new stub

3. **Migrate `schedule` → `daemon schedule`** (thin delegation)

4. **Hide remaining legacy top-levels**:
   - `soul`, `memory`, `init`, `clone`, `world`, `schedule`

5. **Un-ignore `v2_target_creator_subcommands`**

6. **Un-ignore `v2_only_six_visible_command_groups`** (final — only 6 visible after this)

7. **Update `current_state_visible_command_groups`** (12 → 6 visible)

### Out of scope

- `creator kb` full implementation (Plan 5)
- `sync retry` full implementation
- Removing hidden legacy commands (later cleanup)
- `v2_target_sync_subcommands` (sync already has pull/push/status/resolve; retry may be stub)

## Tasks

| ID | Task | Depends on | Owner | Done criteria | Status |
|----|------|------------|-------|---------------|--------|
| T1 | Add `soul`/`memory`/`kb`/`logout` variants to `CreatorCommand`, delegate to existing modules | — | @fullstack-dev | `creator soul/memory/kb --help` work | [x] |
| T2 | Extend `CreatorWorkspaceCommand` with init/clone/link/unlink/status | — | @fullstack-dev | `creator workspace init/clone --help` work | [x] |
| T3 | Add `world` + `retry` to `SyncCommand`, delegate world to existing | — | @fullstack-dev | `sync world --help` works | [x] |
| T4 | Add `schedule` to `DaemonCommand`, delegate to existing | — | @fullstack-dev | `daemon schedule --help` works | [x] |
| T5 | Hide legacy top-levels: soul/memory/init/clone/world/schedule | T1–T4 | @fullstack-dev | Only 6 visible in `--help` | [x] |
| T6 | Update contract tests (un-ignore creator + six-groups, update snapshot) | T5 | @fullstack-dev | All tests pass | [x] |
| T7 | Verify all tests + clippy + fmt | T6 | @fullstack-dev | Clean | [x] |

## Sign-off

| Role | Verdict | Date |
|------|---------|------|
| QC1 (@qc-specialist) | Approve | 2026-05-12 |
| QC2 (@qc-specialist-2) | Request Changes → Fixed | 2026-05-12 |
| QC3 (@qc-specialist-3) | Approve | 2026-05-12 |
| QA (@qa-engineer) | **PASS** | 2026-05-12 |

## Acceptance criteria

| # | Criterion | Status |
|---|-----------|--------|
| 1 | `nexus42 creator --help` shows register/status/use/list/pair/unpair/logout/credentials/workspace/soul/memory/kb | [x] PASS |
| 2 | `nexus42 creator soul --help` works | [x] PASS |
| 3 | `nexus42 creator memory --help` works | [x] PASS |
| 4 | `nexus42 creator workspace init --help` works | [x] PASS |
| 5 | `nexus42 creator workspace clone --help` works | [x] PASS |
| 6 | `nexus42 sync world --help` works | [x] PASS |
| 7 | `nexus42 daemon schedule --help` works | [x] PASS |
| 8 | Only 6 visible top-level commands: daemon/acp/creator/sync/platform/system | [x] PASS |
| 9 | `v2_target_creator_subcommands` passes | [x] PASS |
| 10 | `v2_only_six_visible_command_groups` passes | [x] PASS |
| 11 | `cargo test --all` passes | [x] PASS (1 pre-existing flaky test, tracked as R-V113-007) |
| 12 | `cargo clippy --all -- -D warnings` clean | [x] PASS |
| 13 | `creator logout` actually clears credentials (not just stub) | [x] PASS |
| 14 | `creator workspace clone --dry-run` preserves the flag | [x] PASS |
