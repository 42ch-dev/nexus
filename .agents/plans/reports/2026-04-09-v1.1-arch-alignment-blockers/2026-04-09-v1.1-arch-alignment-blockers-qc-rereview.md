---
report_kind: qc_rereview
plan_id: "2026-04-09-v1.1-arch-alignment-blockers"
verdict: Approve with residuals
generated_at: "2026-04-09"
reviewer: qc-specialist
reviewer_index: 1
---

# QC Re-Review — V1.1 Architecture Alignment Blockers

## Review context

| Field | Value |
|--------|--------|
| Review cwd | `<repository-root>` |
| Working branch | `v1.1-arch-alignment-blockers` |
| `HEAD` | `9a8a81545b9d4c759bafb4824ed7e5ea7633d093` |
| Scope | **Working tree** (includes uncommitted changes in `.agents/plans/status.json`, `crates/nexus42/src/commands/sync.rs`, `crates/nexus42d/src/api/handlers/sync.rs`) |
| Prior consolidated gate | [qc-consolidated](2026-04-09-v1.1-arch-alignment-blockers-qc-consolidated.md): **C-01** (status envelope), **H-01** (daemon push: BundleBuilder + precheck + staging / honest deferral) |

## Executive summary

**C-01** and **H-01** are **addressed** in the working tree: `status.json` again matches the harness envelope expected by `AGENTS.md`, and the daemon push path uses **`BundleBuilder`**, **`precheck_bundle_with_auth`**, and **`Outbox::stage`**, with **explicit** documentation that **`SyncClient::push_bundle`** is **not** called yet, backed by open residual **ARCH-SYNC-D1**.

Remaining consolidated themes (**M-\*** and below) are **appropriately deferred** under `metadata.residual_findings["2026-04-09-v1.1-arch-alignment-blockers"]` with sane severities and owners.

**Verdict: Approve with residuals** — no blocking issues for this re-review scope; ship with continued tracking of the nine open rows for this plan id (and program rollup).

## Evidence (verification)

| Check | Result |
|--------|--------|
| `git rev-parse HEAD` | `9a8a81545b9d4c759bafb4824ed7e5ea7633d093` |
| `jq empty .agents/plans/status.json` | OK |
| `cargo clippy -p nexus42d -p nexus42 -- -D warnings` | Pass |
| `cargo test -p nexus42d sync::` | 9 passed |

## 1. `.agents/plans/status.json`

### Envelope (C-01)

- **`version`**: `1` present at root.
- **`plans`**: Array restored with historical **Done** slim rows, **InProgress** `v1-tech-debt-cleanup`, and target plan **`2026-04-09-v1.1-arch-alignment-blockers`** with `status: "InReview"`, full metadata, and updated `notes` referencing post–C-01/H-01 state.
- **Root `metadata`**: `versioning`, `tech_debt_summary`, `residual_findings` present.

### Residuals for this plan id (multi-row)

Under `metadata.residual_findings["2026-04-09-v1.1-arch-alignment-blockers"]` there are **9** open entries: **ARCH-SYNC-D1**, **ALIGN-HASH-01**, **ALIGN-PRE-01**, **ALIGN-USER-01**, **ALIGN-CTX-01**, **ALIGN-NPM-01**, **ALIGN-BLD-01**, **ALIGN-DOC-01**, **ALIGN-CLI-01**. Severities align with consolidated **M-\*** / **L-01** / nit themes (no `critical`/`high` in JSON).

### `tech_debt_summary` consistency

| Field | Value | Cross-check |
|--------|--------|-------------|
| `total_open` | `17` | `1` (domain-models) + `7` (v1.1-tech-debt-mitigation) + `9` (this plan) = **17** |
| `by_plan` | `1`, `7`, `9` | Matches per-plan array lengths |
| `by_severity` | `critical: 0`, `high: 0`, `medium: 6`, `low: 10`, `nit: 1` | Tallies to **17** (historical `"warning"` rows counted as `low` per `severity_note`) |

Rollup is **internally consistent** with the open `residual_findings` map.

**Non-blocking (SSOT hygiene):** Plan row `v1-tech-debt-cleanup` narrative (`batch_c` list including **DM-R3**) mixes residuals that live under **`2025-04-05-domain-models`** in SSOT; readers should trust `residual_findings` keys, not batch prose.

## 2. `crates/nexus42d/src/api/handlers/sync.rs` (H-01)

### Implementation

- **Push** builds a bundle with **`BundleBuilder`**, runs **`precheck_bundle_with_auth`**, then **`outbox.stage(&bundle)`** (delivery **`ready`**), matching the intended pipeline elevation.
- **Comments** state clearly that **`SyncClient::push_bundle`** is **not** invoked yet and that platform push is optional follow-up — consistent with **ARCH-SYNC-D1**.
- **Tests** updated: after push, **`ready_count == 1`** and **`staged_count == 0`**, matching `Outbox::stage` semantics.

### Comment honesty

Module and `push` doc comments are **honest** about offline-first queuing and the absence of live `SyncClient` upload on this handler.

## 3. `crates/nexus42/src/commands/sync.rs` (TD-1 doc)

Module-level **Wiring (TD-1)** text correctly describes the CLI as a **daemon HTTP** client and assigns **BundleBuilder / precheck / `Outbox::stage`** to the daemon-backed path, with **`canonical_hash`** mentioned as part of bundle build.

**Non-blocking:** The phrase *“HTTP upload to the platform via **`SyncClient`** is offline-first …”* is **slightly easy to misread** as “upload already happens via SyncClient.” The daemon crate already clarifies that SyncClient is not wired. For perfect cross-layer honesty, the CLI blurb could add one clause mirroring the daemon (“daemon does not call `SyncClient::push_bundle` yet”).

## 4. Prior “Request Changes” — addressed vs deferred

| Consolidated ID | Theme | Status in working tree |
|-----------------|--------|-------------------------|
| **C-01** | Standard `status.json` shape | **Addressed** — full envelope restored |
| **H-01** | Real bundle + precheck + staging path; no false claims re SyncClient | **Addressed** for builder + precheck + `Outbox::stage`; **SyncClient** **deferred** with residual **ARCH-SYNC-D1** and explicit comments |
| **M-01**–**M-06**, **L-01**, **N-01** | Hash preimage, stricter precheck, User typing, ID binding, npm semver, `build_validated` panic, docs, CLI nits | **Deferred** as **ALIGN-\*** / **ALIGN-DOC-01** / **ALIGN-CLI-01** (appropriate) |

No evidence that consolidated **blockers** were silently ignored without residuals or documentation.

---

## Findings

### Blocking

*None* for this re-review (C-01 / H-01 closure criteria).

### Non-blocking

1. **NB-01 — Precheck warnings omitted on success path**  
   When `precheck_bundle_with_auth` returns **`PrecheckResult::Valid`** but the internal report had **warnings only** (`has_warnings_only()` branch in `precheck.rs`), the handler maps that to a summary with **`warning_count: 0`** and **`"All checks passed."`**, so API consumers never see those warnings. Consider threading a summary for the warning-only case (medium / UX-observability).

2. **NB-02 — CLI TD-1 blurb vs daemon**  
   Optional tightening: state explicitly that **daemon push does not call `SyncClient` yet** (nit / doc alignment with `nexus42d` module docs).

3. **NB-03 — Test name drift**  
   `test_sync_push_stages_command` still reads “stages command” though the path stages a **bundle** via **`Outbox::stage`** (nit).

4. **NB-04 — Tech-debt cleanup plan narrative**  
   `v1-tech-debt-cleanup` `batch_status.batch_c` text vs SSOT plan keys (see §1) — nit for PM/editorial cleanup, not merge gate.

---

## Cross-reviewer notes

- **ARCH-SYNC-D1** provides the explicit, traceable deferral expected by consolidated **H-01** for optional `SyncClient::push_bundle`.
- **ALIGN-CTX-01** remains the right home for workspace/world/creator **binding** validation beyond empty-string checks now present in the handler.

## Plan / gates (for @project-manager)

- Recommend advancing **`qc_status`** from `pending_rereview` to **passed** for this wave after PM accepts this report; **QA** and **merge_to_main** remain per program policy.
- **Do not** treat NB-01–NB-04 as merge blockers for the architecture-alignment branch unless product policy requires warning visibility on push responses in this release.

## Handoff

- **@project-manager**: Close QC re-review loop; refresh plan gates and queue QA per consolidated handoff.
- **@fullstack-dev**: Optional follow-ups for NB-01–NB-03 in a later batch or tie to existing ALIGN residuals where applicable.
