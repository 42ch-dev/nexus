---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 2
plan_id: "2026-04-09-v1.1-arch-alignment-blockers"
batch: "v1.1-alignment-residuals (ALIGN closure)"
verdict: "Approve"
generated_at: "2026-04-08"
---

# QC Report #2 — Residual batch (`v1.1-alignment-residuals`)

## Executive summary

Review emphasis: **API semantics**, **error surfaces**, and **consistency** with existing daemon patterns (`sync.rs` / `workspace_meta`).

Optional sync binding correctly returns **`NexusApiError::InvalidInput`** when keys are **partially** populated — maps to **400** per existing `error_code` rules; push **mismatch** returns **`200` + `SyncPushResponse.success: false`** with a clear summary — acceptable for this local JSON API (clients already parse `success` + `precheck_result`).

`check_canonical_hash` runs after `check_required_fields`; empty-delta bundles short-circuit in `check_canonical_hash` (no hash required) while `check_required_fields` still rejects empty `deltas[]` — **no contradiction**.

**Verdict:** **Approve**.

## Findings

### Low

- **RS2-L1 — `query_row(...).await.unwrap_or(None)`**  
  Matches `last_sync_at` pattern in the same module; database errors collapse to “missing row”. Acceptable for local daemon; if stricter diagnostics are desired later, log `Err` paths explicitly.

### Nit

- **RS2-N1 — CLI warning is stderr-only**  
  Non-interactive automation may still push placeholders; consider future structured `--json` output — out of scope for this batch.

## Cross-checks

- Precheck tests updated for computed `canonical_hash` in `valid_bundle()` — **good regression guard**.
- `is_well_formed_canonical_hash` rejects uppercase hex — consistent with `hex::encode` output from `canonical_hash_for_deltas`.

## Automated checks

Reviewer re-ran full CI parity suite on `35726ab` — **pass** (see QA report for command list).
