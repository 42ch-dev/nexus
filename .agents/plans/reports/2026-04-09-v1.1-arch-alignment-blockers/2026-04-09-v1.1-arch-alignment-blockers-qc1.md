---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-04-09-v1.1-arch-alignment-blockers"
verdict: "Request Changes"
generated_at: "2026-04-08"
---

# QC Report #1 — V1.1 Architecture Alignment Blockers

## Executive summary

The branch delivers substantial value: JSON Schema enum tightening (TD-4), regenerated Rust/TS contracts, a large `enum_conversions` companion layer, domain alignment (World/M membership enums), `User` schema + domain aggregate scaffolding, and **`canonical_hash` implementation inside `BundleBuilder`** (TD-5). `cargo clippy --all -- -D warnings`, `cargo test --all`, and `pnpm run typecheck` all pass on the reviewed tip.

The **primary architecture gap** is that **TD-1 is overstated relative to the code**: the live `nexus42` / `nexus42d` sync push path uses `Outbox::append(&SyncCommand)` only. It does **not** invoke `BundleBuilder`, `precheck_bundle` / `precheck_bundle_with_auth`, or `SyncClient::push`. As a result, **`canonical_hash` and full bundle construction are not exercised on the actual CLI→daemon push path**, and the plan’s checked acceptance row for TD-1 is **misaligned with behavior**. This is a **plan/implementation integrity** issue for a P0 item, not merely a style nit.

**Verdict:** **Request Changes** — merge is **not** recommended until PM/engineering either (a) wire the real bundle + precheck + client flow to match TD-1 acceptance, or (b) explicitly **downscope** TD-1 in the plan + `status.json` and register an open **residual** with a concrete follow-up scope (avoid silent drift).

## Scope reviewed

| Field | Value |
|--------|--------|
| **Plan ID** | `2026-04-09-v1.1-arch-alignment-blockers` |
| **Review cwd** | `<repository-root>` |
| **Working branch (verified)** | `v1.1-arch-alignment-blockers` @ `9a8a81545b9d4c759bafb4824ed7e5ea7633d093` |
| **Diff basis** | `merge-base: 33443dc2f98a56927aeea2371ad16c948f005a75`, `HEAD: 9a8a81545b9d4c759bafb4824ed7e5ea7633d093`, `rev-range: 33443dc2f98a56927aeea2371ad16c948f005a75..9a8a81545b9d4c759bafb4824ed7e5ea7633d093` |

**Areas emphasized (per assignment):** schemas + codegen outputs, `enum_conversions.rs`, `nexus-sync` (`canonical_hash`, `delta_bundle`, `outbox`), User schema/domain, `contract_assertions`, merge safety vs `origin/main`.

**Plan row / `status.json`:** Plan is **InReview**; gates show `qc_status: pending`, `qa_status: pending`. `metadata.implementation_tip` (`717c61d`) **does not match** reviewed `HEAD` (`9a8a815`) — PM should refresh notes when reconciling QC.

**Merge safety:** `git merge-base HEAD origin/main` equals **`33443dc2f98a56927aeea2371ad16c948f005a75`**, i.e. the same as this review’s merge-base — **linear ahead of `main`** in this clone, **low mechanical conflict risk**.

**Automated checks (evidence):**

- `cargo clippy --all -- -D warnings` — pass  
- `cargo test --all` — pass  
- `pnpm run typecheck` — pass  

## Findings

### Critical / High

- **R1-H1 — TD-1 “sync wiring” does not match declared acceptance (architecture / product truth)**  
  - **Severity:** **High** (P0 item; blocks honest “TD-1 complete” sign-off).  
  - **Evidence:** `crates/nexus42d/src/api/handlers/sync.rs` `push` builds a `SyncCommand` and calls `outbox.append(&command)` only. `Outbox::append` (`crates/nexus-sync/src/outbox.rs`) persists **command JSON** and synthetic `bundle_id` / `idempotency_key` without a `Bundle` envelope or deltas. There are **no** call sites in `nexus42`/`nexus42d` (outside tests) for `BundleBuilder::build`, `precheck_bundle`, or `SyncClient::push`.  
  - **Impact:** End-to-end sync still lacks the bundle-level pipeline the plan describes; `canonical_hash` (TD-5) applies to **`BundleBuilder`-built bundles**, not to the current push path.  
  - **Cross-review:** Other reviewers should **confirm** the same call-graph gap; this is objective from grep/read, not subjective style.

### Medium

- **R1-M1 — Misleading module documentation vs actual dependencies**  
  - **Severity:** **Medium** (maintainability / trust).  
  - **Evidence:** `crates/nexus42d/src/api/handlers/sync.rs` and `crates/nexus42/src/commands/sync.rs` claim wiring of **Precheck** and **SyncClient**; handlers only use **`Outbox`** and ad-hoc “precheck” (non-empty IDs).  
  - **Risk:** Future refactors assume precheck/platform push already integrated.

- **R1-M2 — `canonical_hash` definition vs cross-stack idempotency**  
  - **Severity:** **Medium** (interop / long-term contract risk).  
  - **Evidence:** `canonical_hash_for_deltas` hashes `serde_json::to_vec(deltas)`. Nested `payload` `serde_json::Value` key order and formatting are **Rust serde_json’s** choices; platform/TS may canonicalize differently unless explicitly specified and tested cross-language.  
  - **Suggestion:** Confirm against sync-contract / platform expectations; add cross-language golden-vector tests if required.

- **R1-M3 — `User` aggregate: typed enums exist but aggregate stores strings; infallible `unwrap` on wire → domain**  
  - **Severity:** **Medium** (robustness vs plan text).  
  - **Evidence:** `crates/nexus-domain/src/user.rs` uses `String` for `account_status` / `subscription_tier` while domain enums `AccountStatus` / `SubscriptionTier` exist; `From<User> for nexus_contracts::User` uses `FromStr::from_str(...).unwrap()`. Corrupt or future-variant strings **panic** instead of returning `DomainError`. Plan acceptance called for enum-aligned invariants — partial match.

- **R1-M4 — `BundleBuilder::build_validated` uses `expect` after `validate()`**  
  - **Severity:** **Medium** (API safety).  
  - **Evidence:** `crates/nexus-sync/src/delta_bundle.rs` — validation does not guarantee `build()` cannot fail (e.g. serialization edge cases). Prefer propagating `SyncError` instead of panic in library code.

### Low

- **R1-L1 — `enum_conversions.rs` parallel to generated enums**  
  - **Severity:** **Low** (drift risk).  
  - **Evidence:** `crates/nexus-contracts/src/enum_conversions.rs` manually mirrors all variant strings for `Display` / `FromStr` / `as_str`. New schema enum variants require updating **both** codegen output and this file (compiler helps via exhaustiveness, but TS/Rust codegen stays the SSOT for wire — document the “second place” clearly).

### Nit

- **R1-N1 — Plan markdown checklists vs `status.json`**  
  - Many checkbox items under TD-5/TD-6 / verification remain unchecked in `.agents/plans/2026-04-09-v1.1-arch-alignment-blockers.md` while `status.json` asserts broad completion — reconcile to avoid contradictory SSOTs.

- **R1-N2 — CLI defaults for sync push**  
  - `crates/nexus42/src/commands/sync.rs` defaults `workspace_id` / `world_id` / `creator_id` to `"local"` / `"unknown"` / config or `"unknown"` — easy to stage nonsense in the outbox during informal use (mitigated if later stages validate).

## Unique to Reviewer #1 (architecture / evolution)

- **Dependency direction:** Centralizing stringly wire enums into schema + generated types is **consistent** with repo AGENTS guidance; the **extra** `enum_conversions` layer is a deliberate extension point — acceptable if team accepts ongoing dual maintenance.  
- **Integration surface:** The gap between **“command-only outbox append”** and **“bundle + precheck + HTTP client”** is the main **long-horizon** risk; without closing it, TD-4/TD-5 benefits remain **mostly latent** for real sync.

## Cross-reviewer overlap (verify together)

- **Canonical hash tests** only cover builder path; **agree** whether QC#2/#3 should require a test that exercises **daemon push → stored row** semantics.  
- **Security / trust boundary:** Local daemon accepts IDs from JSON body — **confirm** with reviewer #2/#3 whether any binding to `WorkspaceState` is required.

## Residual notes for `status.json` (PM — open items only)

Suggested rows for `residual_findings["2026-04-09-v1.1-arch-alignment-blockers"]` (wording for PM; **do not** treat as closed):

1. **TD-1 scope truth:** Implement or formally descope — wire `BundleBuilder` + `precheck_bundle`[_`with_auth`_] + `Outbox::stage` + `SyncClient::push` on the real push path **or** update plan acceptance and track remainder.  
2. **Interop:** Verify `canonical_hash` algorithm matches platform contract (serde JSON bytes vs canonical JSON).  
3. **User robustness:** Replace string fields with typed enums (or fallible conversions) per plan TD-6 acceptance tone.

## Recommendation

**Not merge-ready** as an honest closeout of **TD-1** and **TD-5** in production sync — **merge with follow-ups** only if PM explicitly **downscopes** TD-1 in the plan and adds the residuals above with owner/priority.

**Handoff:** @project-manager — reconcile plan checkboxes, `status.json` gates, and residuals; @fullstack-dev — TD-1 completion or doc correction; @qa-engineer — manual “sync push toward platform” per plan’s open checkbox.

## Completion alignment (Reviewer #1)

| Category | This review |
|----------|-------------|
| **Primary evidence** | `git diff` over stated rev-range; reads of `sync.rs`, `outbox.rs`, `delta_bundle.rs`, `canonical_hash.rs`, `user.rs`, `enum_conversions.rs`, `contract_assertions.rs`; `cargo clippy` / `cargo test` / `pnpm typecheck` |
| **Evidence quality** | High for TD-1 gap (call graph); Medium for cross-language hash until spec cited |
