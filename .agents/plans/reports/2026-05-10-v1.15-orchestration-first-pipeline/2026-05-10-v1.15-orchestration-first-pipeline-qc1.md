---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: 2026-05-10-v1.15-orchestration-first-pipeline
verdict: "Request Changes"
generated_at: "2026-05-11"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: bailian-coding-plan/glm-5
- Review Perspective: Architecture consistency, maintainability, long-term evolution risks (module boundaries, abstraction levels, dependency direction, extensibility)
- Report Timestamp: 2026-05-11T12:00:00Z

## Scope
- plan_id: 2026-05-10-v1.15-orchestration-first-pipeline
- Review range / Diff basis: git diff main...HEAD
- Working branch (verified): feature/v1.15
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 82
- Commit range: main...HEAD (82 files changed, +3387/-3570 lines)
- Tools run: cargo clippy, cargo test

## Findings

### 🔴 Critical
(none)

### 🟡 Warning

#### W-001: CI Gate Blocker — auth tests failing due to error message drift
- **Location**: `crates/nexus42/src/auth/user_auth.rs` (tests)
- **Evidence**: 
  ```
  test auth::user_auth::tests::ensure_valid_token_no_token_returns_error ... FAILED
  Expected 'Not authenticated', got: Access token expired and no refresh token available...
  
  test auth::user_auth::tests::ensure_valid_token_valid_token_skips_refresh ... FAILED
  Unexpected error: Access token expired and no refresh token available...
  
  test auth::user_auth::tests::refresh_access_token_invalid_grant_clears_tokens ... FAILED
  Expected 'expired' in error, got: No refresh token available
  ```
- **Impact**: CI pipeline will fail; per `mstar-review-qc` CI gate rules, any test failure defaults to >= Warning and blocks approval until resolved.
- **Recommendation**: Update test assertions to match the current error message format, or verify that the error message change was intentional and aligns with expected user-facing behavior.

### 🟢 Suggestion

#### S-001: skill_link.rs test uses conditional compilation correctly
- **Observation**: Platform-specific symlink tests in `skill_link.rs` correctly use `#[cfg(unix)]` / `#[cfg(windows)]` attributes.
- **Quality**: Good cross-platform handling; no action required.

#### S-002: embedded_skills.rs internal types are properly scoped
- **Observation**: `SkillManifest` and `SkillManifestEntry` are internal structs (not exported), separate from wire DTOs in `nexus-contracts`.
- **Quality**: Correct separation of internal parsing types from contract types; maintains single-source discipline.

#### S-003: skill_sync.rs conflict detection is non-destructive
- **Observation**: User-modified skill files are never overwritten — conflicts are logged and preserved.
- **Quality**: Good defensive design for user data; aligns with principle of user ownership.

## Source Trace
- Finding ID: W-001
- Source Type: cargo test output
- Source Reference: `cargo test --workspace 2>&1 | grep -E "FAILED|failures:" -A 30`
- Confidence: High

- Finding ID: S-001, S-002, S-003
- Source Type: manual-reasoning (code review)
- Source Reference: `crates/nexus-orchestration/src/skill_link.rs`, `embedded_skills.rs`, `skill_sync.rs`
- Confidence: High

## Crate Boundary Verification (Architecture Focus)

| Check | Status | Evidence |
|-------|--------|----------|
| `nexus-orchestration` NOT import from `nexus42` | ✅ PASS | `grep -r "use nexus42" crates/nexus-orchestration/src/` → no output |
| `nexus-orchestration` NOT import from `nexus42d` | ✅ PASS | `grep -r "use nexus42d" crates/nexus-orchestration/src/` → no output |
| `nexus42` NOT import from `nexus42d` | ✅ PASS | `grep -r "use nexus42d" crates/nexus42/src/` → no output |
| `nexus-local-db` only depends on `nexus-contracts` | ✅ PASS | Cargo.toml: only `nexus-contracts` in workspace deps |
| `nexus-home-layout` has no dependencies | ✅ PASS | Cargo.toml: no path dependencies, pure path helpers |
| `nexus-acp-host` only depends on `nexus-contracts` | ✅ PASS | Cargo.toml: only `nexus-contracts` as internal dep |
| DTO single-source: `nexus-contracts` used correctly | ✅ PASS | `preset/manifest.rs`: `pub use nexus_contracts::local::orchestration::preset::*;` |

### Dependency Direction Summary
```
nexus-contracts (leaf) ← nexus-home-layout (leaf)
                        ← nexus-local-db
                        ← nexus-acp-host
                        ← nexus-orchestration ← nexus42 ← nexus42d
```
All arrows point inward → correct dependency direction (no cycles).

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 1 |
| 🟢 Suggestion | 3 |

**Verdict**: Request Changes

### Reasoning
- CI gate failure (3 auth tests) must be resolved before merge.
- Crate boundaries and dependency directions are correct.
- No duplicate DTOs detected — contracts are single-source.
- New modules (`skill_link`, `skill_sync`, `embedded_skills`) are well-structured with good separation of concerns.
- No architecture-level concerns; the warning is test maintenance, not structural flaw.

### Next Steps for @fullstack-dev
1. Investigate `crates/nexus42/src/auth/user_auth.rs` test failures.
2. Determine if error message changes were intentional (UX improvement) or unintended drift.
3. Update test assertions if intentional, or revert error messages if unintended.
4. Re-run `cargo test --workspace` to verify fix.
5. Request QC re-review (rev2) after CI passes.