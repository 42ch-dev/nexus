# QC Consolidated Decision — WS1 `nexus-acp-host` crate extraction

**Plan ID**: `2026-04-17-v1.4-ws1-acp-host-extraction`
**Date**: 2026-04-17
**PM**: `project-manager`

## Decision: **Approve**

## Reviewers

| # | Reviewer | Report | Verdict |
|---|----------|--------|---------|
| 1 | `qc-specialist` | `2026-04-17-v1.4-ws1-acp-host-extraction-qc1.md` | Request Changes |
| 2 | `qc-specialist-2` | `2026-04-17-v1.4-ws1-acp-host-extraction-qc2.md` | Request Changes |
| 3 | `qc-specialist-3` | `2026-04-17-v1.4-ws1-acp-host-extraction-qc3.md` | Approve |

## Review scope

- **plan_id**: `2026-04-17-v1.4-ws1-acp-host-extraction`
- **Working branch**: `feature/v1.4` (merged from `feature/v1.4-ws1-acp-host`)
- **Review range**: `git diff main...HEAD -- crates/ .github/workflows/ Cargo.toml Cargo.lock`
- **Wave-0 spec**: `archived/archived/knowledge/acp-client-tech-spec.md` §11

## Findings analysis

### Critical — 0

No critical/blocking issues identified by any reviewer.

### Warnings — triaged

| ID | Source | Finding | PM Triage | Disposition |
|----|--------|---------|-----------|-------------|
| W1 | QC1 | Missing `capabilities/session_persistence.rs` and `capabilities/session_modes.rs` per spec §11.2/§5.2a; V1.4 capability IDs not declared in `skills.rs` | **Non-blocking** — these are WS2 scope (orchestration capability registration), not WS1 extraction responsibility | Deferred to WS2 |
| W2 | QC1 | `SimpleClientHandler` API surface change — removed `daemon_client` parameter | **Non-blocking** — intentional design decision documented in Completion Report (field was never used) | Design-as-intended |
| W-01 | QC2 | `tempfile` in `[dependencies]` instead of `[dev-dependencies]` in `nexus-acp-host/Cargo.toml` | **Should fix** — trivial (move one line) | **Fixed** in commit `d95007a` |
| W-02 | QC2 | `session_capture` re-exports removed from compat shim — API break | **Non-blocking** — `session_capture.rs` intentionally left in nexus42 due to deep coupling to `api::daemon_client`, `config`, `errors` | Design-as-intended |

### Suggestions — noted, not blocking

| ID | Source | Finding | Disposition |
|----|--------|---------|-------------|
| S1 | QC1 | Unused `nix` dep possible duplication | Accepted — `nix` also used by `daemon.rs` for process management |
| S2 | QC1 | `tempfile`/`toml` could be dev-deps | `toml` is production (config parsing); `tempfile` fixed in W-01 |
| S3 | QC1 | CI regex adequacy | Sufficient for V1.4; enhance later if needed |
| S4 | QC1 | Unused dep audit | Acceptable as-is |
| S-01 | QC2 | CI regex comment edge case | Low priority |
| S-02 | QC2 | No acp-worker integration test | Deferred — WS2 grows the stub |
| S3 | QC2 | `serde_json` in worker stub | Acceptable for Phase-1 |
| S4 | QC2 | Stub could validate required args | Low priority |
| S1 | QC3 | CI regex comment | Low priority |
| S2 | QC3 | No acp-worker integration test | Deferred |

## Blocking Items

None.

## Residual Findings

No formal residual findings. All warnings were either design-as-intended decisions or deferred to subsequent workstreams (WS2 capability registration).

## Assigned Fix Owners

- `fullstack-dev` — W-01 (tempfile dep): **Fixed** in commit `d95007a fix(acp-host): move tempfile to dev-dependencies (QC2-W01)`

## Next Step

QA verification → Done → merge to `feature/v1.4`.
