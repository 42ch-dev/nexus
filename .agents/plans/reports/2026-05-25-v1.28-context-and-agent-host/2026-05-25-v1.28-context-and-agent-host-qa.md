# QA Report

**Report Kind**: qa  
**Role**: qa-engineer  
**Runtime Agent ID**: qa-engineer  
**Checkout**: `/Users/bibi/workspace/organizations/42ch/nexus`  
**Working branch**: `feature/v1.28-context-and-agent-host`  
**plan_id**: `v1.28-context-and-agent-host`  
**Report Timestamp**: 2026-05-25T00:00:00Z  

---

## Scope Tested

- **Crates**: `nexus42`, `nexus-moment-context-assembly`, `nexus-agent-host`
- **Diff basis**: `merge-base: origin/main` + `tip: HEAD` on `feature/v1.28-context-and-agent-host`
- **Success criteria**: 6 criteria from delivery compass §1.4

## Findings

### Criterion 1: No `assemble-local` subcommand; `assemble-moment --help` documents Stage0 + KB query flags

**Verdict: ✅ PASS**

- `rg 'assemble-local|AssembleLocal' crates/nexus42/src/` returns zero hits in production command tree. All hits are within compiled test assertions that verify the removal (e.g., `!non_test.contains("AssembleLocal")`).
- `assemble-moment` flags in `crates/nexus42/src/commands/platform/context.rs` include: `--max-tokens`, `--no-fragments`, `--hint`, `--kb-limit`, `--kb-search`, `--kb-type`, `--knowledge-limit` — all frozen flags per cli-spec §6.6.
- cli-spec.md (§6.6) documents the breaking change: `assemble-local` is **removed**; `assemble-moment` is the single local assembly SSOT.

### Criterion 2: `rg 'AssembleLocal' crates/nexus42` — zero in production command tree

**Verdict: ✅ PASS**

- All grep matches for `AssembleLocal` are within inline test assertions (`#[cfg(test)]`) explicitly verifying the removal.
- No `AssembleLocal` variant in `ContextCommand`, no `assemble-local` subcommand registration.

### Criterion 3: `cargo test` passes for all 3 crates

**Verdict: ✅ PASS**

| Crate | Tests | Result |
|-------|-------|--------|
| `nexus42 --lib` | 603 passed | 0 failed |
| `nexus-moment-context-assembly --lib` | 21 passed | 0 failed |
| `nexus-agent-host` | 182 passed (lib) + 0 doc-tests | 0 failed |
| **Total** | **806** | **0 failed** |

### Criterion 4: DF-18–DF-26 marked Shipped in deferred tracker

**Verdict: ✅ PASS**

All six deferred features in `.agents/knowledge/deferred-features-cross-version-tracker.md`:
- **DF-18** (Native multi-turn) → `V1.28 ✅ Shipped`
- **DF-19** (ACP session/request_permission) → `V1.28 ✅ Shipped`
- **DF-20** (SetModel/SetMode truthfulness) → `V1.28 ✅ Shipped`
- **DF-24** (HostManager shutdown→adapter) → `V1.28 ✅ Shipped`
- **DF-25** (AdmissionPolicy enforcement) → `V1.28 ✅ Shipped`
- **DF-26** (Cross-platform `which`) → `V1.28 ✅ Shipped`

### Criterion 5: `docs/ARCHITECTURE.md` and `cli-spec.md` match V1.27/28 product wiring

**Verdict: ✅ PASS**

- `cli-spec.md` (at `.agents/knowledge/specs/cli-spec.md` per compass §5.2) was updated in commit `f68a932`:
  - `assemble-local` → removed; `assemble-moment` → **single local four-domain Moment SSOT**
  - Frozen flags listed: `--max-tokens`, `--no-fragments`, `--hint`, `--kb-limit`, `--kb-search`, `--kb-type`, `--knowledge-limit`
  - Breaking change noted: "assemble-local is removed. Use assemble-moment"
  - PD-01 enforced: World fork is platform-only
- `docs/ARCHITECTURE.md` was updated in same commit:
  - Evidence dates refreshed to 2026-05-25
  - Product integration gaps table updated (assemble-moment SSOT, narrative read-only routes, SQLite user knowledge)
  - Narrative routes (`GET /v1/local/narrative/*`) registered
  - KCA-002 marked as shipped; assemble-local removed
  - World fork explicitly marked platform-only (PD-01)

### Criterion 6: No new platform HTTP client code; no local world fork implementation

**Verdict: ✅ PASS**

- **Platform HTTP**: The `reqwest` references in `crates/nexus42/src/commands/platform/context.rs` are pre-existing (`git diff origin/main...HEAD` shows zero new `reqwest`/`hyper` additions in the diff).
- **Local world fork**: `rg 'world.*fork|WorldFork|fork_world' crates/nexus42/src/` returns zero results. PD-01 in the deferred tracker and entity-scope-model.md confirms: World fork is **platform-only**; no local fork CLI or daemon surface exists.

## Evidence

### Build

```
Finished `dev` profile [unoptimized + debuginfo] target(s) in 1m 03s
```

### Test Output

```
test result: ok. 603 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
test result: ok. 21 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
test result: ok. 182 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

### Clippy

```
Finished `dev` profile (clippy clean, exit 0)
```

### Format Check

```
cargo +nightly fmt --all -- --check — clean (no output, exit 0)
```

### AssembleLocal Removal

```
rg 'assemble-local|AssembleLocal' crates/nexus42/src/ — zero production hits
(all hits are in #[cfg(test)] assertions verifying the removal)
```

## Not Tested

- Daemon runtime integration (daemon start + assemble-moment via CLI)
- Platform cloud context assembly (deferred, not in scope)
- End-to-end CLI `assemble-moment --world-id <id>` with populated World/KB
- Cross-platform build (Windows) — macOS-only environment

## Overall QA Verdict

**PASS** — All 6 success criteria verified with reproducible evidence. No regressions detected in 806 tests. Clippy clean, fmt clean. No AssembleLocal residue in production code. All DF-18/19/20/24/25/26 marked Shipped. Architecture doc and cli-spec match V1.27/28 product wiring. No leaked platform HTTP or local world fork code.