---
report_kind: qa
plan_id: "2026-04-30-v1.12-theme-a-preset-completion"
generated_at: "2026-04-30"
verdict: "Pass"
---

# QA Verification Report — V1.12 Theme A Preset Module Completion

## Scope Verified

- **plan_id**: 2026-04-30-v1.12-theme-a-preset-completion
- **Review range / Diff basis**: feature/v1.12..HEAD
- **Working branch (verified)**: feature/v1.12-theme-a
- **Review cwd (verified)**: /Users/bibi/workspace/organizations/42ch/nexus
- **Agent**: @qa-engineer
- **Runtime Model**: qwen3.6-plus
- **Report Timestamp**: 2026-04-30T00:00:00Z

## Acceptance Criteria Results

### AC1: `preset list` works without daemon running — PASS

**Evidence:**
- `crates/nexus42/build.rs` scans `crates/nexus-orchestration/embedded-presets/` at compile time, generating `EMBEDDED_PRESET_IDS` const array in `$OUT_DIR/embedded_preset_ids.rs`.
- `crates/nexus42/src/commands/preset.rs:19` includes the generated file: `include!(concat!(env!("OUT_DIR"), "/embedded_preset_ids.rs"))`.
- `list_presets_offline()` (line 155) reads embedded presets from `EMBEDDED_PRESET_IDS.to_vec()` — no daemon connection required.
- System and user presets are scanned directly from the filesystem.
- Module doc comment (line 7-9): "`preset list` works offline — embedded preset IDs come from compile-time data generated by build.rs".

### AC2: YAML depth and size limits enforced with clear error messages — PASS

**Evidence:**
- `crates/nexus-orchestration/src/preset/loader.rs:73-76`: `DEFAULT_MAX_YAML_SIZE = 1024 * 1024` (1 MiB), `DEFAULT_MAX_YAML_DEPTH = 10`.
- `load_preset_from_str_with_limits()` (line 172):
  - Step 0a (line 178-184): Size check BEFORE parse — returns `PresetLoadError::YamlSizeExceeded` with actionable message including actual vs limit.
  - Step 0b (line 186-195): Depth check on parsed `serde_yaml::Value` — returns `PresetLoadError::YamlDepthExceeded` with actionable message.
- Unit tests exist in `loader.rs`:
  - `reject_oversized_yaml()` (line 2155)
  - `reject_deeply_nested_yaml()` (line 2184)
  - `normal_yaml_passes_size_and_depth_limits()` (line 2214)
  - `load_preset_from_str_with_limits_allows_higher_limits()` (line 2242)

### AC3: `preset validate` subcommand exists and reports invalid YAML — PASS

**Evidence:**
- `PresetCommand::Validate { path }` registered at `crates/nexus42/src/commands/preset.rs:63-65`.
- Subcommand parsed correctly: `preset_validate_parses()` test (line 498).
- Accepts valid presets: `validate_accepts_valid_preset()` test (line 508).
- Rejects invalid YAML: `validate_rejects_invalid_yaml()` test (line 538) — returns "YAML parse error".
- Rejects non-existent files: `validate_rejects_nonexistent_file()` test (line 553) — returns "File not found".
- Read-only operation confirmed: `validate_preset()` (line 289-392) performs only reads, parses, and checks — no file writes, no state mutations.

### AC4: `_`-prefixed preset handling is consistent — PASS

**Evidence:**
- `preset init` accepts `_`-prefixed names (except `_system`): `init_preset_at()` (line 100-145) rejects only `_system`, `.`, `..`, empty, and path separators.
- `init_preset_accepts_underscore_prefix()` test (line 485): confirms `_draft` is accepted and creates directory.
- `preset list` warns on `_`-prefixed entries: `list_presets_offline()` (lines 188-260) scans for `_`-prefixed dirs, collects them in `underscore_ids`, and prints warning to stderr (line 253-259): "⚠ Warning: the following user presets start with '_' and are skipped during preset resolution".
- Scan logic in `user_preset_dir.rs:114` silently skips `_`-prefixed directories — consistent with the warning.

### AC5: Large preset collections resolve efficiently — PASS

**Evidence:**
- `UserPresetScanResult` (user_preset_dir.rs:38-48) contains `index: HashMap<String, usize>` mapping preset ID → Vec position.
- Index built at scan time (line 147): `result.index.insert(entry.id.clone(), idx)`.
- O(1) lookup: `find_user_preset()` (line 246-251): `result.index.get(id).map(|&idx| &result.presets[idx])`.
- Cache invalidation via mtime: `is_scan_cache_fresh()` (line 260-307) compares current mtime with cached mtime, detects modifications, additions, and removals.
- Tests:
  - `find_user_preset_is_o1_with_index()` (line 539) — verifies O(1) lookup across 3 presets.
  - `index_size_matches_preset_count()` (line 568) — index size matches preset count.
  - `scan_cache_fresh_when_no_changes()` (line 591)
  - `scan_cache_stale_after_modification()` (line 610)
  - `scan_cache_stale_after_new_preset_added()` (line 635)
  - `scan_cache_stale_after_preset_removed()` (line 661)

### AC6: AGENTS.md accurately describes preset module — PASS

**Evidence:**
- `crates/nexus-orchestration/AGENTS.md` has been updated:
  - "User-Installed Presets" section (lines 17-28) describes the composable search order correctly.
  - "Preset Resolution" section documents `scan_user_presets()`, `find_user_preset()` O(1) lookup, and mtime-based cache invalidation.
  - "YAML Hardening" section documents `load_preset_from_str_with_limits()` limits.
  - "CLI Validation" section documents `nexus42 preset validate`.
  - No "Not Yet Implemented" language remains. No "load_preset() is a stub" language remains.

### AC7: Test suites pass — PASS (with caveat)

| Suite | Result | Notes |
|-------|--------|-------|
| `cargo test -p nexus-orchestration` | **PASS** | 237 passed, 0 failed, 1 ignored (network-dependent) |
| `cargo test -p nexus42` (preset tests) | **PASS** | All 12 preset-related tests pass |
| `cargo test -p nexus42` (full) | **2 FAILED** | Pre-existing auth test failures — NOT in Theme A scope |
| `cargo clippy -p nexus42 -p nexus-orchestration -- -D warnings` | **PASS** | Clean, no warnings |
| `cargo +nightly fmt --all -- --check` | **PASS** | Clean (fixed minor formatting issue in `mod.rs` line 19-23) |

**Pre-existing test failures (NOT caused by Theme A changes):**
- `auth::user_auth::tests::ensure_valid_token_no_token_returns_error` — expects "Not authenticated" but gets "Access token expired..."
- `auth::user_auth::tests::ensure_valid_token_valid_token_skips_refresh` — unexpected token expiry error
- Both failures are in `crates/nexus42/src/auth/user_auth.rs` — **zero auth files changed in Theme A diff**. QC2 and QC3 both confirmed these as pre-existing.

### AC8: QC warning fixes verified — PASS

| QC Warning | Fix | Verification |
|------------|-----|-------------|
| **W-001** (QC3): Unbounded allocation in `validate_preset` — file read before size check | File size checked via `std::fs::metadata(file_path).len()` BEFORE `read_to_string` (preset.rs:301-310) | Confirmed: metadata check at line 301-310, defense-in-depth check at line 317-324 |
| **W-002** (QC3): Double-parse widens stack-overflow surface | Uses `serde_yaml::from_value(yaml_value)` for second deserialization (preset.rs:343, loader.rs:199-200), avoiding re-parse from string | Confirmed: single parse to Value, then from_value for manifest |
| **W-A01** (QC1): Duplicate `yaml_value_depth` violates single truth source | CLI imports from `nexus_orchestration::preset::yaml_value_depth` (preset.rs:332) — `yaml_value_depth` re-exported as `pub` from `nexus-orchestration/src/preset/mod.rs:21` | Confirmed: single definition in `loader.rs:917-939`, re-exported in `mod.rs:21`, CLI uses it at line 332 |
| **W-A02** (QC1): Cross-crate build-time dependency without explicit contract | `build.rs` has comprehensive documentation header (lines 1-27) describing the contract, what it reads, why, and what breaks if the contract changes | Confirmed: build.rs lines 1-27 document the contract |

## Phase Gate Checklist

- [x] Plan file exists at `.agents/plans/2026-04-30-v1.12-theme-a-preset-completion.md`
- [x] Plan has `clarify` equivalent (design decisions documented in compass + plan)
- [x] Plan has `tasks` table (T1-T6 with dependencies and done criteria)
- [x] Implementation matches plan scope — no "out-of-scope" changes detected in diff
- [x] All 6 tasks completed per done criteria
- [x] All 4 residuals (R-M1-W03/W04/W05/W06) addressed
- [x] 4 QC warnings (W-001, W-002, W-A01, W-A02) fixed
- [x] No new medium+ residuals introduced

## Not Tested

- Live CLI execution against real daemon (offline-only verification performed)
- Preset loading with >100 real user presets (unit tests cover 5-preset scenarios)
- Platform cross-repo integration (`@42ch/nexus-contracts` npm consumption)

## QA Verdict

**PASS** — All 8 acceptance criteria verified with evidence. The 2 failing auth tests are pre-existing, confirmed by QC2 and QC3 as unrelated to Theme A changes (no auth files in diff). All QC warnings have been addressed with surgical fixes.

## Issues Found

1. **Pre-existing auth test flakiness**: 2 tests in `user_auth.rs` fail due to token expiry logic unrelated to preset changes. These should be tracked as a separate residual.
2. **Minor formatting issue**: `mod.rs` lines 19-23 had a formatting drift that was fixed with `cargo +nightly fmt --all` during verification.

## Completion Report v2

**Agent**: @qa-engineer
**Plan ID**: 2026-04-30-v1.12-theme-a-preset-completion
**Status**: Done
**Scope Delivered**: Full Theme A verification — 8 acceptance criteria verified, 4 QC warnings confirmed fixed, 4 residuals confirmed closed.
**Artifacts**: This QA report, command output from cargo test/clippy/fmt.
**Validation**: feature/v1.12-theme-a branch at HEAD (1712075), Review cwd /Users/bibi/workspace/organizations/42ch/nexus, Review range feature/v1.12..HEAD.
**Issues/Risks**: 2 pre-existing auth test failures (unrelated to Theme A scope). Minor fmt drift fixed.
**Plan Update**: All acceptance criteria marked [x] in plan file are verified. QA verdict: Pass.
**Handoff**: @project-manager — QA complete, ready for merge decision.
