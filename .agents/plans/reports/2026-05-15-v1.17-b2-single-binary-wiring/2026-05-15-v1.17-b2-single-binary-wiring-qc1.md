---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-05-15-v1.17-b2-single-binary-wiring"
verdict: "Approve"
generated_at: "2026-05-15"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: glm-5
- Review Perspective: Architecture coherence and maintainability risk
- Report Timestamp: 2026-05-15T14:30:00Z

## Scope
- plan_id: `2026-05-15-v1.17-b2-single-binary-wiring`
- Review range / Diff basis: `git diff main...HEAD`
- Working branch (verified): `feature/v1.17-b2-single-binary-wiring`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus`
- Files reviewed: 10
- Commit range: `bd2e1dc..6e61290`
- Tools run: `cargo clippy --all`, `cargo check -p nexus42 --all-targets`, `git diff`

## Findings

### 🔴 Critical

None identified.

### 🟡 Warning

#### W-001: Boot.rs function length exceeds ergonomic threshold

- **Location**: `crates/nexus-daemon-runtime/src/boot.rs:119` (`run_daemon`)
- **Issue**: The `run_daemon()` function is ~280 lines with `#[allow(clippy::too_many_lines)]`. While the comment explains this is inherent to orchestrated initialization, the cognitive load for maintenance is elevated.
- **Impact**: Moderate — increases risk of errors during future modifications to initialization sequence.
- **Fix**: Consider extracting logical blocks into helper functions (e.g., `init_workspace()`, `wire_engine()`, `wire_schedule_supervisor()`, `setup_signal_handlers()`). Defer to Batch 4 (hardening) if not urgent.
- **Decision**: Accept — documented rationale is valid; defer decomposition to B4.

#### W-002: Panic on schedule supervisor pool failure is documented but operationally risky

- **Location**: `crates/nexus-daemon-runtime/src/boot.rs:210-213`
- **Issue**: `expect("open pool for schedule supervisor")` will panic if database pool cannot be opened. While documented in the function's `# Panics` section, a corrupted database in production will crash without graceful shutdown.
- **Impact**: Moderate — could leave in-flight schedules orphaned if database corruption occurs.
- **Fix**: Consider returning an error instead of panicking, allowing the lifecycle HSM to enter a degraded state and attempt graceful shutdown of other subsystems.
- **Decision**: Accept — mirrors original binary behavior; B4 hardening can refine.

### 🟢 Suggestion

#### S-001: Consider adding #[must_use] to run_daemon()

- **Location**: `crates/nexus-daemon-runtime/src/boot.rs:119`
- **Rationale**: The function returns `anyhow::Result<()>`. Adding `#[must_use]` encourages callers to handle errors rather than silently ignoring them.
- **Impact**: Low — improves correctness hygiene.
- **Recommendation**: Add in B4 hardening batch.

#### S-002: Non-Unix platform detach behavior is implicit

- **Location**: `crates/nexus42/src/commands/daemon.rs:233-236`
- **Issue**: On non-Unix platforms, `child.try_wait()` is used but there's no explicit detach mechanism. The child may keep running but could potentially block or create zombie processes depending on platform behavior.
- **Impact**: Low — Windows/macOS non-Unix behavior is niche for this daemon.
- **Recommendation**: Document platform limitations in function doc; defer to B4 if Windows support is needed.

#### S-003: Transport enum duplication across B1/B2 boundaries

- **Location**: `crates/nexus-daemon-runtime/src/boot.rs:40-46` and former `nexus42d/main.rs`
- **Issue**: The `Transport` enum was duplicated during extraction. It's correctly placed in `boot.rs` now, but `nexus42d/main.rs` still references it (via the runtime).
- **Impact**: Low — B3 will remove `nexus42d` entirely.
- **Recommendation**: Confirm duplication is removed in B3 when deleting `nexus42d`.

#### S-004: Panic hook chains to default abort behavior

- **Location**: `crates/nexus-daemon-runtime/src/boot.rs:272-282`
- **Issue**: The panic hook dispatches `FatalError` to lifecycle then calls `previous_hook(info)`. The default Rust panic hook will eventually abort the process, potentially before lifecycle HSM finishes processing.
- **Impact**: Low — acceptable for supervisor behavior; not a correctness issue.
- **Recommendation**: Consider adding a brief sleep or synchronization if panic recovery logging is critical, but current behavior is acceptable.

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|------------|-------------|------------------|------------|
| W-001 | linter | `clippy::too_many_lines` annotation at line 118 | High |
| W-002 | manual-reasoning | `expect()` call at line 212-213, documented in `# Panics` | High |
| S-001 | doc-rule | Rust API guidelines for `#[must_use]` | Medium |
| S-002 | manual-reasoning | `#[cfg(not(unix))]` block at line 233-236 | Medium |
| S-003 | git-diff | Comparison of Transport enum positions | High |
| S-004 | manual-reasoning | Panic hook implementation at lines 272-282 | Medium |

## Architecture Coherence Assessment

### Boot Sequence Extraction ✅

The extraction from `nexus42d/main.rs` to `nexus-daemon-runtime/src/boot.rs` is clean and follows spec Section 3.2:

- `DaemonConfig` struct centralizes all daemon configuration with clear defaults
- `resolve_transport()` method handles priority chain correctly (CLI → env → defaults)
- Initialization sequence is preserved: workspace → engine → schedule supervisor → lifecycle → signal handlers → server
- Tests cover transport resolution logic

### Self-Spawn Pattern ✅

Background mode correctly implements spec Section 4.2:

- Uses `std::env::current_exe()` for self-spawn — no external binary lookup
- Hidden `__internal daemon-run` command is correctly gated with `#[command(hide = true)]`
- Unix: `process_group(0)` for detachment, immediate `wait()` to reap intermediate
- Non-Unix: `try_wait()` for non-blocking check
- Health check polling loop confirms daemon started before parent exits

### Signal Handler / PID File Safety ✅

- Signal handlers registered in spawned tokio task — doesn't block initialization
- PID file uses 0600 permissions on Unix (owner-only)
- Foreground mode: writes own PID, cleans up on exit
- Background mode: writes child PID before parent exits
- Health check loop validates daemon is running before success message

### Parallel Compile Path Viability ✅

- `nexus42d/main.rs` is now ~60 lines — thin wrapper calling `run_daemon()`
- Cargo.lock correctly updated with `nexus-daemon-runtime` dependency in `nexus42`
- Both binaries compile and link to same runtime
- B3 will cleanly remove `nexus42d` without breaking `nexus42`

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 2 |
| 🟢 Suggestion | 4 |

**Verdict**: **Approve**

The implementation correctly follows the spec for single-binary wiring. The boot sequence extraction preserves all lifecycle, signal handling, and subsystem wiring behavior. The self-spawn pattern for background mode is correct and platform-aware. PID file handling is safe with proper permissions and cleanup. The parallel compile path (keeping `nexus42d` as thin wrapper) is viable for B3 removal.

Warnings W-001 and W-002 are deferred to B4 (hardening) per spec Section 9 batch strategy. No blocking issues found.