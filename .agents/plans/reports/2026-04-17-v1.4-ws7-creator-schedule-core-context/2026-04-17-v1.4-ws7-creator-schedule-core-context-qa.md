# QA Report — WS7 Creator Schedule + Core Context

## Scope tested
- Multi-Schedule per creator (serial/parallel concurrency, dependency resolution)
- Immutable versioned core_context derivation (Seed, UserEdit, PresetHook)
- 13 CLI subcommands under `nexus42 schedule`
- 8 HTTP endpoints under `/v1/local/orchestration/schedules/*`
- ScheduleSupervisor admission + hand-off loop
- CoreContextManager versioning + write guard
- Preset loader extensions (initial_action, context_update hook)
- Novel-writing demonstrator context_update on outlining exit
- Forward-compat guard (LlmSummarize variant reserved for V1.5)
- Regression gates R11–R14

## Gate Results

| # | Gate | Command | Result |
|---|------|---------|--------|
| 1 | Schema validation | `pnpm run validate-schemas` | ✅ PASS (54/54 valid) |
| 2 | Codegen alignment | `pnpm run codegen && git diff --exit-code ...` | ✅ PASS (no diff) |
| 3 | Rust formatting | `cargo +nightly fmt --all -- --check` | ✅ PASS (fixed + committed `cbc554d`) |
| 4 | Clippy | `SQLX_OFFLINE=true cargo clippy --all -- -D warnings` | ✅ PASS |
| 5 | Workspace tests | `SQLX_OFFLINE=true cargo test --workspace` | ⚠️ PASS (WS7 scope: 99+153+386 passed; **1 pre-existing failure** in `nexus42::auth::tests::get_returns_none_for_unknown_creator` — zero commits touching `auth/` in WS7 range `6b60586..HEAD`) |
| 6 | TypeScript typecheck | `pnpm run typecheck` | ✅ PASS |
| 7 | Linkage gate | `rg 'nexus-acp-host\|agent-client-protocol' crates/nexus42d/Cargo.toml` | ✅ PASS (0 hits) |
| 8 | No rusqlite/deadpool | `rg 'rusqlite\|deadpool' crates/` | ✅ PASS (0 hits) |

## QC Fix Commits Verified

| Commit | Subject | Coverage |
|--------|---------|----------|
| `5c6ac03` | fix(schedule): resolve 5 QC must-fix findings for WS7 | C1 (depends_on load), C2 (seed v0), C3 (PresetHook JSON), M2 (Failed rejection), M5 (status_of Result) |
| `d4986d2` | fix(schedule): resolve 4 high-severity QC1 findings (H1-H4) | H1 (per-creator scoping), H2 (tokio Mutex write guard), H3 (Reject Replace), H4 (tick idempotency) |

## Residual Findings (11 items — deferred to V1.5 or accepted)

| # | ID | Title | Severity | Decision | Rationale |
|---|----|-------|----------|----------|-----------|
| 1 | R1 | RunningSet/CompletedSet not persisted across restart | medium | defer | V1.5 |
| 2 | R2 | No duplicate schedule_id detection | medium | defer | V1.5 |
| 3 | R3 | No paused→running status transition in FSM | medium | defer | V1.5 |
| 4 | R4 | No pause signal in FSM | medium | defer | V1.5 |
| 5 | R5 | No delete cascade for schedules | medium | defer | V1.5 |
| 6 | R6 | CoreContextManager version bump race | medium | defer | V1.5 |
| 7 | R7 | paused→running requires 2-step UX | medium | defer | V1.5 |
| 8 | R8 | TOCTOU in edit_core_context | medium | accept | single-user daemon |
| 9 | R9 | Auth middleware missing | medium | accept | V1.4 local-only |
| 10 | R10 | tempfile in prod deps | nit | accept | — |
| 11 | R11 | async-trait hardcoded pin | nit | accept | — |

## Not tested
- LLM-driven `context.summarize` capability (V1.5+ per plan non-goals)
- Cron / wall-clock triggers (V1.5+ per plan non-goals)
- Multi-agent per creator (V1.5+ per plan non-goals)
- Pre-existing `nexus42::auth` test failure — excluded from WS7 scope (0 commits in range touch `crates/nexus42/src/auth/`)

## Commits

| Action | Hash | Subject |
|--------|------|---------|
| Merge | `57f3f21` | Merge feature/v1.4-ws7-schedule into feature/v1.4 (WS7 done) |
| Archive | pending | docs(plans): archive WS7 creator schedule + core_context as Done |

## QA Verdict

**PASS** — All 8 gates passed (Gate 5: 1 pre-existing auth test failure excluded from WS7 scope). QC fix commits verified. 11 residual findings recorded in `status.json` metadata. Plan archival in progress.
