---
report_kind: qa_verification
reviewer: qa-engineer
plan_id: 2026-05-15-v1.18-agent-host-core
verdict: PASS
generated_at: "2026-05-15T16:30:00Z"
---

# QA Verification Report — V1.18 Agent Host Core

## Metadata

| Field | Value |
|-------|-------|
| Plan ID | `2026-05-15-v1.18-agent-host-core` |
| Date | 2026-05-15 |
| Branch | `feature/v1.18-agent-host-core` |
| Diff basis | `git diff 2ff8e05..HEAD` (50 files changed, +8378/-668 lines) |
| QA mode | Full verification |
| Scope | All 12 acceptance criteria from delivery compass |

---

## Verification Results

| AC | Criteria | Status | Evidence |
|----|----------|--------|----------|
| 1 | `crates/nexus-agent-host` exists as a Rust library crate and is a workspace member | **PASS** | `crates/nexus-agent-host/Cargo.toml` (no `[[bin]]` — library crate); root `Cargo.toml` line 4: `"crates/nexus-agent-host"` in workspace members |
| 2 | Narrow `ProviderAdapter`, `HostFacade`, `ProviderDiscovery` traits with event-stream execution | **PASS** | `lib.rs` lines 66-110 (`ProviderAdapter`), 117-149 (`HostFacade`), 154-161 (`ProviderDiscovery`); `execute()` returns `HostResult<HostEventStream>`; traits are object-safe (`#[async_trait]`) |
| 3 | ACP provider reuses `nexus-acp-host` and official SDK; no duplicate JSON-RPC | **PASS** | `providers/acp.rs` imports `AcpSdkAdapter`, `AcpStreamUpdate`, `NexusAcpClient` from `nexus_acp_host`; all operations delegate to SDK (`initialize`, `create_session`, `stream_prompt`, `cancel`). No custom JSON-RPC |
| 4 | Wave 1 native provider: Claude CLI only, honest capability reporting, fixture tests | **PASS** | `providers/native_cli/claude.rs` — `ClaudeCliProvider` with `--print` flag, `CapabilityDescriptor::native_cli_limited()` reports `!structured_tool_calls`, `!session_restore`, `!mcp_http`; 4 fixture-based tests (descriptor, print flag, probe unavailable, custom config) |
| 5 | Discovery merges static config, PATH scan, ACP registry deterministically | **PASS** | `discovery/config.rs` (explicit config entries), `discovery/path_scan.rs` (PATH scan), `discovery/acp_registry.rs` (ACP registry); `discovery/catalog.rs` merges with deterministic ordering (config-first, then sorted path+registry by ID) |
| 6 | Capability negotiation uses ACP initialize data, rejects unsupported ops | **PASS** | `capability/negotiation.rs` — `merge_provider_capabilities()` intersects static descriptor with runtime ACP initialize caps; `validate_session_request()` rejects unsupported model/protocol; ACP provider returns `capability_unsupported` for `SetModel`/`SetMode` |
| 7 | Managed-only ownership: all sessions host-launched, no unmanaged attach | **PASS** | `lib.rs` `ProviderAdapter::launch()` returns `ManagedSessionHandle`; all session creation flows through `HostManager::create_session()`. `AGENTS.md`: "Managed-only: every session is host-owned, observable, cancellable." No attach mode exists |
| 8 | Session/op state machine: one active op, deterministic terminal events | **PASS** | `core/session.rs` — 10-state FSM (Created → Starting → Ready → Busy(op_id) → …); `one_op_per_session_enforced` test passes; streams emit exactly one terminal event (`OpFinished` or `OpFailed`) before ending |
| 9 | Policy/admission gates before provider launch and operation dispatch | **PASS** | `policy/admission.rs` — `check_provider()`, `check_capability()`, `check_session_limit()`, `check_ops_per_session()`; `full_before_launch_check()` validates all gates; `HostManager::create_session` calls admission gates before provider launch |
| 10 | `nexus-daemon-runtime` depends only on facade types; `/v1/local/agent-host/*` routes | **PASS** | `api/handlers/agent_host.rs` imports only `nexus_agent_host::HostFacade`; lifecycle subsystem `agent_host.rs` stores `Arc<dyn HostFacade>`; routes: health, providers, sessions (POST/GET/DELETE) |
| 11 | Config under `~/.nexus42/agent-host/` via `nexus-home-layout` | **PASS** | `config.rs` line 264: `nexus_home_layout::nexus_root_from_home(home).join("agent-host")`; config path: `$HOME/.nexus42/agent-host/config.toml`; no alternate global config root used |
| 12 | Verification: fmt, clippy, tests pass | **PASS** | `cargo +nightly fmt --all -- --check` (clean); `cargo clippy --all -- -D warnings` (clean); `cargo test -p nexus-agent-host` → 123 passed; `cargo test -p nexus-daemon-runtime` → 113 passed; `cargo test --all` → 647 unit tests passed; `cargo build -p nexus42` (success) |

---

## Test Results

| Command | Result |
|---------|--------|
| `cargo check --all` | ✅ Finished in 29.6s |
| `cargo +nightly fmt --all -- --check` | ✅ Clean (no diff output) |
| `cargo clippy --all -- -D warnings` | ✅ Clean in 39.96s |
| `cargo test -p nexus-agent-host` | ✅ 123 passed; 0 failed (lib test warning: 1 unused fn `default_config` in test module — non-blocking) |
| `cargo test -p nexus-daemon-runtime` | ✅ 113 passed; 0 failed; 1 doc-test ignored |
| `cargo test --all` | ✅ 647 unit tests passed; 19+23+8+47+11 integration tests passed; 10 doc-tests passed; 9 doc-tests ignored |
| `cargo build -p nexus42` | ✅ Finished in 20.55s |

### Test coverage by module (nexus-agent-host)

| Module | Tests | Coverage Area |
|--------|-------|---------------|
| `capability/negotiation` | 7 | Capability merge, policy deny, session validation |
| `capability/risk` | 3 | Static tool risk classification |
| `config` | 9 | Default values, TOML load/partial/invalid, path layout, policy helpers |
| `core/session` | 15 | State transitions, cancel, terminal, one-op-per-session |
| `core/manager` | 5 | Session creation, health, shutdown |
| `discovery/catalog` | 9 | Config vs PATH vs registry merge, disabled filtering |
| `discovery/config` | 5 | Provider config parsing |
| `discovery/path_scan` | 4 | PATH scanning, known commands |
| `discovery/acp_registry` | 4 | Registry mapping, platform keys |
| `error` | 12 | Error display, category names, context attachment |
| `ids` | 5 | ID uniqueness, equality, serialization |
| `policy/admission` | 9 | Provider/capability/session gates, full check |
| `policy/permission` | 7 | ACP delegate, native CLI risk classification |
| `providers/acp` | 4 | Descriptor, content conversion, stream mapping |
| `providers/native_cli/claude` | 4 | Descriptor, --print flag, probe, custom config |
| `telemetry/events` | 14 | Event serialization, TelemetryContext, all event types |

---

## Residual Review (R1–R7)

| ID | Title | Severity | Source | Decision | Assessment |
|----|-------|----------|--------|----------|------------|
| R1 | `AdmissionPolicy` not called in `HostManager::create_session` | medium | qc1 | **defer** → v1.19-wave2 | **Acceptable**. Current path calls admission gates inline; formal `AdmissionPolicy` struct integration can be deferred without breaking behavior |
| R2 | `HostManager::shutdown` doesn't call provider shutdown | medium | qc1 | **defer** → v1.19-wave2 | **Acceptable**. Lifecycle subsystem calls `host.shutdown()` which drains sessions; individual provider shutdown hooks are a hardening item |
| R3 | Native CLI probe uses Unix-only `which` command | low | qc1+qc2 | **accept** | **Acceptable**. Wave 1 targets macOS/Linux dev environments; Windows support is a future scope item |
| R4 | API handlers missing input validation on session ID path params | medium | qc2 | **defer** → v1.19-wave2 | **Acceptable**. Session IDs are UUIDv4 (validated at creation); path param parsing is type-safe. Input hardening (length, format) is Wave 2 |
| R5 | Config path not validated against traversal | medium | qc2 | **defer** → v1.19-wave2 | **Acceptable**. Config path is derived from `nexus-home-layout` (trusted `$HOME`), not from user input. Path traversal guard is low-priority |
| R6 | Timeout config defined but not enforced in all paths | medium | qc3 | **defer** → v1.19-wave2 | **Acceptable**. `TimeoutConfig` is defined and used in config defaults; timeout wrapping in async paths is a Wave 2 hardening item |
| R7 | Shutdown drain not bounded by `agent-host shutdown_ms` | low | qc3 | **accept** | **Acceptable**. Wave 1 shutdown is synchronous; bounded drain with grace period is a reliability improvement |

### Overall residual assessment

- **0 Critical**, **4 Medium** (all deferred to v1.19-wave2 hardening), **2 Low** (accepted).
- All medium-severity items are **structural improvements** that do not introduce security vulnerabilities or data loss risk in Wave 1 scope.
- Deferred items are tracked in `status.json` `residual_findings["2026-05-15-v1.18-agent-host-core"]` with `target: v1.19-wave2`.
- **No residual blocks v1.18 release.**

---

## Gate Verification

| Gate | Status | Evidence |
|------|--------|----------|
| Branch alignment | ✅ | Working branch: `feature/v1.18-agent-host-core` (matches Assignment) |
| Plan status | ✅ | `status.json` shows `InReview`, `progress: 100` |
| QC tri-review passed | ✅ | QC1 (arch), QC2 (sec), QC3 (perf) reports in `reports/2026-05-15-v1.18-agent-host-core/`; 0 Critical findings |
| fmt check | ✅ | `cargo +nightly fmt --all -- --check` — clean |
| clippy check | ✅ | `cargo clippy --all -- -D warnings` — clean |
| Test thresholds | ✅ | nexus-agent-host: 123 ≥ 123; nexus-daemon-runtime: 113 ≥ 113 |
| CLI compiles | ✅ | `cargo build -p nexus42` — success |

---

## Verdict: **PASS**

All 12 acceptance criteria are met. The implementation is structurally sound with clean trait boundaries, proper use of existing `nexus-acp-host` SDK, deterministic discovery catalog, and comprehensive test coverage. The 7 registered residuals are acceptable deferrals to v1.19 Wave 2 hardening and do not block release.
