---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: 2026-06-03-v1.32-orchestration-security-followup
verdict: Request Changes
generated_at: 2026-06-03T05:50:23Z
---

# Code Review Report

## Reviewer Metadata

- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: openai/gpt-5.5
- Review Perspective: Architecture coherence and maintainability risk
- Report Timestamp: 2026-06-03T05:50:23Z

## Scope

- plan_id: 2026-06-03-v1.32-orchestration-security-followup
- Review range / Diff basis: merge-base: feature/v1.32-preset-quality-gate..HEAD (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- Working branch (verified): feature/v1.32-orchestration-security-followup
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p3-security
- Files reviewed: 5
  - `crates/nexus-orchestration/src/capability/builtins/judge_llm.rs`
  - `crates/nexus-orchestration/src/capability/builtins/context_summarize.rs`
  - `crates/nexus-orchestration/src/capability/builtins/acp_prompt.rs`
  - `crates/nexus-orchestration/src/tasks/mod.rs`
  - `crates/nexus-orchestration/src/engine.rs`
- Commit range: `feature/v1.32-preset-quality-gate...HEAD` (tip `60b0f9f`)
- Tools run:
  - `git rev-parse --show-toplevel`
  - `git branch --show-current`
  - `git log --oneline -5`
  - `git diff feature/v1.32-preset-quality-gate...HEAD --stat`
  - `git diff feature/v1.32-preset-quality-gate...HEAD`
  - `cargo +nightly fmt --all --check`
  - `cargo test -p nexus-orchestration judge_llm 2>&1 | tail -30`
  - `cargo test -p nexus-orchestration context_summarize 2>&1 | tail -30`
  - `cargo test -p nexus-orchestration capability 2>&1 | tail -30`
  - `cargo clippy -p nexus-orchestration -- -D warnings 2>&1 | tail -30`
  - `rg 'creator_id|session_id|_creator_id|_session_id' crates/nexus-orchestration/src/capability/builtins/judge_llm.rs crates/nexus-orchestration/src/capability/builtins/context_summarize.rs crates/nexus-orchestration/src/capability/builtins/acp_prompt.rs`

## Checklist

- [x] 行为回归是否已被显式确认 — targeted tests pass, but see Critical C-001 for missing caller-path regression.
- [x] 阻塞级安全或数据一致性风险 — C-001 blocks closure because the context-injection contract is not enforced at the orchestration caller boundary.
- [x] 变更行为的测试覆盖是否充分 — direct capability tests exist; caller-path/session routing coverage is insufficient (C-001/S-001).
- [x] `Working branch` / `Branch policy` 一致 — verified `feature/v1.32-orchestration-security-followup`.
- [x] `Review cwd` 已确认 — verified `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p3-security`.
- [x] `plan_id` 与 `Review range` 逐字回写 — see Scope.
- [x] Pattern mirrors `acp.prompt` faithfully — direct capability extraction now uses `_creator_id` / `_session_id` and ignores raw fields.
- [x] No new abstractions beyond the existing pattern — no broad refactor introduced.
- [x] Tests are isolated and fast — targeted unit tests use mock providers.
- [ ] Internal callers still work with the new contract — not demonstrated; current `StateCompositeTask`/`CapabilityTask` path still passes raw `args` without merging context identity (C-001).

## Findings

### 🔴 Critical

- **C-001 — Generic preset capability calls do not inject `_creator_id` / `_session_id` into capability input, so the new contract is not wired at the orchestration boundary.**
  - Evidence: `StateCompositeTask::run` copies `EnterAction::Capability { args }` into `_capability_input` unchanged (`crates/nexus-orchestration/src/tasks/mod.rs:536-540`), then `CapabilityTask::run` passes that JSON directly to `cap.run(input)` (`crates/nexus-orchestration/src/tasks/mod.rs:66-76`). The changed capabilities now only read `input["_creator_id"]` / `input["_session_id"]` and otherwise fall back to `"default"` (`judge_llm.rs:92-103`, `context_summarize.rs:113-124`).
  - Impact: The code fixes direct spoofing inside `judge.llm` and `context.summarize`, but it does not implement the promised task-execution injection layer for generic preset capability actions. Legitimate internal executions that rely on graph context identity can silently route worker IPC to `default` instead of the active creator/session, and existing preset args cannot be used as a safe replacement because raw `creator_id`/`session_id` are now intentionally ignored.
  - Required fix: Before invoking a capability from `CapabilityTask`/`StateCompositeTask`, merge trusted context values into the capability input (at minimum `_creator_id` and `_session_id` when present), without allowing raw user/preset `creator_id` / `session_id` to override them. Add a regression test that exercises the actual task path, not only direct `cap.run(json!(...))`.

### 🟡 Warning

- None.

### 🟢 Suggestion

- **S-001 — Extend the mock-provider assertions to capture `session_id` as well as `creator_id`.** The direct regression tests are useful, but both mock providers only record `creator_id` while their comments claim `creator_id` and `session_id` are proven. Capturing both fields would make future regressions against session routing harder to miss.

- **S-002 — Consider a small shared helper after C-001 is fixed.** `acp.prompt`, `judge.llm`, and `context.summarize` now contain parallel `_creator_id` / `_session_id` extraction logic. This is acceptable for the narrow P3 fix and should not block, but a future helper could reduce drift once the task-injection seam is settled.

## Source Trace

- Finding ID: C-001
  - Source Type: manual-reasoning + git-diff
  - Source Reference:
    - `crates/nexus-orchestration/src/tasks/mod.rs:536-545`
    - `crates/nexus-orchestration/src/tasks/mod.rs:66-76`
    - `crates/nexus-orchestration/src/capability/builtins/judge_llm.rs:92-103`
    - `crates/nexus-orchestration/src/capability/builtins/context_summarize.rs:113-124`
    - Plan risk checklist: internal callers must still work with the new contract.
  - Confidence: High

- Finding ID: S-001
  - Source Type: manual-reasoning + test-review
  - Source Reference:
    - `crates/nexus-orchestration/src/capability/builtins/judge_llm.rs:216-240`
    - `crates/nexus-orchestration/src/capability/builtins/context_summarize.rs:224-245`
  - Confidence: High

- Finding ID: S-002
  - Source Type: architecture-review
  - Source Reference:
    - `crates/nexus-orchestration/src/capability/builtins/acp_prompt.rs:101-112`
    - `crates/nexus-orchestration/src/capability/builtins/judge_llm.rs:92-103`
    - `crates/nexus-orchestration/src/capability/builtins/context_summarize.rs:113-124`
  - Confidence: Medium

## Validation Evidence

- `cargo +nightly fmt --all --check` — pass (no output).
- `cargo test -p nexus-orchestration judge_llm 2>&1 | tail -30` — pass (exit code 0; tail shows filtered integration suites ok).
- `cargo test -p nexus-orchestration context_summarize 2>&1 | tail -30` — pass (exit code 0; tail shows filtered integration suites ok).
- `cargo test -p nexus-orchestration capability 2>&1 | tail -30` — pass (exit code 0; tail shows filtered integration suites ok).
- `cargo clippy -p nexus-orchestration -- -D warnings 2>&1 | tail -30` — pass (`Finished dev profile`).
- Manual `rg` confirms changed capabilities no longer call `.get("creator_id")` / `.get("session_id")` in production extraction, but direct tests and comments still mention raw spoof inputs.

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 1 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 2 |

**Verdict**: Request Changes

The local extraction pattern mirrors `acp.prompt`, and direct tests/lint pass. However, the orchestration task path still forwards preset `args` unchanged into `CapabilityTask`; it does not inject trusted context identity into the capability input. That leaves the new `_creator_id` / `_session_id` contract incomplete for internal callers and blocks closing `SEC-V131-01` as implemented.
