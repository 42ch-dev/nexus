---
report_kind: qc
reviewer: qc-specialist-3
reviewer_index: 3
plan_id: 2026-06-03-v1.32-preset-validator-core-and-cli
verdict: Approve
generated_at: 2026-06-03T00:00:00Z
revision: rev3
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-3
- Runtime Agent ID: qc-specialist-3
- Runtime Model: k2p6
- Review Perspective: Performance and reliability risk
- Report Timestamp: 2026-06-03T00:00:00Z

## Scope
- plan_id: 2026-06-03-v1.32-preset-validator-core-and-cli
- Review range / Diff basis: merge-base: feature/v1.32-preset-quality-gate..HEAD (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- Working branch (verified): feature/v1.32-preset-validator-core-and-cli
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p1-validator
- Files reviewed: 1
- Commit range: d179f95..e0f44c4
- Tools run: cargo +nightly fmt --check, cargo test -p nexus-orchestration preset, cargo test -p nexus-daemon-runtime preset, cargo clippy -p nexus-orchestration -p nexus-daemon-runtime -p nexus42 -- -D warnings

## Findings

### 🔴 Critical
_None._

### 🟡 Warning
_None._

### 🟢 Suggestion
- **S-001**: Minor O(n) redundancy in asset reference collection  
  `load_preset()` calls both `validate_path_safety()` and `validate_assets_in_bundle()`, and each internally calls `collect_asset_file_references(manifest)`, walking the manifest twice. For typical presets this is negligible (preset loading is not a hot path), but a future refactor could collect once and pass the `Vec` to both validators, or expose a combined `validate_a3()` entry point.  
  -> Fix: Non-blocking; consider as follow-up optimization if preset manifests grow very large.

## Rev3 Verification of Prior Findings

Prior rev2 (qc3) verdict was **Approve** with zero open findings. The rev3 delta (`e0f44c4`) touches only `crates/nexus-orchestration/src/preset/loader.rs` and:

1. **Replaces** the old inline `validate_template_files_in_sandbox()` (55 lines removed) with shared A3 validation surfaces (`validate_path_safety` + `validate_assets_in_bundle`).
2. **Adds** A3 error/warning collection and `tracing::warn!` logging for warnings.
3. **Adds** two regression tests:
   - `a3_loader_rejects_missing_template_file_in_bundle` — verifies missing asset rejection via `validate_assets_in_bundle()`.
   - `a3_loader_rejects_dotdot_in_system_prompt_file` — verifies `..` traversal rejection in `roles[].system_prompt_file` via `validate_path_safety()`.
4. **Fixes** two existing tests (`load_preset_from_directory_loads_valid_preset`, `load_preset_from_directory_rejects_validation_errors`) to align bundle directory names with manifest `preset.id` to satisfy the new `check_bundle_id_vs_directory` validation.

**Perf/reliability assessment of the A3 wiring:**
- The new validators are not on a hot path (preset loading is infrequent).
- `validate_assets_in_bundle` performs `exists()` + `canonicalize()` per asset reference. This is slightly more filesystem work than the old inline function (which only checked `template_file` entries), but the additional coverage (all asset types: `prompt_file`, `system_prompt_file`, inner-graph `template_file`, etc.) is a security improvement.
- No new TOCTOU concerns are introduced; filesystem race conditions between `exists()` and `canonicalize()` are inherent and were present in the old code as well.
- `tracing::warn!` for warnings is correct (warnings are informational and do not block loading).
- Both new tests are deterministic, fast, and isolated (use `tempfile::tempdir()` with minimal YAML fixtures, no async/sleep/external deps).

**No new Critical or Warning-level issues introduced by rev3.**

## Source Trace
- S-001: `loader.rs` lines 308–309 — `validate_path_safety` and `validate_assets_in_bundle` each call `collect_asset_file_references` independently.
- Finding source: manual-reasoning
- Confidence: Medium

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 1 |

**Verdict**: Approve

The rev3 change correctly wires the shared A3 validation surface into the runtime loader, adds focused regression tests, and aligns existing tests with the new bundle-id check. All lint, tests, and clippy pass. The sole suggestion is a minor redundancy that does not block approval.
