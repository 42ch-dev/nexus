---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-05-15-v1.17-b4-naming-hardening"
verdict: "Request Changes"
generated_at: "2026-05-15"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: bailian-coding-plan/glm-5
- Review Perspective: Architecture coherence and maintainability risk
- Report Timestamp: 2026-05-15T12:30:00Z

## Scope
- plan_id: `2026-05-15-v1.17-b4-naming-hardening`
- Review range / Diff basis: `git diff main...HEAD`
- Working branch (verified): `feature/v1.17-b4-naming-hardening`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus`
- Files reviewed: 16
- Commit range: `6fb1ab2`
- Tools run: `git diff`, `grep`, `cargo clippy --all`, `cargo test --all`, `bash tooling/check-schema-drift.sh`

## Findings

### 🔴 Critical

#### C-001: tooling/check-schema-drift.sh references deleted `nexus42d` crate

- **Location**: `tooling/check-schema-drift.sh` lines 16, 28, 66, 85, 106
- **Issue**: The schema drift detection script contains 5 references to `crates/nexus42d/` paths that were removed in B3:
  - Line 16: `grep -q 'nexus-local-db' crates/nexus42d/Cargo.toml`
  - Line 28: `grep -E '...DB_SCHEMA_VERSION...' crates/nexus42d/src/db/schema.rs`
  - Line 66: `grep -r "CREATE TABLE..." crates/nexus42d/src/db/`
  - Line 85: `grep -r "WIRE_SCHEMA_VERSION" crates/nexus42d/src/`
  - Line 106: `grep -q 'use nexus_local_db::' crates/nexus42d/src/db/schema.rs`
- **Impact**: 
  - Script execution fails immediately: `grep: crates/nexus42d/Cargo.toml: No such file or directory`
  - CI job `schema-consistency-check` (`.github/workflows/ci.yml` line 177) calls this script and will fail
  - False positive/negative detection risk
- **Fix**: Update script to:
  1. Remove daemon-specific checks (since daemon functionality is now integrated in `nexus42` via `nexus-daemon-runtime`)
  2. Or update paths to reference `nexus-daemon-runtime` if daemon-specific schema validation is still needed
  3. The script's purpose was to verify CLI + daemon both depend on nexus-local-db — this should be replaced with checking `nexus42` + `nexus-daemon-runtime` instead

#### C-002: CI workflow contains stale `nexus42d` verification job

- **Location**: `.github/workflows/ci.yml` lines 157-169
- **Issue**: Job `verify-no-acp-in-daemon` (named "Verify nexus42d linkage matrix") checks `crates/nexus42d/Cargo.toml`:
  - Line 158: Job name "Verify nexus42d linkage matrix"
  - Line 162: Step name "Assert nexus42d does not link ACP host / SDK"
  - Lines 165-166: Path `crates/nexus42d/Cargo.toml` and error message reference `nexus42d`
- **Impact**: 
  - CI job will fail because `crates/nexus42d/Cargo.toml` does not exist
  - The linkage gate purpose (enforcing ACP separation) is still valid, but needs to be updated
- **Fix**: 
  1. Update job to check `nexus-daemon-runtime` Cargo.toml instead
  2. Update job name and error messages to reference "daemon-runtime"
  3. The architectural invariant (daemon not linking ACP host) remains valid per `knowledge/acp-client-tech-spec-v2.md §11.3`

### 🟡 Warning

(None)

### 🟢 Suggestion

#### S-001: Naming consistency verified

- **Observation**: All `nexus42d` references in source code (`.rs` files) have been removed. Naming is now consistent:
  - Log message: `Starting daemon-runtime v...`
  - Doc comments: "daemon runtime" used consistently
  - CLI help text: "Manage the daemon runtime", "Start the daemon runtime"
- **Assessment**: Good — naming cleanup complete in source. Only stale references remain in tooling/CI scripts.

#### S-002: Hardening fixes are correct

- **Observation**: Two hardening changes in `boot.rs` and `daemon.rs`:
  1. Schedule pool `.expect()` replaced with `bail!` (lines 194-201) — graceful error propagation instead of panic
  2. Health check returns `CliError::Daemon` instead of `Ok(())` (lines 261-269) — callers know startup status
- **Assessment**: Both changes are correct and follow Rust best practices. Error messages don't leak implementation details to user-facing output.

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|------------|-------------|-------------------|------------|
| C-001 | git-diff + bash execution | `tooling/check-schema-drift.sh` + execution output | High |
| C-002 | grep + read | `.github/workflows/ci.yml` lines 157-169 | High |
| S-001 | grep | `grep -r "nexus42d" crates/` → no output (excluding AGENTS.md) | High |
| S-002 | git-diff + read | `boot.rs` lines 194-201, `daemon.rs` lines 261-269 | High |

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 2 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 2 |

**Verdict**: **Request Changes**

The B4 naming cleanup is correctly implemented in all source files — `nexus42d` references are gone, naming is consistent, and hardening fixes are correct. However, two **Critical** findings block approval:

1. **C-001**: `tooling/check-schema-drift.sh` is broken and will cause CI failures
2. **C-002**: CI workflow `verify-no-acp-in-daemon` job references deleted crate

Both issues stem from B3's removal of `nexus42d` crate without updating tooling/CI scripts. These must be fixed before merging to prevent CI failures on main.

## Checked Items

### Code Quality
- [x] Naming clear and consistent (verified)
- [x] Responsibilities not overly mixed (verified)
- [x] Error handling explicit and actionable (verified — hardening correct)
- [x] Comments explain intent (verified)

### Security & Correctness
- [x] Input validation present (no new inputs)
- [x] No injection/path traversal issues (no new paths)
- [x] Sensitive data handled appropriately (error messages don't leak internals)
- [x] Invariants and state transitions coherent (verified)

### Performance & Reliability
- [x] Hot paths avoid unnecessary overhead (no new hot paths)
- [x] Resource lifecycle handled correctly (pool creation now graceful)
- [x] Unbounded operations handled (no change)
- [x] Degradation and failure observable (hardening improves this)

### Maintainability
- [x] Contracts and interfaces easy to understand (verified)
- [x] Dependencies introduced with good reason (no new deps)
- [x] Breaking changes have migration guidance (no breaking changes)
- [x] Reuse over duplication (verified — naming unified)