# QA Report — T4 SOUL Lifecycle Implementation

**Agent**: @qa-engineer  
**Task**: Verify T4 implementation against all acceptance criteria and QC fix validation  
**Review cwd**: `/Users/bibi/workspace/organizations/42ch/nexus`  
**Working branch**: `feature/v1.2-ws3a-soul`  
**Review range**: `git diff c18dbb1...HEAD`  
**QC Fix Commit**: `30b0dbb` ("fix(nexus-domain): validate creator_id against path traversal (R1) and wire soul_meta into CLI (R2)")  
**Date**: 2026-04-14  
**QA Mode**: Default (verification of implemented feature)

---

## Scope Tested

- `crates/nexus-home-layout/src/lib.rs` — `creator_soul_md_path` function and path traversal defense
- `crates/nexus-domain/src/soul_io.rs` — SOUL.md file I/O with `validate_creator_id` on all public functions
- `crates/nexus-domain/src/soul.rs` — `SoulDocument` parse/render roundtrip with serde_yaml frontmatter
- `crates/nexus42/src/commands/soul.rs` — CLI commands: init, show, edit-personality, validate
- `crates/nexus-local-db/src/soul_meta.rs` — `soul_meta` table CRUD (upsert, get, delete)
- `crates/nexus-local-db/src/migration.rs` — DB v3 migration (idempotent)
- `crates/nexus-local-db/src/schema.rs` — `SOUL_META_TABLE` definition
- `crates/nexus-domain/src/errors.rs` — `DomainError::InvalidIdFormat` for path traversal errors
- Integration tests: `crates/nexus42/tests/integration.rs`

---

## Acceptance Criteria Verification

| AC | Criterion | Status | Evidence |
|----|-----------|--------|----------|
| **AC1** | SOUL.md created at correct path `~/.nexus42/creators/<id>/SOUL.md` | ✅ PASS | `creator_soul_md_path` in `nexus-home-layout/src/lib.rs:49-55` produces correct path. `soul_io::create()` calls this at line 69. Test: `soul_md_path_layout` passes. |
| **AC2** | Frontmatter (YAML) parsed and roundtripped | ✅ PASS | `SoulDocument::parse()` extracts frontmatter via `serde_yaml::from_str()` (soul.rs:133). `render()` uses `serde_yaml::to_string()` (soul.rs:81). Test: `render_roundtrip` passes (soul.rs:259-273). |
| **AC3** | `soul init` creates, fails if exists | ✅ PASS | `soul_io::create()` returns `Err(DomainError::ValidationError(...))` if path exists (soul_io.rs:70-75). CLI `init` command checks `soul_io::exists()` first (soul.rs:44-48). Test: `create_already_exists` passes. |
| **AC4** | `soul show` displays content | ✅ PASS | `soul show` command calls `soul_io::load()` then `doc.render()` and prints output (soul.rs:76-81). |
| **AC5** | `soul edit-personality` updates section | ✅ PASS | `edit_personality` calls `doc.set_personality()` then `soul_io::save()` (soul.rs:104-105). |
| **AC6** | `soul validate` checks sections | ✅ PASS | `validate` command calls `soul_io::validate()` which calls `doc.validate()` checking required sections (soul.rs:134-138). |
| **AC7** | SoulDocument parse+render roundtrip | ✅ PASS | `render_roundtrip` test (soul.rs:259-273) verifies: create → render → parse → personality matches. |
| **AC8** | soul_meta table populated after create/save | ✅ PASS | `init` command (soul.rs:54-68) calls `upsert_soul_meta` after successful create. `edit_personality` command (soul.rs:107-127) calls `upsert_soul_meta` after save. `upsert_soul_meta` re-exported from `nexus_local_db` (lib.rs:44-46). |
| **AC9** | DB migration v2→v3 idempotent | ✅ PASS | `migration.rs:233-253` test `run_migrations_is_idempotent` runs migrations 3x and verifies version unchanged. Migration v3 uses `CREATE TABLE IF NOT EXISTS` (schema.rs:163). |
| **AC10** | All tests pass, clippy clean, fmt clean, codegen no diff | ✅ PASS | See "Test Results" section below. |

---

## QC Fix Verification

### R1: Path Traversal Rejection

| Function | `validate_creator_id` Called | Test Coverage |
|----------|------------------------------|---------------|
| `load` | ✅ soul_io.rs:50 | `load_rejects_path_traversal` passes |
| `create` | ✅ soul_io.rs:68 | `create_rejects_path_traversal` passes |
| `save` | ✅ soul_io.rs:91 | `save_rejects_path_traversal` passes |
| `validate` | ✅ soul_io.rs:107 | `validate_rejects_path_traversal` passes |
| `delete` | ✅ soul_io.rs:115 | `delete_rejects_path_traversal` passes |
| `exists` | ✅ soul_io.rs:42 (uses `is_valid_creator_id` directly) | `exists_returns_false_for_invalid_id` passes |

**Defense-in-depth**: `creator_soul_md_path` (nexus-home-layout/lib.rs:49-55) panics on path separators, backslashes, `..`, or control characters via `assert_creator_id_safe()`. Tests: `soul_md_path_rejects_forward_slash`, `soul_md_path_rejects_backslash`, `soul_md_path_rejects_dotdot`, `soul_md_path_rejects_control_chars` all pass.

### R2: soul_meta Wired to CLI

| Operation | `upsert_soul_meta` Called | File |
|-----------|---------------------------|------|
| `soul init` | ✅ soul.rs:66 | crates/nexus42/src/commands/soul.rs |
| `soul edit-personality` | ✅ soul.rs:125 | crates/nexus42/src/commands/soul.rs |

`upsert_soul_meta` is correctly exported from `nexus_local_db` (lib.rs:44-46 as `upsert as upsert_soul_meta`).

---

## Test Results

### Unit Tests (nexus-domain, nexus-local-db, nexus42 lib)

```
cargo test -p nexus-domain -p nexus-local-db -p nexus42 --lib
```

- **nexus-domain**: 236 tests — ✅ ALL PASS
- **nexus-local-db**: 59 tests — ✅ ALL PASS  
- **nexus42 lib**: 293 tests — ✅ ALL PASS

### Integration Tests

```
cargo test -p nexus42 --test integration
```

- **21 tests** — ✅ ALL PASS
- Notable SOUL tests: `soul_help`, `soul_requires_active_creator`, `soul_validate_requires_active_creator`

### Clippy

```
cargo clippy --all -- -D warnings
```

✅ **CLEAN** — no warnings, no errors

### Formatting

```
cargo +nightly fmt --all -- --check
```

✅ **CLEAN** — no formatting diffs

### Schema Validation

```
pnpm run validate-schemas
```

✅ **CLEAN** — 62/62 schemas valid

### Codegen

```
pnpm run codegen && git diff --exit-code packages/nexus-contracts/src/generated/ crates/nexus-contracts/src/generated/
```

✅ **CLEAN** — no diff on generated output

---

## Minor Fix Applied During QA

**Issue**: Import ordering in `crates/nexus42/src/commands/soul.rs:8` — `upsert_soul_meta` appeared before `get_soul_meta` alphabetically.

**Fix**: Reordered to `use nexus_local_db::{get_soul_meta, upsert_soul_meta, SoulMeta};`

**Verification**: `cargo +nightly fmt --all -- --check` now passes cleanly.

---

## Not Tested

- **E2E CLI flow** — requires a fully initialized workspace with an active creator identity set via `nexus42 identity use <id>`. This would require daemon initialization and OAuth setup which is out-of-scope for unit/integration testing.
- **Performance/load testing** — not applicable to this feature scope.
- **Concurrent soul operations** — file locking not in scope for T4.

---

## Recommended Follow-up

None — all acceptance criteria met, QC fixes verified, CI gates pass.

---

## Handoff

**Status**: ✅ **DONE** — Ready for plan status update to `Done` and merge to `feature/v1.2`.

**Residual Findings**: None (plan `2026-04-14-v1.2-soul-lifecycle` has no open R# entries in SSOT).

**To close plan**: Update `.agents/plans/status.json`:
- Set `plans[?].status` to `"Done"`
- Set `plans[?].done_at` to `"2026-04-14"`
- Verify `metadata.residual_findings["2026-04-14-v1.2-soul-lifecycle"]` is `null` or key removed (already clean)

**Merge target**: `feature/v1.2-ws3a-soul` → `feature/v1.2`
