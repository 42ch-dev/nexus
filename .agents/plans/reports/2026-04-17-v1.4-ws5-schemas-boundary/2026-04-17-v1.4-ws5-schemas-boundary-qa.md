# QA Report: 2026-04-17-v1.4-ws5-schemas-boundary

**Agent**: @qa-engineer
**Plan ID**: `2026-04-17-v1.4-ws5-schemas-boundary`
**Working branch**: `feature/v1.4-ws5-schemas-boundary`
**Review cwd**: `/Users/bibi/workspace/organizations/42ch/nexus`
**Review range**: `3709e27385a47252da378d987a35de8017d2282e..7b9f69c`
**Date**: 2026-04-18

---

## Scope Tested

Full CI verification gate + WS5-specific schema boundary checks on the `feature/v1.4-ws5-schemas-boundary` branch covering all 8 commits in the review range (WS5 T1–T5 + QC reports).

---

## Verification Gate Results

| # | Command | Result | Evidence |
|---|---------|--------|----------|
| 1 | `pnpm run validate-schemas` | **PASS** | 54/54 schema files valid (10 local schemas correctly removed from `schemas/`) |
| 2 | `pnpm run codegen && git diff --exit-code` | **PASS** | Codegen produced 51 TS + 51 Rust types; `git diff --exit-code` returned 0 (no diff) |
| 3 | `cargo +nightly fmt --all -- --check` | **PASS** | Exit code 0, no formatting issues |
| 4 | `SQLX_OFFLINE=true cargo clippy --all -- -D warnings` | **PASS** | 7 crates checked, 0 warnings |
| 5 | `SQLX_OFFLINE=true cargo test --workspace` | **PARTIAL** | 920 tests passed across 7 test binaries; `acp_tool.rs` timed out after 180s (see §Findings) |
| 6 | `pnpm run typecheck` | **PASS** | `nexus-contracts` + `nexus-codegen` tsc --noEmit clean |

## WS5-Specific Checks

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | `rg 'rusqlite\|deadpool' crates/` | **CLEAN** | No matches (EXIT_CODE=1). Inherited WS8 gate passes. |
| 2 | `crates/nexus-contracts/src/local/` file count | **10 files** | `daemon_status_v2.rs`, `registry_manifest.rs`, `agent_profile.rs`, `local_identity.rs`, `manuscript_state.rs`, `outbox_entry.rs`, `reference_source.rs`, `runtime_mode.rs`, `workspace_binding.rs`, `meta.rs` |
| 3 | No stale generated types for moved schemas | **CLEAN** | All 10 moved schema names verified absent from `*/generated/` dirs |
| 4 | `packages/nexus-contracts/package.json` version | **0.4.0** | Confirmed via `jq '.version'` |
| 5 | Shared enums in `common_types.rs` | **OK** | `ReferenceSourceType` and `AgentProfileStatus` are generated from `schemas/common/common.schema.json` (wire schema), correctly imported by local types — not stale |

---

## Findings

### Medium

#### F1: Phase Gate `clarify` section missing
- **Scope**: `.agents/plans/2026-04-17-v1.4-ws5-schemas-boundary.md`
- **Detail**: The plan file contains a well-structured `tasks` section (T1–T7) but has no `clarify` section. Per `phase-gate-playbook.md`, non-hotfix plans should include both `clarify` and `tasks`.
- **Impact**: QA sign-off blocked pending PM decision on whether to accept this deviation or require a post-hoc `clarify` supplement.

### Low

#### F2: `acp_tool.rs` integration test timeout (pre-existing)
- **Scope**: `crates/nexus42d/tests/acp_tool.rs`
- **Detail**: The `acp_tool.rs` test binary timed out after 180 seconds. `git log` confirms this test was **not modified** in the WS5 review range (last touch: `35e29eb feat(acp): implement daemon-mediated tool access`). The test requires an external ACP agent to be running, which is not available in this environment.
- **Impact**: Not a WS5 regression. 920/920 non-ACP tests pass. Recommend tracking as environment limitation.

### Nit

#### F3: Audit table header says 11 local, only 10 moved
- **Scope**: `knowledge/schemas-boundary-v1.md` §5.2; referenced in `status.json` `audit_result` field
- **Detail**: The `status.json` metadata records `"audit_result": "64 total: 53 wire, 11 local (2 reclassified from sketch: VersionRef→wire, KeyBlock→wire)"` but only 10 hand-written Rust files exist in `src/local/`. QC3 (S1) flagged this discrepancy.
- **Decision**: Accept as documented nit — the header count was an early audit estimate before reclassification; the actual moved count is 10.
- **Owner**: fullstack-dev
- **Target**: V1.5

---

## Not Tested

- Cross-repo handoff note to `nexus-platform` (Task 6) — external to this repo, not verifiable here.
- `acp_tool.rs` integration tests — require external ACP agent infrastructure.

---

## Phase Gate Checklist Status

| Item | Status |
|------|--------|
| `clarify` section | ❌ Missing |
| `tasks` section | ✅ Present (T1–T7) |
| Delivery matches plan scope | ✅ 5 commits match T1–T5 |
| Hotfix RCA (if applicable) | N/A (not hotfix) |

---

## QA Verdict

**Status**: `Needs PM Decision`

**Reason**: Phase Gate `clarify` section is missing from the plan file. All CI gates pass (920/920 tests, with pre-existing `acp_tool.rs` timeout excluded), all WS5-specific checks pass, and the code quality is clean. The missing `clarify` section requires PM to decide whether to:
1. Accept the deviation (clarify happened informally during QC), or
2. Require a post-hoc `clarify` supplement before merge.

**Recommended action**: Accept F1 as minor process deviation, merge, and record in plan notes. F3 is already addressed as a residual finding (R1, severity: nit).
