# WS-C — `--name` Positional → Flag (V1.8)

**Plan ID**: `2026-04-23-v1.8-ws-c-name-positional-to-flag`
**Status**: Todo
**Created**: 2026-04-23
**Branch**: `feature/v1.8`

---

## §1 Overview

Change `creator register <name>` to `creator register --name <name>` for consistency with the frozen spec. This is a breaking change for existing user scripts using positional argument syntax.

**Compass**: [v1.8-delivery-compass-v1.md](../knowledge/v1.8-delivery-compass-v1.md)

**Effort (agent-oriented)**: XS (~0.5 agent session)
**Dependencies**: None — can begin immediately.

---

## §2 Scope

### In scope

- Modify clap definition in `crates/nexus42/src/commands/creator.rs` from positional `name` to optional `--name` flag.
- Update help text to match frozen spec: `nexus42 creator register --name "My Agent" [--source cli|web_agent] [--handle <handle>]`.
- Update E2E tests in `crates/nexus42/tests/creator_register_e2e.rs` to use new syntax.
- Ensure positional syntax produces a clear error message with migration hint.

### Out of scope

- Other subcommand argument changes.

---

## §3 Tasks

| ID | Task | Depends on | Parallel | Owner (planned) | Done criteria |
|----|------|------------|----------|-----------------|---------------|
| T1 | Read current `creator register` clap definition and help text | — | no | @fullstack-dev | Documented current CLI surface |
| T2 | Change `name` from positional arg to `--name` optional flag in clap | T1 | no | @fullstack-dev | Clap definition updated; help text matches frozen spec |
| T3 | Add clear error message when positional syntax is used (migration hint) | T2 | no | @fullstack-dev | Error message visible |
| T4 | Update E2E tests to use new `--name "X"` syntax | T2 | no | @fullstack-dev | E2E tests pass with new syntax |
| T5 | Add E2E test: positional syntax produces clear error | T3 | no | @fullstack-dev | Test passes |
| T6 | Verify `cargo test --workspace` green; `cargo clippy --all -- -D warnings` clean; `cargo +nightly fmt --all -- --check` clean | T4, T5 | no | @fullstack-dev | All CI checks pass |

---

## §4 Evidence

- E2E test: `nexus42 creator register --name "Test Agent"` succeeds.
- E2E test: `nexus42 creator register "Test Agent"` (positional, old syntax) produces clear error with migration hint.
- `cargo test --workspace` green; clippy/fmt clean.

---

## §5 Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| `--name` positional→flag is breaking change for existing users | Medium | Low | Clear error message with migration hint. V1.x pre-1.0 allows breaking changes. |

---

## §6 Milestone Gate

**M1 exit** (shared with WS-A): `--name` changed to flag; old positional syntax rejected with clear error; all tests pass.
