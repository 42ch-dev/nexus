# V1.16 Prompt + Skills Compass v1

**Status**: Draft — **GATED**
**Scope**: `nexus` OSS — **post-V1.15** iteration to systematically improve **prompts, embedded skills quality, preset flows (including `novel-writing` and `research`)**, and **output artifacts**, informed by prior work in `novels-system` (see §0 for reachability rules).
**Predecessor**: [v1.15-delivery-compass-v1.md](v1.15-delivery-compass-v1.md)
**Created**: 2026-05-10

**Hard gate (program)**:

- **Do not** treat this compass as active execution SSOT until **`nexus` V1.15 is Done** per `v1.15-delivery-compass-v1.md` §7 and cross-repo G5.
- Until then: use this file **only** for early research notes or backlog shaping inside `{HARNESS_DIR}/knowledge/` — **no** `{PLAN_DIR}` row may cite V1.16 as blocking V1.15.

**Directory authority (Morning Star)**:

- `{HARNESS_DIR}` → `.agents/`
- `{PLAN_DIR}` → `.agents/plans/`
- This file → `{HARNESS_DIR}/knowledge/`

---

## 0) External research constraint (repo reachability)

Per [AGENTS.md](../../AGENTS.md) *Documentation and plans*: **do not** reference paths outside this repository in **tracked** artifacts as the sole authority.

- **Allowed**: Summarize patterns, decisions, and prompts **into** `embedded-skills/` or `{HARNESS_DIR}/knowledge/` with provenance footnotes (date, author, source name — not absolute disk paths).
- **Not allowed**: Hard dependency on `~/workspace/.../internal-sharing/novels-system` paths inside committed markdown that agents must read from clone.

---

## 1) Purpose and SSOT boundary

- **Authoritative for** (once gate lifted): V1.16 scope for prompt/skill/preset quality iteration on the **OSS** side.
- **Not authoritative for**: Platform HTTP contracts; field-level `v1-spec` (platform repo owns those edits when CLI semantics touch cross-repo contracts).

---

## 2) V1.16 positioning (intent — execution deferred)

After V1.15 lands the **mechanism** (embedded skills, preset-driven flows, workspace policy, sync module), V1.16 focuses on **quality**:

- Curate embedded skills from proven `novels-system` workflows (ported as text + tests in-repo).
- Tighten preset prompts for `novel-writing`, `research`, and related flows.
- Expand golden outputs / evaluation harnesses for chapter + research artifacts.

---

## 3) Planned workstreams (indicative — not committed until gate lift)

### Theme S1 — Embedded skills library quality

- Skill authoring standards (frontmatter, triggers, expected evidence).
- Versioning and changelog per skill pack.

### Theme S2 — Preset prompt + flow refinement

- `novel-writing`: chapter quality, consistency checks, revision loops.
- `research`: report structure, artifact taxonomy, linking to story refs.

### Theme S3 — Output evaluation

- Static golden files + optional LLM-as-judge harness (if adopted — separate ADR).

---

## 4) Entry criteria (when gate opens)

1. `nexus` V1.15 delivery compass **Done**.
2. Platform V1.15 program compass **Done** (or explicitly waived items documented).
3. `{PLAN_DIR}` plan row created for V1.16 with `plan_id` registered in `{HARNESS_DIR}/status.json`.

---

## 5) Done definition (preview)

- Measurable improvement in embedded skill coverage + preset prompt quality (criteria frozen when plan opens).
- No regression on V1.15 O1–O3 invariants (skills sync, session injection, workspace policy).

---

## 6) Related index

- Blocking delivery: [v1.15-delivery-compass-v1.md](v1.15-delivery-compass-v1.md)
- Platform companion: `nexus-platform/.agents/knowledge/v1.15-program-compass-v1.md`

---

*Created: 2026-05-10. Status: Draft — gated.*
