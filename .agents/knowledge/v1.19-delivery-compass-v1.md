# V1.19 Delivery Compass v1

**Status**: Draft — Active planning SSOT
**Scope**: `nexus` OSS — Agent Host Hardening: close V1.18 functional gaps, fix safety/correctness issues, resolve QC residuals
**Predecessor**: [v1.18-delivery-compass-v1.md](v1.18-delivery-compass-v1.md)
**Created**: 2026-05-15

**Design inputs**:

- [v1.18-delivery-compass-v1.md](v1.18-delivery-compass-v1.md) §9 (Deferred Features D-001–D-008)
- [v1.18-delivery-compass-v1.md](v1.18-delivery-compass-v1.md) §11 (Post-implementation risk rows)
- `.agents/status.json` `residual_findings["2026-05-15-v1.18-agent-host-core"]` (R1–R7)
- [deferred-features-cross-version-tracker-v1.md](deferred-features-cross-version-tracker-v1.md) §3.1 (DF-18–DF-25)

---

## 0) Scope lock

### In scope (frozen)

1. **Batch 1 — Safety/Correctness** (5 items): persistent native sessions (DF-18), ACP permission handling (DF-19), SetModel/SetMode truthfulness (DF-20), HostManager shutdown wiring (DF-24), AdmissionPolicy enforcement (DF-25).
2. **Batch 2 — Hardening** (3 items): stage-level timeout enforcement (DF-21), auto tool-risk classification (DF-22), provider-level streaming adaptation (DF-23).
3. **Batch 2 — QC residuals** (3 items): Unix-only `which` command (R3 → D-009), API input validation (R4 → D-010), config path traversal protection (R5 → D-011).
4. Close V1.18 residual findings R1–R7 where resolved by V1.19 work; accept R7 (shutdown drain timeout) as subsumed by D-004 scope.

### Out of scope (frozen)

- Native session persistence to disk (DF-23 tracker item — tracked as V1.20+ backlog).
- New native CLI provider adapters beyond Claude CLI (Codex, Gemini, etc.).
- Unmanaged attach mode.
- Cross-daemon crash recovery / task lease semantics.
- CLI UX expansion.
- Wire contract / schema changes (no new schemas in this version).
- V1.17 prompt/skills follow-ups.
- V2.0+ items (DF-13 Entitlements, DF-16 Billing, BL-08 Social).

---

## 1) Purpose and SSOT boundary

- **Authoritative for**: V1.19 Agent Host Hardening scope, consolidated from V1.18 deferred features and QC residuals.
- **Consolidation note**: V1.18 produced two sets of gaps — compass §9 deferred features (D-001–D-008, design-level) and QC residuals (R1–R7, code-level). Several overlap and are merged below.
- **Execution tracking**: Plan file TBD at task breakdown.
- **Conflict resolution**: runtime code must follow current repository APIs at implementation time. If this compass and current code disagree, update this compass or the plan through PM/architect coordination.

---

## 2) V1.19 positioning

V1.19 is a **hardening release** — no new crates, no new architectural layers. The goal is to make the V1.18 skeleton **functionally complete** for real usage:

1. **Safety first**: shutdown wiring (D-007) and admission enforcement (D-008) are correctness bugs, not enhancements.
2. **Capability honesty**: the descriptor must not lie about what the provider supports (D-003).
3. **Multi-turn is basic**: per-op spawn makes native providers unusable for conversation (D-001).
4. **Permission handling is mandatory**: ACP providers hang without it (D-002).
5. **Timeout enforcement prevents indefinite hangs**: all provider paths must be bounded (D-004).
6. **QC residuals close out**: input validation (R4), path traversal (R5), and cross-platform probe (R3) are hardening staples.

---

## 3) Consolidated item register

### 3.1 Merge analysis: V1.18 D-* vs V1.18 R-*

| V1.18 Deferred | V1.18 QC Residual | Action |
|----------------|-------------------|--------|
| D-007 (HostManager shutdown) | R2 (same) | **Merge** — one item |
| D-008 (AdmissionPolicy wiring) | R1 (same) | **Merge** — one item |
| D-004 (TimeoutConfig enforcement) | R6 (same) + R7 (shutdown drain not bounded) | **Merge** — R7 subsumed by D-004 scope |
| D-001 (Native multi-turn) | — | **New item** |
| D-002 (ACP permission handling) | — | **New item** |
| D-003 (SetModel/SetMode lie) | — | **New item** |
| D-005 (Risk auto-classification) | — | **New item** |
| D-006 (Native session persistence) | — | **Out of scope** → V1.20+ |
| R3 (Unix-only `which` command) | — | **New item** (D-009) |
| R4 (API input validation) | — | **New item** (D-010) |
| R5 (Config path traversal) | — | **New item** (D-011) |

### 3.2 Final item list (11 items, 2 batches)

#### Batch 1 — Safety / Correctness (6 items)

| ID | Title | Severity | Effort | Depends on | Related |
|----|-------|----------|--------|------------|---------|
| D-001 | Native CLI multi-turn session (persistent child process) | HIGH | M | — | claude.rs L28-55, L212-325 |
| D-007 | HostManager::shutdown → ProviderAdapter::shutdown() | MEDIUM | S | — | manager.rs L320-347 |
| D-008 | AdmissionPolicy enforcement in create_session / exec | MEDIUM | S | — | manager.rs L159-268 |
| D-002 | ACP session/request_permission handling | HIGH | M | D-005 | acp.rs L212-289, risk.rs |
| D-003 | SetModel / SetMode capability truthfulness | MEDIUM | S | — | acp.rs L219-232, model.rs L353-370 |
| D-009 | Cross-platform command probe (replace Unix-only `which`) | LOW | S | — | path_scan.rs |

#### Batch 2 — Hardening (5 items)

| ID | Title | Severity | Effort | Depends on | Related |
|----|-------|----------|--------|------------|---------|
| D-004 | Stage-level timeout enforcement | MEDIUM | M | — | config.rs, all providers |
| D-005 | Auto tool-risk classification | MEDIUM | M | — | risk.rs L24-60 |
| D-010 | API handler input validation on session ID path params | MEDIUM | S | — | daemon-runtime/api |
| D-011 | Config path traversal protection | MEDIUM | S | — | config.rs |
| D-006 | Provider-level streaming adaptation | MEDIUM | L | — | acp.rs, streaming DTOs |

**Note**: D-006 (DF-23, streaming adaptation) is lowest priority in Batch 2. If the batch runs long, D-006 can be deferred to V1.20 without blocking functional completeness — the current buffered event approach works for single-turn interactions.

### 3.3 Item closure matrix: V1.18 residuals → V1.19 items

| V1.18 Residual | V1.19 Item | Expected Outcome |
|----------------|-----------|------------------|
| R1 (AdmissionPolicy not called) | D-008 | **Close** |
| R2 (shutdown doesn't call provider) | D-007 | **Close** |
| R3 (Unix-only `which`) | D-009 | **Close** |
| R4 (API input validation) | D-010 | **Close** |
| R5 (Config path traversal) | D-011 | **Close** |
| R6 (Timeout not enforced) | D-004 | **Close** |
| R7 (Shutdown drain not bounded) | D-004 | **Close** (subsumed) |

---

## 4) Item detail

### D-001: Native CLI multi-turn session support [HIGH]

**Problem**: `ClaudeCliProvider::execute()` spawns a fresh subprocess per call (claude.rs L239-325). Each operation gets a new `HostSessionId`. There is no persistent native session — stdin is written once and closed. Multi-turn conversation is broken.

**V1.18 audit note**: "Multi-turn is a basic feature, not a reasonable simplification." The `ProviderAdapter` trait and session state machine were designed for persistent sessions. `ManagedSessionHandle` carries a `session_id` expected to survive across operations.

**Design**:

1. `ClaudeCliProvider::launch()` must spawn the child process and store it in a session-scoped `NativeSession { child, stdin, stdout }` (struct already defined at L28-55 but fields unused).
2. `stdin` pipe stays open across `execute()` calls.
3. Each `execute()` writes a new prompt to `stdin` and reads the response from `stdout`.
4. `shutdown()` closes stdin (EOF), waits for process exit with `shutdown_ms` timeout, then force-kills.
5. Claude CLI `--print` mode is single-turn by design — multi-turn requires switching to interactive/repl mode or using the Claude CLI's session-resume flags if available. Research required at T1.
6. If Claude CLI does not support persistent interactive stdin in a usable way, consider Claude CLI's `--resume` flag or `--session-id` for session continuity across invocations (multi-process multi-turn instead of single-process multi-turn).

**Open question**: Does Claude CLI `--print` support multi-turn via persistent stdin, or does it require `--resume` / `--session-id` for multi-turn continuity? This must be resolved at T1 before implementation.

**Risk**: If Claude CLI cannot support multi-turn at all in Wave 2, D-001 scope reduces to "best-effort session continuity via resume flags" with a note that full multi-turn requires ACP provider.

**Related**: claude.rs L28-55 (NativeSession), L212-237 (launch), L239-325 (execute).

### D-002: ACP session/request_permission handling [HIGH]

**Problem**: `AcpProvider::execute()` does not handle ACP `session/request_permission` messages. When an ACP provider requests permission to use a tool, the host must evaluate and respond. Currently the provider hangs or timeouts.

**V1.18 compass requirement (R-003)**: "ACP provider permissions delegate to `nexus-acp-host::PermissionPolicy::evaluate_for_agent()`."

**Design**:

1. The ACP provider's event stream loop must monitor for permission request messages (distinct from `session/update` content events).
2. On receipt, extract tool name, description, and risk classification.
3. Call through to `nexus_acp_host::PermissionPolicy::evaluate_for_agent()` with tool context and session identity.
4. If risk classifier (D-005) is available, use `AutoToolRiskClassifier` to determine risk level; otherwise fall back to `StaticToolRiskClassifier`.
5. Permission outcome preference order: `approve_for_session > allow_always > allow_once`; reject otherwise.
6. Send the permission response back through the ACP SDK.
7. Emit a `ToolCallUpdate` host event so the caller knows a permission decision was made.

**Depends on**: D-005 (risk classifier enables informed policy decisions; without it, all unknown tools default to deny/ask).

**Related**: acp.rs L212-289 (execute loop), risk.rs, policy/admission.rs, `nexus-acp-host::PermissionPolicy`.

### D-003: SetModel / SetMode on ACP provider [MEDIUM]

**Problem**: `AcpProvider::execute()` returns `CapabilityUnsupported` for `SetModel` and `SetMode` (acp.rs L219-232). `CapabilityDescriptor::acp_full()` claims both as `true`. This is a capability lie.

**Design** (two options, decide at T1):

- **Option A (implement)**: Use ACP SDK session config APIs to apply model/mode changes. If the provider rejects, apply graceful fallback per OpenDesign pattern — emit `Status` warning and continue with default (only when `allow_model_fallback = true`).
- **Option B (remove claim)**: Set `set_model = false` and `set_mode = false` in `acp_full()` static descriptor. Only enable after successful ACP initialize negotiation confirms support.

**Recommendation**: Option A if the ACP SDK exposes session config methods; Option B if not. The key constraint is that the descriptor must never claim what the adapter cannot deliver.

**Related**: acp.rs L219-232, model.rs L353-370.

### D-004: Stage-level timeout enforcement [MEDIUM]

**Problem**: `TimeoutConfig` defines `launch_ms`, `initialize_ms`, `session_ms`, `prompt_ms`, `shutdown_ms` but none are enforced. A hung provider blocks the host indefinitely.

**Design**:

1. Wrap each provider operation with `tokio::time::timeout()` using the corresponding config value.
2. Operations to wrap:
   - `probe()` → `launch_ms` (use launch timeout as a reasonable default for probe)
   - `launch()` → `launch_ms` + `initialize_ms` (two-stage: spawn timeout + initialize timeout)
   - `execute()` → `prompt_ms`
   - `shutdown()` → `shutdown_ms` (drain timeout with force-kill fallback)
3. On timeout, cancel the operation and emit `OpFailed` with `error_category = "timeout"`.
4. Shutdown drain in `HostManager::shutdown()` must use `shutdown_ms` per-session, then force-kill survivors. This closes R7 (shutdown drain not bounded).

**Related**: config.rs (TimeoutConfig), all providers, manager.rs.

### D-005: Auto tool-risk classification [MEDIUM]

**Problem**: Only `StaticToolRiskClassifier` (manual registration) exists. OpenDesign's regex-based auto-classification was identified as valuable (R-006) but only the extension point was created.

**Design**:

1. Implement `AutoToolRiskClassifier` using the `ToolRiskClassifier` trait.
2. Classification rules (borrowed from OpenDesign `classifyConnectorToolSafety()`):
   - Destructive: `drop|truncate|purge|erase|wipe|rm |force|kill|destroy` → `Destructive`
   - Write: `create|update|delete|send|manage|write|insert|modify|edit|remove` → `Write`
   - Read: `get|list|search|fetch|view|read|show|find|query|check|describe` → `Read`
   - Default (unknown tools): `Write` — not `Read`
3. Compiled regex patterns (not runtime-compiled per call).
4. Integration: `HostManager` uses `AutoToolRiskClassifier` as the default classifier; `StaticToolRiskClassifier` overrides for specific tools when configured.
5. Used by ACP permission handling (D-002) to inform `PermissionPolicy` decisions.

**Related**: risk.rs L24-60 (trait + static impl).

### D-006: Provider-level streaming adaptation [MEDIUM → LOW priority]

**Problem**: ACP streaming events are not fully translated to `HostEvent::MessageDelta` / `ToolCall` etc. The scaffold (`into_event_stream`) exists but is not wired for all ACP event types.

**Design**:

1. Complete the ACP event → `HostEvent` mapping:
   - `session/update` with `thinking` content → `HostEvent::ThoughtDelta`
   - `session/update` with `text` content → `HostEvent::MessageDelta`
   - `session/update` with `tool_use` → `HostEvent::ToolCall`
   - `session/update` with `tool_result` → `HostEvent::ToolCallUpdate`
   - `session/update` with `plan` / `reasoning` → `HostEvent::PlanUpdate`
2. Handle partial line buffering for JSON-RPC newline-delimited messages.
3. Ensure each prompt produces exactly one terminal `OpFinished` or `OpFailed`.

**Priority note**: Current buffered event approach works for single-turn. This item can be deferred to V1.20 if Batch 2 runs long — it is an enhancement, not a correctness fix.

**Related**: acp.rs, capability/model.rs (HostEvent variants).

### D-007: HostManager::shutdown → ProviderAdapter::shutdown() [MEDIUM]

**Problem**: `HostManager::shutdown()` (manager.rs L320-347) transitions sessions to Stopped and clears provider mappings but never calls `ProviderAdapter::shutdown()`. ACP sessions are not cleanly closed; native child processes are not terminated — they become orphans.

**Design**:

1. Before clearing session registry state, iterate all active sessions.
2. For each session, call the corresponding `ProviderAdapter::shutdown()`.
3. Wait up to `shutdown_ms` per session (from `TimeoutConfig`).
4. Force-kill any surviving processes.
5. Only then clear session registry and provider mappings.
6. This is the managed-only safety boundary.

**Effort**: Small — the adapter `shutdown()` methods already exist (acp.rs L317-330, claude.rs L344-358). The gap is purely in `HostManager::shutdown()` not calling them.

**Related**: manager.rs L320-347, acp.rs L317-330, claude.rs L344-358.

### D-008: AdmissionPolicy enforcement in create_session / exec [MEDIUM]

**Problem**: `AdmissionPolicy` is instantiated in `HostManager` but its methods (`check_before_launch`, `check_before_exec`, `check_provider`, `check_session_limit`, `check_ops_per_session`) are never called from `create_session()` or `exec()`.

**Design**:

1. In `create_session()` (manager.rs L159-213):
   - Call `admission.check_provider(&request.provider_id)` — verify provider is allowed.
   - Call `admission.check_session_limit(session_count)` — enforce `max_sessions`.
2. In `exec()` (manager.rs L215-268):
   - Call `admission.check_ops_per_session(active_ops)` — enforce `max_ops_per_session`.
   - Call `admission.check_before_exec(&session, &op)` — verify capability and policy.
3. On denial, return `HostError::PolicyDenied` with descriptive message.
4. Update `AdmissionPolicy::from_config()` to consume discovered providers from the catalog (not just config entries).

**Effort**: Small — the methods exist; the gap is purely wiring.

**Related**: manager.rs L159-268, policy/admission.rs.

### D-009: Cross-platform command probe [LOW]

**Problem**: `path_scan.rs` uses Unix-only `which` command to check if a binary exists. This breaks on Windows.

**Design**:

1. Replace `Command::new("which")` with a cross-platform check:
   - Use `which::which()` from the `which` crate (already a reasonable lightweight dependency), or
   - Implement platform-conditional probe: `which` on Unix, `where` on Windows via `Command::new`.
2. Prefer the `which` crate for reliability — it handles PATH, PATHEXT, and edge cases.
3. Update probe in `ClaudeCliProvider::probe()` and `PathScanDiscovery::discover()`.

**Related**: path_scan.rs, claude.rs (probe).

### D-010: API handler input validation [MEDIUM]

**Problem**: API handlers in `daemon-runtime/api` accept session ID path parameters without validation. Malformed or non-UUID values could cause panics or unexpected behavior.

**Design**:

1. Parse session ID path params as `uuid::Uuid` at the handler boundary.
2. Return 400 Bad Request with descriptive error for invalid IDs.
3. Return 404 Not Found for valid UUIDs that don't match any session.
4. Apply to all `/v1/local/agent-host/sessions/{id}/*` routes.

**Related**: daemon-runtime/api agent-host route module.

### D-011: Config path traversal protection [MEDIUM]

**Problem**: Config paths from `AgentHostConfig` (e.g., `config_path`, `workspace_root`) are not validated against directory traversal attacks (e.g., `../../etc/passwd`).

**Design**:

1. Validate `workspace_root` in `CreateSessionRequest`:
   - Must be an absolute path.
   - Must not escape above a configured trust boundary (default: current user's home directory or the workspace root passed to `HostManager::start()`).
2. Validate `config_path` in `HostStartConfig`:
   - Must be under `{NEXUS_HOME}/agent-host/` or an explicitly allowed path.
3. Return `HostError::PolicyDenied` for traversal attempts.
4. Use `std::path::canonicalize()` for resolution and prefix checking.

**Related**: config.rs, manager.rs (create_session).

---

## 5) Risk register

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Claude CLI does not support multi-turn via persistent stdin | D-001 scope reduces to best-effort resume flags | Medium | Research at T1; fall back to `--resume` / `--session-id` multi-process approach; document limitation |
| ACP SDK does not expose config-option APIs for model/mode | D-003 must fall back to Option B (remove claim) | Low | Check SDK docs at T1; Option B is safe and correct |
| `which` crate adds new dependency | Minor version footprint | Low | `which` is lightweight (no transitive deps); alternative is platform-conditional Command |
| Timeout enforcement breaks existing tests | Test failures from new timeout wrapping | Medium | Use generous default timeouts in test fixtures; add timeout config override for tests |
| AdmissionPolicy enforcement breaks existing API flows | Clients that worked before now get PolicyDenied | Low | Default policy is permissive (`unknown_provider = "allow"`); enforcement only activates when explicitly configured |
| Permission handling requires ACP SDK message type not yet in nexus-acp-host | Cannot parse permission request messages | Medium | Check SDK types at T1; may require small extension to `nexus-acp-host` public API |

---

## 6) Milestones

| Milestone | Exit condition | Items |
|-----------|---------------|-------|
| M1 | D-007 (shutdown wiring) + D-008 (admission enforcement) + D-009 (cross-platform probe) + D-011 (path traversal) — all safety/correctness small fixes done | D-007, D-008, D-009, D-011 |
| M2 | D-001 (native multi-turn) + D-003 (capability truthfulness) — core functional gaps closed | D-001, D-003 |
| M3 | D-004 (timeout enforcement) + D-005 (risk classification) + D-010 (API validation) — hardening complete | D-004, D-005, D-010 |
| M4 | D-002 (ACP permission handling) + D-006 (streaming adaptation) — full feature set; verification gates pass | D-002, D-006 |

**Batch gate**: M1+M2 = Batch 1 complete. M3+M4 = Batch 2 complete.

---

## 7) Done definition (minimum)

1. `HostManager::shutdown()` calls `ProviderAdapter::shutdown()` for all active sessions before clearing state (D-007).
2. `AdmissionPolicy` methods are called in `create_session()` and `exec()` — session limits, provider allow/deny, and ops-per-session are enforced (D-008).
3. Native CLI probe works on both Unix and Windows (D-009).
4. Config paths are validated against traversal (D-011).
5. `ClaudeCliProvider` supports multi-turn conversation within a session (D-001) — either via persistent stdin or resume flags.
6. `CapabilityDescriptor::acp_full()` does not claim capabilities that `AcpProvider` cannot deliver (D-003).
7. All provider operations (`probe`, `launch`, `execute`, `shutdown`) are bounded by `TimeoutConfig` values (D-004).
8. `AutoToolRiskClassifier` classifies unknown tools by name pattern (D-005).
9. ACP `session/request_permission` messages are handled with policy delegation (D-002).
10. API handlers validate session ID path parameters (D-010).
11. V1.18 residual findings R1–R7 are all closed (R7 subsumed by D-004).
12. Verification passes: `cargo +nightly fmt --all`, `cargo clippy --all -- -D warnings`, and relevant Rust tests.

---

## 8) Out-of-scope items (tracked for future)

| ID | Title | Tracker ID | Target | Reason |
|----|-------|-----------|--------|--------|
| — | Native session persistence to disk | DF-23 | V1.20+ | Requires multi-turn first (D-001); product spec needed |
| — | New native CLI providers | — | V1.20+ | Codex, Gemini, etc. — separate wave |
| — | ACP streaming full adaptation | D-006 | May defer to V1.20 | Enhancement, not correctness |

---

## 9) Related index

- Previous: [v1.18-delivery-compass-v1.md](v1.18-delivery-compass-v1.md) §9 (deferred features), §11 (risks)
- Primary spec: [nexus42-single-binary-daemon-runtime-architecture-v1.md](nexus42-single-binary-daemon-runtime-architecture-v1.md) Section 10
- Residuals source: `../status.json` → `residual_findings["2026-05-15-v1.18-agent-host-core"]`
- Cross-version tracker: [deferred-features-cross-version-tracker-v1.md](deferred-features-cross-version-tracker-v1.md) (DF-18–DF-25)
- V1.18 archived plan: [../archived/plans/2026-05-15-v1.18-agent-host-core.json](../archived/plans/2026-05-15-v1.18-agent-host-core.json)

---

*Created: 2026-05-15. Status: Draft — Active planning SSOT for V1.19. 11 items consolidated from V1.18 deferred features (D-001–D-008) and QC residuals (R1–R7). 2 batches: Batch 1 (safety/correctness, 6 items), Batch 2 (hardening, 5 items). Estimated: ~10 tasks.*
