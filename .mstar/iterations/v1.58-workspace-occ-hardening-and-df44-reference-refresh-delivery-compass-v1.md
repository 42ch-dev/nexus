# V1.58 — Workspace OCC Hardening & DF-44 Reference Refresh — Delivery Compass v1

**Version**: V1.58 delivery
**Created**: 2026-06-22
**Status**: **Prepare active (P-1 in progress; pre-implement gate pending PM review)**
**Scope**: `nexus` OSS — harden workspace OCC (tracing + metrics + concurrent integration test + sha2 dep + path canonicalize + TOCTOU + blocking I/O) and converge capability surface quality (tracing gaps, reqwest reuse, help text, test vectors, fmt/hygiene protocol); ship DF-44 reference body refreshable pipeline (`nexus.reference.refresh` capability + daemon refresh-scheduler hook + `nexus42 reference refresh` CLI + DB migration).
**Predecessor**: [v1.57-df46-df47-full-parity-and-adapter-unification-delivery-compass-v1.md](v1.57-df46-df47-full-parity-and-adapter-unification-delivery-compass-v1.md) (Shipped 2026-06-22, PR #79 @ `76e02d41`)
**Successor**: TBD (V1.59+)
**North star**: Every reference source either has a refresh policy or is explicitly offline; every `nexus.reference.*` capability is rostered + admitted + tested; every workspace OCC operation has tracing + metrics + a concurrent integration test.

---

## 0. Grill decisions (PM-locked, 2026-06-22)

| # | Decision | Locked conclusion |
|---|----------|-------------------|
| Q1 | V1.58 main axis | **Track A (Workspace OCC hardening + capability quality convergence) + Track B (DF-44 Reference Body Refreshable Pipeline)**. T-A absorbs the V1.57+ backlog (32 residuals from V1.56 OCC/registry/routing surfaces + V1.57 process/V157). T-B ships DF-44 end-to-end: capability admission, daemon refresh-scheduler, CLI subcommand, DB migration. |
| Q2 | Wire contract posture | **Additive only** — new capability IDs (`nexus.reference.refresh` and likely 1-2 siblings for refresh policy/diff status); may include 1 DB migration to track refresh schedules / last-refreshed timestamps. CLI gains `nexus42 reference refresh` and `--reference-policy` flags. Cross `acp-capability-set.md` §4 roster (extends V1.57's 41-row roster with ~2 new IDs) + `capability-registry.md` + `cli-spec.md` §6.2N. |
| Q3 | Plan topology | **7 plans, dual-track, 3-wave dispatch**. Wave 1 (parallel): P0 (T-A) + P1 (T-B). Wave 2 (sequential T-A): P2 after Wave 1 accepts. Wave 3 (sequential T-B): P3 after Wave 2 accepts. P-mid meta tracking; P-last closeout. Track A canonical owner = `@fullstack-dev`; Track B canonical owner = `@fullstack-dev-2`. Matches V1.51-V1.57 rhythm. |
| Q4 | Spec governance | **Amend only** — no Master promotions. `acp-capability-set.md` §4 roster extends from 41 to ~43 rows (add `nexus.reference.refresh` + sibling). `capability-registry.md` body amend for refresh capability admission + per-ID test vectors. `cli-spec.md` adds §6.2N `reference refresh` subcommand. `daemon-runtime.md` adds refresh-scheduler hook section. `entity-scope-model.md` No change (reference refresh is reference-source-scoped, not KB-scoped). |
| Q5 | Compile / build / test baseline | `cargo check -p <crate>` per-edit iteration; `cargo clippy --all -- -D warnings` + `cargo test --all` pre-commit/CI gate. `cargo +nightly fmt --all` CI-enforced. `pnpm run codegen` runs if `schemas/` changes (Track B plans only). `cargo clean` once before final verification per `target/` hygiene policy. |
| Q6 | Branch policy | **Project-standard two-tier**: integration branch `iteration/v1.58` (PM creates from main after P-1 reviewed). Per-plan topic branches `feature/v1.58-{plan-slug}` created from integration. Hotfix: `fix/v1.58-{short}` if needed. P-1 working branch: `feature/v1.58-prepare` from main HEAD `c0c03f0d`. |
| Q7 | DF-44 closure semantics | DF-44 goes from "Open — Any future" to **Closed (V1.58)**. Static registration shipped V1.26; V1.58 adds the refreshable scan pipeline: `nexus.reference.refresh` capability + daemon refresh-scheduler + `nexus42 reference refresh [work_ref|all]` CLI + DB migration (extend `reference_sources` with `last_refreshed_at`, `refresh_policy`, `refresh_status` columns). Deferred-features-tracker DF-44 row removed; shipped-features-tracker appended. |

---

## 1. Scope / non-goals / out-of-scope

### 1.1 In scope

- **P-1**: This compass, seven plan stubs, and iteration index update.
- **P0 / Track A / Wave 1**: Workspace OCC hardening — absorb the 6 V1.56 medium workspace OCC residuals (R-V156P0-M001 sha2 dep workspace-managed; R-V156P0-M002 path boundary canonicalize/symlink/prefix; R-V156P0-M003 no concurrent integration test for OCC; R-V156P0-M004 blocking sync I/O in async handler; R-V156P0-M005 TOCTOU window; R-V156P0-M006 no metrics/tracing at OCC conflict). Capability surface quality — absorb V1.56 registry-refresh residuals (R-V156P1-M003 force param ignored; R-V156P1-M004 no tracing in capability handler; R-V156P1-M005 reqwest Client per invocation; R-V156P1-L001..L007: help text, body-size hammer, naming prefix, retry jitter, latency benchmark, generated_at determinism, structured metrics). Process residuals: R-V156-MIDQA-01 cargo +nightly fmt drift; R-V156-PROCESS-01 .sqlx/ cache hygiene protocol; R-V156P1-CACHE-01 .sqlx/ cache hygiene for test query! macros (paired with R-V156-PROCESS-01).
- **P1 / Track B / Wave 1**: DF-44 reference body refreshable pipeline core — `nexus.reference.refresh` capability admission + handler (extends `nexus.reference.*` family); `nexus.reference.refresh_policy.get` / `nexus.reference.refresh_status` capability IDs (if 1-2 siblings are needed); acp-capability-set.md §4 roster extend from 41 to ~43 rows; capability-registry.md body extension for refresh capability admission + per-ID test vectors; DB migration (extend `reference_sources` with `last_refreshed_at`, `refresh_policy`, `refresh_status` columns, or new `reference_refresh_log` table).
- **P2 / Track A / Wave 2**: Remaining V1.56 medium/low residuals — DF-56 independent/dependent slice polish (R-V156P2-M001..M003 null semantics / converge fan-in / expression routing integration tests + R-V156P2-L001..L004 latency bench / eval tracing / converge timeout / 0-predecessor loader error). Process: R-V156P2-CACHE-01 engine test fidelity (convergence tests must exercise real runtime paths). DF-56 dependent slice polish (R-V156P3-W001 inject_workspace_context zero tracing; R-V156P3-W002 registry.refresh invocation instrumentation; R-V156P3-S001 synthetic fallback source field ambiguous; R-V156P3-S002 throttle-path .await; R-V156P3-S004 with_workspace_state hook activation). V1.57-new residuals: R-V157P0-L001 AC5 plan wording reconciliation; R-V157P0-L002 per-ID failure-path test vectors; R-V157P1-W001 host-call smoke #[ignore] fix.
- **P3 / Track B / Wave 3**: Reference CLI and cross-cut tests — `nexus42 reference refresh [work_ref|all]` CLI subcommand wired to daemon refresh-scheduler; daemon-runtime.md refresh-scheduler hook section + integration; local-runtime-boundary.md topology update for refresh capability caller; cross-reference E2E test (CLI → daemon → capability::Registry::dispatch → reference_source refresh). Schema codegen (`pnpm run codegen`) if new capability enum variants added.
- **P-mid**: Meta tracking across 3 waves; no product code.
- **P-last**: Status.json initialization + deferred-tracker update + shipped-features-tracker V1.58 snapshot + tech-debt rollup + Profile B compaction + report-only QA.

### 1.2 Non-goals / out-of-scope

| Item | Disposition |
| --- | --- |
| T-C (dual outbox / capability registry 拆分) | OUT; not in V1.58 scope. |
| Platform publish / platform integration | OUT; `metadata.platform_integration` remains paused — local-only until further notice. |
| Stripe / billing / entitlements | OUT; platform/V2.0+ dependency. |
| Standalone MCP server (DF-49) | OUT; backlog. |
| World KB cross-profile unification | OUT; not OCC or reference-surface work. |
| `nexus.publish.chapter` / `nexus.publish.story` full implementation | OUT; rostered as OUT in acp §4 roster. |
| New profile types (beyond essay/game-bible/script) | OUT; not in V1.58 scope. |
| DF-12 (dual outbox consolidation) | OUT; "Any future". |
| DF-13 (entitlements), DF-16 (Stripe) | OUT; "V2.0+". |
| DF-49 (MCP server) | OUT; "Backlog". |
| DF-55 (cloud context assembly) | OUT; "V2.0+". |
| DF-59 (platform publish) | OUT; "Backlog". |
| BL-01..BL-08 | OUT; "Backlog". |
| DB migrations / wire enum edits in P-1 | OUT for prepare; implement plans describe them, do not commit them here. |

### 1.3 Wire contracts note

`wire_contracts_changed: true` for V1.58 — additive enum: new capability IDs (`nexus.reference.refresh` and likely `nexus.reference.refresh_policy.get` / `nexus.reference.refresh_status` siblings) extend the capability roster; new CLI subcommand `nexus42 reference refresh` extends CLI surface; likely 1 DB migration (extend `reference_sources` with refresh-tracking columns or new `reference_refresh_log` table) extends schema. Per project convention this crosses the additive-enum threshold.

---

## 2. Normative specs

| Spec | Starting status | V1.58 target / transition | Plans |
| --- | --- | --- | --- |
| [acp-capability-set.md](../knowledge/specs/acp-capability-set.md) | Master | **Amend §4** — extend roster from 41 to ~43 rows: add `nexus.reference.refresh` + likely `nexus.reference.refresh_policy.get` / `nexus.reference.refresh_status` siblings. Cross-validate via `catalog_registry_invariant_all_ids_present`. | P1, P-last |
| [capability-registry.md](../knowledge/specs/capability-registry.md) | Master (V1.54 P-last) | **Amend** — body extension for refresh capability admission + per-ID test vectors (success + failure paths for `nexus.reference.refresh`). | P1, P3, P-last |
| [cli-spec.md](../knowledge/specs/cli-spec.md) | Master | **Amend** — add §6.2N for new `reference refresh` subcommand (`nexus42 reference refresh [work_ref|all] --policy <on_change|scheduled> --dry-run`); document CLI/daemon reference-refresh path. | P3, P-last |
| [daemon-runtime.md](../knowledge/specs/daemon-runtime.md) | Master | **Amend** — add refresh-scheduler hook section: daemon-side periodic reference-source freshness check + scheduled refresh dispatch through capability::Registry. | P1, P3, P-last |
| [local-runtime-boundary.md](../knowledge/specs/local-runtime-boundary.md) | Master | **Amend** — topology update for refresh capability caller (CLI `reference refresh` → daemon IPC → `capability::Registry::dispatch`). | P3, P-last |
| [entity-scope-model.md](../knowledge/specs/entity-scope-model.md) | Master | **No change** — reference refresh is reference-source-scoped (per `nexus-knowledge` crate domain), not KB-scoped. Document the boundary. | — |
| [reference-knowledge.md](../knowledge/specs/reference-knowledge.md) | Does not exist | **New Draft (V1.58)** — reference body refreshable scan pipeline spec: refresh policy model (on_change / scheduled / offline), refresh scheduler contract, DB schema for refresh tracking. Authored in P1, promoted to Master at P-last. | P1, P-last |
| [workspace-session.md](../knowledge/specs/workspace-session.md) (or equivalent OCC spec) | Master | **Amend** — OCC hardening notes: concurrent integration test spec, tracing/metrics requirements, TOCTOU mitigation, path canonicalization contract. | P0, P-last |

**wire_contracts_changed: true** for V1.58 — see §1.3 rationale.

---

## 3. Plan topology

V1.58 uses **7 registered plans** across two implement tracks (Track A + Track B) with a 3-wave topology. Wave 1 runs P0 (Track A) and P1 (Track B) in parallel; Waves 2 and 3 are sequential within each track.

| Slot | Plan ID | Track | Effort | Owner | Review | Wave | Dependency |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P-1 | `2026-06-22-v1.58-compass-and-plan-stubs` | prepare | M | `@project-manager` (PM whitelist; authored by `@writing-specialist`) | QC skipped | — | — |
| P0 | `2026-06-22-v1.58-workspace-occ-hardening` | Track A | L | `@fullstack-dev` (Track A canonical owner) | Tri-review + mid-QA + targeted fix-wave | Wave 1 (parallel w/ P1) | P-1 PM signoff |
| P1 | `2026-06-22-v1.58-df44-reference-refresh-pipeline` | Track B | L | `@fullstack-dev-2` (Track B canonical owner) | Tri-review + mid-QA + targeted fix-wave | Wave 1 (parallel w/ P1) | P-1 PM signoff |
| P2 | `2026-06-22-v1.58-capability-quality-convergence` | Track A | M | `@fullstack-dev` (Track A canonical owner) | Tri-review + mid-QA + targeted fix-wave | Wave 2 (after Wave 1 accepts) | P0 + P1 merged to `iteration/v1.58` |
| P3 | `2026-06-22-v1.58-reference-cli-and-cross-cut-tests` | Track B | M | `@fullstack-dev-2` (Track B canonical owner) | Tri-review + mid-QA + targeted fix-wave | Wave 3 (after Wave 2 accepts) | P0 + P1 + P2 merged to `iteration/v1.58` |
| P-mid | `2026-06-22-v1.58-mid-qc-and-fix-waves` | meta | M | `@project-manager` | Meta tracking | Across all 3 waves | — |
| P-last | `2026-06-22-v1.58-hygiene-and-closeout` | closeout | M | `@project-manager` | Report-only QA | After Wave 3 | All 4 implement plans integrated + reviewed |

**Track-ownership pattern** (PM-locked during P-1): each track has one canonical owner across all waves. Track A = `@fullstack-dev` (P0, P2). Track B = `@fullstack-dev-2` (P1, P3). Wave 1 dispatches P0 on `fullstack-dev` and P1 on `fullstack-dev-2` in parallel on separate worktrees. Wave 2 dispatches P2 on `fullstack-dev` and Wave 3 dispatches P3 on `fullstack-dev-2` — both sequential within their track.

**3-wave dispatch rhythm**:

- **Wave 1 (parallel)**: P0 (Track A) and P1 (Track B) implement in parallel on separate worktrees from `iteration/v1.58`; each merges to integration independently; each receives its own tri-review + mid-QA + targeted fix-wave. Wave 1 ends when BOTH P0 and P1 are accepted.
- **Wave 2 (single)**: P2 (Track A) implements from `iteration/v1.58` after P0+P1 acceptance; merges to integration; receives tri-review + mid-QA + targeted fix-wave.
- **Wave 3 (single)**: P3 (Track B) implements from `iteration/v1.58` after P0+P1+P2 merged; merges to integration; receives tri-review + mid-QA + targeted fix-wave.
- **P-last**: runs after Wave 3 completes. Report-only QA, not tri-review.

---

## 4. Branch policy

V1.58 uses the project-standard **two-tier branch model** from [`.mstar/AGENTS.md`](../AGENTS.md): an iteration integration branch plus per-plan topic branches. Topic branches are created **from `iteration/v1.58`, not from `main`**. PM creates `iteration/v1.58` from `main` after this Prepare task is reviewed.

| Tier | Branch | Purpose |
| --- | --- | --- |
| Iteration integration | `iteration/v1.58` (PM creates from `main` AFTER this Prepare task is reviewed) | Single integration line and QC/QA review branch; final PR target is `main`. |
| Prepare working branch | `feature/v1.58-prepare` (where P-1 artifacts are authored) | Compass + plan stubs. PM commits after review. |
| P0 topic | `feature/v1.58-workspace-occ-hardening` | Workspace OCC hardening + capability surface quality; from `iteration/v1.58` after PM signoff; merges back to integration. |
| P1 topic | `feature/v1.58-df44-reference-refresh-pipeline` | DF-44 reference body refreshable pipeline core; from `iteration/v1.58` after PM signoff; merges back to integration. |
| P2 topic | `feature/v1.58-capability-quality-convergence` | Remaining V1.56/V1.57 residual convergence; from `iteration/v1.58` after P0+P1 merged; merges back to integration. |
| P3 topic | `feature/v1.58-reference-cli-and-cross-cut-tests` | Reference CLI + cross-cut E2E tests; from `iteration/v1.58` after P0+P1+P2 merged; merges back to integration. |
| P-last closeout | `feature/v1.58-hygiene-and-closeout` | Status.json init, tracker update, Profile B compaction; merges to integration. |

**Dispatch / merge policy**: PM dispatches Wave 1 (P0 + P1) in lockstep. Every implement plan must be merged to `iteration/v1.58` before its formal tri-review and mid-QA. QC/QA assignments use `Working branch: iteration/v1.58` at integrated `HEAD`.

---

## 5. Planned tracks / plan inventory

| Plan | Track | Deliverables |
| --- | --- | --- |
| P-1 | prepare | Compass doc; seven plan stubs; iteration index update only. |
| P0 | Track A / Wave 1 | Workspace OCC hardening: absorb R-V156P0-M001 (sha2 dep workspace-managed), R-V156P0-M002 (path canonicalize/symlink/prefix), R-V156P0-M003 (concurrent integration test for OCC), R-V156P0-M004 (blocking sync I/O → async), R-V156P0-M005 (TOCTOU window mitigation), R-V156P0-M006 (metrics/tracing at OCC conflict). Capability surface quality: absorb R-V156P1-M003 (force param handled), R-V156P1-M004 (tracing in capability handler), R-V156P1-M005 (reqwest Client reuse), R-V156P1-L001..L007 (help text, body-size, naming, retry jitter, latency bench, generated_at determinism, structured metrics). Process: R-V156-MIDQA-01 (fmt drift), R-V156-PROCESS-01 (.sqlx/ cache hygiene). V1.57-new: R-V157P0-L001 (AC5 wording), R-V157P0-L002 (per-ID failure-path test vectors). |
| P1 | Track B / Wave 1 | DF-44 reference body refreshable pipeline: `nexus.reference.refresh` capability admission + handler; `nexus.reference.refresh_policy.get` / `nexus.reference.refresh_status` capability IDs (if needed); acp-capability-set.md §4 roster extend (41→~43 rows); capability-registry.md body extension for refresh capability; DB migration (extend `reference_sources` with `last_refreshed_at`, `refresh_policy`, `refresh_status` columns, or new `reference_refresh_log` table); new Draft spec `reference-knowledge.md`. |
| P2 | Track A / Wave 2 | Remaining V1.56 medium/low residuals: DF-56 independent slice (R-V156P2-M001..M003 + L001..L004), DF-56 dependent slice (R-V156P3-W001/W002 + S001/S002/S004), process (R-V156P2-CACHE-01 engine test fidelity). V1.57-new: R-V157P1-W001 (host-call smoke #[ignore] fix). |
| P3 | Track B / Wave 3 | Reference CLI: `nexus42 reference refresh [work_ref|all]` CLI subcommand; daemon-runtime.md refresh-scheduler hook integration; local-runtime-boundary.md topology update; cross-reference E2E test (CLI → daemon → capability::Registry::dispatch → reference_source refresh); schema codegen (`pnpm run codegen`). |
| P-mid | meta | Checklist and evidence rhythm for 3-wave merge + tri-review + mid-QA + fix-wave cycles. No product code. |
| P-last | closeout | Status.json initialization (7 plans registered; `metadata.latest_active_iteration = "V1.58"`; `metadata.integration_branch = "iteration/v1.58"`); deferred-features-tracker DF-44 row removed; shipped-features-tracker V1.58 snapshot; `reference-knowledge.md` Draft → Master promotion; tech-debt rollup; Profile B compaction; report-only QA. |

### 5.1 Carry-forward absorption choices

V1.58 absorbs **all** V1.57+ open residuals distributed across Track A plans. DF-44 closure is the single Track B deferred feature closed this iteration.

| Carry-forward | Absorbing plan | Reason |
| --- | --- | --- |
| R-V156P0-M001 (sha2 dep workspace-managed) | P0 | Core workspace OCC concern. Fix the dependency declaration so sha2 is scoped to the workspace crate. |
| R-V156P0-M002 (path boundary canonicalize/symlink/prefix) | P0 | Core workspace OCC concern. Add path canonicalization on workspace open; reject symlink traversal; enforce prefix boundary. |
| R-V156P0-M003 (no concurrent integration test for OCC) | P0 | Core workspace OCC concern. Add a concurrent integration test exercising two writers on the same workspace. |
| R-V156P0-M004 (blocking sync I/O in async handler) | P0 | Workspace handler reliability. Replace blocking std::fs calls with tokio::fs equivalents. |
| R-V156P0-M005 (TOCTOU window) | P0 | Workspace session integrity. Close the read-check-write window with an atomic CAS or transaction guard. |
| R-V156P0-M006 (no metrics/tracing at OCC conflict) | P0 | Observability. Add tracing::warn! + metrics counter on OCC conflict detection. |
| R-V156P1-M003 (force param ignored in host handler) | P0 | Capability surface quality. Wire the `force` param through the registry.refresh handler; add test for force path. |
| R-V156P1-M004 (no tracing in capability handler) | P0 | Observability. Add tracing spans to the registry.refresh handler. |
| R-V156P1-M005 (reqwest::Client per invocation no reuse) | P0 | Performance. Extract a shared LazyLock<reqwest::Client> with connection pooling. |
| R-V156P1-L001..L007 (help text, body-size, naming, retry jitter, latency bench, generated_at, metrics) | P0 | Capability surface quality. 7 low-severity polish items grouped for efficient absorption. |
| R-V156-MIDQA-01 (cargo +nightly fmt drift) | P0 | Process. Run `cargo +nightly fmt --all` and verify clean in CI. |
| R-V156-PROCESS-01 + R-V156P1-CACHE-01 (.sqlx/ cache hygiene protocol — workspace AND test queries) | P0 | Process. Document and enforce .sqlx/ cache refresh protocol with `--tests` flag (run `cargo sqlx prepare --workspace -- --tests` before commit; commit `.sqlx/` artifacts; add CI check). Two residuals share the same lesson — workspace AND test query! macros both need cache hygiene. |
| R-V156P2-M001..M003 (null semantics, converge fan-in, expr routing tests) | P2 | DF-56 independent slice polish. Surgical convergence of remaining medium gaps. |
| R-V156P2-CACHE-01 (engine test fidelity — convergence tests bypass real runtime) | P2 | Process. Audit existing convergence tests to ensure they exercise real `record_converge_arrival` runtime paths; replace bypass paths with real-runtime E2E tests. |
| R-V156P2-L001..L004 (latency bench, eval tracing, converge timeout, loader error) | P2 | DF-56 independent slice polish. Low-severity polish items. |
| R-V156P3-W001 (inject_workspace_context zero tracing) | P2 | DF-56 dependent slice. Add tracing to workspace context injection path. |
| R-V156P3-W002 (registry.refresh invocation instrumentation) | P2 | DF-56 dependent slice. Add latency/failure instrumentation to registry.refresh invocation. |
| R-V156P3-S001 (synthetic fallback source field ambiguous) | P2 | DF-56 dependent slice. Clarify source field semantics; add doc comment or rename. |
| R-V156P3-S002 (throttle-path .await) | P2 | DF-56 dependent slice. Ensure throttle path properly yields with .await. |
| R-V156P3-S004 (with_workspace_state hook activation) | P2 | DF-56 dependent slice. Activate the with_workspace_state hook for registry.refresh dependent edges. |
| R-V157P0-L001 (AC5 plan wording reconciliation) | P2 | Spec/doc. Reconcile V1.57 P0 plan text with actual 41-row/18-shipped count. |
| R-V157P0-L002 (per-ID failure-path test vectors) | P2 | Test coverage. Add missing per-ID failure-path test vectors for capability handlers. |
| R-V157P1-W001 (host-call smoke #[ignore]) | P2 | Test enablement. Un-ignore host-call smoke tests or add documented justification. |
| DF-44 (reference body refreshable pipeline) | P1 + P3 | Track B primary scope. P1 ships capability + DB migration; P3 ships CLI + cross-cut tests. |

### 5.2 Track ordering guardrails

- **Track A is sequential**: P0 must ship workspace OCC hardening + capability surface quality before P2 can polish routing/DAG residuals. P2's DF-56 polish depends on a stable post-hardening workspace and capability surface.
- **Track B is sequential**: P1 must ship the refresh capability + DB migration before P3 can wire the CLI subcommand and cross-cut E2E tests. P3's `nexus42 reference refresh` depends on the capability handler existing in the registry.
- **Cross-track dependency**: P3 cannot start until P0 + P1 + P2 are all merged to `iteration/v1.58` — because P3 needs the hardened workspace (P0), the refresh capability (P1), and the post-convergence stable surface (P2).
- **P-last** is not a hidden implement plan; it closes/publishes only what P0–P3 have already made reviewable.

---

## 6. Deferred design sketches

V1.58 intentionally **opens zero new deferrals**. All absorbed V1.57+ residuals are closed; DF-44 is closed. The following open items are explicitly NOT absorbed:

- **DF-12** (dual outbox consolidation) — remains "Any future".
- **DF-13** (entitlements), **DF-16** (Stripe) — remain "V2.0+".
- **DF-49** (MCP server) — remains "Backlog".
- **DF-55** (cloud context assembly) — remains "V2.0+".
- **DF-59** (platform publish) — remains "Backlog".
- **BL-01 through BL-08** — all remain "Backlog".
- **T-C** (dual outbox / capability registry 拆分) — remains OUT.
- **V1.52-era carry-forwards NOT in V1.58 scope** — 14 low-severity polish residuals remain deferred to V1.59+ WL-A sweep (`R-V150KBED-02`, `R-V151Q1-10`, `R-V152TA-S001..S011`, `R-V152TB-W006/W007/W008`). These are out of V1.58 OCC/DF-44 scope but should be picked up in a future WL-A consolidation pass.
- **Platform / MCP / registry-publish / Stripe / preset-registry** — all OUT per §1.2.

Any new deferral discovered during P0–P3 must be recorded in the owning plan and root `status.json.residual_findings` before the plan can enter review.

---

## 7. Risk register

| Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- |
| Workspace OCC hardening introduces regression in existing workspace session behavior (P0 touches session open/close/commit paths) | Medium | High | P0 must include concurrent integration test (R-V156P0-M003) + existing workspace test suite regression gate. All existing workspace tests must pass before P0 enters review. |
| DB migration for reference refresh tracking conflicts with existing `reference_sources` schema assumptions | Low | Medium | Migration is additive (new columns, not schema restructure). `ALTER TABLE reference_sources ADD COLUMN` with DEFAULT values. Pre-migration integration test verifies existing rows survive migration. |
| Capability roster extension from 41 to ~43 rows triggers cross-validation test failure if catalog/registry drift | Low | Medium | `catalog_registry_invariant_all_ids_present` test is the gate. P1 must update both the catalog table and registry handler set atomically. |
| V1.57+ residual absorption count (22+ items across P0/P2) exceeds per-plan review budget — QC tri-review may surface new findings | Medium | Medium | Residuals are grouped by surface (workspace OCC, capability quality, routing polish). Each group is surgical; no cross-cutting refactors. P-mid tracks residual closure rate across waves. |
| `reqwest::Client` reuse extraction (R-V156P1-M005) changes initialization order and may interact with daemon lifecycle | Low | Low | LazyLock<Client> with static lifetime; no daemon lifecycle dependency. Integration test covers daemon-mode reqwest reuse. |
| DF-56 dependent slice residuals (R-V156P3-*) interact with registry.refresh capability newly introduced in P1 | Medium | Medium | P2's DF-56 dependent slice work explicitly depends on P1's registry.refresh capability being stable. Sequential dispatch (P2 after P0+P1 merged) ensures this dependency is met. |
| `nexus42 reference refresh` CLI subcommand name collision risk with existing `nexus` prefix patterns | Low | Low | `reference` is a new CLI noun (V1.58), not a collision with existing creator/world/kb/works nouns. CLI three-plane IA unchanged. |
| P-last status.json initialization fails due to stale main HEAD after implement plans merged to integration | Low | Medium | P-last operates on `iteration/v1.58` HEAD after all implement plans merged. Status.json update is a surgical metadata-only change. |

---

## 8. QC/QA strategy

| Plan | Review strategy | Notes |
| --- | --- | --- |
| P-1 | QC skipped | PM-whitelisted Prepare-phase plan authoring; PM reviews before implement dispatch. |
| P0 | Merge to integration → tri-review → mid-QA → targeted fix-wave | Focus: workspace OCC hardening correctness (tracing, metrics, TOCTOU, async I/O, path canonicalization); capability surface quality (tracing gaps, reqwest reuse, help text); regression on existing workspace/capability tests. |
| P1 | Merge to integration → tri-review → mid-QA → targeted fix-wave | Focus: refresh capability admission correctness; DB migration safety; acp §4 roster completeness; per-ID test vectors for new refresh IDs; cross-validation test update. |
| P2 | Merge to integration → tri-review → mid-QA → targeted fix-wave | Focus: DF-56 routing/DAG polish surgical correctness; V1.57-new residual closure (host-call smoke un-ignore, per-ID test vectors, AC wording); no regression on existing orchestration/registry tests. |
| P3 | Merge to integration → tri-review → mid-QA → targeted fix-wave | Focus: CLI subcommand wiring correctness; daemon refresh-scheduler hook integration; cross-reference E2E test completeness; schema codegen correctness. |
| P-mid | Meta tracking | No standalone QC; PM uses it to ensure review evidence exists before next dispatch. |
| P-last | Report-only QA | QA verifies status.json initialization, tracker updates, spec promotion, Profile B compaction, tech-debt rollup. |

**Rule**: each implement plan must finish by merging its topic branch into `iteration/v1.58`; QC and QA review the integrated branch, not an isolated topic branch. P-last is not a feature implementation plan; it is closeout plus report-only QA.

**Total**: 4 implement plans × tri-review = 12 QC reports + 4 mid-QA reports. P-last adds 1 report-only QA report.

---

*Last updated: 2026-06-22 (initial compass). Status: Prepare active — P-1 in progress; pre-implement gate pending PM review.*
