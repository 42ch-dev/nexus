# V1.53 Capability Surface Completion & Skills CLI Cleanup — Delivery Compass v1

**Version**: V1.53 delivery  
**Created**: 2026-06-20  
**Status**: **Active (P-1 Done; P0 pending)**  
**Scope**: `nexus` OSS — complete the runtime capability surface around a registry SSOT, ship the first DF-46 read-heavy `nexus.*` slice, and remove the obsolete skills-export CLI/spec line.  
**Predecessor**: [v1.52-author-completion-and-multi-branch-preset-orchestration-delivery-compass-v1.md](v1.52-author-completion-and-multi-branch-preset-orchestration-delivery-compass-v1.md)  
**Successor**: TBD (V1.54+)  
**North star**: Make every supported `nexus.*` capability discoverable, admissible, dispatched, testable, and failure-shaped through one runtime registry so future capability expansion is additive rather than another parallel command/tool matrix.

---

## 0. Grill decisions (PM-locked, 2026-06-20)

| Round | PM-locked conclusion |
| --- | --- |
| Q1 | V1.53 main axis is **Theme A — Capability Surface Completion**. |
| Q2 | Plan topology is five slots: P-1 prepare, P0 registry unification, P1 DF-46 read slice, P-c skills CLI cleanup, P-last spec hygiene closeout. |
| Q3 | P0 refactor seam is a full `CapabilityRegistry` SSOT: id → access → admission → handler → ACP wire → failure mode → test vector. |
| Q4 | **DF-50 Cancelled**. `nexus42 acp skills export|verify`, the spec, and tracker row are closed because skills-export is redundant with the static committed `embedded-skills/` model. |
| Q5 | P1 anchor tools are `nexus.world.snapshot.get`, `nexus.timeline.recent.get`, `nexus.kb_snapshot.read` (name TBD in P0), `nexus.manuscript.chapter.get`, and `nexus.observability.daemon.health`. |
| Q6 | Spec form: add `capability-registry.md` as Draft overlay; amend `acp-capability-set.md` with a registry pointer; amend `agent-nexus-tool-bridge.md`; delete `skills-export-compatibility.md` and fix references. |
| Q7 | Review strategy: P-1 no QC; P0/P1 tri-review with emphasis; P-c single-review (`qc1`); P-last report-only QA. |
| Q8 | Branch/parallel policy: two-tier `iteration/v1.53` + `feature/v1.53-*`; sequential P-1 → P0 → P1 → P-c → P-last; P0 complete before P1/P-c PR; peak one worktree. |

**Additional PM-locked P0 rule**: P0 sub-phases must carry explicit cutover triggers; no lingering dual paths. Any temporary parallel path during P0 must be recorded in the P0 plan `Roadmap / Batch Plan` or as `residual_findings` / `deferred`.

---

## 1. Scope / non-goals / out-of-scope

### 1.1 In scope

- Theme A capability surface completion.
- Capability registry Draft overlay and Master-spec pointers.
- P0 central registry SSOT refactor plan.
- P1 first DF-46 read-heavy `nexus.*` tool slice.
- P-c skills-export CLI/spec/tracker cleanup.
- P-last Profile B and shipped-feature tracker hygiene.

### 1.2 Non-goals / out of scope

| Item | Disposition |
| --- | --- |
| Game-bible / script implementation | V1.54+ |
| Standalone MCP server | Out; DF-49 remains backlog |
| Cloud assemble / platform publish | Out; platform remains paused |
| Stripe / entitlements | Out; V2.0+ platform dependency |
| Third-party registry / preset publish | Out; backlog |
| Static committed skills model | Out of P-c deletion; do **not** touch `embedded-skills/` or skills runtime sync/link code |

---

## 2. Normative specs

| Spec | Status in V1.53 | Action |
| --- | --- | --- |
| [acp-capability-set.md](../knowledge/specs/acp-capability-set.md) | Master | Add §0.5 pointer: logical catalog only; registry is runtime SSOT. |
| [agent-nexus-tool-bridge.md](../knowledge/specs/agent-nexus-tool-bridge.md) | Master/Feature line | Amend §8 to record skills-export cancellation and registry direction. |
| [capability-registry.md](../knowledge/specs/capability-registry.md) | Draft overlay V1.53 (new) | Framework only in P-1; field semantics iterate in P0. |
| [cli-spec.md](../knowledge/specs/cli-spec.md) | Master amend | Remove `nexus42 acp skills export|verify` and retire Skills-second export section. |
| `skills-export-compatibility.md` | Retired | Deleted in P-1; DF-50 archived as Cancelled. |

---

## 3. Plan topology

| Slot | Plan ID | Effort | Owner | Review | Dependency |
| --- | --- | --- | --- | --- | --- |
| P-1 | `2026-06-20-v1.53-capability-registry-prepare` | S | `@project-manager` | No QC | — |
| P0 | `2026-06-20-v1.53-capability-registry-unification` | L | `@fullstack-dev` | Tri-review + emphasis | P-1 Done |
| P1 | `2026-06-21-v1.53-df46-read-capability-slice` | M/L | `@fullstack-dev-2` | Tri-review | P0 Done; no PR before P0 merge to integration |
| P-c | `2026-06-22-v1.53-skills-cli-cleanup` | XS | `@fullstack-dev` | Single-review (`qc1`) | P0 Done |
| P-last | `2026-06-22-v1.53-spec-hygiene-closeout` | S | `@project-manager` | Report-only QA | P0 + P1 + P-c Done |

---

## 4. Branch policy

| Tier | Branch | Purpose |
| --- | --- | --- |
| Iteration integration | `iteration/v1.53` | Single integration line; final PR target is `main`. |
| Per-plan topic | `feature/v1.53-<slug>` | One feature branch per plan, merged sequentially into `iteration/v1.53`. |

**Dispatch order**: P-1 → P0 → P1 → P-c → P-last. P0 must be complete before any P1/P-c PR. Peak worktree count is one.

---

## 5. Planned tracks / plan inventory

| Plan | Track | Inventory |
| --- | --- | --- |
| P-1 | prepare | Compass, Draft overlay framework, Master pointers, skills-export retirement, plan stubs, status/tracker activation. |
| P0 | registry | Adapter-first registry seam: introduce → cutover → cleanup; explicit cutover triggers per sub-phase. |
| P1 | DF-46 slice | Five read-heavy tools: world snapshot, timeline recent, KB snapshot read, manuscript chapter get, daemon health. |
| P-c | cleanup | Remove obsolete CLI skills export/verify surface and tests; update tracker/archive. |
| P-last | closeout | Profile B compaction and V1.53 shipped snapshot after implementation plans land. |

---

## 6. Risks / rollback

| Risk | Mitigation / rollback |
| --- | --- |
| P0 registry cutover breaks existing host-tool dispatch | Adapter-first rollout; each sub-phase has a cutover trigger and rollback point to previous dispatch table. |
| Lingering dual paths after P0 | PM-locked Roadmap Gate: temporary parallel paths must be written in P0 plan or residual/deferred state. |
| Fast migration hides behavioral drift | No “fast migration” without explicit Roadmap Gate entry, handler parity tests, and failure-mode comparison. |
| P-c scope creep into static skills runtime | P-c must not touch `embedded-skills/`, `embedded_skills.rs`, `skill_link.rs`, `skill_sync.rs`, or `crates/nexus42/src/skills/`. |
| Spec retirement link rot | P-1 deletes the spec and updates current indexes/pointers; historical references remain historical unless they point to live authority. |

---

## 7. Acceptance criteria

| Plan | Acceptance |
| --- | --- |
| P-1 | Compass and five stubs written; `capability-registry.md` Draft overlay exists; `acp-capability-set.md` and bridge pointers amended; skills-export spec deleted; DF-50 removed from active tracker and archived Cancelled; `status.json` activates V1.53. |
| P0 | Registry SSOT owns id/access/admission/handler/wire/failure/test vector; three cutover triggers executed; no untracked double-path remains. |
| P1 | Five read-heavy tools route through registry; naming sub-grill resolved; tests cover success/failure/admission vectors. |
| P-c | Obsolete CLI commands and tests removed; tracker/archive reflect DF-50 Cancelled; no static committed skills model files touched. |
| P-last | Profile B compaction complete; `latest_ship` updated to V1.53; shipped-features tracker §2 gains V1.53 snapshot; report-only QA passes. |

---

*Last updated: 2026-06-20 — V1.53 P-1 prepare; five plans registered; capability registry Draft overlay introduced; DF-50 Cancelled.*
