---
iteration_id: V1.96
start_date: 2026-07-07
end_date: 2026-07-07
status: completed
iteration_base_branch: main
target_branch: main
plans:
  - 2026-07-07-v1.96-implement-rework
---

# V1.96 — Setup Wizard Rework & Daemon Diagnostic Chain — Delivery Compass v1

**Status**: active (Phase 1 in flight — Review & Edit chain pending). Headline:
V1.95 shipped 6 setup-wizard bugfixes (PR #124, merge `fa4815fb`), but
post-merge author testing surfaced **3 concrete regressions/gaps**, one of
which is a P0 blocker. This iteration closes them with a true layout rework
(full DESIGN.md audit) and a real daemon-diagnostic chain (stderr capture +
wizard timeout + `'starting'` state handling). No new product surface; no
new wire contracts. `wire_contracts_changed: false`.

## 0. Context

V1.95 was supposed to fix the 6 setup-wizard bugs blocking first launch. PR
#124 merged with QC tri Approve (6 residuals deferred to V1.96). The author
then did a real wipe-and-relaunch smoke test (`~/.nexus42/` cleared) and
reported 3 issues, quoted verbatim from the feedback round:

1. **Layout is still ugly.** "这个布局真难看，太丑了，你必须提升高级感。
   可以考虑Steps 和右边内容呈现一体化设计，现在 steps 和步数文字都没对齐。
   整体我认为需要居中呈现。" The V1.95 left-sidebar layout landed but is
   visually unrefined: the wizard is left-aligned (no centering), the
   `StepIndicator` circle sits at the top of a 56px row while the label is
   vertically centered → circle and number do not align horizontally with
   the label, and the sidebar + content card read as two disconnected blocks
   (no integrated feel). The V1.95 token-pipeline fix was necessary but not
   sufficient — the layout IA itself needs a redesign, not a CSS-variable
   patch.
2. **Step 1 Browse button shows `[object Object]`.** "点 Browse 按钮报错
   `[object Object]`." Root cause: `setup-step-welcome.tsx:54` uses
   `err instanceof Error ? err.message : String(err)`. Tauri invoke errors
   arrive as plain objects (with a `message` field), not `Error` instances,
   so `String(err)` returns the literal string `[object Object]`. The daemon
   step already has the correct `errorMessage()` helper
   (`setup-step-daemon.tsx:85-90`) that checks `'message' in err` — but it
   was never extracted to a shared module. Author also requested: Browse
   button inline with the location display (stronger association), errors via
   unified hint/notification (not inline `<p>`), Continue as a large bottom
   button (visual prominence).
3. **Step 2 daemon never leaves "Starting daemon…".** "依然一直处于
   'Starting daemon…'的状态，长时间没反应。属于极其严重P0 级别的 blocker。
   （注意：我测试时已经清空了 ~/.nexus42/ 目录，没有任何残余）" This is
   **not** the V1.95 migration-mismatch scenario — `~/.nexus42/` was wiped.
   Multiple root causes confirmed by code reading:
   - **React subscription race**: `setup-step-daemon.tsx` `useEffect`
     subscribes via `desktop.onDaemonStatusChanged`. The callback only
     branches on `'running'` / `'error'` / `'stopped'` — there is **no
     `'starting'` branch**. The daemon auto-starts at Tauri boot
     (`lib.rs:529-538`) and enters `DaemonState::Starting` **before** the SPA
     subscribes. The first status the SPA receives is `state: 'starting'` →
     the callback does nothing → the wizard stays in the "Starting daemon…"
     UI forever (no state update).
   - **Stderr discarded** (V1.95 residual `R-V195-ARCH-STRERR-GAP`, medium):
     `sidecar.rs:246` does `let (_rx, child) = command.spawn()...` — the
     `_rx` receiver is dropped, so the daemon's real stderr (the actual crash
     reason — migration error, missing config, port bind failure) is never
     captured. `DaemonStatus.detail` only ever carries generic SidecarManager
     strings ("Daemon did not start. Check the logs…"). Even when the wizard
     does receive an Error status, the message is unhelpful.
   - **No wizard-side timeout** (V1.95 residual `R-V195QC3-S001`, low): if
     the sidecar never emits a terminal status event (e.g. SPA subscribes
     after the only `notify()` fired, or the daemon process is alive but
     hung), the wizard waits indefinitely. `useEffect` cleanup only
     unsubscribes; there is no hard timeout that forces a probe or surfaces
     "Taking longer than expected".
   - **First-launch daemon boot path** (suspected, needs RCA during
     implement): with a wiped `~/.nexus42/`, there is no `config.toml` and no
     `active_creator_id`. Per the V1.95 plan T9 architecture note,
     `crate::config::resolve_state_db_path()` requires `active_creator_id`;
     `WorkspaceState::initialize()` uses `?` on it → daemon may crash or hang
     on boot. The SidecarManager would normally detect a quick exit via
     `process_alive(pid)` and transition to Error — but if the daemon process
     stays alive (hung in initialization) the 15s `HEALTH_START_TIMEOUT`
     applies and then Error fires. Either way the wizard **should** eventually
     see Error — the fact that it does not is the React subscription bug
     above. Capturing stderr will confirm the true daemon-side cause.

## 0.1 Terminology conventions

Inherits V1.95 vocabulary. No new terms.

## 1. Locked Decisions (grill-me output, 2026-07-07)

| Decision | Resolution |
|---|---|
| Iteration shape | **Single V1.96 — bundle all 3 issues.** The daemon P0 needs architect-level RCA + stderr capture (not a quick hotfix), and the layout work shares DESIGN.md token evaluation with the Step1/daemon UX tweaks. Splitting into hotfix + V1.96 would fragment the Prepare-phase DESIGN.md work and double QC overhead. |
| Layout depth | **Full DESIGN.md "Setup Wizard Surface" audit + Level upgrade.** Pull architect for a dedicated `apps/web/DESIGN.md` "Setup Wizard Surface" section (spacing, hierarchy, motion, dark theme) — not just a token-pipeline patch. Rewrite `setup-wizard-page.tsx` + `StepIndicator` + `setup-step-*` visual layer against the audited tokens. Risk acknowledged: V1.95 just touched the token pipeline; this re-audit must not regress the V1.95 fix (compound doc `tailwind-theme-key-routing-for-sizing-tokens.md` is the guardrail). |
| Iteration direction | **Rework + diagnostic chain.** Not a feature iteration — closes the gaps V1.95 left and unblocks the author's smoke test. |
| Layout IA target | **Centered, integrated card with steps + content as one visual unit.** The wizard centers in the viewport (not left-aligned fill); the step indicator and the content area live inside a single card chrome (border + shadow + bg), not as a bare sidebar + separate card. Circle and label in `StepIndicator` must align horizontally per row. |
| Step1 Browse UX | **Browse button inline with location display + errors via unified toast + Continue as a prominent bottom button.** Workspace location row becomes one inline block (folder icon + location text + Browse button). Errors from `pickDirectory` / `setWorkspacePath` route through the global toast helper (no inline `<p role="alert">`). Continue becomes a wide bottom button. |
| Error notification pattern | **Global unified toast/hint across all wizard steps.** Extract the daemon step's `errorMessage()` helper to a shared module (e.g. `apps/web/src/lib/error-message.ts`) that handles Tauri invoke errors (`'message' in err`), `Error` instances, and unknown values. All wizard steps route errors through `useToast()` (already wired in `setup-wizard-page.tsx`). Fixes the `[object Object]` root cause. |
| Daemon diagnostic chain | **Three-layer fix.** (1) Rust: capture sidecar stderr from the `_rx` receiver (close `R-V195-ARCH-STRERR-GAP`); append verbatim to `DaemonStatus.detail` on Error transition. (2) React `setup-step-daemon.tsx`: add `'starting'` branch in the `onDaemonStatusChanged` callback (no-op or "still working" indicator), add a hard timeout (e.g. 25s) that forces a `getDaemonStatus()` probe + surfaces "Taking longer than expected — Retry / Reset" if no terminal event arrived (close `R-V195QC3-S001`). (3) Verify the first-launch daemon boot path with a wiped `~/.nexus42/` (RCA during implement; the `WorkspaceState::initialize()` `active_creator_id` error path is the prime suspect). |
| Stderr capture approach | **Bounded async task reading `_rx` event receiver; appended to `detail` on Error.** Avoid unbounded buffering (cap at e.g. 2 KiB tail); avoid blocking the spawn path. Keep the existing generic SidecarManager messages as a fallback when stderr is empty. |
| Residual sweep scope | **Daemon-diagnostic residuals only.** Close in-flight: `R-V195-ARCH-STRERR-GAP` (stderr capture), `R-V195QC3-S001` (wizard timeout). **Defer** the rest to V1.97+: `R-V195-ARCH-DUPLICATE-DEFAULTS` (default-path consolidation — touches 3 crates, structural), `R-V195QC3-W002`/`W003` (config-write atomicity — codebase-wide hardening), `R-V195QC1-S002` (test parity — polish). Plus the deferred V1.94-QC cluster (9 lows) and earlier — not in scope. |
| Branch policy | `iteration_base_branch=main` (HEAD `fa4815fb`, post-V1.95 PR #124); `spec_integration_branch=iteration/v1.96`; `target_branch=main`. |
| Contract impact | **NONE.** `wire_contracts_changed: false`. All fixes are in `apps/web/`, `apps/desktop/src-tauri/`, and `apps/web/DESIGN.md`. No new endpoints, no schema changes. |

## 2. Scope

**Single-plan iteration.** The prepare deliverables (DESIGN.md "Setup
Wizard Surface" audit + spec amendments + daemon-startup RCA strategy)
are produced **in the Phase 1 Review & Edit chain** (§1.6:
product-manager → architect → writing-specialist → PM lock) — they are
NOT a separate plan. `mstar-iteration` §1.5.5 artifact boundaries put
`{SPECS_DIR}/` + `{ITERATION_DIR}/<id>/guides/` authoring squarely on the
architect/product-manager roles in the start chain; `apps/web/DESIGN.md`
is the architect's design SSOT per `apps/web/AGENTS.md`. The single
implement plan's Prepare gate (`mstar-phase-gates`) verifies these
inputs exist before implement dispatch.

- **SP-1 (Phase 1 Review & Edit chain deliverables, NOT a plan)**:
  - **D1 — DESIGN.md "Setup Wizard Surface" audit**: architect adds a
    dedicated section to `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md`
    at Level 3 Production (centered integrated card IA, step-indicator
    alignment invariants, inline input row pattern, primary bottom CTA
    pattern, toast tokens, light+dark parity). Preserve V1.95
    `setup-wizard-step-*` token names where possible; the V1.95 compound
    doc `tailwind-theme-key-routing-for-sizing-tokens.md` is the
    guardrail.
  - **D2 — spec amendments**: architect amends `knowledge/specs/desktop-shell.md`
    §13 (Setup Wizard section — centered integrated card + inline Browse
    row + global toast error pattern + daemon diagnostic chain: stderr
    surfacing, bounded timeout, "Taking longer than expected" state) and
    `knowledge/specs/web-ui.md` §29 (IA — centered integrated layout +
    shared `errorMessage()` helper pattern). Product-manager reviews D2
    for product-coherence. No new specs; no `{KNOWLEDGE_DIR}/` additions
    outside `knowledge/specs/`.
  - **D3 — daemon-startup RCA strategy**: architect writes
    `.mstar/iterations/v1.96/guides/daemon-startup-rca.md` (iteration
    workspace — promote to knowledge at iteration-close via
    `mstar-compound` if reusable). Captures the 3 confirmed wizard-side
    root causes, the suspected daemon-side first-launch failure mode
    (clean `~/.nexus42/` → no `active_creator_id` →
    `WorkspaceState::initialize()` errors via `?`), the stderr-capture
    implementation strategy (bounded async task reading `_rx`, 2 kiB tail
    cap, append to `DaemonStatus.detail` on Error transition), and
    whether a separate daemon-side fix is needed beyond stderr capture.
- **SP-2: Implement the 3 rework clusters (P0 — the single plan).**
  Single-track SDD plan, fullstack-dev primary (sticky session — carries
  Rust + React + DESIGN.md context across all tasks). Tasks span: global
  error-message helper extraction, Rust sidecar stderr capture, React
  daemon-step subscribe/timeout/starting fix, wizard-page + StepIndicator
  layout rework, Step1 inline Browse+location rework, DESIGN.md token
  consumption across all setup-step-* components, tests. See plan for
  task breakdown.
- **SP-3: Closure.** QC tri-review (the layout rework + DESIGN.md change
  is substantial enough to warrant tri, not single-seat) + QA + compound
  + PR.

## Plans

| plan_id | Name | Status | Notes |
|---------|------|--------|-------|
| `2026-07-07-v1.96-implement-rework` | Implement 3 rework clusters (layout + Step1 UX + daemon diagnostic chain) | Done | QC tri Approve (after 1 fix-wave); QA Pass with notes; manual smoke deferred to author. Closes R-V195-ARCH-STRERR-GAP + R-V195QC3-S001. 5 low/nit V1.97 residuals registered. |

Status values: `Todo` | `InProgress` | `InReview` | `Done` | `Blocked`

## 2.1 Architecture Hierarchy

- All code changes in `apps/web/src/` (ClientProvider unaffected; setup wizard
  components, new shared error-message helper, CSS tokens) +
  `apps/desktop/src-tauri/src/sidecar.rs` (stderr capture) +
  `apps/web/DESIGN.md` (Setup Wizard Surface section).
- No `crates/` changes.
- No `schemas/` changes.
- No `apps/nexus42/` changes (CLI/daemon producer untouched).

## 2.2 Architecture review (architect, Phase 1 Review & Edit chain)

**Date**: 2026-07-07. **Author**: architect.

### React subscription race confirmed as primary wizard-side cause

The daemon auto-starts at Tauri boot (`lib.rs:529-538`) and enters
`DaemonState::Starting` **before** the SPA subscribes. The first status event
the SPA receives is `state: 'starting'`. The `onDaemonStatusChanged` callback
in `setup-step-daemon.tsx` (lines 41–48) has no `'starting'` branch — the
callback is a no-op → `setReady` stays false → the wizard stays in "Starting
daemon…" forever. This is the **primary mechanism** for the P0 hang.

A secondary race: on a clean `~/.nexus42/` first launch, the daemon crashes
within milliseconds (see below), `wait_for_first_health` returns `None`,
SidecarManager transitions to `Error` and fires `notify()`. If the SPA has not
yet subscribed, the Error event is lost. The SPA must call `getDaemonStatus()`
on mount to check the current state, not rely solely on the event listener for
initialization. This is the **secondary mechanism** — even after adding the
`'starting'` branch, the mount-time probe is needed to catch the "event already
fired" case.

**Conclusion**: the daemon does NOT hang — it crashes immediately. The wizard
just never learns about it. This is a React subscription race, not a daemon-side
hang.

### Stderr-capture implementation strategy

The `_rx` receiver at `sidecar.rs:246` is dropped, so daemon stderr is never
captured. The fix (T3) spawns a bounded async task that drains the event
receiver from `command.spawn()`, accumulates a tail capped at 2 kiB (nearest
newline-boundary), and appends the captured text verbatim to
`DaemonStatus.detail` on the Error transition in `start_with_budget`. The
stderr-reading task runs concurrently with `wait_for_first_health` — the spawn
path is not blocked. Existing generic SidecarManager messages are preserved as
fallback when stderr is empty. No schema change — the existing `detail:
Option<String>` field carries the combined message. Full strategy and code
sketch: `.mstar/iterations/v1.96/guides/daemon-startup-rca.md` §3.

### First-launch daemon boot path: no separate fix needed

With a wiped `~/.nexus42/`, `Config.toml` does not exist → `CliConfigSnapshot`
defaults to `active_creator_id: None` → `resolve_state_db_path()` (config.rs:98)
returns `Err("No active creator…")` → the `?` at workspace/mod.rs:166
propagates → `WorkspaceState::initialize()` returns `Err` → the daemon exits
immediately (crash, not hang). The SidecarManager's `wait_for_first_health`
detects the exit, transitions to `Error`, and fires `notify()` — but (1) the SPA
may miss the event (race), (2) the error message is generic (stderr discarded),
(3) there is no timeout escape. All three are wizard-side bugs.

**No daemon-side fix is needed.** The daemon correctly refuses to start without
an active creator. The wizard's job is to create the environment and retry. A
daemon that started silently without a creator would have undefined behavior
for every API route. **No T9 addition** — existing 8 tasks suffice.

### DESIGN.md "Setup Wizard Surface" audit outcome

**Audit scope**: `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md`. **Target
level**: Level 3 Production (dual-theme tokens for layout shell, step indicator
alignment invariants, inline input row pattern, primary bottom CTA pattern,
toast surface coverage).

**Token additions** under two new component groups preserving the existing
`setup-wizard-step` V1.95 tokens verbatim:

- **`setup-wizard-surface`** (21 new tokens): integrated card chrome
  (`card-bg`, `card-border`, `card-shadow`, `card-rounded`), dual-panel internal
  geometry (`step-panel-width`, `step-panel-right-divider`, panel padding tokens),
  inline input row (`input-row-*` — bg, border, padding, gap, label/path/icon
  colors), primary bottom CTA (`cta-primary-max-width`, `cta-container-gap`),
  motion (`step-transition-duration` references existing motion tokens).

- **`setup-wizard-step`** (1 new token added to existing 13): `step-row-height`
  for the per-row baseline alignment invariant (circle + label sit on the same
  horizontal baseline — the V1.96 layout bug fix).

**Preserved V1.95 tokens**: all 13 existing `setup-wizard-step` tokens
(`step-circle-size`, `step-circle-active/completed/pending-bg/text`,
`step-connector`, `step-label-typography`, `step-label-active/pending-color`,
`wizard-max-width`, `wizard-padding`) are unchanged. No renames.

All new sizing tokens are registered under the correct Tailwind theme keys
(`maxWidth`, `spacing`, `padding`) per the V1.95 compound doc
`tailwind-theme-key-routing-for-sizing-tokens.md` guardrail.

**Light + dark parity**: every token added to `apps/web/DESIGN.md` has a
matching entry in `apps/web/DESIGN.dark.md` with the same token name and a
dark-tuned value. All entries pass WCAG AA contrast.

**Toast surface**: the existing `components.toast` token group already covers
the wizard's error toast needs (`variant: "error"` → red-700 accent bar) for
error surface. No new toast tokens needed. The `useToast()` hook in
`setup-wizard-page.tsx` (line 33) is already wired — step components need to
call it, not maintain inline `<p>` error blocks.

**Level 3 Production completeness**: the new section adds all required Level 3
dimensions — dual-theme tokens, elevation (card shadow), motion (step transition),
shapes (card rounded), and component-level voice documentation. The
`<!-- COMPLETENESS_LEVEL: 3 -->` marker in `apps/web/DESIGN.md` is already at
Level 3 — this audit upgrades the Setup Wizard surface from the V1.94
Standard-level step chrome (13 color-focused tokens) to a full Production-level
component group with spacing, hierarchy, motion, and dark-theme parity.

## 5. Acceptance Criteria

- Desktop app opens without "The string did not match the expected pattern."
  (V1.95 regression check — must still pass on clean `~/.nexus42/` and on
  existing installs).
- **Step 1 Browse button no longer shows `[object Object]`.** Any Tauri
  invoke error from `pickDirectory` or `setWorkspacePath` (including the
  object-shaped errors Tauri returns) produces a readable message in the
  unified toast. The literal string `[object Object]` never appears for
  these failures.
- **Step 1 layout & error surface**: The Browse button appears inline with
  the workspace location display (icon + label + path + button grouped on
  one row or two tightly-coupled rows inside the same visual block). No
  wizard step renders an inline `<p role="alert">` for Tauri invoke
  failures; all such errors appear only via the global toast (variant
  "error"). The primary "Continue" button is a wide, prominent bottom CTA
  inside the card.
- **Step 2 wizard never hangs in "Starting daemon…" forever.** On every
  first-launch path (including clean `rm -rf ~/.nexus42/`), within a
  bounded timeout (≤30 s after step entry or subscription) the wizard
  either:
  - transitions to the "running" / success state and allows Continue, **or**
  - surfaces a real error whose text contains verbatim sidecar stderr (not
    only the generic SidecarManager wrapper), **or**
  - surfaces a "Taking longer than expected" state exposing visible
    Retry and Reset actions.
  The subscription callback explicitly handles `state === 'starting'`.
- **Step 2 surfaces the real daemon error.** When the daemon reaches
  `error` on boot (migration failure, missing config, port conflict,
  binary issues, etc.), the wizard `detail` contains the actual stderr
  captured from the sidecar process (clearly distinguishable from
  generic fallback text). Closes `R-V195-ARCH-STRERR-GAP`.
- **Setup wizard layout is centered + integrated.** The wizard card is
  centered in the viewport (horizontal + vertical). The step indicator
  list and the step content share a single card chrome container (one
  bordered/shadowed/background block). In every `StepIndicator` row the
  circle/marker and the label text align on the same horizontal baseline
  (no vertical offset).
- **DESIGN.md "Setup Wizard Surface" section exists** at Level 3 Production
  (light + dark tokens for spacing, hierarchy, motion, primary bottom CTA,
  inline row, toast surface, step indicator alignment). All setup wizard
  components consume tokens from this section for the audited surface; no
  ad-hoc Tailwind utilities replace the audited values.
- The author can complete a real wipe-and-relaunch smoke test
  (`rm -rf ~/.nexus42/` → launch desktop app → Step 1 shows inline Browse
  row + wide bottom Continue, no `[object Object]` on any error path →
  Step 2 exits "Starting daemon…" within ≤30 s with real output or
  timeout state → wizard completes → lands in `/works`) without manual
  recovery or console intervention.
- `pnpm --filter web test` green; `pnpm --filter web typecheck` clean;
  `pnpm --filter web build` green; `cargo test --all` green;
  `cargo clippy --all -- -D warnings` green;
  `cargo +nightly-2026-06-26 fmt --all --check` green.

## 6. Non-Goals

- No new product features or UI surfaces beyond the three rework clusters locked in §1 (centered-integrated layout, Step 1 inline Browse + bottom CTA + toast errors, daemon diagnostic chain).
- No changes under `crates/` or in `schemas/`.
- No V1.94-QC residual sweep and no out-of-scope V1.95 residuals (only `R-V195-ARCH-STRERR-GAP` and `R-V195QC3-S001` are closed in this iteration).
- No security, signing, notarization, or auto-update work (`R-V192SEC-001` remains deferred).
- No default workspace path consolidation (`R-V195-ARCH-DUPLICATE-DEFAULTS` — structural change spanning three crates).
- No config-write atomicity or durability hardening (`R-V195QC3-W002` / `W003` — codebase-wide, not wizard-specific).
- V1.95 shipped fixes must remain functional and are not regressed: immediate TauriClient in ClientProvider, opt-in "Reset local database" button, stale workspace path overwrite rules, and FingerprintGate `/setup` bypass.
- No multi-workspace, tray/menu bar, or desktop shell surface expansion.

## 7. Roadmap Position

- **Current (V1.96)**: `delivered` — 3 setup-wizard rework clusters complete (layout centered+integrated + Step1 inline Browse/toast/bottom-CTA + daemon diagnostic chain with stderr capture + mount-probe/timeout/starting-branch). QC tri Approve after 1 fix-wave; QA Pass. Manual wipe-and-relaunch smoke deferred to author (human gate). Closes `R-V195-ARCH-STRERR-GAP` + `R-V195QC3-S001`. 5 low/nit V1.97 residuals registered. Compound: updated `daemon-ready-gate-pattern.md` with Rules 5-8 (late-subscription race + diagnostic surfacing).
- **Next (V1.97)**: deferred-residual sweep — 5 V1.96 residuals (`R-V196QC3-W001` stderr drain race, `R-V196QC1-S002` rounded-control inline, `R-V196QC1-S004` format_error_detail structured envelope, `R-V196QC3-S001` test computed-style assertion, `R-V196QC1-S001` path-context-menu duplicate helper) + the V1.94-QC low cluster + earlier deferred items. Plus any new findings from the author's manual smoke test.
- **Long-term**: a desktop app that "just works" on first launch.

## 8. Delivery Branch Policy

| Field | Value |
|-------|-------|
| `iteration_base_branch` | `main` (HEAD `fa4815fb`, post-V1.95 PR #124) |
| `spec_integration_branch` | `iteration/v1.96` |
| `target_branch` | `main` |

## Compound Round Summary

- **Docs updated**: 1 — `knowledge/architecture-patterns/daemon-ready-gate-pattern.md` gained V1.96 Rules 5-8 (mount-time probe before subscribe; explicit state-enum branching; bounded timeout with re-probe; stderr capture for diagnostic surfacing) + 3 new anti-patterns. Phase 2 overlap detection found high overlap → updated existing doc rather than creating new (the V1.94-origin doc already covered the single-source-of-truth + two-consumer pattern; V1.96 added the late-subscription-race + diagnostic-surfacing refinements).
- **Workspace promoted**: 1 — `iterations/v1.96/guides/daemon-startup-rca.md` → marked "Promoted to: knowledge/architecture-patterns/daemon-ready-gate-pattern.md § V1.96 refinements". Iteration-scoped RCA details (code sketches, line citations) kept as snapshot in workspace.
- **New CONCEPTS.md entries**: 0 (no new project-specific domain terms).
- **`{KNOWLEDGE_DIR}/README.md`**: 1 row added for `daemon-ready-gate-pattern.md` (was missing from V1.94 compound round).
- **compound-refresh**: not triggered (no stale docs identified).
- **Skipped crystallization**: none (1 candidate, 1 crystallized via update).

## Iteration Retrospective (minimal)

**What went well**:
- Sticky SDD session across 8 tasks carried Rust + React + DESIGN.md context efficiently (one fullstack-dev session, 8 task dispatches + 7 fresh task reviewers + 1 QC fix-wave).
- Grill-me locked direction upfront (single V1.96, full DESIGN.md audit, global toast, branch policy) — no mid-iteration re-litigation.
- Phase 1 architect RCA correctly identified "daemon crashes, not hangs" — saved a wrong fix (the original assumption was a daemon-side boot fix; the real fix was wizard-side subscription + stderr capture).
- T1 task reviewer's S1 sweep caught 4 wizard-external `[object Object]` sites that T1's brief didn't list — folded into T2 cheaply.
- qc3 caught the `setup-wizard-page.tsx:54` finish() handler that fell through T1's sweep — 1 fix-wave resolved it.

**What to improve**:
- Phase 1 architect DESIGN.md token additions (22 tokens) weren't wired into `index.css` + `tailwind.config.ts` — caught at T5 Blocked. The V1.95 compound doc `tailwind-theme-key-routing-for-sizing-tokens.md` documented the theme-key routing but didn't flag that ALL THREE layers (DESIGN.md frontmatter → index.css CSS vars → tailwind.config.ts theme entries) must be updated. Future architect deliverables should include a "token pipeline completeness" checklist.
- Sticky resume returned empty once (T4 fix-wave) — needed a fresh commit dispatch. Sticky session reliability is host-dependent; have a fresh-dispatch fallback ready.
- The `setup-wizard-page.tsx:54` finish() handler fell through T1's sweep scope (T1 brief listed only setup-step-daemon + setup-step-welcome). A repo-wide `grep "instanceof Error"` sweep at T1 would have caught it. Future error-helper extractions should grep the whole `apps/web/src/` not just the brief's file list.

**Next iteration suggestions**:
- V1.97 residual sweep (5 low/nit V1.96 + deferred clusters).
- Add a "token pipeline completeness" Phase 1 checklist item for architect deliverables.
- Consider a repo-wide `errorMessage()` audit to consolidate the `path-context-menu.tsx` duplicate (`R-V196QC1-S001`).
