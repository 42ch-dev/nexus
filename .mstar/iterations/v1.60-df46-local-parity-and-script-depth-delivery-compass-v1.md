# V1.60 — DF-46 Local Capability Parity & Script Profile Depth — Delivery Compass v1

**Status**: PM-locked (2026-06-22) — pre-implement gate pending
**Author**: `@writing-specialist` (draft) → `@project-manager` (review, forensic audit, lock)
**Iteration**: V1.60 (active iteration — Prepare phase)
**Integration branch**: `iteration/v1.60` (PM creates from `main` @ `a0d2e503` after this compass commits)
**Integration merge target**: `main`
**Pre-implement gate**: `pending` (Prepare deliverables not yet on integration branch)
**Structure**: numeric headers per V1.59 template (no `§` prefix).

---

## 0. Grill decisions (PM-locked, 2026-06-22)

Locked decisions after PM review of `@writing-specialist` draft + forensic audit of `status.json` data integrity. Each row records the question, the locked answer, and the rationale/evidence.

| # | Question | Locked answer | Rationale / evidence |
| --- | --- | --- | --- |
| Q1 | V1.60 main axis | **Dual-track single-wave: Track A = DF-46 local parity (5 orchestration capabilities) + Track B = Script Depth promotion (Draft→Master).** P-1 prepare includes a mandatory `status.json` data-hygiene prerequisite. P-last sweeps all open residuals. | DF-46 has been open since V1.34 — highest product-completeness leverage available while platform is paused. Script is the dangling third of the non-novel profile trilogy; game-bible V1.55 P2 Depth 3.5 template lowers design risk. |
| Q2 | Are the 5 remaining catalog-only IDs local-shippable in one iteration? | **Yes, with explicit fallback.** All 5 (`nexus.world.state.query`, `nexus.world.delta.propose`, `nexus.world.delta.apply`, `nexus.timeline.event.append`, `nexus.fork.create`) are `orchestration`-scope per acp §4 — they need `CapabilityRegistry` handler implementations, not daemon `host_tool_registry()` bindings. **More work than V1.59's host_tool pattern**; Track A calibrated at L effort. Fallback (Q4) applies if P0 T1 spec work reveals scope > L. | acp-capability-set.md §4 roster SSOT. `nexus.reference.refresh` (V1.58 P1) is the existing orchestration-scope precedent — proven achievable in one plan. |
| Q3 | Capability ID naming | **Use acp §4 SSOT verbatim**: `nexus.world.state.query`, `nexus.world.delta.propose`, `nexus.world.delta.apply`, `nexus.timeline.event.append`, `nexus.fork.create`. | acp-capability-set.md §4 lines 117, 120, 121, 122, 123. The agent brief's shorthand ("world.create/list/show") was inaccurate — the delta/query model is the real contract. |
| Q4 | World delta spec depth — `world.delta.apply` is an acp §8 Open Item | **Ship all 5; spec-first with hard fallback.** P0 T1 authors Draft overlay `world-delta-propose-apply.md` (2-4 pages: delta package structure, policy gates, atomicity contract) BEFORE any handler implementation. **Fallback**: if T1 reveals scope > L effort, PM trims Track A to 3 capabilities (`world.state.query` + `timeline.event.append` + `fork.create`); defers `world.delta.propose` + `world.delta.apply` to V1.61. DF-46 roster goes 27→30 (not 32). | acp-capability-set.md §8 line 223: *"Decide whether `world.delta.apply` is agent-side or runtime-side by default."* The Draft overlay answers this in writing before code. |
| Q5 | Script profile: is the V1.55 P3 Draft scaffold sufficient for one-cycle Depth 3.5 promotion? | **Yes.** V1.55 P3 delivered scaffold (Scripts/Beats/Characters/Logs layout, `dialogue`/`beat`/`act` BlockType, `script_category` validation, ScaffoldTransaction atomicity). Depth 3.5 mirrors game-bible's V1.55 P2 template (script-writing preset + 五问 rubric + section completion + profile-aware KB extraction). | game-bible-profile.md §4 documents the Depth 3.5 template; script-profile.md (Draft, 277 lines) is well-specified for incremental extension. |
| Q6 | Should essay profile be in-scope? | **No — defer to V1.61.** Script trilogy completion first. Essay stays at Draft (V1.52, 179-line spec at `essay-profile.md`); V1.61 is the natural target for a focused single-track essay Depth promotion. | Essay is the lowest-complexity non-novel profile; can ship in a smaller iteration. Script is the higher-priority gap. |
| Q7 | Is the residual sweep overload? | **P-last absorbs 28 residuals (16 V1.52-era WL-A + 8 V1.60+ + 4 V1.58-leftover). P-1 bulk-resolves 28 more (data hygiene only).** Total open at compass lock: 56; after P-1: 28; after P-last: 0. P-last T1+T2 budget expanded to 14 surgical commits (8 WL-A + 4 V1.58-leftover + 2 V1.60+). | Forensic audit (2026-06-22) verified 25 of 32 V1.57+ residuals were genuinely closed by V1.58 (lifecycle never updated — data hygiene bug). PM accepts 2 PARTIALLY_CLOSED + 1 wontfix (Q9/Q10). 4 NOT_CLOSED items are real polish work. |
| Q8 | Profile B compaction scope | Standard per `.mstar/AGENTS.md`. 5 V1.60 plans archived at P-last; `plans-done.json` +5 entries (string only); per-`<plan-id>.json` files written. | Repo convention; no deviation. |
| Q9 | status.json `tech_debt_summary.total_open=22` vs `residual_findings.deferred=56` — 34-row discrepancy | **Forensic audit verdict: 25 CONFIRMED_CLOSED + 2 PARTIALLY_CLOSED (PM accepts) + 1 wontfix + 4 NOT_CLOSED (real open) + 0 AMBIGUOUS = 32 V1.57+ rows.** P-1 T0 bulk-updates 28 rows (`lifecycle: resolved` + `closed_at` + `closure_note` + `resolution.{plan_id, commit}` per forensic audit table). The remaining 4 V1.57+ NOT_CLOSED items enter V1.60 P-last scope (Q10/Q11). | Audit evidence: `git log iteration/v1.58` + completion reports + `main` HEAD code state. See §9 audit trail. |
| Q10 | R-V156P2-L004 (loader: 0-predecessor converge should be hard error) — V1.58 P2 actively reversed the spec to legitimize runtime-skip | **wontfix.** V1.58 P2 T2 reconciled `preset-conditional-routing.md` §3.3 from "validation error (orphan)" to "gate is skipped, state advances immediately (no validation error)". Re-flipping would be more disruptive than the underlying issue warrants (pre-1.0, local-only, no measured incident). Mark `resolved` with `closure_note: "Spec reconciled in V1.58 P2 to match runtime skip; original ask explicitly superseded."` | qc1 V1.58 P2 already flagged this as a residual to track; PM judges wontfix is correct given pre-1.0 local-only scope. |
| Q11 | R-V156P2-L001/L002 + R-V156P3-S001/S002 (4 NOT_CLOSED low-severity polish from V1.58 P2) | **V1.60 P-last T1 absorbs all 4.** Small-scope polish: (a) criterion bench for expression eval, (b) `tracing::debug!` spans in expression evaluator, (c) doc-comment distinguishing `source = "synthetic_fallback"` vs `"synthetic"`, (d) throttle-path `.await` yield audit in `tasks/mod.rs`. | qc1/qc3 V1.58 P2 explicitly listed these as deferred ("not in scope for this P2 review cycle"). Each is <50 LOC. |
| Q12 | R-V156P2-L003 (converge timeout) + R-V156P3-S004 (with_workspace_state hook) — PARTIALLY_CLOSED | **PM accepts spec/doc-only closure.** L003: spec paragraph at `preset-conditional-routing.md` line 256 documents the deferral explicitly; bounded-timeout enforcement needs `ConvergeConfig` schema work → V1.61+ when engine boundary changes. S004: doc-comment at `tasks/mod.rs` line 785 documents non-activation; production activation needs engine→task context injection boundary changes → V1.61+. Mark both `resolved` with `closure_note` describing the partial scope. | qc1 V1.58 P2 recommended tracking as new residuals; PM judges the existing doc-only closure is sufficient given pre-1.0 local-only scope. Re-opening would create audit noise without product value. |
| Q13 | R-V156P2 narrative inconsistency: merge `4bfb1399` says "4 deferred" but qc1 says "6 deferred" | **Documented in §9 audit trail. No retroactive commit-message amendment** (provenance preserved). The 5 NOT_CLOSED items from V1.58 P2 are absorbed per Q11; the merge-message drift is closed by the audit. | Forensic audit Discrepancy #3. |
| Q14 | DF-46 closure semantics after V1.60 | **Keep DF-46 open with status "Reduced — V1.60 local complete; 4 sync.* remain platform-gated (PD-05); 2 publish.* OUT (DF-59)."** Do NOT archive until sync.* resolves (could be never if platform stays paused). The row stays as a marker that sync.* is the only local-blocker. | Deferred-features-tracker §3.3 DF-46 row currently says "Reduced — V1.59"; update to "Reduced — V1.60 (local complete)" at P-last. |

---

## 1. Scope / non-goals / out-of-scope

### 1.1 In scope

**P-1 — Prepare (M effort, `@project-manager` with `@writing-specialist` draft support)**:

- **T0 (mandatory prerequisite)**: `status.json` data hygiene — bulk-update 28 V1.57+ residuals to `lifecycle: "resolved"` per forensic audit table (§9). 25 CONFIRMED_CLOSED + 1 wontfix (Q10) + 2 partial-accepted (Q12). Each row gets `closed_at: "2026-06-22"`, `closure_note`, `resolution.plan_id` (V1.58 plan), `resolution.commit` (audited SHA). Updates `metadata.tech_debt_summary.total_open: 56 → 28`.
- **T1**: This compass (PM-locked).
- **T2**: 5 plan stubs at `.mstar/plans/` (P0, P1, P-mid, P-last, plus this P-1).
- **T3**: Iteration index update (`iterations/README.md` V1.60 entry).
- **T4**: `status.json` preamble — `metadata.latest_active_iteration: "V1.60"`, `metadata.latest_active_compass`, `metadata.integration_branch: "iteration/v1.60"`, `metadata.pre_implement_gate: "pending"`, register 5 plans in `plans[]` with `status: "Todo"`.

**Track A — P0: DF-46 local parity (L effort, `@fullstack-dev`)**:

- **T1**: Author Draft overlay `world-delta-propose-apply.md` at `.mstar/knowledge/specs/world-delta-propose-apply.md` (2-4 pages). Defines: delta package structure (`{proposed_changes: [{entity, field, old_value, new_value, rationale}], policy_context: {world_id, creator_id, source_work_id}}`), policy gates (world ownership, workspace binding, creator isolation), atomicity contract (temp+rename, workspace lock), agent-side vs runtime-side decision (closes acp §8 line 223 Open Item). **Hard fallback gate**: if T1 reveals scope > L, PM triggers Q4 fallback (ship 3 of 5; defer world deltas to V1.61).
- **T2**: Implement `nexus.world.state.query` orchestration handler — query KB/timeline slices filtered by `world_id` + creator context; returns structured JSON snapshot. Pattern: mirror `nexus.reference.refresh` (V1.58 P1) handler structure.
- **T3**: Implement `nexus.world.delta.propose` — produce structured delta package from input changeset; validate world policy (ownership, binding).
- **T4**: Implement `nexus.world.delta.apply` — apply staged deltas to KB/world state under workspace lock; atomic write with temp+rename; emit audit log.
- **T5**: Implement `nexus.timeline.event.append` — append event to narrative timeline (immutable append; validate no canon rewrite; reject if `event_id` collision).
- **T6**: Implement `nexus.fork.create` — create explicit timeline fork (new branch point + metadata); clarify boundary with PD-01 (this is local timeline branching, NOT community/social fork which is platform-only).
- **T7**: Per-capability test vectors — ≥1 success + ≥1 failure + admission gate tests (world ownership, creator isolation) per ID. 5 IDs × 3 tests minimum = 15 vectors.
- **T8**: Amend `acp-capability-set.md` §4 roster — flip 5 rows `catalog-only → shipped` (Shipped in: `V1.60 P0`); roster 27→32 shipped / 4 sync catalog-only / 2 publish OUT. Update `catalog_registry_invariant_all_ids_present` cross-validation test (auto-derive via registry iterator — closes R-V159P0-002).
- **T9**: Amend `capability-registry.md` — 5 new orchestration entries + per-ID test vectors. Cross-link to `world-delta-propose-apply.md` Draft overlay.

**Track B — P1: Script Depth 3.5 promotion (L effort, `@fullstack-dev-2`)**:

- **T1**: Extend `script-profile.md` — add Depth 3.5 sections (mirror game-bible-profile.md §4 structure): `script-writing` preset chain (outline→draft→revise→finalize), dialogue/beat/act 五问 quality rubric (5 dimensions: dialogue coherence, beat pacing, act structure, character voice, scene economy), section completion detection (per-act), profile-aware KB extraction.
- **T2**: Author `embedded-presets/script-writing/preset.yaml` — stage chain + gates + template variables (`{{script.acts}}`, `{{script.characters}}`, `{{script.beats}}`).
- **T3**: Author `embedded-presets/script-writing/prompts/` — outline prompt, draft prompt, revise prompt, finalize prompt; 五问 judgment prompt with 5 script-specific dimensions.
- **T4**: Implement section completion detection — detect when all acts reach `accepted` status; trigger completion stop.
- **T5**: Implement profile-aware KB extraction — extend `candidate_from_llm_json_for_profile` for `work_profile: script` using `dialogue`/`beat`/`act` BlockType variants (registered in V1.55 P3 scaffold; verify `entity-scope-model.md` §5.1.1).
- **T6**: Wire `script-writing` into `preset_version_for_id` + `preset_version_mapping_matches_yaml_includes_cron_presets` sync test (closes V1.50 R-V150P1CRONBW-01 pattern for the third non-novel profile).
- **T7**: Hermetic tests — scaffold atomicity (ScaffoldTransaction) + preset execute end-to-end + 五问 quality gate fires on finalize + KB extraction produces typed candidates + section completion stops when all acts accepted.
- **T8**: Promote `script-profile.md` Draft → Master at P-last (after Track B QC Pass).

**P-mid — Meta tracking (M effort, `@project-manager`)**: Single-wave evidence gate across P0 + P1 parallel dispatch. Verify both tracks merged to `iteration/v1.60` before tri-review coordination. No product code.

**P-last — WL-A sweep & closeout (M effort, `@project-manager`)**:

- **T1**: Close 16 V1.52-era WL-A + 4 V1.58-leftover (Q11) = **20 low-severity residuals** via surgical fixes (≤50 LOC each). See §4 absorption table.
- **T2**: Close 8 V1.60+ residuals (V1.59 ship leftovers) via surgical fixes.
- **T3**: Deferred-features-tracker update: DF-46 row → "Reduced — V1.60 local complete"; archive DF-46 carry-forward history to `shipped-features-tracker.md` §2 V1.60 snapshot.
- **T4**: Promote `world-delta-propose-apply.md` Draft → Master (if Track A shipped all 5; otherwise stays Draft).
- **T5**: Promote `script-profile.md` Draft → Master.
- **T6**: Profile B compaction per `.mstar/AGENTS.md` — 5 V1.60 plans archived.
- **T7**: `status.json` finalize — `metadata.latest_ship` V1.60; `tech_debt_summary.total_open: 0`; `integration_branch_retired: true`.
- **T8**: Report-only QA.

### 1.2 Non-goals / out-of-scope

| Item | Disposition |
| --- | --- |
| sync.* capabilities (prepare_push/push/pull/status) | OUT; platform-blocked per PD-05. Remain `catalog-only` in roster. |
| Platform publish / platform integration (DF-59) | OUT; `metadata.platform_integration` remains paused. |
| Essay profile Depth promotion | OUT; deferred to V1.61 (Q6). |
| Standalone MCP server (DF-49) | OUT; backlog XL. |
| Cloud context assembly (DF-55) | OUT; V2.0+. |
| DF-13 (entitlements), DF-16 (Stripe) | OUT; V2.0+. |
| BL-01..BL-08 | OUT; backlog. |
| DF-41 (agent slot ACP connection stub) | OUT; S-sized, V1.61+. |
| Outbox follow-up (DF-12 post-V1.59 hardening) | OUT; too soon after V1.59 close. |
| R-V156P2-L004 (loader 0-pred hard error) | OUT (wontfix per Q10); spec reconciled by V1.58 P2. |
| R-V156P2-L003 + R-V156P3-S004 full implementation | OUT (Q12); spec/doc closure accepted; production activation deferred to V1.61+ engine boundary work. |

### 1.3 Carry-forward residuals absorbed

| Source bucket | Count | Absorbing plan | Disposition |
| --- | --- | --- | --- |
| V1.52-era WL-A (R-V150KBED-02, R-V151Q1-10, R-V152TA-S001..S011 ×11, R-V152TB-W006/W007/W008 ×3) | 16 | P-last T1 | Surgical fixes |
| V1.59 ship leftovers (R-V159P0-001/002 + R-V159P1-001..006) | 8 | P-last T2 | Surgical fixes |
| V1.58 P2 NOT_CLOSED (R-V156P2-L001/L002 + R-V156P3-S001/S002) | 4 | P-last T1 | Surgical fixes (Q11) |
| **V1.60 P-last total absorbed** | **28** | | All low severity |
| V1.57+ CONFIRMED_CLOSED (25 rows from forensic audit) | 25 | **P-1 T0** | Data hygiene bulk-resolve (no code change) |
| V1.57+ PARTIALLY_CLOSED (R-V156P2-L003 + R-V156P3-S004) | 2 | **P-1 T0** | Data hygiene bulk-resolve with partial closure_note (Q12) |
| V1.57+ wontfix (R-V156P2-L004) | 1 | **P-1 T0** | Data hygiene bulk-resolve with wontfix closure_note (Q10) |
| **P-1 T0 total bulk-resolved** | **28** | | Data hygiene only |
| **Compass-lock total open** | **56** | | Per `residual_findings` lifecycle honest count |
| **Post-P-1 open** | **28** | | |
| **V1.60 ship target** | **0** | | Zero carry-forward to V1.61 |

### 1.4 Wire contracts note

`wire_contracts_changed: true` (planned). Concretely:

- **DF-46 roster**: 5 capability IDs flip `catalog-only → shipped` in `acp-capability-set.md` §4 (roster 27→32 shipped / 4 sync catalog-only / 2 publish OUT). `catalog_registry_invariant_all_ids_present` cross-validation auto-derived via registry iterator (closes R-V159P0-002).
- **Capability registry**: 5 new orchestration entries + per-ID test vectors in `capability-registry.md`.
- **Script profile**: `script-profile.md` Draft → Master. New embedded preset `script-writing/preset.yaml` + prompts. `preset_version_for_id` + sync test extended.
- **New spec**: `world-delta-propose-apply.md` Draft → Master (if Track A ships all 5).
- **New orchestration handlers**: `crates/nexus-orchestration/src/capability/builtins/{world,timeline,fork}.rs`.
- **No DB migrations expected** — world/timeline/fork operations use existing `narrative_worlds`, `timeline_events`, `kb_key_blocks` tables. Verify during P0 T1 spec work.
- **Additive only** — no breaking changes to existing wire contracts.

---

## 2. Normative specs

| Spec | Class | Status change in V1.60 | Plan |
| --- | --- | --- | --- |
| `world-delta-propose-apply.md` | Feature line | NEW — Exploration → Draft (P0 T1) → Master (P-last T4) | P0 |
| `script-profile.md` | Feature line | Draft (V1.55 P3) → Master | P1 + P-last T5 |
| `acp-capability-set.md` §4 | Master | 5 rows catalog-only → shipped | P0 T8 |
| `capability-registry.md` | Master | +5 orchestration entries | P0 T9 |
| `entity-scope-model.md` §5.1.1 | Master | Verify script BlockType variants registered (may be additive) | P1 T5 |
| `deferred-features-cross-version-tracker.md` | Knowledge | DF-46 row updated; V1.60 snapshot in archive §2 | P-last T3 |

---

## 3. Plan topology

V1.60 uses **5 registered plans** across two implement tracks (Track A + Track B) with a **single-wave** topology. P0 (Track A) and P1 (Track B) run in parallel from `iteration/v1.60` HEAD; P-last runs after both implement plans are integrated and reviewed.

| Slot | Plan ID | Track | Effort | Owner | Review | Wave | Dependency |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P-1 | `2026-06-22-v1.60-compass-and-plan-stubs` | prepare | M | `@project-manager` (compass+plan stubs PM-authored; T0 bulk-resolve script-driven) | QC skipped (prepare) | — | — |
| P0 | `2026-06-22-v1.60-df46-local-parity` | Track A | L | `@fullstack-dev` | Tri-review + mid-QA + targeted fix-wave | Wave 1 (parallel w/ P1) | P-1 PM signoff |
| P1 | `2026-06-22-v1.60-script-depth-35` | Track B | L | `@fullstack-dev-2` | Tri-review + mid-QA + targeted fix-wave | Wave 1 (parallel w/ P0) | P-1 PM signoff |
| P-mid | `2026-06-22-v1.60-mid-qc-and-fix-waves` | meta | M | `@project-manager` | Meta tracking | Across single wave | — |
| P-last | `2026-06-22-v1.60-hygiene-and-closeout` | closeout | M | `@project-manager` | Report-only QA | After Wave 1 | P0 + P1 merged to `iteration/v1.60` |

**Single-wave dispatch rhythm**: Wave 1 dispatches P0 on `@fullstack-dev` and P1 on `@fullstack-dev-2` in parallel on separate worktrees from `iteration/v1.60`. Each merges to integration independently; each receives tri-review + mid-QA + targeted fix-wave. P-last runs after Wave 1 completes.

---

## 4. Branch policy

V1.60 uses the project-standard **two-tier branch model** from [`.mstar/AGENTS.md`](../AGENTS.md): an iteration integration branch plus per-plan topic branches. Topic branches are created **from `iteration/v1.60`, not from `main`**. PM creates `iteration/v1.60` from `main` @ `a0d2e503` after this compass commits.

| Tier | Branch | Purpose |
| --- | --- | --- |
| Iteration integration | `iteration/v1.60` | Single integration line and QC/QA review branch; final PR target is `main`. |
| Prepare working branch | `feature/v1.60-prepare` | P-1 compass + plan stubs + status.json T0 bulk-resolve. PM commits after review. |
| P0 topic | `feature/v1.60-df46-local-parity` | DF-46 5 orchestration capability batch; from `iteration/v1.60` after PM signoff; merges back to integration. |
| P1 topic | `feature/v1.60-script-depth-35` | Script Depth 3.5 promotion; from `iteration/v1.60` after PM signoff; merges back to integration. |
| P-last closeout | `feature/v1.60-hygiene-and-closeout` | WL-A sweep + V1.58-leftover + V1.60+ residual close + Profile B; merges to integration. |

**Dispatch / merge policy**: PM dispatches Wave 1 (P0 + P1) in lockstep. Every implement plan must be merged to `iteration/v1.60` before its formal tri-review and mid-QA. QC/QA assignments use `Working branch: iteration/v1.60` at integrated `HEAD`.

**Cross-track independence**: P0 (Track A) touches `crates/nexus-orchestration/src/capability/builtins/{world,timeline,fork}.rs` (new files) + spec amendments (`world-delta-propose-apply.md` new, `acp-capability-set.md` §4, `capability-registry.md`). P1 (Track B) touches `embedded-presets/script-writing/` + `crates/nexus-orchestration/src/quality_loop.rs` (KB extraction for script profile) + `script-profile.md`. Minimal overlap — different code surfaces. `preset_version_for_id` is the only potential collision point — PM resolves on integration branch.

---

## 5. Planned tracks / plan inventory

See §3 Plan topology for the plan table. Each plan's detailed task list lives in §1.1 above and will be expanded into a full plan document at `.mstar/plans/<plan-id>.md` during P-1.

### 5.1 Carry-forward absorption choices

Per §1.3 table: V1.60 absorbs **28 residuals into P-last** (16 V1.52-era WL-A + 8 V1.60+ + 4 V1.58-leftover) and **bulk-resolves 28 more in P-1 T0** (25 confirmed-closed data hygiene + 2 partial-accepted + 1 wontfix). Total disposition: 56 rows touched, 0 carry-forward to V1.61.

### 5.2 Cross-track independence

P0 and P1 touch disjoint code surfaces (see §4). The `preset_version_for_id` SSOT in `nexus-orchestration/src/preset/` is the only file both tracks might amend — PM sequences P0's commit first if collision occurs.

---

## 6. Carry-forward policy / WL-A boundary

V1.60 aims for **zero carry-forward**. However, if the iteration runs long or P-last time-box expires:

- **P-last budget**: T1 (20 residuals: 16 WL-A + 4 V1.58-leftover) and T2 (8 V1.60+) each get a 7-commit budget (14 commits total, ~15-25 LOC per commit).
- **Priority order**: Tracing/observability fixes > doc comments > cosmetic refactors. If budget recedes, low-signal cosmetics are deferred first.
- **Hard fallback (Track A)**: If P0 T1 reveals world delta spec depth > L effort, PM triggers Q4 fallback — ship 3 of 5 capabilities (world.state.query + timeline.event.append + fork.create); defer world.delta.propose + world.delta.apply to V1.61. DF-46 roster 27→30 (not 32).
- **Deferred to V1.61+ (if any)**: Any unfixed residual at P-last close gets `target: V1.61+` and a deferral note. Target count for V1.60 ship: **0 open residuals**.

**Non-novel profile boundary**: Essay profile is OUT of V1.60 (Q6). Stays at Draft (V1.52). V1.61 is the suggested target.

**DF-46 boundary after V1.60** (Q14): If all 5 capabilities ship, DF-46 roster becomes 32 shipped / 4 sync catalog-only / 2 publish OUT. DF-46 stays OPEN with status "Reduced — V1.60 local complete; sync.* platform-gated". The 4 sync.* capabilities are platform-blocked (PD-05).

---

## 7. Risk register

| Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- |
| World delta semantics under-specified (acp §8 Open Item) — `world.delta.propose`/`apply` lack concrete I/O contract beyond one-line roster descriptions | Medium | High | P0 T1 authors Draft overlay `world-delta-propose-apply.md` BEFORE any handler code. **Hard fallback** (Q4): if T1 reveals scope > L, ship only 3 of 5 capabilities and defer world deltas to V1.61. |
| 5 orchestration-scope capabilities are more complex than V1.59's 9 host_tool-scope — each needs `CapabilityRegistry` handler + state mutation + admission gates | Medium | Medium | `nexus.reference.refresh` (V1.58 P1 orchestration-scope) is the proven precedent. Track A effort calibrated at L. Per-capability fallback available if any single ID exceeds budget. |
| Script profile KB extraction may expose gaps in `entity-scope-model.md` §5.1.1 BlockType mapping for `dialogue`/`beat`/`act` | Low | Medium | game-bible V1.55 P2 KB extraction followed the same pattern. Script BlockType variants registered in V1.55 P3 Draft scaffold. P1 T5 verifies before extending. |
| Dual-track merge conflicts on `iteration/v1.60` — P0 touches `nexus-orchestration/src/capability/builtins/` while P1 touches `embedded-presets/script-writing/` + `quality_loop.rs` | Low | Low | Different code surfaces. `preset_version_for_id` is the only collision point — PM resolves on integration branch. |
| 28-residual P-last sweep may overflow time budget — some "low severity one-liner" Suggestions hide non-trivial fix complexity | Medium | Medium | P-last T1+T2 time-boxed (14 commits total). Any residual not fixed within budget gets `target: V1.61+` with explicit justification. PM prioritizes tracing/observability first; defers cosmetic/doc-only items. |
| `fork.create` semantics overlap with PD-01 ("World fork is platform-only") — ensure local `fork.create` is distinct from community fork | Low | Medium | PD-01 rules community/social fork is platform-only. `nexus.fork.create` (acp §4 line 123) is local timeline branching (explicit branch creation when rewrite-past is intended). P0 T1 spec overlay clarifies the boundary. |
| `status.json` P-1 T0 bulk-resolve may miss edge cases (e.g., a CONFIRMED_CLOSED row whose cited commit was later reverted) | Low | Low | Forensic audit (§9) verified each row against current `main` HEAD code state, not just commit existence. Reverted fixes would show as NOT_CLOSED. |
| Track A P0 T1 spec work may need product-manager input for delta policy decisions (ownership transfer, world binding precedence) | Low | Low | PM is reachable during P0 implement for spec review. Default policy: world ownership = `narrative_worlds.owner_creator_id`; workspace binding via `works.workspace_slug`. |

---

## 8. QC/QA strategy

- **P-1**: QC skipped (prepare). PM verifies T0 bulk-resolve correctness via `python3 -c "..."` assertion on `residual_findings` lifecycle counts.
- **Wave 1 (P0 + P1 parallel)**: Tri-review (`@qc-specialist` architecture/maintainability + `@qc-specialist-2` security/correctness + `@qc-specialist-3` performance/reliability) per plan. Mid-QA gate after both plans integrated. Targeted fix-waves as needed.
- **P-mid**: Single-wave evidence gate; verifies both tracks merged cleanly before tri-review coordination.
- **P-last**: Report-only QA (no full tri-review — closeout-only changes; residual fixes are surgical <50 LOC each).

---

## 9. Forensic audit trail (status.json data hygiene prerequisite)

**Audit**: 2026-06-22, `@general` agent delegated by `@project-manager`.
**Subject**: 32 V1.57+ residuals with `lifecycle="deferred"` claimed closed by V1.58 carry-forward index.
**V1.58 ship evidence**: `578be523` (PR #80 from `42ch-dev/iteration/v1.58`).
**V1.58 plan merges**: P0 `38062d1e` (workspace OCC); P1 `3fb2dd51` (DF-44 reference); P2 `4bfb1399` (capability quality convergence, "12/16 tasks done; 4 low-priority deferred" — narrative drift per Discrepancy #3).

### 9.1 Verdict summary

| Verdict | Count | V1.60 disposition |
| --- | --- | --- |
| CONFIRMED_CLOSED | 25 | P-1 T0 bulk-resolve (`lifecycle: resolved` + audited commit SHA) |
| PARTIALLY_CLOSED | 2 | P-1 T0 bulk-resolve with partial closure_note (Q12) |
| NOT_CLOSED | 5 | 1 wontfix (Q10) → P-1 T0; 4 → V1.60 P-last T1 (Q11) |
| AMBIGUOUS | 0 | — |
| **TOTAL** | **32** | 28 bulk-resolved in P-1 T0; 4 absorbed in P-last T1 |

### 9.2 Per-row verdicts — CONFIRMED_CLOSED (25 rows, P-1 T0 bulk-resolve)

Each row gets `resolution.plan_id: "2026-06-22-v1.58-<plan>"` and `resolution.commit: "<sha>"` per forensic audit table. Plan groupings:

| V1.58 plan | Plan ID | Rows | Closing commits |
| --- | --- | --- | --- |
| P0 workspace OCC | `2026-06-22-v1.58-workspace-occ-hardening` | R-V156P0-M001..M006 (6) | `d4a0b3ac`, `764cee49`, `bf9e7677`, `99da008f` |
| P0 capability handler | same | R-V156P1-M003..M005 (3); R-V156P1-L001..L007 (7); R-V156P1-CACHE-01; R-V156-PROCESS-01; R-V156-MIDQA-01 (15) | `3347fee8`, `a2168175`, `55c10891`, `5a53d72e`, `cb6193ed`, `af82ad39` |
| P2 capability convergence | `2026-06-22-v1.58-capability-quality-convergence` | R-V156P2-M001/M002/M003 (3); R-V156P2-CACHE-01; R-V156P3-W001/W002 (2) | `ffcb2473`, `be29f3f8`, `70c626b4`, `adcc13b9` |

**M003 nuance**: `a2168175` REMOVED the dead `force` field rather than wiring it. Closure_note: "Dead code eliminated; backward compat via serde ignore + tolerance test."

**L003 nuance**: Audit-only — symbols already followed `registry_refresh_*` naming convention; no rename needed. qc1 verdict: Approve.

### 9.3 Per-row verdicts — PARTIALLY_CLOSED (2 rows, P-1 T0 with partial note)

| ID | Verdict | Closing commit | Partial scope | PM decision (Q12) |
| --- | --- | --- | --- |--- |
| R-V156P2-L003 | PARTIALLY_CLOSED | `ffcb2473` | Spec paragraph added at `preset-conditional-routing.md:256` documenting the deferral; bounded-timeout enforcement NOT implemented | Accept as closed; production enforcement deferred to V1.61+ `ConvergeConfig` schema work |
| R-V156P3-S004 | PARTIALLY_CLOSED | `ffcb2473` | Doc-comment at `tasks/mod.rs:785` documents non-activation; production activation NOT done | Accept as closed; production activation deferred to V1.61+ engine boundary work |

### 9.4 Per-row verdicts — NOT_CLOSED (5 rows)

| ID | Verdict | V1.60 disposition | PM decision |
| --- | --- | --- | --- |
| R-V156P2-L001 | NOT_CLOSED | P-last T1 | Add criterion bench for expression eval (Q11) |
| R-V156P2-L002 | NOT_CLOSED | P-last T1 | Add `tracing::debug!` spans to expression evaluator (Q11) |
| R-V156P2-L004 | NOT_CLOSED | **wontfix** → P-1 T0 | Spec actively reversed by V1.58 P2 T2 (Q10) |
| R-V156P3-S001 | NOT_CLOSED | P-last T1 | Add doc-comment disambiguating `synthetic_fallback` vs `synthetic` (Q11) |
| R-V156P3-S002 | NOT_CLOSED | P-last T1 | Audit throttle-path `.await` yield in `tasks/mod.rs` (Q11) |

### 9.5 Discrepancies with V1.58 narrative

1. **`tech_debt_summary.total_open=22` vs `residual_findings.deferred=56`**: 34-row gap. After P-1 T0 bulk-resolve of 28 rows, the gap closes (28 open remaining, matching the post-hygiene truth).
2. **`v1_58_carry_forward_index` "33 of 35" claim**: Verified closure is 25 of V1.56-origin + 3 V1.57-new (out of audit scope but separately verified by V1.58 P-last commit `101611fa`) = 28 fully confirmed. The "33" oversells by ~5 rows.
3. **P2 merge message drift**: `4bfb1399` says "4 low-priority deferred" but qc1 says "6 deferred items (T4, T5, T7, T10, T11, T14)" and qc3 says "4 (T4, T5, T11, T14)". Reality (per audit): T7 and T10 have no associated commits — qc1's count of 6 is correct. No retroactive commit amendment (provenance preserved); the 5 NOT_CLOSED items are absorbed per Q11.
4. **P-last rollup `c2be0fc0` claim "35 resolved"**: Includes PARTIALLY_CLOSED items (R-V156P2-L003, R-V156P3-S004) without qualification. PM accepts partial closure per Q12.
5. **R-V156P2-L004 spec contradiction**: V1.58 P2 T2 reconciled spec in opposite direction to original ask. PM judges wontfix (Q10).

---

## 10. References

| Reference | Path | Use |
| --- | --- | --- |
| Deferred features tracker §3.3 | `.mstar/knowledge/deferred-features-cross-version-tracker.md` | DF-46 open row; DF-41/49/59 backlog rows |
| Status.json | `.mstar/status.json` | `metadata.latest_ship` V1.59 @ `a0d2e503`; `tech_debt_summary` (pre-hygiene: total_open=56); `residual_findings` 56 deferred rows |
| ACP capability roster §4 + §8 | `.mstar/knowledge/specs/acp-capability-set.md` | SSOT for 5 catalog-only IDs (lines 117/120/121/122/123); §8 Open Item `world.delta.apply` (line 223) |
| Capability registry spec | `.mstar/knowledge/specs/capability-registry.md` | Runtime dispatch contract; orchestration-scope handler pattern |
| Script profile spec | `.mstar/knowledge/specs/script-profile.md` | Draft (V1.55 P3) — 277 lines; promote to Master in V1.60 P-last |
| Game-bible profile spec | `.mstar/knowledge/specs/game-bible-profile.md` | Master Depth 3.5 — template for script promotion |
| Essay profile spec | `.mstar/knowledge/specs/essay-profile.md` | Draft (V1.52) — 179 lines; NOT in V1.60 scope |
| Entity scope model §5.1.1 | `.mstar/knowledge/specs/entity-scope-model.md` | BlockType taxonomy; §5 KB/world delta semantics (P0 T1 references) |
| Orchestration engine spec | `.mstar/knowledge/specs/orchestration-engine.md` | Capability handler registration pattern |
| V1.59 delivery compass | `.mstar/iterations/v1.59-capability-parity-and-outbox-consolidation-delivery-compass-v1.md` | Structural template (numeric headers) |
| V1.58 delivery compass | `.mstar/iterations/v1.58-workspace-occ-hardening-and-df44-reference-refresh-delivery-compass-v1.md` | Dual-track structural template |
| Iteration index | `.mstar/iterations/README.md` | Naming convention + index entry (P-1 T3 updates) |
| Repo AGENTS.md | `AGENTS.md` | Branch policy, merge discipline, Profile B compaction |
| Harness AGENTS.md | `.mstar/AGENTS.md` | Multi-plan iteration branches, residual detail, pre-existing claim protocol |
| Forensic audit report | (in compass §9) | 32-row V1.57+ residual verdict table; basis for P-1 T0 bulk-resolve |
