# V1.77 — Findings-Remediation UI (quality loop closure) + Post-Canvas-Program Inflection — Delivery Compass v1

**Status**: **Locked (Prepare complete — PM lock, 2026-06-30).** PM grill-me §0 locked (Q1=A findings-remediation lead + companion; Q2=B status+assignment+inline-edit; Q3=cross-profile; Q4=Track B + P-last compound/roadmap-reset bundle). Phase 2b `@architect` technical lock integrated (D1-D4 LOCKED with codebase evidence: server-enforced 6-state adjacency at `findings.rs:172`; last-writer-wins OCC — no conflict modal; update-only; D2 CORRECTS PM hypothesis — `UpdateFindingRequest`/`CreateFindingRequest` already generated + barrel-exported, no codegen change; `wire_contracts_changed: FALSE`; `findings-lifecycle.md` promoted from stub + `local-api-surface-conventions.md` amended). Phase 2b `@product-manager` UX lock integrated (web-ui.md §23 stage + §9 roadmap row + §8 closure + footnote — D4 transcribed faithfully). Phase 2c `@writing-specialist` review integrated (10 edits across 5 files — D2 correction propagated, stale codegen/PENDING language removed, terminology unified). 3 prior-PR review fixes folded in (CONCEPTS.md Cross-Reference 8→24 rows + path-prefixed links; AGENTS.md Subdirectory Index `apps/web/` + `apps/desktop/` rows). Pre-implement gate: **GO** — compass Locked, 4 plans on disk, `status.json` registered, review chain complete.
**Author**: `@project-manager` (compass + grill-me locked scope)
**Iteration**: V1.77 (next after V1.76)
**Integration branch**: `iteration/v1.77` (PM created from `origin/main @ ba71d916`)
**Integration merge target**: `main`
**Predecessor**: [v1.76-relationship-gamma-and-slate-clear-compass-v1.md](v1.76-relationship-gamma-and-slate-clear-compass-v1.md) (Shipped 2026-06-30 via PR #102 merged @ `ba71d916`; integration branch `iteration/v1.76` retired)
**Successor**: TBD (V1.78 — next product surface candidate; roadmap reset in P-last will propose)
**North star**: **Close the quality loop in the UI.** The canvas program (V1.67–V1.76, 10 iterations) is complete — the author can *steer* writing and *review* execution on the canvas. But the parallel quality loop is **read-only** in the UI since V1.64: the runtime produces findings (6-state lifecycle since V1.49, DAO, produce-consume chain, retention), the author can *see* them, but must drop to the CLI to triage, assign, or resolve them. V1.77 promotes findings from a Control-Room read view to a full **remediation authoring surface**: status transitions through the 6-state machine, `target_executor` assignment (route to brainstorm/write/review), and inline edit of title/description/severity/kind/rule_suggestion — consuming the PATCH route the backend already ships. This is the highest-leverage product-completeness move after the canvas: it closes the "observe → triage → resolve" quality loop exactly as the canvas closed the "steer → execute → review" writing loop. A parallel hygiene track closes the 3 open V1.76 residuals, and P-last runs the post-canvas-program inflection (compound + roadmap reset + Draft overlay promotion). **M** iteration (M lead + S-M companion, parallel waves). Out-of-scope: one-click orchestration re-trigger from a finding (the canvas/CLI remains the deliberate steering surface for re-runs), findings producer changes, new quality-loop rules, body editor, platform publish, mobile.
**Effort**: **M** (Track A M — frontend-heavy findings-remediation UI; Track B S-M — 3 world-kb residuals). Out-of-scope: orchestration re-trigger UX, findings producer changes, new canvas surfaces, body editor, platform publish, mobile.

---

## 0. Grill decisions (PM-locked with user, 2026-06-30)

| # | Question | Locked answer | Rationale |
|---|----------|---------------|-----------|
| Q1 | V1.77 主线方向 | **A — Findings-Remediation UI (lead) + companion hygiene/inflection.** | The canvas program just completed (V1.67–V1.76). The natural product-completeness complement to the now-complete "steer→execute→review" writing loop is closing the parallel "observe→triage→resolve" quality loop in the UI. Findings-remediation has been deferred since V1.64 §8 (durable tracking); the runtime quality loop is mature (6-state lifecycle since V1.49); the UI is read-only. Backend is already fully built (full CRUD + PATCH + schemas), so this is primarily a frontend consumption task — high leverage, bounded risk. Rejects: B (pure slate-clear — strongest post-program justification in 10 iterations but 0 new author value; the findings lead delivers author value AND closes the canvas program properly in P-last); C (relationship δ — 4th consecutive relationship iteration, diminishing returns on a surface that just reached γ). |
| Q2 | Remediation action depth | **B — Status + assignment + inline edit.** | Uses the full PATCH surface the backend already exposes: status transitions (6-state lifecycle `open`→`triaged`→`in_review`→`resolved`/`wont_fix`/`duplicate`) + `target_executor` assignment (route to brainstorm/write/review) + inline edit of title/description/severity/kind/rule_suggestion. Closes the complete triage loop. Re-running an orchestration session stays a deliberate canvas/CLI action (avoids coupling UI remediation to scheduler semantics — the canvas is the intended steering surface for re-runs). Rejects: A (status-only — leaves the assignment/routing half of the quality loop in CLI-only territory); C (one-click re-trigger — scope creep into orchestration-trigger UX, overlaps canvas steering surface). |
| Q3 | Profile scope | **Cross-profile.** | The findings DB table is work-scoped/profile-agnostic (keyed on `work_id`, no `work_profile` column), and the backend Local API is already cross-profile. Only novel-writing Works *produce* findings today (via the quality loop), but essay/game-bible/script Works can produce them in future without UI changes. The findings page already lets the author pick any Work; V1.77 removes the read-only restriction uniformly. Novel-only would be an artificial UI restriction the backend doesn't enforce. |
| Q4 | Companion track scope | **Track B (3 residuals) + P-last bundles compound/roadmap-reset/promotion.** | V1.77 is the post-canvas-program inflection point. The 3 open V1.76 residuals are in world-kb code (file-disjoint from the findings frontend) → parallel Track B. P-last bundles: (a) `mstar-compound` to crystallize canvas-program lessons (V1.67–V1.76, 10 iterations); (b) refresh the stale deferred-features tracker (stuck at "Active iteration: V1.67") + STRATEGY.md Decision Log to reflect the completed canvas program; (c) promote accumulated Draft overlays to Normative where safe; (d) Profile B compaction + PR. Makes V1.77 a clean inflection (new quality-loop lead + close the canvas program properly) rather than leaving the stale tracker and unpromoted overlays for another iteration. |

**Direction (consolidated)**: An **M iteration** with two **parallel tracks** in Wave 1. **Track A (P0, M)** promotes findings from a read-only Control-Room view to a full remediation authoring surface — `NexusClient` gains `getFinding`/`updateFinding`, the findings page gains triage/resolve/assign actions + a finding detail/inspector + TanStack Query mutations with optimistic updates, and DESIGN.md gains findings status/severity triage tokens. The backend needs **no new routes** — full CRUD + PATCH + list-with-filters + stale + prune already exist; this is frontend consumption only (D2 LOCKED: request types are already codegen'd and barrel-exported; only consumer-side imports needed). **Track B (P1, S-M)** closes the 3 open V1.76 residuals in file-disjoint world-kb territory. **P-last** runs the post-canvas-program inflection: compound + roadmap reset + Draft overlay promotion + compaction + PR.

### Phase 2b technical decisions — LOCKED (`@product-manager` verified against codebase, 2026-06-30)

> All decisions below are **LOCKED**. Codebase evidence cited as `file:line`. PM hypothesis on codegen exports was **corrected** (types already generated and exported — see D2).

#### D1 — Findings Local API surface (LOCKED)

**D1a — 6-state transition guards: SERVER-SIDE ENFORCED.** The DAO enforces the lifecycle adjacency on every status PATCH. The UI should still disable invalid transitions client-side as defense-in-depth + UX, but the server is authoritative and rejects illegal transitions with HTTP 422 `INVALID_TRANSITION`.

Evidence:
- `crates/nexus-local-db/src/findings.rs:172-189` — `is_valid_transition()` function with full transition table (`open → triaged|in_review|resolved|wont_fix|duplicate`; `triaged → in_review|resolved|wont_fix|duplicate`; `in_review → resolved|wont_fix|duplicate`; terminal = `resolved`/`wont_fix`/`duplicate`).
- `crates/nexus-local-db/src/findings.rs:863-895` — `enforce_status_transition()` reads the row's current status, validates against `is_valid_transition()`, returns `LocalDbError::IllegalTransition` on rejection.
- `crates/nexus-local-db/src/findings.rs:974-976` — `update_finding()` calls `enforce_status_transition()` before the UPDATE when `patch.status` is `Some`.
- `crates/nexus-daemon-runtime/src/api/handlers/findings.rs:402-421` — handler maps `IllegalTransition` → HTTP 422 with stable code `invalid_transition`.
- Enum membership is also server-validated: `findings.rs:949-957` validates `status` against `VALID_STATUSES` (6 states), and `findings.rs:116-123` defines the canonical set. Invalid enum values → HTTP 422 `invalid_input` (handler:394-441).

**D1b — OCC/revision: NO, LAST-WRITER-WINS.** Findings have no revision column, no version field, no OCC. The PATCH is a simple `WHERE creator_id = ? AND finding_id = ?` UPDATE with no compare-and-swap. The transition-guard read-before-write is best-effort under SQLite's serialized writer, but there is no conflict detection or conflict modal. No conflict modal is needed for V1.77 — the quality loop is single-author-triage (the author triages their own findings).

Evidence:
- `schemas/local-api/findings/finding-detail-response.schema.json:7-25` — response shape has no `revision`/`version` field.
- `crates/nexus-local-db/migrations/202606090002_findings.sql:7-20` — `CREATE TABLE findings` has no `revision` column.
- `crates/nexus-local-db/src/findings.rs:21-51` — `Finding` struct has no `revision` field.
- `crates/nexus-local-db/src/findings.rs:1037-1039` — UPDATE WHERE clause is `creator_id = ? AND finding_id = ?` (no version predicate).
- `crates/nexus-local-db/src/findings.rs:851-856` — doc comment notes the read-before-write is "best-effort" and a CAS upgrade path exists but was not implemented for V1.49.

**D1c — V1.77 is UPDATE-ONLY for the lead surface.** The backend ships full CRUD (create/read/update/delete/from-review/list/stale/prune), but the produce side is owned by the quality loop (`POST .../findings/from-review` + `create_finding_handler`). The V1.77 lead surface needs `getFinding` + `updateFinding` to close the triage loop. `createFinding`/`deleteFinding` remain CLI/producer-only for now; the web app can add them in a follow-up if the UX demands in-UI finding creation.

Evidence:
- `crates/nexus-daemon-runtime/src/api/handlers/findings.rs:224-251` — `create_finding_handler` exists.
- `crates/nexus-daemon-runtime/src/api/handlers/findings.rs:453-469` — `delete_finding_handler` exists.
- `apps/web/src/lib/nexus/types.ts:159-160` — web app currently has only `listFindings` on the `NexusClient`. Adding `getFinding` + `updateFinding` closes the triage loop without requiring create/delete.

#### D2 — Codegen wiring (LOCKED — CORRECTS PM HYPOTHESIS)

**The codegen pipeline is glob-based and already emits ALL findings request/response types.** Both `UpdateFindingRequest` and `CreateFindingRequest` are already generated and exported by `@42ch/nexus-contracts`. No codegen config change is needed for V1.77 — the types are already available. Implementers only need to add imports to the web app and wire `getFinding`/`updateFinding` onto `NexusClient` + `BrowserClient`/`TauriClient`.

Correction: the PM hypothesis that `UpdateFindingRequest` is "NOT exported by `@42ch/nexus-contracts`" is **incorrect**. Both request types are already generated and barrel-exported. The web app's `types.ts` just doesn't import them yet.

Evidence:
- `tooling/codegen/src/schema-loader.ts:76-77` — codegen uses `globSync(path.join(schemasDir, '**/*.schema.json'))` — any `.schema.json` file under `schemas/` is auto-discovered. No explicit config or inclusion list.
- `packages/nexus-contracts/src/generated/local-api/findings/UpdateFindingRequest.ts:1-18` — generated TS type exists on disk.
- `packages/nexus-contracts/src/generated/local-api/findings/CreateFindingRequest.ts:1-18` — generated TS type exists on disk.
- `packages/nexus-contracts/src/generated/index.ts:66` — `export * from './local-api/findings/CreateFindingRequest';`
- `packages/nexus-contracts/src/generated/index.ts:71` — `export * from './local-api/findings/UpdateFindingRequest';`
- `packages/nexus-contracts/src/index.ts:9` — `export * from './generated';` re-exports everything.
- `apps/web/src/lib/nexus/types.ts:24-82` — currently imports `FindingDetailResponse`, `ListFindingsQuery`, `ListFindingsResponse` but NOT `UpdateFindingRequest` or `CreateFindingRequest`. The `NexusClient` interface has `listFindings` (line 160) but no `getFinding`/`updateFinding`.
- **Implementers touch**: `apps/web/src/lib/nexus/types.ts` (add imports for `UpdateFindingRequest`/`CreateFindingRequest`; add `getFinding`/`updateFinding` methods), `apps/web/src/lib/nexus/browser-client.ts` (implement), `apps/web/src/lib/nexus/browser-client.test.ts` (adapter-contract parity), plus the Tauri client. **No codegen config or schema changes.**

#### D3 — Wire contracts verdict (LOCKED)

**`wire_contracts_changed: FALSE`.** The findings schemas already exist on disk and are already fully codegen'd into both TypeScript and Rust. The generated types are already barrel-exported by `@42ch/nexus-contracts`. V1.77 only consumes the already-available types from the web app — no schema content change, no codegen config change, no new generated types. **No `@42ch/nexus-contracts` version bump is required.** Current version `0.12.0` stays.

Evidence:
- All 6 findings schemas under `schemas/local-api/findings/` already exist on disk (unchanged since V1.49/V1.64).
- All 6 are fully codegen'd (`pnpm run codegen` output committed in `packages/nexus-contracts/src/generated/local-api/findings/` and `crates/nexus-contracts/src/generated/local_api/findings/`).
- All are barrel-exported by `packages/nexus-contracts/src/generated/index.ts:66-71` and `crates/nexus-contracts/src/generated/mod.rs`.
- The web app already imports several findings types (`FindingDetailResponse`, `ListFindingsQuery`, `ListFindingsResponse`) — V1.77 adds `UpdateFindingRequest` to the import list, a purely additive consumer-side change.

#### D4 — Findings-remediation UX surface (product-manager LOCKED)

The findings page gains three remediation affordances consuming the already-shipped PATCH route:

1. **Status transitions** — inline status dropdown or action buttons driving the 6-state machine, with invalid transitions disabled per the server-enforced adjacency (D1a). The server rejects illegal transitions with 422 `INVALID_TRANSITION`; the UI disables them as defense-in-depth + UX.
2. **`target_executor` assignment** — dropdown/selector routing to `brainstorm`/`write`/`master`/`none`. Valid values per `crates/nexus-local-db/src/findings.rs:192`.
3. **Inline edit** — title/description/severity/kind/rule_suggestion editable in a finding detail/inspector panel. All fields on `UpdateFindingRequest` (7 fields, all optional) per `schemas/local-api/findings/update-finding-request.schema.json:8-17`.

UX layout: **detail-panel + row-action hybrid**. The findings page is a Control-Room table (not a canvas graph), so a detail/inspector panel with the three remediation affordances + row-level status/severity badges is the default, reusing existing `Table` + `StatusBadge` components.

TanStack Query mutations with optimistic updates + invalidation of the findings list and stale-count queries.

DESIGN.md tokens: findings status badges (6 states: `open`/`triaged`/`in_review`/`resolved`/`wont_fix`/`duplicate`), severity colors (reusing existing `severityVariant` where possible), triage chrome (action buttons, assignment selector, inline-edit affordance). Token names preserved verbatim (V1.69 invariant).

---

## 1. Scope / non-goals / out-of-scope

### 1.1 In scope

**Track A — Findings-Remediation UI (the lead)**

- **A1 — Type imports (codegen already done — D2)**: `UpdateFindingRequest` + `CreateFindingRequest` are **already** generated and barrel-exported by `@42ch/nexus-contracts` (`packages/nexus-contracts/src/generated/index.ts:66,71`; confirmed Phase 2b D2). No codegen pipeline change, no contracts bump. The web app extends its local `apps/web/src/lib/nexus/types.ts` re-export to surface `UpdateFindingRequest` (and `CreateFindingRequest` if needed), or imports directly from `@42ch/nexus-contracts`. This is a trivial consumer-side import, not a codegen task.
- **A2 — `NexusClient` promotion**: add `getFinding(workId, findingId)`, `updateFinding(workId, findingId, patch)` to the `NexusClient` interface + `BrowserClient` + `TauriClient` impls. `createFinding`/`deleteFinding` remain CLI/producer-only (D1c LOCKED: V1.77 is update-only — the produce side stays with the quality loop). TanStack Query mutations (`useUpdateFinding`) with optimistic updates + invalidation of the findings list + stale-count queries.
- **A3 — Findings page remediation surface**: the existing read-only findings page (`findings-page.tsx`) gains the three remediation affordances (status transitions + target_executor assignment + inline edit). Finding detail/inspector panel showing full context (`chapter`, `source_schedule_id`, `rule_suggestion`, `routing_hint`, `kind`, timestamps) + the remediation actions. Invalid status transitions disabled per the 6-state adjacency. Stale-findings count refreshes on mutation.
- **A4 — DESIGN.md tokens**: findings status badges (6 states: `open`/`triaged`/`in_review`/`resolved`/`wont_fix`/`duplicate`), severity colors (reuse existing `severityVariant` where possible), triage chrome (action buttons, assignment selector, inline-edit affordance). Token names preserved verbatim (V1.69 invariant continues).
- **A5 — Tests**: `updateFinding` client test (PATCH payload shape); status-transition adjacency enforcement (invalid transitions rejected client-side); optimistic update + invalidation; finding detail render; target_executor assignment; inline edit save. Adapter-contract parity test (BrowserClient + TauriClient).
- **A6 — Spec promotions**: `web-ui.md` V1.77 stage (findings-remediation UI); `findings-lifecycle.md` (currently a stub at specs root — promote to document the UI remediation surface + the 6-state lifecycle that the UI enforces); `local-api-surface-conventions.md` (document the findings PATCH surface the UI consumes — already shipped, just normatively referenced).

**Track B — Slate-Clear (parallel companion, file-disjoint world-kb)**

Close the 3 open V1.76-QC-followup residuals in world-kb code (NOT the findings frontend, which is Track A).

- **B1 — `R-V176QC1-S001` (LlmExtractOutcome.relationships field discoverability)**: improve the `relationships` field discoverability in `llm_extract.rs`. Owner: `@fullstack-dev`.
- **B2 — `R-V176QC1-S002` (relationship graph pagination TODO)**: resolve the pagination TODO on `world_kb.rs` GET graph. Owner: `@fullstack-dev`.
- **B3 — `R-V176QC3-S001` (bulk-promote throttling)**: add throttling/debounce to the bulk-promote path in `suggested-relationships-pane.tsx`. Owner: `@frontend-dev`.

**P-1 — Prepare (`@project-manager` + review chain)**
- This compass; P0 + P1 plan stubs on disk; `status.json` registration; integration branch `iteration/v1.77`; Phase 2b `@product-manager` (findings-remediation UX + DESIGN.md tokens + web-ui stage) + `@architect` (findings Local API verification + type-import verification + OCC verdict + wire-contracts lock + spec amendments) + Phase 2c `@writing-specialist`; PM lock.

### 1.2 Non-goals / out-of-scope

- **No one-click orchestration re-trigger from a finding** — re-running a brainstorm/write session stays a deliberate canvas/CLI action. The canvas is the intended steering surface for re-runs; a finding "Re-run" button would couple UI remediation to scheduler semantics and overlap the canvas steering surface (grill Q2 — option C rejected). `target_executor` is an *assignment* (route hint), not an auto-trigger.
- **No findings producer changes** — V1.77 consumes findings the quality loop already produces. Changing *what* findings are produced (new rules, new review-master output, new extraction) is out of scope.
- **No new quality-loop rules / rubrics** — the 五问 rubric and review-master presets are unchanged.
- **No new canvas surfaces** — the canvas program is complete (V1.67–V1.76). V1.77 deepens the findings *Control-Room* surface, not the canvas.
- **No body editor** (rejected — AI owns prose).
- **No platform publish (DF-59)** — platform paused (local-only).
- **No mobile** — future scope.

### 1.3 Wire contracts note

**`wire_contracts_changed: FALSE` — LOCKED (Phase 2b, 2026-06-30).** The findings schemas (`update-finding-request`, `create-finding-request`, `finding-detail-response`, `list-findings-query/response`, `stale-findings-response`) already exist on disk under `schemas/local-api/findings/` AND are already fully codegen'd into both TypeScript and Rust. The generated `UpdateFindingRequest` and `CreateFindingRequest` types are already barrel-exported by `@42ch/nexus-contracts` (confirmed via codebase inspection: `packages/nexus-contracts/src/generated/index.ts:66,71`). No schema content change, no codegen config change, no new generated types. The `@42ch/nexus-contracts` version stays at `0.12.0`. V1.77 only adds consumer-side imports of already-available types — an additive change that does not constitute a wire-contracts bump.

---

## 2. Normative specs

| Spec | Class | Action this iteration |
|------|-------|----------------------|
| **`web-ui.md`** | Feature line | **Amend** (Track A, `@product-manager`): add a "V1.77 Findings-Remediation UI" stage. The Control-Room findings view (§6.1 #5, read-only since V1.64) is promoted to a full remediation authoring surface: status transitions (6-state lifecycle), `target_executor` assignment, inline edit of title/description/severity/kind/rule_suggestion, finding detail/inspector with full context. Closes the §8 deferred "Findings-remediation UI" item. The roadmap §9 table gains the V1.77 row. |
| **`findings-lifecycle.md`** (currently a stub at `specs/` root) | Master | **Promote from stub** (Track A, `@architect` draft): document the 6-state findings lifecycle (`open`→`triaged`→`in_review`→`resolved`/`wont_fix`/`duplicate`) and its adjacency rules, the `target_executor` routing semantics, and the UI remediation surface that consumes the PATCH route. Coordinates with `novel-writing/quality-loop.md` (the producer side). |
| **`local-api-surface-conventions.md`** | Master | **Amend** (Track A, `@architect` draft): normatively reference the findings PATCH surface the UI consumes (already shipped — `PATCH /v1/local/works/{work_id}/findings/{finding_id}` with the 7-field update payload). Document the OCC/last-writer-wins verdict (architect locks in Phase 2b). |
| **`canvas-strategy-surface.md`** | Draft overlay | **No change** (canvas program complete; V1.77 does not touch canvas). P-last may promote accumulated V1.70–V1.76 Draft overlays to Normative if the inflection review deems them stable. |
| **`entity-scope-model.md`** / **`llm-extract.md`** / **`world-kb-runtime-architecture.md`** | Master | **No change** (Track B residuals are code-level, not spec-level). |

---

## 3. Plan topology

**Two implement plans + Prepare + Closure. Two parallel waves (P0 ∥ P1), then closure.**

### Wave 0 — Prepare

| Plan | Track | Effort | Owner | Depends on |
|------|-------|--------|-------|------------|
| **P-1** Prepare (compass + plan stubs + status + integration branch + Phase 2b/2c + indexes) | — | S | `@project-manager` | — |

### Wave 1 — Parallel implement (P0 ∥ P1, file-disjoint, worktree-isolated)

| Plan | Track | Effort | Owner | Depends on | Files touched |
|------|-------|--------|-------|------------|---------------|
| **P0** Findings-Remediation UI: A1 type imports + A2 NexusClient promotion + A3 findings-page remediation surface + A4 DESIGN.md tokens + A5 tests + A6 specs. | A | M | `@frontend-dev` (findings-page remediation UI + NexusClient + TanStack mutations + DESIGN.md + tests + web-ui spec) + `@fullstack-dev` (type imports/wiring + findings-lifecycle/local-api spec amendments) | P-1 (compass Locked + Phase 2b architect lock) | `apps/web/src/lib/nexus/types.ts` + `browser-client.ts` + `tauri-client.ts` + `adapter-contract.test.ts` (NexusClient methods), `apps/web/src/api/queries.ts` (`useUpdateFinding` mutation), `apps/web/src/pages/findings-page.tsx` (remediation surface), `apps/web/src/components/findings/**` (NEW — detail/inspector + triage actions, if split), `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md` (findings tokens), `.mstar/knowledge/specs/web-ui.md` (V1.77 stage), `.mstar/knowledge/specs/findings-lifecycle.md` (promote from stub), `.mstar/knowledge/specs/local-api-surface-conventions.md` (findings PATCH reference) |
| **P1** Slate-Clear: B1–B3 (LlmExtractOutcome relationships field discoverability + relationship graph pagination TODO + bulk-promote throttling). | B | S-M | `@fullstack-dev` (B1/B2 world-kb backend) + `@frontend-dev` (B3 bulk-promote throttle) | P-1 (compass Locked) | `crates/nexus-orchestration/src/capability/builtins/llm_extract.rs` (B1), `crates/nexus-daemon-runtime/src/api/handlers/world_kb.rs` (B2), `apps/web/src/components/canvas/world-kb/**` (B3) |

> **File-disjointness**: P0 owns the findings frontend + codegen config + findings specs; P1 owns world-kb backend (`llm_extract.rs`/`world_kb.rs`) + the world-kb canvas bulk-promote component. The overlap risk is minimal — P0's `@fullstack-dev` share touches codegen + findings specs, P1's `@fullstack-dev` share touches world-kb handlers; PM sequences B1/B2 (backend) to land first if the same dev owns both shares. `Superpowers: dispatching-parallel-agents` + `using-git-worktrees` apply.

### Wave 2 — Closure

| Plan | Track | Effort | Owner | Depends on |
|------|------|--------|-------|------------|
| **P-last** Post-canvas-program inflection: meta tracking + residual convergence (close 3 V1.77 residuals → slate 0; new QC → V1.78) + **compound round** (crystallize V1.67–V1.76 canvas-program lessons) + **roadmap reset** (refresh stale deferred-features tracker + STRATEGY.md Decision Log) + **Draft overlay promotion** (canvas-strategy-surface / web-ui stages / entity-scope-model where stable) + spec promotion finalization + Profile B compaction + PR. | — | S | `@project-manager` | P0 + P1 merged |

---

## 4. Branch policy

| Tier | Branch |
|------|--------|
| Iteration integration | `iteration/v1.77` (from `origin/main @ ba71d916`) |
| Final landing | `main` (via PR after sign-off) |

| Plan | Working branch | Merge target | Worktree |
|------|----------------|--------------|----------|
| P-1 | (direct commit to `iteration/v1.77`) | `iteration/v1.77` | — |
| P0 | `feature/v1.77-findings-remediation` | `iteration/v1.77` | `.worktrees/v1.77-findings/` |
| P1 | `feature/v1.77-slate-clear` | `iteration/v1.77` | `.worktrees/v1.77-slate/` |
| P-last | (direct commit to `iteration/v1.77`) | `iteration/v1.77` | — |

---

## 5. Residual targets

**Close all 3 V1.76-QC-followup residuals this iteration (slate → 0).**

| Residual | Severity | Plan | Close action |
|----------|----------|------|--------------|
| `R-V176QC1-S001` (LlmExtractOutcome.relationships field discoverability) | low | P1 (B1) | Improve field discoverability |
| `R-V176QC1-S002` (relationship graph pagination TODO) | low | P1 (B2) | Resolve pagination TODO |
| `R-V176QC3-S001` (bulk-promote throttling) | nit | P1 (B3) | Add throttle/debounce |

**Total residuals after V1.77**: 3 closed → **0 open** (slate cleared). New QC findings → V1.78 pointers.

---

## 6. Risk notes

### 6.1 OCC / conflict policy — RESOLVED (Phase 2b LOCKED)
**Verdict: NO OCC, last-writer-wins.** Confirmed: `crates/nexus-local-db/migrations/202606090002_findings.sql` has no `revision` column; `crates/nexus-local-db/src/findings.rs:21-51` (`Finding` struct) has no `revision` field; the UPDATE at `findings.rs:1037-1039` uses `WHERE creator_id = ? AND finding_id = ?` with no version check. No conflict modal needed for V1.77 — the quality loop is single-author-triage.

### 6.2 Status-transition guard location — RESOLVED (Phase 2b LOCKED)
**Verdict: SERVER-SIDE enforced.** The DAO enforces the 6-state adjacency on every status PATCH (`findings.rs:172-189` `is_valid_transition()`, `findings.rs:863-895` `enforce_status_transition()`, `findings.rs:974-976` called from `update_finding()`). Handler maps `IllegalTransition` → HTTP 422 `INVALID_TRANSITION` (`handlers/findings.rs:402-421`). The UI disables invalid transitions client-side as defense-in-depth + UX; the server rejects any that bypass the UI.

### 6.3 Codegen wiring — RESOLVED (Phase 2b LOCKED, OVERTURNS PM HYPOTHESIS)
**Verdict: NO codegen config change needed.** The codegen is glob-based (`tooling/codegen/src/schema-loader.ts:76-77`) — all schemas under `schemas/` are auto-discovered. Both `UpdateFindingRequest` and `CreateFindingRequest` are already generated (`packages/nexus-contracts/src/generated/local-api/findings/`) and barrel-exported (`generated/index.ts:66,71`). The web app just needs to add imports in `types.ts` + wire `getFinding`/`updateFinding` onto `NexusClient`. The PM hypothesis that these types are "NOT exported" was incorrect — they ARE available.

### 6.4 Findings page UX layout — RESOLVED (Phase 2b LOCKED)
**Verdict: detail-panel + row-action hybrid.** The findings page is a Control-Room table (not a canvas graph), so a detail/inspector panel with the three remediation affordances (status transitions, target_executor assignment, inline edit) + row-level status/severity badges is the default, reusing existing `Table` + `StatusBadge` components. A11y: the canvas non-spatial-alternate-view discipline applies to canvas graph surfaces, not to Control-Room tables.

### 6.5 Compound / roadmap-reset scope (P-last)
The post-canvas-program compound + Draft overlay promotion is open-ended. **Mitigation**: P-last bounds the compound to canvas-program lessons only (V1.67–V1.76) using the `mstar-compound` "is it worth crystallizing" checklist; Draft overlay promotion is limited to overlays explicitly marked stable by the architect review. The deferred-features tracker refresh is mechanical (it is stale at V1.72).

---

## 7. Success criteria (DoD)

1. **Findings page is a remediation surface**: an author viewing a Work's findings can transition a finding through the 6-state lifecycle (`open`→`triaged`→`in_review`→`resolved`/`wont_fix`/`duplicate`), with invalid transitions disabled per the adjacency rules. The status change persists via `PATCH /v1/local/works/{work_id}/findings/{finding_id}` and the list refreshes.
2. **Assignment routing**: the author can set/clear `target_executor` (route to brainstorm/write/review or `none`) from the UI; the assignment persists and renders on the finding.
3. **Inline edit**: the author can edit a finding's title/description/severity/kind/rule_suggestion in a detail/inspector; changes persist via PATCH.
4. **Stale-count refresh**: the daemon-status-bar stale-findings count refreshes when a finding transitions out of `open`/`triaged`.
5. **Codegen wired**: `UpdateFindingRequest` (and `CreateFindingRequest` if needed) is exported by `@42ch/nexus-contracts` and consumed by the typed `NexusClient.updateFinding`; `verify-codegen` green.
6. Spec promotions: `web-ui.md` V1.77 stage; `findings-lifecycle.md` promoted from stub (6-state lifecycle + UI remediation surface); `local-api-surface-conventions.md` findings PATCH reference.
7. **All 3 V1.76 residuals closed → slate at 0**; `metadata.tech_debt_summary.total_open: 0` (or new QC → V1.78).
8. **Post-canvas-program inflection**: compound round executed (canvas-program lessons crystallized); deferred-features tracker refreshed (no longer stale at V1.72); STRATEGY.md Decision Log updated; stable Draft overlays promoted.
9. QC tri-review (3/3 Approve) + QA Pass on integrated `iteration/v1.77` HEAD; PR merged to `main` via `--merge`.

---

## Compound Round Summary

- 结晶文档数：1 (`knowledge/architecture-patterns/canvas-surface-implementation-pattern.md` — V1.67–V1.76 canvas program distilled)
- 新增 CONCEPTS.md 条目：0（canvas terms already in CONCEPTS.md）
- 触发 compound-refresh：否（无重叠/stale 文档）

## Iteration Retrospective (minimal)

- 做得好的：grill-me 锁定 findings-remediation 为 post-canvas 拐点（高杠杆产品完整性）；Phase 2b D2 纠正 PM 假设（types 已导出，免 codegen）；qc3 fix-wave 干净（3 Warning 全 resolved via targeted re-review）。
- 可改进的：parallel dispatch 执行失误（两次声称 N≥2 但只发 1 invoke）—— 已在本 session 内纠正并承诺后续同消息发满 N。
- 下迭代建议：V1.78 先清 12 个 QC-suggestion residuals（slate-clear），再评估下一个产品 surface 候选（desktop signing rollout 仍 blocked on cert；DF-49 MCP server 是 backlog 候选）。
