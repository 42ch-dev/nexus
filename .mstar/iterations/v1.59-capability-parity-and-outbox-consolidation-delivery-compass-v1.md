# V1.59 — DF-47 Capability Parity & DF-12 Outbox Consolidation — Delivery Compass v1

**Version**: V1.59 delivery
**Created**: 2026-06-22
**Status**: **Prepare active (P-1 in progress; pre-implement gate pending PM review)**
**Scope**: `nexus` OSS — dual-track single-wave: Track A ships 9 DF-47 catalog-only → shipped host tools (manuscript surface completion); Track B consolidates DF-12 dual-outbox architecture (schema unification + single-writer rule + deprecate legacy table + flush/compact wiring). Plus P-1 deferred-features-tracker audit/correction.
**Predecessor**: [v1.58-workspace-occ-hardening-and-df44-reference-refresh-delivery-compass-v1.md](v1.58-workspace-occ-hardening-and-df44-reference-refresh-delivery-compass-v1.md) (Shipped & MERGED 2026-06-22, PR #80 @ `578be523`)
**Successor**: TBD (V1.60+)
**North star**: Complete the manuscript capability surface (all 9 remaining catalog-only `nexus.manuscript.*` + misc tools → shipped host tools) and unify the outbox architecture (single-schema, single-writer rule, real flush/compact) while restoring deferred-doc accuracy against actual code.

---

## 0. Grill decisions (PM-locked, 2026-06-22)

| # | Decision | Locked conclusion |
|---|---|---|
| Q1 | V1.59 main axis | **Dual-track: Track A = DF-47 capability parity batch + Track B = DF-12 dual outbox consolidation**, PLUS a deferred-features-tracker audit/correction folded into P-1. North star: complete the manuscript capability surface + unify the outbox architecture + restore deferred-doc accuracy against actual code. |
| Q2 | DF-47 batch selection | **9 catalog-only → shipped host tools** (all local-only, no platform dependency): `nexus.manuscript.list`, `nexus.manuscript.read_range`, `nexus.manuscript.write`, `nexus.manuscript.phase.get`, `nexus.manuscript.phase.set`, `nexus.workspace.paths`, `nexus.research.query`, `nexus.runtime.health`, `nexus.trace.correlation`. The sync.* capabilities (prepare_push/push/pull/status) are explicitly OUT of V1.59 (deferred until after DF-12 unblocks them). |
| Q3 | DF-12 scope | **Schema unification + single-writer rule + deprecate legacy table**: migrate `nexus-cloud-sync` outbox DDL into `nexus-local-db/migrations/` (the documented "preferred long-term solution" in archived `dual-outbox-architecture.md`); document & enforce single-writer rule per event type; narrow/deprecate the daemon legacy `outbox` table in `nexus-local-db`; wire the `outbox.flush`/`outbox.compact` orchestration capability stubs to real implementations. Effort L. |
| Q4 | Plan topology | **5 plans, single-wave dual-track**: P-1 (prepare + doc audit) / Wave 1 parallel: P0 DF-47 9 capabilities (@fullstack-dev, Track A) ∥ P1 DF-12 outbox consolidation (@fullstack-dev-2, Track B) / P-mid (meta tracking) / P-last (closeout). Track A canonical owner = `@fullstack-dev`; Track B canonical owner = `@fullstack-dev-2`. |
| Q5 | Doc audit placement | **Folded into P-1** (@writing-specialist owns the audit + compass/stub authoring). |
| Q6 | Branch / version | main @ `578be523` (V1.58 merged); version **V1.59**; integration branch `iteration/v1.59` (PM creates from main AFTER P-1 reviewed); prepare branch `feature/v1.59-prepare` (current working branch). |

---

## 1. Scope / non-goals / out-of-scope

### 1.1 In scope

- **P-1**: This compass, 5 plan stubs, deferred-features-tracker audit/correction, and iteration index update.
- **P0 / Track A / Wave 1**: DF-47 capability parity batch — convert 9 catalog-only capability IDs to shipped host tools:
  1. `nexus.manuscript.list` — list manuscripts in a workspace
  2. `nexus.manuscript.read_range` — read manuscript content range
  3. `nexus.manuscript.write` — write manuscript content
  4. `nexus.manuscript.phase.get` — get manuscript phase
  5. `nexus.manuscript.phase.set` — set manuscript phase
  6. `nexus.workspace.paths` — enumerate workspace paths
  7. `nexus.research.query` — query research data
  8. `nexus.runtime.health` — daemon runtime health check
  9. `nexus.trace.correlation` — trace correlation context
  Each capability needs: `host_tool` binding in `host_tool_registry()` (daemon host tools) + handler implementation + per-ID test vectors (success + failure paths). Spec amendments: `acp-capability-set.md` §4 roster (9 rows flip catalog-only → shipped), `capability-registry.md` (9 new host tools + per-ID test vectors).
- **P1 / Track B / Wave 1**: DF-12 outbox consolidation:
  - T1: Migrate cloud-sync outbox DDL from `nexus-cloud-sync/src/outbox.rs` `init_pool_with_schema` into `nexus-local-db/migrations/` (new migration file).
  - T2: Document & enforce single-writer rule per event type across daemon and sync paths.
  - T3: Narrow/deprecate the daemon legacy `outbox` table in `nexus-local-db` (`crates/nexus-local-db/src/schema.rs`).
  - T4: Wire `outbox.flush` and `outbox.compact` orchestration capability stubs (`crates/nexus-orchestration/src/capability/builtins/outbox.rs`) to real implementations.
  - T5: Regression tests for existing sync CLI commands (`nexus42 sync *`) to verify they still function with consolidated outbox schema.
  Spec amendments: `orchestration-engine.md` (outbox consolidation section), likely new Draft overlay `outbox-consolidation.md` for single-writer contract, `daemon-runtime.md` (flush/compact invocation path).
- **P-mid**: Meta tracking across single wave; no product code.
- **P-last**: Status.json initialization + deferred-tracker archive DF-12 row + DF-46 roster count update (18→9 catalog-only) + shipped-features-tracker V1.59 snapshot + tech-debt rollup + Profile B compaction + report-only QA.

### 1.2 Non-goals / out-of-scope

| Item | Disposition |
| --- | --- |
| sync.* capabilities (prepare_push/push/pull/status) | OUT; deferred until post-DF-12 outbox unification unblocks them. |
| Platform publish / platform integration (DF-59) | OUT; `metadata.platform_integration` remains paused — local-only until further notice. |
| Stripe / billing / entitlements (DF-13, DF-16) | OUT; V2.0+ dependency. |
| MCP server (DF-49) | OUT; backlog. |
| Cloud context assembly (DF-55) | OUT; V2.0+. |
| PF-SCRIPT depth beyond V1.55 scaffold | OUT; separate future iteration. |
| WL-A 14 V1.52-era polish residuals | OUT; stay deferred to V1.60+ WL-A sweep. |
| New profile types (beyond essay/game-bible/script) | OUT; not in V1.59 scope. |
| Any new capability IDs beyond the 9 DF-47 batch | OUT; additive capability surface confined to the 9 locked IDs. |

### 1.3 Wire contracts note

`wire_contracts_changed: true` — DF-47 extends host_tool count (currently 21 per V1.58; +9 = 30); acp §4 roster 9 rows flip catalog-only → shipped; capability-registry.md gains 9 new host tool entries + per-ID test vectors. DF-12 may add 1-2 DB migrations (outbox DDL migration + legacy table deprecation). Additive only — no breaking changes to existing wire contracts.

---

## 2. Normative specs

| Spec | Starting status | V1.59 target / transition | Plans |
| --- | --- | --- | --- |
| [acp-capability-set.md](../knowledge/specs/acp-capability-set.md) | Master | **Amend §4** — 9 rows flip from `catalog-only` to `shipped`: `nexus.manuscript.list`, `nexus.manuscript.read_range`, `nexus.manuscript.write`, `nexus.manuscript.phase.get`, `nexus.manuscript.phase.set`, `nexus.workspace.paths`, `nexus.research.query`, `nexus.runtime.health`, `nexus.trace.correlation`. Cross-validate via `catalog_registry_invariant_all_ids_present`. Host tool count: 21 → 30. | P0, P-last |
| [capability-registry.md](../knowledge/specs/capability-registry.md) | Master (V1.54 P-last) | **Amend** — add 9 new host tool entries + per-ID test vectors (success + failure paths for each capability). | P0, P-last |
| [orchestration-engine.md](../knowledge/specs/orchestration-engine.md) | Master | **Amend** — add outbox consolidation section: single-writer rule per event type, flush/compact invocation contract, migration boundary between daemon legacy outbox and unified outbox schema. | P1, P-last |
| [cli-spec.md](../knowledge/specs/cli-spec.md) | Master | **Amend (if needed)** — if any of the 9 new host tools expose CLI surface directly (e.g. `nexus42 runtime health`). Unlikely for this batch (most are daemon-internal host tools), but verify during P0. | P0, P-last |
| [daemon-runtime.md](../knowledge/specs/daemon-runtime.md) | Master | **Amend** — add flush/compact invocation path for outbox capabilities; document single-writer enforcement in outbox dispatch. | P1, P-last |
| [outbox-consolidation.md](../knowledge/specs/outbox-consolidation.md) | Does not exist | **New Draft (V1.59)** — outbox single-writer contract: per-event-type exclusivity, schema ownership boundary, migration path, flush/compact semantics. Authored in P1, promoted to Master at P-last. | P1, P-last |

**wire_contracts_changed: true** for V1.59 — see §1.3 rationale.

---

## 3. Plan topology

V1.59 uses **5 registered plans** across two implement tracks (Track A + Track B) with a **single-wave** topology. P0 (Track A) and P1 (Track B) run in parallel; P-last runs after both implement plans are integrated and reviewed.

| Slot | Plan ID | Track | Effort | Owner | Review | Wave | Dependency |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P-1 | `2026-06-22-v1.59-compass-and-plan-stubs` | prepare | M | `@project-manager` (PM whitelist; authored by `@writing-specialist`) | QC skipped | — | — |
| P0 | `2026-06-22-v1.59-df47-manuscript-and-misc-capabilities` | Track A | L | `@fullstack-dev` (Track A canonical owner) | Tri-review + mid-QA + targeted fix-wave | Wave 1 (parallel w/ P1) | P-1 PM signoff |
| P1 | `2026-06-22-v1.59-df12-outbox-consolidation` | Track B | L | `@fullstack-dev-2` (Track B canonical owner) | Tri-review + mid-QA + targeted fix-wave | Wave 1 (parallel w/ P1) | P-1 PM signoff |
| P-mid | `2026-06-22-v1.59-mid-qc-and-fix-waves` | meta | M | `@project-manager` | Meta tracking | Across single wave | — |
| P-last | `2026-06-22-v1.59-hygiene-and-closeout` | closeout | M | `@project-manager` | Report-only QA | After Wave 1 | P0 + P1 merged to `iteration/v1.59` |

**Track-ownership pattern** (PM-locked during P-1): each track has one canonical owner across the single wave. Track A = `@fullstack-dev` (P0). Track B = `@fullstack-dev-2` (P1). Wave 1 dispatches P0 on `fullstack-dev` and P1 on `fullstack-dev-2` in parallel on separate worktrees.

**Single-wave dispatch rhythm**:

- **Wave 1 (parallel)**: P0 (Track A) and P1 (Track B) implement in parallel on separate worktrees from `iteration/v1.59`; each merges to integration independently; each receives its own tri-review + mid-QA + targeted fix-wave. Wave 1 ends when BOTH P0 and P1 are accepted.
- **P-last**: runs after Wave 1 completes. Report-only QA, not tri-review.

---

## 4. Branch policy

V1.59 uses the project-standard **two-tier branch model** from [`.mstar/AGENTS.md`](../AGENTS.md): an iteration integration branch plus per-plan topic branches. Topic branches are created **from `iteration/v1.59`, not from `main`**. PM creates `iteration/v1.59` from `main` after this Prepare task is reviewed.

| Tier | Branch | Purpose |
| --- | --- | --- |
| Iteration integration | `iteration/v1.59` (PM creates from `main` @ `578be523` AFTER this Prepare task is reviewed) | Single integration line and QC/QA review branch; final PR target is `main`. |
| Prepare working branch | `feature/v1.59-prepare` (current branch; P-1 artifacts authored here) | Compass + plan stubs + tracker audit. PM commits after review. |
| P0 topic | `feature/v1.59-df47-manuscript-and-misc-capabilities` | DF-47 9-capability batch; from `iteration/v1.59` after PM signoff; merges back to integration. |
| P1 topic | `feature/v1.59-df12-outbox-consolidation` | DF-12 outbox consolidation; from `iteration/v1.59` after PM signoff; merges back to integration. |
| P-last closeout | `feature/v1.59-hygiene-and-closeout` | Status.json init, tracker update, Profile B compaction; merges to integration. |

**Dispatch / merge policy**: PM dispatches Wave 1 (P0 + P1) in lockstep. Every implement plan must be merged to `iteration/v1.59` before its formal tri-review and mid-QA. QC/QA assignments use `Working branch: iteration/v1.59` at integrated `HEAD`.

---

## 5. Planned tracks / plan inventory

| Plan | Track | Deliverables |
| --- | --- | --- |
| P-1 | prepare | Compass doc; 5 plan stubs; deferred-features-tracker audit/corrections; iteration index update. |
| P0 | Track A / Wave 1 | DF-47 9-capability batch: for each capability — host_tool binding in `host_tool_registry()` + handler implementation + per-ID success/failure test vectors. Amend `acp-capability-set.md` §4 (9 rows catalog-only → shipped). Amend `capability-registry.md` (9 new host tool entries). Verify `catalog_registry_invariant_all_ids_present` passes. CLI spec amend if any capability exposes CLI surface. |
| P1 | Track B / Wave 1 | DF-12 outbox consolidation: T1 migrate cloud-sync outbox DDL to `nexus-local-db/migrations/`; T2 single-writer rule documentation + enforcement; T3 deprecate/narrow daemon legacy `outbox` table; T4 wire `outbox.flush`/`outbox.compact` stubs to real implementations; T5 regression tests for sync CLI (`nexus42 sync *`). New Draft spec `outbox-consolidation.md`. Amend `orchestration-engine.md` and `daemon-runtime.md`. |
| P-mid | meta | Checklist and evidence rhythm for single-wave merge + tri-review + mid-QA + fix-wave cycle. No product code. |
| P-last | closeout | Status.json initialization (5 plans registered; `metadata.latest_active_iteration = "V1.59"`; `metadata.integration_branch = "iteration/v1.59"`); deferred-features-tracker archive DF-12 row; DF-46 roster count update (18→9 catalog-only); shipped-features-tracker V1.59 snapshot; `outbox-consolidation.md` Draft → Master promotion; tech-debt rollup; Profile B compaction; report-only QA. |

### 5.1 Carry-forward absorption choices

V1.59 absorbs **zero** V1.52-era WL-A residuals (they stay deferred to V1.60+). The two primary closures are:

| Carry-forward | Absorbing plan | Reason |
| --- | --- | --- |
| DF-47 (9 catalog-only capability IDs) | P0 | Track A primary scope. 9 rows flip catalog-only → shipped in acp §4 roster. DF-46 roster count drops 18→9 catalog-only at P-last. |
| DF-12 (dual outbox consolidation) | P1 | Track B primary scope. Schema unification, single-writer rule, legacy table deprecation, flush/compact wiring. DF-12 row archived at P-last. |

### 5.2 Cross-track independence

- **P0 (Track A)** and **P1 (Track B)** touch **different crates** with minimal overlap:
  - P0 touches: `nexus-orchestration` (host_tool_registry + capability handlers), `nexus42` (CLI surface if needed), `nexus-local-db` (minimal — DAO for manuscript read/write if needed).
  - P1 touches: `nexus-cloud-sync` (outbox DDL extraction), `nexus-local-db` (migration, legacy table deprecation), `nexus-orchestration` (flush/compact capability stubs).
- **Shared crate boundary**: Both P0 and P1 may touch `nexus-local-db` (P0 for manuscript DAO if needed; P1 for outbox migration + legacy table deprecation). These are different tables/subsystems — no merge conflict expected. If both plans land schema changes, PM resolves conflicts on `iteration/v1.59`.
- **Single-wave dispatch** means no sequential dependency between P0 and P1 — they can implement, merge, and review in parallel.
- **P-last** depends on both P0 and P1 being merged to integration and accepted.

---

## 6. Deferred design sketches

V1.59 intentionally **opens zero new deferrals**. All absorbed DF-47 catalog-only rows are closed; DF-12 is closed. The following open items are explicitly NOT absorbed:

- **sync.* capabilities** (prepare_push/push/pull/status) — remain deferred until post-DF-12 outbox unification unblocks them. These 4 capability IDs remain `catalog-only` in acp §4 roster.
- **DF-13** (entitlements), **DF-16** (Stripe) — remain "V2.0+".
- **DF-49** (MCP server) — remains "Backlog".
- **DF-55** (cloud context assembly) — remains "V2.0+".
- **DF-59** (platform publish) — remains "Backlog".
- **BL-01 through BL-08** — all remain "Backlog".
- **WL-A 14 V1.52-era polish residuals** (`R-V150KBED-02`, `R-V151Q1-10`, `R-V152TA-S001..S011`, `R-V152TB-W006/W007/W008`) — remain deferred to V1.60+ WL-A sweep.
- **Platform / MCP / registry-publish / Stripe / preset-registry** — all OUT per §1.2.
- **PF-SCRIPT depth** — remains deferred to separate future iteration.

**DF-46 roster count update**: Currently 41-row roster (18 shipped + 18 catalog-only + 3 scaffold-equivalent + 2 OUT). After V1.59 P0 ships 9 catalog-only → shipped, count becomes 27 shipped + 9 catalog-only + 3 scaffold-equivalent + 2 OUT. This update is performed at **V1.59 P-last**, not during P0.

Any new deferral discovered during P0 or P1 must be recorded in the owning plan and root `status.json.residual_findings` before the plan can enter review.

---

## 7. Risk register

| Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- |
| DF-47 touches `host_tool_registry()` — single function, shared across all host tools. If both P0 and P1 add entries, merge conflict on `iteration/v1.59`. | Low | Low | P0 is the sole track adding host_tool entries. P1 (DF-12) touches capability stubs in `orchestration/src/capability/builtins/outbox.rs`, NOT `host_tool_registry()`. Single-owner per function. |
| DF-12 outbox DDL migration changes `nexus-local-db` schema — may interact with existing sync CLI that uses cloud-sync `Outbox` directly. | Medium | Medium | P1 T5 includes regression tests for `nexus42 sync *` commands. Migration is schema consolidation (moving DDL from inline to migrations), not schema redesign. Existing sync CLI uses the same table structure — only the initialization path changes. |
| DF-12 deprecating the daemon legacy `outbox` table may break daemon command queue operations. | Medium | High | Audit all daemon-side references to `nexus-local-db::schema::outbox` before deprecation. If the legacy table has active consumers (beyond the sync client), narrow scope to add a deprecation notice + plan for phased removal rather than immediate drop. |
| DF-47 9 capabilities are currently `catalog-only` with `Registry row ref = orchestration` — some may have incomplete or placeholder handler stubs. | Medium | Medium | Audit each of the 9 capability's handler readiness before writing host_tool bindings. If a handler is a pure stub, implement the handler as part of the same task. Per-ID test vectors (success + failure) serve as verification gates. |
| DF-12 `outbox.flush` / `outbox.compact` stubs currently return "cloud line disabled" — wiring them to real implementations requires defining flush/compact semantics. | Medium | Medium | Flush semantics: drain pending outbox entries to sync transport (or queue for later sync). Compact semantics: remove successfully-delivered entries older than retention window. Both are local-only operations (platform paused). Document the contract in `outbox-consolidation.md` Draft spec. |
| Cross-track `nexus-local-db` touches (P0 DAO + P1 migration) may produce merge conflicts on `iteration/v1.59`. | Low | Low | Different subsystems (manuscript DAO vs outbox schema). PM resolves conflicts on integration branch. |

---

## 8. QC/QA strategy

| Plan | Review strategy | Notes |
| --- | --- | --- |
| P-1 | QC skipped | PM-whitelisted Prepare-phase plan authoring; PM reviews before implement dispatch. |
| P0 | Merge to integration → tri-review → mid-QA → targeted fix-wave | Focus: host_tool binding correctness for 9 capabilities; handler implementation completeness; per-ID test vector coverage (success + failure paths); acp §4 roster accuracy; regression on existing capability dispatch tests. |
| P1 | Merge to integration → tri-review → mid-QA → targeted fix-wave | Focus: outbox DDL migration safety; single-writer rule enforcement correctness; legacy table deprecation scope; flush/compact implementation correctness; sync CLI regression tests; schema ownership boundary clarity. |
| P-mid | Meta tracking | No standalone QC; PM uses it to ensure review evidence exists before next dispatch. |
| P-last | Report-only QA | QA verifies status.json initialization, tracker updates, spec promotion, Profile B compaction, tech-debt rollup. |

**Rule**: each implement plan must finish by merging its topic branch into `iteration/v1.59`; QC and QA review the integrated branch, not an isolated topic branch. P-last is not a feature implementation plan; it is closeout plus report-only QA.

**Total**: 2 implement plans × tri-review = 6 QC reports + 2 mid-QA reports. P-last adds 1 report-only QA report.

---

*Last updated: 2026-06-22 (initial compass). Status: Prepare active — P-1 in progress; pre-implement gate pending PM review.*
