# V1.5 WS-A — Schedule FSM hardening

**Plan ID**: `2026-04-18-v1.5-ws-a-schedule-fsm`
**Status**: InReview
**Compass (SSOT)**: [.mstar/iterations/v1.5-nexus-delivery-compass-v1.md](.mstar/iterations/v1.5-nexus-delivery-compass-v1.md) §4 WS-A
**Milestone**: M1 — Residual cleanup complete
**Working branch**: `feature/v1.5-ws-a-schedule-fsm` from `feature/v1.5-stabilization`
**Dependencies**: none

**Goal**: Resolve all 7 schedule-related residuals (WS7 R1–R7) to deliver a robust Schedule state machine with correct persistence, transitions, and UX.

**Authoritative design**: [knowledge/creator-schedule-and-core-context.md](knowledge/creator-schedule-and-core-context.md) §3 (data model), §4 (lifecycle FSM), §5 (concurrency), §8 (CLI surface), §10 (SQLite schema).

**Residual coverage** (7 medium → all resolved):

| Residual | Title | Status | Scope |
|----------|-------|--------|-------|
| WS7 R1 | RunningSet/CompletedSet not persisted across restart | [x] Done | `supervisor.rs` — `pause_schedule()` updates DB + cache atomically |
| WS7 R2 | No duplicate schedule_id detection | [x] Done | `supervisor.rs` — `insert_pending()` checks (creator, preset, label) |
| WS7 R3 | No paused→running FSM transition | [x] Done | `supervisor.rs` — `resume_schedule()` admits directly if eligible |
| WS7 R4 | No pause signal in FSM | [x] Done | `supervisor.rs` — `pause_schedule()` method; handler updated |
| WS7 R5 | No delete cascade for schedules | [x] Done | `schedules.rs` handler — cancel session + NULL FK before delete |
| WS7 R6 | CoreContextManager version bump race | [x] Done | `derivation.rs` — per-schedule `Mutex<HashMap>` replacing single lock |
| WS7 R7 | paused→running requires 2-step UX | [x] Done | Backend: R3 smart resume; CLI follow-up tracked elsewhere |

**Downstream blockers**: WS-C (context.summarize) and WS-D (cron triggers) depend on WS-A completion.

**Evidence**:
- 112 passed + 1 ignored in `cargo test -p nexus-orchestration` (0 failures)
- 10 passed in integration test `schedule_admission`
- 4 passed in regression tests `schedule_regression_r11_r14`
- `cargo clippy -p nexus-orchestration -- -D warnings` — clean
- `cargo clippy -p nexus42d -- -D warnings` — clean

**Commits**:
1. `ccdebc2` feat(schedule): resolve WS7 R1-R4, R7 — FSM hardening (T1+T2)
2. `c1ad895` feat(schedule): resolve WS7 R5, R6 — delete cascade + version race (T3+T4)

**Effort (agent-oriented)**: L — completed in 1 agent session.

---

*Created: 2026-04-18. Implemented: 2026-04-18.*
