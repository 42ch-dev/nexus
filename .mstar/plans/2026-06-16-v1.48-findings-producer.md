# V1.48 Findings Producer — Plan

**Plan ID**: `2026-06-16-v1.48-findings-producer`  
**Title**: P0 — Review-report parsing + producer hardening  
**Created**: 2026-06-16  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.48-findings-producer`  
**Merge target**: `iteration/v1.48`  
**Depends on**: `2026-06-16-v1.48-harness-docs-prepare` (merged)  
**Parallel with**: `2026-06-16-v1.48-serial-hardening` (S-B Track B — no hard dependency)  
**Iteration compass**: [v1.48-novel-quality-loop-completion-delivery-compass-v1.md](../iterations/v1.48-novel-quality-loop-completion-delivery-compass-v1.md)  
**Primary specs**:

- [archived/knowledge/novel-findings-maturity.md](../archived/knowledge/novel-findings-maturity.md) §1 Producer
- [novel-writing/quality-loop.md](../knowledge/specs/novel-writing/quality-loop.md) §8

**Target residuals**: `R-V147P0-01`, `R-V147P0-06`; hotfix `R-V147P0-05` (H-1 — verify closed or fix here if not pre-merged).

---

## 1. Problem Statement

V1.47 P0 persists findings via a **placeholder synthesis** (`kind=craft`, `severity=info`, empty `rule_suggestion`). Authors cannot trust finding severity or kind for routing. `REVIEW_PRESET_ID` is duplicated across three modules.

---

## 2. Goals

1. Parse `Works/<work_ref>/Logs/review/review-report.md` when present to populate `kind`, `severity`, `body`, and optional `rule_suggestion`.
2. Define safe fallback when report missing or parse fails (degrade to current placeholder with tracing).
3. Centralize `novel-chapter-review` preset id constant (R-V147P0-06).
4. Close R-V147P0-05 if hotfix H-1 not already on `main`.
5. Hermetic tests: parsed report → finding row shape; fallback path.

---

## 3. Non-goals

- Findings → draft prompt (P1).
- `AGENTS.md` write (P2).
- Retention policy (P3).
- New wire JSON schemas.

---

## 4. Acceptance Criteria

1. Review terminal with valid `review-report.md` creates findings with parsed fields per overlay §1.
2. Missing/malformed report still creates ≥1 finding with documented fallback + `tracing::warn!`.
3. Single SSOT constant for review preset id shared by auto_chain, validation allowlist, supervisor hook.
4. R-V147P0-01 and R-V147P0-06 closed in `status.json`.
5. R-V147P0-05 closed (hotfix or P0 T0).

---

## 5. Tasks

- [x] T0: Verify/merge hotfix R-V147P0-05 (if not on `main`).
- [x] T1: Implement `review-report.md` parser + mapping table (kind/severity vocabulary per `novel-writing/quality-loop.md`).
- [x] T2: Wire parser into supervisor `from-review` synthesis path.
- [x] T3: Extract `REVIEW_PRESET_ID` (or equivalent) to shared module.
- [x] T4: Hermetic integration tests (parsed + fallback).
- [ ] T5: Residual closure notes (PM closes root residual_findings in same round; implementer evidence in Completion Report).

### 5.1 P0-fix1 — QC3 targeted re-review fixes (branch `feature/v1.48-findings-producer-fix1`)

Addressed the three qc3-blocking Warnings from the initial tri-review
(see `reports/2026-06-16-v1.48-findings-producer/qc3.md` +
`qc-consolidated.md`):

- [x] **W-1**: Bound `review-report.md` read to `MAX_REVIEW_REPORT_BYTES = 256 KiB`.
  Added `ReportLoadError::TooLarge { size_bytes, cap_bytes }`; `metadata()` is
  used for both missing-detection and size-check (drops the redundant
  `exists()` pre-check, closing qc3 S-2 incidentally). Hermetic test
  `large_report_falls_back_to_placeholder`. Commit `c6787df3`.
- [x] **W-2**: Batch parsed-finding inserts in a single `SQLite` transaction.
  Added `create_finding_from_review_tx` to `nexus-local-db` (idiomatic `_tx`
  suffix); `create_finding_from_review` delegates (behavior-preserving).
  `persist_parsed_findings` opens one tx + calls `_tx` per row + commits.
  Hermetic test `parsed_findings_transaction_commits_and_is_idempotent_on_retry`.
  Commit `374edb2a`.
- [x] **W-3**: Add `chapter = ?chapter` to every fallback `tracing::warn!` span
  in `try_persist_parsed_findings` (empty-parsed, Missing, Read, Parse,
  TooLarge). Hermetic test `fallback_warn_includes_chapter_field` using a
  self-contained `tracing_subscriber::Layer` + `Visit` capture (no new deps).
  Commit `bd4539ad`.

Deferred (per `qc-consolidated.md` PM decision):

- W-1 (qc2): path resolution safe-resolve guard → deferred as `low` residual
  under `residual_findings["2026-06-16-v1.48-findings-producer"]` for V1.49
  (P1 prompt-injection touch point). **Not** addressed this round.
- S-1, S-2, S-3 (qc3 suggestions): non-blocking. S-2 incidentally closed by
  W-1's read-path restructure; S-1 (RVM counter) and S-3 (already covered by
  S-2) remain as notes.

---

## 6. Verification

```bash
cargo test -p nexus-orchestration -- review_report 2>&1 | tail -30
cargo test -p nexus-daemon-runtime -- findings 2>&1 | tail -20
```
