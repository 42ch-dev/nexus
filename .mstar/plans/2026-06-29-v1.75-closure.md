# Plan — V1.75 P-last Closure

**plan_id**: `2026-06-29-v1.75-closure`
**Iteration**: V1.75 — [Compass](../iterations/v1.75-canvas-pivot-and-qc-followup-compass-v1.md)
**Status**: Todo
**Owner**: `@project-manager`
**Effort**: S
**Branch policy**: direct commit to `iteration/v1.75`
**Depends on**: P0 + P1 merged into `iteration/v1.75`

## Goal

V1.75 closure: residual convergence (8 → 0; new QC → V1.76), spec promotion finalization, Profile B compaction, integration PR to `main`, PM signoff. Records the canvas-pivot milestone (V1.65 editor retired).

## Tasks

- [ ] T1 — Residual convergence: mark 8 V1.75 residuals `lifecycle: resolved` in `status.json`; any new QC findings → V1.76 pointers. Verify `tech_debt_summary.total_open`.
- [ ] T2 — Spec promotion finalization: confirm `web-ui.md` V1.75 stage, `canvas-strategy-surface.md` canvas-pivot shipped, `chapter-content-local-api.md` PUT deprecation committed.
- [ ] T3 — `tech_debt_summary` refresh; `metadata.latest_ship` block (V1.75 compass, plan_count, wire 0.10.0→0.11.0, residual_summary).
- [ ] T4 — Profile B compaction: archive 4 plan rows; append to `plans-done.json` (strings only); remove from `plans[]`; verify invariant.
- [ ] T5 — `iterations/README.md` V1.75 row: Shipped + PR + merge commit + canvas-pivot summary.
- [ ] T6 — `notes.json` append: V1.75 ship narrative (canvas-pivot milestone — V1.65 editor retired after 8-iteration deferral; parity-close + flip + retire; QC slate-clear).
- [ ] T7 — Integration PR `iteration/v1.75` → `main`; wait for CI; merge with `--merge`.
- [ ] T8 — PM signoff; retire `iteration/v1.75`; set `latest_active_iteration: "V1.76 (pending)"`.

## Acceptance (maps to compass §7 DoD #6, #8)

- 8 residuals resolved; slate at 0 (or new → V1.76).
- Spec promotions committed.
- Profile B compaction verified.
- PR merged to `main`; CI green; `iteration/v1.75` retired.

## Notes

- PM-whitelist plan. Records the canvas-pivot milestone in `notes.json` (the V1.65 editor, shipped V1.65, retired V1.75 after the canvas matured across 4 β surfaces).
