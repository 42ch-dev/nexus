# Plan — V1.74 P0 Canvas World KB Relationships β

**plan_id**: `2026-06-29-v1.74-world-kb-relationships`
**Iteration**: V1.74 — [Compass](../iterations/v1.74-world-kb-relationships-and-hygiene-compass-v1.md)
**Status**: Todo
**Owner**: `@frontend-dev` (UI: world-kb-canvas split A10 + relationship UI A6 + DESIGN.md tokens) + `@fullstack-dev` (schemas/codegen A1 + migration/store A2 + daemon validation A4 + route A3 + NexusClient A5 + tests A9)
**Effort**: L
**Branch policy**: `Working branch: create feature/v1.74-world-kb-relationships from iteration/v1.74`; worktree `.worktrees/v1.74-world-kb/`; merge target `iteration/v1.74`
**Depends on**: P-1 (compass Locked + Phase 2b architect technical lock)
**Wire contracts**: `wire_contracts_changed: TRUE` — `@42ch/nexus-contracts` 0.9.0 → 0.10.0 (additive relationship DTOs + graph-response item schema refinement)

## Goal

Implement the Canvas World KB relationships β slice — the 4th canvas capability, completing the World KB surface. Promote typed relationship editing from Draft (V1.73 read-only) to Shipped β: hybrid taxonomy (core enum + custom_label) + directed/symmetric semantics + single `world_kb.patch_relationship` route (add/update/remove) with per-row OCC + `GET graph` populate + canvas relationship UI + non-spatial alternate view + world-kb-canvas.tsx split (R-V173QC1-S2 absorbed).

## Tasks (single-stream within P0; A10 split precedes A6 UI)

- [ ] A1 — Schemas + codegen: new relationship DTOs (`PatchWorldKbRelationshipRequest`/`Response`, `WorldKbRelationshipInput`, `WorldKbRelationshipProjection`, `RelationshipKind` enum schema) in `schemas/local-api/canvas/world-kb/`; refine `world-kb-graph-response.schema.json` `relationships` item from `{}` → `$ref` to projection. Regenerate TS + Rust; CI `verify-codegen` green. **Coordinate codegen+commit with P1 B3/B4** (PM-serialized on integration branch).
- [ ] A2 — Storage: migration `202606290001_kb_relationships.sql` (architect-locked columns; per-row OCC `revision`); store functions `insert/update/delete_relationship_in_tx` (CAS) + `list_relationships_for_world` + `get_relationship` in `crates/nexus-local-db/src/kb_store.rs` or new `kb_relationships.rs`.
- [ ] A3 — Route: extend `world_kb_routes()` in `crates/nexus-daemon-runtime/src/api/mod.rs` with `POST /v1/local/worlds/{world_id}/kb/patch-relationship`; path-authoritative; 409/422 reuse V1.73 shapes.
- [ ] A4 — Daemon validation + atomic persistence (extend `handlers/world_kb.rs` or new `world_kb_relationships.rs`): ID existence, authorization, self-loop guard (422), taxonomy validity (core enum OR custom+custom_label), confidence range (0.0–1.0), version precondition (OCC → 409). Atomic `*_in_tx`; symmetric read-projection in graph read.
- [ ] A5 — TS client: `worldKbPatchRelationship` method in `NexusClient`; refetch/merge canonical graph after patch; `lastKnownVersion` per-relationship.
- [ ] A10 — **(first UI task)** Close `R-V173QC1-S2`: split `world-kb-canvas.tsx` (546 lines) into focused modules per compass §3 P0 A10 — orchestrator + graph-projection + entity/candidate/relationship inspectors + conflict-modal + non-spatial + tests; each ≤ 250 lines; public facade re-export preserves imports; behavior unchanged.
- [ ] A6 — Canvas UI relationship affordances (build on A10 split modules): relationship-edge rendering (label + confidence + grounding badges) + relationship inspector (taxonomy dropdown + custom_label + symmetric toggle + anchor picker + confidence) + edge authoring from canvas drag + KB-flavored conflict modal reuse + non-spatial relationship table (sortable, full write parity). DESIGN.md relationship token values (light + dark) per architect-locked names.
- [ ] A7 — Tauri WKWebView parity smoke (relationship inspector + conflict modal + non-spatial table).
- [ ] A8 — Accessibility (ARIA live region, focus trap, keyboard create/edit/delete, non-spatial table equivalent, `prefers-reduced-motion`).
- [ ] A9 — Tests: unit (taxonomy + self-loop + symmetric projection + OCC + conflict + confidence), integration (patch-relationship via NexusClient + daemon), e2e (canvas create→save→refetch; canvas edit→conflict→reconcile), regression (concurrent daemon + canvas writes → 409).

## Acceptance (maps to compass §7 DoD #1–#6, #9)

- `world_kb.patch_relationship` route (add/update/remove) shipped with per-row OCC; `GET graph` populates `relationships[]` (symmetric reverse edges auto-projected).
- Hybrid taxonomy + directed/symmetric + anchors optional + confidence display-only shipped.
- Canvas relationship UI shipped (edge rendering + inspector + drag-authoring + conflict modal + non-spatial table with write parity).
- `@42ch/nexus-contracts` 0.10.0; codegen deterministic; `verify-codegen` green.
- `R-V173QC1-S2` closed (world-kb-canvas split, each module ≤ 250 lines).
- WKWebView parity + accessibility verified.

## Notes

- Architect Phase 2b technical lock (migration columns, route path, DTO names, enum values, DESIGN.md token names, wire countersign) is a P-1 dependency — do not start A1/A2 until compass Status = Locked.
- Single-stream within P0 (`single_stream_justified: yes`): A10 split precedes A6 UI; `@frontend-dev` + `@fullstack-dev` coordinate within one plan.
- File-disjoint from P1 except the V1.73-schema codegen coordination (B3/B4) — PM serializes that codegen+commit.
