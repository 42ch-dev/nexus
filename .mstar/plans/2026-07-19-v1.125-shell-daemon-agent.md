# P0 — Shell daemon gate + agent instant-apply

> **Iteration compass:** `.mstar/iterations/v1.125/delivery-compass.md`  
> **Spec:** `.mstar/iterations/v1.125/specs/shell-daemon-agent.md`  
> **SSOT status:** `.mstar/status.json` → `plans[]` / `residual_findings`

**Goal:** Desktop authors never use Control Room with a dead daemon behind a banner; agent selection applies immediately and footer reflects it; Codex shows version.

**Execution:** mstar-sdd (multi-task)

### Wire contracts (architect LOCKED)

- **Verdict: `wire_contracts_changed: false`** — native version fills existing optional `AgentScanEntry.version`; gate/agent UX is frontend + daemon handler lookup only; zero `schemas/` diff.

## Tasks

- [x] **T1 (AC-V1125-1):** Remove `MainBanner` from `root-layout`; fullscreen `DaemonLaunchGate` only until `running`; add **1–2s interval** `client.health()` poll during wait; unlock **only** on `running` (drop `degraded` unlock in `applyStatus`/`markReady`/timeout).
  - **Verify (author):** Starting app with daemon not ready → splash only, no “守护进程启动中…” strip; shell appears when status is `running` (not `degraded`).
  - Files: `daemon-launch-gate.tsx`, `root-layout.tsx`, `daemon-status-bar.tsx` (comment), tests; delete or orphan `main-banner.tsx` mount only
- [x] **T2 (AC-V1125-2 — agent apply):** Agent instant `setAgentProfile` on card select / verify success; **remove Save button** + dirty gate; invalidate `queryKeys.agentProfile.detail()` + `queryKeys.agentHost.scan({ filter: 'all' })`; fix `applySavedProfile` to use `launchCommandMatches` / `resolveAgentKey` (not name-only).
  - **Verify (author):** Settings → Agent: tap card → profile applies immediately; `DaemonStatusBar` shows new agent without Save tap.
  - Files: `settings-agent-section.tsx`, `daemon-status-bar.tsx`, `agent-catalog.ts` (if preselect fix needed), tests
- [x] **T3 (AC-V1125-2 — Codex version):** Native scan version in `map_native_catalog_entry` — pass `&by_binary` from scan handler; version lookup mirrors `build_scan_entry` (reuse `probe_local_binary` results, no new probe). Studio fixture align `codex-native`.
  - **Verify (author):** Codex native card shows version when CLI `--version` succeeds.
  - Files: `crates/nexus-daemon-runtime/src/api/handlers/agent_host.rs`, `settings-host-fixtures.tsx`

## Done when

- **AC-V1125-1:** Fullscreen gate only (no MainBanner); poll; unlock on `running`.
- **AC-V1125-2:** Instant agent apply; no Save; footer sync; Codex version when probed.

## Working branch

`feature/v1.125-shell-daemon-agent` → merge `iteration/v1.125`
