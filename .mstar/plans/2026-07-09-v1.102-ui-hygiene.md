# P2 — UI Hygiene + Surfaces Polish (Stretch)

**Goal:** Make Design Studio Surfaces reviewable by section and polish Control Room / setup chrome residuals. Whole plan may defer to V1.103+.

**Architecture:** Studio-first chrome fixes shared across fixtures and App; Surfaces IA split for review (**Studio-only**); optional guardrails list consolidation.

**Tech Stack:** Design Studio, `apps/web` setup/shell, lucide-react, Vitest, `tooling/check-ui-guardrails.sh`.

**Execution:** mstar-sdd

**Tier:** **Stretch (P2)** — not Must. Deferral does **not** leave Must incomplete.

## Prepare Gates

- specify: done
- clarify: done
- plan: done

## Product Framing

**User value:** Faster visual review in Studio; clearer wizard/shell/picker/daemon chrome.

**Stretch / defer rule (HARD):** If Must work (P0+P1) consumes the iteration, PM defers this **entire** plan to V1.103+ with reason in compass + `status.json`. Partial “half of P2” is not a Must obligation. Do **not** start P2 until P0+P1 automated paths are Done (unless PM documents an explicit capacity exception).

**Non-goals:** Iconify; schemas; treating this plan as Must / iteration-incomplete; shipping Surfaces section menu into the App product Settings IA; inventing full Settings taxonomy under Stretch polish; reopening P0 Badge or P1 thin Settings host locks.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| Tier | Stretch / deferrable |
| Icons | lucide only (`ChevronLeft`, `ArrowUpRight`, …) |
| Bundle | Hygiene + wizard + sidebar + AgentPicker + daemon strip + Surfaces menu |
| Surfaces menu | **Design Studio only** — routes `/surfaces`, `/surfaces/setup`, `/surfaces/shell`, `/surfaces/agent-picker`, `/surfaces/daemon` |
| Shared boundaries | StepIndicator / DaemonStatusRegion = app+fixture local; AgentPicker chrome on app-shared component; sidebar `sidebar-nav` tokens; DaemonStatusBar ↔ Studio strip — **no** nexus-ui promotion this plan |
| Human smoke | Separate human gate; not automated Done blocker for this plan |

## Acceptance Criteria (Stretch — only if plan runs)

If deferred, none of the following block Must completeness.

1. **Surfaces menu (Studio-only):** deep-linkable `/surfaces` (+ `/surfaces/setup|shell|agent-picker|daemon`); Studio nav + smoke tests updated. **Not** an App Settings IA deliverable.
2. **Wizard chrome:** Steps connector does not paint above step 1; Back uses `ChevronLeft` + `aria-label="Back"`; daemon error region = Retry top-center + concise left-aligned small copy below — Studio → App.
3. **Shell / picker / daemon:** Sidebar aesthetic within `sidebar-nav` tokens; AgentPicker Installed Badge + arrow/dot/muted states; daemon strip single-line + right Restart — Studio → App.
4. **Guardrails (capacity):** Address `R-V1100P1QC1-W002` if capacity remains; optional V1.101 doc nits.
5. No Iconify; prefer `wire_contracts_changed: false`.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.102/specs/surfaces-polish-contract.md`
- Process: `.mstar/iterations/v1.102/guides/studio-first-visual-then-app.md`
- Starts only after P0+P1 automated paths Done (or PM capacity exception).

---

### Task 1: Surfaces Section Menu

**Files:**
- Modify: `apps/design-studio/src/pages/surfaces.tsx`, `App.tsx` routes, `App.test.tsx`, nav

- [x] Add Studio routes: `/surfaces`, `/surfaces/setup`, `/surfaces/shell`, `/surfaces/agent-picker`, `/surfaces/daemon`.
- [x] Update Studio nav + smoke tests.
- [x] Confirm Studio-only — no App Settings IA from this task.

### Task 2: Wizard Chrome (Steps / Back / Error)

**Files:**
- Modify: `apps/design-studio/src/fixtures/setup-wizard-chrome-fixtures.tsx`
- Modify: `apps/web/src/pages/setup-wizard-page.tsx`, setup-step daemon/agent CTAs

- [x] Fix Steps connector (no line above step 1) + panel fill policy.
- [x] Back → `ChevronLeft` + `aria-label="Back"`.
- [x] Daemon error: Retry top-center; concise left-aligned small copy below.

### Task 3: Sidebar + AgentPicker + Daemon Strip

**Files:**
- Modify: Surfaces `AppShellFixture`, App sidebar; AgentPicker; `DaemonStatusStrip` / `DaemonStatusBar`

- [x] Sidebar aesthetic pass within `sidebar-nav` tokens.
- [x] AgentPicker: Installed Badge; small arrow icons; hollow/lit dots; muted not-installed.
- [x] Daemon strip: single-line + right Restart.

### Task 4: Guardrails Hygiene (capacity)

**Files:**
- Modify: `tooling/check-ui-guardrails.sh` (± related docs)

- [x] Address `R-V1100P1QC1-W002` if capacity remains.
- [x] Optional V1.101 doc nits. *(skipped — no quick doc nits needed; script SSOT is the deliverable)*

### Task 5: Deferral Or Done Summary

- [ ] ~~If deferred: update compass + `status.json` target V1.103+ with reason; Must completeness unchanged.~~ *(N/A — plan ran)*
- [x] If Done: QC/QA placeholders for PM gate; human smoke remains a separate gate.
  - Stretch Tasks 1–4 implemented on `feat/v1.102-ui-hygiene`; P0+P1 Must completeness unaffected.
  - Human interactive desktop smoke remains a **separate gate**, not an automated Done blocker.
  - QC: Approve with residuals (`da0e5f61`..`138d219a`); QA: **Pass with residuals** — recommend Done (PM finalize).

## Plan self-review (PM before locked)

1. Stretch/defer rule explicit; whole-plan defer allowed.
2. Surfaces menu Studio-only.
3. Studio-first for all chrome items.
4. No Iconify / no schemas.
5. No Stretch item listed as Must blocker.

## Review Gate Summary

- **Decision:** Approve with residuals
- **Review range / Diff basis:** `da0e5f61` .. `138d219a` on `feat/v1.102-ui-hygiene`
- **Review bundle:** `.mstar/sdd/2026-07-09-v1.102-ui-hygiene/review/`
- **QC inputs:** qc1 Approve with residuals · qc2 Approve · qc3 Approve → `qc-consolidated.md`
- **Blocking result:** none
- **Residual findings:** R-V1102P2QC-S001..S003 (Suggestion; SSOT `status.json`)

## QA Gate Summary

- **Verdict:** Pass with residuals (2026-07-09, qa-engineer)
- **QA gate / mode:** mandatory / acceptance-only (+ key re-runs)
- **Review range:** `da0e5f61` .. `138d219a` on `feat/v1.102-ui-hygiene` (HEAD `138d219a`)
- **Evidence (QA re-run):** Studio Vitest 60/60; web scoped chrome Vitest 62/62; `check-ui-guardrails.sh` green; no Iconify; `schemas/` empty in range
- **Residuals:** R-V1102P2QC-S001..S003 — Suggestion only (leave open)
- **Human smoke:** Separate gate — not claimed; not an automated Done blocker
- **Recommend:** plan **Done**; merge feature → `iteration/v1.102` (PM finalize)
- **Raw report:** `.mstar/sdd/2026-07-09-v1.102-ui-hygiene/review/qa.md` (ephemeral)
