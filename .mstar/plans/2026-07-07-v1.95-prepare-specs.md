# 2026-07-07-v1.95-prepare-specs (P-1)

**Iteration**: V1.95 · **Type**: prepare/spec · **Owner**: architect
**Status**: Done · **Plan ID**: `2026-07-07-v1.95-prepare-specs`

## Specify

### Problem

V1.94 specs for the setup wizard (`desktop-shell.md` §13, `web-ui.md` §29) describe the intended behavior but don't cover the bugfix patterns V1.95 introduces: ClientProvider immediate-initialization, daemon error surfacing, migration-mismatch recovery, and the layout redesign.

### Scope

**In**:
- `knowledge/specs/desktop-shell.md` §13: amend to document (a) ClientProvider must use TauriClient from first render on desktop; (b) wizard step 2 surfaces real SidecarManager error; (c) "Reset local database" recovery path for migration mismatch (opt-in, clear copy that only daemon state is cleared). Also correct all default workspace examples from `~/Documents/nexus42/default/` to `~/Documents/nexus/default/` (brand `nexus/`, system data under `~/.nexus42/`).
- `knowledge/specs/web-ui.md` §29: amend to document the left-sidebar wizard layout (steps vertical on left, content on right).
- No new specs; no schema changes; no codegen.

**Out**: Code, schemas, harness state.

### DoD

- Both spec sections updated with V1.95 amendments.
- No code changes in this plan.

## Plan

Minimal spec amendment. Architect edits + writing-specialist pass.

### Tasks

1. Architect: amend `desktop-shell.md` §13 with ClientProvider fix + error surfacing + recovery.
2. Architect: amend `web-ui.md` §29 with layout redesign.
3. Writing-specialist: terminology pass.
