---
plan_id: 2026-06-23-v1.62-manifest-schemas-and-host-validation
title: V1.62 P1 — manifest.json `schemas` Block + Host Validation + basic-combat Enrichment
iteration: V1.62
slot: P1
status: Todo
effort: L
owner: "@fullstack-dev-2"
review: Tri-review + mid-QA
wave: 2
dependency: P0 merged to integration
working_branch: feature/v1.62-manifest-schemas-and-host-validation
merge_target: iteration/v1.62
---

# P1: manifest.json `schemas` Block + Host Validation + basic-combat Enrichment

Realize V1.61 compass Q8's "module-declared fields via manifest.json" promise: extend `manifest.json` with optional `schemas` block carrying inline JSON-Schema fragments; host validates `ComputeInput.key_blocks` (per block_type), `ComputeInput.invocation`, and `ComputeOutput.battle_report` against declared schemas. Backward compat: omit `schemas` → no validation.

Full task list in compass §1.1 P1 (T1–T8). Key deliverables:

## Tasks

- [ ] T1: Extend `ModuleManifest` in `crates/nexus-wasm-host/src/manifest.rs` with `schemas: Option<ModuleSchemas>` field
- [ ] T2: Define `ModuleSchemas` struct: `key_block_attributes: Option<HashMap<String, serde_json::Value>>`, `key_block_state: Option<HashMap<String, serde_json::Value>>`, `invocation: Option<serde_json::Value>`, `battle_report: Option<serde_json::Value>`
- [ ] T3: Implement host-side validation in `crates/nexus-wasm-host/src/compute.rs`:
  - Pre-invocation: validate `ComputeInput.key_blocks[]` against `manifest.schemas.key_block_attributes[block_type]` if declared
  - Pre-invocation: validate `ComputeInput.invocation` against `manifest.schemas.invocation` if declared
  - Post-invocation: validate `ComputeOutput.battle_report` against `manifest.schemas.battle_report` if declared
  - Add `ComputeError::ManifestValidationFailed { path, detail }` variant (see P2 T2 §8 error taxonomy)
  - Resolve design item #1 (validation library — `jsonschema` crate recommended) + #2 (fail-fast with JSON path)
- [ ] T4: Backward compat: omit `schemas` block → no validation performed (V1.61 modules unchanged)
- [ ] T5: Enrich `modules/basic-combat/manifest.json` with full `schemas` block:
  - `key_block_attributes.character`: `{ max_hp: int≥0, base_atk: int≥0, base_def: int≥0, speed: int≥0, level: int≥1 }`, `required: [max_hp, base_atk, base_def]`
  - `key_block_state.character`: `{ current_hp: int≥0, status_effects: array<string>, is_alive: bool }`
  - `invocation`: `{ attacker_id: string, defender_id: string }` (both optional)
  - `battle_report`: `{ kind: string const "combat", attacker_id, defender_id, damage, defender_hp_before, defender_hp_after }`, `required: [kind, attacker_id, defender_id, damage]`
- [ ] T6: Tests:
  - Valid manifest + valid input → succeeds
  - Valid manifest + invalid input (missing required `base_atk`) → fails with `ManifestValidationFailed` carrying JSON path
  - Manifest without `schemas` block → no validation performed (backward compat)
  - basic-combat module end-to-end compute with manifest validation enabled
- [ ] T7: Update `modules/README.md` §`manifest.json` to document the new `schemas` block (4 optional JSON-Schema fragments; behavior when omitted; basic-combat authoring example)
- [ ] T8: Update `crates/nexus-wasm-host/AGENTS.md` "Key Rules" to mention manifest-driven validation

## Done when

- `ModuleManifest::schemas` field parsed correctly from JSON
- Host validation triggers only when `schemas` is `Some` and the specific sub-field is declared
- Validation errors carry a meaningful JSON path (e.g. `key_blocks[1].body.attributes.base_atk: missing required field`)
- basic-combat module's manifest carries its full character/invocation/battle_report shape (the fix the user identified in PR #83 review)
- `modules/README.md` has new authoring guide section
- `cargo test -p nexus-wasm-host` PASS (including new validation tests)
- `cargo clippy -p nexus-wasm-host -- -D warnings` PASS

## Coordination notes

- This is the **PRIMARY product work** of V1.62 (per grill Q2). It realizes the design fix the user identified: basic-combat's KB params now live in its own manifest, not in product-level schemas.
- Do NOT touch `schemas/` (P0 territory) or `.mstar/knowledge/specs/` (P2 territory).
- The `ComputeError::ManifestValidationFailed` variant must match what P2 T2 §8 documents in `wasm-host.md` — coordinate the variant name via P-mid if P2 lands first.
