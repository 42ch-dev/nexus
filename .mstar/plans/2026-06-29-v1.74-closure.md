# Plan — V1.74 P-last Closure

**plan_id**: `2026-06-29-v1.74-closure`
**Iteration**: V1.74 — [Compass](../iterations/v1.74-world-kb-relationships-and-hygiene-compass-v1.md)
**Status**: Todo
**Owner**: `@project-manager`
**Effort**: S
**Branch policy**: direct commit to `iteration/v1.74`
**Depends on**: P0 + P1 merged into `iteration/v1.74`

## Goal

V1.74 closure: residual convergence (slate → 0), feature-tracker update (FEAT-WORLD-KB-RELATIONSHIPS → shipped archive), spec promotion, Profile B compaction, integration PR to `main`, PM signoff.

## Tasks

- [ ] T1 — Residual convergence: mark all 12 V1.74 residuals `lifecycle: resolved` with resolution commit + plan_id in `status.json` root `residual_findings`. Verify `metadata.tech_debt_summary.total_open: 0`.
- [ ] T2 — Feature closure: move FEAT-WORLD-KB-RELATIONSHIPS from `deferred-features-cross-version-tracker.md` §2.3 to `archived/shipped-features-tracker.md` with V1.74 plan + brief note.
- [ ] T3 — Spec promotion: `canvas-strategy-surface.md` §3.3 surface 3 + §3.4 WorldKbEdgeData Draft → Shipped β; `entity-scope-model.md` §5.6 relationship semantics; `web-ui.md` V1.74 stage; DESIGN.md World KB relationship tokens added; `local-api-surface-conventions.md` §7 patch-relationship extension.
- [ ] T4 — `tech_debt_summary` refresh: 12 → 0 open; update `metadata.latest_ship` block (V1.74 compass, plan_count: 4, wire_contracts_changed: true, residual_summary: {closed: 12, deferred_to_v175: <n>, open_post_v174: <n>}).
- [ ] T5 — Profile B compaction: archive 4 plan rows (P-1, P0, P1, P-last) to `.mstar/archived/plans/<plan-id>.json`; append plan_ids to `.mstar/archived/plans-done.json` (strings only); remove from `status.json.plans[]`; verify layout invariant (`python3 -c` strings-only assertion).
- [ ] T6 — `iterations/README.md` V1.74 row: Shipped status + PR + merge commit + summary.
- [ ] T7 — `notes.json` append: V1.74 ship narrative (timeline + plan count + wire bump 0.9.0→0.10.0 + residual delta 12→0 + integration branch retirement + slate-clear milestone).
- [ ] T8 — Integration PR `iteration/v1.74` → `main`; wait for all CI checks (default + 1 hour budget); merge with `--merge` (merge commit) for provenance.
- [ ] T9 — PM signoff; retire `iteration/v1.74` in metadata (`integration_branch_retired: true`, `last_integration_branch: "iteration/v1.74"`, `latest_active_iteration: "V1.75 (pending /iteration-start)"`).

## Acceptance (maps to compass §7 DoD #7, #8, #10)

- 12 residuals `lifecycle: resolved` in SSOT; `tech_debt_summary.total_open: 0`.
- FEAT-WORLD-KB-RELATIONSHIPS in shipped archive; not in active tracker §2.3.
- Spec promotions committed; DESIGN.md relationship tokens in both themes.
- Profile B compaction verified (strings-only `plans-done.json` + per-file archive).
- PR merged to `main`; CI green; `iteration/v1.74` retired.

## Notes

- PM-whitelist plan (status/spec/index/PR maintenance). No specialist artifact body.
- Residual severity strings must use the machine enum from `mstar-plan-artifacts` (`medium`, `low`, `nit`).
- This iteration's signature achievement: **residual slate cleared to 0** for the first time — record in `notes.json` as a milestone so V1.75 canvas-pivot starts clean.
