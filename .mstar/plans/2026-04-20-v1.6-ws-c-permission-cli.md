# V1.6 WS-C — ACP Permission Policy CLI Surface

**Plan ID**: `2026-04-20-v1.6-ws-c-permission-cli`
**Status**: InReview
**Compass (SSOT)**: [.mstar/iterations/v1.6-delivery-compass-v1.md](.mstar/iterations/v1.6-delivery-compass-v1.md) §4 WS-C
**Milestone**: M2 — ACP preparation + creator tooling complete
**Working branch**: `feature/v1.6`
**Dependencies**: none (parallel with WS-A/B/D)

**Goal**: Expose existing `policy.rs` rule engine through `nexus42 permission` CLI command group.

**Tasks**:

- [x] T1: Implement `nexus42 permission list [--agent <id>]` — list grant/deny/ask rules.
- [x] T2: Implement `nexus42 permission grant <agent> <capability>`.
- [x] T3: Implement `nexus42 permission deny <agent> <capability>`.
- [x] T4: Implement `nexus42 permission ask <agent> <capability>`.
- [x] T5: Implement `nexus42 permission revoke <agent> <capability>` — remove specific rule.
- [x] T6: Implement `nexus42 permission reset [--agent <id>]` — wipe rules.

**Evidence**:
- Grant → list → deny → revoke → list roundtrip succeeds.
- Reset removes all rules for agent.
- `.nexus42/permissions.toml` reflects changes.
- `cargo test --workspace` green; clippy/fmt clean.

**Effort (agent-oriented)**: S — 1 agent session.

---

*Created: 2026-04-20.*
