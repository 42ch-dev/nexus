# P0 — Orchestration load 404 repair

> **Iteration compass:** `.mstar/iterations/v1.132/delivery-compass.md`  
> **Spec:** `.mstar/iterations/v1.132/specs/orch-load-404.md`  
> **SSOT status:** `.mstar/status.json`

**Goal:** Strategy / Sessions / Modules detail load on healthy daemon — no generic `Request failed with status 404`.

**Architecture lock (2026-07-22):** RCA first. The web client owns request construction and state classification; `crates/nexus-daemon-runtime` owns route registration and healthy/engine-absent semantics; the `apps/desktop` sidecar/Overlay owns daemon process and endpoint context. A framework/router 404 is a route/runtime miss until curl + Network evidence proves another boundary. Fix only the proven owner; engine absence remains 503/UnavailableState. No classification-only Done.

**Tech Stack:** `apps/web`, `crates/nexus-daemon-runtime`, `apps/desktop` sidecar.

**Execution:** mstar-sdd

### Wire contracts

- **Locked verdict:** `wire_contracts_changed: false` unless RCA proves a wire-shape gap and the plan is amended

## Prepare gates

- specify: [done]
- clarify: [done]
- plan: [locked] — architect lock 2026-07-22

### Architecture lock block

- RCA matrix must record exact method, URL, status, and body for client, daemon, and desktop-sidecar contexts.
- Client route mapping, daemon route registration, and sidecar endpoint/process selection are separate ownership boundaries; do not collapse them into a generic unavailable state.
- Healthy daemon 404, stale daemon/wrong host-port, and engine-absent 503 are distinct failure modes with distinct owners and regression expectations.

## Intent gate

| Item | Statement |
| --- | --- |
| Real goal | 编排 surfaces load on a healthy daemon |
| User value | Authors/operators can use Strategy / Sessions / Compute Modules without hitting 404 on a healthy daemon; restores dogfood trust after V1.130 P3a false-Done |
| Success criteria | Compass AC-0, AC-0b |
| Done gate | No classification-only; dogfood Network/curl evidence required |
| Non-goals | Engine auto-start; IA moves |

## T1 RCA gate (2026-07-22)

**Status:** DONE (curl + code-path; no browser Network — Vite not running).

**Ranked owner:** `crates/nexus-daemon-runtime` route registration (`{param}` vs `:param` matchit capture). Same class as `R-HOTFIX-404-PARAM-SYNTAX` / hotfix `2026-07-16-hotfix-setup-continue-404`. Client paths correct; desktop sidecar ruled out (direct curl repro).

**Healthy daemon (`:8420`):** list routes 200 (`/presets`, `/orchestration/sessions`, `/compute/modules`); parametric detail routes framework 404 empty body (`/presets/{id}`, `/compute/modules/{id}`, `/orchestration/sessions/{id}`).

**Engine absent:** sessions list → 503 `service_unavailable` (TestServer `error_envelope.rs`); presets/modules list do not require engine.

**Evidence:** `.mstar/iterations/v1.132/guides/p0-orch-load-404-rca.md`

**T2–T4 boundary:** fix daemon `:param` route patterns + TestServer regressions; no client URL changes expected.

## Tasks

- [x] **T1 · Must:** RCA matrix (curl + Network) for presets / sessions / compute modules → **DoD:** actual URL + status + body; root owner ranked; healthy vs absent fixtures noted
- [x] **T2 · Must:** Fix Strategy load at proven boundary → **DoD:** list + canvas detail load; engine-absent honest UX
- [x] **T3 · Must:** Fix Sessions load at proven boundary → **DoD:** sessions load; 409/503 remain distinct
- [x] **T4 · Must:** Fix Modules list/detail at proven boundary → **DoD:** list + detail load under healthy contract
- [ ] **T5 · Must:** Regression + dogfood evidence → **DoD:** tests cover proven failure mode; Network/curl evidence attached to plan gate summary

## Roadmap / deferred

Engine auto-start if proven needed — separate plan.

## Working branch

`plan/v1.132-p0-orch-load-404` → `iteration/v1.132`
