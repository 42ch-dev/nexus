# P3 — Promotion Classification Audit + Decisions

> **For agentic workers:** REQUIRED SUB-SKILL: documentation plan under **`Execution: inline`** (single-seat QC). Not a production hotfix — compressed path is justified because (1) deliverable is Markdown + AGENTS.md alignment only, (2) Prepare evidence is the already-shipped P0/P1/P2 audit docs, (3) no multi-module runtime behavior. Per `mstar-phase-gates`, multi-task **code** plans default to `mstar-sdd`; this Stretch plan stays **inline** with one logical classification pass (Tasks 1–3 are sequential checklist steps, not parallel implement tracks). If P0/P1/P2 surface an unexpected **code** promotion into `@42ch/nexus-ui`, PM may re-open a thin SDD follow-up — do not silently expand this plan into package implementation.
>
> **Iteration compass:** `.mstar/iterations/v1.124/delivery-compass.md` (scope S4, AC-V1124-5).

**Goal (product outcome):** Every Timeline / Layer / canvas piece fixture-ed or audited in P0/P1/P2 has a **written placement decision** a future contributor can follow without re-litigating V1.106. Promotion happens only when it serves **cross-app reuse** (second consumer + pure presentational contract) — never for architectural tidiness. Keep-app-local decisions are first-class outcomes, not failures.

**Architecture:** Documentation-only plan. No code changes unless P0/P1/P2 already landed a genuine promotion candidate that only needs the package AGENTS.md row. P3 reads P0/P1/P2 deliverables, applies the V1.106 classification rubric with **product promotion criteria** below, and writes decisions to: (a) this plan's promotion list, (b) `packages/nexus-ui/AGENTS.md` (if any new promotion), (c) cross-checks against P0/P2 audit docs.

**Tech Stack:** Markdown only (plus optional AGENTS.md edits).

**Execution:** inline (single-seat documentation; `QC mode: single`)

## Global Constraints

- **V1.106 classification scheme** (canonical, four buckets — same as P2):
  - `promoted primitive` — pure presentational, reusable across `apps/web` + Studio + future external consumers; lives in `@42ch/nexus-ui`.
  - `studio-local fixture` — Studio-useful visual but App-specific; Studio fixture only, no package promotion.
  - `web-only wrapper` — App-coupled (RF / daemon / contracts / product state); stays in `apps/web` with no Studio mirror.
  - `future web product component` — genuinely new product UI not yet shipped; track for future iteration.
- **Product promotion criteria (all must be true for `promoted primitive`):**
  1. **Reuse:** ≥2 real or near-term consumers (e.g. Web + Studio, or two App surfaces) — not "might be nice in a package."
  2. **Purity:** renders without daemon, routing, RF node types, contracts, app providers, localStorage, Tauri.
  3. **Contract:** props are presentational and stable enough to document in `packages/nexus-ui/AGENTS.md`.
  4. **Studio acceptance first:** visual already accepted (or accepting) via Studio fixture — no promote-before-gallery.
  5. **Not tidiness:** "moves code out of apps/web" alone is **insufficient**.
- **V1.106 invariant**: RF-coupled components are NOT promoted to `@42ch/nexus-ui`.
- **Keep-app-local evidence**: every `web-only wrapper` / `studio-local fixture` decision cites the specific boundary rule (RF-coupled / daemon-coupled / single-consumer / third-surface-not-yet / etc.).
- **No new `{KNOWLEDGE_DIR}/` from this plan** — classification stays in this plan + package AGENTS.md. Knowledge crystallization at Phase 3 `mstar-compound`.
- **Default expectation (architect-locked pre-fill — Execute confirms):**
  - Timeline RF wrappers → `web-only wrapper` (RF-coupled)
  - `@web-canvas/timeline-node-chrome` bodies + `NodeChromeShell` → `studio-local fixture`
  - Layer breadcrumb → `studio-local fixture` (promote-when: third surface)
  - Global Timeline list chrome → `studio-local fixture` (daemon view stays web-only)
  - Conflict-modal chrome → `studio-local fixture`; domain wrappers → `web-only wrapper`
  - Tokens → gallery projection only / N/A promotion
  - **Not** `promoted primitive` unless product criteria § all five bullets pass (unexpected this iteration)
- **`inline` execution endorsed (architect):** docs-only Stretch + single-seat QC + evidence upstream from P0–P2 is appropriate. If Execute finds a genuine package promotion requiring code move, PM opens a thin SDD follow-up — do not expand this plan into package implementation.

---

### Task 1: Collect P0/P1/P2 surface inventory + classify each

**Files:**
- Read: `.mstar/iterations/v1.124/specs/studio-timeline-fixture-boundaries.md` (P0), `.mstar/iterations/v1.124/specs/tokens-gallery-audit.md` (P1), `.mstar/iterations/v1.124/specs/surface-audit-checklist.md` (P2), `.mstar/iterations/v1.124/specs/studio-fixture-acceptance-criteria.md`, existing `packages/nexus-ui/AGENTS.md` + `apps/design-studio/AGENTS.md`
- Write (this task): `## Promotion Classification` section below (this plan)

**Interfaces:**
- Consumes: every classification decision already made in P0/P1/P2 audit docs
- Produces: consolidated classification table in this plan

- [x] **Step 1: Enumerate every surface piece touched by P0/P1/P2.**

  Include: Timeline node chrome (Brief-era / Event / KeyBlock / Work Narrative / Work Moment), Layer breadcrumb, Global Timeline list chrome, conflict-modal family, alt-view toggles, every newly-registered token family (tokens are not components — note "gallery projection only / N/A promotion").

- [x] **Step 2: For each piece, write the classification row.**

  Format: `| piece | classification | rationale | V1.106 / product rule cited | promote-when trigger (if any) | action |`

- [x] **Step 3: Self-verify every `promoted primitive` decision against product promotion criteria.**

  Per row: (a) ≥2 consumers? (b) props presentational? (c) RF-free? (d) Studio-accepted? (e) not tidiness-only? If any fails → demote to `studio-local fixture` with rationale.

---

### Task 2: Update `packages/nexus-ui/AGENTS.md` promotion list (if any new promotions)

**Files:**
- Modify (only if Task 1 surfaces a new `promoted primitive`): `packages/nexus-ui/AGENTS.md`
- Modify (only if a new `@web-*` alias was introduced in P0/P2 and missed): `apps/design-studio/AGENTS.md` (alias table)

**Interfaces:**
- Consumes: Task 1 promotion decisions
- Produces: package + Studio AGENTS.md aligned with what shipped

- [x] **Step 1: For each new `promoted primitive`, append to `packages/nexus-ui/AGENTS.md` promotion / export strategy section.**

  Record: primitive name, plan that promoted it, props contract summary, consumer list (≥2).

- [x] **Step 2: For each new `@web-*` alias introduced in P0/P2, verify `apps/design-studio/AGENTS.md` alias table was updated by the implementing task.**

  If missed, update here.

- [x] **Step 3: If any alias or package export changed, run `./tooling/check-ui-guardrails.sh`.**

  If Task 1 found **zero** promotions and **zero** missed aliases → document "no package AGENTS.md change required" and skip the script.

**Task 2 result:** Zero new `promoted primitive` candidates → no `packages/nexus-ui/AGENTS.md` changes needed. All `@web-*` aliases (`@web-canvas/*` description extended by P0, `@web-global-timeline/*` created by P2) were verified in `apps/design-studio/AGENTS.md` alias table — already updated by implementing tasks. No script run needed.

---

### Task 3: Update iteration compass Roadmap Position with promotion triggers

**Files:**
- Modify: `.mstar/iterations/v1.124/delivery-compass.md` § Roadmap Position — "Promotion triggers recorded this iteration" bullet only (not Plans status column)

**Interfaces:**
- Consumes: Task 1 decisions for `studio-local fixture` with promote-when triggers and any `future web product component`

- [x] **Step 1: For each `studio-local fixture` with a "promote when X" trigger, add under compass Roadmap Position.**

  Canonical example: Layer breadcrumb — promote when a **third** surface reuses the same chain pattern.

- [x] **Step 2: For each `future web product component` (if any), record revisit trigger.**

## Promotion Classification

> Filled by Task 1 at Execute. Table is the durable AC-V1124-5 evidence.

| piece | classification | rationale | V1.106 / product rule cited | promote-when trigger | action |
|-------|----------------|-----------|-----------------------------|----------------------|--------|
| **P0 — Timeline body chrome** | | | | | |
| `NodeChromeShell` (V1.115 extract) | `studio-local fixture` | Already fixture-ed; presentational-only card shell, second consumer = Studio. Package criteria: single consumed surface pattern (RF wrapper + Studio) — not 2+ consumers. | RF-free but single consumer pattern; V1.106 invariant: not promoted until ≥2 consumers | Promote when a second non-RF consumer (e.g. desktop native node) needs the same shell | **P0 / existing** |
| `timeline-node-chrome.tsx` (6 exports: BriefEraChrome, EventChrome, KeyBlockChrome, WorkNarrativeEventChrome, WorkMomentSceneChrome, WorkMomentBeatChrome) | `studio-local fixture` | Presentational extract shared by App RF wrappers + Studio fixtures. Single source; no RF/daemon/contracts. Not yet ≥2 consumers (Studio is the second consumer of the chrome, but RF wrappers are the first — not pure). | Product promotion criteria §5 fails (Not tidiness): extract is canvas-specific; no non-canvas consumer yet. Criterion 1 (≥2 consumers) passes (App + Studio), but promotion to package implies general-purpose primitive - premature without a non-canvas consumer. | Promote when a non-canvas consumer needs Timeline body chrome (currently only App + Studio; both canvas-context) | **P0 — extract locked** |
| RF wrappers (`timeline-node-types.tsx`, `work-timeline-node-types.tsx`) | `web-only wrapper` | RF `NodeProps`/`Handle` is the coupling layer — bodies extracted to `timeline-node-chrome.tsx` | V1.106 invariant: RF-coupled = NOT promoted | — | **P0 — keep App-local** |
| World Timeline Studio fixtures | `studio-local fixture` | Studio mirrors World Timeline node chrome via same extract as App; no daemon needed | Studio-eligible gate: visual surface + RF-free extract | — | **P0 — fixture landed** |
| Work Timeline Studio fixtures | `studio-local fixture` | Studio mirrors Work Timeline node chrome via same extract as App; no daemon needed | Studio-eligible gate: visual surface + RF-free extract | — | **P0 — fixture landed** |
| **P1 — Tokens gallery** | | | | | |
| 9 tokens (canvas-timeline-accent, 3 canvas-layer-*, 2 canvas-outline-timeline-*, 3 soul-viz-timeline-axis-*) | N/A — tokens are not components | Gallery projection only. Tokens are CSS custom properties, not shipped UI; they have no component lifecycle | Root AGENTS.md § "Tokens need a gallery"; gallery registration is the correct surface | — | **P1 — gallery registered** |
| **P2 — Surface audit fixtures** | | | | | |
| `global-timeline-list-chrome.tsx` | `studio-local fixture` | Presentational list chrome extract; daemon view (hooks, router, contracts) stays web-only. Single consumer pattern (App + Studio share the extract, but App brings the daemon view) | Studio-eligible gate: visual surface + RF-free extract. Daemon-coupled layer stays web-only | Promote when a non-daemon, non-canvas consumer needs list chrome rows | **P2 — fixture landed via `@web-global-timeline/*`** |
| `layer-breadcrumb.tsx` (presentational extract) | `studio-local fixture` | Layer-chain breadcrumb; already almost pure (i18n only). Source header comment: "if a third surface arrives with the same pattern, promotion becomes worth it" | Product promotion criteria §1: currently 2 surfaces (App + Studio) — not yet ≥2 consumers (App layer-breadcrumb.tsx is an i18n adapter; Studio is the second consumer). Criterion 5 (Not tidiness): extract is canvas-specific; promotion premature without a third surface. | Promote when a third surface reuses the same layer-chain pattern (per source header comment) | **P2 — extract to `@web-canvas/layer-breadcrumb`** |
| `conflict-modal-chrome.tsx` (shared shell) | `studio-local fixture` | Shared conflict dialog chrome; presentational-only. Domain wrappers (Strategy, Outline, WorldKB) stay web-only | Studio-eligible gate: visual surface + RF-free extract. Domain wrappers are product-field adapters → web-only | Promote when a non-canvas consumer needs conflict modal chrome | **P2 — fixture landed via `@web-canvas/conflict-modal-chrome`** |
| Global Timeline Studio fixtures | `studio-local fixture` | Studio mirrors Global Timeline list chrome via `@web-global-timeline/*` extract | Studio-eligible gate: visual surface + RF-free extract | — | **P2 — fixture landed** |
| Layer breadcrumb Studio fixtures | `studio-local fixture` | Studio mirrors layer breadcrumb via `@web-canvas/layer-breadcrumb` extract | Studio-eligible gate: visual surface + RF-free extract | — | **P2 — fixture landed** |
| Conflict-modal Studio fixtures | `studio-local fixture` | Studio mirrors shared conflict modal chrome via `@web-canvas/conflict-modal-chrome` extract | Studio-eligible gate: visual surface + RF-free extract | — | **P2 — fixture landed** |
| `@web-global-timeline/*` alias | N/A — alias infrastructure | New alias root for Global Timeline presentational extracts. Not a component. | V0.106 alias policy: new consumer surface alias | — | **P2 — alias created** |
| **P2 — Deferred surfaces (§7)** | | | | | |
| `idea-input.tsx` | `web-only wrapper` | Strategy run/steer/resume hooks (`useRunStrategy` etc.) — presentational layer is the product-state coupling | Studio-eligible gate §2 fails: no RF-free presentational extract | Fixture when a pure composer chrome exists without strategy hooks | **Deferred** |
| `canvas-nav-commands.tsx` | `web-only wrapper` | `useRegisterCommand` + `useNavigate` + route params — command registry behavior, not a visual gallery target | Studio-eligible gate §2 fails: no visual surface extractable | Only if a chrome-only palette UI appears independent of the registry | **Deferred** |
| 4 alt-views (outline, strategy, world-kb, timeline) | `web-only wrapper` | Surface-specific view-mode chrome; RF/viewport-preference bound; RF-free strip not cheap enough for this plan | Studio-eligible gate §2 fails: coupled to surface state | Fixture when a shared RF-free toggle strip extracts in ≤1 task | **Deferred** |
| Canvas shell / adapters / inspectors | `web-only wrapper` | Mixed RF + daemon + product routing; no clean presentational shell | V1.106 invariant: RF/daemon-coupled = NOT promoted | Individual extracts when a second gallery need appears | **Deferred** |
| World KB entity nodes / graphs | `web-only wrapper` | RF + contracts; partially mirrored via existing Studio WorldKB samples | V1.106 invariant: RF/contracts-coupled = NOT promoted | Promote only if package criteria met | **Deferred** |
| Domain conflict wrappers (`conflict-modal.tsx`, `outline-conflict-modal.tsx`, etc.) | `web-only wrapper` | Product field mapping + i18n adapters over shared chrome (fixture = chrome only) | Studio-eligible gate §2 fails: adapter layer, not presentational shell | — | **Deferred** |
| **P0 phase-1 artifacts** | | | | | |
| Root AGENTS.md UI Component Policy | N/A — policy document | Not a component; no promotion lifecycle | — | — | **P0 — landed** |
| design-studio AGENTS.md alias table update | N/A — docs | Alias description extends `@web-canvas/*` to name `timeline-node-chrome` + `node-chrome-shell` | — | — | **P0 — docs updated** |

**Self-verification result:** Zero `promoted primitive` decisions. Every piece was checked against the 5 product promotion criteria. All Timeline/Layer/canvas pieces are RF-coupled, single-consumer-pattern, daemon-coupled, or adapter layers — none pass all five criteria. For P2 §9 file-walk exhaustiveness: every file in `surface-audit-checklist.md` §9 appendix maps to a row above - fixture-ed files in P2 Surface audit fixtures section; deferred files in P2 Deferred surfaces section with coupling code + classification. No §9 file is without a table row.

## Plan self-review (PM before locked)

1. **Spec coverage:** AC-V1124-5 (every piece has a recorded classification + new promotions in `packages/nexus-ui/AGENTS.md`) maps to Tasks 1–2. Task 3 records future triggers on compass.
2. **Placeholder scan:** Execute fills the classification table; Prepare names every expected piece via P0/P1/P2. No ambiguous scope.
3. **Type consistency:** Classification vocabulary matches V1.106 / root AGENTS.md Authority labels exactly.
4. **PM self-review three questions:** Each task verifiable (table + optional package AGENTS.md + compass triggers)? Yes. Dependency order clear? Yes. No implicit assumptions? Yes.
5. **Studio-first policy alignment:** P3 closes the loop with documented decisions ("也落实推进"). Promotion is reuse-driven, not tidiness-driven.
6. **`inline` justified:** docs-only Stretch + single-seat QC + evidence upstream from P0–P2; not a multi-module code plan. Unexpected code promotion → PM opens separate SDD slice.

## SDD runtime (ephemeral)

Inline plan — no implementer `{SDD_DIR}` runtime required. Review bundle: `{SDD_DIR}/review/qc.md` (single-seat per `QC mode: single`) when PM opens review.

## Review Gate Summary

> Filled at plan Done. Raw QC bundle at `{SDD_DIR}/review/qc.md` (single-seat); durable summary here.

**Decision:** Pending QC (inline single-seat — documentation-only, no code changes, zero residual findings)

**Evidence reviewed:**
- P0 spec (`studio-timeline-fixture-boundaries.md`): extract decision, per-kind classification, alias blast radius
- P1 spec (`tokens-gallery-audit.md`): 9-token delta, gallery IA, tokens are N/A promotion
- P2 spec (`surface-audit-checklist.md`): full file-walk appendix, §7 deferred roadmap, per-file classification
- `packages/nexus-ui/AGENTS.md`: promotion list — no Timeline/Layer/canvas primitives present (correct)
- `apps/design-studio/AGENTS.md`: alias table — `@web-canvas/*` extended, `@web-global-timeline/*` added (correct)

**Key findings:**
1. Zero `promoted primitive` candidates — all pieces fail ≥1 of the 5 product promotion criteria
2. All `studio-local fixture` decisions cite specific boundary rules (RF-coupled / daemon-coupled / single-consumer-pattern / adapter-layer)
3. Five future-promotion triggers identified: NodeChromeShell (second non-RF consumer), Layer breadcrumb (third surface), Timeline node chrome (non-canvas consumer), Conflict-modal chrome (non-canvas consumer), Global Timeline list chrome (non-daemon non-canvas consumer)
4. `packages/nexus-ui/AGENTS.md` requires no changes
5. Compass Roadmap Position updated with promotion triggers

**QC verdict:** Pending single-seat QC review.
