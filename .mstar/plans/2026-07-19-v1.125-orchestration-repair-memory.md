# P1 — Orchestration repair + Memory move

> **Iteration compass:** `.mstar/iterations/v1.125/delivery-compass.md`  
> **Spec:** `.mstar/iterations/v1.125/specs/orchestration-repair-memory.md`  
> **SSOT status:** `.mstar/status.json`

**Goal:** Orchestration views load honestly; Memory lives under Orchestration; deep links select Orchestrator tab.

**Execution:** mstar-sdd

### Wire contracts (architect LOCKED)

- **Verdict: `wire_contracts_changed: false`** — classify existing daemon 503 `service_unavailable` / `"engine not available"` on the client; no handler or schema change in P1.

## Tasks

- [ ] **T1 (AC-V1125-3):** Fix Strategies / Modules / Sessions ErrorState misuse; add `isOrchestrationEngineUnavailable` helper; map 503 engine-unavailable to honest empty/unavailable (not “无法加载此视图”); keep Back on strategy detail errors. **Prefer client classification** — do not edit `sessions.rs` unless dogfood proves insufficient.
  - **Verify (author):** Idle or engine-down states show honest copy + Retry; no generic crash title.
  - Files: `strategies-page.tsx`, `strategy-page.tsx`, `strategy-canvas.tsx`, `modules-page.tsx`, `sessions-page.tsx`, `lib/nexus/errors.ts` (helper), `queries.ts` as needed, tests
- [ ] **T2 (AC-V1125-4):** Move Memory to Orchestrator as **first** group; label order **Memory → Strategies → Runtime (Sessions, Schedule) → Compute (Modules)**; remove Creator Memories; **route-derived** Orchestrator tab from pathname prefix set.
  - **Verify (author):** Sidebar group order matches; `/memory` etc. selects Orchestrator tab; Creator has no Memories.
  - Files: `sidebar.tsx`, `shell-sidebar-chrome.tsx` if needed, locale keys, tests

## Done when

- **AC-V1125-3:** No false “无法加载此视图”; 503 → honest unavailable/empty.
- **AC-V1125-4:** Groups Memory → Strategies → Runtime → Compute; deep links select Orchestrator tab.

## Working branch

`feature/v1.125-orchestration-repair-memory` → merge `iteration/v1.125`
