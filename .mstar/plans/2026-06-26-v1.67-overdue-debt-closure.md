# Plan — V1.67 P2: Overdue Debt Closure (V1.52/V1.60)

**plan_id**: `2026-06-26-v1.67-overdue-debt-closure`
**Iteration**: V1.67 · **Wave**: 2 · **Track**: D · **Effort**: M
**Owner**: `@fullstack-dev`
**Status**: Todo
**Depends on**: P-1
**Working branch**: `feature/v1.67-overdue-debt-closure`
**Compass**: §1.1 Track D

## Goal

Close the 9 V1.52/V1.60 residuals targeting V1.61+ that have been carried 6+ iterations. All low-severity, low-risk targeted fixes across `nexus-orchestration` / `nexus-kb` / `nexus-local-db` internals. Re-anchor the residual slate.

## Residual coverage (9)

- `R-V152TA-S001` — heuristic/LLM signal drift risk between `extract_kb_candidates_for_review` and `LlmExtract`.
- `R-V152TA-S006` — outline 五问 gate observability: info-log on NOGO with dimension scores.
- `R-V160P0-QC1-W002` — `R-V159P0-002` auto-derivation hardcodes positional column indices (`cols[1]`/`cols[3]`).
- `R-V160P0-QC2-W001` — duplicated ownership gate (`ensure_world_owned` inline copy from daemon `host_tool_…`).
- `R-V160P0-QC2-W002` — `timeline.event.append` does explicit-ID rename AFTER `append_event` succeeds, outside tx.
- `R-V160P0-QC3-W001` — N+1 query pattern in `world.delta.apply` loop (one SELECT + one UPDATE per change).
- `R-V160P1-QC1-W001` — `script.section_status.update` capability referenced in spec + preset but missing. **Additive capability — Phase 2b §5 #6 confirms class** (local vs wire).
- `R-V160P1-QC2-W001` — `is_script_complete` builds paths from caller-supplied `workspace_dir` + `work_ref` without validation.
- `R-V160P1-QC2-W002` — table-recreate migration `202606230001` lacks explicit post-rename `PRAGMA foreign_key_check`.

## Acceptance criteria

- AC1: All 9 residuals closed with rationale + regression test where behavior-changing (N+1, append-outside-tx, path validation, migration PRAGMA).
- AC2: `cargo test --all` + `cargo clippy --all -- -D warnings` clean.
- AC3: `script.section_status.update` capability implemented per §5 #6 verdict (local-only or folded into F-P3 bump).
- AC4: No unintended behavior change (surgical fixes only — `mstar-coding-behavior`).

## Tasks (skeleton)

- T1: KB signal drift + 五问 observability (R-V152TA-S001/S006).
- T2: V1.60 P0 quartet — column indices, ownership-gate dedup, timeline-append tx, world.delta N+1.
- T3: V1.60 P1 trio — script.section_status.update capability, is_script_complete path validation, migration PRAGMA.
- T4: tests + clippy/fmt.

## Out of scope

Local API surface work (P0). Frontend gaps (P1). Any V1.68 feature.

## Dependencies / branch policy

Topic branch `feature/v1.67-overdue-debt-closure` from `iteration/v1.67`. Wave 2 (sequential with P1 for QC load; different crates so could parallelize — PM decides at dispatch). Merge target `iteration/v1.67`. `script.section_status.update` confirmed local-only (compass §5 #6 LOCKED; stays in P2).

## Completion Report v2

All 9 residuals closed on branch `feature/v1.67-overdue-debt-closure`.

| Residual | Fix location | Key change | Regression test |
|----------|--------------|------------|-----------------|
| R-V152TA-S001 | `crates/nexus-orchestration/src/quality_loop.rs` | `tracing::warn!` when heuristic candidate confidence differs from LLM confidence by ≥0.2 | `llm_signal_drift_warns_on_large_confidence_gap` |
| R-V152TA-S006 | `crates/nexus-orchestration/src/quality_loop.rs` | `outline_five_q_check` info-logs dimension scores on NOGO; extracted `build_five_q_verdict` helper | `outline_five_q_nogo_info_logs_dimensions` |
| R-V160P0-QC1-W002 | `crates/nexus-daemon-runtime/src/capability_registry.rs` | Parse catalog table by named column headers instead of positional indices | `catalog_registry_invariant_all_ids_present` already covers; comment added |
| R-V160P0-QC2-W001 | `crates/nexus-local-db/src/narrative_write.rs` + `crates/nexus-orchestration/src/capability/builtins/{world.rs,timeline.rs,host_tool_handlers.rs}` | Extract shared `is_world_owned`; dedupe ownership gate in orchestration and daemon | `world.rs` ownership tests |
| R-V160P0-QC2-W002 | `crates/nexus-local-db/src/narrative_write.rs` + `crates/nexus-orchestration/src/capability/builtins/timeline.rs` | Wrap timeline append + explicit-id rename in one transaction via `append_event_in_tx` | `timeline_event_append_explicit_id_persists_atomically` |
| R-V160P0-QC3-W001 | `crates/nexus-orchestration/src/capability/builtins/world.rs` | Batch pre-fetch `body_json` for all KB update targets in one SELECT | `world_delta_apply_batch_kb_updates_prefetch` |
| R-V160P1-QC1-W001 | `crates/nexus-orchestration/src/capability/builtins/script_section_status.rs` + `capability/mod.rs` | Implement `script.section_status.update` capability (mirrors game_bible, operates under `Scripts/`) | `run_updates_script_section_status_draft_to_reviewed`, etc. |
| R-V160P1-QC2-W001 | `crates/nexus-local-db/src/work_chapters.rs` | Canonicalize workspace boundary and validate `work_ref` in `is_script_complete` | `test_is_script_complete_rejects_path_traversal_in_work_ref` |
| R-V160P1-QC2-W002 | `crates/nexus-local-db/migrations/202606230001_work_profile_script.sql` + `src/lib.rs` | Add `PRAGMA foreign_key_check` after table-rename; add migration FK violation test | `migrations_leave_no_foreign_key_violations` |

Verification run (final):
- `cargo test --all` — passed
- `cargo clippy --all -- -D warnings` — passed
- `cargo +nightly fmt --all --check` — passed
- `./tooling/check-wire-drift.sh` — passed (no schema changes)

Registry counts updated: `with_builtins` = 34, `with_runtime_deps` (no pool) = 33.

Note: `.mstar/status.json` residual lifecycle was not edited per explicit instruction.

## Completion Report v2 (fix-wave-1)

**Agent**: `@fullstack-dev` (primary track)
**Task**: qc3 W-001 + W-002 blocking fixes
**Status**: Done
**Scope Delivered**:
- **W-001 — hard `PRAGMA foreign_key_check` failure in `run_migrations`**
  - `crates/nexus-local-db/src/lib.rs`: after `sqlx::migrate!().run(pool)`, run `PRAGMA foreign_key_check`, consume returned rows, and return `LocalDbError::ConstraintViolation` when violations exist.
  - Corrected the existing regression test's tuple mapping to `(String, i64, String, i64)` to match SQLite's actual result columns.
  - Added `migrations_fail_on_foreign_key_violation` regression test that injects a dangling `kb_key_blocks.world_id` FK and asserts `run_migrations` returns an error.
- **W-002 — bound `world.delta.apply` kb_key_block pre-fetch IN-list**
  - `crates/nexus-orchestration/src/capability/builtins/world.rs`: introduced `KB_PREFETCH_CHUNK_SIZE = 500`, deduplicated update target ids, and chunked the pre-fetch `SELECT ... WHERE key_block_id IN (...)` so SQL/bind count stays bounded for any caller input size.
  - Added `world_delta_apply_batch_kb_updates_prefetch_chunks` regression test exercising `chunk_size + 5` updates and verifying all are applied correctly.

**Working branch used**: `feature/v1.67-p2-fixwave1` (worktree `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/v1.67-p2-fixwave1/`)

**Git commits**:
- `2174c07e` — `fix(nexus-local-db): enforce PRAGMA foreign_key_check as hard migration failure (W-001)`
- `76e9a60b` — `fix(nexus-orchestration): chunk and dedupe world.delta.apply kb_key_block pre-fetch (W-002)`

**Validation** (all green):
- `SQLX_OFFLINE=true cargo test -p nexus-orchestration -p nexus-local-db` — passed
- `cargo clippy -p nexus-orchestration -p nexus-local-db -- -D warnings` — passed
- `cargo +nightly fmt --all --check` — passed
- `bash tooling/check-wire-drift.sh` — passed

**Issues/Risks**:
- `gitnexus_impact` on `run_migrations` reported **CRITICAL** blast radius because the function is central to DB initialization. The change is a fail-closed safety gate that only errors when the DB already has FK violations, so it does not alter normal-path behavior.
- `gitnexus_detect_changes()` reported no changes (likely index/worktree detection quirk); the actual diff is the two files above.

**Handoff**: Ready for qc3 targeted re-review.
