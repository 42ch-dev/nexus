# Plan — V1.67 P-sec: Desktop Shell Polish (V1.66 QC closure)

**plan_id**: `2026-06-26-v1.67-desktop-shell-polish`
**Iteration**: V1.67 · **Wave**: 1 · **Track**: C · **Effort**: S–M
**Owner**: `@fullstack-dev-2`
**Status**: Todo
**Depends on**: P-1
**Working branch**: `feature/v1.67-desktop-shell-polish`
**Compass**: §1.1 Track C

## Goal

Close the 10 fresh V1.66 desktop-shell QC suggestions while the context is hot, finishing the just-shipped Tauri shell to a clean state. All `apps/desktop` / `src-tauri/` / Tauri-side; **no wire change**.

## Residual coverage (10)

- `R-V166-QC1-S1` — event-driven daemon-status (replace Rust 100ms + React 5s dual poll loops with Tauri event).
- `R-V166-QC1-S2` — `sidecar.rs:198` robust unwrap (`unwrap_or_else` fallback message).
- `R-V166-QC1-S4` — `desktop-build.yml` §5 #10 conditional fallback to per-arch artifacts when universal binary fails.
- `R-V166-QC1-S5` — idiomatic Tauri exit hook (`on_window_event`/`RunEvent::ExitRequested`) vs trailing handler.
- `R-V166-QC3-S1` — reset `restart_count` on manual `startDaemon` (currently stays at cap after give-up).
- `R-V166-QC3-S2` — reuse single `reqwest::Client` in `probe_health` (currently allocates per call).
- `R-V166-QC3-S3` — directory fsync after atomic rename (full crash durability).
- `R-V166-QC3-S4` — `RuntimeLockGuard` doc comment misleading (says releases-on-drop; actually explicit).
- `R-V166-QC3-S5` — restart backoff lacks jitter (pure 500ms×2^n).
- `R-V166-QC2-TOCTOU` — TOCTOU comment in both path guards (desktop `guard_path` + daemon `resolve_guarded_…`).

## Acceptance criteria

- AC1: All 10 residuals closed with rationale + test where applicable (event-driven status, robust unwraps, restart-count reset, dir fsync, backoff jitter, etc.).
- AC2: `apps/desktop` build green; `cargo test` for affected crates; desktop 13-test baseline preserved/extended.
- AC3: No wire/contract change (verified: `check-wire-drift` clean, no schema touch).
- AC4: macOS CI `desktop-build` job still green (QC1-S4 fallback path tested).

## Tasks (skeleton)

- T1: event-driven daemon-status (QC1-S1) — largest item; Tauri event bridge.
- T2: robust-unwrap + doc-comment + TOCTOU-comment trio (QC1-S2, QC3-S4, QC2-TOCTOU) — mechanical.
- T3: restart/health trio (QC3-S1 reset, QC3-S2 reqwest reuse, QC3-S5 jitter).
- T4: durability (QC3-S3 dir fsync).
- T5: CI (QC1-S4 per-arch fallback) + exit hook (QC1-S5).
- T6: tests + clippy/fmt.

## Out of scope

Any new desktop feature (distribution v2, signing, tray — V1.68+). Local API surface work (P0). Frontend gaps (P1).

## Dependencies / branch policy

Topic branch `feature/v1.67-desktop-shell-polish` from `iteration/v1.67`. Wave 1 parallel with P0 (low overlap). One git worktree. Merge target `iteration/v1.67`.

## Completion Report v2

**Completed**: 2026-06-26
**Branch**: `feature/v1.67-desktop-shell-polish`
**Commits**: `cf48a8f1`, `bc8d4bea`, `d21a089b`

### Residuals closed (10 / 10)

| Residual | Title | Commit |
|---|---|---|
| R-V166-QC1-S1 | Event-driven daemon-status (Tauri AppHandle::emit) | cf48a8f1 |
| R-V166-QC1-S2 | Robust unwrap fallback in sidecar.rs | cf48a8f1 |
| R-V166-QC1-S4 | CI universal→aarch64 fallback | bc8d4bea |
| R-V166-QC1-S5 | Idiomatic Tauri exit hook comment | cf48a8f1 |
| R-V166-QC3-S1 | Reset restart_count on manual start | cf48a8f1 |
| R-V166-QC3-S2 | Reuse reqwest::Client via OnceLock | cf48a8f1 |
| R-V166-QC3-S3 | Directory fsync after atomic rename | bc8d4bea |
| R-V166-QC3-S4 | RuntimeLockGuard doc correction | bc8d4bea |
| R-V166-QC3-S5 | Restart backoff jitter | cf48a8f1 |
| R-V166-QC2-TOCTOU | TOCTOU comments in both path guards | bc8d4bea |

### Files changed

- `apps/desktop/src-tauri/src/sidecar.rs`
- `apps/desktop/src-tauri/src/lib.rs`
- `apps/desktop/src-tauri/Cargo.toml`
- `apps/desktop/src-tauri/Cargo.lock`
- `apps/web/src/lib/nexus/desktop-capabilities.ts`
- `apps/web/src/components/layout/daemon-status-bar.tsx`
- `apps/web/src/components/layout/daemon-status-bar.test.tsx`
- `apps/web/src/components/path-context-menu.test.tsx`
- `apps/web/src/lib/nexus/desktop-capabilities.test.ts`
- `crates/nexus-daemon-runtime/src/api/runtime_lock.rs`
- `crates/nexus-daemon-runtime/src/api/path_guard.rs`
- `crates/nexus-daemon-runtime/src/api/handlers/chapters.rs`
- `crates/nexus-daemon-runtime/src/api/handlers/host_tool_handlers.rs`
- `.github/workflows/desktop-build.yml`
- `.mstar/status.json`

### Verification evidence

- `cargo check` in `apps/desktop/src-tauri/`: green
- `cargo test` in `apps/desktop/src-tauri/`: 16 passed
- `cargo clippy -- -D warnings` in `apps/desktop/src-tauri/`: green
- `SQLX_OFFLINE=true cargo check -p nexus-daemon-runtime`: green
- `SQLX_OFFLINE=true cargo clippy -p nexus-daemon-runtime -- -D warnings`: green
- `SQLX_OFFLINE=true cargo test -p nexus-daemon-runtime --lib`: 311 passed
- `cargo +nightly fmt --all --check`: green (root workspace + desktop standalone)
- `pnpm --filter web test`: 118 passed
- `pnpm --filter web typecheck`: green
- `bash tooling/check-wire-drift.sh`: 4 passed, no drift

### Notes / key decisions

- Tauri v2 `Builder::run` only accepts context; the exit cleanup is correctly implemented via `RunEvent::ExitRequested` in the run handler. Added a clarifying comment rather than an unidiomatic API change.
- Added `fastrand = "2"` for backoff jitter; it was already a transitive dependency in the Tauri tree.
- Directory fsync is implemented with `tokio::fs::File::open(parent).await?.sync_all().await?` after atomic rename.
- No wire/contract changes; `check-wire-drift.sh` remains clean.

### Status

- `.mstar/status.json` updated: plan `2026-06-26-v1.67-desktop-shell-polish` set to `Done`, all 10 residuals marked `resolved` with closure notes and resolution commits.
- Ready for merge to `iteration/v1.67` via PR per repository merge discipline.
