# Plan: V1.70 CI / Desktop-Build Optimization

**Plan ID**: `2026-06-27-v1.70-ci-desktop-build-optimization`
**Iteration**: V1.70
**Status**: Todo
**Owner**: `@ops-engineer`
**Effort**: S
**Depends on**: P-1 (compass Locked)
**Working branch**: `feature/v1.70-ci-desktop-build-optimize` from `iteration/v1.70`
**Merge target**: `iteration/v1.70`
**Worktree**: `.worktrees/v1.70-ci/`
**Task category**: `ops`
**Superpowers**: `using-git-worktrees` (parallel with P0; same repo, file-disjoint)

## Compass reference

[`v1.70-canvas-strategy-implement-and-ci-optimization-compass-v1.md`](../iterations/v1.70-canvas-strategy-implement-and-ci-optimization-compass-v1.md) §1.1 Track B (B1–B5), §6.2 path-filter regression risk.

## Scope

- **B1** Narrow `desktop-build.yml` `pull_request.paths` trigger: remove `crates/**`, `apps/nexus42/**`, `packages/nexus-contracts/**`, `pnpm-lock.yaml`, `package.json`, `pnpm-workspace.yaml`, `Cargo.toml`, `Cargo.lock`. Keep `apps/desktop/**`, `apps/web/**`, `.github/workflows/**`. Rust-only PRs no longer trigger the 75-min macOS packaging build.
- **B2** Keep `push:main` full build as integration safety net (broad path filter or no path filter on main).
- **B3** Add release/tag-gated full release build (`on: push.tags: ['v*']` or `release: types: [published]`) — full universal build + `.app`/`.dmg` upload with long retention / release-attachment. Distributable release packages produced here, not on every PR.
- **B4** Close `R-V167PSEC-QC1-S-CI-SETUO`: add `set -euo pipefail` to fallback + universal build blocks in `desktop-build.yml`.
- **B5** `ci.yml` untouched (test gate stays).

## Non-goals

- No `ci.yml` test-gate change
- No desktop distribution v2 (signing / tri-OS / auto-update) — V1.71+ backlog
- No CI runner/architecture changes (stay macos-14 aarch64-native + universal fallback)

## Tasks

- [ ] T1: Narrow `pull_request.paths` in `desktop-build.yml` (B1)
- [ ] T2: Verify `push:main` trigger retains broad coverage (B2)
- [ ] T3: Add release/tag-gated full release build trigger (B3 — decide: separate `desktop-release.yml` or additional trigger in `desktop-build.yml`)
- [ ] T4: Add `set -euo pipefail` to build blocks (B4)
- [ ] T5: Verify `ci.yml` untouched (B5)
- [ ] T6: Path-completeness self-check — confirm no surface affecting Tauri bundle is silently dropped from PR trigger (regression precedent: `R-V168P0-QC1-W001`)

## Acceptance

- `desktop-build.yml` PR trigger only fires on `apps/desktop/**`, `apps/web/**`, `.github/workflows/**` (and Tauri config if applicable)
- `push:main` trigger retains full build coverage
- Release/tag trigger produces distributable artifacts
- `set -euo pipefail` present in build blocks
- `ci.yml` unchanged
- `R-V167PSEC-QC1-S-CI-SETUO` closed in SSOT
- QC verifies path completeness (no regression like R-V168P0-QC1-W001)
