---
plan_id: 2026-06-23-v1.61-daemon-integration-and-closeout
title: V1.61 P-last — Daemon Integration + E2E + Closeout
iteration: V1.61
slot: P-last
status: Done
effort: M
owner: "@project-manager"
review: Report-only QA
wave: 4
dependency: P3 merged to integration
---

# P-last: Daemon Integration + E2E + Closeout

Wire wasm host into daemon runtime. End-to-end test. Hygiene + closeout.

## Tasks

- [x] T1: Wire wasmtime engine initialization into daemon `boot.rs`
- [x] T2: Load embedded modules from `include_dir!` at startup
- [x] T3: Scan `~/.nexus42/modules/` for user installed modules
- [x] T4: Register `narrative.compute` in daemon CapabilityRegistry
- [x] T5: E2E test — world with 2 computable characters → combat-engine preset → verify compute + state delta + events + battle report
- [x] T6: `cargo clippy --all -- -D warnings` + `cargo test --all` regression
- [x] T7: `cargo +nightly fmt --all --check`
- [ ] T8: Profile B compaction — 6 plans archived
- [ ] T9: `status.json` finalize — `metadata.latest_ship: V1.61`, `integration_branch_retired: true`
- [ ] T10: PR from `iteration/v1.61` to `main`

## Done when

- E2E test passes
- Full regression green
- Profile B compaction done
- PR merged to `main`

---

## Completion Report v2

- Plan: 2026-06-23-v1.61-daemon-integration-and-closeout
- Working branch: `feature/v1.61-daemon-integration-and-closeout`
- Commits: `ee042682` `2dc47d07` `08b81861` `32c53caa` `97f627fb` `1c1068a1` `21c6362c`
- Files changed: 19 (17 modified, 2 new — `module_cache.rs`, `compute_e2e.rs`; 5 prompt files deleted)
- Tasks: T1..T7 done; H1..H6 done. T8/T9/T10 deferred to PM closeout (Profile B compaction, `status.json` finalize, PR) — out of leaf-executor scope.
- Validation:
  - `cargo clippy --all -- -D warnings`: **pass** (clean)
  - `cargo test --all`: **pass** — 4375 passed / 0 failed
  - `cargo +nightly fmt --all --check`: **pass** (clean)
  - E2E test: **pass** — `narrative_compute_e2e_full_cycle_applies_side_effects` runs the deterministic `basic-combat` module (damage = atk−def) and verifies side effects read back from the DB: battle_report (`damage=20`, `defender_hp_after=100`), state_delta applied (defender HP 120→100, attacker unchanged), one timeline event persisted, zero new_key_blocks. Plus preset-load + capability-resolution + admission-gate tests (3 E2E tests total).
- Residuals closed (pending PM/QA update to `status.json` — leaf executor did not edit it):
  - `R-V161P3-PERF-001` — singleton `WasmEngine` at daemon boot, injected via `CapabilityRegistry::with_runtime_deps_and_wasm`.
  - `R-V161P3-PERF-002` — `Arc<RwLock<HashMap>>` `ModuleCache`; modules compile once during warmup and are reused (no recompile per `run()`).
  - `R-V161P3-CORR-002` (H1) — `create_new_key_blocks` re-asserts `world_id` before insert; cross-world injection rejected with `InputInvalid` (2 unit tests).
  - `R-V161P3-MAINT-001` (H2) — 5 unreferenced combat-engine prompt files removed.
  - `R-V161P3-MAINT-002` (H3) — stale test renamed `registry_has_twenty_six_builtins` → `registry_has_thirty_two_builtins`.
  - `R-V161P2-LOW-001` (H4) — `modules/README.md` §"Embedding a module" rewritten to compile-from-source / `build.rs`.
  - `R-V161P2-LOW-002` (H5) — `src/embedded.rs` doc comment + test note updated ("compiled by build.rs", not "committed .wasm blobs").
  - `R-V161P2-LOW-003` (H6) — `nexus-wasm-host` added to CI `rust-tests` matrix (orchestration-domain group).
- New residuals: none.
- Notes for PM/QA:
  - `with_runtime_deps` signature is unchanged → 11 existing callers (incl. tests) untouched; the daemon-wide singleton is opt-in via the new `with_runtime_deps_and_wasm`. `WasmEngine` init failure is non-fatal (falls back to the non-wasm registry; `narrative.compute` then returns `WorkerUnavailable`).
  - `cargo clippy --all-targets -- -D warnings` surfaces pre-existing pedantic lints in **untouched** files (`nexus-kb/src/key_block.rs:533` `doc_markdown`, `nexus-contracts/src/local/schedule/http.rs:236` `redundant_clone`). These are out of P-last scope (surgical-changes) and NOT part of the CI gate, which is `cargo clippy --all -- -D warnings` (passed). Flagging for a future hygiene sweep.
  - Residual closure + plan `Done` + Profile B compaction + `status.json` finalize are PM/QA authority (leaf executor did not edit `status.json`).
- Branch state: clean (7 commits on `feature/v1.61-daemon-integration-and-closeout`); not merged — awaiting QC + PM closeout.
