# 2026-06-27-v1.68-cli-relocation-and-product-surface-formalization

**Plan ID**: `2026-06-27-v1.68-cli-relocation-and-product-surface-formalization`
**Iteration**: V1.68
**Track**: A (relocate) + B (refs) + C (formalize) + D (path-prefix canonicalize)
**Owner**: `@fullstack-dev` (mechanical relocation + light docs)
**Working branch**: `iteration/v1.68` (single-stream; same as integration branch)
**Merge target**: `iteration/v1.68` → PR to `main` after sign-off
**Status**: Todo
**Depends on**: P-1 (compass Locked)
**Effort**: S
**wire_contracts_changed**: FALSE (pure relocation + docs; no schema/codegen/contract)

> Compass: [`v1.68-cli-to-apps-product-surface-unification-compass-v1.md`](../iterations/v1.68-cli-to-apps-product-surface-unification-compass-v1.md)

## Context

`nexus42` is the product (CLI + integrated daemon runtime); the other `crates/*` are its libraries. Today the product binary is buried among 15 library crates, and `apps/` — the dir contributors open for "what can I run" — lists only the TS clients and omits the CLI. V1.68 relocates `crates/nexus42` → `apps/nexus42` to unify the **"all product surfaces under `apps/`"** mental model, returns `crates/` to pure libraries, and pins the placement rule in docs. Low-risk: Cargo resolves intra-workspace deps by crate **name** → zero source changes; CI references crate names → CI-invisible.

## Pre-flight verification (PM done 2026-06-27 — recorded here for the implementer)

- [x] **`nexus42` is a leaf consumer** — no other crate has a real `[dependencies]` entry on it. `git grep -n 'nexus42' -- 'crates/*/Cargo.toml'` matches only `crates/nexus-home-layout/Cargo.toml` (a *description* string: `~/.nexus42/` home-dir path — unrelated to source location) and `crates/nexus-orchestration/Cargo.toml` (a *comment*). Neither is a dep. → **zero source changes** confirmed.
- [x] **No `path = "..."` fields reference the dir** — intra-workspace deps resolve by crate name.
- [x] **CI-invisible** — `.github/workflows/**` has 0 `crates/nexus42` path hits (uses `-p nexus42` / `--all`).
- [x] **Live-ref count is 12, not ~6** — 7 non-`.mstar` files + 5 live `knowledge/specs/*.md`. The ~978 other `.mstar/` hits are historical (left as-is per scope lock).
- [x] **0 open residuals** in `status.json` reference the path (all 25 hits are closed/resolved) → no residual path-swap needed.

## Tasks

### Track A — Relocate
- [ ] A1 `git mv crates/nexus42 apps/nexus42`

### Track B — Live reference updates (12 mechanical edits; pre-flight verified)
- [ ] B1 `Cargo.toml` workspace `members`: `crates/nexus42` → `apps/nexus42`
- [ ] B2 `tooling/check-schema-drift.sh`: swap `crates/nexus42` → `apps/nexus42` (~6 strings)
- [ ] B3 `AGENTS.md` (root): subdirectory index — `crates/nexus42/` row → `apps/nexus42/` (+ link target); note `apps/` is the polyglot product-surfaces dir
- [ ] B4 `docs/ARCHITECTURE.md`: binary-location table row (~line 117) → `apps/nexus42`; add product-surfaces sentence if absent
- [ ] B5 `apps/desktop/src-tauri/Cargo.toml`: update comment mirroring `crates/nexus42/src/config.rs` → `apps/nexus42/src/config.rs`
- [ ] B6 `apps/desktop/src-tauri/src/lib.rs`: doc comment (~line 76) `crates/nexus42/src/config.rs` → `apps/nexus42/src/config.rs`
- [ ] B7 `apps/web/src/pages/dialogs/create-work-dialog.test.tsx`: comment (~line 187) `crates/nexus42/src/commands/creator/bootstrap.rs` → `apps/nexus42/...`
- [ ] B8 `.mstar/knowledge/specs/acp-client-tech-spec.md`: `crates/nexus42` → `apps/nexus42`
- [ ] B9 `.mstar/knowledge/specs/concurrency.md`: same
- [ ] B10 `.mstar/knowledge/specs/desktop-shell.md`: same
- [ ] B11 `.mstar/knowledge/specs/novel-writing/workflow-profile.md`: same
- [ ] B12 `.mstar/knowledge/specs/orchestration-engine.md`: same
- [ ] B13 `apps/nexus42/AGENTS.md` (moved with the dir) — review content still accurate (it uses crate names, no self-path-ref to fix)

### Track C — Mental-model formalization
- [ ] C1 Create `apps/AGENTS.md`: declare `apps/` = polyglot product-surfaces dir; entries table (`nexus42` producer / `desktop` consumer / `web` consumer); **durable placement rule** (new product surface any language → `apps/`; Rust library → `crates/`); producer/consumer wire-boundary note
- [ ] C2 Root `README.md`: replace single-row Components table with a **Monorepo Layout** section (`apps/` products + `crates/` Rust libs + `packages/` npm contracts + `modules/` domain content + `tooling/` codegen + `schemas/` wire truth)
- [ ] C3 Review `apps/desktop/AGENTS.md` + `apps/web` docs for consistency with the new `apps/AGENTS.md` framing (light touch; do not rewrite)

### Track D — Scope boundary (what is explicitly NOT edited)
- [ ] D1 **Confirm in the commit that historical records are intentionally untouched**: ~978 files (26 past compasses + ~532 past plans + ~420 past QC reports) + all `.mstar/archived/**` + the 25 closed/resolved `status.json` residuals. These are point-in-time audit records; rewriting them falsifies history. **Only the 12 live files in Track B get path edits.**

### Verify & gate
- [ ] V1 `cargo build --all` (whole workspace builds at new location — proves name-based resolution with zero source edits)
- [ ] V2 `cargo +nightly-2026-06-26 fmt --all --check` (pinned nightly per root AGENTS.md)
- [ ] V3 `cargo clippy --all -- -D warnings`
- [ ] V4 `cargo test -p nexus42` (CLI crate tests pass at new location)
- [ ] V5 `bash tooling/check-schema-drift.sh` runs clean (proves B2 path fix)
- [ ] V6 `pnpm -r run build` smoke (proves `apps/*` pnpm workspace unaffected — `apps/nexus42` has no package.json, ignored)
- [ ] V7 The **12 live files** (Track B) each return **zero** remaining `crates/nexus42` hits: `git grep -n 'crates/nexus42' -- Cargo.toml tooling/check-schema-drift.sh AGENTS.md docs/ARCHITECTURE.md apps/desktop/src-tauri/Cargo.toml apps/desktop/src-tauri/src/lib.rs apps/web/src/pages/dialogs/create-work-dialog.test.tsx .mstar/knowledge/specs/acp-client-tech-spec.md .mstar/knowledge/specs/concurrency.md .mstar/knowledge/specs/desktop-shell.md .mstar/knowledge/specs/novel-writing/workflow-profile.md .mstar/knowledge/specs/orchestration-engine.md` → expect no output. (Historical ~978 hits elsewhere are expected and intentional — see Track D.)
- [ ] V8 Commit on `iteration/v1.68`; push; open PR `iteration/v1.68` → `main` (per merge discipline)

## Acceptance criteria

1. `apps/nexus42` exists; `crates/nexus42` does not.
2. `crates/` contains only library crates (no binary).
3. `cargo build --all` + `cargo test -p nexus42` + `cargo clippy --all -D warnings` + fmt check all green, with **zero source-code edits** (only relocation + config/doc/script/comments).
4. `apps/AGENTS.md` exists and states the polyglot product-surfaces model + durable placement rule.
5. Root `README.md` has a Monorepo Layout section listing all product surfaces (incl. desktop + web, which it previously omitted).
6. The 12 live files (Track B) have zero remaining `crates/nexus42` hits (V7); historical records (~978) intentionally retained.
7. `wire_contracts_changed: FALSE` — no `schemas/`, codegen, or `@42ch/nexus-contracts` change.

## Out of scope

Crate rename; source changes; functional/normative spec edits; `modules/`/`packages/`/`tooling`/`schemas` reorg; `archived/` rewrite; canvas/body-editor/distribution work (V1.69+).

## Notes

- **Why zero source changes**: intra-workspace Cargo deps use the crate *name* (`nexus42`), resolved via the workspace; the `members` list is the only Cargo.toml that tracks the *path*. Moving the dir + updating that one line is sufficient.
- **Why pnpm is safe**: `pnpm-workspace.yaml` globs `apps/*`, but pnpm only treats dirs containing `package.json` as packages; `apps/nexus42` (pure Rust) has none → ignored.
- **Why `archived/` is untouched**: those records (legacy `nexus42d`, closed residuals, historical design docs) are point-in-time snapshots; rewriting their paths falsifies the audit trail. They age out naturally.
