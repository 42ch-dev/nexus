---
plan_id: 2026-06-24-v1.63-essay-preset-and-rubric
title: V1.63 P2 — Essay essay-writing Preset + 4-dim Rubric + Completion Detection + Optional KB
iteration: V1.63
slot: P2
status: Todo
effort: M
owner: "@fullstack-dev"
review: Tri-review + mid-QA
wave: 2
dependency: P0 merged to iteration/v1.63
working_branch: feature/v1.63-essay-preset-and-rubric
merge_target: iteration/v1.63
---

# P2: Essay `essay-writing` Preset + 4-dim Rubric + Completion Detection + Optional KB

Track A depth: author the `essay-writing` embedded preset (intake → outline → draft → revise → finalize stage chain) with an essay-specific 4-dimension quality rubric (thesis clarity / evidence support / coherence / ending takeaway). Wire `creator run essay-writing` CLI entry. Optional KB extraction via existing `nexus.llm.extract` capability (single character/location context block per spec §6). This brings essay to Depth 3.5 parity with game-bible/script.

## Tasks

- [ ] T1: Author `embedded-presets/essay-writing/preset.yaml` + prompt files for the 5-stage chain (intake → outline → draft → revise → finalize). Follow V1.60 script-writing preset structure as the closest analogue (single-artifact, non-chaptered).
- [ ] T2: Implement essay 4-dimension quality rubric (`essay-quality-check` capability or preset gate): thesis clarity / evidence support / coherence / ending takeaway. Decision: blocking with `--force-gates` override (§5 item #1) — calibrate pass/fail thresholds based on evidence.
- [ ] T3: Wire completion detection: finalize stage checks rubric pass (or override) → writes `Drafts/draft.md` frontmatter `status: finalized` → triggers works.status = completed (P0 T3 path).
- [ ] T4: CLI: `creator run essay-writing` entry point (parity with `creator run script-writing` / `creator run game-bible-design`).
- [ ] T5: Optional KB extraction: on finalize, invoke `nexus.llm.extract` for single character/location context block (spec §6). Bounded; no per-Work Worldbuilding subtree.
- [ ] T6: Tests: preset validation; rubric pass/fail; completion detection; `--force-gates` override; KB extraction bounded.
- [ ] T7: Spec amendments: `orchestration-engine.md` (essay-writing preset + rubric), `cli-spec.md` (`creator run essay-writing`), `creator-workflow.md` (FL-E stage model entry).

## Done when

- `essay-writing` preset passes embedded-preset validation (parity with script-writing V1.60 gate).
- 4-dim rubric produces pass/fail deterministically on test essays.
- `creator run essay-writing` executes the full chain end-to-end on a scaffolded essay Work.
- Completion → works.status = completed.
- Optional KB extraction is bounded (max 1 context block) and reuses existing capability.
- `cargo test -p nexus-orchestration` + `cargo clippy -p nexus-orchestration -- -D warnings` pass.

## Open design items

- Rubric advisory vs blocking (§5 item #1): lean blocking with override; confirm in T2 based on calibration evidence.
