# P2 — Canvas Graph Scale + Viewport Reliability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `mstar-sdd`. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Invest in canvas graph scale and viewport reliability — preserve pan/zoom across graph↔list toggle, optimize selection sync, add real React Flow integration test coverage, and sweep the 10 non-blocking V1.108 residuals.

**Architecture:** A viewport-preservation hook (`useCanvasViewport`) in `canvas-shell.tsx` caches and restores RF viewport state across alt-view toggles. Selection sync is memoized to fire only on selection changes (not position changes). A real RF integration test harness replaces mocked CanvasShell in orchestrator tests. V1.108 residuals get targeted fixes or tracked deferrals.

**Tech Stack:** `@xyflow/react`, TypeScript, Vitest + RTL + `@xyflow/react` test utilities.

**Execution:** mstar-sdd

**Primary spec:** `.mstar/iterations/v1.109/specs/canvas-graph-scale.md` (§5.1 product-complete — FB-GS-000..003 + residual disposition)

## Global Constraints

- **Studio-first invariant (carry-forward)**
- **Wire boundary:** UI/test only — `wire_contracts_changed: false`
- **DESIGN.md SSOT:** consume existing tokens; no new tokens
- **Transport invariant:** `NexusClient` only
- **Accessibility:** viewport preservation must not break keyboard focus paths
- **Test discipline:** real RF integration tests, not mocked CanvasShell (R-V1108P0QC3-S003)

## Architectural context

**V1.108 residuals to address (10 findings, all post-V1.108)** — product disposition SSOT in primary spec:

| ID | Issue | Plan action |
|----|-------|-------------|
| R-V1108P0QC3-S002 | Graph↔list toggle unmounts CanvasShell, losing RF viewport state | **Fix** — viewport preservation hook (FB-GS-000) |
| R-V1108P0QC3-W001 | Selection-sync useEffect fires on every node interaction | **Fix** — memoize selection comparator (FB-GS-001) |
| R-V1108P0QC3-S003 | CanvasShell fully mocked in orchestrator tests | **Fix** — real RF integration test harness (FB-GS-002) |
| R-V1108P0QC1-S001 | Consider extracting useOutlineCanvasGraph() hook for Scene/Beat roadmap | **Fixed in P0** (§5.2 Q5 — C2 complexity justifies extract now; P2 consumes the extracted hook for FB-GS-001) |
| R-V1108P0QC1-M001 | Alt view is read-only (no sort controls) | **Defer** — UX enhancement; re-target post-V1.109 |
| R-V1108P1QC1-S001 | CREATOR_NAV/ORCHESTRATOR_NAV duplicated | **Fix** — consolidate constant (small) |
| R-V1108P1QC1-S002 | Studio node chrome mirror should import P0 extract | **Defer** — depends on extract; track |
| R-V1108P1QC2-S001 | Verify error helper doesn't distinguish transport vs no-match | **Fix** — improve helper; Voice: no jargon (see primary spec) |
| R-V1108P1QC3-W001 | AgentPicker re-renders entire card grid on verifyStatus | **Fix** — memoize card grid (small) |
| R-V1108P1QC3-W002 | useVerifyAgent mutation lacks unmount cancellation | **Fix** — AbortController (small) |

**Scope cap:** FB-GS-003 = targeted fixes for the fix-oriented set above; **2** items deferred with updated residual tracking. The `useOutlineCanvasGraph` extract (R-V1108P0QC1-S001) is **owned by P0** per §5.2 Q5 — not in P2's sweep. Anything discovered larger gets a new residual, not scope creep.

**Viewport product bar (FB-GS-000):** preserve `{ x, y, zoom }` for Outline surface session across graph↔list; hard navigation may clear; no disk persistence required.

**Selection-memo target (FB-GS-001) — §5.2 coordination protocol:** The selection overfire lives in the outline orchestrator's `useEffect`, NOT in `use-outline-data.ts`. After P0's `useOutlineCanvasGraph()` extract (P0 Task 1), the selection-sync effect moves into the extracted hook. **FB-GS-001's target file is `apps/web/src/components/canvas/outline-canvas/use-outline-canvas-graph.ts`** (the extracted hook), not `use-outline-data.ts`. If P2 runs parallel to P0, P2 rebases on P0's extract.

---

### Task 1: Viewport preservation hook (FB-GS-000)

**Files:**
- Create: `apps/web/src/components/canvas/use-canvas-viewport.ts`
- Modify: `apps/web/src/components/canvas/canvas-shell.tsx` (integrate hook)
- Test: `apps/web/src/components/canvas/__tests__/use-canvas-viewport.test.ts`

**Interfaces:**
- Consumes: RF `onMove`/`onViewportChange` events; alt-view toggle state
- Produces: cached viewport `{ x, y, zoom }` restored on re-mount

- [ ] **Step 1: Write failing test** — toggling graph→list→graph preserves pan/zoom viewport

- [ ] **Step 2: Run test — expect FAIL**

- [ ] **Step 3: Implement viewport hook** — cache on viewport change; restore on mount if cached

- [ ] **Step 4: Run test — expect PASS**

- [ ] **Step 5: Commit**

### Task 2: Selection-sync optimization (FB-GS-001)

**§5.2 coordination:** Target file is `use-outline-canvas-graph.ts` (P0's extract), NOT `use-outline-data.ts`. The selection overfire lives in the selection-sync effect inside the extracted hook.

**Files:**
- Modify: `apps/web/src/components/canvas/outline-canvas/use-outline-canvas-graph.ts` (the P0-extracted hook — memoize selection comparator inside the selection-sync effect)
- Test: selection effect does not fire on position change

**Interfaces:**
- Consumes: RF `onSelectionChange` (not `onNodesChange`)
- Produces: selection state that only updates when selected node ids change

- [ ] **Step 1: Write failing test** — dragging a node (position change only) does not trigger the selection-sync effect inside `useOutlineCanvasGraph`; changing selected node id still drives inspector

- [ ] **Step 2: Run test — expect FAIL**

- [ ] **Step 3: Implement memoized comparator** — compare selected id sets, not full node change; guard the selection-sync effect

- [ ] **Step 4: Run test — expect PASS**

- [ ] **Step 5: Commit**

### Task 3: Real RF integration test harness (FB-GS-002)

**Files:**
- Create: `apps/web/src/components/canvas/__tests__/rf-integration-harness.tsx`
- Modify: `apps/web/src/components/canvas/outline-canvas/__tests__/outline-canvas.test.tsx` (replace mocked CanvasShell)
- Test: integration tests

**Interfaces:**
- Produces: test wrapper that renders real `<ReactFlow>` with test nodes/edges

- [ ] **Step 1: Write failing integration test** — outline canvas renders real RF; selection drives inspector; node interaction works without mock

- [ ] **Step 2: Run test — expect FAIL**

- [ ] **Step 3: Implement RF integration harness** — real ReactFlowProvider + test data; replace mocked CanvasShell

- [ ] **Step 4: Run test — expect PASS**

- [ ] **Step 5: Commit**

### Task 4: V1.108 residual sweep (FB-GS-003)

**Files (targeted fixes):**
- Modify: settings/surfaces constant consolidation (R-V1108P1QC1-S001)
- Modify: verify error helper (R-V1108P1QC2-S001)
- Modify: AgentPicker card grid memo (R-V1108P1QC3-W001)
- Modify: useVerifyAgent AbortController (R-V1108P1QC3-W002)
- Update: `status.json` residual_findings — close fixed items; update deferred items

- [ ] **Step 1: Fix CREATOR_NAV/ORCHESTRATOR_NAV duplication**

- [ ] **Step 2: Fix verify error helper transport vs no-match**

- [ ] **Step 3: Fix AgentPicker card grid memo**

- [ ] **Step 4: Fix useVerifyAgent unmount cancellation**

- [ ] **Step 5: Update status.json residuals** — close fixed; defer R-V1108P0QC1-M001 (sort controls) + R-V1108P1QC1-S002 (studio import — depends on P0's extract which lands in P0 Task 1) with updated target. **R-V1108P0QC1-S001 (extract) is closed by P0, not P2** — do not double-close here.

- [ ] **Step 6: Commit**

## Voice & Content (product-locked)

| Element | Copy |
|---------|------|
| Alt toggle | **Show list view** / **Show graph** (unchanged) |
| Verify success | *Agent responded successfully.* |
| Verify transport fail | *Could not reach this agent. Check the command and try again.* |
| Verify no-match (if distinguishable) | *No matching agent for this command. Check the command and try again.* |

## FB acceptance (product-expanded)

| ID | Product bar (summary) | Tasks |
|----|----------------------|-------|
| FB-GS-000 | Outline pan/zoom restored after list↔graph | T1 |
| FB-GS-001 | Position drag ≠ selection sync; id change still binds | T2 |
| FB-GS-002 | Real RF harness + ≥1 Outline integration test | T3 |
| FB-GS-003 | Disposition table applied in `status.json` | T4 |

## Plan self-review (architect-finalized)

1. **Spec coverage:** FB-GS-000..003 all map to tasks; residual sweep covers fix-oriented + 2 deferred
2. **Placeholder scan:** no product TBD
3. **Type consistency:** viewport hook interface consistent across shell + test
4. **Wire:** hard `wire_contracts_changed: false`
5. **Cross-plan coordination (§5.2):** R-V1108P0QC1-S001 (extract) owned by P0; FB-GS-001 targets the P0-extracted `use-outline-canvas-graph.ts`, not `use-outline-data.ts`; if parallel, P2 rebases on P0's extract
## SDD runtime (ephemeral)

When using `mstar-sdd`, artifacts live under `{SDD_DIR}` = `.mstar/sdd/2026-07-11-v1.109-canvas-graph-scale/`. Do not duplicate briefs/reports in this file.
