# V1.47 Serial Completion Hardening — Plan

**Plan ID**: `2026-06-15-v1.47-serial-completion-hardening`  
**Title**: P2 — §4.5.7 tests #1–#3 + completion edge disposition  
**Created**: 2026-06-15  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.47-serial-completion-hardening`  
**Merge target**: `iteration/v1.47`  
**Depends on**: `2026-06-15-v1.47-gate-remediation-audit` (merged)  
**Iteration compass**: [v1.47-novel-quality-loop-closure-delivery-compass-v1.md](../iterations/v1.47-novel-quality-loop-closure-delivery-compass-v1.md)  
**Primary specs**:

- [novel-writing/workflow-profile.md](../knowledge/specs/novel-writing/workflow-profile.md) §4.5.7, §6

**Target residuals**: `R-V138P1-01` (baseline verify → likely close).

---

## 1. Problem Statement

Multi-chapter acceptance tests #1–#3 in §4.5.7 remain unimplemented. `R-V138P1-01` may already be addressed by V1.39 P5 `reject_produce_when_novel_complete` but remains open in `status.json`.

---

## 2. Goals

1. Implement hermetic tests for §4.5.7 items **#1–#3** (chapter selection, `current_chapter` transitions, novel completion).
2. Baseline-verify `reject_produce_when_novel_complete`; formal close R-V138P1-01 if guard is sufficient.
3. Confirm completed Works do not enqueue empty produce schedules via auto-chain edge cases.

---

## 3. Non-goals

- §4.5.7 tests #4 (reconcile) and #5 (resume) — defer V1.48.
- Multi-volume re-hardening (V1.44 shipped).
- Duplicating V1.39 P5 guard implementation unless baseline finds gaps.

---

## 4. Acceptance Criteria

1. Three new tests (or one parameterized suite) cover §4.5.7 #1–#3 per spec semantics.
2. R-V138P1-01 closed with evidence or narrowed scope documented in residual archive.
3. No spurious `novel-writing` schedule when `next_chapter` is `None` and Work is complete.

---

## 5. Tasks

- [x] T1: Baseline review `reject_produce_when_novel_complete` + auto-chain completion paths.
- [x] T2: Test #1 chapter selection (`next_chapter`).
- [x] T3: Test #2 `current_chapter` finalize-only advance.
- [x] T4: Test #3 completion criteria (§6.1).
- [x] T5: Residual disposition R-V138P1-01.

### T1 Baseline Review Memo

**Guard: `reject_produce_when_novel_complete`** (`crates/nexus42/src/commands/creator/run.rs:502`)

- V1.39 P5 (commit `02948f59`) guard returns `CliError::Other` with actionable
  NOVEL_COMPLETE message when `target_stage == "produce" && next_chapter.is_none()`.
- Called at `run.rs:716` **before** `build_schedule_for_stage` — so no empty-chapter
  `novel-writing` schedule can be created for a completed Work via the CLI
  `stage_advance` path.
- 3 existing unit tests (`reject_produce_when_novel_complete_errors_on_none_next_chapter`,
  `..._allows_chapter_present`, `..._skips_other_stages`) at `run.rs:948–1000`.
- **Verdict: guard is sufficient** for R-V138P1-01 closure.

**Auto-chain completion paths** (`crates/nexus-orchestration/src/auto_chain.rs`)

- `evaluate_next_step` returns `NoAction` when `work.status == "completed"` (line 273).
- `evaluate_after_persist` returns `WorkComplete` when `current_chapter >= total_chapters`.
- `evaluate_after_persist_volume_aware` queries DB for next active chapter; returns
  `WorkComplete` when `next_chapter_volume_aware` returns `None`.
- No path enqueues a `novel-writing` produce schedule for a completed Work.

**§4.5.7 existing coverage:**

| Test | Status |
|------|--------|
| #1 chapter selection (`next_chapter`) | Covered by T10.1–T10.3, T10.8 in `work_chapters.rs` (scattered, pre-§4.5.7-tagged). T2 adds a canonical §4.5.7 test consolidating the 3-chapter varied-statuses scenario. |
| #2 `current_chapter` transitions | **Not directly covered.** The invariant is enforced in `novel_chapter_transition.rs:234` (`if inp.to_status == "finalized"`) but no test verifies that `current_chapter` only advances on finalize. T3 adds this. |
| #3 completion criteria (§6.1) | Covered by T10.4–T10.7 + F-002 in `work_chapters.rs` (negative cases). T4 adds a canonical §4.5.7 test asserting all three §6.1 conditions in one flow. |

**AC #3 (No spurious schedule when next_chapter is None):** Already enforced by the
guard; V1.39 P5 tests confirm it. T2–T4 do not change runtime behavior.

### T5 Residual Disposition R-V138P1-01

**Verdict: guard sufficient — close R-V138P1-01.**

Evidence:
- Guard `reject_produce_when_novel_complete` at `run.rs:502` + 3 unit tests (commit `02948f59`).
- §4.5.7 #1–#3 tests added by this plan (T2–T4) further exercise the chapter-selection
  and completion paths that converge on `next_chapter=None`.
- No gaps found in the guard or auto-chain completion paths.

PM to mirror in `status.json` `residual_findings["2026-06-08-v1.38-novel-writing-parameterization"]`
→ set `lifecycle: "resolved"` + `resolution.commit` + `resolution.plan_id` at merge time.

---

## 6. Verification

```bash
cargo test -p nexus-local-db -p nexus42 -- next_chapter 2>&1 | tail -25
cargo test -p nexus-orchestration -- auto_chain 2>&1 | tail -25
cargo test -p nexus-orchestration --lib -- current_chapter 2>&1 | tail -10
cargo test -p nexus-orchestration --lib -- completion 2>&1 | tail -10
```
