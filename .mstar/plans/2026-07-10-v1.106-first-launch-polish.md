# P1 — First-Launch Polish

**Goal:** Align first-launch wizard chrome with Settings Agent experience and reduce portrait empty space on Workspace/Done; track V1.105 residual seeds.

**Architecture:** Shared `AgentPicker` remains app-owned (`apps/web/src/components/setup/agent-picker.tsx`). Studio wizard chrome replaces stub `AgentListBody` with `@web-setup/agent-picker`. `density?: 'default' | 'compact'` on the same component (`default` when omitted; wizard may pass `compact` only) — no second picker. `TopStepIndicator` SSOT: single module at `top-step-indicator.tsx`; Studio imports `@web-setup/top-step-indicator` and removes inline duplicate (closes `R-V1105P2-001`). Portrait shell height (V1.105 H1) stays; content centering only.

**Interfaces:**
- Studio: `setup-wizard-chrome-fixtures.tsx`, `@web-setup/agent-picker`, `@web-setup/top-step-indicator`
- App: `setup-step-agent.tsx`, `setup-step-workspace.tsx`, `setup-step-done.tsx`, `top-step-indicator.tsx`
- Shared: `agent-picker.tsx` — `density?: 'default' | 'compact'`

**Implementer SSOT:** `.mstar/iterations/v1.106/specs/first-launch-polish.md`

**Tech Stack:** Design Studio, apps/web, Vitest; no wire/schema.

**Execution:** mstar-sdd

**Tier:** **Must (P1)**

## Prepare Gates

- specify: **done** (§5.1 PM — iteration spec product-complete)
- clarify: **done** (grill-me locked)
- plan: **done** (tasks + acceptance criteria product-complete)
- architect: **done** (§5.2 — AgentPicker density API, TopStepIndicator SSOT, portrait centering)
- writing: **done** (§5.3 — Voice & Content, DESIGN SP-002 prose, FB copy tables)

## Product Framing

**User value:** Choosing an agent in first-launch feels the same as Settings; Done/Workspace no longer look like empty tall cards.

**Author journey:** After DaemonReadySplash → Agent (real AgentPicker) → Workspace (centered) → Done (celebratory, centered, short copy) → Open Nexus.

**Iteration gate:** Must for V1.106 close.

**Non-goals:** Abandoning portrait max-height; Radix Select; package-promoting AgentPicker; Settings Advanced nav (P2 FB-005).

## Clarify (locked from grill-me)

| Topic | Decision |
|-------|----------|
| Agent UI | Same `AgentPicker` as Settings; compact density OK |
| Portrait | Keep H1 height; center Workspace/Done body; CTA bottom-anchored |
| Done copy | Heading **You're ready 🎉** + one-line sentence-case helper |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria

1. **FB-003:** Studio wizard Agent chrome renders `data-testid="agent-picker"` with real shared `AgentPicker` — not stub name-only rows.
2. Settings and wizard import the same `agent-picker.tsx` module; optional compact density does not fork a second component.
3. **FB-004:** Studio Workspace/Done fixtures show centered body content; Done heading **You're ready 🎉** with one-line sentence-case helper; CTAs bottom-anchored.
4. App steps match accepted Studio after visual accept (studio-first).
5. V1.105 residual seeds in table below closed or re-targeted with evidence.
6. Prefer no schemas change (`wire_contracts_changed: false`).

## V1.105 residual seeds

| ID | Title | Target action |
|----|-------|---------------|
| `R-V1105P2-001` | Dual TopStepIndicator sources (Studio vs App) | Consolidate or document SSOT |
| `R-V1105P2-002` | No App overflow integration test | Add or accept with rationale |
| `R-V1105P0-003`..`005`, `R-V1105P1-001` | DaemonLaunchGate / re-run test edges | Close or re-target |

## Global Constraints

- Primary spec: `.mstar/iterations/v1.106/specs/first-launch-polish.md`
- Compass: `.mstar/iterations/v1.106-delivery-compass.md`
- Studio-first: **required** for FB-003/004 chrome
- AgentPicker: `@web-setup/agent-picker`; `density` default `'default'` — Settings omits prop
- TopStepIndicator: `@web-setup/top-step-indicator` — no duplicate Studio implementation
- Related: V1.105 portrait shell + wizard IA masters
- `wire_contracts_changed: false`

---

### Task 1: Wizard AgentPicker parity (FB-V1106-003)

**Files:**
- Modify: `apps/design-studio/src/fixtures/setup-wizard-chrome-fixtures.tsx`
- Modify (if density): `apps/web/src/components/setup/agent-picker.tsx`
- Modify: App wizard only if density wiring needed (`setup-step-agent.tsx`)
- Test: Studio + App AgentPicker / wizard tests

- [x] Replace Studio stub agent list with `@web-setup/agent-picker`; optional `density="compact"` for wizard portrait only.
- [x] Consolidate `TopStepIndicator`: Studio imports `@web-setup/top-step-indicator`; remove inline duplicate.
- [x] Visual accept Studio → App claim.

### Task 2: Workspace / Done layout (FB-V1106-004)

**Files:**
- Modify: `apps/design-studio/src/fixtures/setup-wizard-chrome-fixtures.tsx`
- Modify: `apps/web/src/pages/setup-step-workspace.tsx`
- Modify: `apps/web/src/pages/setup-step-done.tsx`
- Test: Studio chrome + App step tests

- [ ] Center Workspace form cluster in scroll body; CTA stays bottom-anchored.
- [ ] Done: centered stack + celebratory emoji + shortened sentence-case helper.
- [ ] Portrait max-height tokens unchanged unless a later FB promotes resize.

### Task 3: V1.105 residual seed pass

**Files:**
- As needed for TopStepIndicator consolidation / overflow test / gate test edges
- Update: `.mstar/status.json` residual rows when closed

- [x] Address or re-target listed residual seeds with evidence.

## Review Gate Summary

**QC mode:** full tri-review (N=3) · **Verdict:** Approve with residuals (no open Criticals/Warnings after fix)
**Branch:** `plan/v1.106-p1-first-launch-polish` · **Range:** `f95191ea..1c45cd8b`
**Reports:** `.mstar/sdd/2026-07-10-v1.106-first-launch-polish/review/qc1.md`, `qc2.md`, `qc3.md`, `qc-consolidated.md`

- T1 (FB-003): AgentPicker parity + density API + TopStepIndicator SSOT — Approved after duplicate-import fix.
- T2 (FB-004): Workspace/Done centered layout + celebratory Done copy — Approved clean.
- T3: V1.105 residual seeds — 6 residuals closed (R-V1105P2-001/002, R-V1105P0-003/004/005, R-V1105P1-001).
- QC fix: unused `cn` import removed (design-studio build passes).
- Tests: 606/606 web, 87/87 design-studio — all green.
- **No open Criticals/Warnings.** Cleared for QA gate (mandatory).

## QA Gate Summary

**QA gate:** mandatory · **QA mode:** acceptance-only · **Verdict:** Accept
**Report:** `.mstar/sdd/2026-07-10-v1.106-first-launch-polish/review/qa-acceptance.md`
All FB-003/004 acceptance criteria + 6 V1.105 residual closures verified. No new residuals. No open Criticals/Warnings.
