# P2 — VI aesthetic retune

> **Iteration compass:** `.mstar/iterations/v1.132/delivery-compass.md`  
> **Spec:** `.mstar/iterations/v1.132/specs/vi-aesthetic-retune.md`  
> **SSOT status:** `.mstar/status.json`

**Goal:** Chronos VI readability — compact timeline marks, plain vs `*-square` assets, inset app icon, Setup selection clarity, theme-aware primary Button (light shell ≠ neon cyan + deep ink).

**Architecture lock (2026-07-22):** Studio-first. `@42ch/nexus-ui` owns the theme-split `Button` and shared tokens; `apps/design-studio` proves light/dark visuals; `apps/web` consumes accepted primitives; `apps/desktop` consumes square assets for platform composition. Plain marks and `*-square` plates are separate assets, square composition owns transparent inset margins, and compact timeline scale is shared across Brand/titlebar/icon usage. Error blocks must consume Button variants rather than add local overrides.

**Tech Stack:** `@42ch/nexus-ui`, `apps/design-studio`, `apps/web`, `apps/desktop` icons.

**Execution:** mstar-sdd

### Wire contracts

- **Locked verdict:** `wire_contracts_changed: false`

## Prepare gates

- specify: [done]
- clarify: [done]
- plan: [locked] — architect lock 2026-07-22

### Architecture lock block

- Button theme behavior is centralized in the `@42ch/nexus-ui` SSOT; light-shell and dark-shell variants are validated in Studio before App wiring.
- Asset naming and inset composition are owned by the asset/compose layer, preventing plain/square swaps and halo fixes in individual consumers.
- The VI ledger remains the durable triage boundary for further notes; unresolved Must items block visual acceptance, while wire contracts remain unchanged.

## VI dogfood ledger (seeded)

| ID | Severity | Note | Track | Status | Evidence |
|----|----------|------|-------|--------|----------|
| VI-001 | Must | Setup agent selection competing signals | Interactive | **Closed** | T3 `4e1feb3a`; `agent-picker.test.tsx` (2px ring); Studio VI-001 fixture |
| VI-002 | Must | Light shell primary Button / Error Retry neon+deep ink | Interactive | **Closed** | T3 `4e1feb3a`; `button.test.tsx` theme-split; `TransportErrorBlock` consumes `Button` |
| VI-003 | Must | Titlebar/hero mark −30%–50% compact timeline | Brand | **Closed** | T2 `8bd81ca3`; `logoCompactMarkHeightPx`=14; `NexusInkLogo`, Brand gallery |
| VI-004 | Must | App icon plate edge halo | Brand / compose | **Closed** | T4 `df8ae183`; compose 12% inset; preview `6223d10c…`. Dock human: `R-VI-003` open (QA) |
| VI-005 | Must | Plain vs `*-square` asset split | Brand | **Closed** | T2 `8bd81ca3`; `logoVariants` + `logoSquareVariants`; Studio VI-005 fixture |

## Intent gate

| Item | Statement |
| --- | --- |
| Real goal | Light + dark Chronos UI feels intentional |
| User value | VI reads as intentional across themes; no neon-on-ink primary in light mode; one clear Setup selection affordance |
| Success criteria | Compass AC-3..AC-5d |
| Non-goals | Multi-theme switcher; wholesale DESIGN rewrite |

## Tasks

- [x] **T1 · Must:** Studio Brand + Button + AgentPicker + TransportError fixtures (light+dark) for target states → **DoD:** visual acceptance before App wiring
- [x] **T2 · Must:** Asset split plain vs `*-square` + compact mark SSOT scale → **DoD:** Brand hero + titlebar match AC-4
- [x] **T3 · Must:** Theme-aware primary Button + Setup selection retune → **DoD:** AC-5b, AC-5c; no one-off error hacks
- [x] **T4 · Must:** Icon compose inset + regenerate → **DoD:** AC-5; Dock/preview evidence
- [x] **T5 · Must:** DESIGN.md / tokens + ledger triage → **DoD:** AC-5d

## Working branch

`plan/v1.132-p2-vi-aesthetic-retune` → `iteration/v1.132`
