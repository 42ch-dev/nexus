# Plan — V1.73 P-1 Prepare

**plan_id**: `2026-06-28-v1.73-compass-and-plan`
**Iteration**: V1.73 — [Compass](../iterations/v1.73-canvas-world-kb-beta-and-outline-hardening-compass-v1.md)
**Status**: Done (Prepare complete — PM lock)
**Owner**: `@project-manager`
**Effort**: S
**Branch policy**: direct commit to `iteration/v1.73`
**Depends on**: —

## Goal

Lock V1.73 Prepare: compass on disk, plan stubs registered, `status.json` updated, integration branch created, predecessor retired, Phase 2b review chain dispatched and integrated, PM lock.

## Tasks

- [x] T1 — Write V1.73 compass (`iterations/v1.73-canvas-world-kb-beta-and-outline-hardening-compass-v1.md`).
- [x] T2 — Write P-1, P0, P1, P-last plan stubs under `plans/`.
- [x] T3 — Register 4 plans + integration branch + residual targets + gate pointers in `status.json`.
- [x] T4 — Create integration branch `iteration/v1.73` from `origin/main @ 87ab75bb`; retire `iteration/v1.72` (set `metadata.integration_branch_retired: true`, `metadata.last_integration_branch: "iteration/v1.72"`).
- [x] T5 — Phase 2b `@product-manager` review (compass scope, `web-ui.md` V1.73 stage amend, World KB conflict modal UX copy, non-spatial alternate view product design). PM integrates edits.
- [x] T6 — Phase 2b `@architect` lock: storage/OCC mechanism (per-row OCC on existing `kb_key_blocks.revision` — no migration) + route signatures (4 paths + DTO field names) + validation rules + `local-api-surface-conventions.md` World KB patch-route extension + DESIGN.md 17 `canvas-worldkb-*` token-name interface + `wire_contracts_changed: TRUE` countersign + `outline-canvas.tsx` split approach (B5 ≤250 lines) + `canvas-strategy-surface.md` §3.3 surface 3 Draft→Shipped β promotion wording. PM integrates edits.
- [x] T7 — Phase 2c `@writing-specialist` review (7 edits: Status line accuracy + `base_version`→`expected_version` consistency across 6 locations + §5 residual math clarification). PM integrates edits.
- [x] T8 — PM lock compass; set Pre-implement gate `GO`; update `iterations/README.md` V1.73 row.

## Acceptance

- Compass on disk with §0 grill decisions recorded.
- 4 plan stubs on disk.
- `status.json` has 4 plan rows + `metadata.integration_branch: "iteration/v1.73"` + residual targets.
- `iteration/v1.73` branch exists; `iteration/v1.72` retired in metadata.
- Phase 2b/2c review notes integrated into compass (visible edit log in compass Status line).
- Pre-implement gate `GO`.

## Notes

- This plan is PM-whitelist (compass/status/index maintenance). No specialist artifact body authored in PM thread — Phase 2b edits delegated to `@product-manager` / `@architect`; PM only integrates their returned edits into the compass.
- Plan stubs are intentionally concise; the compass carries full scope. P0/P1 implement tasks are expanded at implement dispatch.
