# 2026-07-04-v1.89-closure (P-last)

**Iteration**: V1.89 · **Plan**: P-last (Closure) · **Status**: Todo · **Owner**: `@project-manager`
**Compass**: [v1.89-deeper-manuscript-reading-delivery-compass-v1.md §3 + §5](../iterations/v1.89-deeper-manuscript-reading-delivery-compass-v1.md)
**Depends on**: P0 + P1 merged to `iteration/v1.89` + QC tri-review Approve + QA Pass.

## Goal

Close V1.89: update the deferred-features tracker (BL-11 MVP shipped; profile chrome remains deferred), run the compound round, Profile B compaction, refresh indexes, and open the PR to `main`.

## Scope

### T1 — Tracker hygiene

| Action | Detail |
|---|----|
| BL-11 row | Move from `deferred-features-cross-version-tracker.md` §2.3 to `archived/shipped-features-tracker.md` §1 with note: "Shipped V1.89 — persisted reading progress + annotations/highlights (MVP); profile-specific reading chrome remains deferred." |
| Profile chrome | Ensure BL-11 profile-chrome portion remains an open/backlog row if not already present. Do **not** close the profile-chrome sub-item. |
| Quick-status | Update tracker quick-status line: "V1.89 active → delivered (BL-11 MVP)". |

### T2 — Compound round

Trigger `mstar-compound`. Candidate patterns:
- Character-offset annotation anchoring + drift-notice pattern (local-first consumption metadata).
- Additive Local API contract for read-heavy "author comfort" features.

Skip-with-reason if nothing meets the Q1–Q8 bar; record in compass `## Compound Round Summary`.

### T3 — Profile B compaction

Per `.mstar/AGENTS.md` Profile B layout invariant:
1. For each `Done` plan in `status.json.plans[]`: write `.mstar/archived/plans/<plan-id>.json`; append `"<plan-id>"` string to `archived/plans-done.json` `plans[]`.
2. Remove Done plans from `status.json.plans[]`.
3. Add `iteration_summaries[V1.89]` snapshot.
4. Verify the layout invariant with the `python3 -c` check documented in [`.mstar/AGENTS.md`](../AGENTS.md) (Profile B section) — asserts every entry in `plans-done.json` is a string.

### T4 — Index + metadata refresh

- `iterations/README.md`: V1.89 row Status → `completed`.
- `metadata.latest_ship` in `status.json` → V1.89 entry.
- `metadata.latest_active_iteration` in `status.json` → cleared (set to `null` or the next iteration id once V1.90 opens).
- `STRATEGY.md` Decision Log: add V1.89 row (Deeper Manuscript Reading).

### T5 — PR

Open PR `iteration/v1.89` → `main`. Wait for all CI checks. Merge per repo convention.

## Branch policy

- Working branch: `feature/v1.89-closure` (from `iteration/v1.89` after P0 + P1 merge).
- Merge target: `iteration/v1.89`.
- PR: `iteration/v1.89` → `main`.

## Acceptance (DoD)

1. BL-11 moved to shipped archive; profile chrome remains tracked.
2. Compound round done (docs created OR documented skip reason).
3. Profile B compaction verified.
4. Trackers + indexes + `metadata.latest_ship` + STRATEGY Decision Log updated.
5. PR merged to `main`; integration branch `iteration/v1.89` retired.

## Wire contracts

`wire_contracts_changed: TRUE` — additive reading schemas shipped; `@42ch/nexus-contracts` 0.18.0. Schema validation plan: P-1 authors schema files + `validate-schemas` gate; P0 runs codegen + commits generated output + bumps npm version; P-last confirms `validate-schemas` is clean and `pnpm --filter @42ch/nexus-contracts run build` passes.
