# V1.40 World Create and Validation — Plan

**Plan ID**: `2026-06-10-v1.40-world-create-and-validation`  
**Title**: P0 — align `creator world` + `world_id` / `world_refs` validation  
**Created**: 2026-06-10  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`, `@architect`  
**Task category**: `logic`  
**Working branch**: `feature/v1.40-world-create-and-validation`  
**Merge target**: `iteration/v1.40`  
**Depends on**: `2026-06-10-v1.40-harness-docs-prepare`  
**Iteration compass**: [v1.40-novel-world-kb-delivery-compass-v1.md](../iterations/v1.40-novel-world-kb-delivery-compass-v1.md)  
**Primary spec**: [novel-workflow-profile.md](../knowledge/specs/novel-workflow-profile.md)  
**Spec refs**:

- [cli-spec.md](../knowledge/specs/cli-spec.md)
- [entity-scope-model.md](../knowledge/specs/entity-scope-model.md)
- [local-db-schema.md](../knowledge/specs/local-db-schema.md)

**Targets deferred**: DF-63 (slices W1 + W4)

**Reuse note (grill-me #11):** `creator world create` (`--title`) / `list` / `event-add` and `novel_scaffold` `world_id` FK check already ship in `crates/nexus42/src/commands/creator/world.rs` and `novel_scaffold` capability. P0 closes gaps — do not rewrite create from scratch.

---

## 1. Problem Statement

`novel-project-init` grill-me offers "create new World" but init path is not atomically bound to world create. `world_refs` validation is spec-only. CLI flags diverge from spec (`--title` vs `--name`). `creator world show` is missing. World-bound users lack structured `world_refs` gate behavior.

---

## 2. Goals

1. **Align** `creator world` CLI with spec (add `--name` alias or update spec to `--title`; document narrative kind default).
2. Add `creator world show <world_id>` (reuse `SqliteNarrativeGateway` / existing list row shape).
3. Wire init grill-me "create new" path to existing `create_world` (atomic with scaffold transaction).
4. Reuse / extend `novel_scaffold` `world_id` FK validation on Work PATCH/create; structured remediation.
5. Implement `world_refs` validator: warnings at outline, errors at finalize (world-bound Works).
6. Preserve worldless Work behavior unchanged.

---

## 3. Non-goals

- Rewriting `create_world` / narrative persistence (already in `nexus-local-db`).
- Full taxonomy (P1), prompt context block (P2), kb-extract binding (P3).
- Multi-volume PK, platform publish, multi-work switch.

---

## 4. Acceptance Criteria

1. Existing `creator world create` + `list` continue to work; `show` added.
2. Init "create new World" binds `world_id` or fails closed (no partial scaffold).
3. PATCH with invalid `world_id` returns remediation pointing to `creator world create` or `list`.
4. World-bound outline with invalid `world_refs` emits warnings; finalize blocks with error unless `--force`.
5. Worldless Works: no `world_id` gate failure; `world_refs` warn-only if present.
6. Hermetic tests cover valid/invalid `world_id` and `world_refs` paths.

---

## 5. Tasks

- [ ] T1: Spec preflight — novel-workflow-profile §3.5.1.1–§3.5.1.4, cli-spec `creator world` alignment.
- [ ] T2: Add `creator world show`; align CLI flags with spec (alias or spec patch).
- [ ] T3: Wire `novel-project-init` create_world path to `create_world` in scaffold transaction.
- [ ] T4: Confirm / extend `world_id` existence validation (reuse `novel_scaffold` + Work PATCH).
- [ ] T5: Implement `world_refs` validator (outline warning, finalize error).
- [ ] T6: Extend gate evaluator / stage gates for `world_refs` timing.
- [ ] T7: Hermetic API + CLI tests.
- [ ] T8: Update deferred tracker DF-63 partial disposition on ship.

---

## 6. Verification

```bash
cargo test -p nexus42 -p nexus-daemon-runtime -p nexus-local-db -- world create world_id world_refs 2>&1 | tail -30
cargo clippy -p nexus42 -p nexus-daemon-runtime -- -D warnings
nexus42 creator world create --title "Test World"
nexus42 creator world show wld_<id>
```

---

## 7. Explicitly out

| ID | Reason |
| --- | --- |
| DF-63 W2/W3/W5 | P1–P3 |
| Multi-volume PK | Deferred beyond V1.40 |
| DF-60/61/59 | Backlog |
| DF-56 | Conditional routing |
| DF-47 | Unless incidentally required |
