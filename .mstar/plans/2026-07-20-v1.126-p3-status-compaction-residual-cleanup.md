# P3 — status.json compaction + V1.121+…+V1.125 residual cleanup gate

> **Iteration compass:** `.mstar/iterations/v1.126/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.126/specs/status-compaction-residual-cleanup.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Get `.mstar/status.json` under the 20 KB hygiene line + close ≥ 50 low/nit residuals from V1.121 → V1.125 cluster. Closes `DF-V1123-STATUS-COMPACT` + `DF-V1123-RESIDUAL-CLEANUP` + retires the superseded `DF-V1122-STATUS-COMPACT` row.

**Execution:** mstar-sdd (multi-task) — but the implementer is the PM (or a delegated `fullstack-dev`) because the work is largely `jq` + JSON editing, not product code. Per `mstar-dispatch-gates`, PM still dispatches — does not implement inline.

### Wire contracts (architect LOCKED — pending seat 2)

- **Verdict: `wire_contracts_changed: false`** — harness hygiene only; no schemas, no daemon, no web consumer.

## Tasks

> **T1 sizing (architect-locked — P3 spec ND-A1, AQ-7):** T1 is a **single SDD task** — the 8 steps are structurally sequential (archival → compaction → verify). Splitting would create intermediate malformed `status.json` states. Each step has its own checkbox for progress tracking. **If the single-task scope exceeds one SDD session duration, fall back to inline execution** — PM dispatches `fullstack-dev` directly (`Execution mode: inline`) per the P3 plan preamble.

- [ ] **T1 (AC-V1126-4 — status.json compaction) · Must:** Drive `.mstar/status.json` from current 128 KB → < 20 KB:
  1. Run `wc -c .mstar/status.json` baseline; capture in plan checkbox.
  2. For every plan row in `plans[]` with `status: Done` AND `metadata.iteration_id` ≤ V1.125: confirm the row is already snapshotted under `.mstar/archived/plans/<plan-id>.json` (Profile B). If missing, snapshot first.
  3. Remove the `plans[]` row entries for archived Done plans (Profile B compacted state). Keep only non-`Done` (hot) plans.
  4. For every plan_id key in root `residual_findings`: if **all** entries are `nit` or `low` severity AND the plan is `Done` AND the plan_id is from V1.120 or earlier, archive the entire `residual_findings[<plan-id>]` array to `.mstar/archived/residuals/<plan-id>.json` (extending the existing pattern). Use `jq` to move (not copy) the array.
  5. **Mixed-severity plan_id handling (P3 spec ND-2):** For V1.121 → V1.125 plans with a mix of nit/low and medium/high residuals, archive **only** the nit/low entries individually — keep medium/high entries in `status.json::residual_findings[<plan-id>]`. Do not move the whole plan_id array; do not leave orphaned plan_id keys (if a plan_id retains any medium/high entry, the plan_id key stays in `residual_findings` with just those entries).
   6. Refresh `metadata.tech_debt_summary` via `.mstar/plan-artifacts/scripts/tech-debt-rollup.sh`. Verify `total_open ≤ 30` (P3 spec ND-A4).
   7. `wc -c .mstar/status.json` < 20_000 (target ≤ 18_000 for headroom).
   - **Verify:** `wc -c .mstar/status.json` reported in Completion Report; Profile B hot `plans[]` matches `git ls .mstar/archived/plans/`; archived residuals load via `jq .mstar/archived/residuals/<plan-id>.json`; no plan_id key in `residual_findings` is orphaned by T1 step 5.
   - Files: `.mstar/status.json`, `.mstar/archived/residuals/<plan-id>.json` (new files), `.mstar/archived/plans/<plan-id>.json` (snapshots), `.mstar/notes.json` (one entry recording the compaction)
   - **Architecture locks:**
     - Single SDD task (P3 spec ND-A1, AQ-7); fallback inline per preamble
     - `closure_note` string enum: 4 values — `"Fixed by <plan-id>"` / `"Stale — <reason>"` / `"Superseded — <reason>"` / `"Archived as low/nit bulk (V1.126 P3 gate)"` (P3 spec ND-A2)
     - Archived residual shape: JSON array at `.mstar/archived/residuals/<plan-id>.json`; each entry carries original fields + `closure_note` + `archived_at` (P3 spec ND-A3)
     - `tech_debt_summary` refresh via `.mstar/plan-artifacts/scripts/tech-debt-rollup.sh`; `total_open ≤ 30` (P3 spec ND-A4)
     - `wire_contracts_changed: false` — harness hygiene only (P3 spec ND-A5)
- [ ] **T2 (AC-V1126-4 — residual close-out review) · Must:** Audit each V1.121+ residual row before archival per P3 spec ND-3 closure labels:
  - If the residual is observably **fixed by a later plan** (e.g. V1.124 P0 fixture gap closed by V1.126 P1 directed-axis), label the archive entry `closure_note: "Fixed by <plan-id>"`.
  - If the residual is observably **stale** (file path no longer exists; code pattern already removed; documentation gap that has since been documented), label `closure_note: "Stale — <reason>"`.
  - If the residual is **superseded by a different mechanism** (e.g. "need composite endpoint for fan-out" superseded by V1.126 P2 itself), label `closure_note: "Superseded — <reason>"` (new label added at seat 1) with a cross-reference to the new mechanism.
  - If the residual is real low/nit tech debt, label `closure_note: "Archived as low/nit bulk (V1.126 P3 gate)"`. **P3 spec ND-8 safeguard:** if the original `title`/`notes` field suggests ≥ medium product impact (subjective but documented here), add a corresponding entry to `.mstar/notes.json` for future tech-debt roadmap visibility.
  - **Verify:** Each archived residual entry has a `closure_note`; spot-check 10 random entries; notes.json gains ≥ 1 entry if any "bulk" archive hit the ND-8 safeguard.
  - Files: same as T1 + `.mstar/notes.json`
- [ ] **T3 (AC-V1126-4 — tracker update) · Must:** Update `knowledge/deferred-features-cross-version-tracker.md`:
  - `DF-V1123-STATUS-COMPACT`: **close** — move to shipped archive with V1.126 P3 reference.
  - `DF-V1123-RESIDUAL-CLEANUP`: **close** — move to shipped archive.
  - `DF-V1122-STATUS-COMPACT`: **close** (superseded by V1.123 row; both retire together).
  - Refresh quick-status line to V1.126 + residual count summary.
  - **Verify:** Tracker diff shows three rows closed; shipped-archive gains three entries.
  - Files: `.mstar/knowledge/deferred-features-cross-version-tracker.md`, `.mstar/archived/shipped-features-tracker.md`
- [ ] **T4 (AC-V1126-4 — pre-P-last gate reset) · Must:** Confirm `.mstar/AGENTS.md` § Pre-merge checklist #5 (`wc -c .mstar/status.json < 20_000`) passes. Run `tooling/check-ui-guardrails.sh` to confirm no extract/alias drift introduced. Update `.mstar/notes.json` with one entry summarising the compaction + residual closure count.
  - **Verify:** Pre-merge checklist passes; notes.json entry present; tech_debt_summary refreshed.
  - Files: `.mstar/notes.json`, `.mstar/status.json`

## Done when

- **AC-V1126-4:** `wc -c .mstar/status.json < 20_000`; ≥ 50 low/nit residuals archived; `tech_debt_summary.total_open ≤ 30`; no medium/high/critical archived without explicit closure label.

## Roadmap

- **Single-iteration slice:** V1.121 → V1.125 cluster only. V1.126 P0/P1/P2 plan residuals stay open (P3 runs last so they exist).
- **Recurring gate:** `DF-V1123-STATUS-COMPACT` closure does **not** mean the 20 KB gate is permanently retired — it means the threshold is met at V1.126 close. Future iterations must keep `status.json` < 20 KB by archiving Done-plan residuals in their P-last gate.
- **Tech-debt roadmap items with `medium` severity** (V1.92 Resume TOFU, V1.112 i18n migration remainder, V1.116 CodexNativeProvider registration, V1.16 hotfix creator PATCH 404) **stay open** in `status.json` — they need product/code work, not archival.
- **V1.121 residual cluster** (`DF-V1122-V1121-RES`): scoped roadmap — V1.121 design-elevation residuals are **not** bulk-archived unless observably fixed/stale. They get their own roadmap entry if any remain post-P3.

## Working branch

`feature/v1.126-p3-status-compaction-residual-cleanup` → merge `iteration/v1.126`
