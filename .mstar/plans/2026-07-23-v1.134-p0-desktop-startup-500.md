# Desktop Startup 500 — RCA + Fix

> **For agentic workers:** REQUIRED SUB-SKILL: `mstar-sdd` (multi-task plan). Steps use checkbox (`- [ ]`) syntax.
>
> **Iteration compass:** `.mstar/iterations/v1.134/delivery-compass.md`

## Metadata

| Field | Value |
|-------|-------|
| plan_id | `2026-07-23-v1.134-p0-desktop-startup-500` |
| iteration_id | V1.134 |
| priority | **Must** (no Stretch) |
| wave | 1 · seq 0 (backend first; isolated) |
| primary_spec | compass AC-0…AC-3 + RCA guide (created in T1) |
| spec_integration_branch | `iteration/v1.134` |
| working_branch | `plan/v1.134-p0-desktop-startup-500` |
| merge_target | `iteration/v1.134` |
| wire_contracts_changed | false |
| rca_retarget | T2 → apps/web Vite proxy + query gate (not daemon handler) |

## Problem Statement

**User impact:** On desktop launch the author still sees a **`500 Internal Server Error`** in the startup network log. The app continues because the probe is non-blocking, so the defect is easy to ignore and has survived prior 404-class fixes (V1.132 P0, V1.133 brace sweep). Spurious 500s erode trust that the local daemon is healthy.

**User ask:** Locate the 500 ("需要定位500的报错是什么") and eliminate it — not paper over it.

## User value

Clean cold start: opening Nexus desktop does not report server errors for ordinary startup probes. Empty/uninitialized local state returns an expected domain outcome (e.g. empty 200), never Internal Server Error.

## Goal

Eliminate the non-blocking `500 Internal Server Error` that appears on desktop startup.

## Priority

**Must.** Entire plan is hardening. No Stretch tasks. If RCA reveals a large unrelated refactor, stop and escalate — do not expand scope.

## Architecture

**RCA (T1 locked):** Startup `500`s are **Vite proxy transport** — upstream `ECONNREFUSED` on `127.0.0.1:8420` while the sidecar boots, mapped to HTTP 500 with empty body. Daemon handlers return expected domain codes once listening. Fix boundary is **`apps/web`** (proxy error mapping + gate early queries), not a daemon `Internal` handler. Anticipated `wire_contracts_changed: false`.

**Tech Stack:** Vite proxy (`apps/web`), Tauri desktop shell, apps/web fetch/query gating. Local-only daemon at `127.0.0.1:8420`.

**Execution:** mstar-sdd

## Acceptance Criteria

| ID | Criterion (user-observable / verifiable) | Evidence |
|----|------------------------------------------|----------|
| **AC-0** | Clean desktop launch: no `500` from any `/v1/daemon/*` or `/v1/local/*` startup probe | Startup network log / daemon log capture |
| **AC-1** | Failing route + root cause documented | RCA guide + plan Review Gate Summary |
| **AC-2** | Regression test for the previously-failing condition (proxy refusal / pre-ready probes → no 500) | web/unit or Playwright evidence |
| **AC-3** | Touched-package health | scoped `pnpm`/lint/test (and cargo only if Rust touched) |

## Non-Goals

- Residual burn-down, unrelated handler refactors, new product features
- Wire-contract bumps unless RCA proves a shape mismatch
- Reintroducing `{param}` brace routes (matchit `:param` only)
- "Silent catch" that hides real errors without a correct domain response

## Global Constraints

- Pre-1.0 repo: breaking changes allowed, but this fix must be surgical — a 500 fix, not a refactor.
- Single DTO SSOT: if RCA shows a shape mismatch, fix the schema/handwritten-DTO alignment, do not hand-write a parallel type (`apps/web` consumes `@42ch/nexus-contracts` only).
- Path syntax: all daemon routes use matchit 0.7 `:param` (V1.133 sweep) — do not reintroduce `{param}`.
- Scoped commands only: prefer `apps/web` package scripts for this plan after RCA; `cargo check/test/clippy -p <crate>` only if Rust is touched; `--all` only at gate.
- Nightly rustfmt pin `nightly-2026-06-26` for any Rust formatting.
- Do not "fix" this by silencing real daemon `Internal` errors; the defect is proxy refusal during boot.

---

### Task 1: RCA — reproduce + pin the failing route · Must

**Files:**
- Modify (evidence): `.mstar/iterations/v1.134/guides/p0-startup-500-rca.md` (create — RCA findings)
- Read: `apps/web/src/lib/nexus/queries.ts`, `apps/web/src/components/setup/daemon-launch-gate.tsx`, app-shell mount effects, `crates/nexus-daemon-runtime/src/api/mod.rs` (route table)

**Interfaces:**
- Consumes: the desktop/web startup fetch sequence
- Produces: the exact failing route (URL + method), the server-side stack/branch, and the repro steps — recorded in the RCA guide.

- [x] **Step 1: Reproduce.** Run `pnpm dev:desktop` (dist-load mode) against a fresh-built sidecar (or web dev + daemon). Open the desktop shell and capture the startup network log (DevTools or daemon stdout). Identify the request(s) returning `500`.
- [x] **Step 2: Capture server-side evidence.** From the daemon log / the route's error mapping (`crates/nexus-daemon-runtime/src/api/errors.rs` `Internal` → 500), identify the handler + the unhandled branch (DB error, serialization, unwrap, missing workspace/no-Profile state, empty cursor, etc.).
- [x] **Step 3: Record RCA.** Write the failing route, root cause, and repro to `.mstar/iterations/v1.134/guides/p0-startup-500-rca.md`. Do not fix yet — confirm the root cause first.
- [x] **Step 4: Commit RCA evidence** (guide only).

### Task 2: Eliminate startup 500s (Vite proxy + query gate) · Must

> **RCA retarget (T1):** Root cause is **Vite `/v1/daemon` proxy** mapping upstream `ECONNREFUSED` → HTTP **500** (empty body) while the sidecar is still booting — **not** a `nexus-daemon-runtime` handler `Internal` branch. See `.mstar/iterations/v1.134/guides/p0-startup-500-rca.md`. Do **not** patch a daemon handler for this defect.

**Files (expected):**
- Modify: `apps/web/vite.config.ts` (proxy error mapping — Option B)
- Modify: `apps/web/src/App.tsx` and/or query enablement around `DefaultProfileCoordinator` / `DaemonLaunchGate` (Option A — gate pre-ready data probes)
- Read: `apps/web/src/lib/nexus/tauri-client.ts`, `apps/web/src/components/setup/daemon-launch-gate.tsx`
- Test: focused web/unit or Playwright assertion that startup probes do **not** receive HTTP 500 when `:8420` is down (or after health 200); prefer cheapest durable test

**Interfaces:**
- Consumes: RCA from Task 1 (Options A + B preferred; Option C only if A+B insufficient)
- Produces: no HTTP **500** on `/v1/daemon/*` startup probes during sidecar boot window; regression evidence.

- [ ] **Step 1: Write / extend a failing test** that captures the current 500-on-proxy-refusal behavior (or asserts absence of 500 after the intended fix surface). Prefer unit/proxy-config test or a narrow Playwright check over full desktop.
- [ ] **Step 2: Run test — expect FAIL** against current behavior (or red on the new assertion).
- [ ] **Step 3: Minimal fix** — (B) map Vite proxy `ECONNREFUSED`/connect errors to **503** (or equivalent non-500) with a clear body if useful; (A) gate `DefaultProfileCoordinator` / TanStack startup queries until daemon ready / health OK so creator/works/worlds/scan do not fire into the boot window. Surgical — no daemon handler drive-by.
- [ ] **Step 4: Run test — expect PASS.**
- [ ] **Step 5: Scoped verification** for touched packages (`pnpm` test/lint in `apps/web` as appropriate). Do **not** require `cargo test -p nexus-daemon-runtime` unless you also touched Rust.
- [ ] **Step 6: Commit.**

### Task 3: Startup-log cleanliness check + plan summary · Must

**Files:**
- Modify: this plan (`## Review Gate Summary`) + compass `## Quality Gate Summary` at close
- Optional: a small integration test asserting no daemon route returns 500 across the startup fetch set (if cheap)

**Interfaces:**
- Consumes: Task 2 fix
- Produces: AC-0 evidence (startup log clean) + AC-1 (root cause documented in plan summary).

- [ ] **Step 1: Re-run desktop startup; confirm no 500** in the startup network log for any `/v1/daemon/*` (or `/v1/local/*`) route. Record evidence (which routes were probed, all non-500).
- [ ] **Step 2: Write the durable Review Gate Summary** on this plan (root cause + fix + regression evidence).
- [ ] **Step 3: Commit.**

## Plan self-review (PM before locked)

1. **Spec coverage:** RCA-first means Task 1 pins the real route before Task 2 fixes — no assumed route.
2. **Placeholder scan:** no TBD; handler path is resolved in Task 1, not hardcoded here.
3. **Type consistency:** no new DTOs anticipated; if RCA finds a shape mismatch, schema/handwritten alignment only.
4. **Must-only:** no Stretch smuggled; scope is the startup 500 class only.

## SDD runtime (ephemeral)

When using `mstar-sdd`, artifacts live under `{SDD_DIR}` (`sdd/2026-07-23-v1.134-p0-desktop-startup-500/`). Do not duplicate briefs/reports in this file.
