# V1.45 Delete Bespoke Run Subcommands — Plan

**Plan ID**: `2026-06-13-v1.45-delete-bespoke-run-subcommands`  
**Title**: P1 — Remove hardcoded `creator run` subcommands + DEPRECATED presets  
**Created**: 2026-06-13  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.45-delete-bespoke-run-subcommands`  
**Merge target**: `iteration/v1.45`  
**Depends on**: P0 generic runner (same merge — Grill #19)  
**Parallel with**: P0, P2  
**Iteration compass**: [v1.45-creator-run-preset-unification-delivery-compass-v1.md](../iterations/v1.45-creator-run-preset-unification-delivery-compass-v1.md)  
**Tracker**: BL-12, BL-13, Grill #20

---

## 1. Problem Statement

V1.44 added `audit-chapter` and `review-master` as bespoke handlers. V1.33–V1.39 added `start`, `continue`, `stage`, `resume`, `reconcile-chapters`. All must be removed when generic runner ships.

---

## 2. Goals

1. Delete `RunCommand` variants: `Start`, `Continue`, `Stage`, `Resume`, `ReconcileChapters`, `AuditChapter`, `ReviewMaster`.
2. **Hard delete** `embedded-presets/novel-manuscript-audit/` parent directory (DEPRECATED).
3. Remove `review_master_cli.rs` list behavior; tests migrate to generic runner.
4. Fix `STAGE_PRESET_ALLOWLIST` `memory-review` drift (BL-13) — remove or add preset (implement decision).
5. Update daemon/CLI hint strings referencing deleted commands.

---

## 3. Non-goals

- `creator bootstrap` / works migration (P2).
- Spec promotion to Shipped (P-last).

---

## 4. Acceptance Criteria

1. `nexus42 creator run --help` shows no legacy subcommands.
2. No references to `audit-chapter`, `review-master`, `stage advance` in `run.rs`.
3. Parent `novel-manuscript-audit` preset dir absent.
4. Integration tests use preset id commands.

---

## 5. Tasks

- [ ] T1: Remove enum variants + handlers from `run.rs`.
- [ ] T2: Delete DEPRECATED preset directory + loader references.
- [ ] T3: Migrate/delete `review_master_cli.rs` tests.
- [ ] T4: BL-13 allowlist cleanup.
- [ ] T5: Update works/mod.rs hint strings.

---

## 6. Verification

```bash
cargo test -p nexus42
rg -n 'audit-chapter|review-master|RunCommand::Start|stage advance' crates/nexus42/
test ! -d crates/nexus-orchestration/embedded-presets/novel-manuscript-audit
```
