# V1.55 P2 — Game-Bible Depth 3.5

**plan_id**: `2026-06-22-v1.55-game-bible-depth-35`  
**status**: Todo  
**created**: 2026-06-22  
**owner**: `@fullstack-dev-2` (Track B canonical owner per compass §3 track-ownership pattern)  
**effort**: L  
**working_branch**: `feature/v1.55-game-bible-depth-35` (from `iteration/v1.55`)  
**merge_target**: `iteration/v1.55`  
**type**: implement  
**track**: Theme D / Track B  
**iteration**: V1.55  
**compass**: `v1.55-non-novel-profile-completion-and-infrastructure-refactor-delivery-compass-v1.md`

---

## Goal

Upgrade game-bible from V1.54 scaffold to Depth 3.5: LLM-driven `design-writing`, design 五问 quality rubric, section completion detection, and KB extraction for game-bible design facts.

## Scope In

- `game-bible-profile.md` Draft body updates for Depth 3.5.
- `design-writing` preset with rubric and section-aware prompts.
- Section completion detection over `Design/*.md` frontmatter.
- KB extraction path using `game_bible_category` and existing BlockType taxonomy.
- Close `R-V154P1-S002` profile-gate observability by adding trace/audit evidence to profile-gate paths.

## Scope Out

- Script profile scaffold (P3).
- Platform publish / export.
- New game-bible BlockType variants beyond V1.54 unless PM approves.
- Full auto-chain beyond the Depth 3.5 scope.

## Plan Steps

| ID | Step | Deliverable |
| --- | --- | --- |
| T1 | Read V1.54 game-bible spec/implementation, essay pattern, novel workflow baseline, and R-V154P1-S002 | Design summary |
| T2 | Run GitNexus impact before editing profile/preset/completion symbols | Blast-radius evidence |
| T3 | Author/update spec sections for Depth 3.5 | `game-bible-profile.md` Draft updates |
| T4 | Implement `design-writing` preset and design 五问 rubric | Preset + rubric tests |
| T5 | Implement section completion detection for critical Design sections | Completion tests |
| T6 | Implement KB extraction mapping for game-bible facts | Extraction tests |
| T7 | Add profile-gate tracing/audit observability | Closure evidence for `R-V154P1-S002` |
| T8 | Run scoped + integration verification | Test/clippy/fmt evidence |

## Acceptance Criteria

- [ ] `design-writing` can draft/review game-bible sections with a design-specific 五问 rubric.
- [ ] Completion detection evaluates critical `Design/*.md` sections and intake completion.
- [ ] KB extraction creates/updates World KB facts with correct `game_bible_category` handling.
- [ ] `R-V154P1-S002` is closed with tracing/audit evidence or explicitly re-residualized.
- [ ] `game-bible-profile.md` status remains Draft during P2 and is ready for Master promotion in P-last.
- [ ] P2 topic branch is merged to `iteration/v1.55` before tri-review.

## Branch Policy

- Create `feature/v1.55-game-bible-depth-35` from `iteration/v1.55`.
- Merge to `iteration/v1.55` before QC.
- P3 starts only after P2 has passed mid-QC/mid-QA or PM records an override.

## Dependencies

- V1.54 game-bible scaffold shipped.
- V1.54 BlockType + `ValidationMode::GameBible` are baseline.
- P0 may run in parallel but spec drift must be resolved at merge checkpoints.

## Owner / Effort

- **Owner**: `@fullstack-dev-2` (Track B canonical owner; see compass §3 track-ownership pattern).
- **Effort**: L.

## QC Strategy

Tri-review + mid-QA after merge to integration. QC focus: rubric determinism, completion correctness, KB taxonomy safety, and observability closure. QA focus: scaffolded game-bible Work can run through design/review/completion happy path locally.

## Risks

| Risk | Mitigation |
| --- | --- |
| Rubric too novel-specific | Explicitly use design pillars/mechanics/continuity questions, not prose chapter 五问. |
| Completion reads stale frontmatter | Tests cover accepted/reviewed/draft combinations and missing files. |
| KB extraction misclassifies categories | Use V1.54 taxonomy table and category validation tests. |

## Completion Notes — P2 Fix-Wave (2026-06-21)

**Branch**: `feature/v1.55-game-bible-depth-35`  
**Merge target**: `iteration/v1.55`  
**QC findings addressed**: qc1 F-001 (critical), F-002 (high); qc3 W-1, W-2, W-3 (medium); qc1 S-001 (low)

### Fixes Delivered

| Fix | Finding | Approach | Evidence |
|-----|---------|----------|----------|
| F-001 | KB extraction not profile-aware | Refactored `candidate_from_llm_json` → `candidate_from_llm_json_for_profile(work_profile)`; game-bible path emits `game_bible_category` via `block_type_to_game_bible_category`; added 5 tests (direct, cross-domain, unknown-default, novel-regression) | `quality_loop.rs` tests pass |
| F-002 | design-writing preset no durable section_status | **Option B**: narrowed preset/spec comments to state accepted frontmatter is manual V1.55 step; registered `R-V155P2-F002` residual targeting V1.56+ for auto-transition capability | `status.json`, `preset.yaml` |
| W-1 | info-level tracing on hot path | Downgraded per-section `tracing::info!` → `debug!`; kept `info!` at intake-gate and all-critical-accepted transitions; added level-intent doc comment | `work_chapters.rs` |
| W-2 | synchronous file I/O in async handler | Changed `std::fs::read_to_string` → `tokio::fs::read_to_string`; added `fs` feature to tokio dep | `work_chapters.rs`, `Cargo.toml` |
| W-3 | helpers defined but not wired | **Closed** — addressed by F-001 fix (profile-aware materialization is the wiring) | Same as F-001 |
| S-001 | spec language reconcile | Updated §7.2 to clarify cross-domain `BlockType` values **map** to closest `game_bible_category`, not that `"character"` is a valid literal; linked to canonical mapping table | `game-bible-profile.md` |

### CI Gates

- `cargo test --all`: 0 failures
- `cargo clippy --all -- -D warnings`: clean
- `cargo +nightly fmt --all -- --check`: clean

### Residuals

- `R-V155P2-F002` registered in `status.json` (`lifecycle: deferred`, target V1.56+)

### Files Changed

- `crates/nexus-orchestration/src/quality_loop.rs` — F-001 profile-aware extraction + tests
- `crates/nexus-orchestration/src/tasks/mod.rs` — work_profile context read in LlmExtractTask
- `crates/nexus-local-db/src/work_chapters.rs` — W-1 tracing levels + W-2 async I/O
- `crates/nexus-local-db/Cargo.toml` — tokio `fs` feature
- `crates/nexus-orchestration/embedded-presets/design-writing/preset.yaml` — F-002 comments
- `.mstar/knowledge/specs/game-bible-profile.md` — S-001 §7.2 reconcile
- `.mstar/status.json` — F-002 residual registration
