# Plan — V1.73 P-last Closure

**plan_id**: `2026-06-28-v1.73-closure`
**Iteration**: V1.73 — [Compass](../iterations/v1.73-canvas-world-kb-beta-and-outline-hardening-compass-v1.md)
**Status**: Todo
**Owner**: `@project-manager`
**Effort**: S
**Branch policy**: direct commit to `iteration/v1.73`
**Depends on**: P0 + P1 merged into `iteration/v1.73`

## Goal

V1.73 closure: residual convergence (close 7 + defer 9 + feature-defer relationships), spec promotion, Profile B compaction, integration PR to `main`, PM signoff.

## Tasks

- [ ] T1 — Residual convergence: mark 7 closed residuals (`R-V172P0-QC2-001/002/003/004`, `R-V172P0-QC1-002`, `R-V171P1-QC1-001`, `R-V172P1-QC3-003`) `lifecycle: resolved` with resolution commit + plan_id in `status.json` root `residual_findings`. Move 9 hygiene residuals to `target: tbd-v1.74-hygiene` (durable plan-id pointer, not narrative).
- [ ] T2 — Feature deferral: add World KB relationships surface row to `deferred-features-cross-version-tracker.md` §2.3 with `Target: V1.74 (tbd-v1.74-world-kb-relationships)`.
- [ ] T3 — Spec promotion: `canvas-strategy-surface.md` §3.3 surface 3 Draft → Shipped β + §3.4 WorldKbNodeData/WorldKbEdgeData schema-backed; `web-ui.md` V1.73 stage; DESIGN.md World KB canvas write tokens added; `local-api-surface-conventions.md` World KB patch pattern extended.
- [ ] T4 — `tech_debt_summary` refresh: 16 → 9 open (7 closed); update `metadata.latest_ship` block (V1.73 compass, plan_count, wire_contracts_changed: true, residual_summary).
- [ ] T5 — Profile B compaction: archive 4 plan rows (P-1, P0, P1, P-last) to `.mstar/archived/plans/<plan-id>.json`; append plan_ids to `.mstar/archived/plans-done.json` (strings only); remove from `status.json.plans[]`; verify layout invariant.
- [ ] T6 — `iterations/README.md` V1.73 row: Shipped status + PR + merge commit + summary.
- [ ] T7 — `notes.json` append: V1.73 ship narrative (timeline + plan count + wire bump + residual delta + integration branch retirement).
- [ ] T8 — Integration PR `iteration/v1.73` → `main` (PR #99 expected); wait for all CI checks (default + 1 hour budget); merge with `--merge` (merge commit) for provenance.
- [ ] T9 — PM signoff; retire `iteration/v1.73` in metadata (`integration_branch_retired: true`, `last_integration_branch: "iteration/v1.73"`).

## Acceptance (maps to compass §7 DoD #5, #6, #9)

- 7 residuals `lifecycle: resolved` in SSOT; 9 deferred to `tbd-v1.74-hygiene`; relationships deferred to `tbd-v1.74-world-kb-relationships` in feature tracker.
- Spec promotions committed; DESIGN.md tokens in both themes.
- `tech_debt_summary`: 9 open; `latest_ship` = V1.73.
- Profile B compaction verified (strings-only `plans-done.json` + per-file archive).
- PR merged to `main`; CI green; `iteration/v1.73` retired.

## Notes

- PM-whitelist plan (status/spec/index/PR maintenance). No specialist artifact body.
- Residual severity strings must use the machine enum from `mstar-plan-artifacts` (`medium`, `low`, `nit`) — no non-canonical values.
- Feature deferral (relationships) goes to `deferred-features-cross-version-tracker.md`, NOT `status.json` (it's a feature, not a residual row).
