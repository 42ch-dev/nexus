# P1 — Settings Agent Section + getAgentProfile

**Goal:** Agent section mounts AgentPicker and preselects saved profile via Tauri-only `getAgentProfile` (G1).

**Architecture:** `SettingsAgentSection` at `/settings/agent`; `get_agent_profile` Tauri command + `DesktopCapabilities.getAgentProfile()` returning `{ name, launchCommand? } | null` (no schemas). Write via existing `set_agent_profile`. Browser: honest no-op preselect.

**Implementer SSOT:** `.mstar/iterations/v1.103/specs/settings-agent-section.md`

**Tech Stack:** React, Tauri IPC via `DesktopCapabilities`, Vitest, Design Studio.

**Execution:** mstar-sdd

**Tier:** **Must (P1)**

**Blocked by:** `2026-07-09-v1.103-settings-shell-nav`

## Prepare Gates

- specify: done
- clarify: done
- plan: done

## Product Framing

**User value:** Settings opens showing the current agent, not a blank re-pick.

**Non-goals:** BYOK; execution-mode matrix; package promotion; scan schema changes.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| Preselect | **G1** Must Tauri-only `getAgentProfile` |
| Picker | Existing app-shared module |
| Wire | `wire_contracts_changed: false` |

## Acceptance Criteria

1. Default Settings landing shows AgentPicker with saved profile pre-selected on desktop when readable.
2. Author can change selection; persist via `setAgentProfile`.
3. Browser: honest desktop-oriented behavior (no fake preselect success).
4. Vitest covers mount + mocked preselect.
5. No schemas change.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.103/specs/settings-agent-section.md` (Agent helper + browser copy)
- Shell: `.mstar/iterations/v1.103/specs/settings-shell-ia.md`

---

### Task 1: Desktop getAgentProfile Capability

**Files:**
- Modify: `apps/desktop/src-tauri/src/lib.rs` — add `get_agent_profile` command (read first `native_cli` provider)
- Modify: `apps/web/src/lib/nexus/desktop-capabilities.ts` — interface + `TauriDesktopCapabilities.getAgentProfile`
- Test: `desktop-capabilities.test.ts`

- [x] Read path returns null on empty/malformed; preselect treats errors as null.

### Task 2: Studio — Agent Section Fixture

- [ ] Visual acceptance of Agent section chrome with preselected fixture state.

### Task 3: App — Wire Agent Section

**Files:**
- Modify/Create: settings agent section page; reuse AgentPicker + mapping helpers

- [ ] Preselect on mount; persist on commit; Vitest.

### Task 4: Verify

- [ ] Tests green; no schemas drift.

## Plan self-review (PM before locked)

1. G1 Must explicit.
2. No wire schemas.
3. Depends on P0 shell.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-09-v1.103-settings-agent-preselect/`
