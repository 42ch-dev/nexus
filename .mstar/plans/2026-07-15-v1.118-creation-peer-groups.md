# P1 — Creation peer groups

> **Status:** Draft (Phase 1). Product specify + clarify **done**; architect **plan done** (2026-07-15).

| Field | Value |
|-------|-------|
| plan_id | 2026-07-15-v1.118-creation-peer-groups |
| iteration_refs | V1.118 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.118/specs/creation-peer-groups.md` |
| iteration_compass | `.mstar/iterations/v1.118/delivery-compass.md` |
| spec_integration_branch | iteration/v1.118 |
| merge_target | iteration/v1.118 |
| wire_contracts_changed | false |

**Goal:** Creator tab list mode uses three peer groups — Works / Worlds / Memories — wired to existing pages.

**Depends on:** — (parallel-safe with P0; must merge before P2)

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors (browsing) | Creation nav matches Works / Worlds / Memories mental model — no canvas meta-group |
| Authors (Worlds / Memories) | Existing `/worlds` and `/memory` reachable from peer groups |
| Authors (Works) | Works visible under Works peer group, not buried in Creator meta-group |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs Checked:** V1.117 `creatorGroups` in `sidebar.tsx`; `useWorks` query exists; compass AD-P1; P2 overlap guard.
- **Impactful Ambiguities:** None.
- **Gate Decision:** go

### Plan

- **Option A — Flat Works list under peer heading:** All Works + up to 12 work rows from `useWorks({ limit: 12 })`. **Trade-off:** sidebar height grows — acceptable with internal scroll.
- **Option B — Collapsible nested tree:** **Trade-off:** extra chrome; rejected for MVP.
- **Selected Approach:** Option A.
- **Long-term Target State:** Creation tab = Works | Worlds | Memories peers; Outline only inside work shell (P2).
- **Durable Slice for This Batch:** List-mode `creatorGroups` rewrite + locales + sidebar tests; **no** `isDrillIn` changes.
- **Module Boundaries:** `sidebar.tsx` (T1), `shell.json` locales (T3), `canvas-nav.ts` unchanged for list mode (canvas items removed from Creation groups only).
- **API/Data Contracts:** Reuse `listWorks` / `WorkSummary` — `wire_contracts_changed: false`.
- **Risks and Rollback:** P2 sidebar conflict — **P1 merges first**; P2 rebases. Highlight on work routes interim until P2.
- **Validation Plan:** AC-P1-* visual + `pnpm --filter web test` sidebar; Orchestrator regression.
- **Effort (agent-oriented):** S — 1 session.

## Prepare Package (Product)

### Specify

- **Problem:** V1.117 "Creator" meta-group mixes Outline, World KB, Memory — wrong mental model.
- **User value:** Three parallel peers matching author domains.
- **Scope:** List-mode Creator tab only; remove meta-group; wire existing routes. AC-P1-*.
- **Non-goals:** Work shell (P2); new editors; Orchestrator redesign; drill-in retirement.
- **Target state:** IC-5, IC-6 in delivery compass.
- **Roadmap if split:** P2 owns `/works/:workId/*` shell — P1 must land first.
- **Draft DoD:** All AC-P1-* pass; locales + sidebar tests green.

### Clarify

- **Decisions:** Outline not a Creation peer (grill-me #2). Strategy stays Orchestrator. Worlds/Memories = existing pages only (grill-me #4).
- **Open questions (architect):** ~~Works group flat list shape; highlight rules on work routes before P2.~~ **Resolved** — flat list + interim drill-in highlight until P2 (compass AD-P1).
- **Still blocked:** None for product.

## Scope

### In scope

- Rewrite Creator-tab groups in `apps/web/src/components/layout/sidebar.tsx` for **list mode**
- Remove V1.117 "Creator" meta-group (Outline + World KB + Memory mix)
- Works group: entries from existing Works query (AC-P1-5)
- Worlds group → `/worlds` (AC-P1-3)
- Memories group → `/memory` + SOUL entry points (AC-P1-4)
- Locales (en / zh-CN) + sidebar tests (AC-P1-7)
- Opportunistic V1.117 residuals on touched files

### Not in scope

- Canvas-first work shell (P2) — including `isDrillIn` retirement
- Right-rail Works preview
- New Memories editor / world authoring flows
- Orchestration tab redesign
- Canvas content i18n (R-P1-001) unless touched opportunistically

## Acceptance Criteria

Detail in `primary_spec` (AC-P1-*):

1. Three peer group labels: Works / Worlds / Memories (AC-P1-1).
2. No Creator meta-group mixing canvas surfaces (AC-P1-2).
3. Worlds → `/worlds`; Memories → `/memory` (AC-P1-3, AC-P1-4).
4. Works group lists real works from Works data (AC-P1-5).
5. Orchestrator tab + Strategy unchanged (AC-P1-6).
6. en + zh-CN keys + sidebar tests (AC-P1-7).
7. Footer Profiles + Orchestrator regression (AC-P1-8).

## Tasks

- [ ] T1: Sidebar peer groups — Works (`/works` + flat work rows), Worlds (`/worlds`), Memories (`/memory`); remove Creator meta-group / canvas items from list mode
- [ ] T2: Wire work rows via `useWorks({ limit: 12 })`; Worlds/Memories single links; active highlight prefix rules (non-drill-in paths only)
- [ ] T3: Locales (`nav.works`, `nav.worlds`, `nav.memories`) + sidebar unit tests
- [ ] T4: Opportunistic residuals on touched files

## Roadmap / deferred scope

- Deeper Memories multi-entry IA → next iteration
- Enter-work Canvas shell → P2 (`2026-07-15-v1.118-canvas-work-shell`)

## SSOT status

`.mstar/status.json` → `plans[]` / `residual_findings`
