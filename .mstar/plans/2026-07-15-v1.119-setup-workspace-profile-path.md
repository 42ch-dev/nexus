# P1 — Workspace profile + path

> **Status:** Draft (Phase 1). Product specify + clarify **done** (§5.1). Architect plan **done** (§5.2). Writing review **done** (§5.3).

| Field | Value |
|-------|-------|
| plan_id | 2026-07-15-v1.119-setup-workspace-profile-path |
| iteration_refs | V1.119 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.119/specs/setup-workspace-profile-path.md` |
| iteration_compass | `.mstar/iterations/v1.119/delivery-compass.md` |
| spec_integration_branch | iteration/v1.119 |
| merge_target | iteration/v1.119 |
| working_branch | plan/v1.119-p1-workspace-profile-path |
| wire_contracts_changed | false |
| blocked_by | 2026-07-15-v1.119-setup-continue-unblock |

**Goal:** Default Profile name `default`; fix focus overlap; sync path last segment unless folder picked (F4–F6).

**Depends on:** P0 (same Workspace step)

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors | Name and folder path stay coherent; focus UI not broken; Continue enabled on first paint |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Product)

### Specify

- **Problem statement:** Empty default name disables Continue; focus ring covers Workspace label; path last segment ignores name until Browse (F4–F6).
- **User value:** Defaults match author mental model (`default` / `.../nexus/default`); typing name keeps path coherent.
- **Scope:** Wizard init + Workspace step sync/layout + slug helper. Primary spec AC-P1-*.
- **Non-goals:** Continue root cause / Reset classifier (P0); AgentPicker (P2); Settings workspace editor.
- **Target state:** IC-3, IC-4, IC-5; AC-P1-5 (Continue enabled without typing).
- **Draft DoD:** All AC-P1-* pass; slug unit tests include CJK and empty edge cases.

### Clarify

- **Decisions (locked):**
  - Initial `profileDisplayName: 'default'` in wizard state.
  - Placeholder remains example copy (`My Profile`) — not the field value.
  - Reuse `workspacePicked`; set only on successful directory pick.
  - Slug applies to path segment only; display name as typed.
  - Slug rule: trim, whitespace→`-`, strip illegal FS chars, NFKC Unicode preserved (CJK allowed in segment), empty→`default`.
  - Focus/layout: Workspace folder label fully visible when Profile Input focused at 480px card width.
- **Open questions (architect §5.2):**
  - ~~Windows reserved device-name list for slug sanitizer.~~ **Resolved** — AD-P1 blocklist + `-profile` suffix (compass).
  - ~~Whether initial `getWorkspaceRoot` response should be re-slugged on mount when name is `default`.~~ **Resolved** — one-time mount reconcile when basename ≠ slug and `!workspacePicked` (AD-P1).

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs Checked:** Wizard init `profileDisplayName: ''` disables Continue; `workspacePicked` flag exists; stale path persist rules in `shouldPersistWorkspacePath`; compass AD-P1 locked.
- **Impactful Ambiguities:** None.
- **Gate Decision:** go (implement blocked on P0 merge for same step)

### Plan

- **Option A — Shared slug helper + mount reconcile + onChange sync:** **Trade-off:** must not call `setWorkspacePath` on mount (display-only until Continue).
- **Option B — Rust-side path rewrite in `get_workspace_root`:** **Trade-off:** couples display name to desktop IPC; rejected.
- **Selected Approach:** Option A.
- **Long-term Target State:** Profile name and folder path stay coherent; defaults enable Continue on first paint.
- **Durable Slice for This Batch:** Wizard init `default`, slug helper, mount reconcile, onChange sync gated by `workspacePicked`, focus scroll-margin, slug unit tests.
- **Module Boundaries:**

  | Module | Change |
  | --- | --- |
  | `apps/web/src/lib/workspace-profile-slug.ts` | `slugProfileSegment`, reserved-name handling |
  | `apps/web/src/pages/setup-wizard-page.tsx` | Init `profileDisplayName: 'default'` |
  | `apps/web/src/pages/setup-step-workspace.tsx` | Mount reconcile, onChange path sync, focus layout |

- **API/Data Contracts:** Reuse `getWorkspaceRoot` / `setWorkspacePath` — `wire_contracts_changed: false`.
- **Risks and Rollback:** Mount reconcile fights user-edited path — gated by `!workspacePicked` + basename mismatch only. P0 classifier unchanged.
- **Validation Plan:** AC-P1-* + slug edge cases (CJK, reserved, empty).
- **Effort (agent-oriented):** S — 1 session.

## Scope

### In scope

- Pre-fill `profileDisplayName: 'default'`
- Focus/overlap layout fix (scroll-margin or spacing)
- Path sync when `!workspacePicked` via shared slug helper
- Unit tests for sync + freeze + slug edge cases

### Not in scope

- Continue bootstrap / Reset UX (P0)
- Agent catalog (P2)

## Acceptance Criteria

1. Default name `default` (AC-P1-1).
2. Focus does not cover folder label (AC-P1-2).
3. Path sync / freeze per rules (AC-P1-3, AC-P1-4).
4. Continue enabled on first paint with defaults (AC-P1-5).

## Tasks

- [ ] T1: Default name + i18n placeholder alignment
- [ ] T2: Path sync helper + workspacePicked gate
- [ ] T3: Focus/layout fix
- [ ] T4: Tests + verify AC-P1-*

## Roadmap / deferred scope

- N/A for this slice.
