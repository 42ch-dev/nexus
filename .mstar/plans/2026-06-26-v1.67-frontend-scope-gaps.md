# Plan — V1.67 P1: Frontend Scope Gaps (de-risk V1.68)

**plan_id**: `2026-06-26-v1.67-frontend-scope-gaps`
**Iteration**: V1.67 · **Wave**: 2 · **Track**: B · **Effort**: M
**Owner**: `@frontend-dev`
**Status**: Todo
**Depends on**: P0 merged (shared `apps/web` surface)
**Working branch**: `feature/v1.67-frontend-scope-gaps`
**Compass**: §1.1 Track B, §1.3 Wire note (G1/G2 frontend-only)

## Goal

Close the V1.64 frontend scope gap that is a prerequisite for the V1.68 authoring iteration, plus the **transport half** of G2. **Both frontend-only** (verified §1.3: wire already carries `work_profile`; daemon already ships preset CRUD routes + contracts). No schema/contract change — only TS interface promotion + a UI control. **Note (revised 2026-06-26 Q6): the form-based preset-management UI half of G2 is deferred to the V1.68 canvas** (the canvas Strategy surface supersedes a form UI); V1.67 ships ONLY the TS-client promotion.

## Residual coverage

- `R-V164-P2-G1` — work_profile field UI (full close). Wire types already carry `work_profile: Option<String>`; Create-Work dialog lacks the selector. Add UI control + wire.
- `R-V164-P2-G2` — preset CRUD TS-client promotion (**transport half only; UI half deferred to V1.68 canvas per compass §0 Q6**). Daemon ships `GET/PATCH/DELETE /v1/local/presets/{id}` + contracts; TS generated types exist. Promote 3 methods on TS `NexusClient` (21 → 24). The form-based management UI is **not** built in V1.67 (canvas supersedes it).

## Acceptance criteria

- AC1: Create-Work dialog exposes a `work_profile` selector (novel/essay/game-bible/script) wired to the existing field.
- AC2: TS `NexusClient` interface promotes `getPreset`/`updatePreset`/`deletePreset` (21 → 24 methods); generated TS types confirmed present (compass §5 #5 LOCKED). **No form-based preset-management UI is built** (deferred to V1.68 canvas per §0 Q6).
- AC4: No schema change (verified: `check-wire-drift` clean). `@42ch/nexus-contracts` version unaffected by G2 alone (TS types already generated; compass §5 #5 LOCKED). If batched with P0's 0.5.0→0.6.0 bump on the same integration branch, coordinate — but G2 adds no new wire surface.
- AC5: `pnpm --filter web typecheck/build/test` clean; vitest coverage for new UI.

## Tasks (skeleton)

- T1: G1 — work_profile selector in Create-Work dialog + patch wiring.
- T2: G2 (transport half) — TS interface promotion (3 methods: getPreset/updatePreset/deletePreset) + generated-types check.
- T3: tests + typecheck/build.

## Out of scope

P2-G3 (CapabilityInfo admission-gate UI — V1.68). Backend preset routes (already shipped). **Preset-management form UI (G2 UI half — V1.68 canvas)**. Canvas surfaces (V1.68). Body editor (rejected).

## Dependencies / branch policy

Topic branch `feature/v1.67-frontend-scope-gaps` from `iteration/v1.67`. **After P0 merges** (both touch `apps/web`; avoid dual-frontend-write conflict). Merge target `iteration/v1.67`.
