# Plan — V1.67 P1: Frontend Scope Gaps (de-risk V1.68)

**plan_id**: `2026-06-26-v1.67-frontend-scope-gaps`
**Iteration**: V1.67 · **Wave**: 2 · **Track**: B · **Effort**: M
**Owner**: `@frontend-dev`
**Status**: Todo
**Depends on**: P0 merged (shared `apps/web` surface)
**Working branch**: `feature/v1.67-frontend-scope-gaps`
**Compass**: §1.1 Track B, §1.3 Wire note (G1/G2 frontend-only)

## Goal

Close the 2 V1.64 frontend scope gaps that are prerequisites for the V1.68 authoring iteration. **Both frontend-only** (verified §1.3: wire already carries `work_profile`; daemon already ships preset CRUD routes + contracts). No schema/contract change — only TS interface promotion + UI.

## Residual coverage (2)

- `R-V164-P2-G1` — work_profile field UI. Wire types already carry `work_profile: Option<String>` (`create-work`/`patch-work`-request + work-detail-response schemas + generated Rust DTOs). Create-Work dialog lacks the selector. Add UI control + wire.
- `R-V164-P2-G2` — preset CRUD TS-client + UI. Daemon ships `GET/PATCH/DELETE /v1/local/presets/{id}` + contracts (`GetPresetResponse`/`UpdatePresetRequest`/`UpdatePresetResponse`); TS `NexusClient` interface (21 methods) was not promoted. Promote 3 methods (21 → 24) + add management UI.

## Acceptance criteria

- AC1: Create-Work dialog exposes a `work_profile` selector (novel/essay/game-bible/script) wired to the existing field.
- AC2: TS `NexusClient` interface promotes `getPreset`/`updatePreset`/`deletePreset` (21 → 24 methods); generated TS types confirmed present (or contracts refresh done per `@architect` Phase 2b §5 #5).
- AC3: Preset management UI surface (view/edit/delete user presets; embedded presets read-only) consumes the promoted methods.
- AC4: No schema change (verified: `check-wire-drift` clean). `@42ch/nexus-contracts` version unaffected by G2 alone (TS types already generated; compass §5 #5 LOCKED). If batched with P0's 0.5.0→0.6.0 bump on the same integration branch, coordinate — but G2 adds no new wire surface.
- AC5: `pnpm --filter web typecheck/build/test` clean; vitest coverage for new UI.

## Tasks (skeleton)

- T1: G1 — work_profile selector in Create-Work dialog + patch wiring.
- T2: G2 — TS interface promotion (3 methods) + generated-types check.
- T3: G2 — preset management UI surface.
- T4: tests + typecheck/build.

## Out of scope

P2-G3 (CapabilityInfo admission-gate UI — V1.68). Backend preset routes (already shipped). Body editor (V1.68).

## Dependencies / branch policy

Topic branch `feature/v1.67-frontend-scope-gaps` from `iteration/v1.67`. **After P0 merges** (both touch `apps/web`; avoid dual-frontend-write conflict). Merge target `iteration/v1.67`.
