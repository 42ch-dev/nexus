# Plan — V1.67 P1: Frontend Scope Gaps (de-risk V1.68)

**plan_id**: `2026-06-26-v1.67-frontend-scope-gaps`
**Iteration**: V1.67 · **Wave**: 2 · **Track**: B · **Effort**: M
**Owner**: `@frontend-dev`
**Status**: Todo
**Depends on**: P0 merged (shared `apps/web` surface)
**Working branch**: `feature/v1.67-frontend-scope-gaps`
**Compass**: §1.1 Track B, §1.3 Wire note (G1/G2 frontend-only)

## Goal

Close the V1.64 frontend scope gap that is a prerequisite for the V1.68 authoring iteration, plus the **transport half** of G2. **Both frontend-only** (verified §1.3: wire already carries `work_profile`; daemon already ships preset CRUD routes + contracts). No schema/contract change — only TS interface promotion + a UI control. **Note (revised 2026-06-26 Q6): the form-based preset-management UI half of G2 is deferred to the V1.68 canvas** (the canvas Strategy surface supersedes a form UI); V1.67 ships ONLY the TS-client promotion.

## Residual coverage

- `R-V164-P2-G1` — work_profile field UI (full close). Wire types already carry `work_profile: Option<String>`; Create-Work dialog lacks the selector. Add UI control + wire.
- `R-V164-P2-G2` — preset CRUD TS-client promotion (**transport half only; UI half deferred to V1.68 canvas per compass §0 Q6**). Daemon ships `GET/PATCH/DELETE /v1/local/presets/{id}` + contracts; TS generated types exist. Promote 3 methods on TS `NexusClient` (21 → 24). The form-based management UI is **not** built in V1.67 (canvas supersedes it).

## Acceptance criteria

- AC1: Create-Work dialog exposes a `work_profile` selector (novel/essay/game-bible/script) wired to the existing field.
- AC2: TS `NexusClient` interface promotes `getPreset`/`updatePreset`/`deletePreset` (21 → 24 methods); generated TS types confirmed present (compass §5 #5 LOCKED). **No form-based preset-management UI is built** (deferred to V1.68 canvas per §0 Q6).
- AC4: No schema change (verified: `check-wire-drift` clean). `@42ch/nexus-contracts` version unaffected by G2 alone (TS types already generated; compass §5 #5 LOCKED). If batched with P0's 0.5.0→0.6.0 bump on the same integration branch, coordinate — but G2 adds no new wire surface.
- AC5: `pnpm --filter web typecheck/build/test` clean; vitest coverage for new UI.

## Tasks (skeleton)

- T1: G1 — work_profile selector in Create-Work dialog + patch wiring.
- T2: G2 (transport half) — TS interface promotion (3 methods: getPreset/updatePreset/deletePreset) + generated-types check.
- T3: tests + typecheck/build.

## Out of scope

P2-G3 (CapabilityInfo admission-gate UI — V1.68). Backend preset routes (already shipped). **Preset-management form UI (G2 UI half — V1.68 canvas)**. Canvas surfaces (V1.68). Body editor (rejected).

## Dependencies / branch policy

Topic branch `feature/v1.67-frontend-scope-gaps` from `iteration/v1.67`. **After P0 merges** (both touch `apps/web`; avoid dual-frontend-write conflict). Merge target `iteration/v1.67`.

## Completion Report v2

**Agent**: frontend-dev
**Task**: V1.67 P1 — Frontend Scope Gaps (G1 work_profile selector + G2 preset-CRUD TS-client promotion, transport half only)
**Status**: Delivered (implementation complete; ready for PM → InReview → QC/QA gate)
**Working branch used**: `feature/v1.67-frontend-scope-gaps`

**Scope Delivered** — both residuals fully closed, frontend-only, no schema/contract change:

- **`R-V164-P2-G1` (work_profile selector) — full close.** The Create-Work dialog now exposes a native `Select` (novel / essay / game-bible / script) defaulting to `novel`, wired to the existing `work_profile` field on `CreateWorkRequest`. An untouched form yields the same outcome as V1.66 (a novel-profile Work) — no behavior change.
- **`R-V164-P2-G2` (preset CRUD TS-client promotion) — transport half.** `getPreset` / `updatePreset` / `deletePreset` promoted onto the `NexusClient` interface (21 → 24 methods); wired in `BrowserClient`; inherited unchanged by `TauriClient` (§5 #1 thin-augmentation architecture). Generated TS types `GetPresetResponse` / `UpdatePresetRequest` / `UpdatePresetResponse` confirmed present in `@42ch/nexus-contracts` (compass §5 #5 LOCKED). **No form-based management UI built** (deferred to V1.68 canvas per §0 Q6).

**Artifacts** (3 commits, all on `feature/v1.67-frontend-scope-gaps`):

- `e74321db` — T2 transport: `apps/web/src/lib/nexus/types.ts` (interface + header-note refresh), `browser-client.ts` (3 methods + private `delete()` transport helper), `tauri-client.ts` (doc count 21 → 24), `apps/web/AGENTS.md` (preset row resolved).
- `963fa1ed` — T1 selector: `apps/web/src/pages/dialogs/create-work-dialog.tsx` (`Select` + `work_profile` wiring), `create-work-dialog.test.tsx` (assertion updated + new selector-change test), `apps/web/AGENTS.md` (work_profile row resolved).
- `aeaaf91a` — T3 tests: `apps/web/src/lib/nexus/browser-client.test.ts` (new "BrowserClient preset CRUD" block: getPreset / updatePreset / deletePreset-on-204).

**Validation** (AC5, all green):
- `pnpm --filter @42ch/nexus-contracts run build` — dist rebuilt (82.09 KB `.d.ts`).
- `pnpm --filter web typecheck` — clean (`tsc --noEmit`).
- `pnpm --filter web build` — built (2163 modules; pre-existing chunk-size warning only).
- `pnpm --filter web test` — **114 passed / 114** across 15 files (includes the new selector + 3 preset-CRUD client tests).
- `bash tooling/check-wire-drift.sh` (AC4) — **clean** (4 drift tests ok; confirms no schema change; contracts version unaffected).

**Issues/Risks**: None. The interface change is purely additive; the only `NexusClient` implementers are `BrowserClient` (+ `TauriClient` via inheritance); the test `noopClient` uses an unsafe cast so the 3 new methods do not break it. GitNexus impact on the TS `NexusClient` interface returned "not found" (the index is Rust-focused); scope was verified manually as contained to `apps/web`. Stale `apps/web/AGENTS.md` "Contracts status" rows for both residuals were updated to "Resolved (V1.67 …)" as traceable housekeeping.

**Plan Update**: Tasks T1, T2, T3 delivered; both residuals closed. **Not** marked `Done` (PM/QA gate authority). `status.json` / residual lifecycle intentionally **not** edited by this leaf executor.

**Handoff**: PM to merge `feature/v1.67-frontend-scope-gaps` → `iteration/v1.67`, then dispatch QC tri-review + aligned QA. Form-based preset-management UI (G2 UI half) and `R-V164-P2-G3` admission-gate UI remain deferred to the V1.68 canvas (compass §0 Q6 / §1.3).

**Git**: `git log --oneline` (this plan's 3 commits) —
```
aeaaf91a test(web): cover promoted preset get/update/delete client methods (R-V164-P2-G2)
963fa1ed feat(web): add Work-profile selector to Create-Work dialog (R-V164-P2-G1)
e74321db feat(web): promote preset get/update/delete onto NexusClient (R-V164-P2-G2 transport)
```

