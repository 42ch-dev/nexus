# 2026-07-01-v1.78-slate-clear (P1)

**Iteration**: V1.78 · **Plan**: P1 (Slate-Clear companion) · **Status**: Todo · **Owner**: `@project-manager` (coord) → `@frontend-dev` (findings-frontend 7 + world-kb frontend nits) + `@fullstack-dev` (world-kb backend 2)
**Compass**: [v1.78-…-compass-v1.md §3 Wave 1 + §5](../iterations/v1.78-creator-memory-review-loop-ui-and-slate-clear-compass-v1.md)
**Depends on**: P-1 (compass Locked)

## Goal

Close all 12 open V1.77-QC-followup residuals (7 findings-frontend + 5 world-kb, all low/nit). File-disjoint from P0 except shared `apps/web/src/api/queries.ts` + `types.ts` — PM sequences the findings `queries.ts` nits to land before P0-Batch2 memory additions (or merges cleanly at integration; both additive).

## Scope — residual-by-residual

### Findings-frontend (`@frontend-dev`, 7)

| Residual | Severity | Scope | Close action |
|----------|----------|-------|--------------|
| `R-V177P0-QC1-S001` | nit | `apps/web/src/api/queries.ts` (useFinding forward-staging marker) | Add the qc1-suggested forward-staging marker comment/closure. |
| `R-V177P0-QC1-S002` | low | `apps/web/src/components/findings/finding-detail-panel.tsx` (305-line split) | Split the panel under the ≤250-line module discipline (mirrors V1.74 qc1 F-001 pattern). |
| `R-V177P0-QC1-S003` | low | `apps/web/src/lib/findings-lifecycle.ts` (client adjacency golden test vs DAO) | Add a golden test asserting the client adjacency table matches the DAO source (`crates/nexus-local-db/src/findings.rs:172-189`). |
| `R-V177P0-QC1-S004` | nit | `apps/web/src/api/queries.ts` (getQueriesData/setQueryData asymmetry note) | Add the rationale comment for the asymmetry. |
| `R-V177P0-QC2-S001` | nit | `finding-detail-panel.tsx` (client-side validation guard defense-in-depth) | Add the client-side disable guard for invalid transitions (defense-in-depth; server already enforces via 422). |
| `R-V177P0-QC2-S002` | nit | `finding-detail-panel.test.tsx` (detail-panel rollback assertion) | Add the optimistic-rollback assertion test. |
| `R-V177P0-QC3-S001` | low | `apps/web/src/api/findings-mutation.test.tsx` (cross-Work invalidation-breadth regression test) | Add the cross-Work invalidation-breadth regression test. |

### World-kb (`@fullstack-dev` / `@frontend-dev` / `@project-manager`, 5)

| Residual | Severity | Scope | Owner | Close action |
|----------|----------|-------|-------|--------------|
| `R-V177P1-QC1-S001` | nit | `.mstar/status.json` (register R-V176QC1-S002 open residual row) | `@project-manager` | Register the missing residual row (data hygiene). |
| `R-V177P1-QC1-S002` | low | `crates/nexus-daemon-runtime` world_kb tests (GRAPH_RELATIONSHIP_CAP exhaustion regression test) | `@fullstack-dev` | Add the cap-exhaustion regression test. |
| `R-V177P1-QC1-S003` | nit | `world-kb-canvas.tsx` (PROMOTE_BATCH_SIZE rationale comment) | `@frontend-dev` | Add the rationale comment. |
| `R-V177P1-QC2-S001` | nit | `world_kb.rs` + `world-kb-canvas.tsx` (rationale comments on new constants) | `@fullstack-dev` | Add rationale comments on the V1.76-introduced constants. |
| `R-V177P1-QC3-S001` | low | `suggested-relationships-pane.tsx` (bulk Promote-all progress/failure copy) | `@frontend-dev` | Add progress/failure copy for the bulk Promote-all action. |

## Branch policy

- Working branch: `feature/v1.78-slate-clear` (from `iteration/v1.78`); worktree `.worktrees/v1.78-slate/`.
- Merge target: `iteration/v1.78`. Shared-file nits (`queries.ts`) land first; PM coordinates with P0-Batch2.

## Acceptance (DoD — see compass §7 #7)

1. All 12 residuals `lifecycle: resolved` in `status.json` `residual_findings` (with `resolution.commit` + `resolution.plan_id`).
2. Slate at 0 open (or new QC → V1.79 pointers).
3. `metadata.tech_debt_summary` refreshed (P-last responsibility, but P1 close triggers the slate→0 state).

## Wire contracts

`wire_contracts_changed: FALSE` (slate-clear — comment/test/copy/data only; no schema or runtime behavior change).
