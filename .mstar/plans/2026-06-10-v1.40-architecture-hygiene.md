# V1.40 Architecture Hygiene — Plan

**Plan ID**: `2026-06-10-v1.40-architecture-hygiene`  
**Title**: P0.5 — embedded-rules relocation + World KB runtime architecture doc  
**Created**: 2026-06-10  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.40-architecture-hygiene`  
**Merge target**: `iteration/v1.40`  
**Depends on**: `2026-06-10-v1.40-harness-docs-prepare`  
**Iteration compass**: [v1.40-novel-world-kb-delivery-compass-v1.md](../iterations/v1.40-novel-world-kb-delivery-compass-v1.md)  
**Primary spec**: [novel-workflow-profile.md](../knowledge/specs/novel-workflow-profile.md) §5.5.4  
**Knowledge SSOT**: [world-kb-runtime-architecture.md](../knowledge/world-kb-runtime-architecture.md)

---

## 1. Problem Statement

V1.39 shipped Layer 1 rules at `embedded-presets/rules/writing-craft.md`, which is not a preset (no `preset.yaml`) and conflicts with spec path `embedded-rules/`. World KB layering was undocumented; grill-me locked crate boundaries before P1–P3 implement.

---

## 2. Goals

1. Move `writing-craft.md` to `crates/nexus-orchestration/embedded-rules/` with `include_dir!` or equivalent module (parallel to `embedded-skills/`).
2. Update `read_rules_layers()` and tests to load from `embedded-rules`, not `embedded-presets/rules`.
3. Align deferred tracker / shipped notes that cite wrong path.
4. Ship [world-kb-runtime-architecture.md](../knowledge/world-kb-runtime-architecture.md) (grill-me locked; no spec master change required).

---

## 3. Non-goals

- World KB taxonomy, query, or ingest (P1–P3).
- Wiring `~/.nexus42/rules/writing-craft.md` user override if not already read (may be follow-up in P4).
- `novel-review-master` child schedule (P3 tail).

---

## 4. Acceptance Criteria

1. No `embedded-presets/rules/` directory in tree after ship.
2. `read_rules_layers` hermetic tests pass with `embedded-rules` path.
3. `novel-workflow-profile.md` §5.5.4 path matches implementation.
4. `list_embedded_presets()` unchanged — `rules` never appeared as preset id.
5. Architecture knowledge doc linked from V1.40 compass.

---

## 5. Tasks

- [ ] T1: Add `embedded-rules/` module + move `writing-craft.md`.
- [ ] T2: Update `stage_gates.rs` `read_rules_layers` + embedded template reader.
- [ ] T3: Fix unit tests in `stage_gates.rs`.
- [ ] T4: Remove `embedded-presets/rules/`; grep for stale paths.
- [ ] T5: Update DF-65 tracker note path (docs only if code path fixed).
- [ ] T6: Verify architecture knowledge doc present and compass-linked.

---

## 6. Verification

```bash
cargo test -p nexus-orchestration -- read_rules_layers 2>&1 | tail -15
rg -n 'embedded-presets/rules' crates/ .mstar/ || true
```

---

## 7. Explicitly out

| ID | Reason |
| --- | --- |
| DF-63 W2–W5 | P1–P3 |
| P4 hygiene sweep | P4 |
