# V1.34 Agent Tool Implementation — Locked Plan

**Plan ID**: `2026-06-04-v1.34-agent-tool-implementation`  
**Title**: P4 — HostToolExecutor + worker upcall + tests  
**Created**: 2026-06-04  
**Status**: Done  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.34-agent-tool-implementation`  
**Merge target**: `feature/v1.34-creator-workflow-and-agent-tools`  
**Depends on**: `2026-06-04-v1.34-agent-tool-registry-spec`  
**Primary spec**: [agent-nexus-tool-bridge.md](../knowledge/specs/agent-nexus-tool-bridge.md)  

---

## DF-47 Disposition: **CLOSED**

P4 completed the unified dispatch table (HostToolExecutor::execute handles both
HTTP tool execute and worker upcall via dispatch_from_worker). Per spec §12:
DF-47 is **not registered** — no deferred tracker entry needed.

---

## 1. Problem Statement

External agents cannot call Nexus Work/context tools through a unified, audited daemon path. Worker upcalls and HTTP tool execute are split.

---

## 2. Goals

1. Implement handler registry in `nexus-daemon-runtime` (extend `host_tool_executor.rs`).
2. Register V1.34 minimal `nexus.*` tools per P3.
3. Route `worker/agent_tool_request` through same registry.
4. Integration tests: whoami, work.get, patch, assemble policy_blocked path.
5. Audit logging for `nexus.*` invocations.

---

## 3. Non-goals

- Full acp-capability-set (DF-46)
- MCP server (DF-49)
- CLI subprocess bridge (DF-48)

---

## 4. Tasks

- [x] T1: Registry module + handlers delegating to existing Local API
- [x] T2: Wire worker upcall dispatch
- [x] T3: Permission + active-creator tests
- [x] T4: Document Completion Report: DF-47 closed or open

---

## 5. Acceptance

1. Mock agent tool call returns Work JSON for active creator.
2. Cross-creator work_id returns 403.
3. assemble returns `policy_blocked` when platform paused (or local slice when wired).
4. If registry unification incomplete → P5 registers **DF-47** only.

---

## 6. Verification

```bash
cargo test -p nexus-daemon-runtime host_tool
cargo test -p nexus-daemon-runtime works_api
cargo clippy -p nexus-daemon-runtime -- -D warnings
```
