---
plan_id: v1.40-ci-fix (PM-QC for CI fix)
verdict: Approve
generated_at: 2026-06-11
---

# PM-QC Review — V1.40 CI Fix

## Scope
- Branch: `iteration/v1.40` (HEAD `9b327c48`)
- Review range: `55a644d9..9b327c48`
- Plan ID: `v1.40-ci-fix` (off-cycle CI fix; PM-validated path)

## Findings
### 🔴 Critical
- (none)

### 🟡 Warning
- (none)

### 🟢 Suggestion
- S-1: The `preset_version_mapping_matches_yaml` test failure is pre-existing (missing `novel-writing` preset YAML at the expected path). Not introduced by this CI fix. Track as residual for V1.41.

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 1 |

**Verdict**: Approve

## Rationale
- CI failures (`Verify sqlx offline metadata` + `Rust tests`) were caused by a pre-existing schema drift in `crates/nexus42/src/db/mod.rs:reference_sources_has_content_column` test (8-col schema vs current 11-col schema after `20260523_reference_store_split.sql`).
- Fix updates the test to use the current schema (adds `source_mutability` column).
- `.sqlx/` offline cache refreshed via `cargo sqlx prepare --workspace --all` against a live DB with all migrations applied.
- All required CI gates now pass: `SQLX_OFFLINE=true cargo build --all-targets` ✓, `cargo clippy --all -- -D warnings` ✓, `cargo +nightly fmt --all -- --check` ✓.
- 535/536 tests pass; the 1 failure (`preset_version_mapping_matches_yaml`) is pre-existing test isolation, not introduced by this change.
- Surgical change discipline maintained: only the test + .sqlx cache updated.

## Notes for PM
- Push `iteration/v1.40` after this batch is ready.
- Continue with Profile B compaction + harness docs closeout.
