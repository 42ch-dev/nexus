# QA Verification Report — WS1 `nexus-acp-host` crate extraction

**Plan ID**: `2026-04-17-v1.4-ws1-acp-host-extraction`
**Date**: 2026-04-17
**QA Engineer**: `qa-engineer`

## Scope

- **Branch**: `feature/v1.4` (WS1 merged)
- **Diff basis**: `git diff main...HEAD -- crates/ .github/workflows/ Cargo.toml Cargo.lock`

## Verification Results

| # | Criterion | Command | Result |
|---|-----------|---------|--------|
| 1 | `cargo build --workspace` clean | `cargo build --workspace` | ✅ PASS |
| 2 | `cargo test --workspace` green | `cargo test --workspace` | ✅ PASS (0 failures) |
| 3 | `cargo +nightly fmt --all -- --check` clean | `cargo +nightly fmt --all -- --check` | ✅ PASS |
| 4 | `cargo clippy --all -- -D warnings` clean | `cargo clippy --all -- -D warnings` | ✅ PASS |
| 5 | `nexus42 agent list` functionally unchanged | `cargo run -p nexus42 -- agent list` | ✅ PASS (27 agents listed) |
| 6 | `nexus42 agent show claude-acp` functionally unchanged | `cargo run -p nexus42 -- agent show claude-acp` | ✅ PASS |
| 7 | `nexus42 acp-worker --creator test-id` returns JSON, exits 0 | `cargo run -p nexus42 -- acp-worker --creator test-id` | ✅ PASS |
| 8 | `acp-worker` hidden from `--help` | `cargo run -p nexus42 -- --help \| grep acp-worker` | ✅ PASS (no output) |
| 9 | CI linkage gate verified | `rg nexus-acp-host\|agent-client-protocol crates/nexus42d/Cargo.toml` | ✅ PASS (no match) |
| 10 | Moved tests green | `cargo test -p nexus-acp-host --tests` | ✅ PASS (28/28) |

## Findings

- **Critical**: None
- **High/Medium/Low**: None

## Sign-off

**Status**: **APPROVE** — All 10 acceptance criteria pass. No regressions. WS1 is verified complete.
