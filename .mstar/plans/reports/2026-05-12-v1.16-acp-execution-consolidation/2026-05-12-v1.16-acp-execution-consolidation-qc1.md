---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-05-12-v1.16-acp-execution-consolidation"
verdict: "Approve"
generated_at: "2026-05-13"
---

# Code Review Report — QC #1 (Architecture)

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: glm-5
- Review Perspective: Architecture — command surface organization, delegation patterns, module boundaries
- Report Timestamp: 2026-05-13T05:00:00Z

## Scope
- plan_id: `2026-05-12-v1.16-acp-execution-consolidation`
- Review range / Diff basis: `28a4903..HEAD`
- Working branch (verified): `feature/v1.16-acp-execution-consolidation`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus`
- Files reviewed: 10
- Commit range: `28a4903..HEAD`
- Tools run: `cargo clippy -p nexus42 -- -D warnings`, `cargo test -p nexus42 --test command_surface_contract`

## Findings

### 🔴 Critical

None.

### 🟡 Warning

None.

### 🟢 Suggestion

**S-001: Consider future extraction of `acp.rs` subcommand groups**

- Location: `crates/nexus42/src/commands/acp.rs` (1246 lines)
- Issue: The file now contains 7 subcommand groups (Registry, Agent, Skills, Session, Policy, Permission, Run) plus the `cmd_run` implementation (~200 lines). While the current size is acceptable for a central command module, continued growth may impact maintainability.
- Fix: Consider a future refactoring to extract subcommand groups into separate files (e.g., `acp/session.rs`, `acp/policy.rs`) if more subcommands are added in subsequent plans. The current delegation pattern from shims (`session.rs`, `policy.rs`, etc.) already demonstrates how to organize cross-module calls without circular dependencies.
- Confidence: Medium
- Note: This is a forward-looking suggestion, not a current blocker. The file remains coherent and well-organized with clear section markers.

**S-002: Deprecation notice format is consistent and well-implemented**

- Location: `session.rs`, `policy.rs`, `permission.rs`, `explore.rs`, `doctor.rs`, `agent.rs`
- Issue: None — positive observation.
- Note: All shims follow the same pattern: `eprintln!("Note: `nexus42 {old}` is deprecated. Use `nexus42 {new}` instead.");` followed by delegation to the new path. This ensures backward compatibility while guiding users to the V2 API surface.

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|---|---|---|---|
| S-001 | manual-reasoning | `wc -l acp.rs` = 1246, `git diff 28a4903..HEAD --stat` | Medium |
| S-002 | git-diff | `git diff 28a4903..HEAD crates/nexus42/src/commands/*.rs` | High |

## Architecture Analysis

### Command Migration Verification

| Legacy Path | New Path | Shim Present | Delegation Correct |
|---|---|---|---|
| `session` | `acp session` | ✅ `session.rs:59` | ✅ `super::session::run()` |
| `policy` | `acp policy` | ✅ `policy.rs:52` | ✅ `super::policy::run()` |
| `permission` | `acp permission` | ✅ `permission.rs:79` | ✅ `super::permission::run()` |
| `explore` | `platform explore` | ✅ `explore.rs:100` | ✅ `super::explore::run()` |
| `doctor` | `system doctor` | ✅ `doctor.rs:46` | ✅ `super::doctor::run()` |
| `agent run` | `acp run` | ✅ `agent.rs:93` | ✅ `super::acp::cmd_run()` |

### Delegation Pattern Consistency

All shims follow the identical pattern:
1. Print deprecation notice via `eprintln!`
2. Delegate to the corresponding V2 implementation via `super::<module>::run()`

The pattern is **consistent across all 6 migrated commands**.

### Module Boundary Analysis

Dependency graph verification:

```
acp.rs ──► session.rs  (delegation, no back-reference)
acp.rs ──► policy.rs   (delegation, no back-reference)
acp.rs ──► permission.rs (delegation, no back-reference)
agent.rs ──► acp.rs   (delegation for cmd_run)
platform.rs ──► explore.rs (delegation)
system.rs ──► doctor.rs (delegation)
```

**No circular dependencies detected.** All delegations flow from the V2 entry point to the legacy implementation module without back-references.

### Contract Tests Verification

All 6 new V2 target tests pass:
- `v2_target_acp_session_subcommands` — ✅
- `v2_target_acp_policy_subcommands` — ✅
- `v2_target_acp_permission_subcommands` — ✅
- `v2_target_acp_run` — ✅
- `v2_target_platform_explore_subcommands` — ✅
- `v2_target_system_doctor` — ✅

Total: 21 tests passed (including 6 new), 0 failed.

### Lint Status

`cargo clippy -p nexus42 -- -D warnings` — **Clean** (no errors, no warnings).

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 2 |

**Verdict**: **Approve**

The implementation correctly follows the plan's delegation/shim pattern:
- All command migrations are complete with proper deprecation notices
- Delegation pattern is consistent across all 6 migrated commands
- No circular dependencies between modules
- Contract tests verify all new V2 command surface positions
- Lint passes clean

The suggestion S-001 is a forward-looking maintainability note for future consideration, not a current blocker.

## Cross-Reviewer Ready Notes

For QC #2 (Security) and QC #3 (Performance):

- The `cmd_run` implementation moved from `agent.rs` to `acp.rs` includes subprocess spawning via `AgentSpawner`. QC #2 should verify input validation on `agent_ref` and potential injection surfaces.
- The interactive prompt loop in `interactive_prompt_loop()` currently notes "ACP prompt integration pending". This is intentional per plan (deferred to follow-up). QC #3 may note resource lifecycle handling in the subprocess management.
- No changes to daemon-side code; all changes are CLI surface refactoring.