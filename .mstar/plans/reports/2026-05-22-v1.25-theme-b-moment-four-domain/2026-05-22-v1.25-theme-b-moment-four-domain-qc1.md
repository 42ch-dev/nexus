---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-05-22-v1.25-theme-b-moment-four-domain"
verdict: "Approve"
generated_at: "2026-05-22"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: k2p6
- Review Perspective: Architecture coherence and maintainability risk
- Report Timestamp: 2026-05-22T17:45:00Z

## Scope
- plan_id: 2026-05-22-v1.25-theme-b-moment-four-domain
- Review range / Diff basis: main...feature/v1.25-knowledge-product-wiring
- Working branch (verified): feature/v1.25-knowledge-product-wiring
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 7
- Commit range: e130f20..42f2f34 (3 commits)
- Tools run: cargo test, cargo clippy --all -- -D warnings, cargo build --bin nexus42, grep verification

## Findings

### 🔴 Critical
- None

### 🟡 Warning
- None

### 🟢 Suggestion
- **S1: `#[allow(clippy::future_not_send)]` justification** — `run_assemble_moment_demo` suppresses `clippy::future_not_send` without a brief comment explaining why the Future is not Send. Consider adding a one-line comment (e.g., "InMemory stores use non-Send internal buffers in test mode") so future readers know this is a known, bounded limitation of the in-memory demo path, not a latent bug. Not blocking.

## Source Trace
- Finding ID: S1
- Source Type: manual-reasoning
- Source Reference: crates/nexus42/src/commands/platform/context.rs:494
- Confidence: Medium

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 1 |

**Verdict**: Approve

## Review Checklist Evidence

### 1. Scope compliance — in-memory only, no persistence/daemon/cloud creep
- **PASS**. The implementation exclusively uses `InMemoryNarrativeGateway`, `InMemoryKbStore`, and `InMemoryKnowledgeStore`. Grep verification across `crates/nexus-narrative/`, `crates/nexus-kb/`, and `crates/nexus-knowledge/` confirms no `sqlite`, `rusqlite`, or `sqlx` runtime dependencies are introduced. The `Cargo.toml` additions (`nexus-narrative`, `nexus-kb`, `nexus-knowledge`) are workspace-internal crate references only.

### 2. Stage-0 vs Moment split — cli-spec clearly distinguishes
- **PASS**. The `cli-spec.md` updates (§6.2F, §6.6, §6.7) explicitly separate:
  - **Shipped Stage-0/TwoStage path**: `nexus42 platform context assemble-local` — reads Creator SOUL, long-term memory, local fragments; does NOT call four-domain `assemble_moment`.
  - **Experimental four-domain path**: `nexus42 platform context assemble-moment` — calls `assemble_moment` with in-memory seeded fixtures; labeled "experimental, in-memory demo only".
  The spec language is unambiguous and correctly warns against conflating the two paths.

### 3. Hidden command — `#[command(hide = true)]` applied correctly
- **PASS**. The `AssembleMoment` variant carries `#[command(hide = true)]`. Verified by building the binary and running `nexus42 platform context --help` — the command does not appear in help output.

### 4. Integration test quality — proves four-domain path reachable from nexus42
- **PASS**. `crates/nexus42/tests/assemble_moment_demo.rs` contains 4 deterministic tests:
  1. `assemble_moment_demo_produces_four_domain_context` — asserts all 4 domains present (Stage-0, World state, Timeline, World KB, User Knowledge).
  2. `assemble_moment_demo_with_custom_ids` — custom world/user IDs produce matching fixtures.
  3. `assemble_moment_demo_non_seeded_ids_yield_stage0_only` — default `MomentContext` has only Stage-0 when unseeded.
  4. `assemble_moment_demo_context_heading_order` — verifies spec-ordered heading structure (`Stage0 < World State < Timeline < World KB < User Knowledge`).
  All tests pass (`cargo test --test assemble_moment_demo`: 4/4 OK). Tests use only in-memory fixtures with no I/O or network.

### 5. No behavioral regression — existing Stage0/TwoStage unchanged
- **PASS**. The `assemble_local_with_routing` function and all existing Stage-0/TwoStage code paths are untouched. Regression test suite passes:
  - `cargo test --test command_surface_contract`: 23/23 OK
  - `cargo test --test cli_agent`: 18/18 OK
  - `cargo clippy --all -- -D warnings`: clean (0 warnings)

### 6. InMemory store usage pattern matches library tests
- **PASS**. The `run_assemble_moment_demo` function follows the identical fixture-and-assemble pattern from `nexus-moment-context-assembly/tests/assembly_integration.rs`:
  - `InMemoryNarrativeGateway::new(InMemoryKbStore::new())`
  - `InMemoryKbStore::new()`
  - `InMemoryKnowledgeStore::new()`
  - Seed → `assemble_moment(&request, &narrative, &kb, &knowledge).await`
  This ensures the CLI demo path is consistent with the library's canonical integration test shape.

### 7. cli-spec accurately describes experimental/demo posture
- **PASS**. The spec documents:
  - "Experimental (V1.25 Theme B)"
  - "In-memory demo only — uses InMemoryNarrativeGateway, InMemoryKbStore, InMemoryKnowledgeStore with seeded fixtures"
  - "No persistence, daemon route, or cloud sync"
  - "Distinct from shipped Stage-0/TwoStage path"
  - "Hidden from help"
  The documentation posture matches the implementation exactly.
