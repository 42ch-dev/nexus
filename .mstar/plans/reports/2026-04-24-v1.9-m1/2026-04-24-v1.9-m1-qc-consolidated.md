---
report_kind: qc-consolidated
consolidator: project-manager
plan_id: 2026-04-24-v1.9-m1
verdict: Request Changes
generated_at: "2026-04-24"
---

# QC Consolidated Report — V1.9 M1

## Review Summary

| Reviewer | Verdict | Critical | Warning | Suggestion |
|----------|---------|----------|---------|------------|
| @qc-specialist (Architecture) | Request Changes | 3 | 5 | 4 |
| @qc-specialist-2 (Security) | Request Changes | 1 | 4 | 2 |
| @qc-specialist-3 (Performance) | Approve | 0 | 4 | 3 |
| **Consolidated** | **Request Changes** | **4** | **13** | **9** |

### Reports
- `2026-04-24-v1.9-m1-qc1.md` — Architecture & Maintainability
- `2026-04-24-v1.9-m1-qc2.md` — Security & Correctness
- `2026-04-24-v1.9-m1-qc3.md` — Performance & Reliability

## Must-Fix Before Approve (Critical + Blocking Warning)

### 🔴 Critical

| ID | Source | Title | Scope | Fix |
|----|--------|-------|-------|-----|
| F-001 | QC1 | `user_preset_base_dir` duplicated across `nexus-home-layout` and `nexus-orchestration` (ADR-014 violation) | `crates/nexus-orchestration/src/user_preset_dir.rs` | Import from `nexus_home_layout` instead of redefining |
| F-002 | QC1 | `scan_user_presets_local` duplicates scanning logic from `nexus-orchestration::user_preset_dir` | `crates/nexus42/src/commands/preset.rs` | Expose lightweight list function from orchestration crate |
| F-003 | QC1 | `resolve_preset` error matching relies on string search `"not found"` inside `PresetLoadError::Validation` | `crates/nexus-orchestration/src/preset/mod.rs` | Add `NotFound` variant to `PresetLoadError` or return `Option<LoadedPreset>` |
| F-QC2-01 | QC2 | `drive_to_running()` test helper dispatches inert `Event::ProcessStarted` with no handler in Starting state | `crates/nexus42d/tests/lifecycle_starting_edges.rs:17` | Remove the `Event::ProcessStarted` dispatch |

### 🟡 Blocking Warning

| ID | Source | Title | Scope | Fix |
|----|--------|-------|-------|-----|
| F-QC2-04 | QC2 | `exit_starting` uses `blocking_lock` on `tokio::sync::Mutex` — potential deadlock in single-threaded runtime | `crates/nexus42d/src/lifecycle/actions.rs:176` | Change `start_handles` to `std::sync::Mutex` |

## Deferred Residuals

### Warnings (defer to M2 or backlog)

| Residual ID | Source | Title | Scope | Decision | Target |
|-------------|--------|-------|-------|----------|--------|
| R-M1-W01 | QC1 F-004 | `start_handles` `try_lock()` may silently drop handles | `crates/nexus42d/src/lifecycle/actions.rs` | defer | M2 (after F-QC2-04 fix makes this moot) |
| R-M1-W02 | QC1 F-007 | `ALL_SCHEMA_PATHS` manually maintained list separate from `build_schema_map()` | `crates/nexus-contracts/tests/schema_drift_detection.rs` | defer | backlog |
| R-M1-W03 | QC1 F-008 | `preset list` requires daemon running for embedded presets | `crates/nexus42/src/commands/preset.rs` | defer | backlog |
| R-M1-W04 | QC2 F-QC2-02 | No YAML depth/size limits on user-supplied preset YAML | `crates/nexus-orchestration/src/preset/loader.rs` | defer | backlog |
| R-M1-W05 | QC2 F-QC2-03 | `init` rejects `_`-prefix but `scan` silently skips — inconsistent UX | `crates/nexus42/src/commands/preset.rs` | defer | backlog |
| R-M1-W06 | QC3 W-1 | Preset lookup scans all user presets O(n) instead of O(1) | `crates/nexus-orchestration/src/preset/mod.rs` | defer | backlog |
| R-M1-W07 | QC3 W-2 | JoinHandle `abort()` risk — no graceful cleanup | `crates/nexus42d/src/lifecycle/actions.rs` | accept | V2.0 (documented RISK-WSC-02) |
| R-M1-W08 | QC3 W-3 | Mutex `try_lock()` contention loses handles (same as R-M1-W01) | `crates/nexus42d/src/lifecycle/actions.rs` | defer | M2 |
| R-M1-W09 | QC3 W-4 | Drift detection test may be slow at scale | `crates/nexus-contracts/tests/schema_drift_detection.rs` | accept | backlog (acceptable for current scale) |

### Suggestions (tracked but non-blocking)

| Source | Title | Decision |
|--------|-------|----------|
| QC1 F-009 | `user_preset_dir.rs` tight coupling to loader internals | accept (same crate) |
| QC1 F-010 | Schema drift should validate `schema_version` | defer (separate concern) |
| QC1 F-011 | `PRESET_INIT_TEMPLATE` fragile `{name}` substitution | defer |
| QC1 F-012 | `HealthDegraded` during Starting may lose context | defer |
| QC2 F-QC2-05 | `#[allow(dead_code)]` on `CheckMode::Subset` | accept |
| QC2 F-QC2-06 | `$ref` resolution silent fallback | defer |
| QC3 S-1 | Preset scan caching for CLI | defer |
| QC3 S-2 | Subsystem startup timeout | defer |
| QC3 S-3 | Drift detection performance metrics | defer |

## Consolidated Decision

**Verdict: Request Changes**

4 Critical findings (concentrated in WS-A: F-001, F-002, F-003; plus WS-C: F-QC2-01) and 1 blocking Warning (F-QC2-04, also WS-C) must be addressed before this can proceed to Approve.

**Cross-reviewer alignment notes:**
- F-004 (QC1) and W-3 (QC3) describe the same `try_lock()` issue → addressed by F-QC2-04 fix (changing to `std::sync::Mutex` moots both)
- WS-D (schema drift detection) received no Critical findings — well-structured implementation
- QC3 was the only Approve; all Critical/Blocking items come from QC1+QC2

**Fix assignment plan:**
- @fullstack-dev: Fix F-001, F-002, F-003 (WS-A architecture fixes) + F-QC2-01, F-QC2-04 (WS-C fixes) on a `fix/v1.9-qc-round1` branch from `feature/v1.9`
- After fixes: re-verify with `cargo test`, `cargo clippy`, `cargo +nightly fmt`
- QC re-review scope: only changed files (targeted, not full re-review)
