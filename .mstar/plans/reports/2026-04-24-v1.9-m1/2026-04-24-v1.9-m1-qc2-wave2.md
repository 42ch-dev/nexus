---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-04-24-v1.9-m1"
verdict: "Approve"
generated_at: "2026-04-24"
---

# Code Review Report — Wave2 Re-Review

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: minimax-cn-coding-plan/MiniMax-M2.7
- Review Perspective: Security & Correctness (input validation, auth boundaries, state consistency, unsafe defaults)
- Report Timestamp: 2026-04-24

## Scope
- plan_id: 2026-04-24-v1.9-m1
- Review range / Diff basis: 3c68b4b..3232cbd
- Working branch (verified): feature/v1.9
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 10
- Commit range: a8107c0, 2466a72, 30d89e2, 598639b, 3232cbd
- Tools run: git diff (full range), cargo clippy -p nexus42d, grep for Event::ProcessStarted, grep for tokio::sync::Mutex in actions.rs

## Findings

### 🔴 Critical
None.

### 🟡 Warning
None. Both originally reported findings are verified as properly resolved (see below).

### 🟢 Suggestion
None.

## Verification of Wave1 Findings

### F-QC2-01 (Critical) — `drive_to_running()` dispatched inert `Event::ProcessStarted`
**Status: RESOLVED ✓**

Commit: `a8107c0 fix(v1.9): address F-QC2-01 — remove inert Event::ProcessStarted from drive_to_running()`

Evidence:
- The diff shows `drive_to_running()` in `lifecycle_starting_edges.rs` no longer dispatches `Event::ProcessStarted`.
- Current implementation:
  ```rust
  async fn drive_to_running(m: &StatigLifecycle) {
      for kind in SubsystemKind::mandatory() {
          m.dispatch(Event::SubsystemUp(*kind));
      }
      tokio::time::sleep(Duration::from_millis(100)).await;
      assert_eq!(m.current_state(), LifecycleState::Running);
  }
  ```
- `grep` across all lifecycle source files confirms `Event::ProcessStarted` is absent from `drive_to_running()`.
- No other occurrence of `Event::ProcessStarted` was found in the review range diff.

### F-QC2-04 (Warning→Blocking) — `exit_starting` used `blocking_lock()` on `tokio::sync::Mutex`
**Status: RESOLVED ✓**

Commit: `2466a72 fix(v1.9): address F-QC2-04 — replace tokio::sync::Mutex with std::sync::Mutex for start_handles`

Evidence:
- `ActionContext::start_handles` is now `std::sync::Mutex<Vec<tokio::task::JoinHandle<()>>>` (line 29 of `actions.rs`).
- `enter_starting` uses `ctx.start_handles.lock()` (non-async, no `try_lock` with fallback).
- `exit_starting` uses `ctx.start_handles.lock().expect("start_handles mutex not poisoned")` — direct blocking lock, no `blocking_lock()`.
- No `tokio::sync::Mutex` usage in `start_handles` field or related paths in `actions.rs`.
- `cargo clippy -p nexus42d` passes with zero warnings.

## Regression Check — QC1 Fixes (30d89e2, 598639b)

### F-001+F-002 (Commit 30d89e2)
- Diff adds `NotFound` variant to `PresetLoadError` enum and replaces `Validation { problems, ... }` error matching with direct `NotFound` pattern matching.
- `resolve_preset` now returns `PresetLoadError::NotFound { preset_id }` cleanly instead of wrapping in `Validation`.
- Test assertions updated to use `matches!` with `NotFound` variant — correct.
- No duplicate code paths introduced.

### F-003 (Commit 598639b)
- Changes `scan_user_presets` to use `nexus_home.join("presets")` directly instead of `user_preset_base_dir(nexus_home)`.
- Added `NotFound` handling in `load_user_preset_from_dir` for consistency.
- Test helper paths updated to use inline `nexus_home.join("presets")` — consistent with production code.
- No path traversal or authority confusion issues.

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 0 |

**Verdict: Approve**

Both F-QC2-01 and F-QC2-04 are verified as properly resolved. QC1 fixes (F-001, F-002, F-003) show no regression patterns. `cargo clippy -p nexus42d` passes clean. The `Starting → Running` transition path is regression-free.