---
report_kind: qc
reviewer: qc-specialist-3
reviewer_index: 3
plan_id: "2026-06-27-v1.70-ci-desktop-build-optimization"
verdict: "Request Changes"
generated_at: "2026-06-27"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-3
- Runtime Agent ID: qc-specialist-3
- Runtime Model: volcengine-plan/ark-code-latest
- Review Perspective: Performance and reliability risk
- Report Timestamp: 2026-06-27T16:01:06Z

## Scope
- plan_id: 2026-06-27-v1.70-ci-desktop-build-optimization
- Review range / Diff basis: merge-base: 69310a3191d05c80460da227360cba6c9d6539b8 + tip: 1d3d1735c4f9c790af59f924e80dda4ff22b8bbd — focused on `.github/workflows/`
- Working branch (verified): iteration/v1.70
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 3 (`desktop-build.yml`, `desktop-release.yml`, `ci.yml`; 2 changed in range)
- Commit range: 69310a3191d05c80460da227360cba6c9d6539b8..1d3d1735c4f9c790af59f924e80dda4ff22b8bbd for `.github/workflows/`
- Tools run:
  - `git rev-parse --show-toplevel && git branch --show-current && git rev-parse HEAD`
  - `git diff --stat 69310a3191d05c80460da227360cba6c9d6539b8..1d3d1735c4f9c790af59f924e80dda4ff22b8bbd -- .github/workflows`
  - `git diff --unified=80 69310a3191d05c80460da227360cba6c9d6539b8..1d3d1735c4f9c790af59f924e80dda4ff22b8bbd -- .github/workflows/desktop-build.yml .github/workflows/desktop-release.yml`
  - `git diff --exit-code 69310a3191d05c80460da227360cba6c9d6539b8..1d3d1735c4f9c790af59f924e80dda4ff22b8bbd -- .github/workflows/ci.yml`
  - `git diff --check 69310a3191d05c80460da227360cba6c9d6539b8..1d3d1735c4f9c790af59f924e80dda4ff22b8bbd -- .github/workflows`
  - YAML parse fallback: `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "parsed #{f}" }' .github/workflows/desktop-build.yml .github/workflows/desktop-release.yml` (used because `actionlint` was not installed)
  - `gh release create --help && gh release upload --help` (to verify release upload / `--clobber` behavior)
  - Context7 GitHub Actions docs check for `paths`, tag push, release, and `github.ref`/`github.ref_name` semantics

## Findings

### 🔴 Critical
- None.

### 🟡 Warning
- **F-QC3-001: `desktop-release.yml` can double-run a tag release and then clobber already-uploaded assets.** The workflow triggers on both tag pushes and `release.published`. In the tag-push path, the job builds for up to 75 minutes and then runs `gh release create "$GITHUB_REF_NAME" ...` with release assets. Publishing that release can emit the `release: published` event for the same tag, causing a second macOS build for the same release. The second path then runs `gh release upload ... --clobber`, and `gh release upload --help` states that `--clobber` deletes existing assets before re-uploading them; if upload fails, original assets are lost. This undermines both stated goals: release reliability and reduced redundant macOS usage. It also interacts poorly with `concurrency.cancel-in-progress: true` on `desktop-release-${{ github.ref }}`, because duplicate runs for the same tag can cancel one another or replace assets mid-release.
  - **Fix:** Make the release workflow single-source per release. Prefer one trigger path: either (a) tag push builds and creates/uploads the release assets, with no `release: published` trigger, or (b) release publication builds/uploads to an existing release using `github.event.release.tag_name`, with no tag-push `gh release create` path. If both triggers are intentionally retained, add explicit guards so a release generated by the tag-push workflow does not start the `release.published` upload path, and avoid `--clobber` unless the workflow first verifies that it is a deliberate replacement run.

### 🟢 Suggestion
- **S-QC3-001: Add `--verify-tag` to the tag-push `gh release create` command as a defensive release invariant.** The workflow is triggered by `push.tags`, so the tag should already exist; `--verify-tag` would make that assumption explicit and prevent accidental release creation against a default-branch tag if the workflow is later refactored or manually reused.

## Source Trace

- Finding ID: F-QC3-001
  - Source Type: git-diff + manual-reasoning + CLI-doc
  - Source Reference:
    - `.github/workflows/desktop-release.yml:3-8` defines both `push.tags: ['v*']` and `release: types: [published]`.
    - `.github/workflows/desktop-release.yml:67-78` creates a release and uploads assets on tag push.
    - `.github/workflows/desktop-release.yml:80-90` uploads the same asset names on `release` events with `--clobber`.
    - `.github/workflows/desktop-release.yml:10-12` uses the same `desktop-release-${{ github.ref }}` concurrency group with `cancel-in-progress: true`.
    - `gh release upload --help` confirms `--clobber` deletes existing assets before uploading and can lose originals if upload fails.
  - Confidence: High

- Finding ID: S-QC3-001
  - Source Type: manual-reasoning + CLI-doc
  - Source Reference:
    - `.github/workflows/desktop-release.yml:74` calls `gh release create "$GITHUB_REF_NAME"` without `--verify-tag`.
    - `gh release create --help` documents that `--verify-tag` aborts if the tag does not exist.
  - Confidence: Medium

## Positive Checks

- PR path filter is materially narrower: `desktop-build.yml` PR paths now include only `apps/web/**`, `apps/desktop/**`, and `.github/workflows/**`; Rust-only paths (`crates/**`, `apps/nexus42/**`, Cargo files, package files, and lockfiles) were removed from the PR trigger.
- `push: main` retains broad desktop-build coverage: `desktop-build.yml` push paths still include `apps/web/**`, `apps/desktop/**`, `apps/nexus42/**`, `packages/nexus-contracts/**`, `crates/**`, package manifests, Cargo manifests/lockfile, and workflow files.
- The desktop-build job keeps the existing 75-minute timeout and now adds `set -euo pipefail` to its universal/fallback build block.
- `ci.yml` is unchanged in the assigned workflow diff, so the normal CI/test gate remains in place.
- YAML parsing of the changed desktop workflow files succeeded; `git diff --check` reported no whitespace errors.

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 1 |
| 🟢 Suggestion | 1 |

**Verdict**: Request Changes
