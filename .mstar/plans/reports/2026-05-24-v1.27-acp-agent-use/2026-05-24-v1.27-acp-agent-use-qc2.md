---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-05-24-v1.27-acp-agent-use"
verdict: "Request Changes"
generated_at: "2026-05-24"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: xai/grok-build-0.1
- Review Perspective: Security and correctness risk (QC2)
- Report Timestamp: 2026-05-24

## Scope
- plan_id: 2026-05-24-v1.27-acp-agent-use
- Review range / Diff basis: merge-base: a563bd841924eb4bd5871fb4ab6b3fb187fb021c → tip: 665c56f (equivalent to: `git diff a563bd841924eb4bd5871fb4ab6b3fb187fb021c...HEAD`)
- Working branch (verified): feature/v1.27-local-authoring
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 2 (Plan 4 acp-agent-use focus only)
  - crates/nexus42/src/commands/acp/mod.rs
  - crates/nexus42/tests/cli_agent.rs
- Commit range: a563bd8...665c56f (merge of feature/v1.27-acp-agent-use into integration branch)
- Tools run: git rev-parse/branch/diff/log (alignment + range), read (plan.md + focus sources + nexus-home-layout validators + Cargo.toml), grep (call sites, deps, banner), bash (state verification, dir prep)

## Findings

### 🔴 Critical
- None (path construction uses validated components from nexus-home-layout; no credential/secrets material written; no obvious traversal or injection to privileged ops).

### 🟡 Warning
- **Incomplete `agent-ref` format validation vs. plan goals** (correctness gap):  
  Plan Goal 5 + Batch A2 explicitly call for validating `agent-ref` format ("registry id or local name per existing ACP list commands"). Implementation in `cmd_agent_use` only does `trim().is_empty()` check (A2.2 acceptance). No format regex, no cross-check against registry/list output, no rejection of control chars or overly long refs. Arbitrary strings (including newlines) are persisted as `default_agent_ref`.  
  -> Fix: Add explicit format validation (e.g., non-empty, no controls, matches `[A-Za-z0-9@._-]+` or equivalent per ACP registry rules) before write. Update A2.2 acceptance if scope reduced.  
  Source Trace: F-001 | git-diff + plan.md (lines 35,72) + acp/mod.rs:676 (trim only) | Confidence: High

- **TOML config write uses incomplete manual escaping (config file safety / correctness risk)**:  
  `cmd_agent_use` builds TOML via `format!` + only `.replace('\\', "\\\\").replace('"', "\\\"")`. Does not escape newlines (`\n`), tabs, other controls, or TOML basic-string specials. Agent-ref containing literal `\n` (possible via shell `$'\n'` or clipboard) produces invalid TOML:  
  `default_agent_ref = "foo
  bar"`  
  Subsequent `default_agent_ref()` parse via `toml_edit::DocumentMut` fails → silent `None` (feature DoS). Read path tolerates bad files; write can create them.  
  -> Fix: Build document with `toml_edit` (e.g., `doc["default_agent_ref"] = toml_edit::Value::from(trimmed); doc.to_string()`) or implement full TOML string escape. Add test case with control chars (expect error or sanitized).  
  Source Trace: F-002 | git-diff (acp/mod.rs:711) + toml_edit usage (746) + home-layout (no escape) | Confidence: High

### 🟢 Suggestion
- **Read/display path (banner) test coverage incomplete**:  
  `default_agent_ref` + list banner logic added (mod.rs:217), but CLI integration tests (`cli_agent.rs`) only exercise `use` write + file contents. No test runs `acp agent list` after `use` and asserts the "Default agent: ..." banner. Persistence test verifies file overwrite but not round-trip read via banner. Unit tests in mod.rs are partial (one drops result with comment "may fail").  
  -> Add CLI test: set agent, run list, assert banner appears (and persists across invocations). Strengthen unit test for `default_agent_ref` helper.  
  Source Trace: F-003 | cli_agent.rs (no list banner assertions post-246) + mod.rs:217,1444 | Confidence: Medium

- **Weak unit test in `agent_use_writes_config`**: Drops `result` and only partially asserts; relies on side-effect HOME override that may not fully exercise in CI env.  
  -> Make assertions strict or use `#[ignore]` + doc why.

- **No length bound or charset normalization on persisted `agent_ref`**: Long or weird unicode refs could bloat config or cause display issues in banner. (Minor; pre-release.)

- **Schema version present but ignored on read**: `schema_version=1` always written (A1.2), but `default_agent_ref()` never inspects it. Future migration hook missing (acceptable per plan "pre-release config wipe acceptable").

## Source Trace
- Finding ID: F-001 (format validation)
- Source Type: plan-doc + git-diff + source review
- Source Reference: plan.md:35,72; acp/mod.rs:676 (trim only); no registry cross-check
- Confidence: High

- Finding ID: F-002 (TOML escape)
- Source Type: git-diff + manual code inspection + toml_edit call
- Source Reference: acp/mod.rs:711 (format! escape), 746 (parse), 325 (validate_entry_id etc. in home-layout)
- Confidence: High

- Finding ID: F-003 (test gap)
- Source Type: git-diff + grep (no banner tests)
- Source Reference: cli_agent.rs:289+ (use tests only); mod.rs:217 (banner)
- Confidence: Medium

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 2 |
| 🟢 Suggestion | 4 |

**Verdict**: Request Changes

**Rationale**: Path traversal prevention is solid (home-layout validators called before every `operational_workspace_dir` + `create_dir_all`). No secrets/credentials written (only `default_agent_ref` string under `~/.nexus42/...`). TOML safety and missing format validation per plan goals are correctness risks that can break the persisted default feature or allow malformed on-disk state from ordinary CLI input. Tests cover write path well but leave read/banner path under-exercised. Fix the two Warnings before Approve; pre-release wipe tolerance noted but does not waive input/config hygiene.

**Alignment verified (per mstar-branch-worktree + mstar-review-qc context gate)**: cwd, branch, plan_id, diff basis all match Assignment exactly. Review performed only on Plan 4 acp-agent-use focus files (other changes in range from parallel plans ignored per scope).

---

## Completion Report v2

**Agent**: qc-specialist-2
**Task**: QC review (security + correctness) for Plan 4 (acp-agent-use) per Assignment 2026-05-24-v1.27-acp-agent-use
**Status**: Done
**Scope Delivered**: 
- Verified review context gate (cwd/branch/HEAD/range/plan_id all match)
- Read plan + full focus diff + home-layout validators + callers/deps
- Executed QC workflow per mstar-review-qc (checklist, findings with severity/trace, verdict)
- Wrote structured QC report (frontmatter + template) to canonical path
- Git commit of report only (per qc-shared NEVER rules + Assignment)
**Artifacts**:
- `.mstar/plans/reports/2026-05-24-v1.27-acp-agent-use/2026-05-24-v1.27-acp-agent-use-qc2.md` (this report + Completion v2)
**Validation**:
- Git state pre-commit: branch `feature/v1.27-local-authoring` @ 665c56f, cwd matches Review cwd, validators confirmed in nexus-home-layout
- No other files touched; only report path written/committed
- All mstar-* required skills loaded in order (harness-core → roles → qc-shared ref → review-qc + branch-worktree + plan-* + dispatch-gates + host-opencode + coding-behavior)
- No dispatch of other QCs (Delegation: forbidden respected)
**Issues/Risks**:
- 2 Warnings (detailed above) require fixes before merge; no Critical
- Test timeout on full `cargo test -p nexus42` (env/build speed); focused git + source review used instead
- Recommend: PM tri-review consolidation + QA manual `acp agent use` + list banner check (per plan §9)
**Plan Update**: None (QC does not own plan/status.json writes per mstar-plan-artifacts)
**Handoff**: 
- To @project-manager: Request Changes issued; fixes for TOML escaping + agent-ref format validation needed. After dev remediation, re-dispatch QC wave (new filename per mstar-review-qc) or incremental.
- Residuals (if any) to be registered by PM in `{HARNESS_DIR}/status.json` root `residual_findings[plan_id]` with severity from mstar-plan-artifacts enum (not report prose).
**Git**: 
```
665c56f Merge branch 'feature/v1.27-acp-agent-use' into feature/v1.27-local-authoring
```
(Report commit to follow in this turn; real hash will be in final log line below.)
