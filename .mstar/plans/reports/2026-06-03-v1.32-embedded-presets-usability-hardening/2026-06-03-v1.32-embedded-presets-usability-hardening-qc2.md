---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: 2026-06-03-v1.32-embedded-presets-usability-hardening
verdict: Approve
generated_at: 2026-06-03T18:42:00Z
---

# QC Review — Reviewer #2 (Security & Correctness)

## Scope

- **Plan ID**: `2026-06-03-v1.32-embedded-presets-usability-hardening`
- **Review range / Diff basis**: `merge-base: feature/v1.32-preset-quality-gate..HEAD` (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- **Working branch (verified)**: `feature/v1.32-embedded-presets-usability-hardening`
- **Review cwd (verified)**: `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p2-embedded`
- **Focus**: Security and correctness risk (path safety / traversal, inner graph reference integrity, asset existence validation, smoke test side-effect freeness, pub(crate) visibility, LLM/agent input boundaries, state machine invariants, sensitive arg handling)
- **Files reviewed**: 8 (diff stat); all 6 embedded `preset.yaml`; `crates/nexus-orchestration/src/preset/mod.rs` (smoke tests + B1/B2/B3/B5); `crates/nexus-orchestration/src/preset/validation.rs` (collect_asset + path safety); loader safety assertions

## Commits Reviewed

| Commit | Subject |
|--------|---------|
| fde656e | docs(orchestration): refresh embedded preset runbooks (B6) |
| 8d9aa0f | fix(orchestration): harden rule-gate behavior with explicit always-true docs (B4/B5, TD-V131-08) |
| 52be888 | fix(orchestration): repair kb-extract inner graph wiring (B3) |
| 9060621 | test(orchestration): add embedded preset smoke discovery + P1 validation gate (B1/B2) |

## Verification Steps Performed

1. Checkout alignment: `git rev-parse --show-toplevel`, `git branch --show-current`, `git log --oneline -8` — matches Assignment.
2. Diff inspection: `git diff feature/v1.32-preset-quality-gate...HEAD --stat` + full (4 commits, 8 files).
3. Targeted file reads (security lens):
   - `kb-extract/preset.yaml` (B3 inner_graph enter + extraction_graph definition)
   - All 6 `*/preset.yaml` (path refs: `prompts/*.md`, `system_prompt_file`, `template_file`)
   - `src/preset/mod.rs` (smoke `all_embedded_presets_pass_strict_validation_gate`, `kb_extract_inner_graph_is_wired`, `memory_augmented_rule_exit_is_explicit_always_true`, `check_embedded_asset_existence`)
   - `src/preset/validation.rs` (`pub(crate) collect_asset_file_references`, `check_path_safety`, `validate_path_safety`)
   - `src/preset/loader.rs` (`assert_template_file_safe`)
4. Static checks:
   - `cargo +nightly fmt --all --check` → clean (no output)
   - `cargo test -p nexus-orchestration --lib all_embedded_presets_pass_strict_validation_gate -- --nocapture` → PASS (known non-blocking CapabilityArgDrift + SchemaCheckSkipped warnings emitted for creator.inject_prompt on novel-writing/research; test does not fail on them)
   - `cargo test -p nexus-orchestration --lib kb_extract_inner_graph_is_wired` → PASS
   - `cargo test -p nexus-orchestration --lib memory_augmented_rule_exit_is_explicit_always_true` → PASS
   - `cargo test -p nexus-orchestration --lib embedded_preset_validation_catches_structurally_invalid_preset` → PASS
   - `cargo clippy -p nexus-orchestration -- -D warnings` → clean (0 warnings)
5. Shared QC checklist + P2-specific (see below).

## Findings

### 🔴 Critical

None.

### 🟡 Warning

None (blocking).

**Note on implementer-flagged residuals (context only; form own judgment):**

- **R-P2-01 (Medium per implementer)** / **R-P2-02 (Low)**: `creator.inject_prompt` `input_schema()` omits `prompt_file` / `vars` (orchestration layer resolves before call). This surfaces as `CapabilityArgDrift` warnings (and one "requires 'prompt'" drift) in the smoke test for `novel-writing` and `research`. The smoke test explicitly downgrades these to non-blocking warnings (see code comments in `all_embedded_presets_pass_strict_validation_gate`). Matches the known A4 limitation documented in P1 validator work. No new defect introduced by P2; the smoke gate accepts the presets. **I agree these are non-blocking for this plan** (they pre-exist the stricter gate and are called out in the test itself). Recommend tracking in `status.json` residual if not already, but do not block Approve.

### 🟢 Suggestion

- **S01 — Smoke test eprintln for warnings is appropriate but could be structured**: The `eprintln!` of non-blocking warnings in `all_embedded_presets_pass_strict_validation_gate` is fine for CI visibility, but for future machine consumption a `tracing::warn!` or returning the warnings list from the test helper would be cleaner. Low priority; current form satisfies "report but do not fail".
- **S02 — README now documents correct `nexus42 daemon schedule add`**: B6 runbook refresh removes stale `orchestrate run` and `schedule add` (without daemon) examples. Good.
- **S03 — All inner_graphs are locally declared and name-matched**: No cross-preset or dynamic name resolution in the changed yamls. The `kb-extract` extraction_graph is defined in the same file and explicitly entered from the `extracting` state (verified by dedicated test + `check_inner_graph_references`).

## Source Trace

| Finding | Changed File | Lines / Section | Commit | Confidence |
|---------|--------------|-----------------|--------|------------|
| kb-extract inner_graph wiring (B3) | `embedded-presets/kb-extract/preset.yaml` | states.extracting.enter + inner_graphs | 52be888 | High |
| Rule exit explicit docs (B4/B5) | `memory-augmented/preset.yaml`, `soul-experience-refresh/preset.yaml` | exit_when comments | 8d9aa0f | High |
| Smoke gate + asset check (B1/B2) | `src/preset/mod.rs` | 899–1113 (tests) | 9060621 | High |
| pub(crate) collector + path safety | `src/preset/validation.rs` | 416 (pub(crate)), 587 (check_path_safety) | 9060621 | High |
| Safety primitive | `src/preset/loader.rs` | 351 (assert_template_file_safe) | (pre-existing, re-invoked) | High |
| Arg drift handling | `src/preset/mod.rs` | 951–965 (CapabilityArgDrift downgrade) | 9060621 | High |

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 (blocking); 2 known non-blocking arg-drift items tracked by implementer |
| 🟢 Suggestion | 3 |

**CI / Static Gate Status** (executed in Review cwd):
- `cargo +nightly fmt --all --check` — PASS
- `cargo test -p nexus-orchestration` (embedded + preset filters + specific B* tests) — PASS (all relevant)
- `cargo clippy -p nexus-orchestration -- -D warnings` — PASS

## Shared QC Checklist (security/correctness emphasis)

- [x] 输入已验证，边界检查显式 — `assert_template_file_safe` (loader) + `validate_path_safety` + `check_path_safety` explicitly reject absolute, `\`, `..`, control chars, null. Called on every asset ref collected from manifests.
- [x] 无明显的注入/路径遍历/权限问题 — All refs relative under `<preset-id>/prompts/...`; no `..` or `/` prefixes survive the safety assert. Inner graph names are static string literals from same YAML. No runtime path construction from untrusted input in P2 changes.
- [x] 敏感数据处理方式恰当 — No new sensitive data paths. Creator IDs etc. are passed as `{{preset.input.*}}` (orchestration context); smoke tests use placeholders only. No secrets or PII in test fixtures or reports.
- [x] 不变量和状态转换逻辑连贯 — Linear state machines (no Conditional); `exit_when: rule` (bare) documented + unit-tested as explicit always-true immediate transition (B5). `graph_complete` for inner-graph states. `kb_extract_inner_graph_is_wired` + semantic validator lock the wiring.
- [x] LLM/Agent 边界：不可信输入未直接驱动特权操作 — Embedded presets are compile-time trusted assets. User-supplied `--var` flow through Handlebars into capability args, but P2 changes do not alter arg injection or trust boundaries (SEC-V131-01 is P3 scope). Smoke validation is offline (no LLM calls).
- [x] 行为回归是否已被显式确认 — Smoke gate runs full P1 semantic + path-safety + asset-existence on every embedded preset at test time. Targeted tests for B3 wiring and B5 rule contract. `embedded_preset_validation_catches_structurally_invalid_preset` proves the gate would reject bad manifests.
- [x] 阻塞级安全或数据一致性风险 — None identified.
- [x] 变更行为的测试覆盖是否充分 — New smoke + 3 targeted unit tests + full preset test matrix. All pass. No runtime execution in smoke (pure manifest load + static checks).

### P2-Specific Checklist

- [x] kb-extract inner graph wiring fix doesn't introduce unsafe references? — The `extraction_graph` is defined locally in the same `inner_graphs:` map; entered explicitly by name in `extracting.enter`. No external resolution, no user-controlled name. Dedicated test + orphan check in semantic validator. Safe.
- [x] All embedded preset path references safe (no `..`, no absolute)? — Inspected all 6 yamls (even if not all touched in this diff). Every `template_file`, `prompt_file`, `system_prompt_file` is `prompts/*.md` (relative, no traversal). Loader `assert_template_file_safe` + `validate_path_safety` + smoke `check_embedded_asset_existence` enforce at load/test time. `research` and `novel-writing` (multi-role) also clean.
- [x] Smoke test doesn't execute anything privileged (just validation)? — Yes. Uses `load_embedded_preset` (manifest parse only), `validate_preset_semantic` / `validate_path_safety` (pure functions), `EMBEDDED_PRESETS.get_file` (include_dir static), no capability dispatch, no filesystem writes outside test temp, no network, no privileged ops. Pure offline validation.
- [x] `pub(crate)` on `collect_asset_file_references` is safe? — Was previously private fn inside validation. Now `pub(crate)` solely so the smoke test helper in `mod.rs` (same crate) can reuse the collector instead of duplicating. Internal use only (validation's own check_* fns + the one test call). No public API surface, no cross-crate exposure, no broadening of attack surface. Correct and minimal.

## Verdict: Approve

All P2 deliverables (B1–B6) meet acceptance. Static gates clean. Security invariants (path sandbox, inner-graph locality, trusted embedded assets, offline smoke) hold. The known `creator.inject_prompt` arg-drift surface is explicitly tolerated in the gate (as designed in P1) and does not represent a new regression or hardening failure introduced by this plan. No Critical or blocking Warning findings.

Ready for merge into the integration branch and subsequent QA.

## Cross-Reviewer Ready Notes

- **For QC #1 (Architecture / Maintainability)**: The smoke test centralizes discovery + full P1 gate + asset existence in one deterministic lib test. Good for future preset additions (just drop yaml + prompts/). The `pub(crate)` reuse is surgical and avoids duplication. Consider whether the arg-drift whitelist should live in a shared const instead of inline if it grows.
- **For QC #3 (Performance / Reliability)**: Smoke is O(1) per preset (no LLM, pure Rust). 6 presets, sub-100ms. No hot paths touched. Rule-gate and graph_complete paths are pre-existing orchestration mechanics, only docs + wiring + tests added.
- **For QA**: After this QC wave, run the exact smoke (`cargo test -p nexus-orchestration --lib all_embedded_presets_pass_strict_validation_gate`) plus manual `nexus42 daemon schedule add --preset ...` examples from the refreshed README in a clean `~/.nexus42` tree. The arg-drift warnings will appear on stderr for novel-writing/research but must not cause test failure or preset load failure.

## Git (post-report commit)

(Will be filled after `git add` + `git commit` of only this report file.)
