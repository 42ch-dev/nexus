---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-05-12-v1.16-trace-correlation"
verdict: "Approve"
generated_at: "2026-05-13"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: bailian-coding-plan/glm-5
- Review Perspective: Architecture — DTO design, module boundaries, trace model soundness
- Report Timestamp: 2026-05-13T12:00:00Z

## Scope
- plan_id: `2026-05-12-v1.16-trace-correlation`
- Review range / Diff basis: `333991d..HEAD`
- Working branch (verified): `feature/v1.16-trace-correlation`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus`
- Files reviewed: 13
- Commit range: `1d46acb..1198a1e` (2 commits)
- Tools run: `cargo clippy --all -- -D warnings`, `cargo test -p nexus-contracts trace`, `cargo test -p nexus-home-layout acp_run`, `cargo test -p nexus42 --lib -- commands::acp_trace`, contract tests

## Findings

### 🔴 Critical
(None)

### 🟡 Warning
(None)

### 🟢 Suggestion

#### S-001: Consider using `tracing` crate for trace write warnings
- **Location**: `crates/nexus42/src/commands/acp_trace.rs:51-52, 62-63, 70-71, 76-77`
- **Issue**: Trace write failures use `eprintln!` for warnings. The project uses `tracing` crate elsewhere.
- **Fix**: Consider using `tracing::warn!` for consistency. Current implementation is acceptable for V1.16 scope; defer to future cleanup.

#### S-002: Could add `TryFrom<String>` for correlation ID newtypes
- **Location**: `crates/nexus-contracts/src/local/acp_runtime/trace.rs:14-15, 22-23`
- **Issue**: `NexusRunId` and `NexusCapabilityCallId` are `pub struct ... (pub String)` with no validation on construction.
- **Fix**: Consider adding `TryFrom<String>` impls that validate prefix and format. Current pattern works because validation is done at CLI entrypoints via `validate_run_id_safe`. Deferable to V1.17.

#### S-003: Consider adding `#[serde(rename = "schema_version")]` alias for future migration
- **Location**: `crates/nexus-contracts/src/local/acp_runtime/trace.rs:55, 78, 95, 119`
- **Issue**: `schema_version` field is consistent across all events. No explicit alias for potential future rename.
- **Fix**: Not needed for V1.16. Current naming is clear and stable.

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|------------|-------------|------------------|------------|
| S-001 | manual-reasoning | `acp_trace.rs:51-52` uses `eprintln!` | Medium |
| S-002 | manual-reasoning | `trace.rs:14-15` newtype design | Medium |
| S-003 | manual-reasoning | `trace.rs:55` field naming | Low |

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 3 |

## Cross-Reviewer Ready Notes

### Architecture Assessment (Reviewer #1 focus)

**DTO Design — Excellent:**
- Newtypes (`NexusRunId`, `NexusCapabilityCallId`) use `#[serde(transparent)]` for clean serialization
- Tagged enum `NexusTraceEvent` with `#[serde(tag = "event", rename_all = "snake_case")]` produces inspectable JSON
- Optional fields use `#[serde(skip_serializing_if = "Option::is_none")]` to reduce output noise
- `schema_version: u32` on every event supports forward-compatible parsing
- Comprehensive round-trip tests (8 tests) validate serialization contracts

**Module Boundaries — Correct:**
- DTOs in `nexus-contracts/local/acp_runtime/trace.rs` — shared across CLI/daemon
- Path helpers in `nexus-home-layout/src/lib.rs` — consistent with existing `~/.nexus42` layout
- Writer helpers in `nexus42/src/commands/acp_trace.rs` — CLI-local, minimal
- Transport extension in `nexus-acp-host/src/transport.rs` — backward-compatible
- Orchestration seeding via TODO comments in `tasks/mod.rs` — correct for V1.17 scope

**JSONL Storage — Reasonable for Pre-1.0:**
- Simple, inspectable, no SQLite migration burden during rapid execution model evolution
- Best-effort writes (non-fatal) align with plan's risk mitigation
- Path validation mirrors existing `validate_creator_id_safe` / `validate_entry_id_safe` patterns
- Storage under `~/.nexus42/acp/runs/<run_id>/trace.jsonl` follows ADR-014 layout

**`spawn_with_env` Backward Compatibility — Preserved:**
- `spawn()` delegates to `spawn_with_env(program, args, &[])` — existing callers unchanged
- No breaking change to `AgentSpawner` API
- New `env: &[(&str, &str)]` parameter is simple slice, no heap allocation overhead

**Integration Risk — Low:**
- Plan 7 seeds correlation IDs; deeper orchestration propagation is V1.17
- TODO comments clearly document intent for future implementation
- Worker IPC fields are optional — backward compatible

### Verification Results

All verification gates from plan passed:
1. ✅ `cargo test -p nexus-contracts trace` — 8 passed
2. ✅ `cargo test -p nexus-home-layout acp_run` — 3 passed
3. ✅ `cargo test -p nexus42 --lib -- commands::acp_trace` — 4 passed
4. ✅ Contract tests for `--run-id` flags — 2 passed
5. ✅ `cargo clippy --all -- -D warnings` — passed (no errors)
6. ⚠️ `cargo fmt --check` — formatting drift in `generated/` (pre-existing, not plan scope; stable toolchain ignores `.rustfmt.toml` ignore field per `AGENTS.md`)

## Verdict: **Approve**

The implementation follows the plan faithfully. DTOs are well-designed with proper serde attributes, module boundaries are correct, JSONL storage is appropriate for pre-1.0, and `spawn_with_env` preserves backward compatibility. No blocking issues found. Suggestions are minor and deferrable.