---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-05-09-v1.14-n1-batch1-runtime-hardening"
verdict: "Request Changes"
generated_at: "2026-05-09"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: openai/gpt-5.5
- Review Perspective: Security & correctness — input validation, state safety, error handling, test coverage, and behavior preservation
- Report Timestamp: 2026-05-09T15:55:05Z

## Scope
- plan_id: 2026-05-09-v1.14-n1-batch1-runtime-hardening
- Review range / Diff basis: git diff feature/v1.14..feature/v1.14-n1-batch1-runtime-hardening
- Working branch (verified): feature/v1.14-n1-batch1-runtime-hardening
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 6 (`crates/nexus42/src/commands/creator.rs`, `crates/nexus42/tests/creator_register_e2e.rs`, `crates/nexus-orchestration/src/tasks/mod.rs`, `crates/nexus-orchestration/tests/e2e_novel_writing.rs`, `crates/nexus-orchestration/src/preset/loader.rs`, `crates/nexus-sync/src/platform_client.rs`)
- Commit range: feature/v1.14..feature/v1.14-n1-batch1-runtime-hardening
- Tools run:
  - `git branch --show-current`
  - `git diff feature/v1.14..feature/v1.14-n1-batch1-runtime-hardening`
  - `cargo test -p nexus42 creator -- --nocapture`
  - `cargo test -p nexus-orchestration`
  - `cargo clippy -p nexus42 -p nexus-orchestration --all-targets -- -D warnings`
  - Context7 docs lookup for `handlebars-rust` quick-start/error-return usage

## Findings
### 🔴 Critical
- None.

### 🟡 Warning
- **QC2-W-001 — Changed migrated creator unit tests do not pass the repository clippy gate.** The moved staged E2E helper/tests in `crates/nexus42/src/commands/creator.rs` introduce clippy-denied issues under `--all-targets -D warnings`: `uninlined_format_args` at line 1262, `err_expect` at line 1297, and `match_same_arms` at lines 1300-1302. Because these are in the changed test migration and CI is expected to run clippy with warnings denied, this branch should not be approved until these are fixed. Suggested fixes: inline `{verify_result:?}`, use `expect_err("register error")`, and merge the accepted `StagedPlatformError` match patterns into one arm.
- **QC2-W-002 — Relevant clippy run also surfaces pre-existing nexus42 test lint failures that currently block the same CI command.** `cargo clippy -p nexus42 -p nexus-orchestration --all-targets -- -D warnings` also fails in unmodified `crates/nexus42/src/commands/preset.rs` (`needless_raw_string_hashes`, `unwrap_used`). These are outside the assigned diff, but the same release/CI gate remains red. PM/dev should either include those fixes in the runtime-hardening closure or explicitly document/route them as a separate blocking residual before claiming the branch is CI-ready.

### 🟢 Suggestion
- **QC2-S-001 — The new template syntax error test is useful but checks a broad substring.** The test correctly verifies error-return behavior rather than panic behavior. For future hardening, prefer asserting on a stable error category/type if `handlebars` exposes one through this call path; the current `"template" || "syntax"` substring is acceptable for now but can become brittle across dependency wording changes.

## Security & Correctness Notes
- Template payload validation and preset template-file path validation remain in the loader path; the Handlebars registry reuse changes only the renderer allocation pattern and preserves `no_escape` behavior.
- `OnceLock<Handlebars<'static>>` initialization is state-safe for this use: the registry is fully configured before publication, then accessed immutably by `render_template`.
- Malformed Handlebars syntax now has explicit regression coverage through `template_syntax_error_returns_deterministic_failure`; no panic path was observed in tests.
- Moving the staged creator E2E helper into the private test module removes test-only public API from production surface while preserving equivalent happy-path and upstream-failure coverage.

## Source Trace
- Finding ID: QC2-W-001
  - Source Type: static-analysis
  - Source Reference: `cargo clippy -p nexus42 -p nexus-orchestration --all-targets -- -D warnings`; `crates/nexus42/src/commands/creator.rs:1262`, `:1297`, `:1300-1302`
  - Confidence: High
- Finding ID: QC2-W-002
  - Source Type: static-analysis
  - Source Reference: `cargo clippy -p nexus42 -p nexus-orchestration --all-targets -- -D warnings`; `crates/nexus42/src/commands/preset.rs:513`, `:533`, `:543`
  - Confidence: High
- Finding ID: QC2-S-001
  - Source Type: manual-reasoning
  - Source Reference: `crates/nexus-orchestration/tests/e2e_novel_writing.rs:343-361`
  - Confidence: Medium

## Validation Evidence
- `cargo test -p nexus42 creator -- --nocapture`: passed (`52` lib tests + `52` bin tests filtered by `creator`, `8` creator integration tests, and filtered integration/regression targets all green).
- `cargo test -p nexus-orchestration`: passed (`240` unit tests passed, `1` ignored; all integration/doc-test targets passed or intentionally ignored).
- `cargo clippy -p nexus42 -p nexus-orchestration --all-targets -- -D warnings`: failed with the findings listed above.

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 2 |
| 🟢 Suggestion | 1 |

**Verdict**: Request Changes
