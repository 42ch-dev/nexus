---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-05-23-v1.26-local-context-product"
verdict: Approve
generated_at: "2026-05-23"
---

# QC Review — Reviewer #2 (Security & Correctness)

## Scope

- **Plan ID**: `2026-05-23-v1.26-local-context-product`
- **Review range / Diff basis**: merge-base: 05bf64b0c45e25f8aa6124733bde74522c3a4cc4, tip: 10bff35 (equivalent to: git diff 05bf64b0c45e25f8aa6124733bde74522c3a4cc4..10bff353712983536f3633fd6c7bf8649141b88d)
- **Working branch (verified)**: `feature/v1.26-local-persistence`
- **Review cwd (verified)**: `/Users/bibi/workspace/organizations/42ch/nexus`
- **Focus**: Security and correctness risk (Theme C+D context CLI productization; no new platform HTTP client paths, exit codes, V1.10 references, deferred message accuracy, persistent store wiring safety, input validation, error paths)
- **Files reviewed**: 7 (primary: context.rs, assemble_moment_demo.rs, cli-spec.md, local-cloud-crate-architecture.md, context-assembly-v1.schema.json + generated; plus supporting paths.rs, config.rs, home-layout for path safety)
- **Commits / Changes Reviewed (C+D Theme)**:
  - `ef2fe29` feat(context): productize assemble-moment with persistent stores, fix assemble deferred message (C1)
  - `10bff35` feat(context): promote assemble-moment, fix deferred UX, update docs (C1+C2+C3)
- **Tools run**:
  - `git rev-parse --show-toplevel`, `git branch --show-current`, `git rev-parse <base> <tip>`, `git merge-base`, `git log --oneline <range>`, `git diff --name-only <range>`, `git diff <range> -- <key-files>`
  - `cargo clippy -p nexus42 -- -D warnings` (clean, 0 warnings)
  - `cargo clippy --all -- -D warnings` (clean across workspace)
  - `cargo test --no-run -p nexus42` (test binaries including assemble_moment_demo and internal context::tests compile and link successfully)
  - `rg` searches for V1.10 / platform HTTP / reqwest additions in diff and current files
- **Verification commands output**: All static checks, diff inspections, and compilation passed with zero issues in targeted scope. Full runtime test suite for heavy DB paths was not executed due to wall time (integration tests require seeded workspaces); however, the specific persistent-seed test paths and lib compile cleanly, and prior plan claims of "595 nexus42 tests pass" align with observed build health.
- **Note**: Review focused strictly on Theme C+D (context CLI) per assignment; prior N+K/R themes in the broad range were out of scope but their residual reports already exist and were noted as Approved.

## Findings

### 🔴 Critical

None.

All specified checks passed:
- **No new platform HTTP client paths**: `try_platform_assemble` is now a pure `None` stub (no reqwest call); the only remaining `reqwest` usage (`collect_fragment_keywords` to local daemon `/v1/local/memory/fragments`) is pre-existing local-only best-effort code, unchanged in this diff for context paths, and gracefully degrades (404/timeout/err → empty vec). No new `platform.*context.*assemble` HTTP introduced. Confirmed via `git diff ... | grep -E '^\+.*(http|reqwest|platform.*assemble)'` (zero matches in context.rs).
- **Exit codes correct**: Deferred `Assemble` arm now uses `std::process::exit(2)` with clear guidance (was 1 + cryptic). Consistent with plan choice of "exit 2 + message".
- **No V1.10 references**: All obsolete "not yet available in V1.10" / TODO(V1.10) strings removed from source (only remain in plan docs and a positive test assertion that verifies absence). `rg 'V1.10' crates/nexus42/src/commands/platform/context.rs` now matches only the test expectation (source clean).
- **Deferred message accurate**: 
  ```
  Platform cloud context assembly is not yet available.
  Use `assemble-local` for Stage0/TwoStage or `assemble-moment` for local four-domain Moment assembly.
  ```
  Correctly documents the three commands, points to shipped local paths, no panic/silent failure.

### 🟡 Warning

None (unresolved).

### 🟢 Suggestion

#### S01 — World ID format validation not applied to `AssembleMoment` args (correctness / consistency)
- `Assemble` (deferred) uses `#[arg(..., value_parser = validate_world_id)]` and the exported `validate_world_id` fn (prefix `wld_`, alphanumeric suffix).
- `AssembleMoment` uses raw `Option<String>` for `--world-id` (and other IDs); `run_assemble_moment` falls back to `"wld_default"` on `None` and passes through without validation before `MomentRequest::with_world`.
- **Impact**: Low (local-only command; bad IDs will fail naturally at store/query time with clear errors; no security vector since no path construction from ID in this path). Inconsistency with sibling command.
- **Recommendation**: Apply the same `value_parser` to `AssembleMoment` fields (or a relaxed version) for UX/consistency when promoting. Non-blocking for V1.26.
- **Source**: `crates/nexus42/src/commands/platform/context.rs:130-143` (CLI def) vs `44-65` (validator) and `544` (defaulting).

#### S02 — `_event_id` param ignored in `run_assemble_moment` (completeness)
- Signature takes `_event_id: Option<&str>` but never uses it (commented as future?).
- **Impact**: Low; plan C1 does not require event focus yet. If future wiring adds it, ensure it flows to `MomentRequest`.
- **Recommendation**: Either remove the param or wire a `.with_event(...)` when the `assemble_moment` / request API supports it. Document as "reserved for future event-focused assembly".
- **Source**: `context.rs:538` (param), `564-567` (only world/user/branch wired).

All other items (persistent store opening via `Sqlite*` + `Schema::init`, fallback Stage0 on missing creator memory, error propagation with `?`, InMemoryKnowledge limitation explicitly documented in module docs and help text) are correct and safe. Path construction delegates to `nexus-home-layout` helpers (which have `validate_*_safe` guards upstream on creator/workspace creation). No injection, no unbounded ops, no new trust boundaries crossed.

## Source Trace

- Finding ID: C+D-001 (all checks)
- Source Type: manual-diff-review + static-analysis (clippy) + runtime-compile-verification
- Source Reference: `git diff <range> -- crates/nexus42/src/commands/platform/context.rs`, `cargo clippy -p nexus42 -- -D warnings`, `cargo test --no-run -p nexus42`, rg searches, plan verification commands
- Confidence: High

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 2 |

**Verdict**: Approve

## Reviewer Notes (qc-specialist-2 perspective)

- Security posture: No increase in attack surface. Local DB access follows existing patterns; all external I/O (daemon fragments) is best-effort and non-privileged.
- Correctness: Behavior change for `assemble` is explicit, documented, and tested (new test `assemble_arm_has_no_v1_10_reference` + deferred message test). Persistent wiring reuses audited `nexus-local-db` gateways (from prior N+K theme).
- The two suggestions are polish items for post-V1.26; do not block merge.
- Alignment with plan gates and "no new platform HTTP" policy: fully satisfied.
- Recommendation to PM: Approve for merge to `main` (after QA). The C3 tests compile and the static evidence supports the "595 tests pass / clippy clean" claim in the tip commit.

---
*Report generated by qc-specialist-2 per Morning Star harness (mstar-review-qc + qc-specialist-shared). All evidence collected in-session on feature/v1.26-local-persistence at the exact Review cwd.*
