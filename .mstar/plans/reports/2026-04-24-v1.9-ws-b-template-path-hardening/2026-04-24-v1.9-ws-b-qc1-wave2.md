---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-04-24-v1.9-ws-b-template-path-hardening"
verdict: "Approve"
generated_at: "2026-04-24"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: qwen3.6-plus
- Review Perspective: Architecture consistency, maintainability, long-term evolution risk (module boundaries, abstraction layers, dependency direction, extensibility)
- Report Timestamp: 2026-04-24T00:00:00Z

## Scope
- plan_id: 2026-04-24-v1.9-ws-b-template-path-hardening
- Review range / Diff basis: 11c9e25..0b083c8 (fix round: F-W1-01, F-W1-02, F-W1-03)
- Working branch (verified): feature/v1.9
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 2
  - `crates/nexus-orchestration/src/preset/loader.rs`
  - `crates/nexus42/src/commands/creator.rs`
- Commit range: 11c9e25..0b083c8
- Tools run: `git diff 11c9e25..0b083c8`, `cargo clippy --all -- -D warnings`, `grep`

## Findings

### 🔴 Critical
- None.

### 🟡 Warning
- None.

### 🟢 Suggestion
- None.

## Finding Verification (Wave 1 → Wave 2)

### F-W1-01: RESOLVED — Backslash (`\`) check added to `assert_template_file_safe()`

**Source Type**: git-diff
**Source Reference**: `crates/nexus-orchestration/src/preset/loader.rs` lines 231–235

The fix adds an explicit `path.contains('\\')` check before the `..` traversal check, with a clear error message: `"template_file contains backslash: {path:?} (backslash separators are not allowed)"`. A corresponding unit test `template_file_safe_rejects_backslash` was added verifying `prompts\\windows\\path.md` is rejected. The placement (after absolute-path check, before `..` check) is correct in the validation chain.

**Confidence**: High

### F-W1-02: RESOLVED — Shared `collect_template_file_entries()` helper extracted

**Source Type**: git-diff
**Source Reference**: `crates/nexus-orchestration/src/preset/loader.rs` lines 256, 328, 616

The shared helper `collect_template_file_entries(manifest: &PresetManifest) -> Vec<(String, &str)>` has been extracted and is now called from **two** call sites:
1. `validate_template_files_in_sandbox()` (line 328) — for sandbox validation
2. `validate_manifest()` (line 616) — for manifest safety validation

Both call sites now use the single helper instead of duplicating the manifest-walking logic. The refactor eliminates ~50 lines of duplicated code across the two functions. The helper returns `(dot_path, &str)` tuples, preserving the dot-path context needed for error reporting.

**Confidence**: High

### F-W1-03: RESOLVED — Name max-length validation moved before `validate_handle()`

**Source Type**: git-diff
**Source Reference**: `crates/nexus42/src/commands/creator.rs` lines 280–294

The `name.len() > MAX_CREATOR_NAME_LENGTH` check has been moved to the **top** of `register_creator()` (line 280), before the `validate_handle()` call (line 290). This is the correct ordering: cheap length validation (O(1)) runs before the more expensive regex-based handle validation. The diff confirms the old code block (after handle validation) was removed and the new block (before handle validation) was added.

**Confidence**: High

## Source Trace
- Finding ID: F-W1-01 → Source Type: git-diff → Source Reference: `crates/nexus-orchestration/src/preset/loader.rs:231-235`
- Finding ID: F-W1-02 → Source Type: git-diff → Source Reference: `crates/nexus-orchestration/src/preset/loader.rs:256,328,616`
- Finding ID: F-W1-03 → Source Type: git-diff → Source Reference: `crates/nexus42/src/commands/creator.rs:280-294`
- Lint check: `cargo clippy --all -- -D warnings` → clean (no warnings)

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 0 |

**Verdict**: Approve

All three QC1 wave 1 findings (F-W1-01, F-W1-02, F-W1-03) are properly resolved. The fixes are surgical, minimal, and introduce no new issues. `cargo clippy --all -- -D warnings` passes cleanly.

## Cross-Reviewer Ready Notes
- No integration risk or migration cost identified — all changes are in-place fixes with no API surface changes.
- The extracted `collect_template_file_entries()` helper improves maintainability and reduces future drift risk between `validate_manifest` and `validate_template_files_in_sandbox`.
