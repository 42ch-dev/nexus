---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: 2026-06-03-v1.32-preset-validator-core-and-cli
verdict: Approve
generated_at: 2026-06-03T05:44:45Z
revision: rev3
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: grok-build-0.1 (xai/grok-build-0.1)
- Review Perspective: Security and correctness risk
- Report Timestamp: 2026-06-03T05:44:45Z

## Scope
- plan_id: 2026-06-03-v1.32-preset-validator-core-and-cli
- Review range / Diff basis: merge-base: feature/v1.32-preset-quality-gate..HEAD (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- Working branch (verified): feature/v1.32-preset-validator-core-and-cli
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p1-validator
- Files reviewed: 1 (only `crates/nexus-orchestration/src/preset/loader.rs` changed in rev3)
- Commit range (rev3): e0f44c4 (only this commit since rev2 base d179f95)
- Tools run:
  - `git rev-parse --show-toplevel`, `git branch --show-current`, `git log --oneline -12`, `git diff d179f95..HEAD --stat`, `git diff d179f95..HEAD`
  - `read` on loader.rs (load_preset A3 wiring, removed sandbox fn, collect fn, assert_template_file_safe, validate_manifest call sites, new tests at end)
  - `read` + `grep` on validation.rs (validate_path_safety, validate_assets_in_bundle, check_path_safety, check_asset_file_existence, check_symlink_escapes, collect_asset_file_references, assert reuse)
  - `grep` across crates for "validate_template_files_in_sandbox", "load_preset(", callers of removed fn and dir loader
  - `cargo +nightly fmt --all --check`
  - `cargo test -p nexus-orchestration a3 --lib` (new C-001 tests), `cargo test -p nexus-orchestration load_preset_from_directory --lib`, full preset filtered tests
  - `cargo test -p nexus-daemon-runtime preset` (parity W1/W2/C4 tests)
  - `cargo clippy -p nexus-orchestration -p nexus-daemon-runtime -p nexus42 -- -D warnings`
  - Read of prior qc2-rev2 report, plan, compass §2.1/§2.2, orchestration-engine.md §7-8, relevant AGENTS.md
  - Date/time for ISO timestamp
- Prior round context: Rev1 Request Changes; Rev2 (qc2 Approve); Rev3 +1 commit e0f44c4 touching only loader.rs (A3 wiring + dead code removal + 2 regression tests)

## Rev3 Verification of Prior Findings

**S-004 (legacy sandbox duplication in loader — Suggestion from rev2 qc2):** **Fixed (dead code removed).**
- Rev3 removes the entire `validate_template_files_in_sandbox` (old canonicalize-based defense-in-depth for template_file only; explicitly accepted missing files).
- Replaced in `load_preset()` (after YAML parse) with:
  ```rust
  let a3_path_result = super::validation::validate_path_safety(&manifest);
  let a3_asset_result = super::validation::validate_assets_in_bundle(&manifest, bundle_root);
  // collect only .errors() into ValidationProblem list; block on any
  // log .warnings() via tracing
  ```
- `collect_template_file_entries` (still used by structural `validate_manifest` for T2 assert) has a stale doc comment referencing the removed fn (minor; not behavioral).
- No other callers of the removed fn remain anywhere in the tree (confirmed by full-crate grep; only old QC reports and the now-deleted definition referenced it).

**C1 / W2 path safety & asset coverage (from prior rounds):** **Further improved for the dir-loader path.**
- Dir `load_preset` (the bundle entry point) now gets full A3: path safety (all asset refs: template_file, prompt_file, system_prompt_file, roles[].system_prompt_file, context_update, initial_action) via shared `assert_template_file_safe`, plus existence + symlink escape + id match.
- Old sandbox was template_file-only + accepted !exists; new surface errors on missing assets (MissingAsset) and broader path issues.
- New regression tests (C-001) explicitly cover:
  - missing template_file in bundle dir → error containing filename + "does not exist"
  - `..` traversal in role `system_prompt_file` → error on path + ".."
- Daemon validate already had this (via infer_bundle_root); loader dir path now matches.

**No regressions or new Critical/Warning from rev3 diff.**
- Structural safety still runs in `load_from_str` (via validate_manifest) after the early A3 guard in dir load — layered defense preserved (A3 errors short-circuit before from_str).
- Embedded / from_str paths unchanged (correct: no FS bundle assets to check at that layer).
- User/system preset runtime load still routes via scan → load_user..._from_dir → load_from_str (no dir-load A3 yet), but validate API provides the surface; rev3 scope was "final A3 loader wiring" for the explicit dir loader fn.
- No new public surfaces, no relaxed checks, no path traversal vectors introduced.
- All tests (new + existing parity) pass; fmt/clippy clean.

**Prior rev2 Approve rationale still holds; rev3 is a net-positive targeted hardening + cleanup.**

## Findings

### 🔴 Critical
(none)

### 🟡 Warning
(none)

### 🟢 Suggestion
- S-001 (carry-over, low): Update `load_preset` function-level doc comment (loader.rs:282-284) which still claims only "sandbox validation that `template_file` paths resolve" — now does full A3 asset + path safety for all refs. Also fix stale doc on `collect_template_file_entries` (line 380) that names the removed fn.
- S-002 (non-blocking for this rev): The dir `load_preset` is now the canonical bundle loader with A3, but main user-preset runtime path (`resolve_preset` / scan) still uses from_str only. If full asset enforcement at user load time is desired, wire `validate_assets_in_bundle` (when bundle known) into `load_user_preset_from_dir` / system equivalent (future non-P1).

## Source Trace
- Finding ID: Rev3-A3-wiring
  - Source Type: git-diff / read-file / test-output
  - Source Reference: loader.rs:305-329 (A3 calls + error collection + warn logging in load_preset), 423- (removed sandbox fn), 2293-2366 (new a3_loader_* tests), validation.rs:186 (path_safety), 169 (assets_in_bundle), 587 (check_path_safety reuses assert), 502/530 (existence + symlink), 416 (collect_asset covers roles/system etc.), e0f44c4 diff
  - Confidence: High
- Finding ID: Rev3-deadcode-safe
  - Source Type: grep / read / git-log
  - Source Reference: full-crate `grep validate_template_files_in_sandbox` (only 1 remaining hit: stale doc in loader), prior rev2 reports noting it was still wired pre-rev3, loader.rs:702 (collect still called by validate_manifest), no external call sites in daemon/user_preset/resolve
  - Confidence: High
- Finding ID: Rev3-tests-verify
  - Source Type: cargo test output / read
  - Source Reference: `cargo test -p nexus-orchestration a3 --lib` (both pass), `... load_preset_from_directory --lib` (4 pass), daemon preset tests (21 pass incl W1/W2 missing-asset + dotdot + symlink), new tests assert on "does not exist" and "system_prompt_file" + ".."
  - Confidence: High
- Finding ID: Rev3-no-bypass
  - Source Type: grep / code walk / prior reports
  - Source Reference: load_from_str path still runs validate_manifest (assert) + semantic; dir load now adds early A3 before delegating; user scan uses from_str (design choice, not regressed by rev3)
  - Confidence: High

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 2 (1 carry-over doc polish; 1 future wiring note) |

**Verdict**: Approve

## Post-Review Actions (for PM)
- Land this rev3 QC report (qc2) + the other two reviewers' rev3 reports.
- The single rev3 delta (A3 parity wiring into dir loader + safe dead-code removal + covering tests) is correct, secure, and closes the prior S-004 item.
- Update the two minor stale docs in loader.rs in a follow-up (or as part of any final polish before merge).
- P1 validator core + CLI/API parity (shared A3 facade, path safety, asset existence, bundle id, daemon parity, loader wiring) is ready for merge after tri-review + QA.

---

**Artifacts produced**: this report only (review-only session; no implementation changes, no edits, no sub-dispatches).

**Git (after commit)**: (to be filled post `git commit`)
