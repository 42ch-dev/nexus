---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: 2026-06-03-v1.32-preset-validator-core-and-cli
verdict: Approve
generated_at: 2026-06-03T05:40:52Z
revision: rev3
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: openai/gpt-5.5
- Review Perspective: Architecture coherence and maintainability risk
- Report Timestamp: 2026-06-03T05:40:52Z

## Scope
- plan_id: 2026-06-03-v1.32-preset-validator-core-and-cli
- Scope label: Shared preset validator core + CLI/API parity (rev3 — final A3 loader wiring)
- Review range / Diff basis: merge-base: feature/v1.32-preset-quality-gate..HEAD (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- Rev3 diff inspected: `git diff d179f95..HEAD`
- Working branch (verified): feature/v1.32-preset-validator-core-and-cli
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p1-validator`
- Files reviewed: 1 (`crates/nexus-orchestration/src/preset/loader.rs`)
- Commit range: `d179f95..HEAD` for rev3-only change; overall assignment diff basis as above
- Tools run:
  - `git rev-parse --show-toplevel`
  - `git branch --show-current`
  - `git log --oneline -12`
  - `git diff d179f95..HEAD --stat`
  - `git diff d179f95..HEAD`
  - `cargo +nightly fmt --all --check`
  - `cargo test -p nexus-orchestration preset`
  - `cargo test -p nexus-daemon-runtime preset`
  - `cargo clippy -p nexus-orchestration -p nexus-daemon-runtime -p nexus42 -- -D warnings`
  - `cargo test -p nexus-orchestration a3_loader -- --nocapture`

## Rev3 Verification of Prior Findings
- **rev2 C-001 — Fixed.** `load_preset()` now parses the manifest and invokes the shared A3 surfaces before delegating to `load_preset_from_str`: `validate_path_safety(&manifest)` for structural path safety and `validate_assets_in_bundle(&manifest, bundle_root)` for bundle-backed file existence, symlink escape, and bundle-directory/id consistency. Error-severity diagnostics from both results are converted into the loader's legacy `ValidationProblem` shape and returned as `PresetLoadError::Validation`, so runtime bundle loading now rejects the same missing-file and unsafe-path classes that the daemon validation path exercises.
- **Regression tests present and passing.** The rev3 tests cover both sides of the prior gap: `a3_loader_rejects_missing_template_file_in_bundle` verifies missing bundle assets are rejected by `load_preset()`, and `a3_loader_rejects_dotdot_in_system_prompt_file` verifies A3 path safety is applied beyond legacy `template_file` collection to role `system_prompt_file`. The focused command `cargo test -p nexus-orchestration a3_loader -- --nocapture` passed both tests.
- **Dead-code removal impact.** Removing `validate_template_files_in_sandbox()` is architecturally safe. Its prior behavior only sandbox-checked collected `template_file` references and explicitly skipped nonexistent files. The replacement A3 path covers a broader set of file-backed references (`template_file`, `prompt_file`, `system_prompt_file`, and role `system_prompt_file`) and preserves symlink escape protection through `check_symlink_escapes()` while adding required existence and id/directory checks. I found no unintended maintainability or behavior regression from the removal.
- **New architecture concerns.** None found in the rev3 delta. The loader still uses `load_preset_from_str()` for non-filesystem parsing/building, while `load_preset()` owns bundle-only A3 checks; that split is coherent because asset existence and symlink checks require a bundle root.

## Findings
### 🔴 Critical
- None.

### 🟡 Warning
- None.

### 🟢 Suggestion
- None.

## Source Trace
- Finding ID: rev3-C-001-closure
- Source Type: git-diff | manual-reasoning | test
- Source Reference: `crates/nexus-orchestration/src/preset/loader.rs:302-331`, `crates/nexus-orchestration/src/preset/loader.rs:2291-2374`, `crates/nexus-orchestration/src/preset/validation.rs:169-190`, `crates/nexus-orchestration/src/preset/validation.rs:501-600`, `cargo test -p nexus-orchestration a3_loader -- --nocapture`
- Confidence: High

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 0 |

**Verdict**: Approve
