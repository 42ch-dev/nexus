---
report_kind: qc
reviewer: qc-specialist-3
reviewer_index: 3
plan_id: "2026-04-18-v1.5-stabilization"
verdict: "Approve"
generated_at: "2026-04-19"
---

# QC Report #3 — V1.5 Stabilization (WS-E + Cross-Module)

**Reviewer**: qc-specialist-3
**plan_id**: 2026-04-18-v1.5-stabilization
**Review range**: git diff 9a3b901..HEAD
**Review cwd**: /Users/bibi/workspace/organizations/42ch/nexus
**Working branch**: feature/v1.5-stabilization
**Date**: 2026-04-19

## Summary

The V1.5 stabilization branch delivers four major workstreams (WS-A Schedule FSM, WS-B Orch/DB residuals, WS-C context.summarize, WS-D cron triggers) plus WS-E platform coordination. The WS-E `as_of` schema addition is correct and properly integrated across all layers (schema → generated TS/Rust → CLI construction sites → test helpers). All sqlx R3 placeholder migrations (`?1` → `?`) are complete and consistent. The primary finding is a **status.json drift**: WS-A and WS-D plan files show `InReview` but `status.json` still shows `Todo` for WS-A and `Todo` for WS-E; this is a documentation inconsistency, not a functional defect.

---

## Critical Issues

None identified.

---

## Warnings

### W1: `status.json` plan status drift (medium severity)

**Location**: `.mstar/status.json`

**Observation**: The following plans have `status` values in `status.json` that do not match the actual implementation state reflected in their plan `.md` files:

| Plan | `status.json` | Plan file |
|------|--------------|-----------|
| WS-A Schedule FSM | `Todo` | `InReview` (per diff) |
| WS-D Cron triggers | `InReview` | `InReview` (matches) |
| WS-E Platform coordination | `Todo` | `InProgress` (per diff) |

WS-A and WS-E are marked `Todo` in `status.json` but their plan files show `InReview`/`InProgress`, indicating work has been completed. WS-B shows `InReview` correctly.

**Risk**: Future agents reading `status.json` will see stale `Todo` states for completed workstreams, potentially causing duplicate effort or incorrect sequencing.

**Recommendation**: PM should sync `status.json` with actual plan file statuses before merging. This is a **documentation fix only**; no code changes required.

---

## Suggestions

### S1: Clean up stale explanatory comments (low/nit severity)

**Location**: `crates/nexus42d/src/api/handlers/sync.rs:321` and `crates/nexus42d/src/api/handlers/memory.rs:313`

**Observation**: After migrating from `?1` to `?`, the inline comments still read:
```rust
// sqlx R3: use ? instead of ?1
```

While not incorrect, the `?1` reference in the comment could cause momentary confusion when skimming. The comment's intent (documenting the R3 migration) would be clearer as:
```rust
// sqlx R3 migration
```

**Recommendation**: Optional cleanup in a future non-blocking patch. Not a merge blocker.

---

## Positive Observations

### Schema Change (WS-E: `as_of` field)

- **Correctness**: `as_of` field definition in `schemas/platform/context-assembly-v1.schema.json` uses proper JSON Schema (`type: ["string", "null"]`, `format: date-time`, `default: null`), consistent with existing nullable datetime fields in the codebase.
- **Semantics**: Description correctly references ADR-009 and documents the optionality and fallback behavior.
- **Generated code**: Both `ContextAssemblyV1.ts` (`as_of?: string | null`) and `context_assembly_v1.rs` (`pub as_of: Option<String>`) correctly reflect the schema.
- **CLI integration**: All three CLI construction sites (`commands/context.rs:159`, `context/client.rs:55`, `context/types.rs:57`) correctly initialize `as_of: None`.
- **Test helpers**: All inline test fixtures in `client.rs` and `types.rs` include `as_of: None`.

### sqlx R3 Migration (`?1` → `?`)

- **Scope**: Four files updated: `sync.rs`, `memory.rs`, `acp.rs`, `research.rs`
- **Verification**: `grep` search for `\?1[^0-9]` in Rust files returned no matches in actual SQL code (only in comments explaining the migration).
- **Consistency**: All four handlers now use `?` uniformly.

### Outbox Migration

- `crates/nexus-local-db/migrations/20260420_outbox_tables.sql` properly defines `outbox_entries` with all required columns (`outbox_entry_id`, `bundle_id`, `idempotency_key`, `delivery_state`, `created_at`).
- `manuscript/manager.rs` test correctly uses the full column set matching the migration.

### Pre-existing Auth Test Failure

- Confirmed that `nexus42::auth::tests::get_returns_none_for_unknown_creator` is **pre-existing** (documented in WS7 QA reports from 2026-04-17) and **out of scope** for V1.5.
- Zero commits in the V1.5 range touch `crates/nexus42/src/auth/`.

### Cross-Module Consistency

- Schedule FSM, cron scheduler, and context modules are implemented across separate crates but share consistent patterns.
- The `context.summarize` capability (WS-C) is properly registered in `CapabilityRegistry`.
- WS-D scheduler uses a hand-rolled implementation (per crate-selection best practices) with `ClockSource` abstraction enabling testability.

---

## Evidence

| Check | Evidence |
|-------|----------|
| Git diff reviewed | `git diff 9a3b901..HEAD` — 47 files changed |
| Schema `as_of` field | `schemas/platform/context-assembly-v1.schema.json` lines 64-69 |
| Generated TS | `packages/nexus-contracts/src/generated/ContextAssemblyV1.ts:26` |
| Generated Rust | `crates/nexus-contracts/src/generated/context_assembly_v1.rs:31` |
| CLI construction sites | `context.rs:159`, `client.rs:55`, `types.rs:57` |
| sqlx R3 verification | `grep` for `\?1[^0-9]` — 0 matches in SQL code |
| Outbox migration | `crates/nexus-local-db/migrations/20260420_outbox_tables.sql` |
| Pre-existing auth failure | Documented in WS7 QA report (2026-04-17) |
| Status drift | `status.json` vs plan file diff comparison |

---

## Cross-Reviewer Ready Notes

1. **Runtime impact**: The `as_of` field is purely additive and optional with `None` defaults everywhere. Zero runtime behavior change for existing users.

2. **Rollback urgency**: Low. The `as_of` field is backward-compatible (optional, defaults to `null`). Rolling back the schema change would require removing the field from three CLI construction sites and regenerated code, but no data migration is needed.

3. **CI gate**: Cannot verify `pnpm run validate-schemas` or `pnpm run codegen` locally due to bash permission restrictions on `pnpm` commands. CI is authoritative for these checks.

4. **Schema validation**: Based on code review, the `as_of` field definition follows existing patterns for nullable datetime fields in the codebase (e.g., `max_timeline_events`, `max_story_summaries`). Expected to pass `pnpm run validate-schemas`.

---

## Recommendation

**Verdict**: `Approve`

The V1.5 stabilization changes are sound. The `as_of` schema addition is correctly implemented, the sqlx R3 migration is complete, and cross-module consistency is maintained. The only issue is the `status.json` drift (Warning W1), which is a documentation inconsistency that should be corrected by PM before merging but does not block the code itself.
