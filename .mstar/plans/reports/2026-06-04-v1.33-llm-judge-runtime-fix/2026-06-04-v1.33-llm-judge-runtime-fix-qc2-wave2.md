---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-06-04-v1.33-llm-judge-runtime-fix"
secondary_plan_id: "2026-06-04-v1.33-memory-review-closed-loop"
verdict: "Approve"
generated_at: "2026-06-04"
---

# Code Review Report — P3 + P4 Re-Check Wave 2 (Security & Correctness)

## Reviewer Metadata

- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: xai/grok-build-0.1 (via OpenCode)
- Review Perspective: Security and correctness risk (re-check of fix wave for R-V133P3-01/02/03 + R-V133P4-01..06)
- Report Timestamp: 2026-06-04 (re-check session)
- Review type: Re-check wave 2 (post `Request Changes` from original tri-review at 51ab12e..46a8e3f; this report covers only the 8 fix commits in 46a8e3f..2217c0a)

## Scope

- plan_id: `2026-06-04-v1.33-llm-judge-runtime-fix` (primary) + `2026-06-04-v1.33-memory-review-closed-loop` (secondary)
- Feature / scope label: P3+P4 fix wave (re-check)
- Review range / Diff basis: `merge-base: 46a8e3f` + `tip: 2217c0a` (fix wave only, 8 fix commits; equivalent to `git diff 46a8e3f..2217c0a`)
- Working branch (verified): `feature/v1.33-work-experience-loop`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus`
- Files reviewed: 12 (per `git diff --stat`; core: judge_llm.rs, tasks/mod.rs, preset/{loader,mod}.rs, memory.rs (daemon), memory_review_fragments_api.rs, memory_fragment.rs (local-db), memory_io.rs (creator-memory), plus status.json + plan/report updates)
- Commit range: 46a8e3f..2217c0a (style fmt at tip)
- GitNexus / impact: focused re-check on the 5 Critical + high-impact Warning items from original QC1/QC2; no broad new symbols (fixes localized to parse, template resolution, min_interval, auth extractors, summarizer, atomic write, alias cleanup).
- Tools run:
  - Pre-alignment: `git rev-parse --show-toplevel`, `git branch --show-current`, `git log -1 --oneline`, `git diff 46a8e3f..2217c0a --stat | tail -10` (all passed; branch + range match Assignment)
  - `cargo check -p nexus-orchestration -p nexus-daemon-runtime -p nexus-creator-memory -p nexus42`
  - `cargo clippy -p ... -- -D warnings`
  - `cargo +nightly fmt --all -- --check`
  - `cargo test -p nexus-orchestration --lib 'parse_'` (covers parse_judge_response + parse_iso8601_duration / R-V133P3-01/03)
  - `cargo test -p nexus-orchestration --lib 'with_resolved_template'` (R-V133P3-02)
  - `cargo test -p nexus-daemon-runtime --test memory_review_fragments_api` (full 15 tests incl. 5 new 401/403/cross for R-V133P4-02)
  - `cargo test -p nexus-creator-memory` (148 + 4 doc-tests, covering save_memory atomic + promote paths for R-V133P4-05)
  - `rg -n 'format!.*\"SELECT|...'` (0 hits in the 3 files)
  - `rg -n 'AuthRequired|NexusApiError::' .../memory.rs | head -20`
  - Manual reads of: judge_llm.rs (parse fn + tests), tasks/mod.rs (LlmJudgeTask, with_resolved_template, throttle, parser + tests), preset/{loader,mod}.rs (assert_template_file_safe + read_embedded_template + build_wired), memory.rs (review/fragments handlers + pending legacy + PassthroughSummarizer + unit tests for header/cap), memory_fragment.rs (list_fragments_filtered), memory_io.rs (save_memory), status.json (residuals), original 3 QC reports, 2 plans.
  - Additional: `cargo test -p nexus-orchestration --lib 'with_resolved_template_loads_embedded_file'`

## Findings

### 🔴 Critical
- **None** (re-check of fix wave). All 5 Criticals from the original tri-review (qc1: F-001/F-002 = R-V133P3-01/02; qc2: 3 Criticals = R-V133P4-01/02/03) have been addressed by the changes in 46a8e3f..2217c0a. No new Criticals (security, correctness, auth, parse, template, injection, or liveness) were introduced in the fix wave. The wave is narrowly surgical on the reported items.

  - R-V133P3-01 (parse_judge_response): Now first-token (ws + .,!;: delimiters), NOGO_WORDS checked **before** GO_WORDS, explicit sets only, defaults NOGO + warn on ambiguous. Tests explicitly cover the original false-positive case (`"NOGO because of missing data"`, `"nogo"`, uppercase, bare, with reason, unrelated → NOGO; "Go ahead.", "go", "Yes" → GO; empty/ambiguous → NOGO). `cargo test ... 'parse_'` green.
  - R-V133P3-02 (template_file pre-load): `StateCompositeTask::with_resolved_template(preset_id)` (called from `build_wired_outer_graph` at session/preset load) calls `read_embedded_template` which does `{preset_id}/{path}` against `EMBEDDED_PRESETS` (bundle root). `assert_template_file_safe` (called at `validate_manifest` / loader time) rejects `..`, absolute, `\`, null, control chars (full test matrix). File-not-found in bundle → keep original value (safe; tests use inline strings). Content (not path) passed downstream to handlebars in `LlmJudgeTask`. Dedicated tests (`with_resolved_template_loads_embedded_file` for novel-writing/prompts/gathering-exit.md etc.) assert non-empty, not-path-str, substantial content. The 3 presets (novel-writing, reflection-loop, research) use valid `prompts/*.md` paths under their roots.
  - R-V133P3-03 (min_interval): Parser now supports date-part `P1D`/`P7D` (before any T); `P1M` (and months/years) hits explicit warn "unsupported unit (only D/H/M/S supported; months/years not supported)" + None. Throttle site: on None falls through (with the warn from parser). New tests: `parse_duration_days`, `parse_duration_seven_days`, `parse_duration_rejects_months`. Matches "PT6H parses, P1D Some, P1M None+warn".
  - R-V133P4-01 (memory auth): `POST /v1/local/memory/review` and `GET /v1/local/memory/fragments` now: `read_active_creator_id(...) .ok_or(AuthRequired)?` (401 if none); `if body/query.creator_id != active { Forbidden 403 }`; format validate on the matched id; queries + `process_review_queue` / `list_*` + `promote` use the **active** (not body) value. Edge cases: no-active + body → 401; active + mismatch → 403; match → 200 + active used; bad-format on matched → 400; SQL uses parameterized query_as! / binds (no format! into SQL). Existing pending-review handlers remain on pre-existing explicit-creator pattern (noted as legacy Warning in original; not part of the P4 "new endpoints" Critical).
  - R-V133P4-02 (tests): The `--test memory_review_fragments_api` now has 15 tests (was ~10), including the 5+ new: `review_returns_401_without_creator`, `fragments_returns_401_without_creator`, `review_returns_403_on_creator_id_mismatch`, `fragments_returns_403_on_creator_id_mismatch`, `cross_creator_isolation_review_other_creator_returns_403` (plus more cross/pending still works). All pass. `test_ctx_with_active_creator("ctr_alice")` + seeding under other creator + 403 assertions cover the matrix.
  - R-V133P4-03 (UNTRUSTED): `PassthroughSummarizer::summarize` (in daemon handler; implements the trait from creator-memory) now prepends exactly:
    ```
    # UNTRUSTED: sourced from session_capture digest
    # session_id: ...
    # task_kind: ...
    # world_id: ...
    ```
    (plus the digest). Test `passthrough_summarizer_includes_untrusted_header` asserts starts_with + contains the fields. The `creator_id` is **not** in the header (the LTM dir path + active_creator enforcement at API boundary before calling promote ensures the write is under the correct/active creator; body creator cannot influence the operation or header). Downstream can now detect untrusted provenance.
  - R-V133P4-04 (! alias): `list_fragments_filtered` (the runtime sqlx path used for keyword=) now emits clean `SELECT fragment_id, session_id, ...` (no ` as "fragment_id!" `). Comment explicitly calls out "R-V133P4-04: removed `!` suffix...". Mapping uses `row.get("fragment_id")`. The handler now calls the filtered path directly when keyword present (no longer only the compile-time workaround for non-keyword). Other list_ fns retain ! only for their query_as! paths.
  - R-V133P4-05 (atomic save): `save_memory` (creator-memory/memory_io.rs) now: writes to `path.with_extension("md.tmp")`, `fs::rename` to final, on rename err does best-effort `remove_file(tmp)`. Comment: "R-V133P4-05: Atomic write...". Matches "write .tmp first, then rename; on failure clean .tmp".
  - R-V133P4-06 (size cap): In same `PassthroughSummarizer`, `const MAX_DIGEST_BYTES: usize = 256 * 1024;`; if > cap: warn + truncate to MAX; header prepended after. Tests: `passthrough_summarizer_truncates_large_digest` (creates >256KiB, asserts body_after_header.len() == MAX), `small_digest_unchanged`. Applied before `promote_to_long_term` / save_memory.

### 🟡 Warning
- (Persisting from original, not re-introduced) Auth pattern inconsistency for **existing** `pending-review` handlers (`list_pending_reviews`, `count_pending_reviews`, `create_pending_review`, `delete_pending_review`): still accept `creator_id` from client query/body/path, do only format `is_valid_creator_id` (→400) or ownership in delete (→403), never call `read_active_creator_id` + AuthRequired(401) or mismatch-403. The fix wave correctly hardened only the *new* P4 surface (review/fragments) to the P1 works `AuthRequired` + active pattern. Pending handlers remain "explicit-creator legacy" (as noted in original qc2 Warning F-004). No new risk for the closed-loop review path, but cross-surface inconsistency remains for future unification.
- (Persisting) `min_interval` still only partial ISO (D/H/M/S); full date components + months/years rejected with warn (now explicit). Presets use PT#H only; no load-time rejection of bad values (throttle silently bypassed on None).
- (Persisting / low) `WorkerUnavailable` → NOGO/Wait still a liveness consideration for scheduled llm_judge states (original qc2).
- (New in wave, low) Header in PassthroughSummarizer uses `task_kind` / `world_id` / `session_id` but not `creator_id` or `captured_at` (the original qc2 suggestion had a specific 4-line format). The active_creator enforcement + LTM dir provides the binding; header is sufficient for provenance detection.
- (Low) In review handler, format validate still runs on `req.creator_id` even after mismatch-403 (harmless, since 403 returned first); could short-circuit but no correctness issue.
- No raw `format!` SQL anywhere in the changed files (rg clean); all new dynamic paths have SAFETY comments + binds.
- No path traversal or untrusted creator_id reaching fs/DB in the new paths (active enforced before any use in queries/promote/save).

### 🟢 Suggestion
- Consider extracting the active-creator + mismatch-403 pattern (now duplicated in review + fragments) into a small `require_active_creator(&state, supplied_id) -> Result<String, NexusApiError>` helper (or reuse from works.rs context) so future endpoints don't forget.
- The pending-review handlers could be incrementally migrated to the active pattern in a later hardening pass (or explicitly documented as "client-supplied creator for legacy CLI parity" with a deprecation note).
- Add `captured_at` (or `created_at` from the pending row) to the UNTRUSTED header for better audit (current has session/task/world; creator is in dir).
- For min_interval, at preset load time (or schedule admission) emit a structured warning / metric when a non-PT#H value is seen, even if currently rejected at runtime.
- The cross-creator test seeds via the legacy `POST /pending-review` (which doesn't enforce); once/if pending handlers are updated, the isolation test matrix can be tightened.

## Source Trace

- R-V133P3-01 (Critical, fixed): `crates/nexus-orchestration/src/capability/builtins/judge_llm.rs:147-191` (parse_judge_response: first-token split on ws/.,!;,: ; NOGO_WORDS loop before GO_WORDS; explicit consts; ambiguous → warn + NOGO); tests at 216-251 (explicit "NOGO because...", "nogo", bare, Go ahead, empty path via default); called from run:128.
- R-V133P3-02 (Critical, fixed): `tasks/mod.rs:590-606` (with_resolved_template); `preset/mod.rs:207-214` (read_embedded_template: `{id}/{path}` vs EMBEDDED_PRESETS); `loader.rs:351-372` (assert_template_file_safe: no .. / abs / \ / \0 / controls; called at 702+ in validate); `loader.rs:863-864` (build_wired calls it); tasks run:738 (uses resolved); test 1795- (loads "prompts/gathering-exit.md" for novel-writing, asserts content not path).
- R-V133P3-03 (medium, fixed): `tasks/mod.rs:1354-1438` (parse_iso8601_duration: date-part P#D support, M after P → unsupported warn + None); throttle at 761-773; tests 1905-1923 (P1D, P7D, P1M→None).
- R-V133P4-01 (Critical, fixed): `memory.rs:482-493` (review: read_active.or(AuthRequired); mismatch → Forbidden 403; then format on matched; use active for query/process); 706-717 (fragments: identical); 505/728 (logs + filter use active); read_active at 783-791 (config.toml); pending handlers unchanged (legacy).
- R-V133P4-02 (Critical, fixed): `tests/memory_review_fragments_api.rs:250` (review_401_without), 270 (fragments_401), 292/302 (403 mismatch), 314 (cross_isolation_403); full 15 tests green; uses test_ctx_with_active_creator.
- R-V133P4-03/06 (Critical/medium, fixed): `memory.rs:664-692` (PassthroughSummarizer: header with UNTRUSTED + session/task/world; MAX_DIGEST_BYTES=256KiB; truncate+warn); 799- (unit tests for header + truncate + small); called from 564 (promote path); active creator from outer handler ensures dir + header context match active.
- R-V133P4-04 (medium, fixed): `memory_fragment.rs:195-196` (comment: R-V133P4-04 removed !); 199-200 (SELECT without as "!" in filtered); 228 (row.get plain names); handler now calls filtered for keyword (previously workaround only).
- R-V133P4-05 (medium, fixed): `memory_io.rs:153-162` (tmp + write + rename + cleanup on err; comment R-V133P4-05); used by promote/save in review path.
- R-V133P4-06: see summarizer above (cap + test).
- Cross-cutting: rg SQL 0 hits; auth rg shows AuthRequired only on the two new paths + Forbidden on mismatch; all cargo/clippy/fmt/test green on scope.

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 4 (1 new/persisting pattern, 3 original low-impact) |
| 🟢 Suggestion | 5 |

**Verdict**: Approve

Rationale: The re-check wave fully resolves the 5 Criticals that caused the original `Request Changes` (parse false-positive, template path-vs-content, memory auth bypass, test matrix gap, untrusted LTM injection). All assigned edge cases, unit/integration tests, static checks, and safety scans (rg) pass. The fixes are minimal, well-commented with R- ids, and preserve the "safe default NOGO / active-creator enforcement / atomic + capped + marked" invariants. Remaining items are pre-existing Warnings (legacy pending handlers, partial duration support) or low-impact Suggestions; none rise to Critical or block under the security/correctness lens. Per `mstar-review-qc`, 0 unresolved Critical → Approve.

## Residual Disposition Table (all 10 R-NN from status.json)

| Residual ID | Original Severity (from status) | Wave Fix Evidence | Disposition | Notes |
|-------------|---------------------------------|-------------------|-------------|-------|
| R-V133P3-01 | critical | judge_llm.rs:147 (first-token, NOGO-first), 8 parse tests, cargo test green | Fixed / verified in re-check | Original false-positive case now explicitly NOGO. |
| R-V133P3-02 | critical | with_resolved_template + read_embedded_template + assert_safe + build_wired + 2 resolution tests + preset paths | Fixed / verified | Content loaded for real presets; inline preserved for tests; safety at load. |
| R-V133P3-03 | medium | parse_iso... now handles P#D, warns+None on P1M, 3 duration tests | Fixed / verified | "PT6H ok, P1D Some, P1M None+warn" matches. |
| R-V133P3-04 | medium | (WorkerUnavailable NOGO) — no change in wave (liveness doc only) | Unchanged (Warning) | Pre-existing; not in wave scope for this Critical list. |
| R-V133P4-01 | critical | review/fragments: active read + 401/403 + use active (memory.rs:482,706); 401/403 tests | Fixed / verified | Pending handlers legacy (see Warning). |
| R-V133P4-02 | critical | 5+ new 401/403/cross tests in memory_review_fragments_api.rs; all 15 pass | Fixed / verified | Matrix covered (no-active, mismatch, cross-isolation). |
| R-V133P4-03 | critical | Passthrough now prepends UNTRUSTED+provenance header (memory.rs:686); unit test asserts | Fixed / verified | Header present; creator binding via active enforcement + LTM dir (not spoofable body). |
| R-V133P4-04 | medium | list_fragments_filtered: no ! aliases (memory_fragment.rs:200), comment R-04, handler uses filtered for kw | Fixed / verified | Keyword path now safe; non-filtered retain ! only for query_as!. |
| R-V133P4-05 | medium | save_memory: tmp + rename + cleanup (memory_io.rs:155-162); comment | Fixed / verified | Atomic; on-fail cleanup. |
| R-V133P4-06 | medium | MAX_DIGEST_BYTES + truncate+warn in summarizer (memory.rs:662,673); 2 size tests | Fixed / verified | 256 KiB cap before LTM write. |

## Verification Evidence

### Pre-review alignment (re-check wave)
```
/Users/bibi/workspace/organizations/42ch/nexus
feature/v1.33-work-experience-loop
2217c0a style: cargo +nightly fmt --all post-fix-wave
 .mstar/status.json                                 | 147 ++++++++++++-
 .../memory_io.rs                                   |  16 +-
 .../handlers/memory.rs                             | 181 ++++++++++++++--
 .../tests/memory_review_fragments_api.rs           | 125 ++++++++++-
 .../memory_fragment.rs                             |  10 +-
 .../judge_llm.rs                                   |  85 ++++++--
 .../preset/loader.rs                               |   5 +
 .../preset/mod.rs                                  |  20 ++
 .../tasks/mod.rs                                   | 237 ++++++++++++++++++---
 12 files changed, 1308 insertions(+), 79 deletions(-)
```

### Static checks
```
$ cargo check ... | tail -1
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.21s
EXIT:0

$ cargo clippy ... -- -D warnings | tail -1
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.22s
EXIT:0

$ cargo +nightly fmt --all -- --check | tail -1
(no output; clean)
EXIT:0
```

### Targeted tests (R- items)
```
$ cargo test -p nexus-orchestration --lib 'parse_' | tail -5
test result: ok. 21 passed; 0 failed; ...
$ cargo test -p nexus-orchestration --lib 'with_resolved_template' | tail -3
test result: ok. 2 passed; ...
$ cargo test -p nexus-daemon-runtime --test memory_review_fragments_api | tail -5
test result: ok. 15 passed; 0 failed; ...
$ cargo test -p nexus-creator-memory | grep -E 'test result|passed'
test result: ok. 148 passed; ...
test result: ok. 4 passed; ...
```

### Safety scans
```
$ rg -n 'format!.*"SELECT|... ' [3 files] || echo 'No raw format! SQL hits (good)'
No raw format! SQL hits (good)

$ rg -n 'AuthRequired|NexusApiError::' .../memory.rs | head -5
... (shows AuthRequired at review:483, fragments:707; Forbidden at 486,710; InvalidInput on format paths; legacy pending still use format-only)
```

(Full command output tails and file reads as listed in Tools run above; all reproducible from the verified branch + range.)

## Completion Report v2

**Agent**: qc-specialist-2  
**Task**: P3+P4 re-check wave 2 (security & correctness) for 2026-06-04-v1.33-llm-judge-runtime-fix (primary) + 2026-06-04-v1.33-memory-review-closed-loop (secondary) on integration branch `feature/v1.33-work-experience-loop`; diff basis `merge-base: 46a8e3f` + `tip: 2217c0a` (fix wave only)  
**Status**: Done  
**Scope Delivered**: Full re-check of the 8 fix commits against the exact 9 assigned R- items (P3-01/02/03 + P4-01..06) + the 10 residuals in status.json. Pre-alignment, all mandated cargo check/clippy/fmt + targeted lib/integration tests (parse_judge, with_resolved, memory_review_fragments_api 15 tests, creator-memory 148 tests) + rg safety scans executed and recorded. Key functions (parse_judge_response first-token+NOGO-first, with_resolved_template + read_embedded + assert_safe, active_creator 401/403 in review/fragments, UNTRUSTED+256KiB in Passthrough, atomic save_memory, ! -free list_fragments_filtered, min_interval P1D support+warn) read + edge cases + tests verified. Report written to canonical path + committed (only the report file).  
**Artifacts**: `.mstar/plans/2026-06-04-v1.33-llm-judge-runtime-fix/reports/2026-06-04-v1.33-llm-judge-runtime-fix-qc2-wave2.md` (YAML frontmatter + standard sections per mstar-review-qc + qc-specialist-shared)  
**Validation**: 
- Git alignment + branch/cwd/range verification passed before any review (cwd = Assignment Review cwd; branch = Working branch).
- `cargo check/clippy +nightly fmt` clean on the four crates.
- All specific tests green (parse 21, resolution 2, memory API 15 incl. 5 auth/cross, creator-memory 148+4).
- rg for raw SQL format! → 0 hits; auth patterns reviewed (new endpoints use AuthRequired+active; pending legacy noted).
- 0 unresolved Critical in this re-check wave; all assigned Critical edge cases (no-active→401, mismatch→403, NOGO substring, template path→content, P1D/P1M, UNTRUSTED+active, ! alias, atomic, 256KiB) confirmed in code + tests.
- No new Criticals; fixes are surgical with R- comments and tests.
**Issues/Risks**: 0 new Critical. 4 Warning (auth pattern inconsistency for legacy pending-review handlers remains; min_interval partial ISO support; WorkerUnavailable liveness; minor header field variance vs original suggestion). All original Criticals closed by the wave. No P1 DTO regression, no SQLi, no traversal, no spoofable creator in LTM path/header.
**Plan Update**: None (QC does not edit plans or status.json; PM/QA own residual lifecycle and checkboxes).
**Handoff**: After this report + sibling qc1-wave2/qc3-wave2 (if dispatched), PM should consolidate, update status.json residual_findings with stable R# (or mark the 5 original Criticals closed based on this evidence), and proceed to QA sign-off on the same `Review cwd` / `Working branch` / range. QA should re-run the exact cargo test commands + manual cross-creator 401/403 scenarios + confirm LTM files on disk have the # UNTRUSTED header.
**Git**: (to be filled after commit)

---

**End of QC Report**
