---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-05-22-v1.24-knowledge-crates-alignment"
verdict: "Approve"
generated_at: "2026-05-22"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: bailian-coding-plan/glm-5
- Review Perspective: Security and correctness risk
- Report Timestamp: 2026-05-22T12:00:00Z

## Scope
- plan_id: `2026-05-22-v1.24-knowledge-crates-alignment`
- Review range / Diff basis: `merge-base: 5f4ac0b (origin/main) .. tip: 75fdd8d (HEAD)`
- Working branch (verified): `feature/v1.24-knowledge-crates-alignment`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus`
- Files reviewed: 20 files changed (873 insertions, 517 deletions)
- Commit range: `5f4ac0b..75fdd8d` (5 commits)
- Tools run: `cargo test --workspace`, `git diff`, `grep` for orphan references

## Findings

### 🔴 Critical
(None)

### 🟡 Warning
(None)

### 🟢 Suggestion

#### S-001: Future platform API placeholder clarity
- **Issue**: `try_platform_assemble` in `context.rs` is a placeholder that always returns `None`. While documented as "future direct platform API integration", callers may expect different behavior when platform API becomes available.
- **Location**: `crates/nexus42/src/commands/platform/context.rs:324-338`
- **Fix**: Consider adding a compile-time `#[cfg(feature = "platform_direct_api")]` or documenting the expected integration path more explicitly in the function signature or module-level docs.
- **Confidence**: Low (this is forward-looking; current implementation is correct for current scope)

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|------------|-------------|------------------|------------|
| S-001 | manual-reasoning | `crates/nexus42/src/commands/platform/context.rs:324-338` | Low |

## Detailed Security & Correctness Analysis

### 1. Daemon context/assemble proxy removal (KCA-002 B2)

**Security Assessment**: ✅ **POSITIVE — reduces attack surface**

- The `POST /v1/local/context/assemble` daemon proxy route is correctly retired
- Context assembly now runs CLI in-process via `nexus-moment-context-assembly` (`Stage0Assembly` / `TwoStageAssembly`)
- Removing the HTTP proxy eliminates a potential loopback attack vector
- The placeholder function `try_platform_assemble` has clear documentation about future direct platform API integration
- Route retirement properly documented in `local-runtime-boundary.md` §3.2.1 table

**Correctness Assessment**: ✅ **Correct**

- `call_assemble` method removed from `daemon_client.rs` — no dead code paths
- `context/client.rs` deleted — verified no orphan imports via grep
- Import cleanup complete:
  - `mod.rs` removes `pub mod client;`
  - `daemon_client.rs` removes unused `AssembleResponse` import
  - `context.rs` removes unused `DaemonClient` import
- `try_platform_assemble` changed from async to sync (returns `None` immediately)
  - This is correct behavior for current scope — triggers fallback to `Stage0Assembly`

### 2. Work-scope KB changes (KCA-003 C2)

**Scope Isolation**: ✅ **Correctly enforced**

- KB handlers now explicitly document `scope=work` only in:
  - `kb.rs` module header (lines 1-20)
  - `kb.rs` handler doc comments
  - `api/mod.rs` `kb_routes()` function comment
  - CLI `creator/mod.rs` `KbScope` enum and `run_kb` function docs
- Test added: `resolve_kb_paths_uses_work_scope_layout` verifies path structure under `~/.nexus42/creators/<id>/workspaces/<slug>/kb/`
- Documentation clearly distinguishes from:
  - `nexus-kb` (World KB — narrative KB graph)
  - `nexus-knowledge` (User knowledge — global knowledge)

**Input Validation**: ✅ **Properly implemented**

- `validate_creator_id_safe` called before constructing paths (prevents path traversal)
- `validate_entry_id_safe` called for `entry_id` validation
- Test `validate_entry_id_rejects_traversal` verifies `../etc/passwd` is rejected
- Comment `F002` in `run_kb` explains validation rationale

**No Scope Escalation**: ✅ Verified

- Handlers do not expose World KB or User knowledge endpoints
- `world_scope_deferred()` prints message when `--scope world` requested — no routing to `nexus-kb`
- Future World KB integration documented but not implemented

### 3. Deleted file cleanup

**Cleanup Verification**: ✅ **Complete**

| Deleted/Removed | Verification |
|-----------------|--------------|
| `context/client.rs` (248 lines) | `grep -r "context::client"` → no references |
| `ContextClient` type | `grep -r "ContextClient"` → no references |
| `call_assemble` method | `grep -r "call_assemble"` → no references |
| `mod.rs` module declaration | Updated to remove `pub mod client;` |

### 4. Spec/Documentation alignment

**Boundary Spec**: ✅ Properly updated

- `local-runtime-boundary.md` §3.2.1: `POST /v1/local/context/assemble` marked **Retired (KCA-002 B2)** with clear explanation
- KB routes documented as `scope=work` only in endpoint table

**Entity Scope Model**: ✅ Aligned

- Minor update to `nexus-cloud-sync` row clarifying dependency status (was gap text, now confirmed wired)

## Test Results

```
cargo test --workspace: 587 passed; 0 failed; 0 ignored
Integration tests: 47 passed
Regression tests: 11 passed
Command surface tests: 23 passed
CLI agent tests: 19 passed
Creator register e2e: 8 passed
Doc-tests: All passed (with expected ignores)
```

**New tests verified**:
- `resolve_kb_paths_uses_work_scope_layout` — work-scope path structure invariant
- Existing `validate_entry_id_rejects_traversal` — path traversal prevention

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 1 |

**Verdict**: **Approve**

### Rationale

The changes represent a correct simplification of the runtime architecture:

1. **Route retirement (KCA-002 B2)**: Removing the daemon context/assemble proxy reduces attack surface and aligns with the documented in-process context assembly path. No orphan references or dangling code paths found.

2. **Work-scope KB isolation (KCA-003 C2)**: KB handlers correctly enforce `scope=work` with proper input validation preventing path traversal. Scope isolation is documented and tested.

3. **Documentation alignment**: Normative specs correctly updated to reflect actual Cargo wiring and route status.

4. **Test coverage**: All 587 workspace tests pass; new test verifies work-scope path layout.

No security vulnerabilities, correctness issues, or blocking concerns identified. The single suggestion (S-001) is forward-looking and does not affect current behavior correctness.

---

*Report generated by qc-specialist-2 (Reviewer #2) — Security and correctness focus*