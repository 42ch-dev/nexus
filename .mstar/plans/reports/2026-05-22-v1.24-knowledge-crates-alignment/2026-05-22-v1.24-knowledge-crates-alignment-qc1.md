---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-05-22-v1.24-knowledge-crates-alignment"
verdict: "Approve"
generated_at: "2026-05-22"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist (Reviewer #1 — Architecture coherence and maintainability risk)
- Runtime Agent ID: qc-specialist
- Runtime Model: k2p6
- Review Perspective: Architecture coherence, maintainability, spec/code alignment
- Report Timestamp: 2026-05-22T12:00:00Z

## Scope
- plan_id: 2026-05-22-v1.24-knowledge-crates-alignment
- Review range / Diff basis: merge-base: 5f4ac0b (origin/main) .. tip: 75fdd8d (HEAD)
- Working branch (verified): feature/v1.24-knowledge-crates-alignment
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 20 files changed, 873 insertions(+), 517 deletions(-)
- Commit range: 5f4ac0b..75fdd8d (5 commits)
- Tools run: cargo clippy --all -- -D warnings, cargo test --workspace

## Findings

### 🔴 Critical
- *None*

### 🟡 Warning
- *None*

### 🟢 Suggestion

**S-001: `try_platform_assemble` dead code path in two-stage assembly**

The `try_platform_assemble` function (crates/nexus42/src/commands/platform/context.rs:336) now unconditionally returns `None` per KCA-002 B2. This means the `TwoStageAssembly` path in `assemble_context` (line 253-262) is currently unreachable in practice — the guard records a platform success that never occurs.

**Impact:** Low — this is intentional per the locked B2 decision. The code is a clean placeholder for future direct platform API integration.

**Recommendation:** Consider adding a code comment or `#[cfg(feature = "cloud-stage")]` gating to make the dead path explicit, or leave as-is with the existing documentation comment. No action required for V1.24.

**S-002: Plan status in `status.json` still shows `Todo`**

The `.mstar/status.json` file shows the plan status as `"Todo"` (line 9). Since implementation is complete and the branch is under QC review, the status should be `"InReview"` per the Morning Star state machine.

**Impact:** Low — metadata tracking only.

**Recommendation:** @project-manager should update `status.json` to `"InReview"` when QC is dispatched.

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|------------|-------------|------------------|------------|
| S-001 | manual-reasoning | crates/nexus42/src/commands/platform/context.rs:336 | High |
| S-002 | manual-reasoning | .mstar/status.json:9 | High |

## Verification Evidence

### cargo clippy
```
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.42s.
```
**Result:** Clean — zero warnings with `-D warnings` (pedantic + nursery enabled).

### cargo test --workspace
```
test result: ok. 23 passed; 0 failed; 0 ignored
...
test result: ok. 1 passed; 0 failed; 1 ignored
```
**Result:** All tests pass (0 failures across all workspace crates).

### git diff verification
- 5 commits on `feature/v1.24-knowledge-crates-alignment`
- Commits: `f44604f` (plan) → `51852a5` (docs refresh) → `ddc193a` (retire context/assemble) → `7f63bc3` (work-scope KB) → `75fdd8d` (tracker hygiene)

## Architecture Coherence Assessment

### Spec/Code Consistency: ✅ PASS

| Area | Assessment |
|------|------------|
| **Normative specs refreshed** | local-cloud-crate-architecture.md §4 matches 2026-05-22 cargo tree; entity-scope-model.md cloud-domain gap text removed; local-runtime-boundary.md marks retired routes explicitly |
| **KCA-002 B2 (retire context/assemble)** | Cleanly implemented: `DaemonClient::call_assemble` removed; `context/client.rs` deleted; CLI uses in-process `Stage0Assembly` / `TwoStageAssembly`; spec marks route as **Retired** |
| **KCA-003 C2 (work-scope KB)** | Daemon handlers and CLI commands explicitly labeled as **work-scope** file index; comments clearly distinguish from `nexus-kb` (World KB) and `nexus-knowledge` (User knowledge); new test verifies work-scope path layout |
| **Entity-scope model** | §5.3 naming clarifications properly reflected in code comments, docstrings, and CLI help text |
| **Naming consistency** | No drift detected between spec terminology and code implementation |

### Maintainability: ✅ PASS

| Concern | Assessment |
|---------|------------|
| **Dead code removal** | `crates/nexus42/src/context/client.rs` (248 lines) fully removed; `DaemonClient::call_assemble` removed; no dangling references |
| **Documentation quality** | All modified handlers and commands carry expanded doc comments explaining scope boundaries; cross-references to entity-scope-model.md §5.3 present |
| **Test coverage** | New test `resolve_kb_paths_uses_work_scope_layout` added to kb.rs; existing tests updated/retained |
| **Error handling** | Work-scope handlers return explicit errors for missing params; path validation via `nexus_home_layout` helpers |

### Risk Assessment: LOW

- **No breaking API changes** — the retired daemon route was already unregistered
- **No new dependencies** — all changes are doc/comment/code removal within existing crate graph
- **No data migration** — work-scope KB storage format unchanged, only semantics clarified
- **Future compatibility** — deferred items (DF-42, DF-43) properly tracked in deferred-features-cross-version-tracker.md

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 2 |

**Verdict**: Approve

The V1.24 knowledge↔crates alignment changes are architecturally coherent and maintainable. The spec refresh accurately reflects the 2026-05-22 Cargo wiring reality. The two locked decisions (KCA-002=B2 retiring daemon context/assemble; KCA-003=C2 extracting work-scope KB semantics) are cleanly implemented with no contradictions between normative specs and code. All static checks pass. The two Suggestions are non-blocking observations.
