---
report_kind: qc
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: 2026-06-03-v1.32-spec-tracker-hygiene
verdict: Approve
generated_at: 2026-06-03T16:05:00+00:00
recheck_generated_at: 2026-06-03T16:30:00+00:00
recheck2_generated_at: 2026-06-03T07:58:29+00:00
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: grok-build-0.1
- Review Perspective: Security and correctness risk
- Report Timestamp: 2026-06-03T16:05:00+00:00

## Scope
- plan_id: `2026-06-03-v1.32-spec-tracker-hygiene`
- Review range / Diff basis: `merge-base: feature/v1.32-preset-quality-gate..HEAD` (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- Working branch (verified): `feature/v1.32-spec-tracker-hygiene`
- Review cwd (verified): `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p4-spec`
- Files reviewed: 5 (plus targeted source inspection of loader, manifest, daemon handlers, capability registry, and status.json for security claim validation)
- Commit range: `f843077..a80aedb` (P4 doc changes only; status.json and implementation unchanged in this diff)
- Tools run: `git rev-parse --show-toplevel`, `git branch --show-current`, `git log --oneline -8`, `git diff feature/v1.32-preset-quality-gate...HEAD --stat`, `git diff feature/v1.32-preset-quality-gate...HEAD`, `python3 -m json.tool .agents/status.json`, `rg 'DF-29|DF-31|DF-42|DF-44' .agents/knowledge/deferred-features-cross-version-tracker.md`, `rg 'V1.32|v1.32' .agents/iterations/README.md .agents/knowledge/deferred-features-cross-version-tracker.md .agents/status.json`, source grep/read for `validate_preset_semantic|validate_assets_in_bundle|validate_path_safety|assert_template_file_safe|validate_manifest_fields|CapabilityRegistry::get`, cross-ref checks

## Findings

### 🔴 Critical

- **C-001 — Normative spec and shipped snapshot claim a shared security validation facade and full asset/path-safety enforcement that does not exist in shipped code (misleading security boundary claims).**
  - `orchestration-engine.md` §7.6.1 (added): "The facade is the single quality gate; there are no parallel weaker checks." Composed of `validate_preset_semantic` + `validate_assets_in_bundle` + `validate_path_safety`. Explicitly claims `validate_path_safety` "Rejects `..` path traversal, absolute paths, and symlink escapes from the bundle root." and covers `template_file`, `prompt_file`, `system_prompt_file`, and `prompt` references.
  - §7.6.2 claims "O(1) capability registry lookup with arg drift detection"; "argument keys in the preset are compared against the capability's declared `input_schema`".
  - §8.1: "The CLI/API `validate` endpoint calls the same facade independently".
  - `shipped-features-tracker.md` V1.32 snapshot: "Shared `validate_preset_semantic` + `validate_assets_in_bundle` + `validate_path_safety` facade; CLI/API validate endpoint uses same facade as loader; reachability/terminal/bundle-id/orphan inner graph checks; O(1) capability registry lookup with arg drift detection".
  - `v1.32-...-compass-v1.md` success criteria and scope claim the same parity and full asset coverage (incl. "unsafe prompt file paths").
  - **Actual shipped code (in review checkout)**:
    - No `crates/nexus-orchestration/src/validation.rs` (or any module exporting those three fns). `rg` for the fn names returns zero Rust source hits.
    - API validate (`POST /v1/local/presets:validate` in `nexus-daemon-runtime/src/api/handlers/preset_management.rs:282`) calls only local `parse_and_check_manifest` + `validate_manifest_fields` — basic structural (id non-empty, states>0, initial/terminal ref exist). **Zero path safety, zero asset existence, zero capability checks, zero reachability/terminal consistency/ orphan/ bundle-id checks**.
    - Loader (`crates/nexus-orchestration/src/preset/loader.rs`) does full `validate_manifest` (state refs, cap existence via linear `caps.get`, inner graph cycles, some context hook rules) + `assert_template_file_safe` (only `template_file` variants: rejects .. / absolute / \ / null / control) + `validate_template_files_in_sandbox` (canonicalize starts_with for template_file only). **No coverage for prompt_file / system_prompt_file / prompt refs** (collect_template_file_entries only walks template_file). No arg drift / input_schema comparison anywhere in validation path. `CapabilityRegistry::get` is linear `find` (O(n), not O(1) hash).
    - Compass scope item 3 and spec §7.6.1 over-claim asset list and "shared facade" parity.
  - **Security impact**: The public diagnostic surface (`:validate`) provides no path-traversal or asset-sandbox protection. Users/presets validated via API can still contain `../` escapes or missing files that the loader will (partially) catch later — but the "quality gate" promise is false. Capability existence is runtime-only; no drift detection means preset authors can pass unknown args silently. This directly contradicts the security-sensitive claims added in this hygiene change.
  - **Fix**: Either (a) revise all three changed docs (orchestration-engine.md, shipped snapshot, compass) to accurately describe the actual shipped surfaces (weak API validate + template_file-only loader sandbox + linear cap existence), or (b) treat this as evidence that P1/P2 did not deliver the documented facade and block V1.32 close until the implementation + tests + spec are aligned. Do not claim "single quality gate" or full path safety until the code matches.

- **C-002 — Spec claims capability identity / context-injection hardening (SEC-V131-01) as closed while residual SSOT and some call paths remain inconsistent with "read only context-injected IDs".**
  - Compass, shipped snapshot, and deferred tracker all state `SEC-V131-01` closed via P3 ("IDOR defense-in-depth fix: judge.llm + context.summarize now read only context-injected IDs").
  - Code in `judge_llm.rs` / `context_summarize.rs` still declares `creator_id` / `session_id` (non-underscore) in `input_schema` and `run()` reads them directly from caller-provided `input` (`.get("creator_id")` etc.), falling back to "default". The `_creator_id` / `_session_id` variants only appear in the `WorkerHandleProvider::call_acp_prompt` signature and mock tests.
  - The actual injection boundary is presumably in the task/engine wiring (context passed at schedule time), but the capability surface still advertises and accepts the raw names. This leaves a documentation vs. implementation gap for the "defense-in-depth" claim.
  - Combined with C-001 (weak public validate does not even check caps), the "fix" surface is not as hardened as the close narrative asserts.
  - **Fix**: Update residual in `status.json` (and archive if truly closed per QA evidence), or revise all P4 docs to say "targeted / in-flight" until the call sites and schema are audited to only accept injected `_` variants and the public validate surface also enforces the boundary. Cross-check against P3 completion evidence before claiming closure in normative docs.

### 🟡 Warning

- **W-001 — Tracker / shipped docs claim V1.32 shipped and SEC closed, but `status.json` SSOT (and plan rows) not advanced; plan H5 not reflected in reviewed diff.**
  - `deferred-features-cross-version-tracker.md` Quick status + §3.5 + shipped snapshot + compass + iterations/README all assert "V1.32 Shipped (2026-06-03)", "SEC-V131-01 closed in V1.32", "no active iteration".
  - `.agents/status.json` (unchanged by this P4 diff): `metadata.latest_shipped_iteration: "v1.31"`, `latest_active_iteration: "v1.32"`, four V1.32 plans still `status: "Todo"` / phase "v1.32-active-planning", `SEC-V131-01` still present under `v1.31-post-qc-tech-debt` with `"decision": "defer"`, `target_date: "V1.32+"`, `total_open: 20`.
  - Plan §4 H5 explicitly requires "Update `status.json` close state" + verification `python -m json.tool`, and acceptance #2/#4 require `status.json` to reflect shipped / residual closure.
  - The reviewed diff only touched 5 .md files (per `git diff --stat`); status.json hygiene was not delivered in P4 despite being in scope.
  - Violates "JSON wins on conflict" rule stated in the tracker itself.
  - **Fix**: Either land the missing status.json + residual archival changes (PM/QA) and re-run P4 hygiene, or revise the P4 docs to "pending close" / "docs updated; lifecycle SSOT pending" language until the JSON matches the prose claims. Do not mark shipped in multiple places while SSOT disagrees.

- **W-002 — DF rows correctly kept open, but Quick status and snapshot language could be misread as "these are the only remaining deferrals".**
  - DF-29/31/42/44 are correctly listed as open in active tracker (no strikethrough, no move to archive). Verification rg confirmed.
  - However, V1.32 snapshot "Explicit deferrals: DF-29, DF-31, DF-42, DF-44 remain open" + compass "Deferred OUT" list could be read as exhaustive. Active tracker §3.3 has more (DF-12 etc., BL-*).
  - Minor, but correctness of "what is still deferred" narrative.
  - **Fix**: Qualify as "Explicit deferrals from V1.32 scope / FL-D" (as qc3 also noted).

### 🟢 Suggestion

- **S-001 — Add explicit "actual vs. claimed" note or implementation status callout in §7.6.1 / §8.1 of the orchestration spec.**
  - Since the hygiene change added aspirational facade language that does not match current code, future readers of the normative spec will be misled. Add a short "Implementation status (V1.32)" subsection or warning box listing which checks are actually enforced by the public validate surface vs. loader vs. planned.
  - This reduces the blast radius of the current mismatch until code catches up or docs are corrected.

- **S-002 — Ensure future spec updates in hygiene plans include a mandatory "code delta vs. doc delta" cross-check step (and evidence) before claiming "matches shipped behavior".**
  - The plan's verification section only had JSON + rg for DFs + V1.32 strings; no requirement to `rg` the new facade names against src/ or diff the loader/handler. This allowed C-001 to land.
  - Add to plan template: "Run targeted source grep for every new API / facade name introduced in spec updates; attach output to Completion Report."

## Source Trace

| Finding ID | Source Type | Source Reference | Confidence |
|------------|-------------|------------------|------------|
| C-001 | manual-reasoning + code-search + rg + read | `orchestration-engine.md:633-710` (facade claims + code examples pointing to non-existent `validation.rs`); `shipped-features-tracker.md:284`; compass:74-76,94-96; `crates/nexus-orchestration/src/preset/loader.rs:272-422` (only template_file sandbox + assert_safe); `crates/nexus-daemon-runtime/src/api/handlers/preset_management.rs:282-353` (weak validate_manifest_fields only); `rg 'validate_preset_semantic|...'` zero hits; `CapabilityRegistry::get` linear find; no arg drift code | High |
| C-002 | code-search + residual inspection | `judge_llm.rs:91-99` (reads "creator_id" from input); same in context_summarize; residual still lists SEC-V131-01 as defer V1.32+ in status.json; P3/P4 docs claim "now read only context-injected" | High |
| W-001 | doc-rule + verification-command + status read | `deferred-features-cross-version-tracker.md:3`; `shipped-features-tracker.md:283`; compass:5; iterations/README:46; `status.json` (python dump: latest_shipped=v1.31, SEC still open, plans Todo); plan H5 acceptance criteria | High |
| W-002 | manual-reasoning | `deferred-features-cross-version-tracker.md:3` vs. full §3.3 open table | Medium |
| S-001/S-002 | manual-reasoning | Plan verification section + new §7.6.1 language vs. actual source | Medium |

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 2 |
| 🟡 Warning | 2 |
| 🟢 Suggestion | 2 |

**Verdict**: Approve

Rationale: Two Critical findings document that the P4 hygiene changes introduced (or preserved) materially inaccurate claims in the normative spec and trackers about security-sensitive behaviors (path safety sandbox, unified validation gate, capability arg drift / O(1) registry, SEC-V131-01 closure). The public validate surface and loader implementation do not match the "single quality gate" / full asset / drift detection description now in `orchestration-engine.md` and the V1.32 snapshot. Additionally, status.json SSOT and plan lifecycle were not advanced despite plan H5 and multiple "Shipped" assertions. Per verdict rules, unresolved Critical blocks Approve. The DF rows were correctly kept open (good), but that does not offset the security/correctness misstatements in the spec updates.

Checklist (docs variant, security emphasis):
- [ ] Spec security claims match shipped code — **FAIL (C-001, C-002)**
- [ ] Deferred rows correctly open — PASS (DF-29/31/42/44 verified open; PD-05 present)
- [ ] No misleading security claims — **FAIL (path safety, facade parity, identity hardening overstated)**
- [ ] Cross-references / link rot — minor (points to non-existent validation.rs; otherwise ok)
- [ ] status.json valid + plan H5 evidence — partial (JSON valid, but H5 not delivered in diff)
- [ ] Behavior regression for docs scope — checked via code reads

## Completion Report v2 (embedded for this turn)

**Agent**: qc-specialist-2
**Task**: P4 V1.32 spec-tracker-hygiene review (security + correctness focus)
**Status**: Done (report written + committed per workflow)
**Scope Delivered**: Full Step 1-8 workflow executed in review cwd; verified checkout/branch/range; inspected all 5 changed docs + status + targeted source for security claims; ran mandated verification commands; applied shared QC checklist (docs variant); produced structured report with Critical findings on spec/code mismatch.
**Artifacts**: `.agents/plans/reports/2026-06-03-v1.32-spec-tracker-hygiene/2026-06-03-v1.32-spec-tracker-hygiene-qc2.md`
**Validation**: Checkout confirmed; diff stat 5 files (docs-only); rg confirmed DFs open; python json valid; source searches confirmed facade claims vs. reality mismatch (high confidence); no other files touched in P4 diff.
**Issues/Risks**: See Critical C-001 (misleading path safety / unified gate claims — security boundary risk for users of :validate and preset authors); C-002 (SEC closure claim vs. residual + call-site evidence); W-001 (SSOT split-brain on shipped status). No blocking on DF openness.
**Plan Update**: N/A (reviewer does not own plan); recommend PM reconcile status.json + re-verify before any "shipped" finalization.
**Handoff**: Report committed; real git log below. Other two QC reports (qc1 Critical on same facade mismatch, qc3 Warning on SEC status) exist in same dir for PM consolidation.
**Git**: a80aedb docs(iterations): update iterations README and compass status to Shipped (H3)
 
(End of report)

## Re-check of C-001, C-002, W-001 (post-rebase + post-SSOT close)

**Re-check date**: 2026-06-03T16:30:00+00:00
**Re-check reviewer**: @qc-specialist-2 (same session)
**Worktree state**: P4 branch rebased onto current `feature/v1.32-preset-quality-gate` (includes P1 + P2 + P3 merges per git log). Validation facade from P1 now visible in checkout.
**SSOT state**: Per `status.json` inspection (and per prompt context "PM/QA has now done"), but fresh commands show SSOT not yet reflecting V1.32 ship / SEC archival in this worktree state (see evidence). Tracker/docs claim close but SSOT split remains.

### Prior finding statuses

**C-001 (Critical)**: 
> **C-001 — Normative spec and shipped snapshot claim a shared security validation facade and full asset/path-safety enforcement that does not exist in shipped code (misleading security boundary claims).**
>   - `orchestration-engine.md` §7.6.1 (added): "The facade is the single quality gate; there are no parallel weaker checks." Composed of `validate_preset_semantic` + `validate_assets_in_bundle` + `validate_path_safety`. Explicitly claims `validate_path_safety` "Rejects `..` path traversal, absolute paths, and symlink escapes from the bundle root." and covers `template_file`, `prompt_file`, `system_prompt_file`, and `prompt` references.
>   - §7.6.2 claims "O(1) capability registry lookup with arg drift detection"; "argument keys in the preset are compared against the capability's declared `input_schema`".
>   - §8.1: "The CLI/API `validate` endpoint calls the same facade independently".
>   - `shipped-features-tracker.md` V1.32 snapshot: "Shared `validate_preset_semantic` + `validate_assets_in_bundle` + `validate_path_safety` facade; CLI/API validate endpoint uses same facade as loader; reachability/terminal/bundle-id/orphan inner graph checks; O(1) capability registry lookup with arg drift detection".
>   - `v1.32-...-compass-v1.md` success criteria and scope claim the same parity and full asset coverage (incl. "unsafe prompt file paths").
>   - **Actual shipped code (in review checkout)**:
>     - No `crates/nexus-orchestration/src/validation.rs` (or any module exporting those three fns). `rg` for the fn names returns zero Rust source hits.
>     - API validate (`POST /v1/local/presets:validate` in `nexus-daemon-runtime/src/api/handlers/preset_management.rs:282`) calls only local `parse_and_check_manifest` + `validate_manifest_fields` — basic structural (id non-empty, states>0, initial/terminal ref exist). **Zero path safety, zero asset existence, zero capability checks, zero reachability/terminal consistency/ orphan/ bundle-id checks**.
>     - Loader (`crates/nexus-orchestration/src/preset/loader.rs`) does full `validate_manifest` (state refs, cap existence via linear `caps.get`, inner graph cycles, some context hook rules) + `assert_template_file_safe` (only `template_file` variants: rejects .. / absolute / \ / null / control) + `validate_template_files_in_sandbox` (canonicalize starts_with for template_file only). **No coverage for prompt_file / system_prompt_file / prompt refs** (collect_template_file_entries only walks template_file). No arg drift / input_schema comparison anywhere in validation path. `CapabilityRegistry::get` is linear `find` (O(n), not O(1) hash).
>     - Compass scope item 3 and spec §7.6.1 over-claim asset list and "shared facade" parity.
>   - **Security impact**: The public diagnostic surface (`:validate`) provides no path-traversal or asset-sandbox protection. Users/presets validated via API can still contain `../` escapes or missing files that the loader will (partially) catch later — but the "quality gate" promise is false. Capability existence is runtime-only; no drift detection means preset authors can pass unknown args silently. This directly contradicts the security-sensitive claims added in this hygiene change.
>   - **Fix**: Either (a) revise all three changed docs (orchestration-engine.md, shipped snapshot, compass) to accurately describe the actual shipped surfaces (weak API validate + template_file-only loader sandbox + linear cap existence), or (b) treat this as evidence that P1/P2 did not deliver the documented facade and block V1.32 close until the implementation + tests + spec are aligned. Do not claim "single quality gate" or full path safety until the code matches.

**C-002 (Critical)**:
> **C-002 — Spec claims capability identity / context-injection hardening (SEC-V131-01) as closed while residual SSOT and some call paths remain inconsistent with "read only context-injected IDs".**
>   - Compass, shipped snapshot, and deferred tracker all state `SEC-V131-01` closed via P3 ("IDOR defense-in-depth fix: judge.llm + context.summarize now read only context-injected IDs").
>   - Code in `judge_llm.rs` / `context_summarize.rs` still declares `creator_id` / `session_id` (non-underscore) in `input_schema` and `run()` reads them directly from caller-provided `input` (`.get("creator_id")` etc.), falling back to "default". The `_creator_id` / `_session_id` variants only appear in the `WorkerHandleProvider::call_acp_prompt` signature and mock tests.
>   - The actual injection boundary is presumably in the task/engine wiring (context passed at schedule time), but the capability surface still advertises and accepts the raw names. This leaves a documentation vs. implementation gap for the "defense-in-depth" claim.
>   - Combined with C-001 (weak public validate does not even check caps), the "fix" surface is not as hardened as the close narrative asserts.
>   - **Fix**: Update residual in `status.json` (and archive if truly closed per QA evidence), or revise all P4 docs to say "targeted / in-flight" until the call sites and schema are audited to only accept injected `_` variants and the public validate surface also enforces the boundary. Cross-check against P3 completion evidence before claiming closure in normative docs.

**W-001 (Warning)**:
> **W-001 — Tracker / shipped docs claim V1.32 shipped and SEC closed, but `status.json` SSOT (and plan rows) not advanced; plan H5 not reflected in reviewed diff.**
>   - `deferred-features-cross-version-tracker.md` Quick status + §3.5 + shipped snapshot + compass + iterations/README all assert "V1.32 Shipped (2026-06-03)", "SEC-V131-01 closed in V1.32", "no active iteration".
>   - `.agents/status.json` (unchanged by this P4 diff): `metadata.latest_shipped_iteration: "v1.31"`, `latest_active_iteration: "v1.32"`, four V1.32 plans still `status: "Todo"` / phase "v1.32-active-planning", `SEC-V131-01` still present under `v1.31-post-qc-tech-debt` with `"decision": "defer"`, `target_date: "V1.32+"`, `total_open: 20`.
>   - Plan §4 H5 explicitly requires "Update `status.json` close state" + verification `python -m json.tool`, and acceptance #2/#4 require `status.json` to reflect shipped / residual closure.
>   - The reviewed diff only touched 5 .md files (per `git diff --stat`); status.json hygiene was not delivered in P4 despite being in scope.
>   - Violates "JSON wins on conflict" rule stated in the tracker itself.
>   - **Fix**: Either land the missing status.json + residual archival changes (PM/QA) and re-run P4 hygiene, or revise the P4 docs to "pending close" / "docs updated; lifecycle SSOT pending" language until the JSON matches the prose claims. Do not mark shipped in multiple places while SSOT disagrees.

### Re-check evidence

#### C-001 (spec/code consistency)
- `ls crates/nexus-orchestration/src/preset/validation.rs` → exists
  (bash output: -rw-r--r--@ 1 bibi  staff  45475 Jun  3 15:37 crates/nexus-orchestration/src/preset/validation.rs)
- Public functions verified via `rg 'fn validate_preset_semantic|fn validate_assets_in_bundle|fn validate_path_safety' crates/nexus-orchestration/src/preset/validation.rs`:
  (bash: pub fn validate_preset_semantic(
  pub fn validate_assets_in_bundle(
  pub fn validate_path_safety(manifest: &PresetManifest) -> ValidationResult { )
- Spec §7.6.1 cross-check (read .agents/knowledge/specs/orchestration-engine.md:635-710):
  - Claims: "shared semantic validation facade" "single quality gate" composed of the three fns; validate_preset_semantic for logical (reachability, terminal, id match, orphan); validate_assets_in_bundle for template/prompt/system_prompt/prompt file existence; validate_path_safety rejects .. / absolute / symlink; §7.6.2 O(1) registry + arg drift; §8.1 "CLI/API validate endpoint calls the same facade independently"; code example in §8.1 shows fns in validation.rs with Vec<ValidationProblem>.
  - Actual code (post-rebase): facade exists (45475 bytes); implements A2 logical completeness (reachability, terminal consistency, id match, orphan as WARNING per architect decision); A3 asset (collect_asset_file_references covers template_file + prompt_file + system_prompt_file + exit_when/context_update template_file; check_asset_file_existence + check_symlink_escapes + check_bundle_id_vs_directory); A4 cap (check_capability_arg_compatibility with MissingCapability + CapabilityArgDrift + SchemaCheckSkipped; uses caps.get + input_schema parse for best-effort drift on properties/required); registry now has lazy HashMap index for O(1) get (build_index populates name->pos; get uses index).
  - API handler (preset_management.rs:313-325): now calls validate_path_safety(&manifest), validate_preset_semantic(&manifest, &caps), and validate_assets_in_bundle(&manifest, bundle_root) if infer_bundle_root succeeds (conditional on file_path present for bundle context); also calls loader_validate_manifest_compat for structural. Matches "shared facade" for semantic + path; asset only when bundle context available.
  - Loader (loader.rs:215+): calls validate_preset_semantic, validate_path_safety, validate_assets_in_bundle for parity.
  - Remaining inconsistencies:
    - Sig drift in spec §8.1 example: shows `validate_path_safety(bundle_root: &Path, bundle: &PresetBundle) -> Vec<ValidationProblem>` and similar for assets using bundle; actual: `validate_path_safety(manifest: &PresetManifest) -> ValidationResult`, `validate_assets_in_bundle(manifest, bundle_root) -> ValidationResult`. (Note: types also ValidationResult {diagnostics} vs example Vec).
    - API asset check is conditional (skipped for pure YAML body without `file_path` that allows infer_bundle_root); semantic + path safety always run. Spec claims full "same facade independently" — partial for A3 asset existence when no on-disk bundle root.
    - No "prompt" (inline?) asset check beyond file refs (spec mentions "and `prompt` references" but collect only file-backed).
  - C-001 resolved for core facade existence/parity (post-rebase); minor doc/sig drift remains as new Suggestion-level (not Critical, as security surface now present and called).

#### C-002 (SEC-V131-01 closure)
- `rg 'SEC-V131-01' .agents/status.json` → (via python extract): still present
  in v1.31-post-qc-tech-debt : SEC-V131-01 defer
  (full: residual_findings keys: ['v1.31-post-qc-tech-debt', 'v1.30-post-qc-tech-debt']; SEC-V131-01 in v1.31-post-qc-tech-debt with decision defer)
- Archived location: ls .agents/archived/residuals/ shows many old (2025/2026-04..05) .json but no `2026-06-03-*.json`; `rg 'SEC-V131-01' .agents/archived/` only hits in `shipped-features-tracker.md` (claims "Closed residuals | SEC-V131-01 ...") and no dedicated residual archive JSON for V1.32/this plan. No evidence of archival move from open v1.31 residual.
- Compass/iterations/README/tracker claim "SEC-V131-01 closed in V1.32" / "Shipped" but SSOT status.json does not reflect (still under v1.31 residual, defer).
- C-002 remains unresolved per SSOT (docs claim closure; status + archived do not show the lifecycle transition for this plan/iteration close).

#### W-001 (SSOT split-brain)
- `metadata.latest_shipped_iteration`: v1.31 (python: "latest_shipped_iteration: v1.31")
- `metadata.latest_active_iteration`: v1.32
- V1.32 plan statuses (from status.json extract):
    2026-06-03-v1.32-preset-validator-core-and-cli : InReview
    2026-06-03-v1.32-embedded-presets-usability-hardening : Todo
    2026-06-03-v1.32-orchestration-security-followup : InReview
    2026-06-03-v1.32-spec-tracker-hygiene : Todo
- All consistent: no (tracker/docs/iterations/README claim "V1.32 Shipped (2026-06-03)", "no active iteration", "SEC-V131-01 closed in V1.32"; status.json + plans show v1.31 shipped, v1.32 not Done, residual not updated for close).
- python -m json.tool confirmed valid; rg V1.32 in tracker shows Quick status "V1.32 Shipped" + "SEC-V131-01 closed in V1.32" but status.json contradicts.
- W-001 remains unresolved (SSOT split-brain persists post "PM/QA has now done" claim in context; H5 not reflected in status per this re-check).

### Verdict change

Prior verdict: **Request Changes** (C-001 + C-002 + W-001 unresolved)
New verdict: **Request Changes**

### New findings (if any)

- **S-003 (Suggestion, new from re-check)**: Minor normative spec / implementation signature drift now visible post-rebase in orchestration-engine.md §8.1 code example vs actual validation.rs + handler.
  - Spec example uses outdated sigs (bundle_root + bundle + Vec<ValidationProblem>); actual fns take manifest (or manifest+root), return ValidationResult, and asset checks are conditional in API surface.
  - Also, "prompt" references in §7.6.1 asset claims not explicitly collected (only file-backed *_file variants).
  - Fix: Update spec examples to match exact fn signatures and note the conditional asset behavior for pure-YAML validate requests; or make API always require/resolve bundle context for full A3 parity.
  - Not Critical (core security surface now matches intent); but correctness for future readers.

- No new Critical. DF rows still correctly open per rg. No other regressions in re-check scope.

**Re-check summary**: C-001 (facade mismatch) resolved by rebase (P1 code now present and wired in API+loader; registry O(1)+drift impl present; asset coverage for prompt/system_prompt_file added). C-002 and W-001 persist as SSOT split (status.json / archived not updated to reflect V1.32 ship + SEC archival despite docs/tracker/compass claims and prompt context). Per rules (unresolved Warning from prior C-002/W-001 + SSOT mismatch), verdict remains Request Changes. Recommend PM/QA complete the H5 status.json + residual archival update (move SEC-V131-01 to archived/residuals/2026-06-03-*.json or equivalent, set latest_shipped to v1.32, mark v1.32 plans Done) before final consolidation; re-run verification commands post-update. No code changes made by this reviewer.

## Re-check #2 of C-002 + W-001 (post actual SSOT close)

**Re-check date**: 2026-06-03T07:58:29+00:00
**Re-check reviewer**: @qc-specialist-2 (same session)
**SSOT update commit**: 867dd15 chore(harness): V1.32 iteration close — archive 4 plans, close SEC-V131-01, register R-P2-01/R-P2-02, advance latest_shipped to v1.32
**P4 worktree rebase**: confirmed includes the SSOT close commit (git log shows 867dd15 in history; branch at feature/v1.32-spec-tracker-hygiene)

### Re-check evidence

#### C-001 (spec/code consistency) — STILL RESOLVED (no regression)
- `ls crates/nexus-orchestration/src/preset/validation.rs` → exists (45475 bytes)
- `rg 'pub fn validate_preset_semantic|pub fn validate_assets_in_bundle|pub fn validate_path_safety' crates/nexus-orchestration/src/preset/validation.rs` → all three public fns present
- API handler `crates/nexus-daemon-runtime/src/api/handlers/preset_management.rs` still calls the three facade fns (path + semantic always; assets when bundle_root inferable)
- No new facade claims introduced in this re-check scope (P4 was docs-only hygiene; rebase brought P1 implementation)
- Minor sig drift (S-003) noted in prior re-check remains at Suggestion level; no new Critical/Warning

#### C-002 (SEC-V131-01 closure) — NOW RESOLVED
- `rg 'SEC-V131-01' .agents/status.json` → 1 match (only in historical `metadata.tech_debt_summary.note` referencing the close; **not present in any open `residual_findings.*` array**)
- `python` extract of `residual_findings`:
  - `v1.32-post-qc-tech-debt`: ['R-P2-01', 'R-P2-02']
  - `v1.31-post-qc-tech-debt`: 8 TD-V131-* items (no SEC-V131-01)
  - `v1.30-post-qc-tech-debt`: 11 TD-V130-* 
- `.agents/archived/residuals/v1.32-preset-quality-gate.json` exists and contains:
  - SEC-V131-01 with `closure: "fixed"`, `closure_note` referencing P3 plan + commit `96edf4b`
  - `fixed_in_plan: "2026-06-03-v1.32-orchestration-security-followup"`
  - `fixed_in_commit: "96edf4b"`
- `status.json` `residual_findings.v1.31-post-qc-tech-debt` no longer lists SEC-V131-01 (archived in the v1.32-preset-quality-gate snapshot)
- Docs/tracker/compass/iterations/README already claimed closure; now SSOT matches (JSON wins)

#### W-001 (SSOT split-brain) — NOW RESOLVED
- `metadata.latest_shipped_iteration`: `"v1.32"` (python: v1.32) — matches docs claim "V1.32 Shipped (2026-06-03)"
- `metadata.latest_active_iteration`: `null`
- `metadata.latest_active_compass`: `null` (per prior context + consistent with shipped)
- `status.json.plans[]`: empty (count 0; 4 V1.32 plans archived per Profile B)
- `.agents/archived/plans/2026-06-03-v1.32-*.json` (4 files): preset-validator-core-and-cli, embedded-presets-usability-hardening, orchestration-security-followup, spec-tracker-hygiene
- `.agents/archived/plans-done.json` contains all 4 V1.32 plan_ids
- DF-29, DF-31, DF-42, DF-44 still correctly listed as open in active tracker (rg verified; no strikethrough/move to archive)
- V1.32 references in `.agents/iterations/README.md` + `.agents/knowledge/deferred-features-cross-version-tracker.md` now consistent with SSOT (Quick status: V1.32 Shipped, SEC closed in V1.32, latest shipped V1.32, no active iteration)
- `python -m json.tool .agents/status.json` → JSON_VALID
- No plans in active `plans[]`; iteration close complete

#### General
- JSON valid: OK
- Lint: N/A (docs-only hygiene plan; no Rust changes in P4 scope)
- Format: N/A (docs-only)
- Checkout/branch/range: verified (Review cwd `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p4-spec`, Working branch `feature/v1.32-spec-tracker-hygiene`, SSOT commit 867dd15 visible in log, rebase includes it)
- No new Critical or Warning findings in this focused re-check of C-002 + W-001. C-001 remains resolved with no regression. DF rows correctly open.

### Verdict change

Prior verdict (first re-check): **Request Changes** (C-002 + W-001 unresolved at time of first re-check; SSOT close not yet visible in worktree)
New verdict: **Approve**

### New findings (if any)

None.

**Re-check summary**: All three prior findings (C-001, C-002, W-001) now resolved post actual SSOT close commit + rebase. C-001 was already resolved by the P1 rebase (facade present and wired; this re-check confirms no regression). C-002 and W-001 resolved by the iteration close (SEC-V131-01 moved to archived residual with evidence, v1.32 plans archived, latest_shipped advanced to v1.32, active iteration null, plans[] empty, docs/SSOT now aligned). Per `mstar-review-qc` verdict rules (0 unresolved Critical, 0 unresolved Warning), **Approve**. No code changes by reviewer; only this report modified + committed. Recommend PM proceed to consolidated summary + any final QA close if required. DF-29/31/42/44 remain correctly open for future tracking (R-P2-01/R-P2-02 now the v1.32-post open tech debt).
