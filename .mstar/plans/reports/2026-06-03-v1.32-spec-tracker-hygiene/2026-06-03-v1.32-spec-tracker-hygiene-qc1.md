---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: 2026-06-03-v1.32-spec-tracker-hygiene
verdict: Approve
generated_at: 2026-06-03T07:32:38Z
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist
- Runtime Agent ID: qc-specialist
- Runtime Model: openai/gpt-5.5
- Review Perspective: Architecture coherence and maintainability risk
- Report Timestamp: 2026-06-03T07:32:38Z

## Scope
- plan_id: 2026-06-03-v1.32-spec-tracker-hygiene
- Review range / Diff basis: merge-base: feature/v1.32-preset-quality-gate..HEAD (equivalently `git diff feature/v1.32-preset-quality-gate...HEAD`)
- Working branch (verified): feature/v1.32-spec-tracker-hygiene
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p4-spec
- Files reviewed: 8 (`git diff` 5 changed docs, `.mstar/status.json`, `crates/nexus-orchestration/src/preset/loader.rs`, `crates/nexus-daemon-runtime/src/api/mod.rs`)
- Commit range: `feature/v1.32-preset-quality-gate...HEAD`; merge-base verified as `2c065a4170786fcefa2525982bb46b2a5c4218e6`
- Tools run: `git rev-parse --show-toplevel`; `git branch --show-current`; `git log --oneline -8`; `git diff feature/v1.32-preset-quality-gate...HEAD --stat`; `git diff feature/v1.32-preset-quality-gate...HEAD`; `python3 -m json.tool .mstar/status.json`; `rg 'DF-29|DF-31|DF-42|DF-44' .mstar/knowledge/deferred-features-cross-version-tracker.md`; `rg 'V1.32|v1.32' .mstar/iterations/README.md .mstar/knowledge/deferred-features-cross-version-tracker.md .mstar/status.json`; targeted source reads/searches for preset validation implementation.

## Findings
### 🔴 Critical
- **F-001 — Normative spec documents a shipped validation facade/API that is not present in the reviewed checkout.** The new spec text says V1.32 exposes and shares `validate_preset_semantic`, `validate_assets_in_bundle`, and `validate_path_safety`, and that loader plus validate endpoint use this facade as the single quality gate. The reviewed code still routes validation through `load_preset_from_str*` → private `validate_manifest`, with bundle-only `validate_template_files_in_sandbox` and `assert_template_file_safe`; searches found no definitions for the three named facade functions. The code also does not show the documented reachability, bundle-id-vs-directory, orphan-inner-graph warning, asset-existence, or capability argument drift checks. This makes `.mstar/knowledge/specs/orchestration-engine.md` an inaccurate normative SSOT for preset authors and future implementers. -> Fix by either updating the docs to match the actual shipped implementation names/semantics or landing the missing P1/P2 implementation before P4 claims V1.32 shipped behavior.

### 🟡 Warning
- **F-002 — Shipped/tracker docs conflict with `status.json` SSOT for V1.32 close and `SEC-V131-01`.** The changed docs mark V1.32 as shipped and say `SEC-V131-01` is closed, but `.mstar/status.json` in the same reviewed checkout still has `metadata.latest_shipped_iteration: "v1.31"`, `metadata.latest_active_iteration: "v1.32"`, all four V1.32 plan rows at `status: "Todo"`, and `SEC-V131-01` still open under root `residual_findings["v1.31-post-qc-tech-debt"]`. Since the plan acceptance requires V1.32 index/compass status to match `status.json`, the current docs create split-brain lifecycle state even though JSON remains syntactically valid. -> Fix by coordinating PM/QA-owned status/residual lifecycle updates before marking the docs as shipped, or keep the docs in active/pending-close language until `status.json` is advanced and residual archival is complete.

### 🟢 Suggestion
- **F-003 — Keep active tracker wording aligned with shipped status language.** The active tracker FL-D row still says “V1.32 active planning targets preset validator quality gate only” while the same file’s quick status says “V1.32 Shipped”. This is less severe than the SSOT mismatches above, but it weakens the close narrative. -> Change to past-tense shipped wording once the blocking lifecycle/spec mismatches are resolved.

## Source Trace
- Finding ID: F-001
  - Source Type: manual-reasoning + code-search
  - Source Reference: `.mstar/knowledge/specs/orchestration-engine.md` lines 633-710; `crates/nexus-orchestration/src/preset/loader.rs` lines 158-280 and 382-692; `rg 'validate_preset_semantic|validate_assets_in_bundle|validate_path_safety'` returned no Rust files.
  - Confidence: High
- Finding ID: F-002
  - Source Type: doc-rule + verification-command
  - Source Reference: `.mstar/iterations/README.md` line 46; `.mstar/iterations/v1.32-preset-quality-gate-delivery-compass-v1.md` line 5; `.mstar/knowledge/deferred-features-cross-version-tracker.md` line 3; `.mstar/status.json` lines 4-188 and 286-294; required `rg 'V1.32|v1.32' ...` output.
  - Confidence: High
- Finding ID: F-003
  - Source Type: manual-reasoning
  - Source Reference: `.mstar/knowledge/deferred-features-cross-version-tracker.md` lines 3 and 56.
  - Confidence: Medium

## Checklist
- [x] Behavior regression explicitly confirmed for docs scope: cross-reference and code-behavior mismatches checked.
- [x] Blocking spec-interpretation risk checked: F-001 is blocking because the normative spec describes non-present facade semantics.
- [x] Examples/claims accuracy checked against code and `status.json`: F-001/F-002 found mismatches.
- [x] `Working branch` / branch policy consistent: verified `feature/v1.32-spec-tracker-hygiene`.
- [x] `Review cwd` confirmed: `/Users/bibi/workspace/organizations/42ch/nexus/.worktrees/p4-spec`.
- [x] `plan_id` and `Review range` / `Diff basis` copied verbatim.
- [x] DF-29/DF-31/DF-42/DF-44 remain open in active tracker.
- [x] PD-05 remains present/open in active tracker.
- [x] `status.json` was not modified by the reviewed diff and remains valid JSON.

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 1 |
| 🟡 Warning | 1 |
| 🟢 Suggestion | 1 |

**Verdict (prior review)**: Request Changes

## Re-check of C-001 (spec/code consistency after P4 rebase)

**Re-check date**: 2026-06-03T07:40:22Z
**Re-check reviewer**: @qc-specialist (same session)
**Worktree state**: P4 branch rebased onto current `feature/v1.32-preset-quality-gate` (P1 + P2 + P3 merged). P1-P3 code is now visible in the worktree.

### Prior C-001 status

> **F-001 — Normative spec documents a shipped validation facade/API that is not present in the reviewed checkout.** The new spec text says V1.32 exposes and shares `validate_preset_semantic`, `validate_assets_in_bundle`, and `validate_path_safety`, and that loader plus validate endpoint use this facade as the single quality gate. The reviewed code still routes validation through `load_preset_from_str*` → private `validate_manifest`, with bundle-only `validate_template_files_in_sandbox` and `assert_template_file_safe`; searches found no definitions for the three named facade functions. The code also does not show the documented reachability, bundle-id-vs-directory, orphan-inner-graph warning, asset-existence, or capability argument drift checks. This makes `.mstar/knowledge/specs/orchestration-engine.md` an inaccurate normative SSOT for preset authors and future implementers. -> Fix by either updating the docs to match the actual shipped implementation names/semantics or landing the missing P1/P2 implementation before P4 claims V1.32 shipped behavior.

### Re-check evidence

- `ls crates/nexus-orchestration/src/preset/validation.rs` → exists
- `rg 'fn validate_preset_semantic|fn validate_assets_in_bundle|fn validate_path_safety' crates/nexus-orchestration/src/preset/validation.rs` → all 3 public functions found
- Spec §7.6.1 claims cross-checked against the now-visible `validation.rs`:
  - `validate_preset_semantic` exists → matches actual code (`pub fn validate_preset_semantic(manifest: &PresetManifest, caps: &CapabilityRegistry) -> ValidationResult`).
  - A2 reachability / terminal marker consistency / bundle-id checks / orphan inner graph warning live in `validate_preset_semantic` → matches actual code via `check_initial_to_terminal_reachability`, `check_terminal_marker_consistency`, `check_bundle_id_match`, and `check_inner_graph_references`; orphan inner graphs emit `DiagnosticSeverity::Warning`.
  - A4 capability compatibility lives in `validate_preset_semantic` → matches actual code via `check_capability_arg_compatibility`, including missing capability, argument drift, and schema-check-skipped diagnostics.
  - `validate_assets_in_bundle` exists → matches actual code (`pub fn validate_assets_in_bundle(manifest: &PresetManifest, bundle_root: &Path) -> ValidationResult`).
  - A3 asset existence / bundle sandbox checks live in `validate_assets_in_bundle` → matches actual code via `check_asset_file_existence` and `check_symlink_escapes`.
  - `validate_path_safety` exists → matches actual code (`pub fn validate_path_safety(manifest: &PresetManifest) -> ValidationResult`).
  - A3 path traversal / absolute path rejection lives in `validate_path_safety` → matches actual code via `check_path_safety`.
  - Loader and daemon/API validation use the facade → matches actual code: `preset/loader.rs` calls the three validation functions, and `api/handlers/preset_management.rs` calls `validate_path_safety`, `validate_preset_semantic`, and `validate_assets_in_bundle`.
- Any remaining inconsistencies: None for the scoped §7.6.1 C-001 claims. Non-blocking outside-scope note: §8.1's illustrative diagnostic-use snippet still uses shorthand signatures/module path that do not exactly match the actual `preset::validation` exports, but this does not invalidate the resolved C-001 facade/function existence claim.
- JSON validity: OK
- DF-29/31/42/44 still open: confirmed
- V1.32 references in docs: confirmed

### Verdict change

Prior verdict: **Request Changes** (C-001 unresolved)
New verdict: **Approve**

### New findings (if any)

None.
