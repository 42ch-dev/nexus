---
report_kind: qc-review
reviewer: qc-specialist-2
reviewer_index: 2
plan_id: "2026-04-30-v1.12-theme-a-preset-completion"
verdict: Approve
generated_at: "2026-04-30T12:00:00Z"
review_perspective: "Security and correctness (input validation, auth boundaries, sensitive data handling, error paths, state consistency)"
---

# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: glm-5
- Review Perspective: Security and correctness (input validation, auth boundaries, sensitive data handling, error paths, state consistency)
- Report Timestamp: 2026-04-30T12:00:00Z

## Scope
- plan_id: 2026-04-30-v1.12-theme-a-preset-completion
- Review range / Diff basis: `feature/v1.12..HEAD` (5 commits: ca9b955, 4a1b0bc, e72f851, 9d4057f, ee80ab3)
- Working branch (verified): feature/v1.12-theme-a
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 10
- Commit range (identical to Review range): ca9b955..ee80ab3
- Tools run: `git diff`, `cargo test -p nexus42 -p nexus-orchestration`

## Findings
### 🔴 Critical
(None)

### 🟡 Warning
(None)

### 🟢 Suggestion
- **S1: build.rs string interpolation consideration** — The build script at `crates/nexus42/build.rs` interpolates directory names directly into generated Rust code as string literals (line 41: `writeln!(code, "    \"{id}\",")`). While the embedded-presets directory is developer-controlled source code and not user-supplied input, consider adding an assertion or escape validation for unusual characters if this pattern is ever reused for user-controlled directories. Current risk: Low (trusted source only).

## Source Trace
- Finding ID: S1
- Source Type: manual-reasoning
- Source Reference: `crates/nexus42/build.rs` lines 39-42
- Confidence: High

## Security & Correctness Checklist (Reviewer #2 Focus)

### YAML Loading: Depth/Size Limits
- [x] Limits are enforced correctly before full deserialization (size check → parse → depth check → deserialize)
- [x] Default limits are generous (1 MiB size, 10 depth) — appropriate for legitimate presets
- [x] No bypass path exists — all user preset loading paths use `load_preset_from_str_with_limits`
- [x] Depth measurement algorithm handles nested mappings and sequences correctly
- [x] Recursion depth is bounded by parsed Value depth (checked before traversal)

### Path Traversal / Injection
- [x] Preset names validated against path separators (`/`, `\`, `.`, `..`) in `init_preset_at`
- [x] `_system` is the only reserved prefix; other `_` prefixes are allowed but warned
- [x] Template file path validation (existing `template_file_safe_*` tests) remains intact
- [x] No new injection vectors introduced via preset file names

### Sensitive Data Handling
- [x] Error messages are actionable without leaking internal paths or secrets
- [x] YAML parse errors show generic error format (not raw stack traces)
- [x] Size/depth exceed messages include counts only (not sensitive)

### `_` Prefix Handling
- [x] `preset init` accepts `_`-prefixed names except `_system` — documented in help
- [x] `preset list` warns about `_`-prefixed presets being skipped during resolution
- [x] `scan_user_presets` skips `_`-prefixed directories — consistent with warning
- [x] No collision risk: `_`-prefixed user presets cannot override embedded presets

### `preset validate` Side Effects
- [x] Read-only operation confirmed — no file writes, no registry mutations
- [x] No state changes; outputs validation status only

### HashMap Cache Invalidation
- [x] Index built correctly at scan time (preset ID → Vec position)
- [x] mtime comparison catches modifications (tested with 10ms sleep)
- [x] Count mismatch catches additions/removals
- [x] Edge case: new preset not in cache → correctly detected as stale
- [x] Edge case: directory missing but cache non-empty → correctly detected as stale

### build.rs Generated Data
- [x] Deterministic output via alphabetical sorting
- [x] String interpolation from trusted source (repo-controlled embedded-presets)
- [x] No user-controlled directory names used
- [x] `cargo:rerun-if-changed` triggers rebuild on preset changes

### State Consistency
- [x] No partial update paths — errors propagate early
- [x] Scan result Vec and HashMap index built in single pass (atomic construction)
- [x] Preset loading errors become warnings (non-blocking) without corrupting results

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 0 |
| 🟢 Suggestion | 1 |

**Verdict**: Approve

All security and correctness requirements are satisfied. The YAML hardening, cache invalidation, and `_` prefix UX alignment are implemented correctly with appropriate tests. One suggestion noted for build.rs pattern awareness, but current implementation is safe due to trusted source directory.

## Test Results
- `cargo test -p nexus42`: 51 passed, 0 failed
- `cargo test -p nexus-orchestration`: 238 passed, 0 failed
- All new preset-related tests pass (YAML limits, cache invalidation, validate subcommand)

## Residual Notes
No new residuals introduced. Theme A residuals (R-M1-W03/W04/W05/W06) appear to be correctly addressed by this implementation.