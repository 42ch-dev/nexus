---
report_kind: qc
reviewer: qc-specialist
reviewer_index: 1
plan_id: "2026-04-09-v1.1-arch-alignment-blockers"
batch: "v1.1-alignment-residuals (ALIGN closure)"
verdict: "Approve"
generated_at: "2026-04-08"
---

# QC Report #1 — Residual batch (`v1.1-alignment-residuals`)

## Executive summary

Reviewed **`v1.1-alignment-residuals`** @ `35726ab` (1 commit ahead of `origin/main` @ `4246e2d`). Scope closes seven archived ALIGN-* items: precheck `canonical_hash`, `User` fallible wire conversion, `build_validated` → `SyncResult`, optional daemon **sync binding** via `workspace_meta`, CLI warnings on placeholder IDs, and AGENTS documentation for `enum_conversions.rs` + npm SemVer.

Architecture is **coherent** with prior merged TD-1~TD-6 work: precheck rules align with TD-5 hash format; binding is **opt-in** (all three keys or none), avoiding surprise breakage for installs that never set `sync_*` metadata.

**Verdict:** **Approve** — no new Critical/High blockers for this batch.

## Scope

| Field | Value |
|--------|--------|
| **Branch** | `v1.1-alignment-residuals` |
| **HEAD** | `35726abc68969e5c8ae69c01c181919e3af3a0a5` |
| **Merge-base / `main`** | `4246e2d9213ac64b22d30e0a35cbd7aeae37edde` |
| **Review cwd** | `<repository-root>` |

## Findings

### Medium

-none blocking merge-

### Low

- **RS1-L1 — `workspace_meta` sync keys undocumented for operators**  
  Keys `sync_workspace_id`, `sync_world_id`, `sync_creator_id` are behaviorally important once teams enable binding; consider a short `docs/` or `AGENTS.md` operational note (follow-up, not merge-blocking).

### Nit

- **RS1-N1 — `TryFrom<User>` adoption**  
  Only tests/assertions use `try_from`; production call sites converting domain `User` → contract should prefer `TryFrom` over any future `From` reintroduction. No violation in this diff.

## Automated checks (reviewer-verified)

Same commands as `AGENTS.md` CI parity: `validate-schemas`, `pnpm run codegen` (no drift), `cargo +nightly fmt -- --check`, `cargo clippy --all -- -D warnings`, `cargo test --all`, `pnpm run typecheck` — **all pass** on reviewed `HEAD`.

## Residual linkage

Open program items unchanged by this batch: **ALIGN-HASH-01** (cross-stack golden vectors), **ARCH-SYNC-D1** (deferred). No request to reopen closed archived ALIGN rows.
