# Code Review Report

## Reviewer Metadata
- Reviewer: @qc-specialist-2
- Runtime Agent ID: qc-specialist-2
- Runtime Model: bailian-coding-plan/glm-5
- Review Perspective: Security and correctness risk
- Report Timestamp: 2026-05-21T15:30:00Z

## Scope
- plan_id: 2026-05-21-v1.23-architecture-crate-wiring-alignment
- Review range / Diff basis: d7aaba3..HEAD
- Working branch (verified): feature/v1.23-architecture-crate-wiring-alignment
- Review cwd (verified): /Users/bibi/workspace/organizations/42ch/nexus
- Files reviewed: 39
- Commit range: d7aaba3..9f6f38e (17 commits)
- Tools run: cargo test --workspace, cargo clippy --workspace, git diff

## Findings
### 🔴 Critical
- None identified.

### 🟡 Warning
- **W-001**: Misaligned doc comment blocks in `key_block.rs` before `deprecate`, `merge_into`, and `delete` methods.
  - Lines 164-167, 187-188, 199-204 contain misplaced `/// # Errors` doc comments that appear before methods but don't describe them correctly.
  - The doc comment pattern is inconsistent — `delete` method has two `/// # Errors` blocks (L199-204 and L202-204).
  - **Fix**: Remove orphaned doc comments or align them to the correct methods. Cosmetic but degrades readability and could confuse future readers about error conditions.

### 🟢 Suggestion
- **S-001**: `InMemoryKbStore::is_active` uses string matching for status check (`matches!(kb.status.as_str(), "deleted" | "merged" | "deprecated")`). Consider using `KeyBlockStatus` enum comparison for type safety when available from contracts.
  - Current approach is correct but relies on string literals that could drift if status names change.
  - Source: store.rs L129-131

- **S-002**: `KnowledgeEntry::new` generates timestamps using `chrono::Utc::now().to_rfc3339()` without explicit timezone handling. For production storage, consider using a consistent timestamp provider or injected clock for testability.
  - Source: knowledge.rs L38-39, L45-46

- **S-003**: `InMemoryKnowledgeStore::key` uses format string `format!("{user_id}::{entry_id}")` for composite key. The delimiter `::` could theoretically appear in IDs causing collision (though unlikely given UUID prefix). Consider using a more robust separator or explicit key struct.
  - Source: store.rs L94-96

## Source Trace
- Finding ID: W-001
- Source Type: manual-reasoning
- Source Reference: crates/nexus-kb/src/key_block.rs lines 164-167, 187-188, 199-204
- Confidence: High

- Finding ID: S-001
- Source Type: git-diff
- Source Reference: crates/nexus-kb/src/store.rs lines 129-131
- Confidence: Medium

- Finding ID: S-002
- Source Type: git-diff
- Source Reference: crates/nexus-knowledge/src/knowledge.rs lines 38-39
- Confidence: Low

- Finding ID: S-003
- Source Type: manual-reasoning
- Source Reference: crates/nexus-knowledge/src/store.rs lines 94-96
- Confidence: Low

## Security & Correctness Analysis

### Scope Isolation (Entity Scope Model §5.2)

**User-scope (nexus-knowledge)**: ✅ Properly enforced
- `KnowledgeQuery` requires `user_id` field (no default, must be provided)
- `KnowledgeStore::get`, `delete`, `update_tags` all require `user_id` parameter
- `InMemoryKnowledgeStore::list` filters by `query.user_id` before any other filtering
- Composite key `user_id::entry_id` prevents cross-user data access
- Tests: `get_wrong_user_returns_none`, `delete_wrong_user_returns_false`, `update_tags_wrong_user_returns_none`, `user_isolation` verify isolation

**World-scope (nexus-kb)**: ✅ Properly enforced
- `KbQuery` requires `world_id` field (required, not optional)
- `KbStore::list_by_world` and `KbStore::query` filter by `world_id` before other filters
- `InMemoryKbStore::query` checks `kb.world_id != query.world_id` as first filter (L236)
- Tests: `test_query_world_isolation` verifies world-scoped isolation

### Uniqueness Constraint Implementation

**KB Uniqueness**: ✅ Correctly implemented
- Constraint: Under same `world_id`, at most one **active** KB per `(canonical_name, block_type)`
- Active status: `!matches!(kb.status.as_str(), "deleted" | "merged" | "deprecated")`
- `check_uniqueness` function (L137-159):
  - Filters by `world_id`, `canonical_name`, `block_type`, `is_active`
  - Excludes `exclude_id` during updates (prevents self-conflict)
- Insert checks uniqueness (L194-200)
- Update re-checks uniqueness only if name/type changed (L336-346)
- Tests: T4-T7, T21-T22 cover duplicate rejection, different types/worlds, re-insertion after deprecation/merge

**Knowledge Uniqueness**: ✅ Implicitly enforced
- Composite key `(user_id, entry_id)` ensures unique storage
- No separate uniqueness constraint needed beyond ID uniqueness

### Input Validation & Boundary Checks

| Location | Validation | Status |
|----------|------------|--------|
| `KnowledgeStore::store` | `user_id.trim().is_empty()` check | ✅ Present |
| `KnowledgeStore::store` | `content.trim().is_empty()` check | ✅ Present |
| `KeyBlock::confirm` Gate 1 | `membership.can_confirm_canon` check | ✅ Present |
| `KeyBlock::confirm` Gate 2 | `revision` mismatch check | ✅ Present |
| `KeyBlock::confirm` Gate 3 | `canonical_name.trim().is_empty()` check | ✅ Present |
| `KeyBlock::confirm` Gate 4 | `source_anchor.validate_refs` check | ✅ Present |
| `KeyBlock::confirm` Gate 5 | `conflict_check.has_hard_conflicts` check | ✅ Present |

### Error Handling Patterns

- **KnowledgeError**: Uses `thiserror` with `ValidationError` and `InvalidUri` variants. No sensitive data exposure.
- **KbError**: Uses `thiserror` with `PermissionDenied`, `ImmutableConfirmedState`, `AlreadyInState`, `UnresolvedConflict`, `RevisionMismatch`, `ValidationError`, `ExcerptTooLong`. Structured, no sensitive leakage.
- **KbStoreError**: Uses `thiserror` with `Duplicate`, `NotFound`, `Storage`. Error messages include world_id/name/block_type for debugging but not sensitive secrets.

### State Invariants & Transitions

**KeyBlock state machine**: ✅ Coherent
- `provisional` → `confirmed` (via `confirm()`, 5 gates)
- `provisional`/`confirmed` → `deprecated` (via `deprecate()`, guarded)
- `provisional`/`confirmed` → `merged` (via `merge_into()`, guarded)
- `provisional` → `deleted` (via `delete()`, guarded)
- `confirmed` state is immutable for body/source_anchor (`ImmutableConfirmedState` error)
- `AlreadyInState` error prevents double-transition

### Test Coverage for Security/Correctness

| Test | Security Aspect | Result |
|------|-----------------|--------|
| `get_wrong_user_returns_none` | User isolation | ✅ |
| `delete_wrong_user_returns_false` | User isolation | ✅ |
| `update_tags_wrong_user_returns_none` | User isolation | ✅ |
| `user_isolation` | Cross-user leak prevention | ✅ |
| `test_uniqueness_rejects_duplicate` | Uniqueness enforcement | ✅ |
| `test_deleted_allows_reinsertion` | Uniqueness lifecycle | ✅ |
| `test_deprecated_allows_reinsertion` | Uniqueness lifecycle | ✅ |
| `test_merged_allows_reinsertion` | Uniqueness lifecycle | ✅ |
| `test_confirm_without_permission` | Permission gate | ✅ |
| `test_confirm_with_conflict` | Conflict gate | ✅ |
| `test_confirm_with_revision_mismatch` | Version gate | ✅ |
| `test_modify_confirmed_body_rejected` | Immutability invariant | ✅ |
| `test_confirm_without_valid_source_anchor_fails` | Traceability gate | ✅ |
| `test_query_world_isolation` | World isolation | ✅ |
| `store_validates_user_id` | Input validation | ✅ |
| `store_validates_content` | Input validation | ✅ |

## Summary
| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟡 Warning | 1 |
| 🟢 Suggestion | 3 |

**Verdict**: Approve

The implementation correctly enforces scope isolation (User vs World), implements uniqueness constraints properly, validates inputs at critical boundaries, handles errors without information leakage, and maintains coherent state invariants. The single Warning is cosmetic (doc comment misalignment) and does not affect security or correctness.