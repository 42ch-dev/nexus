# QC Consolidated Decision — 2026-04-14-v1.2-soul-lifecycle

**plan_id**: `2026-04-14-v1.2-soul-lifecycle`
**Review range**: `c18dbb1..5c205a8` on `feature/v1.2-ws3a-soul`
**Consolidated by**: project-manager
**Date**: 2026-04-14

## Decision: **Approve** (after fix round)

## Blocking Items: None (R1 + R2 fixed in `30b0dbb`)

## Review Summary

| QC | Verdict | Critical | Warnings | Suggestions |
|----|---------|----------|----------|-------------|
| QC-1 | Request Changes | 2 (C1, C2) | 6 | 7 |
| QC-2 | Request Changes | 0 | 2 (HIGH, MEDIUM) | 3 |
| QC-3 | Request Changes | 1 (R1) | 2 (MEDIUM) | 3 |

## Deduplicated Findings

| ID | Severity | Finding | QC Sources | Resolution |
|----|----------|---------|-----------|------------|
| R1 | Critical | Path traversal via unsanitized `creator_id` in file paths | QC-1(W2), QC-2(HIGH), QC-3(R1) | **Fixed** in `30b0dbb`: `validate_creator_id()` guard added to all file I/O; `assert_creator_id_safe()` defense-in-depth in layout |
| R2 | High | `soul_meta` dead code (table created but never populated) | QC-1(C1), QC-2(MEDIUM), QC-3(R2) | **Fixed** in `30b0dbb`: `upsert_soul_meta` called in CLI init/edit-personality; re-exports wired |

## Residual Findings (deferred, non-blocking)

| ID | Severity | Finding | Decision |
|----|----------|---------|----------|
| R3 | medium | Frontmatter extraction edge cases (fragile `---` delimiter detection) | Defer — hand-rolled parser sufficient for V1.2 MVP; revisit if complex frontmatter formats needed |
| R4 | medium | Integration tests only cover arg parsing, not actual create/load/validate | Defer — unit tests cover file I/O thoroughly; integration tests expanded in T5 |

## Fix Verification

- R1: QA verified `validate_creator_id()` rejects `../../etc/passwd`, `ctr_../evil`, control chars; called in all 5 file I/O functions
- R2: QA verified `upsert_soul_meta` called after `soul init` and `soul edit-personality`
