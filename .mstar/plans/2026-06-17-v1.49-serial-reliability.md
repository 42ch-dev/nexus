# V1.49 Serial Reliability — Plan

**Plan ID**: `2026-06-17-v1.49-serial-reliability`  
**Title**: P3 — Reconcile lock optimization + findings prune CLI + path guard  
**Created**: 2026-06-17  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.49-serial-reliability`  
**Merge target**: `iteration/v1.49`  
**Depends on**: `2026-06-17-v1.49-author-desk-ux`  
**Iteration compass**: [v1.49-novel-narrative-maturity-and-author-desk-delivery-compass-v1.md](../iterations/v1.49-novel-narrative-maturity-and-author-desk-delivery-compass-v1.md)  
**Primary specs**:

- [novel-quality-loop.md](../knowledge/specs/novel-quality-loop.md) §9.4
- [novel-author-experience.md](../knowledge/specs/novel-author-experience.md) §8

**Target residuals**: `R-V148P4-W3`, `R-V148P0-W1`

**Code touch**:

- `crates/nexus-daemon-runtime/src/api/handlers/works.rs` (`reconcile_chapters` lock scope)
- `crates/nexus-local-db/src/findings.rs` (`prune_resolved_findings_older_than`)
- `crates/nexus42/src/commands/creator/works/mod.rs` (prune subcommand)
- `crates/nexus-orchestration/src/auto_chain.rs` (`load_and_parse_review_report`)

---

## 1. Problem Statement

V1.48 P4-fix1 fixed lock **release on error** but reconcile still holds `RuntimeLockGuard` for the full filesystem walk (R-V148P4-W3). Retention DAO exists without CLI (§9.4). Review-report path lacks defense-in-depth guard before prompt injection surfaces grow (R-V148P0-W1).

---

## 2. Goals

1. Shorten or batch runtime lock hold during reconcile (document trade-off in completion report).
2. Wire `creator works findings prune` (or `findings prune`) to `prune_resolved_findings_older_than` with default 90-day retention.
3. Add safe-resolve guard or invariant test for `load_and_parse_review_report`.
4. Close R-V148P4-W3 and R-V148P0-W1.

---

## 3. Non-goals

- Reconcile dry-run (P2).
- F6 lifecycle (P0).

---

## 4. Acceptance Criteria

1. Reconcile lock strategy documented + tested (no regression on W-2 release fix).
2. Prune CLI invokes DAO; hermetic test with seeded old `resolved` rows.
3. Path guard test for review-report resolution under `Works/<work_ref>/`.
4. Residuals closed in status.json.

---

## 5. Tasks

- [ ] T1: Reconcile lock scope refactor (or chunked processing).
- [ ] T2: Prune CLI + `--dry-run` optional preview count.
- [ ] T3: Path guard + test in `review_report.rs` or `auto_chain` tests.
- [ ] T4: Residual closure.

---

## 6. Verification

```bash
cargo test -p nexus-daemon-runtime runtime_lock
cargo test -p nexus-local-db findings
cargo test -p nexus-orchestration review_report
```
