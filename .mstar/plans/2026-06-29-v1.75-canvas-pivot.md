# Plan — V1.75 P0 Canvas-Pivot (parity-close + flip + retire)

**plan_id**: `2026-06-29-v1.75-canvas-pivot`
**Iteration**: V1.75 — [Compass](../iterations/v1.75-canvas-pivot-and-qc-followup-compass-v1.md)
**Status**: Todo
**Owner**: `@frontend-dev` (in-inspector TipTap A3 + chapter-page morph A5 + spec web-ui A7 + tests A8) + `@fullstack-dev` (content DTO/codegen A1 + daemon persistence A2 + dead-code A6 + spec canvas-strategy A7)
**Effort**: XL
**Branch policy**: `Working branch: create feature/v1.75-canvas-pivot from iteration/v1.75`; worktree `.worktrees/v1.75-canvas-pivot/`; merge target `iteration/v1.75`
**Depends on**: P-1 (compass Locked + Phase 2b architect lock)
**Wire contracts**: `wire_contracts_changed: TRUE` — `@42ch/nexus-contracts` 0.10.0 → 0.11.0 (additive `content` field)

## Goal

Execute the canvas-pivot: close the prose-editing parity gap (add outline-prose editing to the canvas), verify parity, then retire the V1.65 whole-document TipTap outline editor. The V1.72 node-granular canvas becomes the sole outline authoring surface.

## Tasks (single-stream; Phase 1 parity-close precedes Phase 2 retire)

**Phase 1 — Parity-close**
- [ ] A1 — Wire DTO: add `content` (string, chapter outline prose) to `schemas/local-api/canvas/outline/outline-patch-chapter-set.schema.json`. Regenerate TS + Rust. Verify the canvas graph projection carries chapter `content` (read path) so the inspector can populate the editor.
- [ ] A2 — Daemon validation + persistence (`crates/nexus-daemon-runtime/src/api/handlers/outline.rs`): extend V1.72 `patch_chapter` to accept/validate `content`; persist atomically with `outline_revision` CAS (temp+rename+fsync). **Body-ownership invariant**: MUST NOT touch `body_path`. 409 on stale revision. Regression tests.
- [ ] A3 — In-inspector TipTap (`apps/web/src/components/canvas/outline-canvas/inspectors/chapter-inspector.tsx`): add TipTap rich-text editor for `content` below metadata fields; EditorToolbar reuse; save via outline.patch_chapter `set.content`; 409 → reuse V1.72 outline conflict modal. Keep inspector ≤ 250 lines (extract TipTap sub-module if needed).
- [ ] A4 — Parity verification: checklist proving canvas inspector matches V1.65 editor capability (rich-text + markdown round-trip via tiptap-markdown); vitest for round-trip.

**Phase 2 — Flip + retire (GATED on A4 parity passing)**
- [ ] A5 — Retire V1.65 editor: morph `apps/web/src/pages/chapter-page.tsx` — remove TipTap editor (`useEditor`/`EditorContent`/`EditorToolbar`) + `usePutChapterOutline` PUT save path + save-state logic; keep read-only body render + Copy path + read-only structure summary; add canvas redirect CTA.
- [ ] A6 — Dead-code removal: audit + remove dead `usePutChapterOutline` hook + V1.65 PUT route (architect decides keep-deprecated vs remove). Coordinate with wire bump.
- [ ] A7 — Spec promotions: `web-ui.md` V1.75 stage (editor retired, canvas primary); `canvas-strategy-surface.md` canvas-pivot shipped; `chapter-content-local-api.md` PUT deprecation.
- [ ] A8 — Tests + a11y: chapter-page morph tests (read-only + CTA); inspector TipTap tests (round-trip + conflict); regression. Accessibility: TipTap in inspector meets WCAG 2.1 AA.

## Acceptance (maps to compass §7 DoD #1–#5, #7)

- V1.65 editor retired (chapter-page morphed; putOutline removed); canvas is sole outline surface.
- Canvas inspector edits chapter outline PROSE via TipTap (rich-text + markdown round-trip); parity checklist passes.
- `outline.patch_chapter` persists `content` atomically with CAS; body_path untouched.
- `@42ch/nexus-contracts` 0.11.0; codegen deterministic; `verify-codegen` green.
- Spec promotions committed; a11y verified.

## Notes

- Architect Phase 2b lock (`content` field name, daemon persistence, inspector placement, wire, dead-code decision) is a P-1 dependency.
- Single-stream within P0 (`single_stream_justified: yes`): Phase 1 (A1→A4) precedes Phase 2 (A5→A8); retire is GATED on parity verification.
- Body-ownership invariant is critical — A2 regression test must prove body_path untouched.
- File-disjoint from P1 (outline vs world-kb/codegen) except codegen coordination (PM-serialized).
