# Plan — V1.67 P-design: Canvas Strategy Surface Exploration (Prepare-only)

**plan_id**: `2026-06-26-v1.67-canvas-strategy-exploration`
**Iteration**: V1.67 · **Wave**: 0 (Prepare) · **Track**: E · **Effort**: M
**Owner**: `@architect` (architecture + React Flow feasibility + DAG↔canvas mapping + write boundary) + `@product-manager` (product thesis + canvas UX + Strategy terminology)
**Status**: Todo
**Depends on**: P-1 (compass)
**Working branch**: `feature/v1.67-canvas-strategy-exploration`
**Compass**: [v1.67-…-delivery-compass-v1.md](../iterations/v1.67/delivery-compass.md) §1.1 Track E (revised 2026-06-26), §0 re-discussion Q4–Q6
**Supersedes**: `2026-06-26-v1.67-body-editor-lock-design` (retired — body-editor direction rejected)

## Goal

Author a **Canvas Strategy Surface Exploration** spec (`canvas-strategy-surface.md`) that locks the product vision + architecture for the V1.68 canvas lead, so V1.68 implement begins with a reviewed design. **No runtime artifact** — spec-only. This is the V1.67 de-risk deliverable for V1.68 (replaces the retired body-editor Draft after the 2026-06-26 product-vision correction).

## Locked inputs (from user re-discussion, compass §0 Q4–Q6)

- **Product thesis**: Nexus is an AI-autonomous creative executor (Codex/design-tool spirit) — human inputs Idea + steers; AI owns prose. Not a manual editor.
- **Three Canvas surfaces** (infinite-canvas graphs): Strategy (Preset) orchestration, Work outline + timeline, World KB browsing. Renderer: **React Flow** (`@xyflow/react`).
- **Core principle**: visualization products must NOT edit raw files directly — edits are structured/node-granular. **TipTap survives as an in-node editing capability.**
- **Terminology**: "Preset" → "策略 / Strategy" (UI/spec wording only; CLI rename deferred — breaking).

## Acceptance criteria

- AC1: `canvas-strategy-surface.md` authored as **Exploration** (no implement authority), covering: product thesis; the 3 canvas surfaces; React Flow feasibility in the Tauri WKWebView + browser-tab (bundle/perf); **strategy-DAG ↔ React Flow node/edge mapping** (states→nodes, transitions→edges, converge→join nodes, inner DAG→sub-flow) grounded in `orchestration-engine.md` §3; shared canvas shell vs per-surface node types; **structured write boundary** (no raw-file mutation — how node-edits flush to preset YAML / outline / KB); "AI owns prose" execution trigger from a canvas steer; Preset→Strategy terminology.
- AC2: Product/UX section (`@product-manager`): canvas UX per surface + Idea-input affordance + Strategy terminology + accessibility of a graph surface (keyboard nav of nodes/edges — a known React Flow concern).
- AC3: Records the V1.65 whole-document outline editor as a canvas-pivot candidate (V1.68+ target; not changed in V1.67).
- AC4: `body-editor.md` archived with pointer stub to this spec (done at PM re-shape); inbound references re-pointed.
- AC5: Stays **Exploration** (V1.68 promotes Draft).
- AC6: QC single-review (spec-only; QC may be skipped with explicit reason per PM routing — confirm at dispatch).

## Tasks (skeleton)

- T1: `@architect` — React Flow feasibility (Tauri WKWebView + browser) + strategy-DAG↔node/edge mapping (read `orchestration-engine.md` §3 + the runtime strategy model) + structured write-boundary design.
- T2: `@product-manager` — canvas UX per surface + Idea-input + Strategy terminology + accessibility notes.
- T3: Integrate → `canvas-strategy-surface.md` Exploration.
- T4: Verify `body-editor.md` archive + pointer + inbound refs re-pointed.

## Out of scope

Any canvas implement (V1.68). Removal/regression of the shipped V1.65 outline editor. CLI/spec rename of `preset` → `strategy` (breaking; deferred). Body editor (rejected direction).

## Dependencies / branch policy

Topic branch `feature/v1.67-canvas-strategy-exploration` from `iteration/v1.67`. Prepare-phase; runs in the Phase 2b re-discussion window. Merge target `iteration/v1.67`.
