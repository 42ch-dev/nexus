# V1.34 Agent Tool Registry Spec — Locked Plan

**Plan ID**: `2026-06-04-v1.34-agent-tool-registry-spec`  
**Title**: P3 — `nexus.*` tool registry normative + handler design  
**Created**: 2026-06-04  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@architect`, `@writing-specialist`  
**Task category**: `docs` + `logic` (design-only; code in P4)  
**Working branch**: `feature/v1.34-agent-tool-registry-spec`  
**Merge target**: `feature/v1.34-creator-workflow-and-agent-tools`  
**Depends on**: `2026-06-04-v1.34-residual-convergence` (doc parallel ok)  
**Primary spec**: [agent-nexus-tool-bridge.md](../knowledge/specs/agent-nexus-tool-bridge.md)

---

## 1. Problem Statement

`acp-capability-set.md` defines many `nexus.*` ids; runtime only implements `fs/*` host tools. P4 needs a frozen registry and handler map.

---

## 2. Goals

1. Finalize [agent-nexus-tool-bridge.md](../knowledge/specs/agent-nexus-tool-bridge.md) §4–7 against architect review.
2. Produce handler design appendix (trait signatures, error codes, `policy_blocked` for assemble).
3. Define JSON parameter schemas for minimal tool set (inline in spec or contracts follow-up).
4. skills-export L1 stub table for minimal ids (full matrix remains DF-50).

**Grill lock:** `nexus.context.assemble` = local/read-only or `policy_blocked` (not cloud).

---

## 3. Non-goals

- HostToolExecutor Rust implementation (P4)
- Full DF-46 capability surface

---

## 4. Tasks

- [ ] T1: Registry table + admission rules
- [ ] T2: Cross-review with orchestration-engine §6.4
- [ ] T3: Contract gap list for `nexus-contracts` (optional follow-up issue)
- [ ] T4: P4 implementer handoff checklist

---

## 5. Acceptance

1. P4 can implement without reopening tool id list.
2. assemble semantics documented with test vectors.
3. No CLI-subprocess tool path in normative docs.

---

## 6. Verification

```bash
rg 'nexus\.work\.|policy_blocked' .mstar/knowledge/specs/agent-nexus-tool-bridge.md
```
