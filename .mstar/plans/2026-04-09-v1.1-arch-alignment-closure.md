# Plan: V1.1 Architecture Alignment — Final Closure

> **For agentic workers:** Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` for task-by-task execution. Prefer `superpowers:verification-before-completion` before marking Done.

**Goal:** **Fully close** the two remaining open residuals under `2026-04-09-v1.1-arch-alignment-blockers` so architecture-alignment technical debt has **no open rows** on that plan id, and cross-stack bundle hashing is **explicitly agreed and tested**.

**Resolution SSOT:** [.mstar/knowledge/architecture-alignment-review.md](.mstar/knowledge/architecture-alignment-review.md) §1.2 — update that section and the header **SSOT last reconciled** date when this plan completes.

**Program SSOT:** [.mstar/status.json](.mstar/status.json) — close **ALIGN-HASH-01** and **ARCH-SYNC-D1** per [plan-convention](https://github.com/btspoony/mstar-harness/blob/main/docs/agents/plan-convention.md) (archive to `archived/residuals/`, remove empty keys, refresh `tech_debt_summary`).

**Tech stack:** Rust (`nexus-sync`, `nexus42d`), JSON Schema (`schemas/`), optional coordination with private `nexus-platform` for golden vectors (spec content is out-of-repo; do not paste local paths into committed artifacts).

---

## Authoritative inputs

| Document | Role |
| -------- | ---- |
| [architecture-alignment-review.md](.mstar/knowledge/architecture-alignment-review.md) | Narrative + TD baseline; §1.2 tracks live resolution state |
| Open residuals in `status.json` **root** `residual_findings["2026-04-09-v1.1-arch-alignment-blockers"]` | Exact titles and severity |

---

## Overview

**Priority:** P1 — blocks declaring architecture-alignment work **fully closed** for GA cross-verify.

**Open residuals (must be zero at Done):**

1. **ALIGN-HASH-01** (medium) — Define cross-stack `canonical_hash` preimage (e.g. serde JSON bytes vs canonical JSON); align `bundle.schema.json` + **golden vectors** with platform consumers.
2. **ARCH-SYNC-D1** (low, defer) — Optionally invoke `SyncClient::push_bundle` from daemon after local stage when platform URL and auth token are configured (eager push; offline-first remains valid without it).

**Estimated effort:** M (ALIGN-HASH-01 dominates; ARCH-SYNC-D1 is smaller if scoped narrowly).

**Dependencies:** ALIGN-HASH-01 may require **cross-repo** agreement with `nexus-platform` — track coordination in plan notes or `notes.json`; do not commit machine-specific paths.

**Working branch:** `feature/v2.0-arch-alignment-closure` or team prefix per [AGENTS.md](../../AGENTS.md) / `status.json` `metadata.branch_naming` (PM assigns exact branch at kickoff).

---

## Task group A — ALIGN-HASH-01 (canonical_hash parity)

**Owner:** `@fullstack-dev`  
**Acceptance criteria:**

- [ ] **Preimage spec** documented in-repo (reachable path under `docs/` or `.mstar/knowledge/` per AGENTS.md boundary — prefer `docs/` if end-user relevant, else knowledge doc appendix): exact byte sequence definition, field ordering rules, and exclusion rules (e.g. optional fields).
- [ ] `schemas/domain/bundle.schema.json` (and any related codegen) **consistent** with that spec; run `pnpm run codegen` and commit generated trees if schemas change.
- [ ] **Golden vectors**: checked-in test vectors (Rust tests in `nexus-sync` or `nexus-contracts`) that hash a fixed minimal bundle payload and assert expected digest **per documented preimage**.
- [ ] **Platform parity checklist**: explicit note in the same doc or plan completion report stating what was verified against `nexus-platform` (without leaking private paths — use neutral placeholders per AGENTS.md).
- [ ] `cargo test` / `cargo clippy --all -- -D warnings` and CI parity commands pass locally.

**Evidence for Done:** PR or merge commit references; QC optional per team gate; `status.json` row updated.

---

## Task group B — ARCH-SYNC-D1 (optional daemon eager push)

**Owner:** `@fullstack-dev`  
**Acceptance criteria:**

- [ ] When `platform` base URL and valid auth are **configured**, daemon sync path may **optionally** call `SyncClient::push_bundle` (or equivalent single entry point) after successful local staging — **no regression** when offline or unconfigured (current offline-first behavior preserved).
- [ ] Feature gated or config-flagged if product wants default-off behavior — document in code comment + short note in daemon sync handler module.
- [ ] Integration or unit tests cover: configured vs unconfigured; no double-push / idempotency respect per existing outbox semantics.

**Evidence for Done:** tests green; behavior described in completion notes.

---

## Pre-merge checklist (mandatory)

Per [AGENTS.md](../../AGENTS.md) plan lifecycle:

1. Close **ALIGN-HASH-01** and **ARCH-SYNC-D1** in **root** `residual_findings` with `lifecycle`, `closed_at`, `closure_note`, `closure_evidence`.
2. Append closed rows to `.mstar/archived/residuals/2026-04-09-v1.1-arch-alignment-blockers.json` (or per-convention archive file).
3. Remove the plan key from `residual_findings` if empty (no empty `[]` keys).
4. Refresh `metadata.tech_debt_summary` (`total_open`, `by_severity`, `by_plan`, `updated_at`).
5. Mark this plan **Done** with `done_at`; optional `archived_record` for plan row compaction.
6. Update [architecture-alignment-review.md](.mstar/knowledge/architecture-alignment-review.md) §1.2 to state **zero open** alignment-blocker residuals and bump **SSOT last reconciled**.

---

## Assignment template (PM → implementer)

```text
plan_id: 2026-04-09-v1.1-arch-alignment-closure
Working branch: <assigned>
Acceptance: Task groups A + B above; pre-merge checklist complete
Evidence: test output excerpts in PR description; status.json diff in same merge
Superpowers: verification-before-completion, executing-plans
```

---

*End of plan*
