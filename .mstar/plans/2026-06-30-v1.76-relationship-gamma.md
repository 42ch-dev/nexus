# Plan — V1.76 P0 World KB Relationship γ (auto-extraction + confidence)

**plan_id**: `2026-06-30-v1.76-relationship-gamma`
**Iteration**: V1.76 — [Compass](../iterations/v1.76-relationship-gamma-and-slate-clear-compass-v1.md)
**Status**: Todo
**Owner**: `@fullstack-dev` (extraction pipeline + migration + daemon + GET filter + tests) + `@frontend-dev` (Suggested pane + confidence-weighting UX + promote UI + web-ui spec)
**Effort**: L
**Branch policy**: `Working branch: create feature/v1.76-relationship-gamma from iteration/v1.76`; worktree `.worktrees/v1.76-relationship/`; merge target `iteration/v1.76`
**Depends on**: P-1 (compass Locked + Phase 2b architect lock)
**Wire contracts**: `wire_contracts_changed: TRUE` — `@42ch/nexus-contracts` 0.11.0 → 0.12.0 (additive `needs_review` + `source` + extraction DTO + `include_suggested`)

## Goal

Complete the World KB relationship surface to γ: extend `nexus.n` extraction to propose relationships from chapter text (needs_review gate), add confidence-weighting/filtering, + author curation (promote/delete).

## Tasks (single-stream; extraction Phase 1 precedes confidence Phase 2)

**Phase 1 — Extraction**
- [ ] A1 — Migration + DTOs: `202606300001_kb_relationships_needs_review.sql` (add `needs_review` + `source`); `WorldKbRelationshipProjection` gains `needs_review` + `source`; extraction relationship-candidate DTO; GET `?include_suggested` param. Regenerate. Bump contracts 0.11.0 → 0.12.0.
- [ ] A2 — Extraction extension (`nexus.n` capability/preset + `extract_finalize`): emit relationship candidates (entity pairs by canonical_name + relation_type + confidence + source anchor); persist via idempotent upsert into `kb_relationships` (`needs_review=1`, `source='extraction'`). Entity-existence prerequisite: skip unconfirmed-entity candidates (log). Dedup composite key.

**Phase 2 — Confidence + curation**
- [ ] A3 — GET graph filter: default `needs_review=0`; `?include_suggested=true` includes. Suggested pane (badge + filter toggle) for triage.
- [ ] A4 — Promotion/curation: author promotes (clears `needs_review` via patch_relationship update) or deletes; reuse V1.74 inspector + table with needs_review/source badge.
- [ ] A5 — Confidence-weighting UX: edge stroke/opacity ∝ confidence; threshold filter slider; confidence-proportional badges. DESIGN.md confidence tokens.
- [ ] A6 — Tests: extraction proposes relationships (mock nexus.n); needs_review filter; promotion clears flag; dedup across rescans; confidence render; entity-existence skip.
- [ ] A7 — Spec promotions (apply architect-drafted wording): entity-scope-model §5.6 extraction + needs_review; world-kb-runtime-architecture; web-ui V1.76 stage; local-api-surface-conventions; canvas-strategy-surface; nexus.n extraction spec.

## Acceptance (compass §7 DoD #1–#6)

- Extraction proposes relationships (needs_review + confidence + source); extract_finalize persists with dedup.
- GET graph default-excludes needs_review; include_suggested surfaces; Suggested pane triages.
- Author promotes/deletes via V1.74 surface.
- Confidence-weighting (edge weighting + threshold filter + badges) meaningful.
- 0.12.0; codegen deterministic; verify-codegen green.
- Spec promotions committed.

## Notes

- Architect Phase 2b lock (extraction output shape, extract_finalize persistence, canonical_name resolution, entity-existence ordering, needs_review schema, GET filter, wire) is a P-1 dependency.
- File-disjoint from P1 except world_kb.rs (P0 GET filter vs P1 B6 enum metric — different functions; PM sequences if needed).
