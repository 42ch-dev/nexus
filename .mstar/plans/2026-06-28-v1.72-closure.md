# Plan: V1.72 Closure

**Plan ID**: `2026-06-28-v1.72-closure`
**Iteration**: V1.72
**Status**: Todo
**Owner**: `@project-manager`
**Effort**: S
**Depends on**: P0 + P1 merged
**Working branch**: direct commit to `iteration/v1.72`
**Merge target**: `iteration/v1.72`

## Scope

- Meta tracking + residual convergence
- Spec promotion
- Profile B compaction
- PR to main

## Tasks

> Architect lock note: closure edits should promote specs exactly as scoped here; do not broaden P-last into implementation or redesign work.

- [ ] Close 4 V1.71 carry-over residuals in `status.json` `residual_findings` (lifecycle: resolved, resolution.commit, resolution.plan_id) — primary responsibility of P1, but P-last verifies and signs off
- [ ] Document 8 V1.72-targeted residuals as durable V1.73 backlog reference (with plan-id pointer) — not closed, not narrative-only
- [ ] Refresh `metadata.tech_debt_summary` (total_open 12 → 8; update `by_target_active`, `by_plan_active`, `refreshed_at`)
- [ ] Promote `canvas-strategy-surface.md` §3.3 surface 2 Draft → **Shipped β** for the Outline+Timeline surface (read + write + conflict + non-spatial alternate view); replace illustrative `work.patch_outline_graph(...)` examples with promoted route names (`outline.patch_structure`, `outline.patch_chapter`, `timeline.patch_event`); record World KB canvas surface remains Draft V1.73+ and Strategy β remains Shipped (V1.71)
- [ ] Amend `web-ui.md` V1.72 stage (Canvas Outline+Timeline Write-Boundary)
- [ ] Extend `local-api-surface-conventions.md` with V1.72 outline patch-route pattern (3 route signatures, DTO names, path-authoritative ids, `base_revision`/`new_revision`, 409/422 envelopes, and `outline_revision:` frontmatter SSOT; mirrors V1.71 patch-route extension)
- [ ] Amend `apps/web/DESIGN.md` + `DESIGN.dark.md` index (outline/timeline canvas write tokens added with the locked token names from the compass and concrete light/dark values from P0)
- [ ] Update `metadata.latest_ship` (V1.72 ship snapshot: wire_contracts_changed=true, 0.7.0→0.8.0, 4 plans Done)
- [ ] Profile B compaction (archive Done plans to `.mstar/archived/plans/`, append string plan IDs only to `.mstar/archived/plans-done.json`, then keep `status.json.plans[]` to non-`Done` rows)
- [ ] Open PR `iteration/v1.72` → `main`
- [ ] Update `deferred-features-cross-version-tracker.md` quick-status (V1.72 shipped; World KB + canvas-pivot remain V1.73+; outline+timeline moved from Draft to Shipped β)
- [ ] Update `iterations/README.md` V1.72 row to Shipped

## Acceptance

- 4 residuals closed in SSOT with proper `resolution` block; 8 deferred residuals have durable V1.73 backlog reference; `tech_debt_summary` shows 8 open
- Specs promoted/amended (canvas-strategy-surface.md §3.3 surface 2 Shipped β, web-ui.md V1.72 stage, local-api-surface-conventions.md outline patch pattern, DESIGN.md outline/timeline write tokens with locked names)
- Profile B compaction clean (`plans-done.json` strings-only, per-file JSONs written)
- PR opened with CI green
- `@42ch/nexus-contracts` version bump 0.7.0 → 0.8.0 reflected in `package.json` and `Cargo.toml` and documented
