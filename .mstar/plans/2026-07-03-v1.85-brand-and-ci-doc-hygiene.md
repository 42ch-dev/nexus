# P1 — V1.85 Brand and CI Doc Hygiene

| Field | Value |
|-------|-------|
| plan_id | `2026-07-03-v1.85-brand-and-ci-doc-hygiene` |
| iteration | V1.85 |
| type | implement (docs / config) |
| Task category | `docs` (secondary: `ops`) |
| status | Todo |
| primary_spec | `.mstar/iterations/v1.85-app-icon-branding-delivery-compass-v1.md` §2 SP-2 |
| closes_residuals | `R-V184CL-QC1-S001`, `R-V184CL-QC2-S001` (see compass §0, §2 SP-2, §5) |
| working_branch | `feature/v1.85-brand-and-ci-doc-hygiene` |
| merge_target | `iteration/v1.85` |
| parallel_with | P0 (`feature/v1.85-app-icon-branding`) — non-overlapping file sets |

## Context

V1.84's QC registered two low, non-blocking residuals deferred to V1.85+. This plan closes both. Doc/config only; no runtime change.

## Specify

- **Problem**:
  - **R-V184CL-QC1-S001** — `apps/web/DESIGN.md` / `DESIGN.dark.md` brand-blue consumption-path prose still describes the old parallel hardcoded scale; V1.84 consolidated `index.css` to `var(--color-blue-700)` but did not amend the DESIGN.md prose (pre-existing drift from V1.83).
  - **R-V184CL-QC2-S001** — the `lfs: true` checkout steps in CI workflows carry no inline comment noting the LFS purpose (brand PNG provenance); `.gitattributes` scopes it, but a one-line comment aids future maintainers.
- **User value**: DESIGN.md prose matches the implemented single-source token reality (maintainability); CI intent is self-documenting.
- **Scope**:
  - `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md`: amend the brand-blue consumption-path prose to reflect that canvas/SOUL/World KB tokens reference `var(--color-blue-700)` / `color-mix` (the V1.84 consolidation), not a parallel hardcoded scale.
  - `.github/workflows/ci.yml` + `.github/workflows/desktop-build.yml`: add a one-line comment at each `lfs: true` checkout noting "Git LFS — fetch brand PNG provenance (`packages/nexus-ui/assets/logos/*.png`)".
- **Non-goals**:
  - Any root `DESIGN.md` / `DESIGN.dark.md` brand-value change (consumption prose only).
  - Any `apps/desktop/**`, `apps/web/src/**`, or `packages/nexus-ui/**` change.
  - Any runtime/behavior change.
- **DoD (draft)**: both residuals `lifecycle: resolved`; prose matches V1.84 token reality; comments present.

## Clarify

- **Prose scope (QC1)?** Only the brand-blue consumption-path paragraphs/tables that are now inaccurate. Do NOT rewrite unrelated DESIGN.md sections; surgical amendment. Verify against the V1.84 `index.css` token reality before editing.
- **Comment wording (QC2)?** One concise line per `lfs: true` step; cite the `.gitattributes` scope (brand PNGs).
- No open high-impact ambiguity.

## Plan

- **QC1**: Read the brand-blue sections of `apps/web/DESIGN.md` + `DESIGN.dark.md`; identify drift vs the V1.84 `var(--color-blue-700)` + `color-mix` reality; amend the consumption-path prose (not values). Ground truth: `rg "var\(--color-blue-700\)|color-mix" apps/web/src/index.css` (16 matches across canvas node-border-selected, World KB entity-card stroke selected, World KB focus ring, World KB relationship-grounded badge, SOUL visualization drift-band fill, and the global focus-visible ring — all single-sourced through CSS vars, no parallel hardcoded scale).
- **QC2**: At each `lfs: true` in `.github/workflows/ci.yml` + `desktop-build.yml`, add `# Git LFS — brand PNG provenance (packages/nexus-ui/assets/logos/*.png)` (or equivalent concise wording) on the line above.
- **Verify**: YAML still well-formed; DESIGN.md prose reads accurately; no values changed.

## Tasks

- [ ] T1 — QC1: amend `apps/web/DESIGN.md` + `DESIGN.dark.md` brand-blue consumption-path prose.
- [ ] T2 — QC2: add LFS-purpose comment at each `lfs: true` checkout.
- [ ] T3 — Verify (YAML valid; prose accurate); commit; update `status.json` residuals → resolved.

## Acceptance

- `R-V184CL-QC1-S001` resolved: DESIGN.md prose matches V1.84 single-source token reality.
- `R-V184CL-QC2-S001` resolved: `lfs: true` steps carry a purpose comment.
- No root DESIGN brand-value change; no runtime change; no overlap with P0 files.
