# P1 — Setup Wizard UI Polish

**Goal:** Close V1.100 smoke residuals for Back button placement, Steps indicator, and daemon status poll/subscription timing.

**Architecture:** Studio-first chrome fixtures, then App layout and subscription fixes. Poll work is **timing/dedup only** — no event-bus rewrite, no schema change (`wire_contracts_changed: false`).

**Tech Stack:** React setup wizard, Design Studio, Vitest.

**Execution:** mstar-sdd

**Tier:** **Must (P1)** — required for iteration Must completeness. Runs after P0. Not Stretch.

## Prepare Gates

- specify: done
- clarify: done
- plan: done

> Product + architect locks are coherent. Poll remains timing/subscription only — escalate if an event-bus rewrite appears necessary.

## Product Framing

**User value:** Setup chrome feels intentional: Back is where expected, Steps reflect progress, daemon status updates predictably.

**Problem statement:** Residuals `R-V1100P0SMOKE-UI-BACK` (Back), `R-V1100P0SMOKE-UI-STEPS` (Steps), `R-V1100P0SMOKE-UI-POLL` (Poll timing/subscription).

**Target state:** All three closed on **automated** evidence; human smoke remains a separate gate and does not block automated Done.

**Non-goals:** New steps, Settings shell (**DF-70**), daemon FSM / event-bus rewrite, schema changes, Stretch Select (P2), AgentPicker product work (P0).

## Clarify (product-locked)

| Topic | Decision |
|-------|----------|
| Must vs Stretch | This plan is **Must**. Closing BACK/STEPS/POLL is required; P2 Select is unrelated Stretch. |
| Order | After P0 (compass: P0 → P1 → P2). |
| Poll scope | **LOCKED:** Timing + subscription dedup only; no new global event architecture; no schema. |
| Residuals | Close or re-scope all three IDs with automated evidence. |
| Smoke | Human interactive smoke is separate; not an automated Done blocker. |
| Wire | `wire_contracts_changed: false`. |

### Architect note

None open for product scope. Implementers escalate only if Poll fix appears to require event-bus rewrite or schema change (would violate Non-Goals — return to PM/architect).

## Global Constraints

- Primary spec: `.mstar/iterations/v1.101/specs/setup-wizard-ui-polish.md`.
- Process: `.mstar/iterations/v1.101/guides/studio-first-visual-then-app.md`.
- Sequence: after P0 per compass (P0 → P1 → P2).
- `wire_contracts_changed: false`.
- Interactive smoke is not an automated Done blocker.

---

### Task 1: Lock Chrome Polish Contract

**Files:**
- Modify: `.mstar/iterations/v1.101/specs/setup-wizard-ui-polish.md`
- Read: setup wizard layout / Steps / daemon step components

- [ ] Document correct Back placement and Steps state mapping.
- [ ] Document single-subscription / poll expectations for daemon status.
- [ ] Confirm no event-bus rewrite.

### Task 2: Design Studio — Wizard Chrome Fixtures

**Files:**
- Create/Modify: Design Studio fixtures for Back, Steps, daemon status chips

- [ ] Cover welcome/daemon/agent/done Steps states.
- [ ] Cover daemon starting/running/error visuals.
- [ ] Visual acceptance before Task 3.

### Task 3: App Wiring — Layout And Poll Fixes

**Files:**
- Modify: setup wizard layout / step chrome components
- Modify: `setup-step-daemon` (and any duplicated health subscriptions)
- Test: Vitest

- [ ] Fix Back placement.
- [ ] Fix Steps indicator display/state.
- [ ] Deduplicate subscriptions; make poll/update timing coherent.
- [ ] Automated tests for the three behaviors.

### Task 4: Residual Disposition

- [ ] Close or re-scope the three UI residuals with **automated** evidence.
- [ ] Leave human smoke as separate gate note (do **not** block automated Done).

## Plan self-review (PM before locked)

1. All three residuals mapped (`BACK` / `STEPS` / `POLL`).
2. Studio-before-App explicit.
3. Poll scope constrained; Must ≠ Stretch.
