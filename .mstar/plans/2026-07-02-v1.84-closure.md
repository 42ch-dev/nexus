# P-last — V1.84 Closure (QC, QA, Compound, and Closeout)

| Field | Value |
|-------|-------|
| plan_id | `2026-07-02-v1.84-closure` |
| iteration | V1.84 |
| type | closure |
| status | Todo |
| primary_spec | `.mstar/iterations/v1.84-brand-polish-and-token-consolidation-delivery-compass-v1.md` |
| working_branch | n/a (runs on `iteration/v1.84` HEAD) |
| merge_target | `iteration/v1.84` |

## Context

Closure plan for V1.84. After P0 (brand polish) and P1 (CI/tooling hygiene) merge to `iteration/v1.84`, this plan runs the QC tri-review + QA verification on the integrated HEAD, performs the iteration-close compound round, updates the roadmap, does Profile B compaction, and opens the PR to `main`.

## Specify

- **Problem**: P0 (author-facing brand polish + token consolidation) and P1 (infra CI/tooling hygiene) deliver on parallel branches; the iteration needs integrated review/verification and formal closeout before PR.
- **User value**: Confidence that the blue-scale token path is single-sourced (canvas surfaces already render brand-cyan from V1.83; V1.84 removes the parallel hardcoded scale), CI hygiene is in place, and all 5 V1.83 residuals are genuinely closed with clear separation of token-consolidation (P0) vs foundation (P1) work.
- **Scope**: QC tri-review on `iteration/v1.84` HEAD; QA verification (light/dark visual spot-check of the five canvas surfaces + CI green incl. new nexus-ui job + clean clone LFS + `rg` invariant); compound round; roadmap update; Profile B compaction; PR to `main`.
- **Non-goals**: new implementation beyond fix-wave responses to QC findings; reopening V1.83 scope or the locked direction.
- **DoD (draft)**: QC 3/3 Approve (after fix-waves if any); QA Pass; 5 residuals `lifecycle: resolved`; compass `status: completed` + `end_date`; PR to `main` open.

## Clarify

- **QC scope** = integrated `iteration/v1.84` HEAD vs `main` (`iteration_base_branch`). Review range covers P0 + P1.
- **QA scope** = dark/light canvas visual spot-check (P0, regression guard) + CI green incl. new `nexus-ui` job + clean-clone LFS fetch (P1) + `wire_contracts_changed: false` confirmation.
- **Contract-unchanged verification (mechanical)**: `git diff --name-only main...iteration/v1.84` must list **no** path under `schemas/`, `packages/nexus-contracts/`, `crates/nexus-contracts/`, `crates/nexus-daemon-runtime/`, or `apps/nexus42/` (CLI/daemon), and `git diff main...iteration/v1.84 -- packages/nexus-ui/package.json` must show **no** change to the `exports` block. One-liner: `git diff --name-only main...iteration/v1.84 | rg -E '^(schemas/|packages/nexus-contracts/|crates/nexus-contracts/|crates/nexus-daemon-runtime/|apps/nexus42/)'` → empty. Confirms `wire_contracts_changed: false` and no new export surface.
- **Token invariants (mechanical)**: `rg "#1e3a5f|#25d1e0" apps/web/src/index.css` → only L60 + L207; `rg "rgba\(30,? *58,? *95|rgba\(37,? *209,? *224" apps/web/src/index.css` → zero; `rg "rgba\(30,? *58,? *95|rgba\(37,? *209,? *224|#1e3a5f|#25d1e0" apps/web/src/components/canvas/ apps/web/src/pages/` → zero.
- **Compound candidates**: the `color-mix`-from-brand-var tint convention (P0 adopts the existing house pattern; candidate only if it crystallizes a *new* reusable rule, not if it restates an established one) and the CI LFS/nexus-ui-job pattern (if generalizable). Decide via `mstar-compound` Q1–Q8.
- No open high-impact ambiguity.

## Plan

- **QC tri-review** (`mstar-review-qc`): qc1 (architecture/maintainability — token-reference discipline, no parallel scale), qc2 (security/correctness — no accidental contrast/visibility regression), qc3 (performance/reliability — CI cost of LFS + new job; warning-suppression safety). Targeted re-review after any fix-wave.
- **QA**: visual spot-check the five canvas surfaces in dark mode (regression guard — component layer already brand-cyan); confirm CI green on the integration branch (incl. new `nexus-ui` job); confirm the two `rg` token invariants (§Clarify) in `index.css`; confirm clean-clone LFS fetch; confirm `wire_contracts_changed: false` via the `git diff --name-only` one-liner (§Clarify) and no `nexus-ui/package.json` `exports` change.
- **Compound** (`mstar-compound`): crystallize reusable knowledge (tint-from-brand-var convention; CI package-job pattern) if it passes Q1–Q8; register in `knowledge/README.md`.
- **Roadmap**: compass §7 current iteration → `delivered`; next iteration updated.
- **Profile B compaction**: archive the 4 V1.84 plan rows to `archived/plans/<id>.json` + `plans-done.json` string index; drop from `status.json.plans[]`.
- **PR**: open PR `iteration/v1.84` → `main`; wait for CI.

## Tasks

- [ ] T1 — Dispatch QC tri-review (qc1/qc2/qc3) on `iteration/v1.84` HEAD; address fix-wave findings.
- [ ] T2 — QA verification: dark/light canvas spot-check (regression guard) + CI green (incl. `nexus-ui` job) + clean-clone LFS fetch + token `rg` invariants + `wire_contracts_changed: false` (`git diff --name-only` one-liner) + no `nexus-ui/package.json` `exports` change.
- [ ] T3 — Compound round + `knowledge/README.md` registration; CONCEPTS.md if needed.
- [ ] T4 — Compass §7 roadmap → `delivered`; §10 Compound Summary + §11 Retrospective filled; frontmatter `status: completed` + `end_date`.
- [ ] T5 — Profile B compaction (archive 4 plans + `plans-done.json` index + layout invariant verified).
- [ ] T6 — `status.json` residuals W001–W005 → `lifecycle: resolved` with resolution commits; open PR to `main`.
- [ ] T7 — PM signoff.

## Acceptance

- QC tri-review 3/3 Approve (after any fix-waves); QA Pass.
- All 5 V1.83 residuals (`R-V183CLOSE-W001`–`W005`) resolved with commits; P0 closes W004/W005; P1 closes W001/W002/W003.
- `wire_contracts_changed: false` (verified: `git diff --name-only main...iteration/v1.84 | rg -E '^(schemas/|packages/nexus-contracts/|crates/nexus-contracts/|crates/nexus-daemon-runtime/|apps/nexus42/)'` → empty); no `@42ch/nexus-ui` `package.json` `exports` change.
- Compass frontmatter `status: completed` + `end_date`; §7 roadmap updated; §10/§11 filled; Profile B compaction done (layout invariant verified); PR to `main` open.
