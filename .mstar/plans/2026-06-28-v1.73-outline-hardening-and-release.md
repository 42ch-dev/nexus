# Plan — V1.73 P1 Outline β Hardening + Release Hardening (Track B, companion)

**plan_id**: `2026-06-28-v1.73-outline-hardening-and-release`
**Iteration**: V1.73 — [Compass](../iterations/v1.73-canvas-world-kb-beta-and-outline-hardening-compass-v1.md)
**Status**: Todo
**Owner**: `@fullstack-dev` (B1–B4 outline validation) + `@frontend-dev` (B5 outline-canvas split) + `@ops-engineer` (B6–B7 release hardening)
**Effort**: M
**Track**: B (companion)
**Branch policy**: `Working branch: create feature/v1.73-outline-hardening-and-release from iteration/v1.73`; worktree `.worktrees/v1.73-outline-hardening/`; merge target `iteration/v1.73`.
**Depends on**: P-1 (compass Locked)
**wire_contracts_changed**: FALSE
**Superpowers**: `using-git-worktrees` (parallel with P0 on same repo)

## Goal

Close 7 V1.72 carry-over residuals: 4 MEDIUM outline validation gaps + outline-canvas.tsx split + 2 release-hardening items. Surgical commits per cluster. Full scope: compass §1.1 B1–B7.

## Tasks

- [ ] T1 — B1 close `R-V172P0-QC2-001` (MEDIUM slug format validation): add slug format validation (kebab-case, length bounds, uniqueness within Work) in `crates/nexus-daemon-runtime/src/handlers/outline.rs` outline validation path; return structured `OutlineValidationError` (422) on violation. Regression test asserting valid slug passes + invalid rejected.
- [ ] T2 — B2 close `R-V172P0-QC2-002` (MEDIUM volume existence/pre-creation): add volume existence check before chapter volume binding (`attach_to_volume` / `outline.patch_chapter` volume field); return 422 if target volume missing (no silent auto-create). Regression test.
- [ ] T3 — B3 close `R-V172P0-QC2-003` (MEDIUM foreshadow temporal order): add source-before-target temporal-order validation for `link_foreshadow`; source event scheduled at or before target realization point; return 422 on violation. Regression test.
- [ ] T4 — B4 close `R-V172P0-QC2-004` (MEDIUM published-chapter structural edit guard route-specific): extend published-chapter structural edit guard from `patch_chapter` only to also cover `patch_structure` operations that mutate a published chapter's containment/ordering; return 422 on violation. Regression test.
- [ ] T5 — B5 close `R-V172P0-QC1-002` (LOW outline-canvas.tsx 825-line monolith split): split `apps/web/src/features/canvas/outline/outline-canvas.tsx` into focused modules per V1.71 Strategy pattern:
  - `outline-canvas.tsx` (thin orchestrator + composition root + public re-export facade)
  - `outline-canvas/graph-projection.ts`
  - `outline-canvas/inspectors/chapter-inspector.tsx`
  - `outline-canvas/inspectors/event-inspector.tsx`
  - `outline-canvas/inspectors/structure-inspector.tsx`
  - `outline-canvas/conflict-modal.tsx`
  - `outline-canvas/__tests__/`
  - Preserve all behavior; no API/signature changes; each split file ≤ 250 lines (outline denser than strategy). Existing vitest tests pass unchanged.
- [ ] T6 — B6 close `R-V171P1-QC1-001` (LOW tauri.conf.json signingIdentity clarity): resolve config-vs-env-var ambiguity in `apps/desktop/tauri.conf.json` — document single source of truth (env-var wins when present; config field is fallback/default); config comment + `apps/desktop/SIGNING.md` update.
- [ ] T7 — B7 close `R-V172P1-QC3-003` (LOW desktop release Rust cache coverage): add `Swatinem/rust-cache` (or equivalent) to `.github/workflows/desktop-release.yml` Rust build leg; verify cache hit on second run.

## Acceptance (maps to compass §7 DoD #5)

- 4 MEDIUM outline validation gaps closed (B1–B4) with regression tests proving valid input passes + invalid rejected with structured 422.
- outline-canvas.tsx split into ≤250-line modules (B5); behavior preserved; existing tests pass unchanged.
- tauri.conf.json signing clarity (B6); desktop-release.yml Rust cache (B7).
- All 7 residuals marked `lifecycle: resolved` in `status.json` with resolution commit + plan_id.

## Files touched

`crates/nexus-daemon-runtime/src/handlers/outline.rs` (B1–B4 validation), `crates/nexus-daemon-runtime/tests/outline_patch.rs` (B1–B4 regression tests), `apps/web/src/features/canvas/outline/outline-canvas.tsx` (B5 thin orchestrator/public facade), `apps/web/src/features/canvas/outline/outline-canvas/**` (B5 split modules), `apps/desktop/tauri.conf.json` (B6 comment + clarity), `apps/desktop/SIGNING.md` (B6 doc), `.github/workflows/desktop-release.yml` (B7 Rust cache).

## Notes

- **Single-stream within P1**: 7 residuals are surgical and per-cluster. `@ops-engineer` leads B6–B7; `@frontend-dev` leads B5; `@fullstack-dev` leads B1–B4. Coordinate within one plan. `single_stream_justified: yes`.
- **File-disjoint from P0**: P1 owns existing `outline/**` split + outline validation + CI/config; P0 owns new `world-kb/**`. P1 must preserve `outline-canvas.tsx` public export facade; P0 imports through facade only until B5 merges.
- B1–B4 are validation hardening on the V1.72 patch routes — no endpoint change, no wire change. Validation only rejects genuinely-invalid inputs (the 4 MEDIUM gaps are real correctness issues); existing valid patches continue to pass.
