---
plan_id: 2026-07-05-v1.91-closure
status: Todo
type: closeout
iteration: V1.91
---

# P-last — Closure: QC/QA, Tracker, Compound, Compaction, PR

## 1. specify

### Problem statement

After P0 and P1 merge to `iteration/v1.91`, the iteration needs formal review, verification, hygiene, and delivery.

### Scope (P-last closeout)

- QC tri-review of the combined `iteration/v1.91` diff (P-1 + P0 + P1) against `main`.
- QA verification of the **verifiable acceptance criteria** in compass §5 (P0 headline + P1 companion).
- Hygiene: DESIGN.md (reading-chrome tokens), schemas + codegen verification (`schema_drift_detection`, `verify-codegen`), AGENTS.md (if touched), plan/compass cross-references, non-goals enforcement evidence.
- Update deferred-features tracker: BL-13 (profile-specific reading chrome) marked complete; note companion batch triage improvement.
- Compound round (profile-aware chrome switch pattern, additive bulk PATCH helper, client-side CSV export from filtered list).
- Profile B compaction.
- Open PR `iteration/v1.91` → `main` with target_branch=main.

P-last starts only after P-1, P0, and P1 are all `Done` on `iteration/v1.91`.

### Non-goals

- No new feature implementation.
- No new iteration planning beyond next-iteration pointers.

## 2. clarify

### Review scope

- QC tri-review covers the full combined diff on `iteration/v1.91` (P-1 contracts + P0 reading chrome + P1 findings batch) against `main`. Must confirm:
  - P0 is strictly read-only and frontend-only (no `body_path`/outline writes, no backend files, no schemas, no crates touched).
  - P1 is additive batch only (no state machine changes, no P0 chrome files touched, no existing endpoint refactors).
  - Schemas + codegen are in the diff and pass `schema_drift_detection` / `verify-codegen` gates.
  - Non-goals in compass §6 were respected (no TLS, no canvas, no auto-fix, no new profiles, no backend export).
- QA executes the full gate suite and **explicitly verifies every item in compass §5 Acceptance Criteria (verifiable)**, including per-profile visual evidence for P0 and bulk PATCH integration test + UI batch flows for P1.

### Tracker update

- `deferred-features-cross-version-tracker.md`: BL-13 (profile-specific reading chrome) row moves to shipped archive with V1.91 note. Add a line or sub-entry noting the companion "findings batch triage" improvement delivered in the same iteration. Update quick-status to "V1.91 shipped (reading chrome + batch triage)".

### Compound

- Primary candidates (if patterns warrant crystallization):
  - Profile-aware read-only chrome switch driven by `work_profile` + DESIGN.md tokens (P0).
  - Additive bulk PATCH helper endpoint pattern: partial-success loop over existing DAO, inline `not_found`/`conflict` in success response (not HTTP errors), cap enforcement (P1).
  - Client-side CSV export from in-memory filtered list (P1).
  - JSON Schema creation + codegen workflow for additive cross-language DTOs consumed by `apps/web` (P-1/P1).
- If no new knowledge documents are produced, P-last still records the decision and rationale (per mstar-compound discipline).

## 3. plan

### Target state

- PR #? from `iteration/v1.91` to `main` with all gates green.
- Compass `status: completed` + `end_date`.
- `status.json` plans compacted to Profile B.
- Knowledge docs added/updated if compound produces any.

### Verification plan

- Full CI green.
- QC tri-review consolidated Approve.
- QA sign-off.

## 4. tasks

| ID | Task | Owner | Acceptance |
|---|---|---|---|
| T1 | Run full CI on `iteration/v1.91` (after P0 + P1 merged) | `@qa-engineer` | All gates green |
| T2 | QC tri-review (combined diff) | `@qc-specialist`, `@qc-specialist-2`, `@qc-specialist-3` | Consolidated Approve; non-goals + P0/P1 separation cited |
| T3 | Fix any QC/QA issues (P0 or P1 owners) | `@frontend-dev` / `@fullstack-dev` | Re-review Approve |
| T4 | Verify compass §5 AC (verifiable) item-by-item with evidence | `@qa-engineer` + PM | All checkboxes have passing evidence (tests, screenshots, integration test output) |
| T5 | Update deferred-features tracker | `@project-manager` | BL-13 shipped; companion batch note added; quick-status = V1.91 |
| T6 | Compound round (or explicit "none produced" note) | `@project-manager` | Knowledge docs written or rationale recorded |
| T7 | Profile B compaction | `@project-manager` | plans-done.json + archived/plans/<id>.json updated per project rules |
| T8 | Update compass (status: completed + end_date), iterations README, status.json | `@project-manager` | All pointers consistent |
| T9 | Open PR `iteration/v1.91` → `main` (target_branch=main) | `@project-manager` | PR created; links to compass + all plan Completion Reports |

## 5. notes

- P-last starts only after P-1, P0, and P1 are marked `Done` on the integration branch.
- P-last is PM-driven; delegates QC/QA to specialists and dev fixes to the original implementers.
- QA verification **must** walk the compass §5 "Acceptance Criteria (verifiable)" list item-by-item with concrete evidence (not just "tests pass").
- Non-goals enforcement (compass §6) is part of QC scope; any piggybacking of TLS, canvas, auto-fix, etc. is a hard reject.
- Do not skip compound (or the "none produced" rationale) even if the round yields zero new knowledge documents.
- After PR merge to `main`, the integration branch `iteration/v1.91` may be retired per project branch policy.
