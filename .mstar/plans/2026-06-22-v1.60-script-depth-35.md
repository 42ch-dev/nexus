# V1.60 P1 — Script Profile Depth 3.5 Promotion (Draft → Master)

**plan_id**: `2026-06-22-v1.60-script-depth-35`
**title**: Script Profile Depth 3.5 Promotion — Draft → Master
**iteration**: V1.60
**track**: Track B
**wave**: Wave 1 (parallel w/ P0)
**effort**: L
**owner**: `@fullstack-dev-2` (Track B canonical owner)
**status**: Todo
**created**: 2026-06-22
**depends_on**: P-1 PM signoff (`pre_implement_gate: approved`)
**primary_spec**: `script-profile.md` (Draft V1.55 P3 → Master), `game-bible-profile.md` (Master Depth 3.5 template)
**spec_refs**: `script-profile.md`, `game-bible-profile.md`, `essay-profile.md`, `entity-scope-model.md` §5.1.1, `orchestration-engine.md`, `creator-workflow.md`, `creator-run-preset-entry.md`
**branch_policy**: Working branch `feature/v1.60-script-depth-35` created from `iteration/v1.60` after PM signoff; merges back to `iteration/v1.60`. Same-repo parallel with P0: use one git worktree per topic branch per `mstar-branch-worktree`.
**review_strategy**: Merge to integration → tri-review (`@qc-specialist` architecture + `@qc-specialist-2` security/correctness + `@qc-specialist-3` performance) → mid-QA → targeted fix-wave.

---

## Goal

Promote the `script` Work profile from Draft scaffold (V1.55 P3 — 277 lines, layout + BlockType + validation) to Master Depth 3.5 (mirror game-bible V1.55 P2 template). Completes the non-novel profile trilogy: novel (Master) / game-bible (Master Depth 3.5) / **script (Master Depth 3.5)**. Essay remains Draft (V1.52) — deferred to V1.61 per compass Q6.

Depth 3.5 deliverables:
- `script-writing` embedded preset chain (outline → draft → revise → finalize)
- Dialogue/beat/act 五问 quality rubric (5 dimensions: dialogue coherence, beat pacing, act structure, character voice, scene economy)
- Section completion detection (per-act; triggers completion stop)
- Profile-aware KB extraction (extends `candidate_from_llm_json_for_profile` for `work_profile: script`)

---

## Scope

- **T1**: Extend `script-profile.md` — add Depth 3.5 sections mirroring `game-bible-profile.md` §4 structure: `script-writing` preset chain, dialogue/beat/act 五问 quality rubric (5 dimensions: dialogue coherence, beat pacing, act structure, character voice, scene economy), section completion detection (per-act), profile-aware KB extraction contract. Spec stays Draft until P-last T5 promotion.
- **T2**: Author `embedded-presets/script-writing/preset.yaml` — stage chain (outline → draft → revise → finalize) + gates (`exit_when: kind: llm_judge` on finalize with 五问) + template variables (`{{script.acts}}`, `{{script.characters}}`, `{{script.beats}}`, `{{script.scene}}`).
- **T3**: Author `embedded-presets/script-writing/prompts/` — 5 prompt templates:
  - `outline.md` — outline prompt (act structure, beat sheet, character setup)
  - `draft.md` — draft prompt (scene + dialogue generation)
  - `revise.md` — revise prompt (apply review feedback)
  - `finalize.md` — finalize prompt (polish + KB extraction)
  - `finalize-exit.md` — 五问 judgment prompt with 5 script-specific dimensions
- **T4**: Implement section completion detection — detect when all acts reach `accepted` status; trigger completion stop. Pattern: mirror game-bible's `is_game_bible_design_complete` (`crates/nexus-local-db/src/work_chapters.rs`); add `is_script_complete` for `work_profile: script`.
- **T5**: Implement profile-aware KB extraction — extend `candidate_from_llm_json_for_profile` (in `crates/nexus-orchestration/src/quality_loop.rs` or equivalent) for `work_profile: script` using `dialogue`/`beat`/`act` BlockType variants registered in V1.55 P3 scaffold. Verify `entity-scope-model.md` §5.1.1 BlockType taxonomy covers script variants; amend if gaps.
- **T6**: Wire `script-writing` into `preset_version_for_id` SSOT (e.g., `crates/nexus-orchestration/src/preset_ids.rs`) + extend `preset_version_mapping_matches_yaml_includes_cron_presets` sync test. Closes the V1.50 R-V150P1CRONBW-01 pattern for the third non-novel profile.
- **T7**: Hermetic tests — scaffold atomicity (ScaffoldTransaction already applied in V1.55 P3 — verify still works) + preset execute end-to-end (scaffold → outline → draft → revise → finalize → complete) + 五问 quality gate fires on finalize + KB extraction produces typed `dialogue`/`beat`/`act` candidates + section completion stops when all acts accepted.
- **T8**: Verify `cargo test -p nexus-orchestration` passes (existing + new tests); `cargo clippy --all -- -D warnings`; `cargo +nightly fmt --all -- --check`.

**Spec promotion** (Draft → Master) is owned by P-last T5, NOT P1. P1 ships Draft-overlay-quality content; P-last promotes after Track B QC Pass.

---

## Acceptance Criteria

- [ ] `script-profile.md` extended with Depth 3.5 sections (preset chain, 五问 rubric, section completion, KB extraction contract). Spec header stays Draft.
- [ ] `embedded-presets/script-writing/preset.yaml` exists with stage chain + gates + template variables.
- [ ] `embedded-presets/script-writing/prompts/` exists with 5 prompt templates (outline, draft, revise, finalize, finalize-exit).
- [ ] `is_script_complete` (or equivalent) section completion detection implemented; triggers stop when all acts accepted.
- [ ] `candidate_from_llm_json_for_profile` extended for `work_profile: script` with `dialogue`/`beat`/`act` BlockType variants.
- [ ] `script-writing` registered in `preset_version_for_id` SSOT + sync test extended.
- [ ] Hermetic tests pass: scaffold + execute + quality gate + KB extraction + completion.
- [ ] `cargo test -p nexus-orchestration` passes (existing + new tests).
- [ ] `cargo clippy --all -- -D warnings` passes.
- [ ] `cargo +nightly fmt --all -- --check` passes.
- [ ] No regression on existing `novel-writing` or `game-bible` preset execution tests.

---

## Spec changes

- `script-profile.md`: Draft V1.55 P3 → Draft V1.60 P1 (Depth 3.5 sections added; header stays Draft until P-last T5 Master promotion).
- `embedded-presets/script-writing/` (NEW): preset.yaml + 5 prompt templates.
- `entity-scope-model.md` §5.1.1: Verify `dialogue`/`beat`/`act` BlockType variants registered (V1.55 P3 added these). Amend only if gaps found in T5.
- `orchestration-engine.md`: Amend only if `candidate_from_llm_json_for_profile` extension requires new engine contract (verify during T5).

---

## Verification

```bash
# Script-writing preset + prompts exist
ls embedded-presets/script-writing/preset.yaml
ls embedded-presets/script-writing/prompts/{outline,draft,revise,finalize,finalize-exit}.md

# Preset version sync test (must include script-writing)
cargo test -p nexus-orchestration preset_version_mapping_matches_yaml_includes_cron_presets

# Script scaffold + execute + quality gate + KB extraction + completion tests
cargo test -p nexus-orchestration --test script_profile

# Full workspace test suite
cargo test --all

# Full clippy
cargo clippy --all -- -D warnings

# Nightly format check
cargo +nightly fmt --all -- --check

# Verify spec extended (line count growth)
wc -l .mstar/knowledge/specs/script-profile.md
# Expected: > 277 (V1.55 P3 baseline) after T1 Depth 3.5 additions
```

---

## Risks

- **BlockType taxonomy gaps**: `entity-scope-model.md` §5.1.1 may not fully cover `dialogue`/`beat`/`act` variants needed for KB extraction. Mitigation: T5 verifies before extending; amend §5.1.1 if gaps.
- **五问 prompt authoring for script domain**: Dialogue/beat/act quality dimensions need domain-appropriate rubric. Mitigation: PM reviews prompts during P1 spec review (compass Q5); `@fullstack-dev-2` drafts from game-bible Depth 3.5 template.
- **Cross-track merge conflict on `preset_version_for_id`**: P0 (Track A) does not touch `preset_ids.rs`, but if both tracks amend the same file, PM resolves on integration branch. Mitigation: P0 and P1 touch disjoint code surfaces (see compass §4).
- **Section completion edge cases**: Multi-act scripts with optional acts, partially-accepted acts, or rejected acts may have ambiguous completion state. Mitigation: mirror game-bible's `is_game_bible_design_complete` semantics; document edge cases in spec.
- **ScaffoldTransaction regression**: V1.55 P3 applied ScaffoldTransaction to script_scaffold.rs; T7 verifies atomicity still holds. If scaffold atomicity regressed (e.g., by an unrelated change since V1.55),修复 in T7.

---

## Out of scope

P1 does NOT touch: `novel-writing` preset (stable Master); `game-bible` preset (stable Master Depth 3.5); essay profile (deferred to V1.61 per compass Q6); any Track A capability (owned by P0); DF-46 roster update (owned by P-last T3); `script-profile.md` Draft → Master promotion (owned by P-last T5); `novel-writing/workflow-profile.md` (novel-only); any CLI subcommand beyond what `creator run <preset_id>` already provides (per `creator-run-preset-entry.md` Shipped Master).
