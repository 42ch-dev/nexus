# P2 — Composite Timeline daemon endpoint + web consumer

> **Iteration compass:** `.mstar/iterations/v1.126/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.126/specs/composite-timeline-endpoint.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Ship `GET /v1/daemon/timeline/overview` so the web global Timeline + Worlds list stops fanning out N=5–10 `kb/graph` client fetches. Closes `DF-V1122-DEEPER-WB` (route promotion slice) + `DF-V1123-COMPOSITE-ENDPOINT`. Adds an **additive** overview DTO; existing routes unchanged.

**Execution:** mstar-sdd (multi-task)

### Wire contracts (architect LOCKED — pending seat 2)

- **Verdict: `wire_contracts_changed: true`** (PM proposal — pending architect seat 2 confirmation). Additive schema: new `schemas/daemon/responses/timeline-overview-response.schema.json` + codegen to `@42ch/nexus-contracts` + Rust handler. No existing route or DTO is mutated. Per repo policy, schema + codegen + handler + web consumer land in one commit.

## Tasks

- [ ] **T1 (AC-V1126-3 — schema + codegen) · Must:** New schema `schemas/daemon/responses/timeline-overview-response.schema.json` defining the **smallest author-valuable slice** (P2 spec ND-2 — no `recent_events`):

  ```jsonc
  {
    "type": "object",
    "required": ["worlds", "cursor", "total_worlds"],
    "properties": {
      "worlds": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["world_id", "era_count", "event_count"],
          "properties": {
            "world_id": { "type": "string" },
            "title": { "type": ["string", "null"] },
            "era_count": { "type": "integer", "minimum": 0 },
            "event_count": { "type": "integer", "minimum": 0 },
            "last_event_at": { "type": ["string", "null"], "format": "date-time" }
          }
        },
        "maxItems": 20
      },
      "cursor": { "type": ["string", "null"] },
      "total_worlds": { "type": "integer", "minimum": 0 }
    }
  }
  ```

  **No `$ref` to `timeline-event.schema.json`** — the overview response carries counts + `last_event_at` only; deep-link per-World Timeline still calls `kb/graph` for event rows. Run `pnpm run codegen` to regenerate TypeScript + Rust DTOs. Bundle envelope bumped per `schema_version`. **No edits to existing schemas**.
   - **Verify:** `pnpm run codegen` clean; `@42ch/nexus-contracts` dist/ regenerated; `cargo check -p nexus-contracts` clean.
   - Files: `schemas/daemon/responses/timeline-overview-response.schema.json`, `schemas/bundle/*.schema.json` (bundle envelope), `tooling/codegen/*` (if needed), `crates/nexus-contracts/src/generated/` (auto)
   - **Architecture locks:**
     - Schema file: `schemas/daemon/responses/timeline-overview-response.schema.json` (P2 spec ND-A2)
     - Response shape: `{ worlds: [{ world_id, title, era_count, event_count, last_event_at }], cursor, total_worlds }` — no `recent_events` (P2 spec ND-A2)
     - No `$ref` to `timeline-event.schema.json` — counts + `last_event_at` only
     - Bundle envelope `schema_version` bumped (minor)
     - `wire_contracts_changed: true` (P2 spec ND-A7)
- [ ] **T2 (AC-V1126-3 — daemon handler) · Must:** New `crates/nexus-daemon-runtime/src/api/handlers/timeline.rs` (architect-locked — P2 spec ND-A1, AQ-6) gains `get_timeline_overview` handler:
   - Reads from existing `NarrativeGateway::list_worlds()` (internal) + per-World `WorldKbGraphResponse` era/event counts.
   - Aggregates per-World era_count (`block_type=era` count) + event_count (`block_type=event` count) + last_event_at (max `timestamp`).
   - Cursor paginates by `world_id` lexicographic order; default cap 20 worlds.
   - **No event-row aggregation** — response carries counts only (P2 spec ND-2).
   - Registers route `GET /v1/daemon/timeline/overview` in `crates/nexus-daemon-runtime/src/api/mod.rs`.
   - **Wire path unchanged:** existing `/v1/daemon/worlds/{world_id}/kb/graph` + `/v1/daemon/worlds/{world_id}/kb/patch-entity` + `/v1/daemon/narrative/worlds` routes preserved.
   - **Verify:** `cargo test -p nexus-daemon-runtime` includes handler unit/integration tests with mock gateway; `cargo clippy -p nexus-daemon-runtime -- -D warnings` clean.
   - Files: `crates/nexus-daemon-runtime/src/api/handlers/timeline.rs` (NEW), `crates/nexus-daemon-runtime/src/api/handlers/mod.rs` (add `pub mod timeline;`), `crates/nexus-daemon-runtime/src/api/mod.rs` (route registration), tests
   - **Architecture locks:**
     - Handler file: new `timeline.rs` — NOT `narrative.rs` (P2 spec ND-A1, AQ-6)
     - Route registration: `.route("/v1/daemon/timeline/overview", get(handlers::timeline::get_timeline_overview))` (P2 spec ND-A3)
     - Error envelope: `NexusApiError` (single-source rule per `crates/nexus-daemon-runtime/AGENTS.md`)
     - Single commit: schema + codegen + handler + web consumer (P2 spec ND-A4)
- [ ] **T3 (AC-V1126-3 — web consumer migration, two consumers in one task) · Must:** Migrate two consumer screens to one `useTimelineOverview` query. Both consume the same query — cohesion is high, but task is wider than single-consumer migrations:
  - **(a) `global-timeline-view.tsx`:** migrate from N=5–10 parallel `kb/graph` `useQueries` calls to **one** `useTimelineOverview` query against the new route. Per-World drill-down still uses `kb/graph` (deep-link detail).
  - **(b) `worlds-page.tsx` activity surface:** reads `era_count` + `event_count` + `last_event_at` from the same overview call (replaces the current "last-edited timestamp only" projection — V1.123 P3 T3 residual).
   - **Verify (author) — two assertions:** (a) Open `/timeline` global view → one network call to `/timeline/overview`; per-World row shows era/event counts; clicking a World still navigates to per-World Timeline (which calls `kb/graph` as before). (b) Open `/worlds` → activity column shows "X eras, Y events" instead of only "last edited".
   - Files: `apps/web/src/lib/nexus/types.ts` (NexusClient method), `apps/web/src/lib/nexus/desktop-capabilities.ts` (browser impl), `apps/web/src/api/queries.ts` (`useTimelineOverview`), `apps/web/src/components/global-timeline/global-timeline-view.tsx`, `apps/web/src/pages/worlds-page.tsx`, tests
   - **Architecture locks:**
     - `NexusClient.getTimelineOverview(cursor?): Promise<TimelineOverviewResponse>` added to interface (P2 spec ND-A5)
     - Browser impl: `GET /v1/daemon/timeline/overview` + cursor query param (P2 spec ND-A5)
     - `useTimelineOverview` query: cursor-paginated, `staleTime: 10_000` (P2 spec ND-A5)
     - `global-timeline-view.tsx`: replace N=5–10 `useQueries(kb/graph)` with single `useTimelineOverview` — closes V1.123 P3 N+1 residual
     - `worlds-page.tsx`: read `era_count` + `event_count` + `last_event_at` from same overview query — closes V1.123 P3 T3 residual
     - Per-World drill-down: still calls `kb/graph` for event rows (deep-link detail) — unchanged
- [ ] **T4 (Tracker + knowledge) · Must:** Update `knowledge/deferred-features-cross-version-tracker.md`:
  - `DF-V1122-DEEPER-WB`: update in place (it exists in §2.3 open features) — "V1.126 P2 ships `GET /v1/daemon/timeline/overview` composite (overview slice, no event rows). Full per-World `GET /v1/daemon/worlds/{world_id}/timeline` row remains open under the same DF ID (remainder slice)."
  - `DF-V1123-COMPOSITE-ENDPOINT`: per P2 spec ND-9, **append directly to the shipped archive** (`archived/shipped-features-tracker.md`) labelled "V1.123 compass-named; implicitly registered via status.json tracking_links; closed by V1.126 P2 without an open-row phase" — **do not** attempt to close a §2.3 row that was never opened (verified at seat 1).
  - V1.123 P3 T3 + V1.123 timeline-first-ia-deepening residuals (N+1 fan-out): eligible for closure in P3 — note in P3 spec.
  - **Verify:** Tracker diff shows DF-V1122-DEEPER-WB updated; shipped archive gains a new DF-V1123-COMPOSITE-ENDPOINT row (labelled "implicitly registered").
  - Files: `.mstar/knowledge/deferred-features-cross-version-tracker.md`, `.mstar/archived/shipped-features-tracker.md`

## Done when

- **AC-V1126-3:** `GET /v1/daemon/timeline/overview` returns the overview shape; web consumes one call; `DF-V1122-DEEPER-WB` updated; `DF-V1123-COMPOSITE-ENDPOINT` closed.

## Roadmap

- **Composite endpoint slice:** overview shape only. Per-World `GET /v1/daemon/worlds/{world_id}/timeline` (full `TimelineEvent` route promotion) stays under `DF-V1122-DEEPER-WB` remainder.
- **Cross-surface fan-out (`DF-V1123-CROSS-SURFACE-BINDING` hardening):** not in P2 scope. P2 ships the read path only.
- **Performance:** if 20 worlds × 100 events is too large for one payload in dogfood, P2 follow-up may add per-World expansion (deep-link) — track as residual, not now.

## Working branch

`feature/v1.126-p2-composite-timeline-endpoint` → merge `iteration/v1.126`
