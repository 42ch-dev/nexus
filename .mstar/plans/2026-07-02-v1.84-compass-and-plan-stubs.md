# P-1 — V1.84 Prepare (compass + plan stubs)

| Field | Value |
|-------|-------|
| plan_id | `2026-07-02-v1.84-compass-and-plan-stubs` |
| iteration | V1.84 |
| type | prepare |
| status | Todo |
| primary_spec | `.mstar/iterations/v1.84-brand-polish-and-token-consolidation-delivery-compass-v1.md` |
| working_branch | n/a (artifacts authored on integration branch) |
| merge_target | `iteration/v1.84` |

## Context

Prepare plan for V1.84 (Brand Polish and Token Consolidation). V1.83 (Brand UI Foundation) is merged to `main` (PR #110, `1b19d69c`). This plan locks the iteration compass, writes the P0/P1/P-last plan stubs with Prepare gates satisfied, and registers the iteration in `status.json`. It carries no implementation work.

## Specify

- **Problem**: A new iteration (V1.84) needs a locked scope, compass, and registered plans before any implement dispatch.
- **User value**: Locks the product story for authors (consolidating the blue-scale token path so there is a single brand source — the canvas surfaces already render brand-cyan from V1.83) and the infra hygiene that makes future reliable brand work possible. Establishes the durable tracking surface (compass + plans + residuals) that ensures all 5 V1.83 residuals are closed with clear separation of author-facing token-consolidation (P0) vs foundation (P1) framing.
- **Scope**: compass + 4 plan stubs + `status.json` registration + README index.
- **Non-goals**: any code/config/CI change (those belong to P0/P1); any root DESIGN change; re-opening the locked direction (brand polish + token consolidation only).
- **DoD (draft)**: compass `status: locked` after Review & Edit chain (with §2 SP-1 author-facing / SP-2 infra split, measurable §5 AC mapped to W00x, and §6 Non-Goals sharp); plans registered with correct `closes_residuals` and framing alignment; `status.json` metadata + plan rows present.

## Clarify

Resolved via `/pm /iteration-start` grill-me (recorded in compass §1 Locked Decisions):

- Direction: A — brand polish + token consolidation (close all 5 residuals; no primitive extraction, no page redesign).
- Branch policy: `main` → `iteration/v1.84` → `main`.
- Plan structure: P-1 + P0 ‖ P1 + P-last.
- No open high-impact ambiguity.

## Plan

- Author `v1.84-brand-polish-and-token-consolidation-delivery-compass-v1.md` from the iteration-compass template (ensure §0 author value bridge, §2 SP-1/SP-2 split with author-facing headline on P0, §2.2 success criteria split, §5 AC each mapped to a residual ID and testable, §6 Non-Goals sharp, §7 roadmap trigger sound).
- Author this plan + P0 + P1 + P-last stubs with specify/clarify/plan/tasks populated and internal consistency (P0 user-value = author-facing canvas coherence; P1 user-value = infra-only; `closes_residuals` match compass §0 table; DoD aligns with compass AC).
- Register V1.84 in `status.json` root `metadata` (`iteration_base_branch`, `target_branch`, `spec_integration_branch`, `integration_branch`, `latest_active_iteration`, `latest_active_compass`) + `plans[]` rows (Profile B: only active plans).
- Add the V1.84 row to `iterations/README.md` index.
- Hand off to the Phase 1 Review & Edit chain (product-manager → architect → writing-specialist → PM lock) before creating `iteration/v1.84`.

## Tasks

- [ ] T1 — Compass written (this iteration's `*-delivery-compass-v1.md`) with frontmatter + Roadmap Position + close placeholders.
- [ ] T2 — P0/P1/P-last plan stubs written with Prepare gates (specify/clarify/plan/tasks) populated.
- [ ] T3 — `status.json` updated: root metadata + 4 plan rows (Profile B, active plans only).
- [ ] T4 — `iterations/README.md` V1.84 row added.
- [ ] T5 — Review & Edit chain dispatched (product-manager → architect → writing-specialist); PM lock sets compass `status: locked`.

## Acceptance

- Compass on disk with required sections; plan files on disk; `status.json` valid JSON with V1.84 metadata + plan rows; README index updated.
- Prepare gate per plan: specify/clarify/plan = done.
