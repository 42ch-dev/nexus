# Plan — V1.75 P1 QC Followup Slate-Clear

**plan_id**: `2026-06-29-v1.75-qc-followup`
**Iteration**: V1.75 — [Compass](../iterations/v1.75-canvas-pivot-and-qc-followup-compass-v1.md)
**Status**: Todo
**Owner**: `@fullstack-dev` (B1–B6, B8 — daemon/codegen) + `@frontend-dev` (B7 docstring)
**Effort**: S-M
**Branch policy**: `Working branch: create feature/v1.75-qc-followup from iteration/v1.75`; worktree `.worktrees/v1.75-qc/`; merge target `iteration/v1.75`
**Depends on**: P-1 (compass Locked)
**Wire contracts**: `wire_contracts_changed: FALSE` (B1 codegen Eq = derive regeneration only)

## Goal

Close the 8 V1.74-QC-followup residuals (`tbd-v1.75-qc-followup`). Slate → 0 again. File-disjoint from P0 (world-kb/codegen territory, not outline).

## Tasks (per-residual, surgical)

- [ ] B1 — `R-V174QC1-S004` (codegen strips Eq from all structs): fix `tooling/codegen/src/rust-generator.ts` to strip `Eq` only for f64-containing structs; regenerate + commit. Highest-value item (correctness). Owner: `@fullstack-dev`.
- [ ] B2 — `R-V174QC2-S001` (cross-world 404 vs 403): fix world_kb relationship handler to return the canonical "not authorized" status (403 or project code) for cross-world access, not 404 (existence leak); regression test. Owner: `@fullstack-dev`.
- [ ] B3 — `R-V174QC2-S002` (post-commit refetch consistency): fix post-commit graph refetch in world_kb handler; test. Owner: `@fullstack-dev`.
- [ ] B4 — `R-V174QC1-S002` (CAS dedup): refactor `kb_relationships.rs` to reuse `cas::cas_check` helper instead of duplicating the re-read pattern. Owner: `@fullstack-dev`.
- [ ] B5 — `R-V174QC1-S007` (enum coercion): `project_relationship` coerces unknown enum to `Custom` silently — handle explicitly (log/error) instead. Owner: `@fullstack-dev`.
- [ ] B6 — `R-V174QC3-S001` (graph cap/pagination): add a relationship count cap for large worlds OR document the V1.76 pagination plan; pick the cheaper. Owner: `@fullstack-dev`.
- [ ] B7 — `R-V174QC1-S006` (docstring): update `apps/web/src/components/canvas/world-kb/graph-projection.ts` docstring (still describes V1.73 read-only). Owner: `@frontend-dev`.
- [ ] B8 — `R-V174QC2-S003` (test naming): rename world_kb_relationships tests for consistency. Owner: `@fullstack-dev`.

## Acceptance (maps to compass §7 DoD #6)

- All 8 residuals `lifecycle: resolved` in `status.json` with resolution commit + plan_id.
- `metadata.tech_debt_summary.total_open: 0` (or new QC findings → V1.76).
- Each correctness item (B1/B2/B3) has a regression test.

## Notes

- File-disjoint from P0 (world-kb/codegen vs outline) — no overlap except codegen coordination (B1 regenerates all Rust derives; P0 A1 regenerates outline DTOs; PM serializes the integration codegen).
- B1 (codegen Eq) is the substance; B2/B3 are real correctness; B4–B8 are polish.
- Single-stream within P1 (`single_stream_justified: yes`): per-file surgical; `@fullstack-dev` leads B1–B6/B8, `@frontend-dev` leads B7.
