# P3 — Frontend Consolidation & UI Polish Sweep

- **plan_id:** `2026-07-12-v1.111-frontend-consolidation-sweep`
- **iteration_refs:** V1.111
- **primary_spec:** `.mstar/iterations/v1.111/specs/frontend-consolidation-sweep.md`
- **tier:** Stretch
- **wire_contracts_changed:** false (frontend-only)
- **status:** Todo

## Problem

A cluster of frontend residuals accumulated across V1.106–V1.110 (toast
duplication, Studio chrome static-mirror, prop drift, stale comments, UX polish
suggestions) — all `lifecycle: open`, drift risk. User direction names 遗留问题,
UI bug, UI/UX美观. This plan pays them down in one consolidation pass.

## Scope

Close the architect-scoped frontend residual cluster + a UI/UX aesthetics pass.
Architect finalizes the exact residual list at §5.2; overflow → roadmap-next.

### Must

- FB-CS-000: Toast duplication dismissed with reason (already consolidated — `use-toast.tsx` is a re-export barrel; `R-V1106P0-001` closed with disposition)
- FB-CS-001: Stale comment/copy drift swept (closes `R-V1106P2-001` + architect-scoped siblings)
- FB-CS-002: ConnectDaemonFormChrome prop contract reconciled/documented (dispositions `R-V1107QC1-W002`)
- FB-CS-003: UX polish + UI/UX aesthetics pass (architect-scoped)
- FB-CS-004: Closed residuals archived; `status.json` updated; `tech_debt_summary` refreshed

## Non-goals

- Mass archival of V1.91–V1.105 tech-debt residuals (separate spec-hygiene pass);
  token redesign; backend changes; toast API rewrite (consolidation only).

## Architecture locks (architect — finalized 2026-07-12, code-evidenced)

> Each residual verified against `.mstar/status.json` `residual_findings` and
> the current codebase (2026-07-12). Toast + guardrails findings corrected
> against present-day source.

1. **Final residual list — capped (9 items).** Overflow → roadmap-next.

   | Residual | Severity | Disposition | Action |
   |----------|----------|-------------|--------|
   | `R-V1106P0-001` (toast dup) | low | **Dismiss with reason** | ⚠️ **Already consolidated.** `apps/web/src/lib/use-toast.tsx` is a **7-line re-export barrel** from `@42ch/nexus-ui` (verified 2026-07-12: `export { ToastProvider, useToast, Toaster, type Toast, type ToastVariant } from '@42ch/nexus-ui'`). The promoted `packages/nexus-ui/src/components/toast.tsx` is the SOLE implementation. The 40+ call sites import via the App-local barrel, which is the documented V1.99 re-export pattern. Migrating 40+ call sites to import directly from `@42ch/nexus-ui` is mechanical churn with no functional/behavioral benefit and high merge-conflict surface — **deferred to a future hygiene pass if the barrel is removed**. Close residual with this reason. |
   | `R-V1106P0-002` (lucide-react dep doc) | low | **Close by fix** | Add `lucide-react` to the `@42ch/nexus-ui` AGENTS.md runtime-dep record (Toast icon imports). Doc-only. |
   | `R-V1109-P0-QC1-W001` (Studio chrome static-mirror) | suggestion | **Dismiss with reason** | The static-mirror pattern is the CORRECT approach — the RF-coupled import boundary forbids RF deps in Studio (`apps/design-studio/AGENTS.md` Forbidden). P2 *extends* the mirror this iteration (Strategy + World KB chrome). Close residual documenting that the static-mirror is the locked direction, not a gap. |
   | `R-V1107QC1-W002` (ConnectDaemonFormChrome prop drift) | low | **Close by fix (document)** | Document the prop contract: chrome uses `state` (not spec's `matrixState`) + the supporting props (`savedConfig`, `fingerprintValue`, `fetchStatus`, `fetchErrorMessage`, `hasSavedConfig`) are intentionally deferred until the live `ConnectDaemonForm` delegates to the chrome. Add a docstring/AGENTS note reconciling spec vs implementation. Rename/surface happens when the live form adopts the chrome. |
   | `R-V1107QC1-ARCH` (`shell-header-chrome.tsx` not created) | nit | **Dismiss with reason** | No FB acceptance criterion existed; the module-table reference was an architecture-doc aspiration. No second consumer needs the extract. Close as "intentionally deferred — no `shell-header-chrome.tsx` extract needed until a second consumer appears". |
   | `R-V1106P2-001` (stale `/settings/connection` comments) | nit | **Close by fix** | Sweep stale comments in `connect-daemon-form.tsx` + its test header (post route-consolidation to `/settings/advanced#connection`). Trivial. |
   | `R-V1106P2-002` (`scrollIntoView` for `#setup`) | suggestion | **Dismiss with reason (defer)** | Enhancement, not bug — hash sets correctly; auto-scroll is polish. Defer to roadmap-next (not plan expansion). |
   | `R-V1100P2QC3-S001` (stale guardrails comment `:75-76`) | nit | **Verify + close** | The script has evolved since V1.100; verify whether lines 75-76 still carry the "P2 will promote" stale comment. If present → fix; if already gone (script shifted) → dismiss with reason. Trivial either way. |
   | UX polish (architect-scoped) | suggestion/nit | **One concrete task** | Spacing/density/contrast audit **scoped to the new P0 palette + P1 sidebar surfaces** against DESIGN.md token tables — NOT a whole-app sweep. Keeps P3 shippable. |

   **Plan cap = 9 residuals.** Any further frontend residual → roadmap-next (mass
   V1.91–V1.105 archival is a separate spec-hygiene pass, explicitly non-goal).

2. **Toast canonical direction — RESOLVED.** Canonical impl =
   `packages/nexus-ui/src/components/toast.tsx` (the promoted presentational
   Toast). The App-local `apps/web/src/lib/use-toast.tsx` is a **re-export
   barrel** (V1.99 pattern), NOT a duplicate. **No 40+ call-site migration** —
   the barrel is the documented integration seam; removing it is a separate
   decision deferred to a future hygiene pass. FB-CS-000 becomes "verify + close
   `R-V1106P0-001` with reason: already consolidated via re-export barrel".

3. **UX aesthetics concrete deliverable** — ONE task: a focused
   spacing/density/contrast audit on the **new P0 command palette + P1 sidebar**
   surfaces against DESIGN.md token tables. Outputs: a short audit note + any
   token-alignment fixes. NOT a whole-app UI sweep (that is non-goal / roadmap).

4. **Verification per residual** — each close/dismiss verified against
   `status.json` `residual_findings[<plan-id>]`; closed rows archived to
   `archived/residuals/<plan-id>.json` per Profile B; `tech_debt_summary`
   refreshed via `tech-debt-rollup.sh`.

## Tasks (architect — finalized at plan-lock)

> P3 lands **last** — T4 (UX audit) depends on P0/P1 output.

- **T1** — Toast residual `R-V1106P0-001`: verify re-export barrel state; write
  close-with-reason entry (already consolidated; 40+ migration deferred). Archive.
- **T2** — Comment/copy drift sweep: close `R-V1106P2-001` (stale `/settings/connection`
  comments) + verify/close `R-V1100P2QC3-S001` (guardrails stale comment) + close
  `R-V1106P0-002` (lucide-react dep doc in `@42ch/nexus-ui` AGENTS.md).
- **T3** — Prop contract reconcile: document `R-V1107QC1-W002`
  (ConnectDaemonFormChrome `state` vs `matrixState` + deferred props) in the
  chrome docstring/AGENTS note; dismiss `R-V1107QC1-ARCH` (`shell-header-chrome.tsx`)
  with reason; dismiss `R-V1109-P0-QC1-W001` (static-mirror is locked direction);
  dismiss `R-V1106P2-002` (`scrollIntoView` → roadmap-next).
- **T4** — UX polish: spacing/density/contrast audit on P0 palette + P1 sidebar
  against DESIGN.md token tables; apply token-alignment fixes (lands after P0/P1).
- **T5** — Residual archival + `status.json` `residual_findings` update +
  `tech_debt_summary` refresh via `tech-debt-rollup.sh`.

## Global Constraints

- `wire_contracts_changed: false`.
- Read before edit; surgical; each residual verified before close.
- Reuse DESIGN.md tokens; no hardcoded values.
- Each closed residual archived to `archived/residuals/<plan-id>.json`.
- Tests: regression tests where a residual fix changes behavior; existing tests
  preserved (update with documented reason if API consolidates).

## Review Gate Summary

| QC decision | QA gate | Residuals | Notes |
|-------------|---------|-----------|-------|
| TBD | TBD | TBD | |

## QA Gate Summary

TBD
