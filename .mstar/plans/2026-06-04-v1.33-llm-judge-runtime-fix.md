# V1.33 LLM Judge Runtime Fix — Locked Plan

**Plan ID**: `2026-06-04-v1.33-llm-judge-runtime-fix`  
**Title**: P3 — `exit_when: llm_judge` → real `judge.llm`  
**Created**: 2026-06-04  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`, `@architect`, `@qc-specialist`  
**Task category**: `logic`  
**Working branch**: `feature/v1.33-llm-judge-runtime-fix`  
**Merge target**: `feature/v1.33-work-experience-loop`  
**Depends on**: none (may parallel with P1 after interface freeze)  
**Spec refs**:

- [orchestration-engine.md](../knowledge/specs/orchestration-engine.md) §4.4.1
- `crates/nexus-orchestration/src/tasks/mod.rs`
- `crates/nexus-orchestration/src/capability/builtins/judge_llm.rs`

---

## 1. Problem Statement

`exit_when: llm_judge` is documented and used in `novel-writing`, `reflection-loop`, and `research`, but runtime may evaluate stub rules only. Product quality gates (gathering exit, reflection draft/revise) do not invoke `judge.llm` with template files.

---

## 2. Goals

1. Refactor `JudgeTask` / `StateCompositeTask` to call `judge_capability` from registry when kind is `llm_judge`.
2. Load and render `template_file` from bundle root.
3. Require worker handle for LLM path; document behavior when absent.
4. Preserve `min_interval` semantics for re-evaluation.
5. Integration tests with mock worker: GO continues, NOGO waits.
6. No change to linear `next` graph topology.

---

## 3. Non-goals

- Conditional `next` on judge outcome
- New judge capabilities beyond existing `judge.llm` / `judge.rule`
- CLI changes

---

## 4. Tasks

- [x] T1: Architect — sequence diagram JudgeTask → CapabilityRegistry → judge.llm
- [x] T2: Implement template load + context merge
- [x] T3: Wire capability invocation with injected IDs
- [x] T4: Unit tests — stub path removed for llm_judge
- [x] T5: Integration test — novel-writing gathering exit with mock GO/NOGO
- [ ] T6: Update orchestration-engine.md if behavior nuances discovered (P5 sync)

---

## 5. Acceptance

1. `rg 'always_true' crates/nexus-orchestration/src/tasks` — no llm_judge-only stub path without capability call.
2. Test proves `judge.llm` invoked when `exit_when.kind: llm_judge`.
3. `reflection-loop` and `novel-writing` presets still pass strict validation gate.
4. QC tri-review Approve on integration branch.

---

## 6. Verification

```bash
cargo test -p nexus-orchestration judge
cargo test -p nexus-orchestration preset
cargo clippy -p nexus-orchestration -- -D warnings
```
