# V1.6 WS-A — Residual Governance

**Plan ID**: `2026-04-20-v1.6-ws-a-residual-governance`
**Status**: InReview
**Compass (SSOT)**: [.mstar/iterations/v1.6-delivery-compass-v1.md](.mstar/iterations/v1.6-delivery-compass-v1.md) §4 WS-A
**Milestone**: M1 — Residual governance complete
**Working branch**: `feature/v1.6`
**Dependencies**: none

**Goal**: Resolve all 4 V1.5 residuals targeting V1.6 (R1, R2, R3, R6) to zero open medium-severity items.

**Authoritative design**: Compass §4 WS-A + referenced knowledge docs (creator-schedule-and-core-context.md, orchestration-engine.md).

**Residual coverage** (4 items → all resolved):

| Residual | Title | Severity | Scope |
|----------|-------|----------|-------|
| R1 | cancel signal pause_schedule error silently ignored | medium | `nexus42d/.../schedules.rs` |
| R2 | resume_schedule TOCTOU race | medium | `nexus-orchestration/.../supervisor.rs` |
| R3 | Scheduler::tick() dead code | low | `nexus-orchestration/.../scheduler/mod.rs` |
| R6 | Recovered sessions lack runners (NoGraphLoaded) | low | `nexus-orchestration/.../engine.rs` |

**Tasks**:

- [x] T1: R1 — Log `pause_schedule()` error at `warn!` level; fix cancel-path pause (line 545). Standalone pause path (line 482) already properly propagates errors.
- [x] T2: R2 — Check `rows_affected()` after `resume_schedule()` UPDATE; return current status if 0. Applied to both `running` and `pending` fallback paths.
- [x] T3: R3 — Remove dead `Scheduler::tick()` query; delegate to `tick_clocked()`. Removed unused `HashSet`/`ScheduleId` imports.
- [x] T4: R6 — Reconstruct FlowRunner from preset_id + CapabilityRegistry + embedded preset loader + SessionStorage after session recovery. V1.5 QC2 S2 verified (no production caller exists yet).

**Evidence**:
- Unit tests: `r1_cancel_pause_failure_does_not_block_cancel`, `r1_running_schedule_pause_then_cancel_succeeds` — verify cancel-path pause behavior.
- Unit tests: `r2_resume_toctou_race_returns_current_status`, `r2_concurrent_resume_toctou_exercises_rows_affected_zero`, `r2_resume_paused_schedule_succeeds_normally` — verify TOCTOU fix, concurrent race exercised, and no regression.
- Dead code removal: `rg "SELECT schedule_id FROM creator_schedules.*scheduled_at" --type rust` returns 0 results.
- Integration tests: `r6_recovered_session_with_known_preset_has_runner`, `r6_recovered_session_has_runner_no_graph_loaded_fix`, `r6_terminal_sessions_skipped_during_recovery`.
- V1.5 QC2 S2: `apply_llm_summarize` takes `[u8; 32]`, capability returns hex string. No production caller exists — only test calls. No conversion bug in current codebase.
- `cargo test --workspace`: 776 passed, 1 failed (pre-existing `auth::tests::get_returns_none_for_unknown_creator` in `nexus42`, unrelated; flaky — passes when run in isolation).
- `cargo clippy --all -- -D warnings`: clean.
- `cargo +nightly fmt --all -- --check`: clean.

**Commits**:
- `f7379b3` fix(orchestration): resolve V1.5 R1 — log pause error at warn! level in cancel signal path
- `3b0464b` fix(orchestration): resolve V1.5 R3 — remove dead Scheduler::tick() redundant query
- `68a9367` fix(orchestration): resolve V1.5 R6 — reconstruct FlowRunner for recovered sessions
- (T2/R2 fix included in `f7379b3` — same file as T1/R1)
- `d4fcce3` test(orchestration): add concurrent resume_schedule TOCTOU test (QC1 C1 fix)

**Effort (agent-oriented)**: M — 1 agent session.

---

*Created: 2026-04-20. Implemented: 2026-04-20.*
