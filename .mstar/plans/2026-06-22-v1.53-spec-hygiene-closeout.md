# V1.53 Spec Hygiene Closeout — Plan

**Plan ID**: `2026-06-22-v1.53-spec-hygiene-closeout`  
**Title**: P-last — V1.53 spec hygiene, Profile B compaction, shipped snapshot  
**Created**: 2026-06-20  
**Status**: Pending  
**Owner**: `@project-manager`  
**Agents**: `@project-manager`, `@qa-engineer`  
**Task category**: `docs`  
**Working branch**: `feature/v1.53-spec-hygiene-closeout`  
**Merge target**: `iteration/v1.53`  
**Iteration compass**: [v1.53-capability-surface-completion-and-skills-cli-cleanup-delivery-compass-v1.md](../iterations/v1.53-capability-surface-completion-and-skills-cli-cleanup-delivery-compass-v1.md)

---

## 1. Scope

Close V1.53 after P0/P1/P-c: Profile B compaction, shipped tracker snapshot, and promotion/retention decision for `capability-registry.md`.

## 2. Acceptance

1. Move five Done V1.53 plan objects from `status.json.plans[]` to per-file `.mstar/archived/plans/<plan-id>.json`.
2. Append five plan-id strings to `.mstar/archived/plans-done.json`.
3. `status.json.plans[]` retains only non-Done plans after compaction.
4. `metadata.latest_ship` updated with V1.53.
5. `.mstar/archived/shipped-features-tracker.md` §2 gains a V1.53 delivery snapshot.
6. `shipped-features-tracker.md` §2 references V1.53 plan count, DF-50 cancellation, and registry outcomes.
7. **V1.52 retroactive Profile B backfill** (PM-locked 2026-06-20): move seven V1.52 Done plans from `status.json.plans[]` to per-file `.mstar/archived/plans/2026-06-19-v1.52-*.json`; append seven plan-id strings to `.mstar/archived/plans-done.json`. V1.52 P-last did not perform Profile B compaction; this is the cleanup point. After both compactions, `status.json.plans[]` is empty (or contains only in-flight non-Done plans from any later iteration).
8. Verify Profile B layout invariant: `plans-done.json.plans` is a string index only; per-file JSONs exist for all archived plan_ids; no `Done` plans remain in `status.json.plans[]`. Run `python3 -c "import json; d=json.load(open('.mstar/archived/plans-done.json')); assert all(isinstance(p, str) for p in d['plans'])"`.

## 3. Dependencies

- P0, P1, and P-c Done.

## 4. Branch policy ref

Create `feature/v1.53-spec-hygiene-closeout` from `iteration/v1.53`; merge back to `iteration/v1.53`; final iteration PR targets `main`.

## 5. Review strategy

Report-only QA.

## 6. Notes/open sub-grills

- Decide whether `capability-registry.md` promotes to Master or remains Draft overlay with a follow-up.
- Profile B invariant: `plans-done.json.plans` is a string index only; full plan objects live under `.mstar/archived/plans/`.
- **Dual compaction** (V1.52 retro + V1.53 normal) is a one-shot cleanup; failure mode is `Blocked` if either compaction partially completes. Recommend running both compactions in the same P-last execution, then verifying with the python3 check in acceptance §7.
