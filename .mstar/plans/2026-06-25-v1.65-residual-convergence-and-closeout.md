# P-last — V1.65 Residual Convergence & Closeout

**Plan ID**: `2026-06-25-v1.65-residual-convergence-and-closeout`
**Iteration**: V1.65 (Outline & Structure Authoring) — Wave 3 closure
**Status**: Todo (stub)
**Owner**: `@fullstack-dev` + `@project-manager`
**Working branch**: `feature/v1.65-residual-convergence-and-closeout` (from `iteration/v1.65`)
**Merge target**: `iteration/v1.65`
**Depends on**: P-mid (QC verdict)
**Compass**: [v1.65-outline-and-structure-authoring-delivery-compass-v1.md](../iterations/v1.65-outline-and-structure-authoring-delivery-compass-v1.md)

## Overview

Close the iteration: resolve QC leftovers, document V1.66+ deferrals durably, promote specs, Profile B compaction, PR to `main`.

## Tasks (stub)

- [ ] T1 — Resolve QC tri-review non-blocking findings (surgical fixes only; targeted re-review per `mstar-review-qc`).
- [ ] T2 — Residual SSOT sync: confirm the 4 Track B residuals closed in P0/P1 are `resolved` in `status.json` residual_findings (resolution.commit + plan_id); document the 6 V1.66+ deferrals (PaginationInfo dedup / error casing / FE1-ORCH / global F-P3 / F-F1 sort / CapabilityInfo admission-gate) with target + rationale in residual lifecycle.
- [ ] T3 — Spec promotion: `web-ui.md` Content-Authoring stage (Shipped); `local-api-surface-conventions.md` amend (Normative); `apps/web/DESIGN.md` increment (Standard+); daemon-runtime / schemas-directory-layout / cli-spec amendments.
- [ ] T4 — Profile B compaction: archive 6 Done plans to `.mstar/archived/plans/<plan-id>.json` + append IDs to `.mstar/archived/plans-done.json` (string-only invariant); clear `status.json.plans[]`; drop verbose `metadata.v1_65_*` blocks (history → git + shipped-features-tracker + compass).
- [ ] T5 — `metadata.latest_ship` update (V1.65); `integration_branch` retired; branch fields cleared.
- [ ] T6 — PR `iteration/v1.65` → `main` (review trail + CI gate + merge commit provenance per root AGENTS.md merge discipline).
- [ ] T7 — PM signoff; post-merge retirement commit.

## Acceptance Criteria

- 0 open V1.65-scoped residuals; 6 V1.66+ deferrals durably tracked (compass §1.2 + §6 + status.json residual lifecycle).
- Profile B layout invariant verified (`plans-done.json` string-only; per-file archive JSON present).
- `status.json` machine state coherent; no narrative `metadata` fields.
- PR merged to `main` via PR (not local merge); `metadata.latest_ship` = V1.65.

## Dependencies

- P-mid (QC verdict + fix-wave complete).

## Risks / Notes

- Do not bundle V1.66 work into P-last "while we're here" — surgical only (mstar-coding-behavior).
- Wire contracts changed (additive-dominant): `@42ch/nexus-contracts` minor bump; record in `metadata.latest_ship.wire_contracts_note`.
