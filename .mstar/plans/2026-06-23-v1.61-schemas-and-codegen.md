---
plan_id: 2026-06-23-v1.61-schemas-and-codegen
title: V1.61 P0 — Schemas & Codegen Foundation
iteration: V1.61
slot: P0
status: InReview
effort: M
owner: "@fullstack-dev"
review: Tri-review + mid-QA
wave: 1
dependency: P-1 PM signoff
---

# P0: Schemas & Codegen Foundation

Create `schemas/compute/` family + extend key-block schema. Run codegen.
Foundation for all downstream work.

## Tasks

- [x] T1: `schemas/compute/compute-input.schema.json` — WASM input envelope
- [x] T2: `schemas/compute/compute-output.schema.json` — 4-part output envelope
- [x] T3: `schemas/compute/entity-attributes.schema.json` — per-BlockType static attribute schemas
- [x] T4: `schemas/compute/entity-state.schema.json` — per-BlockType dynamic state schemas
- [x] T5: Extend `schemas/domain/key-block.schema.json` — add `state`, `computable` to `KeyBlockBody`
- [x] T6: `pnpm run codegen` → regenerate Rust + TypeScript
- [x] T7: Run `cargo test --test schema_drift_detection`
- [x] T8: `cargo check -p nexus-contracts` + `cargo clippy -p nexus-contracts`

## Done when

- 5 schemas pass JSON Schema validation
- Codegen produces correct Rust + TS types
- Schema drift detection passes
- No clippy warnings

## Completion Report v2

- Plan: 2026-06-23-v1.61-schemas-and-codegen
- Working branch: `feature/v1.61-schemas-and-codegen`
- Commits: `9b5450fd`
- Files changed: 18 (5 schema sources, 2 README/test hand-edits, 11 generated)
- Tasks completed: T1..T8 (all done)
- Validation:
  - codegen: PASS — 58 schemas → 55 Rust structs + 55 TS types (compute_input/output + entity_attributes/state generated with correct KeyBlock/TimelineEvent cross-module imports)
  - schema_drift_detection: PASS — 56 schemas / 55 structs in 9ms (4 new compute schemas registered Strict)
  - cargo check -p nexus-contracts: PASS
  - cargo clippy -p nexus-contracts -- -D warnings: PASS (default targets, 0 warnings)
  - additive-only invariant verified: YES — instance-validated legacy KeyBlock (no state/computable), computable KeyBlock, and body-less KeyBlock all pass; generated `KeyBlock.body` unchanged as `Option<serde_json::Value>`; top-level required fields untouched
- Design decision: computable placement = **body_json** (inside KeyBlock body object). Rationale: `body` serializes to the existing `body_json` TEXT column, so `state`+`computable` need no DB migration and no new column/index; the generated Rust `body: Option<serde_json::Value>` is unchanged, making the change purely additive at the type level too. Matches compass §5 #2 default.
- Residual findings:
  - R-V161P0-LOW-001 (low): `cargo clippy -p nexus-contracts --tests` surfaces 8 pre-existing lints (7 `unnecessary_hashes` in `src/local/orchestration/preset.rs`, 1 `redundant_clone` in `src/local/schedule/http.rs`). Not introduced by this plan; not flagged by the default-target CI gate (`cargo clippy --all -- -D warnings`) or the T8 gate. Out of scope — leave for a hygiene pass.
  - R-V161P0-INFO-001 (info): plan T3/T4 mentioned `environment` as a computable BlockType, but `environment` is not a `BlockType` enum value. Used valid combat-relevant types instead (character fully specified; item/faction/ability/species as permissive placeholders). PM may want to confirm the placeholder set for P1.
- Risks for Wave 2:
  - P1 (KB structured layer) will add the Rust-side `state: Option<serde_json::Value>` + `computable: Option<bool>` to the *local* `KeyBlockBody` in `crates/nexus-kb` (note: that is a separate hand-written local type, not the generated wire `KeyBlock`). The wire schema is ready for it.
  - `state_delta.op` enum currently serializes as a plain `String` in Rust (codegen maps inline string-enums to `String`); P3 (state delta merge) may choose to promote it to a proper enum — schema is additive and won't block that.
  - `battle_report` is intentionally freeform (`serde_json::Value`); consumers (P3) must switch on `kind`. No schema enforcement of per-kind shapes by design (V1 envelope, compass Q8).
- Worktree/Branch state: clean, on `feature/v1.61-schemas-and-codegen`. Not merged to integration — PM handles merge after review.
