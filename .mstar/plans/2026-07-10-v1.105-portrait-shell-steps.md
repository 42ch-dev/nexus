# P2 — Portrait Shell + Top Horizontal Steps (H1/N1)

**Goal:** Restyle the setup wizard into a fixed portrait card (~480×720 / `min(720px, 85vh)`) with top horizontal Steps and scrolling content; remove the left step rail.

**Architecture:** Portrait card in `setup-wizard-page.tsx` with `TopStepIndicator` (horizontal). Tokens: `wizard-max-width` 480px, new `wizard-max-height` 720px, `max-h-[85vh]`. Studio fixtures (`setup-wizard-chrome-fixtures.tsx`) are visual SSOT; App wires after accept.

**Interfaces:**
- Tokens: `DESIGN.md` frontmatter → `tooling/design-tokens/src/tokens.css` → Tailwind `max-w-setup-wizard-step-wizard-max-width`, `h-setup-wizard-wizard-max-height`
- Layout: single card column — top steps, scrollable body, bottom CTA
- Retire: `w-setup-wizard-surface-step-panel-width` in wizard

**Implementer SSOT:** `.mstar/iterations/v1.105/specs/portrait-wizard-shell.md`

**Tech Stack:** Design tokens, Design Studio fixtures, React layout, Vitest.

**Execution:** mstar-sdd

**Tier:** **Must (P2)**

**blocked_by:** `2026-07-10-v1.105-wizard-ia-reorder`

## Prepare Gates

- specify: done (product-reviewed §5.1)
- clarify: done (grill-me H1/N1)
- plan: done (product-reviewed §5.1)
- architect: done (§5.2 lock)
- writing: done (§5.3 polish)

## Product Framing

**User value:** A stable vertical card — height does not jump between Agent / Workspace / Done; steps remain visible in a compact top bar. Long agent lists scroll inside the card instead of resizing the window.

**Author-visible chrome:** Portrait card taller than wide; three labeled steps across the top; primary CTA anchored at bottom of content area.

**Iteration gate:** Must; Studio-first (fixtures → visual accept → App).

**Non-goals:** Changing step order (P1); Daemon gate behavior (P0); Settings chrome; new brand illustration system.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| Geometry | **H1** — fixed height portrait; ~480×720 or `min(720px, 85vh)` |
| Steps | **N1** — top horizontal Steps; no left 208px rail |
| Overflow | Content region scrolls; CTA row stays usable |
| Studio-first | Required |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria

1. Studio fixtures show portrait card + top Steps for Agent / Workspace / Done states.
2. App wizard matches accepted Studio chrome (width/height tokens; top Steps).
3. Left step panel is removed from wizard chrome.
4. Long Agent lists scroll inside content without growing the card.
5. Prefer no schemas change.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.105/specs/portrait-wizard-shell.md`
- Process: `.mstar/iterations/v1.105/guides/studio-first-visual-then-app.md`
- Compass: `.mstar/iterations/v1.105-delivery-compass.md`
- Studio: `apps/design-studio/src/fixtures/setup-wizard-chrome-fixtures.tsx`
- App: `apps/web/src/pages/setup-wizard-page.tsx`, optional `components/setup/top-step-indicator.tsx`
- DESIGN.md § Setup Wizard Surface + `tooling/design-tokens/` codegen

---

### Task 1: Lock Shell Contract + Tokens

**Files:**
- Spec: `.mstar/iterations/v1.105/specs/portrait-wizard-shell.md` (architect-locked)
- Modify: `DESIGN.md` (`wizard-max-width: 480px`, `wizard-max-height: 720px`)
- Modify: `tooling/design-tokens/src/tokens.css`, `tailwind.preset.ts` (codegen)

- [ ] Lock width/height tokens and top Steps layout rules.

### Task 2: Studio — Portrait Wizard Fixtures

**Files:**
- Modify: `apps/design-studio/src/fixtures/setup-wizard-chrome-fixtures.tsx` (`WizardStepId` → agent/workspace/done, `TopStepIndicator`)

- [ ] Visual acceptance for three steps + scroll overflow case.

### Task 3: App — Shell Wiring

**Files:**
- Modify: `apps/web/src/pages/setup-wizard-page.tsx` (portrait card + `TopStepIndicator`)
- Optional extract: `apps/web/src/components/setup/top-step-indicator.tsx`
- Tests: `setup-wizard-page.test.tsx`

- [ ] Portrait shell + top Steps live in App.
- [ ] Left rail removed; assert no `w-setup-wizard-surface-step-panel-width`.

### Task 4: Verify

- [ ] Studio + web tests green; token/DESIGN docs aligned.

## Plan self-review (architect §5.2)

1. H1/N1 explicit with named tokens (`480px`, `720px`, `85vh`).
2. Studio-first required with fixture file path.
3. Depends on P1 three-step IA; left rail tokens deprecated for wizard.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-10-v1.105-portrait-shell-steps/`
