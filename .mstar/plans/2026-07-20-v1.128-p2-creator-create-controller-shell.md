# P2 — Creator Create vs Controller shell

> **Iteration compass:** `.mstar/iterations/v1.128/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.128/specs/creator-create-controller-shell.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Creator **content region** modes — empty selection → **Create page** (honest World/Work CTAs per V1.125); selected Work/World → **Controller Panel stub** (placeholder + **Back** → Create). Controller business widgets **TBD** — stub only. Studio fixtures first, then App.

**Architecture:** Presentational shell chrome + App layout wiring. Reuse V1.125 Worlds-first nav + honest `createWorld` absence (`client.createWorld` confirmed absent V1.127 — fallback to Work create only). No new Daemon API.

**Tech Stack:** React 18 + TypeScript + React Router + Vitest.

**Execution:** mstar-sdd (multi-task)

### Wire contracts

- **Verdict: `wire_contracts_changed: false`**

### Author semantics (PM Seat 1 lock — architect confirmed Seat 2)

| State | Content region | Primary actions |
|-------|----------------|-----------------|
| No Work/World selected | **Create page** | Create World (if API present) or honest Work-create fallback; Create Work |
| Work or World selected | **Controller stub** | Placeholder copy + **Back** |
| **Back** clicked | Returns to **Create page** | Clears `CreatorEntitySelectionContext.selectedEntity`; stays on Creator tab |

### Architect lock (Seat 2) — selection + Back

| Item | Lock |
|------|------|
| Selection SSOT | `CreatorEntitySelectionContext` in `apps/web/src/components/layout/` — **not** route params, **not** `submenuItem` (`shell-sidebar-chrome.tsx:107`) |
| Back target | Clear selection → Create page — **reject** list-highlight alternate |
| Presentational extract | `@web-layout/creator-shell-content` (Create page + Controller stub) — P2 owns; P3 labels |
| Route boundary | Hub routes (`/works`, `/worlds`) host content swap; canvas routes (`/works/:workId/*`) orthogonal per `work-shell-routes.ts:21` |
| Wire contracts | `wire_contracts_changed: false` |
| Depends on | P0 + P1 complete (serial); P3 labels Shell surfaces |

## Tasks

- [ ] **T1 (AC-V1128-3a — Studio Shell fixtures) · Must:** Update `shell-nav-data` to Worlds-first; add Shell fixture frames for (1) **empty → Create page** with card CTAs (World path: `createWorld` detect + honest fallback; Works path: Create Work), (2) **selected entity → Controller stub** (placeholder “TBD” + Back).
  - **Verify (author-observable):** Maintainer toggles Studio `/surfaces/shell` fixtures between both modes; Create shows card CTAs not list chrome; Controller shows no business widgets.
  - Files: `apps/design-studio/src/fixtures/shell-nav-data.ts`, `surfaces.tsx` Shell fixtures, tests

- [ ] **T2 (AC-V1128-3b/c — App Create vs Controller) · Must:** Wire App Creator content region via `CreatorEntitySelectionContext` + `@web-layout/creator-shell-content`: no selection → Create page CTAs per spec table; selection → Controller stub (placeholder + Back). **Back** clears `selectedEntity` and returns to Create page (architect-locked).
  - **Verify (author-observable):** Manual path: Create → select World/Work → Controller stub → Back → Create page with selection cleared. Unit/integration tests for mode switch. Controller shows no widgets beyond placeholder + Back.
  - Files: `apps/web/src/components/layout/*` (incl. `CreatorEntitySelectionContext`), `apps/web/src/components/layout/presentational/creator-shell-content.tsx`, pages as needed, locales, tests

## Plan self-review (PM Seat 1)

1. Controller business content explicitly out of scope — stub + Back only
2. Create CTAs reuse V1.125 Worlds-first honesty (no silent Create World no-op)
3. Back → Create page **locked** (Seat 2); `CreatorEntitySelectionContext` SSOT
4. Studio before App

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-20-v1.128-p2-creator-create-controller-shell/` (gitignored).

## Review Gate Summary

> Filled after QC.

## QA Gate Summary

> Filled after QA.
