# V1.53 Skills CLI Cleanup — Plan

**Plan ID**: `2026-06-22-v1.53-skills-cli-cleanup`  
**Title**: P-c — Remove obsolete `nexus42 acp skills export|verify` surface  
**Created**: 2026-06-20  
**Status**: Pending  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`, `@qc-specialist`  
**Task category**: `logic`  
**Working branch**: `feature/v1.53-skills-cli-cleanup`  
**Merge target**: `iteration/v1.53`  
**Iteration compass**: [v1.53-capability-surface-completion-and-skills-cli-cleanup-delivery-compass-v1.md](../iterations/v1.53-capability-surface-completion-and-skills-cli-cleanup-delivery-compass-v1.md)

---

## 1. Scope

Remove obsolete CLI command handling and tests for `nexus42 acp skills export|verify`. Preserve the static committed skills model.

## 2. Acceptance

1. CLI commands removed.
2. Command tests removed or updated.
3. Specs already retired in P-1 remain retired.
4. Deferred tracker/archive reflect DF-50 Cancelled.
5. No changes to `embedded-skills/`, `embedded_skills.rs`, `skill_link.rs`, `skill_sync.rs`, or `crates/nexus42/src/skills/`.

## 3. Dependencies

- P0 Done so the `acp` module is on the registry seam.

## 4. Branch policy ref

Create `feature/v1.53-skills-cli-cleanup` from `iteration/v1.53` after P0 merge; merge back to `iteration/v1.53`.

## 5. Review strategy

Single-review (`qc1`) only.

## 6. Notes/open sub-grills

- If code search finds similarly named static skills sync/link code, treat it as explicitly out of scope unless it is only a dead reference to the removed CLI command.
