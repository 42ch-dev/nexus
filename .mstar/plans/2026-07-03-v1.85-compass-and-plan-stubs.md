# P-1 — V1.85 Prepare (Compass and Plan Stubs)

| Field | Value |
|-------|-------|
| plan_id | `2026-07-03-v1.85-compass-and-plan-stubs` |
| iteration | V1.85 |
| type | prepare |
| status | Todo |
| primary_spec | `.mstar/iterations/v1.85-app-icon-branding-delivery-compass-v1.md` |
| working_branch | n/a (artifacts authored on integration branch) |
| merge_target | `iteration/v1.85` |

## Context

Prepare plan for V1.85 (App Icon Branding). V1.84 (Brand Polish and Token Consolidation) is merged to `main` (PR #111). This plan locks the iteration compass, writes the P0/P1/P-last plan stubs with Prepare gates satisfied, and registers the iteration in `status.json`. No implementation work.

## Specify

- **Problem**: A new iteration (V1.85) needs a locked scope, compass, and registered plans before any implement dispatch.
- **User value**: Establishes shared understanding and the durable tracking surface for the app-icon-branding work that closes the placeholder-icon product gap and the two V1.84 low residuals.
- **Scope**: compass + 4 plan stubs + `status.json` registration + README index.
- **Non-goals**: any code/asset/CI change (P0/P1); any root DESIGN change.
- **DoD (draft)**: compass `status: locked` after Review & Edit chain; plans registered; `status.json` metadata + plan rows present.

## Clarify

Resolved via `/pm /iteration-start` grill-me (compass §1 Locked Decisions):

- Direction: A — App Icon Branding + V1.84 residual closure.
- Branch policy: `main` → `iteration/v1.85` → `main`.
- Plan structure: P-1 + P0 ‖ P1 + P-last.
- Aesthetic sign-off gate on the composed app-icon source before regeneration.
- No open high-impact ambiguity.

## Plan

- Author `v1.85-app-icon-branding-delivery-compass-v1.md` from the iteration-compass template.
- Author this plan + P0 + P1 + P-last stubs with specify/clarify/plan/tasks populated.
- Register V1.85 in `status.json` root `metadata` + `plans[]` rows (Profile B: active plans only).
- Add the V1.85 row to `iterations/README.md`.
- Hand off to the Phase 1 Review & Edit chain before creating `iteration/v1.85`.

## Tasks

- [ ] T1 — Compass written with frontmatter + Roadmap Position + close placeholders.
- [ ] T2 — P0/P1/P-last plan stubs with Prepare gates populated.
- [ ] T3 — `status.json` updated (root metadata + 4 plan rows).
- [ ] T4 — `iterations/README.md` V1.85 row added.
- [ ] T5 — Review & Edit chain completed; PM lock sets compass `status: locked`.

## Acceptance

- Compass on disk; plan files on disk; `status.json` valid JSON; README index updated. Prepare gate per plan: specify/clarify/plan = done.
