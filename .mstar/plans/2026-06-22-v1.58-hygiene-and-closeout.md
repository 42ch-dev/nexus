# V1.58 P-last — Hygiene & Closeout

**plan_id**: `2026-06-22-v1.58-hygiene-and-closeout`
**title**: Hygiene & Closeout (P-last)
**iteration**: V1.58
**track**: closeout
**wave**: After Wave 3
**effort**: M
**owner**: `@project-manager`
**status**: Todo
**created**: 2026-06-22
**depends_on**: All 4 implement plans (P0, P1, P2, P3) integrated + reviewed on `iteration/v1.58`
**primary_spec**: `acp-capability-set.md`, `capability-registry.md`, `reference-knowledge.md`, `status.json`
**spec_refs**: `acp-capability-set.md`, `capability-registry.md`, `cli-spec.md`, `daemon-runtime.md`, `local-runtime-boundary.md`, `reference-knowledge.md`, `entity-scope-model.md`, `deferred-features-cross-version-tracker.md`
**branch_policy**: Working branch `feature/v1.58-hygiene-and-closeout` created from `iteration/v1.58` after all implement plans merged; merges back to `iteration/v1.58`.
**review_strategy**: Report-only QA (not tri-review).

---

## Goal

Complete V1.58 iteration closeout: initialize status.json with 7 registered plans, update deferred-features-tracker (remove DF-44 row, append V1.58 ship line), promote `reference-knowledge.md` from Draft to Master, append shipped-features-tracker V1.58 snapshot, execute tech-debt rollup, apply Profile B compaction, and verify all spec cross-references are consistent. This is a PM-side closeout plan — no product code, no new capability implementations.

---

## Scope

- **T0**: Status.json initialization. Set `metadata.latest_active_iteration = "V1.58"`, `metadata.latest_active_compass = "v1.58-workspace-occ-hardening-and-df44-reference-refresh"`, `metadata.integration_branch = "iteration/v1.58"`, `metadata.pre_implement_gate = "go"`. Register all 7 plans in `status.json.plans[]` with status reflecting their final state (4 Done implement plans + P-mid Done + P-last Done; P-1 Done). Update `metadata.tech_debt_summary` to reflect post-V1.58 state (absorbed residuals closed, any new residuals filed during P0-P3).
- **T1**: Promote `reference-knowledge.md` from Draft (V1.58 P1) to Master. Update header: `Status: Normative`, `Document class: Master`. Verify all cross-references from `acp-capability-set.md`, `capability-registry.md`, `cli-spec.md`, and `daemon-runtime.md` point to the promoted spec.
- **T2**: Update deferred-features-cross-version-tracker.md. Remove DF-44 row from §3.3; append to shipped-features-tracker.md with completion version (V1.58), plan-ids (P1 + P3), and closure note. Update §5 "Latest active iteration" to V1.58. Update quick-status line.
- **T3**: Append shipped-features-tracker.md V1.58 delivery snapshot. Record: iteration V1.58, 7 plans, DF-44 closed, N residuals absorbed, wire_contracts_changed=true, key highlights.
- **T4**: Execute tech-debt rollup. Cross-check `status.json.residual_findings` for all closed residuals (absorbed in P0/P2). Update `metadata.tech_debt_summary` with accurate pre/post counts. Archive closed residual detail prose under `.mstar/archived/residuals/`. **V1.52-era carry-forwards NOT in V1.58 scope** — note explicitly in tech-debt summary that the following 14 polish residuals remain deferred to V1.59+ WL-A sweep (out of V1.58 OCC/DF-44 scope):
  - `R-V150KBED-02` (low; world-ownership provenance)
  - `R-V151Q1-10` (low; qc commit hygiene process note)
  - `R-V152TA-S001..S011` (11 lows; V1.52 outline-五问 + auto-promote polish)
  - `R-V152TB-W006/W007/W008` (3 lows; V1.52 N-way GoNGo label matching polish)
- **T5**: Profile B compaction. Archive all 7 Done plans: write full plan objects to `.mstar/archived/plans/<plan-id>.json`; append plan_id strings to `.mstar/archived/plans-done.json` `plans` array. Remove Done plan rows from `status.json.plans[]`. Verify layout invariant: `plans-done.json.plans` contains only strings.
- **T6**: Spec consolidation pass. Verify all spec amendments from P0-P3 are reflected in the Master specs (no Draft overlays left behind except those explicitly deferred). Update spec headers where needed.
- **T7**: Final verification. `cargo clippy --all -- -D warnings` clean on `iteration/v1.58`; `cargo +nightly fmt --all -- --check` clean; `cargo test --all` passes. Run `python3 -c "import json; d=json.load(open('.mstar/archived/plans-done.json')); assert all(isinstance(p, str) for p in d['plans'])"` to verify Profile B invariant.
- **T8**: Report-only QA dispatch. QA engineer verifies status.json consistency, tracker updates, Profile B compaction, and spec promotion completeness. Output: 1 QA report under `.mstar/plans/reports/2026-06-22-v1.58-hygiene-and-closeout/qa.md`.

---

## Acceptance Criteria

- [ ] `status.json`: `latest_active_iteration = "V1.58"`, `integration_branch = "iteration/v1.58"`, 7 plans registered with correct status.
- [ ] `deferred-features-cross-version-tracker.md`: DF-44 row removed; V1.58 ship line added; §5 updated.
- [ ] `shipped-features-tracker.md`: V1.58 delivery snapshot appended.
- [ ] `reference-knowledge.md`: promoted from Draft to Master (Status: Normative, Document class: Master).
- [ ] All spec cross-references from acp-capability-set.md, capability-registry.md, cli-spec.md, daemon-runtime.md, local-runtime-boundary.md verified consistent.
- [ ] Profile B compaction: 7 plans archived; `plans-done.json` invariant holds (all strings); `status.json.plans[]` empty (or only non-Done plans remain).
- [ ] Tech-debt rollup: residual counts accurate; closed residuals archived; no orphan residuals.
- [ ] `cargo clippy --all -- -D warnings` clean; `cargo +nightly fmt --all -- --check` clean; `cargo test --all` passes.
- [ ] Report-only QA filed and verdict Pass.

---

## Carry-forwards absorbed

None. P-last is a closeout plan; it absorbs no residuals. Residual absorption is completed in implement plans (P0, P2) before P-last begins.

---

## Spec changes

- `reference-knowledge.md`: Promote Draft → Master (Status: Normative, Document class: Master, V1.58 P-last).
- `acp-capability-set.md`: Amend (if P-last consolidation reveals cross-reference drift).
- `capability-registry.md`: Amend (if P-last consolidation reveals cross-reference drift).
- `deferred-features-cross-version-tracker.md`: Amend — remove DF-44 row; update §5.
- `shipped-features-tracker.md`: Append — V1.58 delivery snapshot.

---

## Verification

```bash
# Full test suite
cargo test --all

# Full clippy
cargo clippy --all -- -D warnings

# Nightly format check
cargo +nightly fmt --all -- --check

# Profile B invariant
python3 -c "import json; d=json.load(open('.mstar/archived/plans-done.json')); assert all(isinstance(p, str) for p in d['plans'])"

# Status.json consistency
python3 -c "
import json
s = json.load(open('.mstar/status.json'))
assert s['metadata']['latest_active_iteration'] == 'V1.58'
assert s['metadata']['integration_branch'] == 'iteration/v1.58'
print('status.json OK')
"

# No Draft overlays left (except deferred)
grep -r 'Status: Draft' .mstar/knowledge/specs/ | grep -v 'v1.58' || echo "OK: no stale drafts"
```

---

## Risks

- **Profile B compaction may encounter stale plan data**: If implement plans were completed with incomplete `completion_report` or `qc_reports` fields, the archive step may fail. Mitigation: verify each plan object has required fields before archiving.
- **Spec cross-reference drift**: If P0-P3 spec amendments were applied as Draft overlays rather than direct Master edits, P-last consolidation may surface conflicts. Mitigation: P3 verifies spec consistency as part of its verification step; P-last only resolves remaining drift.

---

## Out of scope

P-last does NOT: write product code, implement new capabilities, touch `schemas/`, create the integration branch (already exists), dispatch implement plans or tri-reviews. It is a closeout + hygiene plan only.
