# Plan — V1.93 P-last: Closure (Naming Sweep + QC + QA + Compound + PR)

- **plan_id**: `2026-07-06-v1.93-closure`
- **Iteration**: V1.93 (Remote-Access Polish & Residual Convergence) — **closure**
- **Status**: Todo
- **Owner**: `@writing-specialist` (naming sweep) + `@project-manager` (QC/QA/compound/PR orchestration)
- **Blocks**: Runs after P0 + P1 merge to `iteration/v1.93`.
- **Branch**: `Working branch: create feature/v1.93-closure from iteration/v1.93` (after P0 + P1 merge) → merge `iteration/v1.93`
- **Compass**: [`v1.93-remote-access-polish-delivery-compass-v1.md`](../iterations/v1.93-remote-access-polish-delivery-compass-v1.md)
- **primary_spec**: n/a (closure plan)
- **Residual closed**: R-V192HYG-001 (plus verification that the other 7 are resolved)

## Context

P-last closes V1.93: the naming sweep (R-V192HYG-001 — the one V1.93 residual not owned by P0/P1/P-1), then QC tri-review on the integrated `iteration/v1.93` HEAD, QA, compound (iteration-close knowledge crystallization), Profile B compaction, and the PR to `main`.

## Scope (plan locked → tasks → implement)

### Tasks (coverage IDs)
- [ ] **T1 — Legacy naming sweep (R-V192HYG-001)** (`@writing-specialist`, cross-directory): sweep non-historical prose for residual "Local API" / legacy naming stragglers post-V1.90 rename. Scope: live docs, specs, AGENTS.md files, source-doc comments, and current iteration/plan prose. Out of bounds: historical references inside iteration compasses, archived docs, and git history. Replace with the V1.90+ "Daemon API" terminology per the V1.92 glossary.
- [ ] **T2 — QC tri-review** on `iteration/v1.93` HEAD (after P0 + P1 + T1 merge): `qc-specialist` (architecture coherence), `qc-specialist-2` (security lens on the P0 TLS regeneration — confirm SAN regeneration does not widen the remote attack surface and the FingerprintGate re-pin flow handles the new-fingerprint case), `qc-specialist-3` (transport/performance lens — graceful-shutdown timing + cert lifecycle). Tri-review dispatched same-message per `mstar-dispatch-gates`. Reports to `plans/reports/2026-07-06-v1.93-closure/qc{1,2,3}.md`.
- [ ] **T3 — Fix-wave + targeted re-review** if QC returns Request Changes: default to targeted re-review (same `qcN.md`, reviewers = the assigned seats); full tri-review only if the Assignment explicitly says `QC re-review: full tri-review`.
- [ ] **T4 — QA verification** (`@qa-engineer`): verify the P0 regression tests reproduce the pre-fix behaviour as failing-without-the-fix; verify the P1 polish behaviour; verify no regression in local / CLI / desktop / web-dev flows.
- [ ] **T5 — Compound** (PM triggers `mstar-compound`): identify any non-trivial knowledge from V1.93 (e.g., a SAN-validation pattern, a browser-fingerprint-fetch-limitation note); write/update `{KNOWLEDGE_DIR}` docs and register in `{KNOWLEDGE_DIR}/README.md`. If nothing meets the bar, record "无可结晶知识" with a reason in the compass `## Compound Round Summary`.
- [ ] **T6 — Profile B compaction** (`mstar-plan-artifacts` Profile B): archive Done plans to `.mstar/archived/plans/<plan-id>.json`, append plan_id strings to `.mstar/archived/plans-done.json`, remove from `status.json.plans[]`. Verify `plans-done.json` entries are all strings.
- [ ] **T7 — Residual lifecycle sync**: mark all 8 V1.93-targeted residuals `lifecycle: resolved` with `resolution.commit` + `resolution.plan_id`; archive resolved findings to `.mstar/archived/residuals/`. Confirm R-V191P1-005 stays `open` / deferred.
- [ ] **T8 — Iteration-close (Phase 3 §3.1–§3.5)**: print close-entry + exit checklists; compass frontmatter `status: completed` + `end_date`; update `## Roadmap Position` (current → delivered); fill `## Compound Round Summary` + `## Iteration Retrospective`.
- [ ] **T9 — PR to `main`** (Phase 4): `iteration/v1.93` → `main`; record PR URL; enter Phase 5 merge-ready loop.

### Coverage
- T1 covers R-V192HYG-001. T2–T4 cover QC + QA on the integrated iteration HEAD. T5–T8 cover compound + compaction + residual sync + iteration-close. T9 covers PR delivery.

## Acceptance
- R-V192HYG-001 resolved; non-historical "Local API" / legacy naming stragglers swept.
- All 8 V1.93-targeted residuals `lifecycle: resolved` with resolution metadata; R-V191P1-005 unchanged-deferred.
- QC tri-review consolidated Approve; QA Pass.
- Profile B compaction complete (`plans-done.json` entries all strings); `wc -c .mstar/status.json` < 20_000.
- Compass frontmatter `status: completed` + `end_date`; Phase 3 §3.5 exit checklist all `[x]`.
- PR `iteration/v1.93` → `main` created; Phase 5 merge-ready loop entered.

## Out of scope
- Implementation work (P0 / P1). New features. Anything beyond V1.93 closure.
