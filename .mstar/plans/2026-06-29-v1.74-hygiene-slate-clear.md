# Plan — V1.74 P1 Hygiene Slate-Clear

**plan_id**: `2026-06-29-v1.74-hygiene-slate-clear`
**Iteration**: V1.74 — [Compass](../iterations/v1.74-world-kb-relationships-and-hygiene-compass-v1.md)
**Status**: Todo
**Owner**: `@fullstack-dev` (B1/B2/B3/B4/B9 — daemon/crates) + `@frontend-dev` (B5/B6/B7/B10/B11 — frontend) + `@ops-engineer` (B8 doc)
**Effort**: M
**Branch policy**: `Working branch: create feature/v1.74-hygiene-slate-clear from iteration/v1.74`; worktree `.worktrees/v1.74-hygiene/`; merge target `iteration/v1.74`
**Depends on**: P-1 (compass Locked)
**Wire contracts**: `wire_contracts_changed: FALSE` (B3/B4 schema reconciliations ride P0's 0.10.0 bump — not a separate bump)

## Goal

Clear the entire V1.74 residual slate (11 residuals in this plan; the 12th — R-V173QC1-S2 — is in P0). Close 2 correctness items (non-atomic rollback, parity guard) + 2 reconciliation items (naming drift, idempotency_key) + 1 UX fix (can_edit_outline default) + 6 polish items. Slate → 0 open.

## Tasks (per-cluster, surgical)

- [ ] B1 — Close `R-V171-GREPTILE-POST5-ROLLBACK-ATOMIC` (non-atomic strategy template rollback): make atomic (temp + rename + fsync, V1.72 outline persistence pattern); regression test (crash-safe rollback). Owner: `@fullstack-dev`.
- [ ] B2 — Close `R-V172P0-QC3-004` (Tauri/adapter parity guard missing 4 outline methods): extend guard to cover the 4 outline methods; parity test green. Owner: `@fullstack-dev`.
- [ ] B3 — Close `R-V173QC1-S1` (codegen naming drift): reconcile V1.73 world-kb schema titles to verb-prefix canonical (`PatchWorldKbEntityRequest` etc., matching V1.71/V1.72 baseline + P0 A1 relationship DTOs); regenerate. **Coordinate codegen+commit with P0 A1** (PM-serialized). Owner: `@fullstack-dev`.
- [ ] B4 — Close `R-V173QC1-S3` (idempotency_key unused): **remove** `idempotency_key` from V1.73 world-kb patch DTOs (forward-compat footgun — re-add when implemented with a durable store); regenerate. Coordinate with B3/P0 A1 codegen. Owner: `@fullstack-dev`.
- [ ] B5 — Close `R-V171P1-QC1-003` (ChapterPage defaults can_edit_outline true): fix default to correct semantic (absent = not editable unless explicitly true); regression test. Owner: `@frontend-dev`.
- [ ] B6 — Close `R-V165-QC3-VIRT` (chapter table virtualization): virtualize chapter table (react-window or equivalent); verify at 100+ chapters. Owner: `@frontend-dev`.
- [ ] B7 — Close `R-V171P0-QC1-008` (UI e2e/component test edit-save-refetch): add e2e/component test for the edit-save-refetch loop (pick least-covered surface). Owner: `@frontend-dev`.
- [ ] B8 — Close `R-V171P0-QC1-010` (clarify codegen target): document `apps/web/src/api-types/` codegen source/destination + regenerate command in codegen README. Owner: `@ops-engineer` (or `@fullstack-dev` if doc lives in `tooling/codegen/`).
- [ ] B9 — Close `R-V171P1-QC1-002` (stale `__internal daemon-run` doc comments): remove stale references in `daemon_run.rs`/`boot.rs` comments. Owner: `@fullstack-dev`.
- [ ] B10 — Close `R-V171-GREPTILE-POST5-TOAST` (misleading toast description): fix toast copy after strategy state rename. Owner: `@frontend-dev`.
- [ ] B11 — Close `R-V172P1-QC1-001` (React useEffect deps churn): stabilize useEffect deps in inspector save handlers. Owner: `@frontend-dev`.

## Acceptance (maps to compass §7 DoD #7)

- All 11 residuals `lifecycle: resolved` in `status.json` root `residual_findings` with resolution commit + plan_id.
- `metadata.tech_debt_summary.total_open: 0` (combined with P0's R-V173QC1-S2).
- Each B-item has its regression/verification test where applicable (B1, B2, B5).

## Notes

- File-disjoint from P0 except B3/B4 (V1.73 world-kb schema files that P0 A1 regenerates from) — PM serializes the codegen+commit on the integration branch to avoid merge conflict on generated files.
- Single-stream within P1 (`single_stream_justified: yes`): per-file surgical edits; owners split by territory (daemon vs frontend vs ops).
- If Track B slips, polish items (B6/B7/B9/B10/B11) may re-defer to V1.75 with durable pointers without losing correctness value — but the default plan closes all 11 (slate-clear goal).
- B4 decision (remove idempotency_key) is deliberate — keeping an accepted-but-ignored field is a footgun; P0 A1 relationship DTOs ship without it to stay consistent.
