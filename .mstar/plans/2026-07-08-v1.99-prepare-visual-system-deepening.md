# P-1 — Prepare: Visual System Deepening Contract

**Goal:** Lock the product, package-boundary, and visual-direction contract for V1.99 before moving UI primitives or redesigning `/surfaces`.

**Architecture:** This plan is a product/architecture Prepare plan. It updates iteration-scoped specs/guides and any required package rules so implementation plans can work from a stable boundary instead of guessing which components belong in `@42ch/nexus-ui`.

**Tech Stack:** Markdown specs, root DESIGN pair, `@42ch/nexus-ui`, `@nexus/design-tokens`, `apps/design-studio`, `apps/web`.

**Execution:** inline

## Product Framing

**User value:** Contributors should be able to make a visual decision once, see it quickly in Design Studio, reuse it through `@42ch/nexus-ui` when it is truly shared, and integrate it into Web without redesigning or revalidating the same primitive from scratch.

**Problem statement:** V1.98 created a safe read-only studio but preserved two sources of friction: reusable primitives still belong to `apps/web`, and `/surfaces` does not yet demonstrate a compelling product-quality direction. Without a locked boundary and workflow contract, P0/P1 implementers would either over-promote app-specific code or polish fixtures that cannot graduate into real product UI.

**Target state for this plan:** P-1 must leave implementation teams with an explicit promote / keep-web / keep-studio decision model, an observable `/surfaces` quality bar, and a landing strategy for workflow guidance that does not confuse Morning Star harness process with product work.

**Non-goals:** This plan does not implement components, redesign `/surfaces`, run visual QA, create knowledge content, or create skill artifacts. It prepares the product contract those later plans must obey.

## Global Constraints

- Plans must solve real problems; Morning Star harness gates are process, not plan scope.
- Start-chain edits may use `.mstar/iterations/v1.99/**` and `.mstar/specs/**`; do not add `.mstar/knowledge/**` content during iteration-start.
- `@42ch/nexus-ui` may only accept pure presentational, token-driven components with no app state, daemon, route, or layout dependency.
- Package architecture must lock public exports, runtime dependency posture, token consumption, asset rules, and consumer wrapper strategy before P0 implementation starts.
- `apps/design-studio` remains daemon-independent and read-only.
- `wire_contracts_changed: false` unless explicitly revised by architect review.

---

### Task 1: Lock Component Promotion Boundary

**Files:**
- Modify: `.mstar/iterations/v1.99/specs/component-promotion-boundary.md`
- Modify if approved: `packages/nexus-ui/AGENTS.md`
- Modify if approved: `packages/nexus-ui/README.md`

**Interfaces:**
- Consumes: V1.98 design unification spec and existing `packages/nexus-ui` boundary
- Produces: implementation-ready first-batch component candidate list, non-promotion reasons, and package boundary

- [x] Review existing `apps/web/src/components/ui/*` primitives against the promotion rules.
- [x] Decide first-batch promote / keep-web / keep-studio / defer classification.
- [x] Record `Input`, `Label`, and `Textarea` as deferred to a later Form Field slice with a named accessibility/form-state revisit trigger.
- [x] Lock package export shape, tsup build-entry expectations, allowed runtime dependencies, and package-local class-composition helper strategy.
- [x] Amend package docs/rules if the boundary changes.
- [x] Record any deferred component classes with owner, trigger, and why the user/developer value does not justify promotion yet.

### Task 2: Lock Visual Direction For Surfaces

**Files:**
- Modify: `.mstar/iterations/v1.99/guides/studio-first-ui-workflow.md`
- Modify if needed: `DESIGN.md`
- Modify if needed: `DESIGN.dark.md`
- Modify if needed: `.mstar/specs/design-studio.md`

**Interfaces:**
- Consumes: `/surfaces` current fixtures and root DESIGN token contract
- Produces: a visual target and acceptance checklist for P1 that a reviewer can evaluate without reading implementation code

- [x] Define the target visual qualities for setup and shell surfaces.
- [x] Identify missing tokens versus implementation-only issues.
- [x] Decide which surface elements are one-off fixtures, promoted primitives, web-only wrappers, or future web product components.
- [x] Confirm `/surfaces` imports no product pages, layout shell, daemon client, contracts package, or Web data hooks.
- [x] Write an observable light/dark/a11y acceptance checklist for P1.

### Task 3: Lock Studio-First Workflow Landing Strategy

**Files:**
- Modify: `.mstar/iterations/v1.99/guides/studio-first-ui-workflow.md`
- Modify if approved: `apps/design-studio/AGENTS.md`
- Modify if approved: `apps/web/AGENTS.md`
- Modify if approved: `packages/nexus-ui/AGENTS.md`

**Interfaces:**
- Consumes: user direction and `mstar-skill-authoring` purpose test
- Produces: implementation-ready workflow guidance and durable landing decision

- [x] Define Stage 1-4 workflow with concrete validation evidence.
- [x] State how Design Studio can consume promoted `@42ch/nexus-ui` primitives while keeping `@web-ui/*` only for transitional, unpromoted primitives.
- [x] Decide which guidance belongs in package/app `AGENTS.md` during the iteration versus what remains iteration evidence.
- [x] Defer knowledge promotion to iteration-close unless a locked spec requires earlier references.
- [x] Decide whether a later skill proposal should even be considered; default no unless the workflow proves reusable beyond this repo and would materially change future agent behavior.

## Prepare Acceptance Hooks

- P0 can name its first batch without using "promote more components" as open-ended scope.
- P0 can implement without deciding package exports, dependency placement, asset handling, or wrapper strategy during coding.
- P1 can evaluate `/surfaces` against concrete qualities: hierarchy, rhythm, depth, affordance clarity, theme parity, focus visibility, and fixture-to-product traceability.
- P2 can record evidence from P0/P1 rather than inventing workflow guidance before the workflow has been proven.
- `.mstar/specs/design-studio.md` reflects that V1.99 reopens the V1.98 brand-only package boundary only for approved presentational primitives.

## Plan self-review (PM before locked)

1. **Spec coverage:** Boundary, visual direction, and workflow landing each map to a task.
2. **Placeholder scan:** No placeholder may remain after Phase 1 Review & Edit.
3. **Type consistency:** Plan IDs, branch names, and referenced workspace paths match the V1.99 compass.

## Review Gate Summary

**QC: skipped — Prepare contract plan (`qa_gate: N/A`).** Review provided by compass Grill-Me session (locked all major decisions) + sequential specialist review chain: architect (T1 promotion boundary, verified against codebase), product-manager (T2 visual direction + 9-item acceptance checklist), writing-specialist (T3 workflow landing strategy + corpus hygiene). Contract is implementation-ready for P0/P1.

### Locked outputs
- `component-promotion-boundary.md` — Status: Locked; P0 Implementation Contract with verified dependency footprint (cva, radix-slot, clsx, tailwind-merge); 11-component non-promotion record.
- `studio-first-ui-workflow.md` — P1 Surfaces Visual Direction (9-item observable checklist, token gap triage: composition-only) + Workflow Landing Strategy (AGENTS.md targets, @web-ui/* transitional policy, skill default: no).
- `packages/nexus-ui/AGENTS.md` — Added `cn` helper ownership rule.

## QA Gate Summary

> Prepare plan; QA gate N/A unless review chain requests observable validation.
