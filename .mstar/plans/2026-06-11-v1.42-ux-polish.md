# V1.42 UX Polish (V1.41 Carry-Forward) — Plan

**Plan ID**: `2026-06-11-v1.42-ux-polish`  
**Title**: P-last — V1.41 UX/refactor residuals + iteration closeout  
**Created**: 2026-06-11  
**Status**: Implemented  
**Owner**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.42-ux-polish`  
**Merge target**: `iteration/v1.42`  
**Depends on**: `2026-06-11-v1.42-agent-tool-production-wiring` (merged)  
**Iteration compass**: [v1.42-multi-volume-serial-writing-delivery-compass-v1.md](../iterations/v1.42-multi-volume-serial-writing-delivery-compass-v1.md)

---

## 1. Problem Statement

V1.41 QC registered UX/refactor carry-forward items targeted at V1.42. User requirement: **necessary UX not silently deferred**; nits may waive with tracker update.

---

## 2. Goals — residual table (from `status.json`)

| ID | Target | Summary |
| --- | --- | --- |
| R-V141P0-02 | refactor | Deduplicate `handle_status` vs removed `run.rs` |
| R-V141P0-03 | defer | `WorkPatch` builder pattern |
| R-V141P0-05 | UX | `--from-work` + `--reopen` + `--extend-chapters` combined path |
| R-V141P0-06 | UX | Status missing-file hint for completion-lock |
| R-V141P0-08 | defer | `WorkRef::new` newtype |
| R-V141P0-11 | UX | `WorkSummary` list shows `completion_locked_at` |
| R-V141P1-09 | defer | DAO archive `creator_id` explicit param |
| R-V141P1-10 | defer | Pool promote `--set-default` single round-trip |
| R-V141P1-11 | UX | Hide `creator_id` on pool DTOs |
| R-V141P1-12 | UX | CJK `title_to_slug` fallback |
| R-V141P1-13 | UX | Inspiration slug auto-suffix on collision |
| R-V141P1-14 | defer | Split pool help per `PoolAction` |
| R-V141P1-19 | nit | `From<PoolEntry>` for DTO construction |

**Principle**: P1 owns volume **data semantics** in status API; P-last owns **CLI presentation** (tables, copy, combined flags).

---

## 3. Non-goals

- Re-implement P1 multi-volume schema
- runtime_lock (P0)
- Author quickstart doc (BL-10 backlog)

---

## 4. Acceptance Criteria

1. Each row above has fix/waive/defer closure in `status.json` with note.
2. Waived nits update deferred tracker `Target` or §3.4 if product-visible defer.
3. V1.42 compass + `iterations/README` ready for Shipped after QC/QA on `iteration/v1.42`.

---

## 5. Tasks

- [x] T1: Triage 14 rows — implement vs waive with user-path test.
- [x] T2: CLI polish (status list, pool help, slug UX).
- [x] T3: Refactor `handle_status` dedup.
- [x] T4: Combined CLI flag paths.
- [x] T5: Close residuals + iteration harness Shipped markers.

---

## 6. Verification

```bash
cargo test -p nexus42 -- creator_works 2>&1 | tail -30
rg -n 'R-V141P0-0[2-8]|R-V141P1-(09|1[0-9])' .mstar/status.json
```
