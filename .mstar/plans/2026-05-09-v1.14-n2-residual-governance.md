# V1.14 N2 Residual Governance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve V1.14 governance gate `G3` by re-evaluating `R-V113-007` and documenting the accepted `R-V113-005` environment variance decision without runtime behavior changes unless evidence requires a follow-up implementation plan.

**Architecture:** Treat this as a governance and evidence pass over existing residuals. Keep canonical residual state in root `.agents/status.json`, place any long-form rationale under `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/`, and avoid changing Rust code unless the re-evaluation explicitly selects a fix path in a separate logic plan.

**Tech Stack:** `.agents` harness Profile B, Markdown residual rationale docs, cargo focused test evidence, status ledger updates.

---

## File Structure and Responsibility Lock

- `.agents/status.json` — canonical machine-readable residual decisions; update only after evidence is collected and the final decision is selected.
- `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-005-upstream-timeout-e2e-variance.md` — prose evidence matrix for accepted OS/proxy e2e duration variance (`R-V113-005`).
- `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-007-flaky-auth-unit-test.md` — prose re-evaluation record and final decision for flaky auth unit test (`R-V113-007`).
- `crates/nexus42/src/auth/mod.rs` — read-only investigation target for `R-V113-007`; do not edit in this docs-category plan.
- `crates/nexus42/tests/creator_register_e2e.rs` — read-only evidence target for `R-V113-005`; do not edit in this docs-category plan.

---

### Task 1: Document accepted `UpstreamTimeout` e2e variance (`R-V113-005`)

**Files:**
- Create: `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-005-upstream-timeout-e2e-variance.md`
- Modify: `.agents/status.json`

- [ ] **Step 1: Confirm the residual is already accepted and backlog-targeted**

Run: `jq '.residual_findings["2026-05-06-v1.13-oss-forward-delivery"][] | select(.id == "R-V113-005")' .agents/status.json`
Expected: `decision` is `accept` and `target_date` is `backlog`.

- [ ] **Step 2: Capture focused evidence for the e2e timeout path**

Run: `cargo test -p nexus42 creator_register_e2e -- --nocapture`
Expected: PASS, or a known timeout-duration variance that still surfaces deterministic error context. Record OS, proxy/network notes, elapsed duration if available, and whether the variance exceeded the previously observed ~30s envelope.

- [ ] **Step 3: Write the environment matrix rationale doc**

Create `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-005-upstream-timeout-e2e-variance.md` with this structure:

```md
# R-V113-005 UpstreamTimeout E2E Variance

**Residual:** `R-V113-005`
**Decision:** Accept
**Target:** Backlog
**Scope:** `crates/nexus42/tests/creator_register_e2e.rs`

## Evidence Matrix

| Environment | Proxy/Network Condition | Observed Duration | Result | Notes |
|-------------|-------------------------|-------------------|--------|-------|
| macOS local | Record actual condition | Record actual duration or `not measured` | PASS/FAIL | Include command output summary |

## Rationale

The `UpstreamTimeout` path validates deterministic CLI error handling under a simulated upstream timeout. Runtime duration can vary by OS scheduling and proxy/network configuration, and the residual is accepted because the behavioral assertion is deterministic while wall-clock duration is environmental.

## Revisit Trigger

Re-open if the test becomes nondeterministic in result, exceeds CI timeout budgets, or stops surfacing timeout/platform context in the CLI-visible error.
```

Replace `Record actual condition`, `Record actual duration or not measured`, and `PASS/FAIL` with collected evidence before committing.

- [ ] **Step 4: Update residual tracking link only after doc exists**

In `.agents/status.json`, update `R-V113-005.tracking_link` to:

```json
".agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-005-upstream-timeout-e2e-variance.md"
```

Keep `decision: "accept"` and `target_date: "backlog"` unchanged.

---

### Task 2: Re-evaluate flaky auth unit test (`R-V113-007`)

**Files:**
- Create: `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-007-flaky-auth-unit-test.md`
- Modify: `.agents/status.json`
- Read-only: `crates/nexus42/src/auth/mod.rs`

- [ ] **Step 1: Inspect current residual state**

Run: `jq '.residual_findings["2026-05-06-v1.13-oss-forward-delivery"][] | select(.id == "R-V113-007")' .agents/status.json`
Expected: current state is `decision: "accept"`, `target_date: "V1.14"`, and no tracking document yet.

- [ ] **Step 2: Reproduce or clear the reported flake with repeated focused runs**

Run: `cargo test -p nexus42 auth::tests::get_returns_none_for_unknown_creator -- --nocapture`
Expected: record PASS/FAIL and exact output.

Then run a short repeat loop manually from the shell:

```bash
for i in 1 2 3 4 5; do cargo test -p nexus42 auth::tests::get_returns_none_for_unknown_creator -- --nocapture || exit 1; done
```

Expected: either 5/5 PASS, or a reproducible failure with output that explains the race condition.

- [ ] **Step 3: Decide one of three outcomes**

Select exactly one outcome and record the reason:

- `fix-needed`: evidence shows a deterministic race or shared-state issue; open a follow-up logic plan and keep `R-V113-007` deferred.
- `quarantine-needed`: evidence shows ongoing nondeterminism that blocks CI but fix scope is not safe in this governance plan; open a follow-up logic plan to quarantine with rationale.
- `accept-with-expanded-evidence`: repeated runs are stable or flake remains pre-existing/environmental; keep accepted with a written rationale and revisit trigger.

- [ ] **Step 4: Write the re-evaluation record**

Create `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-007-flaky-auth-unit-test.md` with this structure:

```md
# R-V113-007 Flaky Auth Unit Test Re-evaluation

**Residual:** `R-V113-007`
**Scope:** `crates/nexus42/src/auth/mod.rs`
**Test:** `auth::tests::get_returns_none_for_unknown_creator`
**Decision:** accept-with-expanded-evidence | fix-needed | quarantine-needed

## Evidence

| Command | Result | Notes |
|---------|--------|-------|
| `cargo test -p nexus42 auth::tests::get_returns_none_for_unknown_creator -- --nocapture` | PASS/FAIL | Summarize output |
| `for i in 1 2 3 4 5; do cargo test -p nexus42 auth::tests::get_returns_none_for_unknown_creator -- --nocapture || exit 1; done` | PASS/FAIL | Summarize repeat-run result |

## Rationale

State why the selected decision is safe for V1.14 gate `G3`.

## Follow-up

If `fix-needed` or `quarantine-needed`, name the follow-up plan ID to create before implementation starts. If `accept-with-expanded-evidence`, state the revisit trigger.
```

Replace the decision alternatives, `PASS/FAIL`, and summary text with actual evidence before committing.

- [ ] **Step 5: Update canonical residual row**

If the final decision is `accept-with-expanded-evidence`, keep `decision: "accept"`, set `tracking_link` to the new rationale doc path, and move `target_date` to `backlog` only if the rationale explicitly removes it from V1.14 active work.

If the final decision is `fix-needed` or `quarantine-needed`, keep `target_date: "V1.14"`, set `decision: "defer"`, set `tracking_link` to the new rationale doc path, and create/register the follow-up logic plan before claiming `G3` is satisfied.

---

### Task 3: Verify governance consistency and gate alignment

**Files:**
- Modify: `.agents/status.json`
- Read: `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/*.md`

- [ ] **Step 1: Validate JSON and residual links**

Run:
- `jq '.' .agents/status.json >/dev/null`
- `test -f .agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-005-upstream-timeout-e2e-variance.md`
- `test -f .agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-007-flaky-auth-unit-test.md`

Expected: all commands exit `0`.

- [ ] **Step 2: Confirm no runtime files changed in this docs plan**

Run: `git diff --name-only`
Expected: only `.agents/status.json` and files under `.agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/` changed during implementation.

- [ ] **Step 3: Confirm `G3` outcome is explicit**

Run: `rg "R-V113-007|G3|Decision" .agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-007-flaky-auth-unit-test.md .agents/status.json`
Expected: output shows either accepted written rationale or a registered follow-up plan that resolves/quarantines the residual.

- [ ] **Step 4: Commit governance update**

```bash
git add .agents/status.json .agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-005-upstream-timeout-e2e-variance.md .agents/plans/residuals/2026-05-06-v1.13-oss-forward-delivery/r-v113-007-flaky-auth-unit-test.md
git commit -m "docs(agents): document v1.14 residual governance decisions"
```

---

## Gate Alignment

- `G3`: This plan satisfies the gate only after `R-V113-007` is resolved, quarantined with explicit rationale, or accepted with expanded written evidence.
- `R-V113-005`: Already accepted/backlog; this plan adds the requested documentation pass without changing runtime behavior.

## Self-Review Checklist

- Spec coverage: `R-V113-005` and `R-V113-007` each have a task, evidence command, rationale file path, and status update rule.
- Placeholder scan: evidence templates require replacement with actual command results before implementation commit.
- Runtime boundary: this docs-category plan is read-only for Rust sources; any fix/quarantine code must be registered as a separate logic plan.
