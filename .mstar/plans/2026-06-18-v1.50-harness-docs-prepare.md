# V1.50 Harness Docs Prepare — Plan

**Plan ID**: `2026-06-18-v1.50-harness-docs-prepare`
**Title**: P-1 — V1.50 compass, plans, spec overlays, tracker, status (docs-only)
**Created**: 2026-06-18
**Status**: Todo
**Owner**: `@project-manager`
**Agents**: `@project-manager`, `@writing-specialist`
**Task category**: `docs`
**Working branch**: `iteration/v1.50`
**Merge target**: `iteration/v1.50`
**Worktree**: repo root on `iteration/v1.50`
**Iteration compass**: [v1.50-novel-author-production-loop-and-world-kb-closure-delivery-compass-v1.md](../iterations/v1.50-novel-author-production-loop-and-world-kb-closure-delivery-compass-v1.md)

---

## 1. Problem Statement

V1.49 shipped on `main` (PR #62) but V1.50 harness was not registered. Grill decisions (A+B mixed, S-B dual track) and 6 V1.49 carry-forward residuals need compass + plan stubs + spec overlays before Execute.

---

## 2. Goals

1. Author V1.50 compass with grill §0.1 (18 locked decisions) + §0.2 codebase baseline.
2. Register T-A P0..P3 + T-B P0..P2 + P-last plan stubs (9 total including P-1).
3. Draft overlays:
   - `novel-writing/cron-staggering.md` (new file, Draft V1.50)
   - `novel-writing/auto-chronology.md` (new file, Draft V1.50)
   - `entity-scope-model.md` §5.5 World KB promotion state machine (extension to existing Master)
4. Activate `status.json` (`integration_branch: iteration/v1.50`, `latest_active_iteration: v1.50 (prepare GO)`).
5. Update deferred tracker + README indexes.

---

## 3. Non-goals

- Product code (`crates/`, `schemas/`, `embedded-presets/`).
- QC tri-review (PM signoff only).
- V1.50 T-A / T-B / P-last implementation (separate plans).

---

## 4. Acceptance Criteria

1. V1.50 compass + 9 plans + 3 overlays + tracker + status active.
2. `plans[]` shows V1.50 rows: P-1 `Todo` (then `Done` after signoff), T-A P0..P3 + T-B P0..P2 + P-last `Todo`.
3. `pre_implement_gate` → `GO — V1.50 P-1 Done 2026-06-18` after PM signoff.
4. `python3 -m json.tool .mstar/status.json` passes.
5. Residual mapping covers all 6 V1.49 carry-forward + V1.50 P-last WL-A 8-10 placeholder.
6. `git diff --name-only` → only `.mstar/` paths in prepare session.

---

## 5. Tasks

- [x] T1: V1.50 compass with grill §0.1 + §0.2 baseline.
- [x] T2: T-A P0..P3 + T-B P0..P2 + P-last plan stubs (9 total including P-1).
- [x] T3: Spec Draft overlays (3 overlays per §2).
- [x] T4: `status.json` activation (V1.50 metadata + integration branch + latest_active).
- [x] T5: Tracker + README indexes (V1.50 active marker + iteration index entry).
- [ ] T6: Verification (json.tool, rg grep, git diff --name-only audit).
- [ ] T7: PM signoff → P-1 Done.

---

## 6. Verification

```bash
python3 -m json.tool .mstar/status.json >/dev/null
rg -n 'v1\.50|V1\.50|2026-06-18-v1\.50' .mstar/
git diff --name-only          # must show only .mstar/ paths
rg -n 'Document class.*Draft' .mstar/knowledge/specs/novel-writing/cron-staggering.md .mstar/knowledge/specs/novel-writing/auto-chronology.md
rg -n 'V1\.50 Draft' .mstar/knowledge/specs/entity-scope-model.md
```

---

## 7. Sign-off

PM `@project-manager` signs off after §6 verification passes; sets `status: Done` in frontmatter + `done_at: 2026-06-18`; sets `metadata.pre_implement_gate` → `GO` in `status.json`.