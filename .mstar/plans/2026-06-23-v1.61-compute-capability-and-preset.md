---
plan_id: 2026-06-23-v1.61-compute-capability-and-preset
title: V1.61 P3 — narrative.compute Capability + combat-engine Preset
iteration: V1.61
slot: P3
status: Done
effort: L
owner: "@fullstack-dev-2"
review: Tri-review + mid-QA
wave: 3
dependency: P1 + P2 merged to integration
---

# P3: narrative.compute Capability + combat-engine Preset

Register `narrative.compute` capability. Create combat-engine preset.
Wire state delta apply + graceful error handling.

## Tasks

- [x] T1: Register `narrative.compute` in `CapabilityRegistry`
- [x] T2: Implement `Capability` trait — KB + narrative read → WasmEngine compute → apply output
- [x] T3: `apply_state_delta()` merge logic — incremental `add`/`sub`/`set` on state fields
- [x] T4: Error handling — compute failure → TimelineEvent, not daemon crash
- [x] T5: `embedded-presets/combat-engine/preset.yaml`
- [x] T6: State machine: load_world → compute → apply_delta → advance_timeline → done
- [x] T7: Prompt templates (prompts/load-world.md, etc.)
- [x] T8: Register preset in `preset_version_for_id` + sync test
- [x] T9: Integration tests — capability runs, state delta applied, events created

## Done when

- `cargo test -p nexus-orchestration` passes
- Preset passes embedded validation gate
- `narrative.compute` discoverable in CapabilityRegistry

## Completion Report v2

- **Plan:** 2026-06-23-v1.61-compute-capability-and-preset
- **Working branch:** feature/v1.61-compute-capability-and-preset
- **Commits:** c5cc060f
- **Files changed:** 13 (7 new, 6 modified)
- **Tasks completed:** T1..T9 (all)
- **Validation:**
  - `cargo test -p nexus-orchestration`: pass — 927 lib tests + all integration test binaries pass
  - `cargo clippy -p nexus-orchestration`: pass — clean (no warnings)
  - `cargo +nightly fmt --check`: pass — clean
  - Integration test (compute cycle): pass — `narrative_compute_full_cycle` exercises basic-combat WASM module, verifies graceful error handling for compute failures (compute_error TimelineEvent created), state delta unit tests cover add/sub/set operations
- **Design decisions:**
  - state_delta merge semantics: `add`/`sub`/`set` per wire contract `schemas/compute/compute-output.schema.json` (enum values `["add", "sub", "set"]`)
  - state_delta.op enum: kept as `String` (wire contract generates `String`), validated at runtime with `VALID_OPS` constant
  - battle_report size cap: 64 KB (`BATTLE_REPORT_MAX_BYTES = 64 * 1024`)
- **Residual findings:** none
- **Risks for Wave 4 (P-last daemon integration):**
  - `NarrativeCompute::with_pool()` creates a new `WasmEngine` per pool; P-last should create ONE engine at daemon boot and inject via a new constructor (e.g., `with_pool_and_engine()`) to match the "reuse ONE WasmEngine" note from P2
  - The capability currently compiles the embedded WASM module on every `run()` call; a pre-compiled module cache should be added in P-last
  - State delta only handles flat paths inside the `character`/`item`/etc. state namespace (first segment = block_type state key); deeper nested paths (e.g., `character.equipment.weapon.durability`) are not yet tested
- **Worktree/Branch state:** clean, on feature branch
