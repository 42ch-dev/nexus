---
plan_id: 2026-06-23-v1.61-compute-capability-and-preset
title: V1.61 P3 — narrative.compute Capability + combat-engine Preset
iteration: V1.61
slot: P3
status: Todo
effort: L
owner: "@fullstack-dev-2"
review: Tri-review + mid-QA
wave: 3
dependency: P1 + P2 merged to integration
---

# P3: narrative.compute Capability + combat-engine Preset

Register `narrative.compute` capability. Create combat-engine preset.
Wire state delta apply + graceful error handling.

## Tasks

- [ ] T1: Register `narrative.compute` in `CapabilityRegistry`
- [ ] T2: Implement `Capability` trait — KB + narrative read → WasmEngine compute → apply output
- [ ] T3: `apply_state_delta()` merge logic — incremental `+/-/set` on state fields
- [ ] T4: Error handling — compute failure → TimelineEvent, not daemon crash
- [ ] T5: `embedded-presets/combat-engine/preset.yaml`
- [ ] T6: State machine: load_world → compute → apply_delta → advance_timeline → done
- [ ] T7: Prompt templates (prompts/load-world.md, etc.)
- [ ] T8: Register preset in `preset_version_for_id` + sync test
- [ ] T9: Integration tests — capability runs, state delta applied, events created

## Done when

- `cargo test -p nexus-orchestration` passes
- Preset passes embedded validation gate
- `narrative.compute` discoverable in CapabilityRegistry
