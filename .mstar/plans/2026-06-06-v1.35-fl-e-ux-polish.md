# V1.35 FL-E UX Polish — Locked Plan

**Plan ID**: `2026-06-06-v1.35-fl-e-ux-polish`  
**Title**: P4 — chain defaults, stage advance simplification (DF-53 partial)  
**Created**: 2026-06-06  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.35-fl-e-ux-polish`  
**Merge target**: `iteration/v1.35`  
**Depends on**: P3 help baseline  
**Spec refs**: [creator-workflow.md](../knowledge/specs/creator-workflow.md), DF-53

---

## 1. Problem Statement

FL-E requires four manual `stage advance` commands. `chain_novel_writing` defaults false causes intake→produce gap (UX-006).

---

## 2. Goals

1. Improve default chaining after intake (policy TBD: default true vs explicit `--no-chain` opt-out).
2. Ship `creator run next` or equivalent if approved in P3.
3. Partial DF-53: document remaining auto-chain deferrals.
4. Optional: address R-FL-E-ENDP dedicated endpoint (low — only if cheap).

---

## 3. Non-goals

- Conditional routing (DF-56)
- Full unattended auto-chain across all stages without user consent

---

## 4. Tasks

- [x] T1: Product decision on chain_novel_writing default
- [x] T2: Implement default + CLI output next-step hints
- [ ] T3: Implement `run next` if scoped (deferred — not cheap enough for V1.35)
- [x] T4: Update creator-workflow.md if behavior changes
- [x] T5: Test for new default in command_surface_contract

---

## 5. Acceptance

1. Demo path in compass §4 requires fewer manual commands than V1.34 baseline OR documents intentional explicit advance with improved hints.
2. DF-53 tracker row updated (partial vs closed).
3. Tests pass.

---

## 6. Verification

```bash
cargo test -p nexus42 -p nexus-daemon-runtime
# Manual: creator run start → reduced friction to produce stage
```
