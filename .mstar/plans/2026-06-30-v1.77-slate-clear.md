# 2026-06-30-v1.77-slate-clear (P1)

**Iteration**: V1.77 · **Plan**: P1 (Track B companion) · **Status**: Todo · **Owner**: `@fullstack-dev` + `@frontend-dev`
**Compass**: [v1.77-…-compass-v1.md §1.1 Track B (B1–B3)](../iterations/v1.77-findings-remediation-ui-and-post-canvas-inflection-compass-v1.md)
**Depends on**: P-1 (compass Locked)

## Goal

Close the 3 open V1.76-QC-followup residuals in file-disjoint world-kb territory (NOT the findings frontend, which is P0). Slate → 0.

## Scope (B1–B3)

- **B1 — `R-V176QC1-S001` (LlmExtractOutcome.relationships field discoverability, low)**: improve the `relationships` field discoverability in `crates/nexus-orchestration/src/capability/builtins/llm_extract.rs`. Owner: `@fullstack-dev`.
- **B2 — `R-V176QC1-S002` (relationship graph pagination TODO, low)**: resolve the pagination TODO on `crates/nexus-daemon-runtime/src/api/handlers/world_kb.rs` GET graph. Owner: `@fullstack-dev`.
- **B3 — `R-V176QC3-S001` (bulk-promote throttling, nit)**: add throttling/debounce to the bulk-promote path in `apps/web/src/components/canvas/world-kb/suggested-relationships-pane.tsx`. Owner: `@frontend-dev`.

## Branch policy

- Working branch: `feature/v1.77-slate-clear` from `iteration/v1.77`
- Merge target: `iteration/v1.77`
- Worktree: `.worktrees/v1.77-slate/` (parallel with P0)

## Files touched

- `crates/nexus-orchestration/src/capability/builtins/llm_extract.rs` (B1)
- `crates/nexus-daemon-runtime/src/api/handlers/world_kb.rs` (B2)
- `apps/web/src/components/canvas/world-kb/suggested-relationships-pane.tsx` (B3)

## File-disjointness with P0

P1 owns world-kb backend (`llm_extract.rs`/`world_kb.rs`) + the world-kb canvas bulk-promote component. P0 owns the findings frontend + codegen + findings specs. No overlap. PM sequences B1/B2 (backend) if the same `@fullstack-dev` owns both P0's codegen share and P1's backend share.

## Acceptance (DoD)

1. All 3 residuals closed → `residual_findings` slate at 0 for V1.77.
2. Each fix surgical (pattern-match the residual class; no piggyback refactor).
3. `cargo test -p <crate>` + `cargo clippy -p <crate> -- -D warnings` green for touched crates; vitest green for B3.

## Wire contracts

`wire_contracts_changed: FALSE` (code polish only).
