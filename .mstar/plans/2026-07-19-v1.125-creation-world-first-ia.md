# P2 — Creation World-first IA (first slice)

> **Iteration compass:** `.mstar/iterations/v1.125/delivery-compass.md`  
> **Spec:** `.mstar/iterations/v1.125/specs/creation-world-first-ia.md`  
> **SSOT status:** `.mstar/status.json`

**Goal:** Creator tab is Worlds-first experience-driven library; Settings sits with theme; no Timeline peer menu clutter.

**Execution:** mstar-sdd

### Wire contracts (architect LOCKED)

- **Verdict: `wire_contracts_changed: false`** — nav reorder + empty-state CTAs only; Create World gated by `NexusClient.createWorld` presence (absent today — Work-create fallback).

## Tasks

- [ ] **T1 (AC-V1125-5 — nav):** Reorder Creator nav Worlds → Works; remove Timeline + Work Timelines groups; keep routes deep-linkable; update tests + [`web-ui.md`](../../specs/web-ui.md) §29.17.4.
  - **Verify (author):** Creator sidebar shows Worlds then Works only; `/timeline` URL still resolves.
  - Files: `sidebar.tsx`, `root-layout.tsx` mobile nav if any, `shell.json`, `sidebar.test.tsx`
- [ ] **T2 (AC-V1125-5 — empty CTAs):** Card-sized empty CTAs on Worlds/Works. **Worlds:** if `'createWorld' in client` → Create World flow; **else** → open `CreateWorkDialog` with honest copy (“Worlds are created from your Works”) — no dead-end.
  - **Verify (author):** Empty Worlds with no API → one actionable card CTA to Work create; Works empty → Create Work card.
  - Files: `worlds-page.tsx`, `works-page.tsx` (share dialog trigger), locales, tests
- [ ] **T3 (AC-V1125-5 — Settings):** Settings gear icon beside theme toggle in header; demote/remove sidebar Settings text row.
  - **Verify (author):** Header shows gear + theme; sidebar has no prominent Settings row.
  - Files: `header.tsx`, `shell-sidebar-chrome.tsx`, tests

## Done when

- **AC-V1125-5:** Worlds-first nav; card CTAs; honest Worlds→Work-create fallback; Settings beside theme.

## Working branch

`feature/v1.125-creation-world-first-ia` → merge `iteration/v1.125`
