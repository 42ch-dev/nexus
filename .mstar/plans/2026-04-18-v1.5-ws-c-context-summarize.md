# V1.5 WS-C — `context.summarize` capability

**Plan ID**: `2026-04-18-v1.5-ws-c-context-summarize`
**Status**: Todo
**Compass (SSOT)**: [.agents/iterations/v1.5-nexus-delivery-compass-v1.md](.agents/iterations/v1.5-nexus-delivery-compass-v1.md) §4 WS-C
**Milestone**: M2 — Creator Intelligence capabilities functional
**Working branch**: `feature/v1.5-stabilization` (topic branch off integration after WS-A merges)
**Dependencies**: **WS-A must complete first** — depends on hardened Schedule FSM and CoreContextManager (especially R6 race fix).

**Goal**: Implement the LLM-driven `core_context` summarization capability. Users (via preset YAML) can declare `context_update.kind: llm_summarize` on a state's exit, causing the engine to invoke an LLM-backed summarization step and write a new `core_context_versions` row.

**Authoritative design**: [knowledge/creator-schedule-and-core-context.md](knowledge/creator-schedule-and-core-context.md) §6.3 (reserved derivation kind), §11 (V1.5 forward compatibility).

**Key deliverables**:
- New capability `context.summarize` at `crates/nexus-orchestration/src/capabilities/context_summarize.rs`
- Preset YAML extension: `context_update.kind: llm_summarize`
- Zero schema migration (variant already reserved)
- Graceful fallback when no LLM configured (`CapabilityError::TransientExternal`)
- Optional `novel-writing` preset demonstrator

**Evidence**: unit test with mock LLM; preset YAML validation; integration test with novel-writing preset; `cargo test --workspace` green.

**Effort (agent-oriented)**: M — 2–3 agent sessions (new capability + preset loader extension + integration test; design already specified in §11).

---

*Created: 2026-04-18. Detailed task breakdown to be added when WS-A completes.*
