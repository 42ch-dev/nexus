# P2 — Canvas-first work shell

> **Status:** Draft (Phase 1). Product specify + clarify **done**; architect **plan done** (2026-07-15).

| Field | Value |
|-------|-------|
| plan_id | 2026-07-15-v1.118-canvas-work-shell |
| iteration_refs | V1.118 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.118/specs/canvas-work-shell.md` |
| iteration_compass | `.mstar/iterations/v1.118/delivery-compass.md` |
| spec_integration_branch | iteration/v1.118 |
| merge_target | iteration/v1.118 |
| wire_contracts_changed | false |

**Goal:** Entering a work switches the UI to a Canvas-first shell — main Outline canvas + right Works list/preview; body read-only retained; drill-in retired.

**Depends on:** P1 (`2026-07-15-v1.118-creation-peer-groups`) — peer Works model + merge order

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors (writing) | Canvas is the core surface when inside a work — not a drill-down afterthought |
| Authors (switching works) | Right rail lists works + preview without left-rail "back out" drill-in |
| Authors (reading body) | Chapters read-only still reachable from the shell |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs Checked:** `RootLayout` scroll SSOT; V1.117 `isDrillIn`; `App.tsx` work routes; compass AD-P2; P1 dependency.
- **Impactful Ambiguities:** None.
- **Gate Decision:** go (implement blocked on P1 merge to `iteration/v1.118`)

### Plan

- **Option A — Nested `WorkShellLayout` inside RootLayout `<main>`:** Center canvas + right rail; sidebar unchanged except drill-in removal. **Trade-off:** must lift `max-w-[1200px]` for canvas routes.
- **Option B — Replace RootLayout for work routes:** **Trade-off:** duplicates header/banner; rejected.
- **Selected Approach:** Option A.
- **Long-term Target State:** Work context = canvas room; Works switcher on right; Body via chapters routes.
- **Durable Slice for This Batch:** Work shell chrome + rail metadata preview + drill-in retirement + P1 regression.
- **Module Boundaries:**

  | File | Role |
  | --- | --- |
  | `work-shell-layout.tsx` | Flex row: outlet + `WorkRail` |
  | `work-rail.tsx` | List + metadata preview card |
  | `App.tsx` | Nested routes under `works/:workId` |
  | `sidebar.tsx` | Remove `isDrillIn` / `drillInItems` (T3, after P1 merge) |

- **API/Data Contracts:** `listWorks` + `WorkSummary` for rail; preview = title, status, `primary_preset_id`/`work_profile`, `updated_at` — no new endpoints.
- **Risks and Rollback:** Canvas width regression — test outline at lg+; mobile drawer a11y. Rollback: restore drill-in (product rejects).
- **Validation Plan:** AC-P2-* E2E paths; P1 regression AC-P2-7; studio-first tokens for new chrome.
- **Effort (agent-oriented):** L — 2–3 sessions (layout + sidebar + tests).

## Prepare Package (Product)

### Specify

- **Problem:** V1.117 `isDrillIn` hides tabs and replaces nav with Back/Outline/Body — Canvas feels secondary.
- **User value:** Work context = Canvas room with Works switcher on the right.
- **Scope:** `/works/:workId/*` layout only; Outline default main; right rail; retire drill-in. AC-P2-*.
- **Non-goals:** New canvas types; Orchestrator redesign; rich preview; World KB as default main.
- **Target state:** IC-7, IC-8, IC-9 in delivery compass.
- **Roadmap if split:** Richer preview + alternate mains → next iteration (spec § Deferred scope).
- **Draft DoD:** All AC-P2-* pass; P1 regression pass; studio-first tokens for new chrome.

### Clarify

- **Decisions:** Ship peer groups AND work shell same iteration (grill-me #3). Tabs stay visible inside work (AC-P2-5).
- **Open questions (architect):** ~~Preview MVP content; responsive collapse; `drillInItems` removal vs deprecation.~~ **Resolved** — metadata preview; `<lg` end-sheet drawer; remove drill-in usage in P2 T3 (compass AD-P2).
- **Still blocked:** None — implement blocked on P1 merge to integration branch (architect `plan` **done**).

## Scope

### In scope

- Enter `/works/:workId/*` → Canvas-first shell layout (AC-P2-1)
- Main surface default = Outline canvas (AC-P2-2)
- Right rail = Works list + selected-work preview (AC-P2-3, AC-P2-8)
- Body read-only via existing `/works/:workId/chapters…` (AC-P2-4)
- Retire whole-left `isDrillIn` as **primary** enter-work UX (AC-P2-5, AC-P2-6)
- Creator/Orchestrator tabs remain visible in work context
- Studio-first for new chrome tokens/components (AC-P2-9)
- Locales + tests
- Opportunistic V1.117 residuals on touched files (incl. drill-in related)

### Not in scope

- List-mode peer group definitions (P1)
- New canvas surface types beyond existing Outline default
- Orchestration tab redesign
- Full World KB as main enter-work default
- Daemon / Profile boot (P0)

## Acceptance Criteria

Detail in `primary_spec` (AC-P2-*):

1. Work selection opens Canvas-first shell on outline (AC-P2-1, AC-P2-2).
2. Right rail: Works list + preview (AC-P2-3, AC-P2-8).
3. Body read-only at chapters routes (AC-P2-4).
4. Tabs visible; drill-in not primary (AC-P2-5, AC-P2-6).
5. P1 peer groups regression outside work routes (AC-P2-7).
6. DESIGN.md-aligned chrome (AC-P2-9).

## Tasks

- [x] T1: `WorkShellLayout` + `WorkRail` — 280px rail (lg+), metadata preview, drawer (`<lg`)
- [x] T2: Nest `works/:workId/*` routes; default redirect to `outline`; wire list selection → navigate `outline`
- [x] T3: Remove `isDrillIn` / `drillInItems` from `sidebar.tsx`; restore P1 peer groups on work routes (**rebase after P1**)
- [x] T4: Locales, studio-first tokens, tests + opportunistic residuals

## Review Gate Summary

- **QC:** Approve with residuals (`e172e85a` cleared QC2 a11y)
- **Range:** `20e9719a..e172e85a` on `plan/v1.118-p2-canvas-work-shell`
- **Residuals:** `status.json` → `residual_findings[2026-07-15-v1.118-canvas-work-shell]`

## Roadmap / deferred scope

| Deferred item | Trigger | Owner |
| --- | --- | --- |
| Richer right-rail preview (manuscript snippets, findings density) | Post-V1.118 UX slice | PM |
| Strategy/World KB as alternate main surfaces from rail | Canvas trajectory backlog | PM |

## SSOT status

`.mstar/status.json` → `plans[]` / `residual_findings`
