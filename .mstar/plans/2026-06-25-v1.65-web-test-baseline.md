# P1 — V1.65 `apps/web` Test Baseline (R-V164-QC1-S1-P1 closure)

**Plan ID**: `2026-06-25-v1.65-web-test-baseline`
**Iteration**: V1.65 (Outline & Structure Authoring) — Track A frontend test prerequisite
**Status**: Todo (stub; awaits compass lock)
**Owner**: `@frontend-dev`
**Working branch**: `feature/v1.65-web-test-baseline` (from `iteration/v1.65`)
**Merge target**: `iteration/v1.65`
**Depends on**: P-1
**Compass**: [v1.65-outline-and-structure-authoring-delivery-compass-v1.md](../iterations/v1.65-outline-and-structure-authoring-delivery-compass-v1.md)

## Overview

Close V1.64 QC1 finding R-V164-QC1-S1-P1 (**medium**): `apps/web` shipped without a test baseline at its most architectural surface (NexusClient adapter, theme/provider, error parsing). This is the **hard prerequisite** for P2 — the outline editor is the largest new UI architectural surface; shipping it on a test-less scaffold repeats the medium risk. Establish the Vitest baseline that P2 builds on.

V1.64 shipped 30 vitest tests (msw + screen smoke). This plan adds the **architectural-surface** layer that was missing.

## Tasks (stub)

- [ ] T1 — NexusClient adapter coverage: `BrowserClient` fetch path (success, error envelope parse, network failure) with msw; contract that no screen bypasses the adapter (grep guard).
- [ ] T2 — Theme/provider coverage: light/dark token application, provider mount.
- [ ] T3 — Error parsing coverage: `ErrorResponse` envelope unwrap (F-E1 shape from V1.64) → toast/notification hook.
- [ ] T4 — Test infra hardening: vitest config (jsdom), msw handlers registry, CI `web-test` job (pnpm --filter web test).
- [ ] T5 — Coverage gate baseline (lean: architectural-surface files ≥ 80%; record actual; do not block on cosmetic lines).

## Acceptance Criteria

- NexusClient adapter, theme/provider, error parsing have Vitest coverage; msw handlers registry in place.
- CI `web-test` job green on the integration branch.
- R-V164-QC1-S1-P1 marked `resolved` in `status.json` residual_findings with resolution commit + plan_id.
- `pnpm --filter web test` + `pnpm --filter web build` + `pnpm --filter web typecheck` clean.

## Dependencies

- P-1.
- Consumes V1.64 `apps/web` scaffold (NexusClient, theme/provider, error hooks already exist; this plan tests them).

## Risks / Notes

- This plan does **not** add new product features — it is pure test-baseline establishment. Scope creep into editor preview/structure-table tests must wait for P2.
- Coordination with P0: P0 codegen regenerates `@42ch/nexus-contracts` types that `apps/web` consumes; merge order on integration branch may need a codegen lockstep commit (PM coordinates at Wave 1 merge).
