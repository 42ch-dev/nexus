# P1 — NLE Timeline canvas (Studio → App)

> **Iteration compass:** `.mstar/iterations/v1.128/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.128/specs/nle-timeline-canvas.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** **Studio-first** NLE-style multi-track Timeline (centered band, horizontal scrub, layered tracks, pull-off affordance) for **dogfood visual verify**; then thin App adoption of the chrome. **No full editor parity.**

**Architecture:** Presentational extracts under `apps/web/src/components/canvas/presentational/` + Studio fixtures; App Timeline host thin-wires after Studio AC. Default no schema/daemon changes.

**Tech Stack:** React 18 + TypeScript + Vite + Vitest; React Flow only if existing App host already uses it (do not expand RF scope without architect lock).

**Execution:** mstar-sdd (multi-task)

### Wire contracts

- **Verdict: `wire_contracts_changed: false`** (default)

### Scope cap (PM Seat 1 — do not expand)

| In dogfood verify | Deferred |
|-------------------|----------|
| ≥2 labeled tracks, centered band | Trim / ripple / roll |
| Horizontal pan/scrub feel | Audio lanes / waveforms |
| One pull-off demo | Snap-to-frame, transport, shortcuts |
| Light + dark Studio fixtures | Production DnD persistence |

### Promotion list

- None required for Must; any new extract stays `@web-canvas/*` until P3 optional promotion.

### Architect lock (Seat 2) — React Flow boundary

| Item | Lock |
|------|------|
| Extract boundary | RF-free `@web-canvas/*` per `timeline-node-chrome.test.tsx:38–42` |
| Pull-off (T2) | Studio fixture local state only — no `@xyflow/react` in new P1 files |
| App adopt (T3) | Swap chrome into existing RF hosts (`canvas-shell.tsx`, `timeline-canvas.tsx`); no new node types or DnD handlers |
| Pull-off in App | **Out of scope** — Studio dogfood only |
| Wire contracts | `wire_contracts_changed: false` |
| Depends on | P0 complete (serial); P3 labels P1 Canvas surfaces |

## Tasks

- [x] **T1 (AC-V1128-2a — Studio NLE chrome + fixtures) · Must:** Add presentational NLE Timeline chrome (multi-track lanes, vertically centered band, horizontal scrub/pan feel) and Studio Canvas fixtures replacing/augmenting card-matrix Timeline samples. **Gate:** T3 blocked until Studio AC passes.
  - **Verify (author-observable):** Maintainer opens Studio `/surfaces/canvas`, sees ≥2 labeled tracks in centered band, pans horizontally along time axis, in light + dark.
  - Files: `apps/web/src/components/canvas/presentational/*` (new/extend), `apps/design-studio/src/fixtures/*timeline*`, `surfaces.tsx`

- [x] **T2 (AC-V1128-2b — Pull-off affordance) · Must:** Studio fixture demonstrates detaching one timeline-related item from a track onto the canvas area (presentational interaction — click-drag or explicit detach control both OK). No persistence or wire backing required.
  - **Verify (author-observable):** Maintainer performs one pull-off; item visibly leaves track and appears on canvas area.
  - Files: fixtures + chrome as needed

- [x] **T3 (AC-V1128-2c — Thin App adopt) · Must:** Wire App Timeline surface host to consume new chrome where it replaces existing directed-axis chrome without changing wire contracts. **After T1/T2 Studio AC only.**
  - **Verify (author-observable):** App World/Work Timeline loads; visual uses new band/tracks chrome; author does not gain trim/ripple/audio features.
  - Files: `apps/web/src/components/canvas/timeline-canvas/*` (host only as needed), tests

## Plan self-review (PM Seat 1)

1. Studio before App (T1/T2 → T3) — Studio AC is the gate
2. Scope capped: dogfood-verify slice only; spec lists explicit deferrals
3. `wire_contracts_changed: false`
4. User value: maintainer judges NLE metaphor before deeper editor work

## Architect lock (Seat 2)

See **Architect lock (Seat 2) — React Flow boundary** above. No expanded RF DnD; pull-off Studio-only.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-20-v1.128-p1-nle-timeline-canvas/` (gitignored).

## Review Gate Summary

> Filled after QC.

## QA Gate Summary

> Filled after QA.
