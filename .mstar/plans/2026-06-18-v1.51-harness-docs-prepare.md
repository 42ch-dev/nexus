# V1.51 Harness Docs Prepare — Plan

**Plan ID**: `2026-06-18-v1.51-harness-docs-prepare`
**Title**: P-1 — V1.51 compass, plans, spec overlays, tracker, status (docs-only)
**Created**: 2026-06-18
**Status**: Done
**Done at**: 2026-06-18
**Owner**: `@project-manager`
**Agents**: `@project-manager`, `@writing-specialist`
**Task category**: `docs`
**Working branch**: `iteration/v1.51`
**Merge target**: `iteration/v1.51`
**Worktree**: repo root on `iteration/v1.51`
**Iteration compass**: [v1.51-kb-closure-and-multi-writer-concurrency-delivery-compass-v1.md](../iterations/v1.51-kb-closure-and-multi-writer-concurrency-delivery-compass-v1.md)

---

## 1. Problem Statement

V1.50 shipped on `main` (PR #63 at `4db0a37b` on 2026-06-17) but V1.51 harness was not registered. Grill decisions (T-A + T-B mixed, S-B dual track, 7 plans) and 6 spec overlays (5 extensions + 2 new Masters) need compass + plan stubs + status activation before Execute.

---

## 2. Goals

1. Author V1.51 compass with grill §0.1 (16 locked decisions) + §0.2 codebase baseline (V1.50 `4db0a37b`).
2. Register T-A P0..P2 + T-B P0..P1 + P-last plan stubs (7 total including P-1).
3. Draft overlay headers (body authored in respective implement plans):
   - `world-kb-runtime-architecture.md` §5.5 LLM pathway (extension header, Draft V1.51)
   - `world-kb-runtime-architecture.md` §6 OCC stub (extension header, Draft V1.51)
   - `entity-scope-model.md` §5.5 LLM pathway subsection (extension header, Draft V1.51)
   - `novel-writing/quality-loop.md` §5 missing-KB detection extension (extension header, Draft V1.51)
   - `cli-spec.md` §6.2G update (extension header, Draft V1.51)
   - new `specs/concurrency.md` Master (Draft V1.51)
   - new `specs/llm-extract.md` Master (Draft V1.51)
4. Activate `status.json` (`integration_branch: iteration/v1.51`, `latest_active_iteration: V1.51 (prepare GO)`, `latest_active_compass: v1.51-kb-closure-and-multi-writer-concurrency`).
5. Update deferred tracker + README indexes; re-target R-V150KBED-01 + R-V150KBED-08 + R-V149P1-01 (advisory-lock portion) to V1.51.

---

## 3. Non-goals

- Product code (`crates/`, `schemas/`, `embedded-presets/`).
- QC tri-review (PM signoff only — docs-only).
- V1.51 T-A / T-B / P-last implementation (separate plans).
- End-user `docs/` content (per V1.51 grill §0.1 #11 — pre-v1.0 defer).

---

## 4. Acceptance Criteria

1. V1.51 compass + 7 plans + 6 spec overlay headers + tracker + status active.
2. `plans[]` shows V1.51 rows: P-1 `Todo` (then `Done` after signoff), T-A P0..P2 + T-B P0..P1 + P-last `Todo`.
3. `pre_implement_gate` → `GO — V1.51 P-1 Done 2026-06-18` after PM signoff.
4. `python3 -m json.tool .mstar/status.json` passes.
5. Residual mapping covers R-V150KBED-01 (→ T-A P0), R-V150KBED-08 (→ T-A P1), R-V149P1-01 advisory-lock note (→ T-B P0).
6. `git diff --name-only` → only `.mstar/` paths in prepare session.

---

## 5. Tasks

- [x] T1: V1.51 compass with grill §0.1 (16 decisions) + §0.2 baseline (V1.50 `4db0a37b`).
- [x] T2: T-A P0..P2 + T-B P0..P1 + P-last plan stubs (7 total including P-1).
- [x] T3: Spec Draft overlay headers (6 overlays per §2).
- [x] T4: `status.json` activation (V1.51 metadata + integration branch `iteration/v1.51` + latest_active + tech_debt_summary counters).
- [x] T5: Tracker + README indexes (V1.51 active marker + iteration index entry + `iterations/README.md` V1.51 row).
- [x] T6: Verification (`python3 -m json.tool`, `rg` grep, `git diff --name-only` audit).
- [x] T7: PM signoff → P-1 Done; `pre_implement_gate` → GO.

---

## 6. Verification

```bash
python3 -m json.tool .mstar/status.json >/dev/null
rg -n 'v1\.51|V1\.51|2026-06-18-v1\.51' .mstar/
git diff --name-only main...HEAD   # must show only .mstar/ paths
rg -n 'Document class.*Draft' .mstar/specs/concurrency.md .mstar/specs/llm-extract.md .mstar/knowledge/world-kb-runtime-architecture.md
```

---

## 7. Sign-off

PM `@project-manager` signs off after §6 verification passes; sets `status: Done` in frontmatter + `done_at: 2026-06-18`; sets `metadata.pre_implement_gate` → `GO` in `status.json`.

---

## 8. PM signoff (2026-06-18)

Verification §6 passed:

- `python3 -m json.tool .mstar/status.json` → OK
- `rg -c 'v1\.51|V1\.51|2026-06-18-v1\.51' .mstar/` → 67+ matches across compass + 7 plans + status + tracker + README + archived shipped features
- `git diff --name-only main...HEAD` → only `.mstar/` paths (no product code; 11 files / 968 insertions)
- 7 plans registered in `status.json.plans[]`; P-1 marked `Done`; `pre_implement_gate` → `GO`
- Compass present: `.mstar/iterations/v1.51-kb-closure-and-multi-writer-concurrency-delivery-compass-v1.md` (16 locked decisions + §0.2 baseline + §1.1-§1.6 + §2-§5)
- 3 carry-forward residuals visible with `target: V1.51 <plan>`: R-V150KBED-01 → T-A P0, R-V150KBED-08 → T-A P1, R-V149P1-01 advisory-lock portion → T-B P0
- Branch: `iteration/v1.51` (created from `main` @ `1b1b9f95`); integration branch matches V1.50 retired pattern
- Deferred tracker quick-status + §3.5 Machine state table refreshed; V1.50 stale PREPARE flag corrected to SHIPPED

PM `@project-manager` signs off. Pre-implement gate → `GO — V1.51 P-1 Done 2026-06-18`.

**Next dispatch** (per compass §0.1 decision 8 — T-A P0 ∥ T-B P0 parallel):

- **T-A P0** (`feature/v1.51-llm-extraction` from `iteration/v1.51`) — `nexus.llm.extract` capability + heuristic→LLM swap (closes R-V150KBED-01)
- **T-B P0** (`feature/v1.51-advisory-lock` from `iteration/v1.51`) — `Works/<work_ref>/.lock` advisory lock + daemon cron-side + CLI integration (closes R-V149P1-01 advisory-lock note)

Both parallel with separate git worktrees per `mstar-branch-worktree` (S-B dual track).