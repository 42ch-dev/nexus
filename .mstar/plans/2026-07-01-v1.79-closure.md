# 2026-07-01-v1.79-closure (P-last)

**Iteration**: V1.79 · **Plan**: P-last (Closure) · **Status**: Todo · **Owner**: `@project-manager`
**Compass**: [v1.79-…-compass-v1.md §3 + §6](../iterations/v1.79-author-reflection-reading-and-soul-visualization-compass-v1.md)
**Depends on**: P0 + P1 merged to `iteration/v1.79` + QC tri-review Approve + QA Pass.

## Goal

Close V1.79: fold in the light B companion (build-order CI wrapper), execute the DF-49 cancellation (tracker hygiene), run the compound round, Profile B compaction, refresh trackers, and open the PR to `main`.

## Scope

### T1 — B companion: build-order CI/prebuild wrapper (`@fullstack-dev` or `@ops-engineer`)

| Residual | Close action |
|---|---|
| `R-V178P0-QC3-001` (web typecheck requires `@42ch/nexus-contracts` prebuild — build-order not self-contained) | Make web typecheck self-contained: add a prebuild step (or CI wrapper) that runs `pnpm run codegen` (or installs the published package) before `tsc`. Document in `apps/web/AGENTS.md` if a dev-prereq doc gap. |

### T2 — DF-49 cancellation (tracker hygiene, `@project-manager`)

| Action | Detail |
|---|---|
| Cancel DF-49 | Move DF-49 (Standalone MCP server) from `deferred-features-cross-version-tracker.md` §2.3 to the cancelled archive (`archived/shipped-features-tracker.md` cancelled section). Rationale: "Conflicts with ACP-client product direction (`STRATEGY.md`: CLI is an ACP client, not a server) + circular-invocation risk (Nexus drives an agent via ACP → agent calls back into Nexus via MCP → loop). Cancelled, not deferred." |

### T3 — Reliability roadmap residual (`@project-manager`)

| Residual | Action |
|---|---|
| `R-V178P0-QC3-003` (POST /memory/review synchronous whole-queue — no bound/timeout/cancellation/concurrency) | Keep `lifecycle: open`; update `target` to "reliability roadmap (future iteration)"; add a deferred-features tracker row under a new "Reliability roadmap" grouping so it is not silently dropped. |

### T4 — Deferred items from this iteration (`@project-manager`)

Record into `deferred-features-cross-version-tracker.md`:
- Independent SOUL growth-curve view (D stretch, folded into temporal drift for V1.79).
- Standalone maturation dashboard (C stretch).
- Persisted reading progress; reading annotations/highlights; profile-specific reading chrome.
- Per-World SOUL filtering; realtime SOUL viz; SOUL personality-score narrative.

### T5 — Compound round (`@project-manager` triggers `mstar-compound`)

Crystallize any non-trivial lessons from V1.79 (e.g., read-only-consumption-surface pattern; viz-on-thin-DTO pattern; dual-track UI file-disjoint discipline). Skip-with-reason if nothing meets the Q1–Q8 bar; record in compass `## Compound Round Summary`.

### T6 — Profile B compaction (`@project-manager`)

Per `.mstar/AGENTS.md` Profile B layout invariant:
1. For each `Done` plan in `status.json.plans[]`: write `.mstar/archived/plans/<plan-id>.json`; append `"<plan-id>"` string to `archived/plans-done.json` `plans[]`.
2. Remove Done plans from `status.json.plans[]` (only non-Done remain — expect 0 after V1.79 close).
3. `iteration_summaries[V1.79]` snapshot.
4. Verify layout invariant (`python3 -c` check).

### T7 — Tracker + index refresh (`@project-manager`)

- `deferred-features-cross-version-tracker.md` quick-status line → "V1.79 delivered".
- `iterations/README.md` index: V1.79 row Status → `completed`.
- `metadata.latest_ship` in `status.json` → V1.79 entry.
- `metadata.latest_active_iteration` → next placeholder.
- `STRATEGY.md` Decision Log: add V1.79 row (Author Reflection + DF-49 cancellation).

### T8 — PR

Open PR `iteration/v1.79` → `main`. Wait for all CI checks (default +1 hour budget). Merge per repo convention (squash unless regression hotfix).

## Branch policy

- Working branch: `feature/v1.79-closure` (from `iteration/v1.79` after P0 + P1 merge).
- Merge target: `iteration/v1.79`.
- PR: `iteration/v1.79` → `main`.

## Acceptance (DoD — see compass §6)

1. `R-V178P0-QC3-001` `lifecycle: resolved` with `resolution.commit` + `resolution.plan_id`.
2. DF-49 row removed from active tracker; appended to cancelled archive with rationale.
3. `R-V178P0-QC3-003` target updated to reliability-roadmap; deferred-features tracker has the reliability grouping.
4. All V1.79 deferred items recorded in tracker.
5. Compound round done (docs created OR "无可结晶知识" with reason in compass).
6. Profile B compaction verified (layout invariant check passes).
7. Trackers + indexes + `metadata.latest_ship` + STRATEGY Decision Log updated.
8. PR merged to `main`; integration branch `iteration/v1.79` retired.

## Wire contracts

`wire_contracts_changed: FALSE` at P-last (the wire change landed in P1; P-last is closure only).

## Roadmap

Closure batch. Next iteration = V1.80 candidate evaluation at next `/iteration-start`.
