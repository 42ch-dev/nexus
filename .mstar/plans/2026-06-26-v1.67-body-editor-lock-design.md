# Plan — V1.67 P-design: Body-Editor Lock Draft Spec (Prepare-only)

**plan_id**: `2026-06-26-v1.67-body-editor-lock-design`
**Iteration**: V1.67 · **Wave**: 0 (Prepare) · **Track**: E · **Effort**: M
**Owner**: `@architect` (lock protocol + conflict policy + capability/wire implications) + `@product-manager` (editor UX + user stories)
**Status**: Todo
**Depends on**: P-1 (compass)
**Working branch**: `feature/v1.67-body-editor-lock-design`
**Compass**: [v1.67-…-delivery-compass-v1.md](../iterations/v1.67-local-api-surface-convergence-and-derisk-delivery-compass-v1.md) §1.1 Track E

## Goal

Author a **Draft** `body-editor.md` spec that locks the design decisions making the V1.68 body editor risky, so V1.68 implement begins with a reviewed design. **No runtime artifact** — spec-only. This is the V1.67 de-risk deliverable for V1.68.

## Residual coverage

None closed (design-only). Resolves the spec deferrals at `daemon-runtime.md` §161, `chapter-content-local-api.md` §20/§230, `local-api-surface-conventions.md` §238 (all currently defer the lock design to "the body-editor iteration").

## Acceptance criteria

- AC1: `.mstar/knowledge/specs/body-editor.md` authored as **Draft**, covering: per-chapter edit lock protocol (acquire/release/holder-identity), **lock-expiry policy** (heartbeat interval + zombie reclamation, mirroring V1.51 `Works/<work_ref>/.lock` advisory-lock precedent), MD ↔ rich-text lossless round-trip contract, frontmatter/status sync direction + trigger, **conflict policy vs orchestration co-writer** (`host_tool_handlers.rs` `body_path` writes — who wins, residual race window, recovery).
- AC2: `daemon-runtime.md` §161 deferral resolved (design lands V1.67; runtime hook implement V1.68).
- AC3: `chapter-content-local-api.md` §20/§230 deferrals resolved (body read-only reason + soft-concurrency rule reference the V1.67 design).
- AC4: Lock protocol feasibility verified against the actual `host_tool_handlers.rs` body write path (read the code; confirm the lock can gate it without a re-architecture).
- AC5: `@product-manager` user stories for the edit/lock/conflict UX integrated.
- AC6: Spec stays **Draft** (V1.68 flips Draft → Shipped on implement).

## Tasks (skeleton)

- T1: `@architect` reads `host_tool_handlers.rs` body write path + V1.51 `.lock` precedent; drafts lock protocol + conflict policy.
- T2: `@architect` drafts MD round-trip + frontmatter sync contracts.
- T3: `@product-manager` drafts editor UX + user stories.
- T4: Integrate → `body-editor.md` Draft + amend `daemon-runtime.md` + `chapter-content-local-api.md`.
- T5: QC single-review (spec-only; QC may be skipped with explicit reason per PM routing — confirm at dispatch).

## Out of scope

Any implement of the body editor, lock, or runtime hook (V1.68). UI productivity wave (V1.68). Lock *acquisition* UI beyond the user-story level.

## Dependencies / branch policy

Topic branch `feature/v1.67-body-editor-lock-design` from `iteration/v1.67`. Prepare-phase; runs in the Phase 2b window. Merge target `iteration/v1.67`.
