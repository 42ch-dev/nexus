---
plan_id: 2026-06-23-v1.61-kb-structured-layer
title: V1.61 P1 — KB Structured Layer
iteration: V1.61
slot: P1
status: Todo
effort: M
owner: "@fullstack-dev-2"
review: Tri-review + mid-QA
wave: 2 (parallel with P2)
dependency: P0 merged to integration
---

# P1: KB Structured Layer

Extend `nexus-kb` + `nexus-local-db` for structured compute data:
`state` field, `computable` flag, validation mode, query filter.

## Tasks

- [ ] T1: Add `state` + `computable` to `KeyBlockBody` in `key_block.rs`
- [ ] T2: Add `ValidationMode::Structured` + computable KeyBlock validation
- [ ] T3: Add `KbQuery::with_computable(bool)` filter
- [ ] T4: Update `InMemoryKbStore` query filter
- [ ] T5: Update `SqliteKbStore` query — `json_extract(body_json, '$.computable')`
- [ ] T6: Update ~15 `KeyBlockBody` construction sites
- [ ] T7: Tests — store query + state roundtrip + structured validation

## Done when

- `cargo test -p nexus-kb` passes
- `cargo test -p nexus-local-db` passes
- No clippy warnings
