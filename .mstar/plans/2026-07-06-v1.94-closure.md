# 2026-07-06-v1.94-closure (P-last)

**Iteration**: V1.94 · **Type**: closure · **Owner**: project-manager
**Status**: Todo · **Plan ID**: `2026-07-06-v1.94-closure`
**Primary spec**: none (closure-only)
**Working branch (planned)**: `feature/v1.94-closure` from `iteration/v1.94`
**Merge target**: `iteration/v1.94`

> Source compass: [v1.94/delivery-compass.md](../iterations/v1.94/delivery-compass.md) §2 SP-4.

## Specify

### Problem

V1.94 P-1 + P0 + P1 land topic branches onto `iteration/v1.94`; the
iteration needs QC tri-review on the integrated HEAD, QA, compound,
Profile B compaction (V1.93 cleanup if not already done at
iteration-start), and PR delivery to `main`.

### User value

The author's first-impression feedback is shipped coherently as one
iteration; QC gate + QA verify the wizard + IA + button contrast end-to-
end; knowledge crystallises for future first-launch work.

### Scope / Non-Goals

**In**:

- QC tri-review on the integrated `iteration/v1.94` HEAD (qc1 IA +
  structure lens on sidebar rewrite + Strategies unification; qc2
  security lens on the PATH scan + `agent-host/scan` endpoint; qc3
  reliability lens on the per-launch daemon gate + crash-banner logic).
- QA verifies: wizard E2E (first launch + returning launch + crash
  recovery); sidebar tab switch; footer profile switch; button contrast
  in both themes; strategies list+detail.
- compound round (Phase 3 §3.2): crystallise V1.94 lessons (likely
  candidates: the dark-bg → light-text button-invariant lesson; the
  per-launch daemon-ready gate pattern; the two-tab IA as a response to
  "too many items"; the additive agent-scan API as the ACP-detection
  pattern).
- Profile B compaction: V1.93's 4 Done plans (still in `plans[]` at
  iteration-start due to bookkeeping drift) archived to
  `archived/plans/` + `plans-done.json` index; V1.94 plans archived at
  iteration-close per Profile B.
- `metadata.latest_ship` updated to V1.94; `latest_active_iteration` →
  V1.94 (was V1.93 at iteration-start).
- deferred-features-cross-version-tracker refresh (V1.94 status row).
- STRATEGY.md Decision Log: V1.94 row (Desktop App Onboarding & IA Pass).
- PR to `main` (Phase 4) + merge-ready loop (Phase 5).

**Out**:

- Any new implement work (P0/P1 own that).
- R-V192SEC-001, R-V193PL-001, R-V191P1-005 — explicitly stay open and
  re-queued (deferred-tracker + status.json `residual_findings` unchanged).

### DoD (Draft)

- QC tri-review consolidated Approve (qc1 + qc2 + qc3); fix-waves land
  targeted re-reviews. **qc1 must explicitly verify the §5 strengthened
  criteria** (wizard agent-detection UX states, daemon-gate failure
  surfaces, Strategies preservation verbatim, footer keyboard + single-
  creator behaviour, browser-build contract).
- QA Pass on the wizard E2E (first-launch full 4-step + returning launch
  gate + crash-during-gate recovery) + sidebar tabs + footer switcher
  (incl. keyboard + single-creator) + button contrast (light+dark) +
  strategies list+detail preservation.
- compound Phase 6: new knowledge docs registered in
  `knowledge/README.md`; CONCEPTS.md entries if any.
- compass frontmatter `status: completed` + `end_date`.
- PR to `main` mergeable; required CI green; reviews resolved.

## Clarify

**Closed decisions**: standard closure plan per V1.92/V1.93 pattern.
Profile B compaction includes V1.93 cleanup (bookkeeping drift fix).

**Open assumptions**: the fix-wave count depends on QC findings;
targeted re-review per `mstar-review-qc` unless `full tri-review` is
declared.

## Plan

### Target state

`iteration/v1.94` is ready to merge to `main`; V1.94 compass is
`status: completed`; status.json + plans-done.json + tracker are
coherent; STRATEGY.md Decision Log records V1.94.

### Module boundaries / interface contracts

- Closure touches: compass, README.md, plans-done.json, archived/plans/,
  status.json, knowledge/, STRATEGY.md, CONCEPTS.md (if needed).
- No code change in this plan (fix-waves are owned by the implement
  plans' owners via targeted dispatch).

### Risks / rollback

- Fix-wave scope creep (sliding into new feature work): mitigation —
  closure Assignment scopes fixes tightly per QC finding.
- Profile B compaction archiving the wrong plans: mitigation — verify
  each archived plan row is `Done` before moving.

### Verification

- QC reports under `plans/2026-07-06-v1.94-closure.md +
  `qc-consolidated.md`.
- QA report under the same folder.
- compound docs registered.
- PR mergeable.

### Phase Gate Checklist

- [x] `specify` — above.
- [x] `clarify` — above.
- [x] `plan` — above.
- [ ] `tasks` — to fill at Execute (after P0 + P1 merge to integration).
- [ ] `implement` — P-last execute after P0 + P1 land.

### Tasks (to fill at Execute)

1. Dispatch QC tri-review on integrated HEAD (qc1 IA/structure; qc2
   security on agent-scan + PATH; qc3 reliability on daemon gate + banner).
2. Triage QC findings; targeted re-review per `mstar-review-qc`.
3. Dispatch QA (wizard + IA + button E2E).
4. compound round (Phase 3 §3.2).
5. Profile B compaction (archive V1.93 + V1.94 plans; plans-done.json
   index; status.json cleanup; latest_ship → V1.94).
6. deferred-features-cross-version-tracker refresh.
7. STRATEGY.md Decision Log row.
8. compass frontmatter `status: completed` + `end_date`.
9. PR to `main` (Phase 4) + merge-ready loop (Phase 5).

### Out of scope ( compass-level Non-Goals inherited )

- All compass §6 Non-Goals apply.
