# Plan — V1.73 P0 Canvas World KB β (Track A, lead)

**plan_id**: `2026-06-28-v1.73-canvas-world-kb-beta`
**Iteration**: V1.73 — [Compass](../iterations/v1.73-canvas-world-kb-beta-and-outline-hardening-compass-v1.md)
**Status**: Todo
**Owner**: `@frontend-dev` (canvas UI) + `@fullstack-dev` (backend + Local API + schemas + tests)
**Effort**: L
**Track**: A (lead)
**Branch policy**: `Working branch: create feature/v1.73-canvas-world-kb-beta from iteration/v1.73`; worktree `.worktrees/v1.73-world-kb/`; merge target `iteration/v1.73`.
**Depends on**: P-1 (compass Locked + architect Phase 2b storage/OCC mechanism lock)
**wire_contracts_changed**: TRUE (`@42ch/nexus-contracts` 0.8.0 → 0.9.0, additive World KB DTOs)
**Superpowers**: `using-git-worktrees` (parallel with P1 on same repo)

## Goal

Implement the Canvas World KB β slice per [canvas-strategy-surface.md §3.3 surface 3](../knowledge/specs/canvas-strategy-surface.md) and the V1.71/V1.72 patch-route/per-row-conflict pattern. Promote World KB canvas surface Draft → **Shipped β**. Full scope: compass §1.1 A1–A9.

## Architect Phase 2b locks (precondition for implement dispatch)

- **A2 storage/OCC mechanism**: per-row OCC candidate (reuse V1.51 `kb_extract_jobs.version` CAS; add `version INTEGER NOT NULL DEFAULT 1` to `kb_key_blocks` via migration if absent). No world-level single revision counter. Conflict modal fires per-entity. Architect confirms.
- **A3 route signatures**: architect confirms exact path shape (`POST /v1/local/worlds/{world_id}/kb/patch-entity` + `POST /v1/local/worlds/{world_id}/kb/promote-candidate` + read `GET /v1/local/worlds/{world_id}/kb/graph` + `GET /v1/local/worlds/{world_id}/kb/candidates`). DTO field names locked.
- **DESIGN.md token names**: architect locks the World KB canvas-write token-name interface (entity-card fills by BlockType, promotion-state colors pending/confirmed/rejected/merged, source-anchor edge, computable-state badge); values filled during implement.

## Tasks (expanded at implement dispatch)

- [ ] T1 — A1 schemas + codegen: `schemas/local-api/world-kb/` DTOs (`WorldKbPatchEntityRequest`, `WorldKbPromoteCandidateRequest`, `WorldKbPatchResponse`, `WorldKbConflictError`, `WorldKbValidationError`, read projection `WorldKbGraphProjection`); `pnpm run codegen`; verify deterministic artifacts; `@42ch/nexus-contracts` 0.8.0 → 0.9.0.
- [ ] T2 — A2 backend prerequisite: promote World KB read+write from CLI-direct-DB to Local API routes. Refactor `apps/nexus42/src/commands/creator/world/kb.rs` to call new Local API client (thin shell) — behavior-preserving. Migration: add `version` column to `kb_key_blocks` if architect confirms (per-row OCC).
- [ ] T3 — A3 Local API routes: 2 patch routes (`patch-entity`, `promote-candidate`) + 2 read endpoints (`graph`, `candidates`) following V1.64 cursor + ErrorResponse conventions + V1.71/V1.72 patch-route convention.
- [ ] T4 — A4 daemon validation + atomic persistence: `crates/nexus-daemon-runtime/src/handlers/world_kb.rs` + `src/world_kb/{validation,persistence}.rs`. 5-rule validation (ID existence, authorization, promotion transition validity, entity edit validity, version precondition OCC). Atomic DB transaction (V1.50 `insert_key_block_in_tx` + `mark_confirmed_in_tx` pattern for promote; V1.51 per-row CAS UPDATE…WHERE version = ? for entity edit).
- [ ] T5 — A5 TS client methods: `worldKbPatchEntity`, `worldKbPromoteCandidate` in `apps/web/src/api/`; refetch canonical graph after patch; expose `lastKnownVersion` per-entity.
- [ ] T6 — A6 canvas UI: `apps/web/src/features/canvas/world-kb/` — graph projection (entities + promotion states + source anchors + computable badges) + entity inspector + promotion inspector + relationship edges (read-only, derived from `kb_source_anchors`) + conflict modal (KB-flavored copy) + non-spatial alternate view (sortable entity list) + DESIGN.md World KB write-token values.
- [ ] T7 — A7 Tauri WKWebView parity smoke: entity inspector + promotion inspector + conflict modal parity in browser + Tauri shell.
- [ ] T8 — A8 accessibility: ARIA live-region conflict announcement, focus trap, keyboard paths for all actions, `prefers-reduced-motion`, non-spatial alternate view reachable/equivalent.
- [ ] T9 — A9 tests: unit (validation + OCC + conflict + promotion transitions), integration (2 routes via NexusClient mock + canonical daemon), e2e (canvas adopt → daemon promote → refetch; canvas edit → daemon save → refetch), regression (concurrent daemon + canvas writes → 409). `crates/nexus-daemon-runtime/tests/world_kb_patch.rs` + `apps/web/src/features/canvas/world-kb/__tests__/`.

## Acceptance (maps to compass §7 DoD #1–#4, #7, #8)

- World KB surface Shipped β in `canvas-strategy-surface.md` §3.3 surface 3 + §3.4 contracts schema-backed.
- 2 patch routes + 2 read endpoints shipped; per-row OCC conflict detection.
- Canvas UI ships (graph projection + inspectors + conflict modal + non-spatial alternate view).
- `@42ch/nexus-contracts` 0.9.0; codegen deterministic; CI `verify-codegen` green.
- WKWebView parity + accessibility.
- CLI `creator world kb` behavior unchanged (thin-shell regression snapshot tests pass).

## Files touched

`schemas/local-api/world-kb/**` (NEW), `crates/nexus-contracts/src/generated/world-kb/**` (NEW; regenerated), `apps/web/src/api-types/world-kb/**` (NEW; regenerated), `apps/web/src/api/world-kb-patch.ts` (NEW), `apps/web/src/features/canvas/world-kb/**` (NEW), `apps/web/DESIGN.md`, `apps/web/DESIGN.dark.md`, `crates/nexus-daemon-runtime/src/handlers/world_kb.rs` (NEW), `crates/nexus-daemon-runtime/src/world_kb/` (NEW), `crates/nexus-daemon-runtime/tests/world_kb_patch.rs` (NEW), `apps/web/src/features/canvas/world-kb/__tests__/**` (NEW), `apps/nexus42/src/commands/creator/world/kb.rs` (refactor to Local API client thin shell), `crates/nexus-local-db/migrations/<new>_kb_key_blocks_version.sql` (NEW; if architect confirms).

## Notes

- **Single-stream within P0**: sequential internally (schemas → backend prerequisite → daemon → NexusClient → UI → tests). `@frontend-dev` leads UI; `@fullstack-dev` owns backend. `single_stream_justified: yes`.
- **File-disjoint from P1**: P0 owns new `world-kb/**`; P1 owns existing `outline/**` split. P0 imports outline conflict-modal reuse through P1's public facade only until B5 merges.
- **Relationships deferred**: V1.73 renders derived reference/source-anchor edges read-only; `world_kb.patch_relationship` + `kb_relationships` table deferred to V1.74 (`tbd-v1.74-world-kb-relationships`).
