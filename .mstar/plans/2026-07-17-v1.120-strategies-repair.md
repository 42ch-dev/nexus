# P0 — Strategies repair

> **Status:** writing-specialist §5.3 reviewed — awaiting PM §5.4 lock.

| Field | Value |
|-------|-------|
| plan_id | 2026-07-17-v1.120-strategies-repair |
| iteration_refs | V1.120 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.120/specs/strategies-repair.md` |
| iteration_compass | `.mstar/iterations/v1.120/delivery-compass.md` |
| spec_integration_branch | iteration/v1.120 |
| merge_target | iteration/v1.120 |
| working_branch | plan/v1.120-p0-strategies-repair |
| wire_contracts_changed | false |

**Goal:** Make Strategies list/detail usable: fix detail crash, Back navigation, filter `_system.*`, per-row Validate, delete user presets.

**Depends on:** —

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors | Open, validate, and delete Strategies without dead-end errors or `_system.*` clutter |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Product)

### Specify

- **Problem:** F5 — canvas `ErrorState` on `getPreset` failure (`common.error.title` / “无法加载此视图”), no Back, `_system.*` in System presets, global Validate, no Delete for user presets.
- **Scope:** `/strategies`, `/strategies/:presetId`, row actions; reuse existing preset APIs.
- **Non-goals:** Canvas redesign; system/embedded delete; new schemas.
- **Draft DoD:** AC-P0-1…6 in primary spec.

### Clarify (locked via grill + PM §5.1)

- Filter `_system.*` from **System presets** section only (ids matching `_system.` prefix).
- Validate = per-row action; **remove** global header Validate button and `ValidatePresetDialog` header entry.
- Delete = user presets only + confirm dialog before `deletePreset`.
- Detail must provide **Back** to `/strategies` on not-found and canvas load-error; primary fix is `locate_preset` (AD-P0-1b), not catch-only UX.
- `wire_contracts_changed: false`.
- Copy: Back = verb-only `Back` (`strategies.strategyDetail.back`); Delete confirm title names preset; canvas load error may show `common.error.title` but **Back** must remain visible.

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs:** `strategies-page.tsx`, `strategy-page.tsx`, `StrategyCanvas`, `preset_management.rs` (`locate_preset`, `get_preset`), `ErrorState` → `common.error.title`.
- **Gate Decision:** go
- **Resolved (AD-P0-1):** Crash title is canvas load `ErrorState`, not route `errorElement`. Primary bug: `locate_preset` path mismatch for qualified `_system.*` ids. Lazy chunk retained.

### Plan

- **Selected:** Daemon `locate_preset` fix + web list/detail UX + row actions. No schema change.
- **Long-term:** Strategies list is author-facing preset manager; detail loads canvas via `getPreset` → `parsePresetYaml` → adapter projection.
- **Module boundaries:**

  | Module | Change |
  | --- | --- |
  | `crates/nexus-daemon-runtime/.../preset_management.rs` | Fix `locate_preset` for `_system.*` qualified ids (AD-P0-1b) |
  | `apps/web/src/pages/strategies-page.tsx` | `_system.*` filter; per-row Validate/Delete; remove header Validate |
  | `apps/web/src/pages/strategy-page.tsx` | Back on not-found; pass through / surface canvas load error with Back |
  | `apps/web/src/components/canvas/strategy-canvas.tsx` | Optional: custom error title + Back affordance on load error |
  | locales / tests | AC-P0-1…6 |

- **API:** Existing `GET/DELETE/validate` preset routes unchanged (`wire_contracts_changed: false`).
- **Interfaces for implementers:**
  - `locate_preset`: system branch must resolve `presets/_system/<dir>/` where `<dir>` = strip `_system.` prefix from qualified id.
  - Back: `Button` + `useNavigate('/strategies')`; i18n `strategies.strategyDetail.back`.
  - Row Validate: calls existing `validatePreset` / dialog pattern per row.
  - Row Delete (user only): confirm → `deletePreset` → invalidate `queryKeys.presets`.
- **Risks:** Render throw after successful load — T1 decides canvas error boundary (AD-P0-1e).
- **Validation:** Handler unit test for `get_preset("_system.maintenance")`; component tests per AC-P0-6.
- **Effort:** M — 2–3 SDD tasks.

## Tasks (refined)

1. **T1 — Repro + `locate_preset` fix:** Run AD-P0-1c matrix; fix system preset path resolution; add handler test; verify embedded + user `getPreset` 200.
2. **T2 — Detail UX:** Back on `strategy-page` not-found + canvas load error; optional canvas error boundary if T1 requires.
3. **T3 — List actions:** `_system.*` filter; remove header Validate; per-row Validate + user Delete confirm; tests AC-P0-3…5.

## Global Constraints

- No new wire DTOs; follow DESIGN.md Button/Select patterns; i18n keys for new copy.
