# V1.57 P2 — V1.56 Carry-Forwards & Wire Compliance

**plan_id**: `2026-06-22-v1.57-v156-carry-forwards-and-compliance`
**title**: V1.56 Carry-Forwards & Wire Compliance
**iteration**: V1.57
**track**: Track A
**wave**: Wave 2 (after Wave 1 accepts)
**effort**: M
**owner**: `@fullstack-dev` (Track A canonical owner)
**status**: Todo
**created**: 2026-06-22
**depends_on**: P0 + P1 merged to `iteration/v1.57`
**primary_spec**: `acp-capability-set.md` (Master — post-roster rename compliance)
**spec_refs**: `capability-registry.md`, `agent-nexus-tool-bridge.md`
**branch_policy**: Working branch `feature/v1.57-v156-carry-forwards-and-compliance` created from `iteration/v1.57` after P0+P1 merged; merges back to `iteration/v1.57`.
**review_strategy**: Merge to integration → tri-review → mid-QA → targeted fix-wave.

---

## 1. Scope

Absorb the in-scope V1.56 carry-forwards: `R-V156P1-M001` (schema rename: capability_count) and low-severity medium residuals that are safe to fold post-registry-consolidation. Verify wire surface stability.

### In scope

- **`R-V156P1-M001`**: Mechanical rename of `capability_count` schema field to a more descriptive name (per the residual finding's recommendation).
- **Wire surface stability verification**: Post-rename, verify that CLI/agent surfaces using the renamed field remain compatible (old field name → deprecation shim or migration).
- **Low-severity V1.56 medium residuals**: Document which are absorbed and which stay deferred. Non-capability-surface residuals (workspace OCC, path canonicalize) are explicitly excluded.
- **Reproducer test**: Schema rename has a hermetic reproducer test proving the rename does not break existing consumers.

### Out of scope

- `R-V156P0-M001` (sha2 dep) — OUT; workspace OCC concern, deferred to V1.58+.
- `R-V156P0-M002` (path canonicalize) — OUT; workspace OCC concern, deferred to V1.58+.
- Other V1.56 P0 medium residuals (M003..M006) — OUT unless specifically enumerated as in-scope by PM during dispatch.
- New capability implementations, registry changes, or god-file refactors (P0/P1 own those).

---

## 2. Acceptance Criteria

- [ ] `R-V156P1-M001` schema rename applied: `capability_count` → new name per residual recommendation.
- [ ] Reproducer test exists: `test_capability_count_rename_does_not_break_consumers`.
- [ ] CLI surface: `nexus42 daemon status` (or whichever command surfaces the field) returns the new field name.
- [ ] Agent surface: agent tool bridge output using the field reflects the rename (deprecation shim if needed).
- [ ] V1.56 in-scope low-severity residuals documented as "absorbed in P2" in plan completion report.
- [ ] V1.56 out-of-scope residuals explicitly enumerated in plan completion report as "deferred to V1.58+".
- [ ] `cargo test -p nexus-contracts` passes (schema rename lives in contracts crate).
- [ ] `cargo test -p nexus42` passes (CLI surface).
- [ ] `cargo test -p nexus-daemon-runtime` passes (agent surface).
- [ ] `cargo clippy -p nexus-contracts -p nexus42 -p nexus-daemon-runtime -- -D warnings` passes.

---

## 3. Tasks

- [ ] **T1**: Apply `R-V156P1-M001` schema rename.
  - Locate the `capability_count` field in schema/contract definitions.
  - Rename to the recommended name per the residual finding.
  - Add a deprecation alias or JSON `#[serde(alias)]` shim for backward compatibility if the old name is persisted.

- [ ] **T2**: Run `pnpm run codegen` if schema files changed.
  - Verify regenerated output is committed.

- [ ] **T3**: Write reproducer test.
  - Test that the new field name is produced correctly.
  - Test that old-field-name consumers (if any) are handled by the deprecation shim.
  - Test file: `crates/nexus-contracts/tests/schema_rename_compliance.rs`.

- [ ] **T4**: Verify CLI and agent surface.
  - `nexus42 daemon status` (or equivalent) shows the renamed field.
  - Agent tool bridge output (e.g., `nexus.context.whoami` response) reflects the rename.

- [ ] **T5**: Enumerate residual absorption.
  - In-scope: `R-V156P1-M001` (absorbed), plus any other low-severity residuals PM adds during dispatch.
  - Out-of-scope: `R-V156P0-M001`, `R-V156P0-M002`, and other non-capability-surface residuals enumerated.

- [ ] **T6**: Full verify.
  - `cargo test -p nexus-contracts`
  - `cargo test -p nexus42`
  - `cargo test -p nexus-daemon-runtime`
  - `cargo clippy -p nexus-contracts -p nexus42 -p nexus-daemon-runtime -- -D warnings`
  - `cargo +nightly fmt --all -- --check`

---

## 4. Branch policy

- Working branch: `feature/v1.57-v156-carry-forwards-and-compliance` created from `iteration/v1.57` after P0+P1 merged.
- Merge target: `iteration/v1.57`.

---

## 5. Verification

```bash
# Schema rename reproducer test
cargo test -p nexus-contracts -- test_capability_count_rename

# CLI surface verification
cargo test -p nexus42

# Agent surface verification
cargo test -p nexus-daemon-runtime

# Full clippy for touched crates
cargo clippy -p nexus-contracts -p nexus42 -p nexus-daemon-runtime -- -D warnings

# Codegen consistency check (if schema changed)
pnpm run codegen
git diff --stat
# Expected: no uncommitted generated changes
```

---

## 6. Cross-cuts

| Carry-forward | How absorbed |
| --- | --- |
| `R-V156P1-M001` (schema rename: capability_count) | T1–T4: Mechanical rename + deprecation shim + reproducer test. |

**Explicitly NOT absorbed** (documented in plan completion report):
- `R-V156P0-M001` (sha2 dep) — workspace OCC, V1.58+.
- `R-V156P0-M002` (path canonicalize) — workspace OCC, V1.58+.

---

## 7. Open questions

None at prepare time. P2 implementer should return `Blocked` to PM if:
- The schema rename touches a persistence surface (DB column, JSON field in state.db) that has no migration path.
- The rename breaks a codegen invariant (e.g., schema JSON field name must match Rust struct field name exactly without serde aliasing).
- PM adds additional in-scope residuals that conflict with P0/P1 registry or adapter work.
