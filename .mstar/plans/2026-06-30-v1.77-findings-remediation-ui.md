# 2026-06-30-v1.77-findings-remediation-ui (P0)

**Iteration**: V1.77 · **Plan**: P0 (Track A lead) · **Status**: Todo · **Owner**: `@frontend-dev` + `@fullstack-dev`
**Compass**: [v1.77-…-compass-v1.md §1.1 Track A (A1–A6)](../iterations/v1.77-findings-remediation-ui-and-post-canvas-inflection-compass-v1.md)
**Depends on**: P-1 (compass Locked + Phase 2b architect lock)

## Goal

Promote findings from a read-only Control-Room view (since V1.64) to a full **remediation authoring surface**: status transitions (6-state lifecycle), `target_executor` assignment, and inline edit — consuming the PATCH route the backend already ships. Close the "observe → triage → resolve" quality loop in the UI.

## Scope (A1–A6 — see compass §1.1 for authoritative detail)

- **A1 — Type imports (codegen already done — D2 LOCKED)**: `UpdateFindingRequest` and `CreateFindingRequest` are **already** generated and barrel-exported by `@42ch/nexus-contracts` (glob-based codegen auto-discovers all schemas under `schemas/`; confirmed Phase 2b D2). No codegen config change. The web app extends `apps/web/src/lib/nexus/types.ts` to surface these types, or imports them directly from `@42ch/nexus-contracts`.
- **A2 — `NexusClient` promotion**: `getFinding` + `updateFinding` on the interface + `BrowserClient` + `TauriClient`. TanStack `useUpdateFinding` mutation (optimistic + invalidation of findings list + stale-count).
- **A3 — Findings page remediation surface**: status-transition affordance (6-state adjacency enforced), `target_executor` selector, inline edit in a detail/inspector. Stale-count refreshes on mutation.
- **A4 — DESIGN.md tokens**: 6 findings status badges + severity colors (reuse `severityVariant`) + triage chrome. Token names preserved verbatim.
- **A5 — Tests**: `updateFinding` client test; status-transition adjacency; optimistic update + invalidation; detail render; assignment; inline edit; adapter-contract parity.
- **A6 — Specs**: `web-ui.md` V1.77 stage; `findings-lifecycle.md` promote from stub; `local-api-surface-conventions.md` findings PATCH reference.

## Open design questions — ALL RESOLVED (Phase 2b LOCKED, 2026-06-30)

1. **OCC/last-writer-wins** → **RESOLVED (D1b)**: NO OCC, last-writer-wins. No `revision` column; simple `WHERE creator_id = ? AND finding_id = ?` UPDATE. No conflict modal needed — single-author-triage.
2. **Status-transition guard location** → **RESOLVED (D1a)**: SERVER-SIDE ENFORCED. DAO enforces 6-state adjacency (`is_valid_transition()` at `findings.rs:172`); handler returns HTTP 422 `INVALID_TRANSITION`. UI disables invalid transitions as defense-in-depth.
3. **`createFinding`/`deleteFinding`** → **RESOLVED (D1c)**: V1.77 is UPDATE-ONLY. `createFinding`/`deleteFinding` remain CLI/producer-only; the web app can add them in a follow-up.
4. **UX layout** → **RESOLVED (D4)**: Detail-panel + row-action hybrid. Existing `Table` + `StatusBadge` reused. Row-level actions (status dropdown, assignment selector) + detail/inspector panel.

## Branch policy

- Working branch: `feature/v1.77-findings-remediation` from `iteration/v1.77`
- Merge target: `iteration/v1.77`
- Worktree: `.worktrees/v1.77-findings/` (parallel with P1)

## Files touched (indicative — Phase 2b D2 LOCKED: no codegen changes)

- `apps/web/src/lib/nexus/{types,browser-client,tauri-client}.ts` + `adapter-contract.test.ts`
- `apps/web/src/api/queries.ts` (`useUpdateFinding`)
- `apps/web/src/pages/findings-page.tsx`
- `apps/web/src/components/findings/**` (NEW — detail/inspector + triage, if split)
- `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md`
- `.mstar/knowledge/specs/{web-ui,findings-lifecycle,local-api-surface-conventions}.md`

## Acceptance (DoD — see compass §7 #1–#6)

1. Findings page supports 6-state transitions (invalid disabled).
2. `target_executor` assignment set/clear from UI.
3. Inline edit of title/description/severity/kind/rule_suggestion.
4. Stale-count refreshes on mutation.
5. Codegen wired; `verify-codegen` green.
6. Spec promotions done.

## Wire contracts

`wire_contracts_changed: FALSE` (Phase 2b D3 LOCKED). Schemas already on disk under `schemas/local-api/findings/` and fully codegen'd into both TypeScript and Rust. All generated types already barrel-exported by `@42ch/nexus-contracts` (version `0.12.0` stays). V1.77 only adds consumer-side imports — no schema change, no codegen config change, no contracts bump.
