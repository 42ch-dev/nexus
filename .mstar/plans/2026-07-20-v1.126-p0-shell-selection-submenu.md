# P0 — Sidebar selection-mode submenu shell

> **Iteration compass:** `.mstar/iterations/v1.126/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.126/specs/shell-selection-submenu.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Picking a World or Work in the Creator tab opens a contextual submenu with Timeline/KB/Outline entry links, agent assignment, and per-entity quick actions — closes V1.125 Non-Goal "Selection → submenu shell (World/Work selected mode + agent dialog)".

**Execution:** mstar-sdd (multi-task)

### Wire contracts (architect LOCKED — pending seat 2)

- **Verdict: `wire_contracts_changed: false`** (PM proposal — pending architect seat 2 confirmation). Submenu is frontend chrome; agent assignment reuses existing `setAgentProfile` IPC; rename/delete actions reuses existing `PATCH /v1/daemon/works/{id}` + World KB patch routes. No new wire shapes.

## Tasks

- [ ] **T1 (AC-V1126-1 — submenu trigger + dismiss) · Must:** Sidebar World row and Work row gain a contextual submenu trigger. **Trigger contract (locked — P0 spec ND-A1):** **click on row body navigates** (existing behavior preserved); **`Enter` on focused row opens submenu** (primary keyboard path — `Open Timeline` is item #1 so a keyboard user who meant to navigate just presses `Enter → Enter`); **`⌘.`/`Ctrl+.` on focused row opens submenu** (additive power-user chord; fallback `Shift+F10` if dogfood reports persistent chord conflict); **`•••` button at row-end opens submenu** (mouse + touch accessible). Submenu is a popover anchored to the row; **dismiss triggers:** outside-click, Esc, route change, `Tab` out of the submenu.
  - **Verify (author):** Keyboard-only author can navigate to a row, press `Enter` to open the submenu, arrow-key within it, and Esc to close without navigating. `sidebar.test.tsx` matrix updated: existing `Enter` = navigate assertions retargeted to `Enter` = open submenu (do not split this test update out of T1 — locked at architect seat 2 ND-A1).
  - Files: `apps/web/src/components/layout/sidebar.tsx`, `apps/web/src/components/layout/presentational/shell-sidebar-chrome.tsx`, `apps/web/src/components/selection-submenu/selection-submenu.tsx`, `apps/web/src/components/layout/selection-submenu.test.tsx`, `sidebar.test.tsx`
  - **Architecture locks:**
    - `Enter` primary trigger; `⌘.`/`Ctrl+.` additive power-user chord (P0 spec ND-A1)
    - Sidebar integration: `ShellSidebarChrome` gains `renderSubmenu?: (item: ShellNavItem) => ReactNode` prop; focus-trap lifecycle in `selection-submenu.tsx` (P0 spec ND-A3)
    - Test matrix update in same task as Enter handler override — do not split (P0 spec ND-A1/AQ-2)
    - `wire_contracts_changed: false` — frontend chrome only (P0 spec ND-A5)
- [ ] **T2 (AC-V1126-1 — submenu contents) · Must:** Submenu shows (see P0 spec ND-3 locked list):
  1. **Open Timeline** (World: `/worlds/<id>/timeline`; Work: `/works/<id>/timeline`)
  2. **Open KB** (World only — `/worlds/<id>/kb`) **/ Open Outline** (Work only — `/works/<id>/outline`) — conditional copy + target in a single menu slot
  3. **Agent assignment** — shows currently-bound agent (if any) and a submenu-item that opens the existing agent picker (`AgentPicker`) in dialog mode; on apply, persists via existing `setAgentProfile` IPC and shows the agent key beside the World/Work label. **`AgentPicker` is reused as-is — no component edits** (NG-14).
   4. **Quick actions:** Rename (inline edit of label, persists via `PATCH /v1/daemon/works/{id}` for Work, World KB patch for World). **Delete is deferred — no DELETE route exists for works/worlds (ND-A5 violation).** Tracked in `R-V1126P0-T2-001` + V1.127+ roadmap.
   - **Verify (author):** Each item works end-to-end; rename persists; agent assignment reflects in `DaemonStatusBar` after apply (same invalidation keys as V1.125 P0 T2).
   - Files: `selection-submenu.tsx`, agent picker integration (`apps/web/src/components/agents/`), rename inline edit
   - **Architecture locks:**
     - `AgentPicker` reused as-is in dialog mode — no component file edits (P0 spec ND-A4, NG-14)
     - Agent assignment persists via existing `setAgentProfile` IPC (no new wire contract)
     - Rename: `PATCH /v1/daemon/works/{id}` (Work), World KB patch (World) — existing routes, no new routes
     - Delete: **deferred (V1.127+)** — no DELETE route exists for works/worlds (see `R-V1126P0-T2-001`)
- [ ] **T3 (AC-V1126-1 — a11y + i18n) · Must:** Submenu is fully keyboard reachable; ARIA roles (`menu`, `menuitem`, `menuitemcheckbox` for agent toggle); focus trap within submenu while open; visible focus ring per DESIGN.md. All visible strings via `t()` in the `shell` namespace (en + zh-CN). Voice & Content: verb-only actions (Open, Rename, Delete); entity name in dialog title.
   - **Verify (author):** Screen reader announces menu + items; tab order is logical; zh-CN strings present.
   - Files: `selection-submenu.tsx`, `apps/web/src/locales/{en,zh-CN}/shell.json` new keys
   - **Architecture locks:**
     - ARIA `menu`/`menuitem` roles on the extracted `SelectionSubmenu` presentational component (P0 spec ND-A2)
     - Focus trap within submenu while open; focus returns to row on Esc (P0 spec ND-A3)
     - Visible strings via `t()` in `shell` namespace; verb-only action labels per DESIGN.md §Voice
- [ ] **T4 (Studio-first — selection submenu fixture) · Should (V1.106 invariant — must land in-iteration):** Per root `AGENTS.md` UI Component Policy, add a Studio fixture rendering the selection submenu in light + dark, all variants (World with/without agent; Work with/without agent; rename in progress; delete confirm open). Presentational chrome **extracted** to `@web-shell/selection-submenu` (new alias root) per P0 spec ND-A2 (AQ-3 architect verdict). The Studio fixture consumes the extracted component as the second consumer (app → Studio), satisfying V1.106 ≥ 2 consumers rule.
   - **Verify:** `pnpm --filter design-studio dev` shows Selection Submenu page with all variants; smoke tests pass.
   - Files: `packages/nexus-ui/src/selection-submenu/selection-submenu.tsx` (extracted), `apps/design-studio/src/fixtures/selection-submenu-fixtures.tsx` (consumes extracted component), `apps/design-studio/src/pages/surfaces.tsx` (new section)
   - **Architecture locks:**
     - Extract presentational `SelectionSubmenu` to `@web-shell/selection-submenu` (P0 spec ND-A2)
     - App-owned: row data mapping, routing callbacks, i18n injection (stay in `apps/web`)
     - `wire_contracts_changed: false` — frontend chrome only (P0 spec ND-A5)

## Done when

- **AC-V1126-1:** Sidebar row selection opens submenu; submenu has Timeline/KB/Outline entry, agent assignment, rename, delete; keyboard + i18n + a11y complete; Studio fixture shipped.

## Roadmap

- **Single-iteration slice:** Submenu for World + Work rows in Creator tab. Submenu for Orchestrator rows (Memory, Strategies, Sessions, Schedule, Modules) is **out of scope** — those already have dedicated list/detail pages and would duplicate.
- **Future evolution:** if submenu grows past 6 items, refactor to a command-palette-driven contextual action model (roadmap V1.128+).

## Working branch

`feature/v1.126-p0-shell-selection-submenu` → merge `iteration/v1.126`
