# V1.37 Harness Docs Prepare — Locked Plan

**Plan ID**: `2026-06-07-v1.37-harness-docs-prepare`  
**Title**: Prepare — V1.37 compass, plan stubs, tracker/status sync  
**Created**: 2026-06-07  
**Status**: Done  
**Owner**: `@project-manager`  
**Agents**: `@project-manager`  
**Task category**: `docs`  
**Working branch**: `iteration/v1.37` (harness docs only; no product code)  
**Merge target**: `iteration/v1.37`  
**Iteration compass**: [v1.37-novel-writing-foundation-delivery-compass-v1.md](../iterations/v1.37-novel-writing-foundation-delivery-compass-v1.md)  
**Primary spec**: [novel-workflow-profile.md](../knowledge/specs/novel-workflow-profile.md)

---

## 1. Problem Statement

V1.36 shipped a single-chapter novel-writing UX slice, but V1.37 had no active compass or executable harness package. The deferred tracker and `status.json` identify several V1.36 residuals that directly affect ordinary users: init grill-me output is not fully routed into `preset.input`, `gates:` are declared but not enforced at runtime, and the scaffold DB update is not fully atomic. V1.37 needs a docs-only prepare wave that locks the foundation-first direction before any implementation dispatch.

---

## 2. Goals

1. Publish a V1.37 delivery compass with the locked foundation-first Novel UX north star.
2. Register implementation plan stubs for:
   - P0 foundation-first UX hardening.
   - P1 multi-chapter / multi-volume chronology roadmap.
   - P2 World KB continuity roadmap.
   - P3 quality-loop roadmap.
3. Update `status.json` to mark V1.37 active and point integration work at `iteration/v1.37`.
4. Update the deferred tracker and iterations README so V1.37 scope is discoverable.
5. Keep product implementation explicitly out of this session.

---

## 3. Non-goals

- Rust, schema, CLI, preset, or migration implementation.
- QC/QA dispatch for future implementation plans.
- Closing any deferred feature rows as shipped.
- Platform publish or platform unpause.

---

## 4. Grill decisions (locked)

| # | Decision |
| --- | --- |
| 1 | **North Star** = Novel Writing UX continuation |
| 2 | **First slice** = foundation-first: input wiring, runtime gates, scaffold atomicity, first-run remediation |
| 3 | **B roadmap** = multi-chapter / multi-volume chronology via DF-62 |
| 4 | **C roadmap** = World KB schema, binding validation, `creator world create`, and KB extraction via DF-63 |
| 5 | **D roadmap** = findings lifecycle, rules architecture, logs, and master-decision timeout via DF-64/65/66/67 |
| 6 | **Integration branch** = `iteration/v1.37` |
| 7 | **Execution** = no product-code implementation in this session; future plan rows stay `Todo` |

---

## 5. Tasks

- [x] T1: Confirm `nexus` branch state and create `iteration/v1.37` from clean `main`
- [x] T2: Write V1.37 delivery compass
- [x] T3: Create P0/P1/P2/P3 plan stubs
- [x] T4: Update `status.json` with V1.37 active metadata and plan rows
- [x] T5: Update deferred tracker and iterations README
- [x] T6: Verify JSON and targeted text consistency

---

## 6. Acceptance

1. Compass exists at [v1.37-novel-writing-foundation-delivery-compass-v1.md](../iterations/v1.37-novel-writing-foundation-delivery-compass-v1.md).
2. V1.37 plan stubs exist under `.mstar/plans/`.
3. `status.json` registers `iteration/v1.37` and V1.37 plan rows.
4. Deferred tracker says V1.37 is active and does not mark B/C/D deferred rows as shipped.
5. Only harness files changed.

---

## 7. Verification

```bash
python -m json.tool .mstar/status.json >/tmp/nexus-status.json
rg -n 'V1.37|v1\.37' .mstar/iterations .mstar/plans .mstar/knowledge/deferred-features-cross-version-tracker.md .mstar/status.json
git diff --name-only
```
