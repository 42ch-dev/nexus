# P1 — Shell & form polish

> **Status:** Done (2026-07-17) — merged to `iteration/v1.120` @ `5ee1a20b`. QC **Approve with residuals**; QA **Pass with residuals** (7/7 ACs + compass bullets).

## QA Gate Summary (durable)

- **QA verdict: Pass with residuals** — AC-P1-1…7 7/7 + compass Shell/Dirty-Save bullets; vitest 151/151, typecheck clean, build green (+3.2 kB CSS from restored package utilities); `wire_contracts_changed: false` verified empty.
- F-001 adjudication re-verified: Settings-path AC-P1-2 genuinely unaffected by wizard-path residual.
- Full report: `.mstar/sdd/2026-07-17-v1.120-shell-form-polish/review/qa.md` (ephemeral bundle).

## Review Gate Summary (durable)

- **SDD:** T1 `674bb361` (Agent Save dirty-gate + post-save invalidation; status bar → reactive `useAgentProfile()`) → T2 `8add61e9` (Select chevron root-fix via tailwind `content` scan; danger/disabled verified AA 4.90:1 / 6.53:1) → T3 `45d9bc7b` (Restart daemon a11y tests; impl satisfied AC-P1-6 since V1.112). L2 reviews 3/3 clean.
- **QC tri-review (2026-07-17, range `3e4bbca7...45d9bc7b`):** qc1 Approve w/ residuals (0C/1W/5S) · qc2 Approve (0C/0W/3S) · qc3 Approve (0C/0W/3S) → **consolidated: Approve with residuals**. qc1 F-001 (wizard-path invalidation asymmetry, Warning) PM-adjudicated residual per seat recommendation — outside locked module boundary, self-heals ≤15s; no fix wave.
- **Residuals:** 9 registered in `status.json` `residual_findings[2026-07-17-v1.120-shell-form-polish]` (2 low defer: F-001 wizard invalidation, S-003 DESIGN.disabled token cluster; 7 nit). Raw bundle: `.mstar/sdd/2026-07-17-v1.120-shell-form-polish/review/` (ephemeral).

| Field | Value |
|-------|-------|
| plan_id | 2026-07-17-v1.120-shell-form-polish |
| iteration_refs | V1.120 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.120/specs/shell-form-polish.md` |
| iteration_compass | `.mstar/iterations/v1.120/delivery-compass.md` |
| spec_integration_branch | iteration/v1.120 |
| merge_target | iteration/v1.120 |
| working_branch | plan/v1.120-p1-shell-form-polish |
| wire_contracts_changed | false |

**Goal:** Dirty-gate Save; refresh DaemonStatusBar after Agent Save; fix Select chevron artifact; danger Advanced CTA; dark disabled contrast; Restart tooltip.

**Depends on:** —

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors | Save reflects dirty state; footer updates immediately; controls are legible and clearly labeled |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Product)

### Specify

- **Problem:** F1 (footer stale + Save always on), F2 (Select / Advanced / disabled), F6 (Restart hint).
- **Scope:** Settings Agent, shared Button/Select, DaemonStatusBar, Settings Advanced re-run wizard.
- **Non-goals:** Full-app form audit; restart behavior change.
- **Draft DoD:** AC-P1-1…7.

### Clarify (locked — PM §5.1)

- **Dirty Save (iteration rule):** Agent Save disabled until dirty; baseline = last saved profile. Extend to any other persist CTA touched in P1 tasks.
- Footer refresh via invalidation/event after save — **not** poll-only.
- Advanced re-run wizard = `danger` variant.
- Restart `aria-label` + tooltip: explicit **daemon** restart — reuse `shell.daemon.restart`.
- Create Work Select chevron fix in scope; Create Work submit is **not** dirty-gate (create-on-submit).

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs:** `settings-agent-section.tsx`, `daemon-status-bar.tsx`, Select UI primitive, Settings Advanced / setup re-run control, Button variants + dark tokens.
- **Gate Decision:** go

### Plan

- **Selected:** Component + TanStack Query invalidation; token/variant alignment for danger/disabled.
- **Module boundaries:**

  | Module | Change |
  | --- | --- |
  | `settings-agent-section.tsx` | Dirty flag vs last-saved profile (AD-P1-2) |
  | `settings-agent-section.tsx` (save handler) | `invalidateQueries` for status bar keys (AD-P1-1) |
  | `daemon-status-bar.tsx` | Consume invalidated queries; Restart/agent tooltips (AD-P1-5) |
  | `components/ui/select.tsx` (or Create Work Select) | Single chevron (AD-P1-3) |
  | `Button` variants + `settings-advanced-section.tsx` | Danger re-run CTA; dark disabled contrast (AD-P1-4) |

- **Interfaces for implementers:**
  - Dirty baseline: snapshot from last successful `getAgentProfile` / save response.
  - Invalidation keys: document in task commit which `queryKeys.*` entries `DaemonStatusBar` reads.
  - Restart: `title` + `aria-label` from `shell.daemon.restart` (“Restart daemon”).
- **Risks:** Over-broad token change — scope CSS to `danger` + `disabled` primary only.
- **Effort:** M — 2–3 SDD tasks.

## Tasks (refined)

1. Agent dirty-gate + post-save `invalidateQueries` → footer badge refresh (AC-P1-1, AC-P1-2, AC-P1-7).
2. Select chevron + Advanced danger + dark disabled variant fix (AC-P1-3…5).
3. DaemonStatusBar Restart/agent a11y + tests (AC-P1-6, AC-P1-7).

## Global Constraints

- DESIGN.md / DESIGN.dark.md tokens only; i18n for new strings; `wire_contracts_changed: false`.
