# P3 — Settings Setup Re-run

**Goal:** Settings Setup section offers Re-run setup (R1): confirm → clear `setup_completed` → navigate `/setup` — closes V1.94 deferred Re-run setup UI.

**Architecture:** `SettingsSetupSection` at `/settings/setup`; R1 confirm → `setSetupCompleted(false)` only → extend `SetupCompletedProvider` with `setCompleted(false)` → `navigate('/setup', { replace: true })`. No file/DB wipe.

**Implementer SSOT:** `.mstar/iterations/v1.103/specs/settings-setup-section.md`

**Tech Stack:** React, desktop capabilities, Vitest, Design Studio.

**Execution:** mstar-sdd

**Tier:** **Must (P3)**

**Blocked by:** `2026-07-09-v1.103-settings-shell-nav`

## Prepare Gates

- specify: done
- clarify: done
- plan: done

## Product Framing

**User value:** Authors can intentionally re-enter first-run wizard from Settings; data is preserved.

**Non-goals:** DB reset; wiping agent/workspace files; changing wizard steps; auto-wipe on re-run.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| Interaction | **R1** confirm → clear marker → immediate `/setup` |
| Wipe | Forbidden for path/profile/DB in this action |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria

1. Author sees Re-run Setup CTA with confirm dialog on Setup section.
2. Confirm clears marker and opens `/setup` immediately.
3. Cancel leaves marker unchanged.
4. No workspace/agent file deletion in this flow.
5. Vitest covers confirm/cancel paths; prefer no schemas change.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.103/specs/settings-setup-section.md` (author-facing copy table)
- Shell: `.mstar/iterations/v1.103/specs/settings-shell-ia.md`

---

### Task 1: Studio — Setup Section Fixture

- [x] Visual acceptance of Setup section + confirm dialog chrome.

### Task 2: App — Re-run Setup Action

**Files:**
- Create: `apps/web/src/pages/settings/settings-setup-section.tsx`
- Modify: `apps/web/src/lib/setup-completed-context.tsx` — add `setCompleted(boolean)`
- Use: `setSetupCompleted(false)` + context sync + router navigate

- [ ] Confirm/cancel behavior; Vitest for context + navigation.

### Task 3: Verify

- [ ] Tests green; no schemas drift; no wipe side effects.

## Plan self-review (PM before locked)

1. R1 explicit; wipe forbidden.
2. Depends on P0 shell.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-09-v1.103-settings-rerun-setup/`
