# V1.46 Novel Runtime UX Edges — Plan

**Plan ID**: `2026-06-14-v1.46-novel-runtime-ux-edges`  
**Title**: P2 — On-disk chapter hints + dynamic clap `cli_args` help  
**Created**: 2026-06-14  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.46-novel-runtime-ux-edges`  
**Merge target**: `iteration/v1.46`  
**Depends on**: `2026-06-14-v1.46-spec-cli-hygiene` (merged)  
**Iteration compass**: [v1.46-novel-author-maturity-and-spec-hygiene-delivery-compass-v1.md](../iterations/v1.46-novel-author-maturity-and-spec-hygiene-delivery-compass-v1.md)  
**Residuals**: R-V139P5-N1 (chapter body_path hint), R-V145B1-002 (cli_args in --help)

**Effort**: M

---

## 1. Problem Statement

Chapter table in `works status` does not warn when `body_path` / `outline_path` missing on disk (R-V139P5-N1). `creator run <preset_id> --help` does not surface manifest `cli_args` for audit/review presets (R-V145B1-002). R-V138P1-01 (completion produce lock) **already fixed** — out of scope.

---

## 2. Goals

1. **On-disk hints (Grill #9)**: CLI best-effort `exists()` per chapter row; ⚠ + `works reconcile-chapters` hint; daemon reconcile remains authoritative.
2. **Dynamic clap (Grill #20–#21)**: load preset manifest by id; inject `cli_args` into `creator run <preset_id> --help` for first slice:
   - `novel-manuscript-audit-review`
   - `novel-manuscript-audit-extract`
   - `novel-review-master`
3. Extensible loader design for future presets.

---

## 3. Non-goals

- Full preset registry `--help` for all presets in V1.46.
- Research E2E (P3).
- Spec rewrites beyond overlay notes.

---

## 4. Acceptance Criteria

1. `works status` chapter table shows warning when configured path missing on disk.
2. `nexus42 creator run novel-review-master --help` lists manifest `cli_args` flags.
3. Same for both audit presets.
4. Tests for hint rendering + clap help injection (subset).
5. Residual R-V139P5-N1 closed; R-V145B1-002 closed or documented partial in P-last.

---

## 5. Tasks

- [ ] T1: Chapter table on-disk check in `works/mod.rs`.
- [ ] T2: Manifest-driven clap arg injection for 3 presets.
- [ ] T3: Tests.
- [ ] T4: Merge to `iteration/v1.46`.

---

## 6. Verification

```bash
cargo test -p nexus42
nexus42 creator run novel-review-master --help
```
