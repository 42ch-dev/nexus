# P2 — UI Continuity Polish (Stretch)

**Goal:** Park Design Studio / Settings / AgentPicker visual polish from V1.106 feedback intake; ship as capacity allows without blocking Must close.

**Architecture:** Prefer Studio-first changes to `@42ch/nexus-ui` primitives (Badge, Select) and app-shared AgentPicker; Settings IA is single-route `/settings/advanced` with stacked sections + hash redirects (no wire).

**Interfaces:**
- Badge: `packages/nexus-ui/src/components/badge.tsx` — token owner DESIGN.md `components.badge-status-pill`
- Select: `packages/nexus-ui/src/components/select.tsx` — token owner DESIGN.md `components.select`
- Settings nav: `settings-shell-layout.tsx` — three tabs; `settings-advanced-section.tsx` (new) hosts Connection + Setup bodies
- AgentPicker chrome: `agent-picker.tsx` (unchanged import path; FB-006 polish only)

**Implementer SSOT:** `.mstar/iterations/v1.106/specs/ui-continuity.md`

**Tech Stack:** Design Studio, nexus-ui, apps/web; no wire/schema.

**Execution:** mstar-sdd when implement starts

**Tier:** **Stretch (P2)** — may defer; iteration incomplete **No**

## Prepare Gates

- specify: **done** (§5.1 PM — iteration spec product-complete)
- clarify: **done** (grill-me locked)
- plan: **done** (tasks + acceptance criteria product-complete)
- architect: **done** (§5.2 — Advanced routing, Badge/Select token owners)
- writing: **done** (§5.3 — Voice & Content, DESIGN SP-002 prose, FB copy tables)

## Product Framing

**User value:** Clearer Badge semantics, Select chevron breathing room, simpler Settings IA (Agent/Workspace first), richer AgentPicker chrome.

**Non-goals:** DF-70 execution-mode/BYOK; Radix Select; package-promoting AgentPicker; Must-blocking this plan.

## Clarify (locked from grill-me)

| Topic | Decision |
|-------|----------|
| Tier | Stretch — auto-routed component/Settings polish |
| Settings IA | Agent → Workspace → Advanced (Connection + Setup sections) |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria (per FB when picked up)

1. **FB-001:** Soft Badge hues distinct in light/dark; all six Solid fills visible and hue-aligned; Studio Vitest solid matrix green.
2. **FB-002:** Select closed/disabled/invalid show clear chevron–border gap; native `<select>` only (no Radix); App inherits package fix.
3. **FB-005:** Nav shows **Agent**, **Workspace**, **Advanced** only; Advanced hosts Connection + Setup sections; `/settings/connection` and `/settings/setup` redirect.
4. **FB-006:** AgentPicker divider spacing; Installed badge beside title; tighter unified outbound icon; status dots visible; uninstalled title muted.

## Feedback items

### FB-V1106-001 — Badge Soft semantic contrast + Solid fill regression

Soft variants must read as distinct hues; Solid must share Soft hue family; fix Solid `queued`/`warning`/`error`/`preset` missing fills. SSOT: `packages/nexus-ui/src/components/badge.tsx` + DESIGN `badge-status-pill`.

### FB-V1106-002 — Select chevron right inset

Native `<select>` + `px-3` reads flush; asymmetric padding / custom chevron; keep native contract (no Radix). SSOT: `packages/nexus-ui/src/components/select.tsx`.

### FB-V1106-005 — Settings nav Advanced

Top tabs: Agent, Workspace, Advanced. Collapse Connection + Setup into Advanced sections. Redirects from `/settings/connection` and `/settings/setup`. Studio settings-host fixtures + App tests.

### FB-V1106-006 — AgentPicker chrome

Section divider breathing room; Installed badge beside title; unified tighter ArrowUpRight (prefer CSS/`::after`); status dots (selected filled / unselected hollow); uninstalled title faded. Shared `agent-picker.tsx` (Settings + wizard).

### Backlog note (not Must)

- DF-70 execution-mode / BYOK — deferred; do not silent-promote.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.106/specs/ui-continuity.md`
- Compass: `.mstar/iterations/v1.106-delivery-compass.md`
- Studio-first for visual FB items
- Settings Advanced: `/settings/advanced` + `#connection` / `#setup` hash redirects from legacy routes
- Badge/Select token changes require DESIGN.md update + contrast check before merge
- Does not block iteration close if incomplete
- `wire_contracts_changed: false`

---

### Task 1: Badge Soft/Solid (FB-001)

- [ ] Raise Soft tint / RCA Solid fills; DESIGN + Studio matrix; tests green.

### Task 2: Select inset (FB-002)

- [ ] Asymmetric padding or custom chevron; Studio + App inherit.

### Task 3: Settings Advanced nav (FB-005)

- [ ] Three-tab nav; single `/settings/advanced` page with Connection + Setup sections (`id` anchors).
- [ ] Redirects: `/settings/connection` → `#connection`, `/settings/setup` → `#setup`; `/connect` → Advanced Connection.
- [ ] Studio `settings-host` fixtures demonstrate three-tab nav.

### Task 4: AgentPicker chrome (FB-006)

- [ ] Divider, badge placement, link icon, status dots, uninstalled fade; Studio-first.

## Review Gate Summary

_Pending implement + QC (scaffold only)._

## QA Gate Summary

_Pending._
