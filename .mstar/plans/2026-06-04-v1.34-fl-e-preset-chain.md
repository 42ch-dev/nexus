# V1.34 FL-E Preset Chain — Locked Plan

**Plan ID**: `2026-06-04-v1.34-fl-e-preset-chain`  
**Title**: P2 — FL-E preset chain (research, reflection, persist)  
**Created**: 2026-06-04  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.34-fl-e-preset-chain`  
**Merge target**: `feature/v1.34-creator-workflow-and-agent-tools`  
**Depends on**: `2026-06-04-v1.34-fl-e-run-intents-and-stages`  
**Primary spec**: [creator-workflow.md](../knowledge/specs/creator-workflow.md)

---

## 1. Problem Statement

Stage advance (P1) must start **real** preset workflows. V1.34 locks wiring for existing presets — not a new demo preset unless QC requires it.

---

## 2. Goals

1. Wire **research** stage to `research` preset with Work seed from `creative_brief` + `inspiration_log`.
2. Wire **produce** to `novel-writing` (default).
3. Wire **review** to `reflection-loop`.
4. Wire **persist** to memory review CLI path + `kb-extract` / `queue-extract` documentation and schedule hooks.
5. Manual demo path in compass §4 passes on integration branch.

**Grill lock (P2):** use **existing** presets (`research`, `reflection-loop`, `kb-extract`); no `fl-e-pipeline-demo` unless architect documents gap in Completion Report.

---

## 3. Non-goals

- New conditional routing
- New embedded preset bundles (unless minimal seed/template fixes only)

---

## 4. Tasks

- [ ] T1: Stage advance → schedule create with `fl_e_stage` metadata
- [ ] T2: Preset input templates consume Work fields
- [ ] T3: Integration test: full stage chain on demo Work
- [ ] T4: Update orchestration embedded preset README if needed

---

## 5. Acceptance

1. End-to-end stage chain runnable after `creator demo-seed`.
2. `memory-augmented` / persist path documented in spec cross-links.
3. No regression to V1.33 `creator run start` intake path.

---

## 6. Verification

```bash
cargo test -p nexus-orchestration preset
cargo test -p nexus42 -p nexus-daemon-runtime
# Manual: compass §4 script
```
