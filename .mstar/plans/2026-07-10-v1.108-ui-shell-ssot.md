# P1 — UI Shell SSOT + AgentPicker + Canvas Surfaces

**Goal:** Unify Settings/Studio shell chrome on `ShellSidebarChrome` SSOT; land seeded UI FBs (profiles, tabs, nav, AgentPicker status/hover/Verify); Work-context canvas CTAs; Studio `/surfaces/canvas` first fixtures; honest World KB empty-state copy.

**Architecture:** Studio-first. Shell presentational SSOT under `@web-layout/*` / `ShellSidebarChrome`; Settings host fixtures import the same chrome (delete/shrink stale `SettingsShellChromeFixture`). AgentPicker changes in shared setup modules. Canvas Surfaces fixtures are presentational-only (pair with P0).

**Interfaces:**
- Shell: `apps/web/src/components/layout/presentational/*`, Studio `ShellSidebarChrome` / fixtures
- Settings fixtures: `apps/design-studio/src/fixtures/settings-host-fixtures.tsx` (+ related)
- AgentPicker: `apps/web/src/components/setup/agent-picker*.tsx` / `StatusDot` / `CustomLaunchField`
- Work IA: `apps/web/src/pages/work-detail-page.tsx`
- Studio canvas: `apps/design-studio/src/pages/surfaces.tsx` + new canvas fixtures
- World KB header copy: `world-kb-canvas-header.tsx` (or equivalent)

**Implementer SSOT:** `.mstar/iterations/v1.108/specs/ui-shell-ssot.md`

**Tech Stack:** Design Studio, `@42ch/nexus-ui`, `apps/web`; prefer no wire; Verify reuses agent scan if possible.

**Execution:** mstar-sdd when implement starts

**Tier:** **Must (P1)**

## Prepare Gates

- specify: **done** (§5.1 PM — primary spec product-complete; FB-UI-001..010 acceptance + Voice & Content)
- clarify: **done** (grill-me locked + UI review intake 001..010)
- plan: **done** (§5.1 PM — tasks + acceptance criteria product-complete)
- architect: **done** (§5.2 — shell SSOT, AgentPicker ownership, routing, Studio boundaries locked in primary spec § Architecture Locks)
- writing: **done** (§5.3 — Voice & Content locks: **Open Outline** / **Open Strategy**, **Verify Agent**, World KB helpers; corpus hygiene clean)

## Product Framing

**User value:** Settings Studio matches real App shell; AgentPicker selection/hover/custom-launch feel trustworthy; authors discover Outline/Strategy from Work; contributors preview canvas chrome in Studio.

**Who:** Authors (Settings, Agent picker, Work detail, World KB); UI contributors (Design Studio).

**Non-goals:** Full command palette implementation; Sidebar nesting Outline/KB; Worlds list page; DF-70 BYOK.

## Clarify (locked from grill-me + UI review)

| Topic | Decision |
|-------|----------|
| Shell SSOT | `ShellSidebarChrome` (segmented pill + sectioned icon nav + icon-only profiles) |
| Profiles | Icon +「+」only; keep `PROFILES` section label |
| StatusDot | Green only when **selected**; unselected hollow = gray |
| AgentCard hover | Whole card background |
| Custom launch | Verify button + inline success/fail; reuse existing probe |
| Studio canvas | First fixtures under `/surfaces/canvas` |
| Work IA | **Open Outline** + **Open Strategy** CTAs |
| Wire | Prefer `false`; Verify additive only if blocked |

## Acceptance Criteria

1. **FB-UI-001..005:** Settings host fixture shell matches App/`ShellSidebarChrome` (pill tabs, sectioned nav, icon-only profiles).
2. **FB-UI-006..007:** AgentPicker StatusDot (gray unselected) + whole-card hover — Studio accept then App.
3. **FB-UI-008:** **Verify Agent** + inline feedback; probe reuses scan or documented residual.
4. **FB-UI-004:** Studio `/surfaces/canvas` ships shell + context-menu fixtures (light+dark).
5. **FB-UI-009:** Work detail **Open Outline** + **Open Strategy** (`primary_preset_id` gated).
6. **FB-UI-010:** World KB empty copy honest — no command-palette promise.
7. Prefer `wire_contracts_changed: false`.

## Feedback items (summary)

| ID | User-visible outcome |
|----|----------------------|
| FB-UI-001 | Profiles: avatar +「+」only (no name under icons) |
| FB-UI-002 | Creator/Orchestrator = segmented pill everywhere |
| FB-UI-003 | Settings nav = section headers + icon+label + active rail |
| FB-UI-004 | Studio `/surfaces/canvas` fixtures exist |
| FB-UI-005 | `settings-host-fixture-shell` consumes latest shell SSOT |
| FB-UI-006 | Unselected **StatusDot** not green |
| FB-UI-007 | Hover paints entire AgentCard |
| FB-UI-008 | Custom launch **Verify Agent** + feedback |
| FB-UI-009 | Work detail: **Open Outline** + **Open Strategy** |
| FB-UI-010 | World KB empty-state honesty |

## Global Constraints

- Primary spec: `.mstar/iterations/v1.108/specs/ui-shell-ssot.md`
- Compass: `.mstar/iterations/v1.108-delivery-compass.md`
- Studio-first required for visual FBs
- `wire_contracts_changed: false` preferred
- Working branch: `plan/v1.108-p1-ui-shell-ssot`
- Merge target: `iteration/v1.108`

---

### Task 1: Shell SSOT for Settings fixtures (FB-UI-001..003, 005)

**User outcome:** Settings Studio shell matches App shell chrome.

**Files (locked §5.2):**
- Modify: `apps/design-studio/src/fixtures/settings-host-fixtures.tsx` — replace `SettingsShellChromeFixture` inline markup with `ShellSidebarChrome` + `FooterProfilesChrome` via `@web-layout/*` (mirror `surfaces.tsx` `ShellSidebarFixture`)
- Extend only if props gap: `apps/web/src/components/layout/presentational/shell-sidebar-chrome.tsx`, `footer-profiles-chrome.tsx`

- [ ] Pill tabs; sectioned icon nav; icon-only profiles
- [ ] Remove stale underline/plain-nav/profile-name dual track from fixture
- [ ] Studio smoke light+dark

### Task 2: AgentPicker polish (FB-UI-006..008)

**User outcome:** Clear selection semantics; whole-card hover; Verify custom launch.

**Files (locked §5.2):**
- Modify: `apps/web/src/components/setup/agent-picker.tsx` — `StatusDot`, `AgentCard`, `CustomLaunchField`
- Wire (App hosts only): `settings-agent-section.tsx`, `setup-step-agent.tsx` — call `NexusClient.scanAgents` with custom command for Verify
- Studio: `apps/design-studio/src/fixtures/agent-picker-fixtures.tsx` — static verify state matrix (no daemon)

- [ ] Unselected hollow gray; selected filled green
- [ ] Hover on outer `AgentCard` div (not inner select button only)
- [ ] **Verify Agent** via presentational props (`onVerify`, `verifyStatus`); probe reuses scan

### Task 3: Work canvas CTAs + KB honesty (FB-UI-009..010)

**User outcome:** Discover Outline/Strategy from Work; no fake palette promise.

**Files (locked §5.2):**
- Modify: `apps/web/src/pages/work-detail-page.tsx` — **Open Outline** + **Open Strategy** CTAs
- Modify: `apps/web/src/components/canvas/world-kb/world-kb-canvas-header.tsx` — empty helper copy

- [ ] **Open Outline** → `/works/:workId/outline`
- [ ] **Open Strategy** → `/strategies/:primary_preset_id` when `primary_preset_id` present
- [ ] Replace command-palette copy with locked honest empty helper

### Task 4: Studio `/surfaces/canvas` (FB-UI-004)

**User outcome:** Contributors preview canvas shell chrome in Studio.

**Files (locked §5.2):**
- Modify: `apps/design-studio/src/pages/surfaces.tsx` — add Canvas section to `SURFACES_SECTIONS` + route
- Create: `apps/design-studio/src/fixtures/canvas-surfaces-fixtures.tsx` — presentational shell + context-menu chrome (stub outline nodes OK until P0)

- [ ] Route/section for canvas Surfaces (`/surfaces/canvas`)
- [ ] Shell + context menu chrome fixtures (light+dark); no `NexusClient` / contracts
- [ ] Align with P0 node chrome when available; stub acceptable pre-P0 merge

## Review Gate Summary

_Pending implement + QC._

## QA Gate Summary

_Pending._
