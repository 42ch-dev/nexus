# P1 — Canvas directed center axis (Brief / Narrative / Moment)

> **Iteration compass:** `.mstar/iterations/v1.126/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.126/specs/canvas-directed-axis.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Each Timeline layer (Brief / Narrative / Moment) projects onto a differentiated **directed center axis** with a layer-specific visual spine — closes V1.125 Non-Goal "Canvas Brief/Narrative directed center axis". Today all three layers share one flat `when-axis` (Y=0) without layer-differentiated visual treatment.

**Execution:** mstar-sdd (multi-task)

### Wire contracts (architect LOCKED — pending seat 2)

- **Verdict: `wire_contracts_changed: false`** (PM proposal — pending architect seat 2 confirmation). Directed-axis is purely visual decoration on the existing `when-axis`; the `canvas-layer-{brief,narrative,moment}-accent` tokens already ship in `tokens.css` and the Studio Tokens gallery (V1.124 P1). No new DTOs, no daemon changes.

## Tasks

- [ ] **T1 (AC-V1126-2 — Brief directed axis) · Must:** World Timeline Brief layer (`timeline-canvas-adapter.tsx::projectBrief`) gains a **directed era-spanning horizontal arrow** as the center axis. The arrow's gradient ticks mark era start/end (`start_hint`/`end_hint`); arrow direction is left-to-right (chronological). Color: `--color-canvas-layer-brief-accent`. Era nodes attach on top of the arrow; Context cluster nodes stay above the axis. No semantic zoom regression vs V1.123 P4 (the directed arrow is **decoration** on the same axis geometry, not a re-projection). **Visual differentiation threshold (P1 spec ND-7):** the Brief arrow must read at-a-glance as a different visual rhythm from Narrative pins and Moment micro-segments — not just token-color-different. Studio review in T4 catches this.
  - **Verify (author):** Open World Timeline on Brief layer → see a left-to-right arrow with era ticks; light + dark differ in color only.
  - Files: `apps/web/src/components/canvas/timeline-canvas/timeline-canvas-adapter.tsx`, new `directed-axis-spine.tsx` (canvas-local or `@web-canvas/directed-axis-spine`), tests
  - **Architecture locks:**
    - Spine renderer: **React Flow custom node** (`type: 'directedAxisSpine'`) — P1 spec ND-A2 (AQ-5)
    - Decoration-only: `projectBrief` emits `directedAxisSpine` field; existing node/edge arrays unchanged (P1 spec ND-A3)
    - V1.123 P4 semantic-zoom preserved: spine is constant decoration, no zoom-band morph (P1 spec ND-A4)
    - V1.123 P4 factory-rebuild preserved: `createTimelineCanvasAdapter(ctxRef)` pattern untouched (P1 spec ND-A4)
    - `wire_contracts_changed: false` — visual decoration only (P1 spec ND-A7)
- [ ] **T2 (AC-V1126-2 — Narrative directed axis) · Must:** World Timeline Narrative layer (`projectNarrative`) gains a **discrete event-pin axis** — fine-grained tick marks at each event `timestamp`, with a thin connecting spine. Color: `--color-canvas-layer-narrative-accent`. Undated events cluster below with their own off-axis tick row (existing pattern preserved).
   - **Verify (author):** Switch to Narrative → see discrete pin axis with timestamps; undated cluster preserved.
   - Files: same as T1 plus Narrative projection branch
   - **Architecture locks:**
     - Same React Flow custom node, narrative variant: discrete pin axis (P1 spec ND-A2)
     - Undated events cluster below with off-axis tick row — existing V1.123 pattern preserved
     - `wire_contracts_changed: false` (P1 spec ND-A7)
- [ ] **T3 (AC-V1126-2 — Moment directed axis + Work-Timeline Narrative consistency) · Must:** Two sub-changes share T3 because they share the spine component:
   - **(a)** Work Timeline (`work-timeline-canvas-adapter.tsx`) Moment layer gains a **chapter/scene-scoped micro-axis** — short directed segments per chapter, ticks per scene/beat. Color: `--color-canvas-layer-moment-accent`. **Segment layout locked: density-encoding — length proportional to scene count (P1 spec ND-A1, AQ-4).** This is a deliberate rhythm break from Brief+Narrative's time-span convention; documented as intentional divergence (not a defect).
   - **(b)** Work Timeline Narrative layer inherits the same discrete pin axis treatment as World Timeline Narrative (T2) — cross-surface consistency per V1.123 P3 architecture.
   - **Verify (author) — two assertions:** (a) Open Work Timeline → Moment layer shows chapter-scoped micro-axes; (b) Work Timeline Narrative shows the discrete pin axis matching World Timeline Narrative.
   - Files: `apps/web/src/components/canvas/work-timeline-canvas/work-timeline-canvas-adapter.tsx`, micro-axis spine component
   - **Architecture locks:**
     - Moment spine: density-encoding (length ∝ scene count) — deliberate rhythm break (P1 spec ND-A1, AQ-4)
     - Chapter segments ordered by outline position; empty chapters show stub segment
     - Work-Narrative spine matches World-Narrative: same discrete pin axis (cross-surface consistency)
     - Same React Flow custom node, moment variant (P1 spec ND-A2)
     - `wire_contracts_changed: false` (P1 spec ND-A7)
- [ ] **T4 (Studio-first — directed-axis fixtures) · Should (V1.106 invariant — must land in-iteration):** Studio Canvas Surfaces page gains the directed-axis treatment in the existing V1.124 P0 Timeline fixtures. Light + dark. Each layer (Brief / Narrative / Moment) shows its differentiated spine. Smoke tests assert the spine element renders per layer. **Studio review is the visual-differentiation gate (P1 spec ND-7):** if Brief + Narrative + Moment read as the same rhythm with different color, that is a defect — re-cut before P1 ship.
   - **Verify:** `pnpm --filter design-studio dev` → Canvas Surfaces page → Timeline section → see three layer-specific spines; `./tooling/check-ui-guardrails.sh` green.
   - Files: `apps/design-studio/src/fixtures/canvas-surfaces-fixtures.tsx` (extend V1.124 P0 fixtures), `apps/design-studio/src/pages/surfaces.tsx`
   - **Architecture locks:**
     - Extend V1.124 P0 fixtures; light + dark; all three spines side-by-side (P1 spec ND-A6)
     - Legend explaining density-encoding convention on Moment (P1 spec ND-A1)
     - Extraction rule: `@web-canvas/directed-axis-spine` if ≥ 3 consumers (P1 spec ND-A5)

## Done when

- **AC-V1126-2:** Brief / Narrative / Moment each have a directed center axis with layer-specific visual spine; Studio fixtures show all three; V1.123 P4 semantic zoom + layer-swap factory rebuild pattern preserved.

## Roadmap

- **Single-iteration slice:** Visual differentiation only. Continuous-zoom axis morphing (where the axis itself animates between layers) is V1.128+ (V1.123 P4 residual: "adapter factory rebuild pattern does not scale to continuous zoom").
- **Token consumption:** P1 uses existing `--color-canvas-layer-{brief,narrative,moment}-accent` (V1.123 P3 T2 + V1.124 P1 gallery). No new tokens (NG: do not invent tokens to fill the gallery).
- **Extraction:** `@web-canvas/directed-axis-spine` alias root lands only if T1+T2+T3 produce a reusable primitive (≥ 3 consumers = World Timeline Brief + Narrative + Work Timeline); if extraction is single-consumer glue code, keep app-local with a header comment per V1.106 rule.

## Working branch

`feature/v1.126-p1-canvas-directed-axis` → merge `iteration/v1.126`
