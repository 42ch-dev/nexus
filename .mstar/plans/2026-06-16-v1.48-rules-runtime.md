# V1.48 Rules Runtime — Plan

**Plan ID**: `2026-06-16-v1.48-rules-runtime`  
**Title**: P2 — `AGENTS.md` Layer 2 runtime + accept `rule_suggestion` + rules reset CLI  
**Created**: 2026-06-16  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.48-rules-runtime`  
**Merge target**: `iteration/v1.48`  
**Depends on**: `2026-06-16-v1.48-findings-consumer` (merged)  
**Iteration compass**: [v1.48-novel-quality-loop-completion-delivery-compass-v1.md](../iterations/v1.48-novel-quality-loop-completion-delivery-compass-v1.md)  
**Primary specs**:

- [archived/knowledge/novel-findings-maturity.md](../archived/knowledge/novel-findings-maturity.md) §3 Rules runtime, §4 Rules CLI
- [novel-writing/workflow-profile.md](../knowledge/specs/novel-writing/workflow-profile.md) §5.5.4
- [novel-writing/author-experience.md](../knowledge/specs/novel-writing/author-experience.md) §3 (CLI narrative update if needed)

**Target residuals**: `R-V147P0-04`

---

## 1. Problem Statement

Spec declares Layer 2 at `Works/<work_ref>/AGENTS.md` (V1.47 normative) but runtime `read_rules_layers` still reads `Rules/novel-rules.md`. Accepting `rule_suggestion` does not mutate Layer 2. No reset CLI exists.

---

## 2. Goals

1. Migrate runtime reader to prefer `Works/<work_ref>/AGENTS.md`, with documented fallback read of legacy `Rules/novel-rules.md` (read-only) when `AGENTS.md` absent.
2. New scaffolds create `AGENTS.md` stub (not `Rules/novel-rules.md`).
3. API/CLI path to **accept** a finding's `rule_suggestion` → append structured section to `AGENTS.md`.
4. `creator run rules reset <work_id>` (or compass-approved equivalent) restores Layer 2 scaffold.
5. Close R-V147P0-04.

---

## 3. Non-goals

- Migrating existing Works' files on disk (no bulk migration per V1.47 grill #9).
- Layer 3 history file.
- New wire schemas.

---

## 4. Acceptance Criteria

1. `novel-writing` and review presets read Layer 2 from `AGENTS.md` when present.
2. Accept path writes append-only content to `AGENTS.md` with audit-friendly formatting.
3. Reset CLI restores default scaffold without deleting Work.
4. Hermetic tests for read preference + accept + reset.
5. R-V147P0-04 closed.

---

## 5. Tasks

- [x] T1: Update `read_rules_layers` (or successor) path resolution.
- [x] T2: Scaffold `AGENTS.md` in novel-project-init / bootstrap paths.
- [x] T3: Accept `rule_suggestion` command/handler.
- [x] T4: `rules reset` CLI subcommand under `creator run` preset pattern or dedicated group per CLI IA.
- [x] T5: Tests + residual closure.

### P2-fix1 — targeted fix wave (after QC1 + QC2 W-1)

Branch: `feature/v1.48-rules-runtime-fix1` (from `iteration/v1.48` HEAD `6b6602bd`).
Worktree: `.worktrees/v1.48-p2-fix1`.

- [x] **W-1 (qc1)** — Restore the orphan `# Panics` doc-comment summary on
  `update_finding_handler` (`crates/nexus-daemon-runtime/src/api/handlers/findings.rs`),
  absorbed by the T3 insert of `get_finding_creator_scoped_handler`. One-line
  doc restore, no behavior change. Commit `1a5fccac`.
- [x] **W-1 (qc2)** — Add `--dry-run` and `--yes` (`-y`) flags to
  `creator works rules reset` (`crates/nexus42/src/commands/creator/works/mod.rs` +
  `rules_runtime.rs`) plus a new pure, hermetically-tested
  `diff_agents_md_vs_scaffold` helper (`crates/nexus-orchestration/src/rules_layers.rs`).
  Default now prints a unified diff and prompts via `dialoguer` before
  overwriting; `--yes` skips the prompt; `--dry-run` previews without writing.
  Chose `--yes` over `--force` (confirmation-skip semantics, apt/pacman
  convention). New + updated parsing & diff tests. Commit `469679f4`.

Suggestions S-1–S-4 from qc1/qc2 remain deferred (non-blocking; tracked for
future P5 spec merge / backlog).

---

## 6. Verification

```bash
cargo test -p nexus-orchestration -- rules_layers 2>&1 | tail -30
cargo test -p nexus42 -- rules_reset 2>&1 | tail -20
```
