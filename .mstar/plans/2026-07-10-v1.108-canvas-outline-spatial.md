# P0 — Canvas Outline Spatial Parity

**Goal:** Deliver Outline+Timeline **spatial React Flow** parity (C1): `CanvasShell` graph with Volume/Chapter + timeline lane, graph↔inspector sync, non-spatial alt toggle, `canvas-outline-*` token wiring, and foreshadow authoring minimum — on existing outline/timeline patch routes.

**Architecture:** Reuse shared `CanvasShell` (`apps/web/src/components/canvas/canvas-shell.tsx`) and the six-layer canvas pattern (projection → OCC patches → conflict modal → alt view → DESIGN tokens). Prefer client-side graph projection from existing Work outline/timeline reads; no new Daemon routes unless blocked.

**Interfaces:**
- Route: `/works/:workId/outline` (`outline-page.tsx` → `outline-canvas.tsx`)
- Shell: `CanvasShell` + outline node/edge types
- Data: existing `use-outline-data` / outline+timeline patch client methods
- Tokens: root `DESIGN.md` / `DESIGN.dark.md` `canvas-outline-*`
- Conflict: reuse outline-flavored conflict modal

**Implementer SSOT:** `.mstar/iterations/v1.108/specs/canvas-outline-spatial.md`

**Tech Stack:** `apps/web` React Flow (`@xyflow/react`); Design Studio canvas fixtures owned by P1.

**Execution:** mstar-sdd when implement starts

**Tier:** **Must (P0)**

## Prepare Gates

- specify: **done** (§5.1 PM — primary spec product-complete; FB-C1-000..006 acceptance)
- clarify: **done** (grill-me locked — C1 slice + wire prefer false)
- plan: **done** (§5.1 PM — tasks + acceptance criteria product-complete)
- architect: **done** (§5.2 — module table, wire path, layout, blast radius locked in primary spec § Architecture Locks)
- writing: **done** (§5.3 — Voice & Content locks: **Show list view** / **Show graph**, **Link Foreshadow** / **Unlink Foreshadow**; corpus hygiene clean)

## Product Framing

**User value:** Authors can shape Work outline/timeline spatially like Strategy and World KB — not only via side panels.

**Who:** Authors on `/works/:id/outline`; contributors previewing canvas chrome in Studio (P1 fixtures).

**Non-goals:** Scene/Beat nodes; Steer-from-chapter; full command palette; Strategy `onConnect`; Idea persistence.

## Clarify (locked from grill-me)

| Topic | Decision |
|-------|----------|
| Slice | C1 Outline spatial RF + alt toggle + tokens + foreshadow min |
| Wire | Prefer `wire_contracts_changed: false` |
| Shell | Use existing `CanvasShell`; only hooks required for C1 (not full palette) |
| Dual Must | File-disjoint from P1 shell chrome where possible |

## Acceptance Criteria

1. **FB-C1-000:** Outline main view is `CanvasShell` + React Flow graph (Volume/Chapter + timeline lane).
2. **FB-C1-001..002:** Status-painted nodes; timeline lane; foreshadow edges when linked.
3. **FB-C1-003:** Node selection syncs inspectors; OCC patches + conflict modal unchanged semantically.
4. **FB-C1-004:** **Show list view** / **Show graph** toggle; alt lists keyboard-reachable.
5. **FB-C1-005:** **Link Foreshadow** / **Unlink Foreshadow** minimum UI via existing patch routes.
6. **FB-C1-006:** `canvas-outline-*` tokens consumed (light+dark).
7. Prefer `wire_contracts_changed: false`.

## Feedback items (summary)

| ID | User-visible outcome |
|----|----------------------|
| FB-C1-000 | Outline opens as spatial canvas (not panel-only grid) |
| FB-C1-001 | Volume/Chapter cards visible on graph with status paint |
| FB-C1-002 | Timeline events on lane; foreshadow edges visible when linked |
| FB-C1-003 | Click node → inspector; save still OCC-safe |
| FB-C1-004 | **Show list view** / **Show graph** toggle (keyboard-friendly path) |
| FB-C1-005 | **Link Foreshadow** / **Unlink Foreshadow** from UI |
| FB-C1-006 | `canvas-outline-*` tokens applied (no unused token gap for shipped nodes) |

## Global Constraints

- Primary spec: `.mstar/iterations/v1.108/specs/canvas-outline-spatial.md`
- Compass: `.mstar/iterations/v1.108-delivery-compass.md`
- Normative master: `.mstar/specs/canvas-strategy-surface.md` (Outline surface)
- Pattern: `.mstar/knowledge/architecture-patterns/canvas-surface-implementation-pattern.md`
- `wire_contracts_changed: false` preferred
- Working branch: `plan/v1.108-p0-canvas-outline-spatial`
- Merge target: `iteration/v1.108`

---

### Task 1: Outline graph projection module (FB-C1-000..002)

**User outcome:** Outline data projects to React Flow nodes/edges.

**Files (locked §5.2):**
- Create: `apps/web/src/components/canvas/outline-canvas/rf-projection.ts` — pure `WorkOutline` + chapters → RF `Node`/`Edge` + layout coords
- Create: `apps/web/src/components/canvas/outline-canvas/outline-nodes.tsx` — `outlineNodeTypes` map (Volume, Chapter, TimelineEvent)
- Extend: `apps/web/src/components/canvas/outline-canvas/graph-projection.ts` — keep conflict/`changedFieldsOf` helpers only (no RF types here)
- Modify: `apps/web/src/components/canvas/outline-canvas.tsx` — orchestrator consumes projection

- [ ] Project volumes/chapters/events/foreshadows to nodes/edges
- [ ] Deterministic lane/layered layout (World KB grid pattern); defer dagre/elk
- [ ] Unit tests for projection purity (`outline-canvas/__tests__/rf-projection.test.ts`)

### Task 2: Wire CanvasShell + inspectors (FB-C1-000, 003)

**User outcome:** Spatial canvas with live inspector sync.

**Files (locked §5.2):**
- Modify: `outline-canvas.tsx` — mount `CanvasShell` with `outlineNodeTypes`; `useNodeChangeHandler` from `canvas-shell.tsx`
- Modify: `outline-canvas/inspectors/*` — selection driven from graph click + existing panel paths
- Reuse: `outline-canvas/conflict-modal.tsx` → shared `outline-conflict-modal.tsx` (no new modal)

- [ ] Mount `CanvasShell` with outline `nodeTypes`
- [ ] Selection → inspector; patches unchanged semantically (`use-outline-data` hooks)
- [ ] Conflict modal still works on stale revision (409 `OutlineConflictError`)

### Task 3: Alt view toggle (FB-C1-004)

**User outcome:** Graph ↔ list toggle like Strategy/World KB.

**Files (locked §5.2):**
- Create: `apps/web/src/components/canvas/outline-canvas/outline-alt-view.tsx` — non-spatial chapter + timeline lists
- Extend: `outline-canvas/canvas-layout.tsx` — toolbar toggle (**Show list view** / **Show graph**); mirror `strategy-canvas/canvas-layout.tsx`

- [ ] Non-spatial chapter list + timeline list reachable in alt mode
- [ ] Default remains graph for pointer users; alt usable keyboard-first (`aria-pressed`)

### Task 4: DESIGN tokens + foreshadow min (FB-C1-005..006)

**User outcome:** Correct paint; foreshadow authoring possible.

**Files (locked §5.2):**
- Modify: `outline-canvas/outline-nodes.tsx` — consume `canvas-outline-*` tokens
- Modify: `outline-canvas/inspectors/event-inspector.tsx` — **Link Foreshadow** / **Unlink Foreshadow** controls
- Optional graph path: `onConnect` on `CanvasShell` for link gesture (inspector path is minimum)

- [ ] Consume `canvas-outline-*` tokens (light+dark)
- [ ] Link via `timeline.patch_event` op `link_foreshadow` (`event_id` + `foreshadows_event_id`)
- [ ] Unlink via additive `unlink_foreshadow` wire if daemon lacks op (escape hatch — see spec § Wire)
- [ ] Vitest + light/dark smoke notes

## Review Gate Summary

**QC mode: full tri-review (SDD mandatory)**

| Seat | Reviewer | Initial verdict | Post-fix verdict |
|------|----------|----------------|------------------|
| 1 | qc-specialist (architecture) | Request Changes (4 Important) | **Approve** |
| 2 | qc-specialist-2 (security/correctness) | Request Changes (3 Warning) | **Approve** |
| 3 | qc-specialist-3 (performance/reliability) | Approve with residuals (1 Warning, 3 Suggestion) | **Approve with residuals** |

**Consolidated verdict: Approve with residuals.**

Fix wave (8 findings) committed `99450dd6`: empty shell parity, pagination dangling edges, conflict modal consumer, attach payload field, unlink event validation, self-ref guard, Rust test gaps, color-mix fallback.

**wire_contracts_changed: true** — additive `unlink_foreshadow` op on `TimelinePatchEventRequest` (documented escape hatch; daemon genuinely lacked unlink). Schema + generated TS + Rust in same commit.

**Open residuals (5):** R-V1108P0QC3-W001 (selection effect churn), R-V1108P0QC3-S002 (viewport state loss), R-V1108P0QC3-S003 (RF mock in tests), R-V1108P0QC1-M001 (alt view read-only), R-V1108P0QC1-S001 (extract canvas hook). All non-blocking, deferred post-V1.108.

Reports: `.mstar/sdd/2026-07-10-v1.108-canvas-outline-spatial/review/qc-consolidated.md`

## QA Gate Summary

**QA gate: mandatory | QA mode: acceptance-only**

**QA verdict: Pass.** DoD mapping complete — all FB-C1-000..006 acceptance criteria mapped to SDD/QC evidence. Checkout alignment verified. 5 open residuals confirmed non-blocking. Wire change (additive `unlink_foreshadow`) verified consistent schema→TS→Rust.

Report: `.mstar/sdd/2026-07-10-v1.108-canvas-outline-spatial/review/qa-acceptance.md`

**Plan status: Done.**

## QA Gate Summary

_Pending._
