# P4 Stretch — Settings Workspace Path (W2)

**Goal:** Optional Settings Workspace section to view/change current workspace path with honest reload/restart copy.

**Architecture:** `/settings/workspace` + nav item registered only when P4 runs. Reuse `getWorkspaceRoot` / `pickDirectory` / `setWorkspacePath` (wizard parity). Post-persist **copy-only** honesty about restart/reload — no auto daemon restart.

**Implementer SSOT:** `.mstar/iterations/v1.103/specs/settings-workspace-section.md`

**Tech Stack:** React, Tauri desktop capabilities, Vitest, Design Studio.

**Execution:** mstar-sdd

**Tier:** **Stretch (P4)** — whole-plan defer allowed → V1.104+; **does not block iteration close**

**Blocked by:** `2026-07-09-v1.103-settings-shell-nav`  
**Preference:** Start after Must P1–P3 automated paths Done unless PM capacity exception.

## Prepare Gates

- specify: done
- clarify: done
- plan: done

## Product Framing

**User value:** Change workspace after first-run without hunting wizard-only UI.

**Non-goals:** Multi-workspace switcher; file migration between roots; treating as Must; shipping Workspace nav when plan defers.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| Scope | **W2** editable path + honest restart/reload copy |
| Defer | Whole-plan defer OK |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria (if plan runs)

1. Path display + change on desktop.
2. Persist via existing workspace path capability.
3. Honest post-change reload/restart copy.
4. Vitest for mount + persist mock.
5. Prefer no schemas change.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.103/specs/settings-workspace-section.md` (honesty copy table)
- Shell: `.mstar/iterations/v1.103/specs/settings-shell-ia.md`

---

### Task 1: Studio — Workspace Section Fixture

- [x] Deferred — plan not run in V1.103.

### Task 2: App — Workspace Section

- [x] Deferred — plan not run in V1.103.

### Task 3: Verify or Defer

- [x] Deferred to **V1.104+** (2026-07-09). Compass Stretch OK; no Workspace nav/route shipped; DF-70 tracker retargeted. Must P0–P3 complete.

## Deferral Record

- **Decision:** Whole-plan Stretch defer (W2).
- **Reason:** Must P0–P3 delivered Settings shell deepening; Workspace path edit remains optional capacity for V1.104+.
- **Shell:** No `/settings/workspace` route or nav item (per shell IA lock).
- **Tracker:** `knowledge/deferred-features-cross-version-tracker.md` — Workspace W2 → V1.104+.

## Plan self-review (PM before locked)

1. Stretch / deferrable explicit.
2. Multi-workspace Out.
3. Depends on P0 shell.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-09-v1.103-settings-workspace/`
