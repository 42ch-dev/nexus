# V1.34 FL-E Run Intents and Stages — Locked Plan

**Plan ID**: `2026-06-04-v1.34-fl-e-run-intents-and-stages`  
**Title**: P1 — Work stage model + `creator run stage` routing  
**Created**: 2026-06-04  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`, `@architect`  
**Task category**: `logic`  
**Working branch**: `feature/v1.34-fl-e-run-intents-and-stages`  
**Merge target**: `feature/v1.34-creator-workflow-and-agent-tools`  
**Depends on**: `2026-06-04-v1.34-residual-convergence` (implement after P0 merge)  
**Primary spec**: [creator-workflow.md](../knowledge/specs/creator-workflow.md)

---

## 1. Problem Statement

V1.33 `creator run` lacks **explicit FL-E stages**. Users cannot advance research → produce → review → persist as a governed product path.

---

## 2. Goals

1. Add `current_stage`, `stage_status` to Work persistence + Local API.
2. Implement `creator run stage list|advance` per [cli-spec.md](../knowledge/specs/cli-spec.md) §6.2E.
3. Enforce linear stage gates and single active stage schedule per Work.
4. Update [work-experience-model.md](../knowledge/specs/work-experience-model.md) if field names differ from draft.

---

## 3. Non-goals

- New embedded presets (P2)
- Agent tools (P3/P4)
- `--auto-chain` (DF-53)

---

## 4. Tasks

- [ ] T1: DDL migration + `works` API extensions
- [ ] T2: CLI `run stage` submodule + tests
- [ ] T3: Stage→preset allowlist validation
- [ ] T4: Extend `creator run status` output

---

## 5. Acceptance

1. `creator run stage advance --stage research` after intake complete enqueues `research` schedule with `work_id` metadata.
2. Wrong-order advance fails without `--force`.
3. `works_api` tests cover stage fields.
4. If stage persistence incomplete → register **DF-54** in P5 only.

---

## 6. Verification

```bash
cargo test -p nexus42 command_surface_contract
cargo test -p nexus-daemon-runtime works_api
nexus42 creator run stage --help
```
