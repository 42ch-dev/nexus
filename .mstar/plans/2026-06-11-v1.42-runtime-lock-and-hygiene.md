# V1.42 Runtime Lock Production + Hygiene — Plan

**Plan ID**: `2026-06-11-v1.42-runtime-lock-and-hygiene`  
**Title**: P0 — `runtime_lock_holder` production acquire + V1.41 P-last defer-7  
**Created**: 2026-06-11  
**Status**: Done (T1–T7 all complete; commits `1dad80fe`, `e8993870`, `e44c8fda`)
**Owner**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.42-runtime-lock-and-hygiene`  
**Merge target**: `iteration/v1.42`  
**Depends on**: `2026-06-11-v1.42-harness-docs-prepare`  
**Iteration compass**: [v1.42-multi-volume-serial-writing-delivery-compass-v1.md](../iterations/v1.42-multi-volume-serial-writing-delivery-compass-v1.md)  
**Primary spec**: [novel-writing/multi-work-lifecycle.md](../knowledge/specs/novel-writing/multi-work-lifecycle.md) §4.2

---

## 1. Problem Statement

PR #53 security re-review: `runtime_lock_holder` columns exist but **production paths do not acquire/release**. Concurrent mutating CLI/daemon operations on the same Work are not guarded. Additionally, V1.41 P-last hygiene deferred 7 V1.40 residuals + sqlx infra to V1.42.

---

## 2. Goals

1. **Acquire & release** `runtime_lock_holder` on:
   - `creator run` mutating subcommands (`start`, `continue`, `stage`, `resume`, `reconcile-chapters`)
   - Daemon `patch_work` and schedule enqueue paths
   - Schedule **terminal** transition releases `daemon:schedule:<id>` holder
2. **Stale recovery (R-V141P0-01)**: default **2h** TTL on `runtime_lock_acquired_at`; env/config override; hermetic tests.
3. Converge P-last (V1.41) **defer-7** + sqlx infra:

| ID | Title (summary) |
| --- | --- |
| R-V140P0-S3 | Layer 1 compile-time constant doc (if still open) |
| R-V140P1-S6 | Validation path benchmarks |
| R-V140P2-S2 | E2E world_kb_block through schedule→prompt |
| R-V140P3-S1 | Cross-creator job_id claim test |
| R-V140P3-S2 | finalize_extract failure-injection test |
| R-V140P3-S3 | AC3 empty world_id test strengthen |
| R-V140P4-INFRA | `.sqlx/` offline cache refresh |

4. Refresh `.sqlx/` for V1.41 schema columns where sqlx-cli available (R-V141P0-12 / R-V141P1-16 if still open).

---

## 3. Non-goals

- 14 V1.42 UX carry-forward items → **P-last**
- Multi-volume PK → **P1**
- R-V140P4-W2 PM-accept sqlx supervisor waiver
- Re-open V1.40/V1.33 items closed in V1.41 P-last hygiene

---

## 4. Acceptance Criteria

1. Hermetic test: two concurrent mutating operations on same Work → second fails with holder hint.
2. Crashed CLI holder cleared after TTL (configurable).
3. Spec §4.2 marked implemented.
4. Defer-7 disposition updated in `status.json`.
5. `cargo test` + `cargo clippy` scoped crates pass.

---

## 5. Tasks

- [x] T1: Map all mutating paths (CLI + daemon + auto-chain enqueue).
- [x] T2: RAII acquire/release for `cli:<pid>:<uuid>`.
- [x] T3: Schedule holder acquire on enqueue; release on terminal.
- [x] T4: TTL stale recovery task or check-on-acquire.
- [x] T5: Hermetic tests + update spec §4.2 status.
- [x] T6: Defer-7 triage (fix/waive/defer with closure notes).
- [x] T7: `.sqlx/` refresh if schema touched.

---

## 6. Verification

```bash
cargo test -p nexus42 -p nexus-daemon-runtime -p nexus-local-db -- runtime_lock 2>&1 | tail -40
cargo clippy -p nexus42 -p nexus-daemon-runtime -p nexus-local-db -- -D warnings
```
