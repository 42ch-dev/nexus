# Plan — V1.92 P-last: Closure

- **plan_id**: `2026-07-05-v1.92-closure`
- **Iteration**: V1.92 (Remote-Access Hardening)
- **Status**: Todo
- **Owner**: `@project-manager` (orchestration) + QC tri-review + `@qa-engineer`
- **Blocks**: P0 + P1 must be Done first.
- **Branch**: `Working branch: create feature/v1.92-closure from iteration/v1.92` → merge `iteration/v1.92`
- **Compass**: [`v1.92/delivery-compass.md`](../iterations/v1.92/delivery-compass.md)

## Context

Closure for V1.92. Runs after P0 (TLS) + P1 (connection model) merge to `iteration/v1.92`. Owns QC tri-review dispatch (security lens qc2 + transport lens qc3), QA gate, compound (iteration-level knowledge crystallisation), Profile B compaction, and the PR to `main`.

## Scope

- [ ] **T1 — QC tri-review dispatch** (PM): qc-specialist (architecture coherence) + qc-specialist-2 (security lens — remote attack surface not widened, fingerprint-as-trust-anchor sound) + qc-specialist-3 (transport/performance lens — TLS handshake, cert lifecycle, no perf regression). Same `Review cwd` / `Working branch` / `Review range` across all three (aligned on `iteration/v1.92` HEAD). Default targeted re-review after fixes; full tri only if Assignment says so.
- [ ] **T2 — QA gate** (`@qa-engineer`): verify the regression tests reproduce the pre-TLS cleartext exposure as failing-without-the-fix; verify remote-bind-without-cert is refused; verify a GUI-client remote-connect round-trip (or documented manual repro if no CI remote daemon); full `cargo test --all` + `pnpm --filter web test/build` + clippy/fmt/codegen/validate-schemas green.
- [ ] **T3 — Fix-wave + residual registration** (PM + dev): consolidate QC findings; targeted re-review; register non-blocking items into root `residual_findings["2026-07-05-v1.92-closure"]` per `mstar-plan-artifacts`.
- [ ] **T4 — Compound** (PM triggers `mstar-compound`): crystallise the reusable knowledge from this iteration (e.g. header-key-as-CSRF-defence pattern, TOFU-in-a-local-first-tool pattern, rcgen+rustls+axum listener integration). Update `CONCEPTS.md` if new domain vocabulary; register new docs in `knowledge/README.md`.
- [ ] **T5 — Profile B compaction**: archive Done plans to `.mstar/archived/plans/<plan-id>.json` + append IDs (strings) to `archived/plans-done.json`; keep only non-Done in `status.json.plans[]`. Verify the string-only invariant.
- [ ] **T6 — Deferred-tracker + roadmap update**: update `knowledge/deferred-features-cross-version-tracker.md` (R-V191P1-001..004 closed; BL-13 reading-chrome still shipped; remote-endpoint-manager UI candidate for V1.93). Update compass `## Roadmap Position` current iteration → delivered.
- [ ] **T7 — Phase 3 iteration-close**: print §3.1 entry checklist; complete §3.2-§3.5; compass frontmatter `status: completed` + `end_date`; commit to `iteration/v1.92`; PR to `main`.

## Acceptance
- QC consolidated Approve; QA Pass; all four V1.91 residuals (001-004) `lifecycle: resolved` in `status.json`; R-V191P1-005 unchanged-deferred.
- Compound done (or "no crystallisable knowledge" recorded with reason); `knowledge/README.md` index updated.
- Profile B compaction verified (string-only `plans-done.json`); `wc -c .mstar/status.json` < 20_000.
- PR opened to `target_branch=main` (not an unrecorded default); compass `status: completed`.
- Product-verifiable outcomes from the compass are demonstrable:
  - A GUI client on a trusted network, after explicit TOFU confirmation, can drive the full authoring surface (canvases + reading chrome + findings triage) against a remote daemon over TLS with no behavioural difference from local.
  - Fingerprint change triggers an explicit blocking re-pin warning; data operations are gated until the author chooses.
  - Raw-browser-tab remote navigation remains unsupported (self-signed warning is the expected outcome); the supported path is the app Connect screen.

## Out of scope
- Any new feature work. Re-opening V1.91 R-V191P1-005 (stays deferred to list virtualisation). ACME / multi-endpoint manager (V1.93 candidates).
