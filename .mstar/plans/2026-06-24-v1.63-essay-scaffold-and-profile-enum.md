---
plan_id: 2026-06-24-v1.63-essay-scaffold-and-profile-enum
title: V1.63 P0 — Essay Scaffold + work_profile:essay Enum + Completion Semantics
iteration: V1.63
slot: P0
status: Todo
effort: M
owner: "@fullstack-dev"
review: Tri-review + mid-QA
wave: 1
dependency: P-1 PM signoff
working_branch: feature/v1.63-essay-scaffold
merge_target: iteration/v1.63
---

# P0: Essay Scaffold + work_profile:essay Enum + Completion Semantics

Track A foundation: add `work_profile: "essay"` to the Work profile enum, implement the essay artifact scaffold (`Works/<work_ref>/` with `Outlines/` + `Drafts/` + `Logs/`), and wire completion semantics (`Drafts/draft.md` frontmatter `status == finalized` AND intake complete → works.status = completed). Mirrors V1.55 game-bible/script scaffold parity (incl. ScaffoldTransaction atomicity).

## Tasks

- [ ] T1: Add `essay` variant to `work_profile` enum in domain schemas + generated types; codegen cascade.
- [ ] T2: Implement essay project scaffold (artifact layout per spec §3): `Works/<work_ref>/{README.md, Outlines/outline.md, Drafts/draft.md, Logs/}`; ScaffoldTransaction wrapping FS writes + DB PATCH (parity with game-bible/script).
- [ ] T3: Implement essay completion detection: `Drafts/draft.md` frontmatter `status == finalized` AND `works.intake_status == complete` → `works.status = completed`. Gate non-novel paths on `work_profile == essay` before any chapter-table access.
- [ ] T4: CLI wiring: `creator works` scaffold accepts `--profile essay`; `creator works status` renders essay artifact status.
- [ ] T5: Tests: scaffold creates correct layout; completion detection; non-novel profile gate; ScaffoldTransaction rollback.
- [ ] T6: Spec amendment: `work-experience-model.md` profile enum + completion cross-ref.

## Done when

- `work_profile: "essay"` is a valid enum value end-to-end (schema → codegen → DB → CLI).
- Essay scaffold produces the spec §3 layout atomically (ScaffoldTransaction).
- Completion detection matches spec §8.
- No code path assumes chapters for essay Works.
- `cargo test -p <touched crates>` + `cargo clippy -p <touched crates> -- -D warnings` pass.

## Open design items

- Essay BlockType reuse decision (§5 item #2): reuse existing character/location, or introduce essay_subject? Resolve in T1.
