# V1.29 Stage0 Structured Delimiters — Locked Plan

**Plan ID**: `2026-05-26-v1.29-stage0-structured-delimiters`  
**Title**: FL-A3 — replace markdown-header heuristic for Stage0 personality split (R13)  
**Created**: 2026-05-26  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`, `@qc-specialist`  
**Task category**: `logic`  
**Working branch**: `feature/v1.29-stage0-structured-delimiters`  
**Merge target**: `feature/v1.29-author-intelligence-and-hardening`  
**Depends on**: none (may merge after memory plan; no hard code dependency)  
**Primary spec**: [v1.29-author-intelligence-and-agent-hardening-delivery-compass-v1.md](../iterations/v1.29-author-intelligence-and-agent-hardening-delivery-compass-v1.md)  
**Spec refs**:

- [`crates/nexus-moment-context-assembly/src/moment.rs`](../../crates/nexus-moment-context-assembly/src/moment.rs)
- [`crates/nexus-moment-context-assembly/AGENTS.md`](../../crates/nexus-moment-context-assembly/AGENTS.md)
- [`crates/nexus-creator-memory/`](../../crates/nexus-creator-memory/) (Stage0 assembly output format)

---

## 1. Problem Statement

`split_stage0_personality` in `moment.rs` uses a **markdown-header heuristic** to separate personality from other Stage0 content. QC residual **R13** flags brittleness when Stage0 formatting evolves.

---

## 2. Goals

1. Define a **frozen delimiter protocol** for Stage0 full-context output (e.g. `---STAGE0:PERSONALITY:START---` / `END` or equivalent — choose one in implementation, document in AGENTS.md).
2. Update Stage0 assembly in `nexus-creator-memory` / moment pipeline to emit delimiters when building full context.
3. Update `split_stage0_personality` to prefer delimiter split; retain heuristic fallback **only** for legacy fixtures (optional; if removed, migrate tests).
4. Close **R13** in `status.json` or downgrade with PM sign-off if fallback kept.

---

## 3. Non-goals

- Changing four-domain assembly logic beyond Stage0 split.
- SOUL file format breaking change without migration note in soul spec.

---

## 4. Tasks

| ID | Task | Acceptance | Verify |
| --- | --- | --- | --- |
| A3.1 | Delimiter constants + emit in Stage0 | Unit test round-trip | `cargo test -p nexus-moment-context-assembly` |
| A3.2 | Parse in `split_stage0_personality` | R13 scenario covered | Same |
| A3.3 | Update AGENTS.md delimiter spec | Reviewer can implement without reading code | Doc |
| A3.4 | Residual R13 close | `status.json` updated | PM |

---

## 5. Acceptance (plan close)

1. Personality section never truncated incorrectly on current demo seed + assemble-moment.
2. Tests cover delimiter path and at most one legacy fallback case.
3. R13 resolved in residual_findings.
