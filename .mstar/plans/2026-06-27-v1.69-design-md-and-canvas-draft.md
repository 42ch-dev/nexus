# P0 — V1.69 Design-System Maturation + Canvas Draft — Plan

**Iteration**: V1.69 — Design System Maturation & Canvas Draft
**Plan ID**: `2026-06-27-v1.69-design-md-and-canvas-draft`
**Status**: Todo (pending P-1 lock)
**Effort**: M
**Compass**: [v1.69-design-system-maturation-and-canvas-draft-compass-v1.md](../iterations/v1.69-design-system-maturation-and-canvas-draft-compass-v1.md) §1.1 Track A + Track B
**Owner**: `@architect` (lead — DESIGN.md Track A + canvas Draft architecture B1–B4) + `@product-manager` (canvas Draft product/UX B5 + `web-ui.md` amend)
**Branch policy**: `feature/v1.69-design-md-canvas-draft` from `iteration/v1.69` → merge to `iteration/v1.69`
**Wire contracts changed**: FALSE (documentation + spec authoring only)

## Goal

Execute Track A (DESIGN.md Production migration) and Track B (Canvas Exploration → Draft) as one coordinated plan. B4 (canvas token contract list) feeds A4 (DESIGN.md frontmatter placeholders for those tokens). Result: V1.70 implement begins from a frozen Production-level design system + a frozen canvas Draft.

## Scope (in)

### Track A — DESIGN.md Production Migration (owner: `@architect`)

- [ ] **A1 — Author YAML frontmatter** at top of `apps/web/DESIGN.md` per `mstar-design-md` → `references/design-md-spec.md` §1.5:
  - `version: 0.1.0`, `name: "Nexus Local Web UI"`, `description: "...light theme; dark theme at DESIGN.dark.md"`
  - `colors:` flat map — every background/gray/gray-alpha/accent token at current values, **names preserved verbatim** (`background-100`, `gray-1000`, `blue-700`, `gray-alpha-100`, …). Alpha tokens use `rgba(...)` or `#rrggbbaa`.
  - `typography:` map — all 13 type tokens (`heading-32/24/20/16`, `label-14/12`, `copy-16/14/13`, `button-14/12`, `label-12-mono`, `copy-13-mono`) as 5-property sub-objects (`fontFamily`, `fontSize`, `fontWeight`, `lineHeight`, `letterSpacing`)
  - `spacing:` flat map — `base: 4px` + numeric keys (`1: 4px` … `24: 96px`)
  - `rounded:` map — semantic sub-keys. **Naming decision locked by `@architect` in Phase 2b** (compass §6.2): recommend `rounded.control: 6px`, `rounded.card: 8px`, `rounded.popover: 12px`, `rounded.fullscreen: 16px`, `rounded.pill: 9999px` (zero consumer change).
  - `components:` map — all component variants from current DESIGN.md component-primitive tables, each referencing frontmatter keys via `{colors.X}` / `{typography.X}` / `{rounded.X}` syntax: button (primary/secondary/tertiary/destructive + size variants), input/select/textarea (default/error/disabled), card, badge/status-pill (neutral/running/queued/warning/error/preset), sidebar-nav, dialog/popover, editor (V1.65), data-table (V1.65), context-menu (V1.65), desktop-window-chrome, app-menu, native-dialogs, desktop-context-menu, daemon-status-indicator (V1.66)
- [ ] **A2 — Create `apps/web/DESIGN.dark.md`**: same token names, dark values (currently in MD-table "Dark" columns). Includes desktop-shell-supplement + daemon-status-indicator dark values. Add pointer line in DESIGN.md Overview: "The Dark theme lives at `DESIGN.dark.md`."
- [ ] **A3 — Reshape DESIGN.md body** into supplementary documentation:
  - KEEP (rule-type, frontmatter cannot express): Overview/identity prose, Colors §Semantic Mapping table (meaning→token), Typography prose (system-stack rationale, tabular-nums note), Spacing §Rhythm + §Layout Rules, Breakpoints table, Elevation table (shadow values per state), Motion table + reduced-motion rule, Shapes prose (one-radius-family rule), all Component-section state/interaction rules + size tables + behavior copy (Button states, Input helper-text rules, Card padding rules, Table header/row rules, Badge variant mapping, Toast rules, Sidebar-nav rules, Dialog/Popover rules, Editor V1.65 rules + typography, Data Table V1.65 inline-edit rules + chapter-status mapping, Context Menu V1.65 copy-path behavior), Voice & Content section (all rules + examples), Implementation Mapping for P1, Desktop Shell Supplement V1.66 (all behavior rules, daemon-status state labels).
  - COMPRESS (pure-value tables that duplicate frontmatter): Colors §Background/Gray/Gray Alpha/Accent Scales tables → "Values: see frontmatter `colors:`" + a one-line intent note per scale (e.g. "Background scale encodes surface hierarchy: 100 default, 200 subtle panel, 300 hover/selected"). Typography §Font Families + §Type Scale table → keep families as prose; type-scale values "see frontmatter `typography:`" + role-intent note. Spacing §Spacing Scale table → "see frontmatter `spacing:`" + the rhythm notes (already rule-type, kept). Shapes table → "see frontmatter `rounded:`" + the one-radius-family rule (kept).
  - PRESERVE all V1.65/V1.66 stage sections as rule documentation.
- [ ] **A4 — Add Canvas Surface (V1.70 placeholder) section** to DESIGN.md body + stub the B4 token list into frontmatter as `LEVEL` placeholders (commented YAML or a clearly-marked placeholder block per `mstar-design-md` §5). Section body explains: these tokens are stubbed for the V1.70 canvas implement per the `canvas-strategy-surface.md` Draft; V1.69 does not consume them. **Final token list comes from B4.**
- [ ] **A5 — Update completeness tag**: `<!-- COMPLETENESS_LEVEL: 3 — Production, last audited 2026-06-27 -->` (was Level 2 Standard+, 2026-06-25).
- [ ] **A6 — Consumer verification**: confirm `apps/web/src/index.css` + `apps/web/tailwind.config.ts` still resolve every token by name. Expected: **zero source edits** (token names preserved). Update `index.css` header comment to cite `DESIGN.dark.md` as the dark-values source (was inline MD table). Run `pnpm --filter @42ch/nexus-contracts run build && pnpm --filter web run typecheck && pnpm --filter web run build` to verify no breakage.

### Track B — Canvas Exploration → Draft (owner: `@architect` B1–B4 + `@product-manager` B5)

- [ ] **B1 — Promote `canvas-strategy-surface.md` status**: Exploration → Draft (header status line + any Exploration-specific caveats removed/refined).
- [ ] **B2 — Interface contracts** (`@architect`): React Flow node/edge schema for the three surfaces (Strategy / outline+timeline / World KB); state model (selection, hover, collapsed sub-flow); sub-flow nesting model (Strategy = graph-of-graphs per `orchestration-engine.md` §3 → outer DAG + inner DAG as React Flow sub-flows); shared canvas shell vs per-surface node types; React Flow in Tauri WKWebView + browser-tab parity.
- [ ] **B3 — Structured write-boundary design** (`@architect`, supersedes retired `body-editor.md` lock framing): node-granular writes only; visualization products must not edit raw files directly (V1.67 §0 Q5 LOCKED); conflict policy vs orchestration's `host_tool_handlers.rs` `body_path` co-write; TipTap as in-node editing capability (not whole-document editor).
- [ ] **B4 — Canvas → DESIGN.md token contract** (`@architect` + `@product-manager`): the canonical minimal credible list of new tokens V1.70 canvas implement will need. Source-of-truth for A4's frontmatter placeholders. Anticipated (final list authored here): node fills/borders/selected, edge/port/grid/minimap, strategy palette extension (purple family), canvas surface background. Each token gets a name + intent description; V1.70 may extend during implement.
- [ ] **B5 — Product/UX section** (`@product-manager`): canvas UX per surface, Idea-input affordance, Strategy terminology adoption (UI/spec wording only; CLI rename deferred), accessibility of a graph surface (keyboard nav, screen-reader semantics for nodes/edges).
- [ ] **`web-ui.md` V1.69 stage amend** (`@product-manager`): "V1.69 Design System Maturation & Canvas Draft" stage section; V1.70 roadmap reaffirmed as canvas implement lead.

## Non-goals

- No React Flow code implement (V1.70).
- No new canvas UI screens (V1.70).
- No `index.css`/`tailwind.config.ts` token-name changes (A6 verifies, does not edit names).
- No wire/contract/DTO change.
- No CLI rename of Preset → Strategy.

## Acceptance criteria

- `apps/web/DESIGN.md` has valid YAML frontmatter containing all current tokens (names verbatim) + `version`/`name`/`description`; completeness tag = Level 3.
- `apps/web/DESIGN.dark.md` exists with the same token names + dark values; DESIGN.md Overview references it.
- DESIGN.md body retains all rule-type sections; pure-value tables compressed with "see frontmatter" pointers.
- Canvas Surface (V1.70 placeholder) section present; B4 token list stubbed in frontmatter.
- `canvas-strategy-surface.md` status = Draft; B2/B3/B4/B5 sections present.
- `web-ui.md` V1.69 stage section present.
- `pnpm --filter web run typecheck && pnpm --filter web run build` pass (zero consumer breakage).
- 4 V1.67 frontend residuals remain open (closed in P1, not P0).

## Validation

- `pnpm --filter @42ch/nexus-contracts run build` (前置依赖)
- `pnpm --filter web run typecheck`
- `pnpm --filter web run build`
- 手工核对：frontmatter token 名与 `index.css`/`tailwind.config.ts` 引用一一对应（无 drift）。
- DESIGN.md 自审：`mstar-design-md` → `references/completeness-checklist.md` Level 3 项逐一过。

## Risks

- **Token-name drift** during frontmatter authoring — mitigation: A6 verification + QC tri-review.
- **B4 token-list over-design** — mitigation: minimal credible list; V1.70 may extend.
- **Body compression loses rule context** — mitigation: A3 KEEP list is explicit; rule-type tables stay; only pure-value duplicates compress.
