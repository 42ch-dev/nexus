# V1.55 P1 — DF-31 Workspace Open / Commit Interface Skeleton

**plan_id**: `2026-06-22-v1.55-df31-workspace-interface`  
**status**: Todo  
**created**: 2026-06-22  
**owner**: `@fullstack-dev` (Track A canonical owner per compass §3 track-ownership pattern)  
**effort**: M  
**working_branch**: `feature/v1.55-df31-workspace-interface` (from `iteration/v1.55`)  
**merge_target**: `iteration/v1.55`  
**type**: implement  
**track**: Theme B / Track A  
**iteration**: V1.55  
**compass**: `v1.55-non-novel-profile-completion-and-infrastructure-refactor-delivery-compass-v1.md`

---

## Goal

Close or materially narrow DF-31 by introducing a local workspace-session interface skeleton for `workspace.open` and `workspace.commit` that future capabilities can build on without redesigning the entire Local API.

## Scope In

- Define skeleton contracts for `workspace.open` and `workspace.commit`.
- Implement local bounds/session scaffolding if needed by current crate boundaries.
- Document conflict behavior (session id, snapshot, commit precondition, stale/conflict response).
- Add tests for path bounds, session lifecycle, and commit conflict rejection.

## Scope Out

- DF-42 full Local API redesign.
- MCP server or external protocol exposure.
- Multi-branch graph runtime / DF-56 implementation.
- Cloud sync or platform publish integration.

## Plan Steps

| ID | Step | Deliverable |
| --- | --- | --- |
| T1 | Read DF-31 tracker row, V1.54 compass §6.2 sketch, home-layout crate rules, and relevant specs | Boundary summary |
| T2 | Run GitNexus impact before editing symbols | Blast-radius evidence |
| T3 | Specify the skeleton contract: open input/output, commit input/output, conflict model | Spec/plan amendment |
| T4 | Implement minimal local session/path-bounds scaffolding | Code + tests |
| T5 | Add rejection tests: path traversal, stale session, conflicting commit, missing workspace | Hermetic tests |
| T6 | Update specs/tracker to mark DF-31 as V1.55 skeleton shipped or narrowed | Tracker/spec evidence |
| T7 | Run scoped verification | `cargo test -p <touched-crate>` plus clippy/fmt as appropriate |

## Acceptance Criteria

- [ ] `workspace.open` skeleton returns a deterministic session/snapshot contract for repo-local paths.
- [ ] `workspace.commit` skeleton rejects stale/conflicting commits rather than silently overwriting.
- [ ] Path bounds are enforced through `nexus-home-layout` or the chosen local workspace root abstraction.
- [ ] No broad `/v1/local/*` endpoint redesign is introduced.
- [ ] Docs state future expansion points without claiming full DF-42.
- [ ] P1 topic branch is merged to `iteration/v1.55` before tri-review.

## Branch Policy

- Create `feature/v1.55-df31-workspace-interface` from current `iteration/v1.55` after P0 is integrated.
- Merge topic branch back to `iteration/v1.55` before QC.
- QC/QA review the integrated branch checkpoint.

## Dependencies

- P0 integrated or PM-approved parallel seam if P0 does not touch the same files.
- `nexus-home-layout` path rules.
- `capability-registry.md` only if the skeleton exposes registry rows.

## Owner / Effort

- **Owner**: `@fullstack-dev` (Track A canonical owner; see compass §3 track-ownership pattern).
- **Effort**: M.

## QC Strategy

After merge to integration: QC tri-review + mid-QA. Review focus: path traversal safety, conflict semantics, and strict no-DF-42 scope creep. Targeted re-review only for changed findings unless PM requests full tri-review.

## Risks

| Risk | Mitigation |
| --- | --- |
| Skeleton turns into full Local API redesign | Compass §1.2 blocks DF-42; P1 only defines session/open/commit seam. |
| Unsafe file writes | Path-bounds and conflict tests are mandatory acceptance. |
| Interface locks future design too early | Keep contract minimal and explicitly mark future extension fields. |

## Completion Notes (2026-06-22)

**Status**: Implemented (skeleton only). Ready for `InReview`. Do NOT mark as Done — PM or QA authority per `mstar-harness-core`.

### Deliverables

| Artifact | Path | Description |
|----------|------|-------------|
| Path validation | `crates/nexus-home-layout/src/lib.rs` | `validate_workspace_path_safe()` + 6 tests |
| Session manager | `crates/nexus-daemon-runtime/src/workspace/session.rs` | `WorkspaceSessionManager` with TTL, conflict detection, 7 tests |
| API handlers | `crates/nexus-daemon-runtime/src/api/handlers/workspace.rs` | `open_workspace`, `commit_workspace` + 9 handler tests |
| Routes | `crates/nexus-daemon-runtime/src/api/mod.rs` | `POST /v1/local/workspace/open`, `POST /v1/local/workspace/commit` |
| State integration | `crates/nexus-daemon-runtime/src/workspace/mod.rs` | `session_manager` field + accessor in `WorkspaceState` |
| Tracker update | `.mstar/knowledge/deferred-features-cross-version-tracker.md` | DF-31 row narrowed to reflect skeleton shipped |

### Verification Evidence

- `cargo test -p nexus-home-layout` — 60/60 pass (6 new + 54 existing)
- `cargo test -p nexus-daemon-runtime` — all pass (9 new handler tests + 16 new session/workspace tests)
- `cargo clippy -p nexus-daemon-runtime -- -D warnings` — clean
- `cargo clippy -p nexus-home-layout -- -D warnings` — clean
- `cargo +nightly fmt --all --check` — clean

### Scope Confirmation

- ✅ `workspace.open` skeleton returns deterministic session/snapshot
- ✅ `workspace.commit` rejects stale/conflicting sessions
- ✅ Path bounds enforced via `nexus-home-layout` validation
- ✅ No broad `/v1/local/*` endpoint redesign (only 2 new workspace routes)
- ✅ No new wire contracts (no `schemas/` changes, no `nexus-contracts` changes)
- ✅ No new DB migration
- ✅ No second truth source
- ✅ Future expansion points documented in code (DF-31 → DF-42 markers)

### Deferred Scope (explicitly NOT in this skeleton)

- File-level OCC (optimistic concurrency with checksums)
- `changes[]` payload implementation
- Persistent session storage
- Cross-daemon session negotiation
- Git-backed workspace sessions
- DF-42 full Local API redesign

### Commits

```
13b1f4b6 feat(home-layout): add validate_workspace_path_safe (DF-31 skeleton)
1be85b5e feat(daemon-runtime): add WorkspaceSessionManager (DF-31 skeleton)
a14cdd88 feat(daemon-runtime): add workspace open/commit API handlers (DF-31 skeleton)
<next> docs: update DF-31 tracker and plan stub (V1.55 P1)
```
