# V1.59 P-last — Hygiene & Closeout

**plan_id**: `2026-06-22-v1.59-hygiene-and-closeout`
**title**: Hygiene & Closeout (P-last)
**iteration**: V1.59
**track**: closeout
**wave**: After Wave 1
**effort**: M
**owner**: `@project-manager`
**status**: Todo
**created**: 2026-06-22
**depends_on**: Both implement plans (P0, P1) integrated + reviewed on `iteration/v1.59`
**primary_spec**: `acp-capability-set.md`, `capability-registry.md`, `outbox-consolidation.md`, `status.json`
**spec_refs**: `acp-capability-set.md`, `capability-registry.md`, `orchestration-engine.md`, `daemon-runtime.md`, `outbox-consolidation.md`, `deferred-features-cross-version-tracker.md`
**branch_policy**: Working branch `feature/v1.59-hygiene-and-closeout` created from `iteration/v1.59` after all implement plans merged; merges back to `iteration/v1.59`.
**review_strategy**: Report-only QA (not tri-review).

---

## Goal

Complete V1.59 iteration closeout: initialize status.json with 5 registered plans, archive DF-12 row from deferred-features-tracker, update DF-46 roster count (18→9 catalog-only), promote `outbox-consolidation.md` from Draft to Master, append shipped-features-tracker V1.59 snapshot, execute tech-debt rollup, apply Profile B compaction, and verify all spec cross-references are consistent. This is a PM-side closeout plan — no product code, no new capability implementations.

---

## Scope

- **T0**: Status.json initialization. Set `metadata.latest_active_iteration = "V1.59"`, `metadata.latest_active_compass = "v1.59-capability-parity-and-outbox-consolidation"`, `metadata.integration_branch = "iteration/v1.59"`, `metadata.pre_implement_gate = "go"`. Register all 5 plans in `status.json.plans[]` with status reflecting their final state (P0 + P1 Done; P-mid Done; P-last Done; P-1 Done). Fix V1.58 staleness (fields `integration_branch_retired`, `latest_ship.merge_commit`). Update `metadata.tech_debt_summary` to reflect post-V1.59 state.
- **T1**: Archive DF-12 row from deferred-features-cross-version-tracker.md §3.3. Remove DF-12 row; append to shipped-features-tracker.md §1 with completion version (V1.59), plan-ids (P1), and closure note. Update §5 "Latest active iteration" to V1.59. Remove V1.59 temporary entry.
- **T2**: Update DF-46 roster count. In `acp-capability-set.md` §4 and deferred-features-tracker §3.3 DF-46 row: update catalog-only count 18→9 (after 9 shipped in P0). Host tool count: 21→30.
- **T3**: Promote `outbox-consolidation.md` from Draft (V1.59 P1) to Master. Update header: `Status: Normative`, `Document class: Master`. Verify all cross-references from `orchestration-engine.md` and `daemon-runtime.md` point to the promoted spec.
- **T4**: Append shipped-features-tracker.md V1.59 delivery snapshot. Record: iteration V1.59, 5 plans, DF-47 9 capabilities shipped, DF-12 closed, host tool count 21→30, wire_contracts_changed=true, key highlights.
- **T5**: Execute tech-debt rollup. Cross-check `status.json.residual_findings` for any residuals filed during P0/P1. Update `metadata.tech_debt_summary` with accurate pre/post counts. Archive closed residual detail prose under `.mstar/archived/residuals/`. Note: 14 V1.52-era WL-A residuals remain deferred to V1.60+.
- **T6**: Profile B compaction. Archive all 5 Done plans: write full plan objects to `.mstar/archived/plans/<plan-id>.json`; append plan_id strings to `.mstar/archived/plans-done.json` `plans` array. Remove Done plan rows from `status.json.plans[]`. Verify layout invariant: `plans-done.json.plans` contains only strings.
- **T7**: Spec consolidation pass. Verify all spec amendments from P0-P1 are reflected in the Master specs (no Draft overlays left behind except those explicitly deferred). Update spec headers where needed. Verify `outbox-consolidation.md` promoted.
- **T8**: Final verification. `cargo clippy --all -- -D warnings` clean on `iteration/v1.59`; `cargo +nightly fmt --all -- --check` clean; `cargo test --all` passes. Run `python3 -c "import json; d=json.load(open('.mstar/archived/plans-done.json')); assert all(isinstance(p, str) for p in d['plans'])"` to verify Profile B invariant.
- **T9**: Report-only QA dispatch. QA engineer verifies status.json consistency, tracker updates, Profile B compaction, and spec promotion completeness. Output: 1 QA report under `.mstar/plans/reports/2026-06-22-v1.59-hygiene-and-closeout/qa.md`.

---

## Acceptance Criteria

- [ ] `status.json`: `latest_active_iteration = "V1.59"`, `integration_branch = "iteration/v1.59"`, 5 plans registered with correct status. V1.58 staleness fixed.
- [ ] `deferred-features-cross-version-tracker.md`: DF-12 row removed from §3.3; DF-46 catalog-only count updated 18→9; §5 reflects V1.59 Shipped.
- [ ] `shipped-features-tracker.md`: V1.59 delivery snapshot appended; DF-12 closure recorded.
- [ ] `outbox-consolidation.md`: promoted from Draft to Master (Status: Normative, Document class: Master).
- [ ] `acp-capability-set.md` §4: DF-46 roster counts updated (27 shipped + 9 catalog-only + 3 scaffold-equivalent + 2 OUT).
- [ ] All spec cross-references from `orchestration-engine.md`, `daemon-runtime.md`, `capability-registry.md` verified consistent.
- [ ] Profile B compaction: 5 plans archived; `plans-done.json` invariant holds (all strings); `status.json.plans[]` empty (or only non-Done plans remain).
- [ ] Tech-debt rollup: residual counts accurate; closed residuals archived; no orphan residuals. 14 V1.52-era WL-A residuals remain deferred.
- [ ] `cargo clippy --all -- -D warnings` clean; `cargo +nightly fmt --all -- --check` clean; `cargo test --all` passes.
- [ ] Report-only QA filed and verdict Pass.

---

## Carry-forwards absorbed

None. P-last is a closeout plan; it absorbs no residuals. Residual absorption is completed in implement plans (P0, P1) before P-last begins.

---

## Spec changes

- `outbox-consolidation.md`: Promote Draft → Master (Status: Normative, Document class: Master, V1.59 P-last).
- `deferred-features-cross-version-tracker.md`: Amend — remove DF-12 row; update DF-46 catalog-only count.
- `acp-capability-set.md`: Amend — update DF-46 roster counts (18→9 catalog-only; 21→30 shipped host tools).
- `shipped-features-tracker.md`: Append — V1.59 delivery snapshot.

---

## Verification

```bash
# Full test suite
cargo test --all

# Full clippy
cargo clippy --all -- -D warnings

# Nightly format check
cargo +nightly fmt --all -- --check

# Profile B invariant
python3 -c "import json; d=json.load(open('.mstar/archived/plans-done.json')); assert all(isinstance(p, str) for p in d['plans'])"

# Status.json consistency
python3 -c "
import json
s = json.load(open('.mstar/status.json'))
assert s['metadata']['latest_active_iteration'] == 'V1.59'
assert s['metadata']['integration_branch'] == 'iteration/v1.59'
print('status.json OK')
"

# No Draft overlays left (except deferred)
grep -r 'Status: Draft' .mstar/knowledge/specs/ | grep -v 'v1.59' || echo "OK: no stale drafts"
```

---

## Risks

- **Profile B compaction may encounter stale plan data**: If implement plans were completed with incomplete `completion_report` or `qc_reports` fields, the archive step may fail. Mitigation: verify each plan object has required fields before archiving.
- **DF-46 roster count drift**: If P0 shipped fewer than 9 capabilities (e.g. a handler proved un-implementable), the roster count must reflect actual shipped count. Mitigation: cross-check `acp-capability-set.md` §4 against actual `host_tool_registry()` entries before updating counts.
- **status.json V1.58 staleness**: V1.58 fields (`integration_branch_retired`, `latest_ship.merge_commit`) may be stale post-merge. Mitigation: T0 fixes these during status.json initialization.

---

## Out of scope

P-last does NOT: write product code, implement new capabilities, touch `schemas/`, create the integration branch (already exists), dispatch implement plans or tri-reviews. It is a closeout + hygiene plan only.
