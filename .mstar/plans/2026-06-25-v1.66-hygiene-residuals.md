# Plan — V1.66 P-sec — Light Hygiene Residuals (Wave 1, Track B)

- **plan_id**: `2026-06-25-v1.66-hygiene-residuals`
- **Iteration**: V1.66
- **Status**: `Todo`
- **Owner**: `@fullstack-dev`
- **Depends on**: P-1 (Prepare locked)
- **Working branch**: `feature/v1.66-hygiene-residuals` from `iteration/v1.66`
- **Compass**: [`v1.66-tauri-desktop-shell-delivery-compass-v1.md`](../iterations/v1.66-tauri-desktop-shell-delivery-compass-v1.md) §1.1 Track B, §3 Wave 1, §6

## Goal

Close the 3 V1.66-targeted residuals (1 medium + 2 low) as a light hygiene companion to the shell work. No new UI features — all three are test / refactor / daemon-side hardening. De-risks the V1.67 body editor's write path (`R-V165-QC-SUGG-DEFENSE`). Very low overlap with P0 (Track A is frontend/apps; this plan is daemon crates + tests).

## Tasks (stub — elaborated at implement)

- [x] T1: `R-V165-QC1-W2` (**medium**) — HTTP-level integration tests for the V1.65 chapter-content Local API surface (`crates/nexus-daemon-runtime` handler tests, fresh DB via `handler_state()`):
  - [x] T1.1: chapter list — cursor pagination (keyset), `items` array key, `next_cursor` semantics
  - [x] T1.2: outline PUT — atomic file write + DB metadata (`outline_path`, `updated_at`) in one transaction; `finalized`/`published` warning behavior
  - [x] T1.3: structure PATCH — status progression (`not_started → outlined`); reverse-transition gating; `finalized`/`published` protection (hard-block delete, confirm-on-structural-edit)
  - [x] T1.4: body GET — read-only markdown; frontmatter surfacing; W-002 path guard on `body_path`
- [x] T2: `R-V165-QC-SUGG-DEFENSE` (low) — write-path defense-in-depth:
  - [x] T2.1: `host_tool_handlers.rs` write-path guard parity with the V1.65 chapter PUT path (W-002 path-guard equivalent on both write surfaces)
  - [x] T2.2: `RuntimeLockGuard` dedup — single canonical lock-guard type (eliminate the duplicated variants)
  - [x] T2.3: `fsync` after the atomic-rename in the reconcile / outline-save pattern (durability on crash)
- [x] T3: `R-V164-QC1-S1-P0` (low) — `PaginationInfo` struct dedup:
  - [x] T3.1: identify the 5 duplicated `PaginationInfo` definitions (`agent_host` / `kb` / `creators` / `memory` / `workspaces` handlers)
  - [x] T3.2: extract a shared canonical `PaginationInfo` type (`nexus_contracts::PaginationInfo`)
  - [x] T3.3: update the 5 handlers to use the canonical type; verify serialized form is **identical** (no wire change — `wire_contracts_changed: false` preserved)
- [x] T4: Data-quality fix — refresh stale `metadata.tech_debt_summary`. Recomputed from `residual_findings`; counts now match actual open/deferred/resolved state.

## Acceptance criteria

- `R-V165-QC1-W2` resolved — chapter HTTP integration tests exist and pass; residual `lifecycle: resolved` with `resolution.commit` + `resolution.plan_id` in `status.json`.
- `R-V165-QC-SUGG-DEFENSE` resolved — write-path guard parity + `RuntimeLockGuard` dedup + post-rename `fsync` landed; tests green; residual resolved in SSOT.
- `R-V164-QC1-S1-P0` resolved — single canonical `PaginationInfo`; serialized form unchanged (no wire drift — confirmed via existing contract tests); residual resolved in SSOT.
- `cargo test -p nexus-daemon-runtime --test <chapter-tests>` + affected crate tests green; `cargo clippy -p <affected> -- -D warnings` clean; `cargo +nightly fmt --all --check` clean.
- `metadata.tech_debt_summary` no longer stale.

## Open items

- T2.1 + P0 T5 (`openWith` scope-whitelist) coordinate on the shared W-002 path-guard pattern — PM ensures the two plans don't diverge.
- T3.2 canonical `PaginationInfo` location (contracts crate vs shared daemon module) — architect advises at implement (minor).
