# P1 — Frontend Hygiene (4 V1.79-QC residuals)

**Iteration**: V1.80 · **Plan ID**: `2026-07-01-v1.80-frontend-hygiene-residuals`
**Track**: B · **Owner**: `@frontend-dev` · **Status**: Todo
**Working branch**: `feature/v1.80-frontend-hygiene` (from `iteration/v1.80`)
**Closes residuals**: `R-V179P0-QC1-001`, `R-V179P0-QC1-002`, `R-V179P1-QC1-001`, `R-V179P1-QC1-002`

## Problem statement

V1.79 (Author Reflection) shipped two new frontend surfaces (Track A reading surface + Track B SOUL viz) under QC 3/3 Approve, but deferred four low/nit hygiene residuals targeting V1.80+:

- **R-V179P0-QC1-001** — reading-surface keyboard-nav interaction-test coverage + the `hasOpenOverlay` DOM-query guard (`[role=menu]:not([hidden])` / `[role=dialog]:not([hidden])`) is robust for current overlays but should be revisited if future overlays use a different role pattern; add keyboard-nav interaction tests.
- **R-V179P0-QC1-002** — reading-surface component-level unit-test coverage gap + minor nits (`defensive ?? 1` on volume; reading-progress debounce design comment).
- **R-V179P1-QC1-001** — `apps/web/src/pages/memory-page.tsx` is a 360-line module exceeding the ≤250-line discipline (314 pre-V1.79 + 46 from the P1 SOUL additions); extract `PendingReviewsSection` / `FragmentsSection` into `apps/web/src/components/memory/` siblings.
- **R-V179P1-QC1-002** — SOUL `temporal-drift.tsx` hardcodes slots 1–5 BAND_PALETTE as RGBA while slot 0 uses `var()`; promote to DESIGN.md tokens. Also remove the unused `driftDateHelper` alias.

These are not regressions; they are test-coverage / token-discipline / module-size hygiene that should not be left to accumulate.

## specify

- **User value**: A frontend maintainer can change the reading surface, SOUL viz, and memory page with regression-test confidence and without hardcoded palette / god-module drift.
- **Scope**: `apps/web/src/components/reading/*`, `apps/web/src/components/soul/temporal-drift.tsx`, `apps/web/src/pages/memory-page.tsx`, `apps/web/DESIGN.md` (token addition for BAND_PALETTE).
- **Non-goals**: any backend / wire / crate change (P0 owns those); redesigning the reading surface or SOUL viz visuals; BL-11/12 deeper reading / SOUL refinements (backlog).
- **DoD**: all four residuals closed in `status.json`; QC 3/3 Approve; QA Pass; web typecheck/test green.

## clarify

Locked by Review & Edit for architecture/file-boundary purposes:

- **Q1 — Keyboard-nav test depth**: interaction-level coverage for ←/→ navigation plus the overlay-guard short-circuit. This matches the residual text and should live in reading-surface tests, not in P0 memory API tests.
- **Q2 — memory-page split boundary**: extract `PendingReviewsSection`, `FragmentsSection`, and `SoulSection`/`SoulVizSection` (if present at implementation time) into `apps/web/src/components/memory/` siblings. `apps/web/src/pages/memory-page.tsx` remains the route/page shell: active creator lookup, page-level state that coordinates section filters, Card layout, and section composition. It must not absorb P0's `useReviewMemory` drain-loop logic; P0 owns `apps/web/src/api/queries.ts`.
- **Q3 — BAND_PALETTE token shape**: use an ordered token set in the existing V1.79 `soul-viz-*` token family (`apps/web/DESIGN.md`, mirrored in `DESIGN.dark.md`) that maps directly to band index slots and is consumed as CSS variables in `temporal-drift.tsx`. Do not leave hardcoded RGBA values in component code. If the CSS variable bridge is missing, add it alongside the token definition before changing the component.

## plan (sketch)

1. **R-V179P0-QC1-001** — add reading-surface keyboard-nav interaction tests (←/→ chapter/volume nav + overlay-guard short-circuit when a menu/dialog is open); document the `hasOpenOverlay` role-pattern assumption in a comment.
2. **R-V179P0-QC1-002** — add reading component unit tests; address the `defensive ?? 1` on volume + reading-progress debounce design comment.
3. **R-V179P1-QC1-001** — extract `PendingReviewsSection` and `FragmentsSection` (plus `SoulSection`/`SoulVizSection` if present) into `apps/web/src/components/memory/`; reduce `memory-page.tsx` to within the size discipline while leaving API mutation/drain semantics in `apps/web/src/api/queries.ts` untouched for P0.
4. **R-V179P1-QC1-002** — promote BAND_PALETTE to DESIGN.md tokens; replace hardcoded RGBA with `var()`; remove unused `driftDateHelper` alias.

## tasks (sketch — PM to formalize at plan lock)

- T1 — R-V179P0-QC1-001 keyboard-nav interaction tests + guard comment
- T2 — R-V179P0-QC1-002 component unit tests + defensive-coding nits
- T3 — R-V179P1-QC1-001 memory-page split
- T4 — R-V179P1-QC1-002 BAND_PALETTE token promotion + dead-alias cleanup

## Wire contracts note

None. P1 is `apps/web`-only; no schema / codegen / crate change.
