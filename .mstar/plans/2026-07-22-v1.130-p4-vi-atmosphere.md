# P4 — VI atmosphere Chronos lock

> **Iteration compass:** `.mstar/iterations/v1.130/delivery-compass.md`  
> **Spec:** `.mstar/iterations/v1.130/specs/vi-atmosphere-chronos.md`  
> **SSOT status:** `.mstar/status.json`  
> **Wave:** 1∥ · **blocked_by:** — · **Merge before P1 App paint**

**Goal:** Kill office navy; keep cyan; lock Chronos atmosphere into DESIGN + tokens.

**User value:** The shell stops reading as traditional office software. Author feels 生机 / 神秘 / 时间流 on open, while the cyan accent they trust stays put. Visual identity matches the creative-writing product, not a spreadsheet.

**Architecture:** Token name stable; hex + dark casts re-hue; Studio gallery first.

**Execution:** mstar-sdd

### Wire contracts

- **Verdict:** `wire_contracts_changed: false`

## Prepare gates

- specify: [done]
- clarify: [done] author Must + T1 default
- plan: [locked] Seat 2 locked Chronos as compile-time token data, Studio-first projection order, and light/dark AA gates

## Architecture lock

- **Runtime model:** T1 Chronos replaces values behind existing token names. No runtime atmosphere id, preference, storage key, provider branch, feature flag, or switcher is allowed. T2 Umbra/T3 Aurora are Studio-local comparison data only.
- **Projection order:** Studio T1–T3 shell fixture → locked T1 values in `DESIGN.md` / `DESIGN.dark.md` → `packages/nexus-ui/src/tokens.ts` + `theme.css` brand mirror → `tooling/design-tokens/src/tokens.css` / Tailwind projection → Studio Tokens gallery → App smoke.
- **Value invariants:** preserve `brand-cyan: #25D1E0`; retain public token names (`brand-deep-blue*`, `blue-*`) while replacing the office-navy values/casts; remove literal `#1E3A5F` from product shell chrome and runtime brand projections.
- **Contrast:** recompute every changed text/fill, focus, active, logo, and shell-chrome pairing in both themes. Normal text ≥4.5:1; large text/graphical affordances ≥3:1; cyan remains accent-only on light surfaces unless the measured pairing passes.
- **Promotion classification:** shell specimens are Studio fixtures consuming `@web-layout/*`; no new `@42ch/nexus-ui` primitive promotion. Package edits only keep the existing public brand token mirror in sync.

## Tasks

- [ ] **T1 · Must:** Studio T1–T3 swatches on the same shell-chrome fixture, light+dark → **Interfaces:** Studio-local candidate data + `@web-layout/*`; no runtime exports → **DoD:** T1 Chronos / T2 Umbra / T3 Aurora are visually comparable; T2/T3 cannot be selected or persisted by the product
- [ ] **T2 · Must:** Lock T1 Chronos into DESIGN pair and existing package brand mirror → **Interfaces:** stable token names; `@42ch/nexus-ui` token constants/theme CSS → **DoD:** DESIGN pair names T1 as compile-time default; package mirror matches; cyan stays exactly `#25D1E0`; no runtime switcher state exists
- [ ] **T3 · Must:** Project locked values through design-tokens and Tokens gallery; verify AA → **Interfaces:** `tokens.css`, Tailwind preset only if mappings change, Studio Tokens inventory → **DoD:** new swatches render in both themes; changed pairings are measured/documented; no token is added without gallery coverage
- [ ] **T4 · Must:** Smoke App/Studio consume one locked token chain → **DoD:** no `#1E3A5F` in product shell chrome or runtime brand projections; no per-app override; UI guardrails + App/Studio build/test gates pass; compass AC VI section green

## Roadmap / deferred scope

- Target: T1 Chronos as compile-time product default
- Later: Umbra/Aurora tune or runtime switcher evaluation — V1.131; owner: product-manager

## Working branch

`plan/v1.130-p4-vi-atmosphere` → `iteration/v1.130`
