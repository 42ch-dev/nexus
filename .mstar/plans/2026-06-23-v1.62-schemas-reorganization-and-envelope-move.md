---
plan_id: 2026-06-23-v1.62-schemas-reorganization-and-envelope-move
title: V1.62 P0 — Schemas Reorganization + Envelope Move + Boundary Docs
iteration: V1.62
slot: P0
status: Todo
effort: XL
owner: "@fullstack-dev"
review: Tri-review + mid-QA
wave: 1
dependency: P-1 PM signoff
working_branch: feature/v1.62-schemas-reorganization
merge_target: iteration/v1.62
---

# P0: Schemas Reorganization + Envelope Move + Boundary Docs

Reorganize `schemas/` along consumer-scope lines (`common/` + `domain/` + `platform/{http-bff,sync}/` + `local-api/compute/`), delete per-module `entity-attributes`/`entity-state` schemas, run codegen cascade, rewrite 4 boundary docs. Foundation for all downstream V1.62 work.

Full task list in compass §1.1 P0 (T1–T15). Key deliverables:

## Tasks

- [x] T1: Move 33 files `schemas/platform/*.json` → `schemas/platform/http-bff/*.json`
- [x] T2: Move 3 sync-payload files `schemas/domain/{bundle,delta,sync-command}.json` → `schemas/platform/sync/*.json` (keep canonical names)
- [x] T2 (cont.): Move 4 `schemas/cloud-sync/*.json` → `schemas/platform/sync/*.json` (rename `bundle.schema.json` → `bundle-refinement.schema.json` to avoid collision; keep `sync-pull-request`/`sync-pull-response`/`conflict-response` names)
- [x] T2.1: Update `tooling/codegen/src/schema-loader.ts` `SKIP_STRUCT_GENERATION_REL_PATHS` for `platform/sync/bundle-refinement` (was `cloud-sync/bundle`)
- [x] T3: Delete empty `schemas/cloud-sync/`
- [x] T4: Create `schemas/local-api/compute/`; move `schemas/compute/{compute-input,compute-output}.schema.json` into it
- [x] T5: Delete `schemas/compute/{compute-entity-attributes,compute-entity-state}.schema.json` (per-module content migrates to manifest.json in P1)
- [x] T6: Delete empty `schemas/compute/`
- [x] T7: Update all `$id` URIs in moved files
- [x] T8: Update all `$ref` URIs across `schemas/` to point at new paths
- [x] T9: Update `tooling/codegen/src/schema-loader.ts` path constants + `SKIP_STRUCT_GENERATION_REL_PATHS`
- [x] T10: `pnpm run codegen` → regenerate Rust (`crates/nexus-contracts/src/generated/`) + TypeScript (`packages/nexus-contracts/src/generated/`)
- [x] T11: Update `crates/nexus-contracts/tests/schema_drift_detection.rs` `build_schema_map()` entries
- [x] T12: `./tooling/check-wire-drift.sh` + `cargo test --test schema_drift_detection` → fix drift
- [x] T13: `cargo check -p nexus-contracts` + `cargo clippy -p nexus-contracts -- -D warnings`
- [x] T14: Update consumer imports (e.g. `nexus_cloud_sync` imports of bundle/delta types now under `generated::platform::sync::*`); grep for `generated::compute::` → `generated::local_api::compute::` etc.
- [x] T15: Rewrite boundary docs:
  - `.mstar/knowledge/specs/schemas-directory-layout.md` — reflect new tree (§1 normative tree, §2 folder↔product line table, §3 subdirectory contracts, §7 inventory)
  - `.mstar/knowledge/schemas-wire-platform-sync-boundary.md` → rename to `schemas-external-consumer-boundary.md`; expand boundary rule to "consumed by any external client (platform OR local-api client like future WebApp)"; add `schemas/local-api/` row; update all in-repo links
  - `schemas/AGENTS.md` — update "Not in `schemas/`" rule; add codegen note for `local-api/`
  - `schemas/README.md` — update layout table, product-lines table, removed-paths list
  - Create per-folder READMEs: `schemas/platform/http-bff/README.md`, `schemas/platform/sync/README.md`, `schemas/local-api/compute/README.md`

## Done when

- New tree shape matches compass §1.3 diagram exactly (42 files: 3 common + 10 domain + 33 http-bff + 7 sync + 2 local-api/compute; entity-attributes + entity-state deleted)
- `pnpm run validate-schemas` passes on new tree
- `pnpm run codegen` produces Rust + TS types under correct new module paths
- `./tooling/check-wire-drift.sh` PASS
- `cargo check -p nexus-contracts` + `cargo clippy -p nexus-contracts -- -D warnings` PASS
- All consumer imports updated (no stale `generated::compute::` / `generated::cloud_sync::` references)
- Boundary docs rewritten; old `schemas-wire-platform-sync-boundary.md` archived with pointer stub
- Per-folder READMEs created

## Coordination notes

- P2 T3 amends `schemas-directory-layout.md` post-merge to "seal" the spec; coordinate via P-mid to avoid double-edit conflicts (P0 owns the code-driven rewrite; P2 owns the spec-seal language upgrade).
- Do NOT touch `.mstar/knowledge/specs/{compute-module-abi,wasm-host}.md` — those are P2 territory.
- Do NOT touch `crates/nexus-wasm-host/src/` or `modules/basic-combat/manifest.json` — those are P1 territory.
