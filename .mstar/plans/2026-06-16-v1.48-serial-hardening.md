# V1.48 Serial Hardening — Plan

**Plan ID**: `2026-06-16-v1.48-serial-hardening`  
**Title**: P4 — §4.5.7 #4 resume + #5 reconcile (+ optional intake re-trigger)  
**Created**: 2026-06-16  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.48-serial-hardening`  
**Merge target**: `iteration/v1.48`  
**Depends on**: `2026-06-16-v1.48-harness-docs-prepare` (merged)  
**Parallel with**: `2026-06-16-v1.48-findings-producer` (S-B — no hard dep on P1)  
**Iteration compass**: [v1.48-novel-quality-loop-completion-delivery-compass-v1.md](../iterations/v1.48-novel-quality-loop-completion-delivery-compass-v1.md)  
**Primary specs**:

- [novel-writing/workflow-profile.md](../knowledge/specs/novel-writing/workflow-profile.md) §4.5.7 items #4–#5, §4.5.3
- [archived/knowledge/novel-findings-maturity.md](../archived/knowledge/novel-findings-maturity.md) §6 Serial cross-ref

**Target residuals**: `R-V147P1-01` (if capacity)

---

## 1. Problem Statement

V1.47 P2 implemented §4.5.7 tests #1–#3. Tests #4 (resume draft row) and #5 (`reconcile-chapters`) remain unimplemented. Authors lack reliable recovery when filesystem and DB diverge or when resuming mid-chapter.

---

## 2. Goals

1. Hermetic test #4: resume run against Work with one `draft` row does not create duplicate row.
2. Hermetic test #5: `creator run reconcile-chapters <work_id>` rebuilds missing `work_chapters` from `Stories/` with DB-as-status-SSOT conflict rules (§4.5.3).
3. Implement or harden reconcile command behavior if gaps found in baseline review.
4. **Optional (capacity)**: R-V147P1-01 — document/implement intake re-trigger path for existing Work (`creator bootstrap` vs new Work only).

---

## 3. Non-goals

- §4.5.7 #1–#3 (shipped V1.47).
- Multi-volume migration (shipped V1.42).
- Findings chain (Track A).

---

## 4. Acceptance Criteria

1. Tests #4 and #5 pass in CI hermetic suite per spec semantics.
2. Reconcile preserves DB status when file/DB conflict per §4.5.3.
3. R-V147P1-01 closed or explicitly deferred to V1.49 in plan completion report.

---

## 5. Tasks

- [x] T1: Baseline review `reconcile-chapters` and resume selection paths.
- [x] T2: Test #4 resume behavior.
- [x] T3: Test #5 reconcile + any implementation fixes.
- [x] T4: Optional R-V147P1-01 spike/implement. **Deferred to V1.49** — see §8.
- [x] T5: Residual disposition. `R-V147P1-01` remains open (deferred to V1.49); no residuals closed in this plan.

---

## 7. Baseline Review Notes (T1)

**Gaps relative to spec §4.5.7 #4–#5 and §4.5.3:**

1. `reconcile_from_filesystem` (`crates/nexus-local-db/src/work_chapters.rs`) currently treats `Stories/` frontmatter `status` as authoritative for existing rows: when file `status` differs from the DB row, it **overwrites** the DB status. This violates novel-workflow-profile §4.5.3 (“`work_chapters` is the queryable source of truth … the DB row wins”).
2. The reconcile routine does not re-sync the file frontmatter back to the DB status, so prompt-visible state can remain stale after reconcile.
3. Resume selection (`next_chapter` / `next_chapter_volume_aware`) is already correct: it returns the lowest active chapter including `draft`, so a single `draft` row is resumed. The missing coverage is a hermetic acceptance test that asserts no duplicate row is created and the same row is selected.
4. CLI `creator works reconcile-chapters <work_id>` and the daemon `POST /v1/local/works/{work_id}/reconcile-chapters` handler delegate fully to `reconcile_from_filesystem`; the gap is therefore in the DB-layer routine, not the command wiring.

---

## 6. Verification

```bash
cargo test -p nexus-local-db -- v148_serial 2>&1 | tail -30
cargo test -p nexus42 -- reconcile 2>&1 | tail -20
```

---

## 8. Deferred Scope

## 9. P4-fix1 Fix Wave (QC re-review)

Targeted fix wave for QC tri-review warnings on P4.

- [x] W-1 (qc2): `sync_frontmatter_status` writes atomically (write-temp + rename).
- [x] W-1 (qc3): `ReconcileReport.resynced` field added; status-conflict path increments it; CLI/daemon/JSON updated.
- [x] W-2 (qc3): Daemon `reconcile_chapters` handler releases `RuntimeLockGuard` on error paths.
- [ ] W-2 (qc2): `creator works reconcile-chapters` confirmation/dry-run surface — **deferred** to V1.49 UX plan (low).
- [ ] W-3 (qc3): `reconcile-chapters` synchronous lock-held scaling ceiling — **deferred** to V1.49 (medium).

Suggestions (S-1, S-2, S-3) acknowledged as non-blocking.

---

## 8. Deferred Scope

### R-V147P1-01 — intake re-trigger on existing Work

**Disposition**: Deferred to V1.49.

**Reason**: The V1.48 P4 scope is focused on §4.5.7 #4/#5 serial hardening. R-V147P1-01
("intake re-trigger on existing Work") is a standalone CLI/daemon surface gap that would
require a new command path or documented workflow (`creator run creative-brief-intake
<work_id>` vs `creator bootstrap` always minting a new Work). Implementing it safely
would need UX review of the intake preset gate behavior and potentially new CLI IA in
`creator-run-preset-entry.md`, which exceeds the current slice capacity.

**Next step**: Evaluate in V1.49 P0 whether to (a) document the existing `creator run
creative-brief-intake <work_id>` path, (b) add a dedicated `creator works intake <work_id>`
command, or (c) extend `creator bootstrap` with an `--on-work-id` flag.
