# P0 — Studio UI Tune (Visual + Coverage)

**Goal:** Fix Design Studio paint/IA regressions (FB-000..011) and close Studio↔App coverage gaps (FB-012..016) under the studio-first invariant.

**Architecture:** Studio-first primitives in `@42ch/nexus-ui`; app-shared setup modules under `@web-setup/*`; new gallery aliases `@web-layout/*` (shell chrome) and `@web-settings/*` (Settings presentational extracts). Tailwind `content` scans setup + layout/presentational + nexus-ui sources. Toast: thin re-export from package. Shell/Settings fixtures import presentational SSOT — no duplicate stub markup.

**Interfaces:**
- Studio Tailwind: `apps/design-studio/tailwind.config.ts` (+ `vite.config.ts` / `tsconfig.json` aliases)
- Badge/Button/Select: `packages/nexus-ui/src/components/{badge,button,select}.tsx`
- Workspace field: `apps/web/src/components/setup/workspace-path-field.tsx` → `@web-setup/workspace-path-field`
- AgentPicker / TopStepIndicator: `apps/web/src/components/setup/{agent-picker,top-step-indicator}.tsx`
- Wizard pages: `apps/web/src/pages/setup-step-{agent,workspace}.tsx`; Studio `setup-wizard-chrome-fixtures.tsx`
- Toast: `@42ch/nexus-ui` via thin `apps/web/src/lib/use-toast.tsx` re-export
- Shell chrome: `apps/web/src/components/layout/presentational/*` → `@web-layout/*`
- Settings chrome: `apps/web/src/components/settings/presentational/*` → `@web-settings/*`
- Studio fixtures: `apps/design-studio/src/fixtures/*`, `apps/design-studio/src/pages/surfaces.tsx`

**Implementer SSOT:** `.mstar/iterations/v1.107/specs/studio-ui-tune.md`

**Tech Stack:** Design Studio, `@42ch/nexus-ui`, apps/web; no wire/schema.

**Execution:** mstar-sdd when implement starts

**Tier:** **Must (P0)**

## Prepare Gates

- specify: **done** (§5.1 PM — iteration spec product-complete)
- clarify: **done** (§5.1 PM — grill-me locked)
- plan: **done** (§5.1 PM — tasks + acceptance criteria product-complete)
- architect: **done** (§5.2 — architecture contracts in primary spec)
- writing: **done** (§5.3 — Voice & Content, corpus hygiene, promotion-boundary Toast prose)

## Product Framing

**User value:** Studio matrices show real chrome; wizard/Settings feel consistent; contributors trust Studio before App wiring; Toast and shell Surfaces stop drifting.

**Who:** Authors (first-launch, Settings, Control Room) and UI contributors (Design Studio).

**Non-goals:** DF-70 BYOK; package-promoting Dialog/Tabs/Table/States; full author-domain Surfaces routes (backlog index only in 016).

## Clarify (locked from grill-me)

| Topic | Decision |
|-------|----------|
| Plan shape | Single Must P0; FB-000..016 seeded |
| Select | Native + appearance-none + custom chevron (no Radix) |
| Wizard custom launch | Hidden (`showCustomLaunch={false}`) on wizard only |
| Workspace field | Shared `workspace-path-field.tsx`; label **Workspace folder**; CTA **Change Folder…** |
| Toast | Thin re-export from `@42ch/nexus-ui`; promotion-boundary Toast + lucide footnote |
| Shell SSOT | Presentational extract + `@web-layout/*` alias (bounded blast radius) |
| Settings dedup | `@web-settings/*` presentational imports + `@web-setup/workspace-path-field` |
| ConnectDaemon matrix | `firstUse` / `reconnectMatch` / `fingerprintMismatch` / `loopbackOnly` |
| Outbound links | Lucide `ArrowUpRight` inside `<a>` (not CSS `::after`) |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria

1. **FB-000:** Studio content includes setup + nexus-ui sources; Steps/Badge solid/Button destructive paint after rebuild (light+dark).
2. **FB-001..011:** Each visual FB Studio-accepted then App-mirrored per primary spec; wizard IA locked (single-column compact, no custom launch, shared workspace field).
3. **FB-012:** App uses `@42ch/nexus-ui` Toast; duplicate `use-toast` removed or thin re-export; tests green.
4. **FB-013..015:** Shell/Settings fixture hygiene landed or residual-deferred with evidence.
5. **FB-016:** Domain badge matrices + author Surfaces backlog index with promotion triggers.
6. Prefer no schemas change (`wire_contracts_changed: false`).

## Feedback items (summary)

| ID | User-visible outcome |
|----|----------------------|
| FB-000 | Studio matrices paint — Steps, Badge solid, Button destructive visible |
| FB-001 | Status pills distinguish hues at a glance |
| FB-002 | Destructive/disabled buttons read correctly |
| FB-003 | Select chevron has right inset |
| FB-004 | MainBanner title dominates description |
| FB-005 | Wizard step indicator circles/connectors visible |
| FB-006 | Wizard Agent one card per row |
| FB-007 | Wizard hides custom-launch pitch |
| FB-008 | Workspace field matches Settings (**Workspace folder** / **Change Folder…**) |
| FB-009 | Outbound links spaced with ArrowUpRight icon |
| FB-010 | Status dots: filled vs hollow green |
| FB-011 | Uninstalled cards muted with badge near title |
| FB-012 | App Toast unified with package |
| FB-013 | Studio shell matches App Creator/Orchestrator IA |
| FB-014 | Footer profiles + header health states in Studio |
| FB-015 | Settings fixtures import App chrome; ConnectDaemonForm matrix |
| FB-016 | Domain badge matrices + Surfaces backlog indexed |

## Global Constraints

- Primary spec: `.mstar/iterations/v1.107/specs/studio-ui-tune.md`
- Compass: `.mstar/iterations/v1.107/delivery-compass.md`
- Studio-first: required for all visual FBs (Studio accept before App parity)
- `wire_contracts_changed: false`

---

### Task 1: Studio Tailwind content coverage (FB-V1107-000)

**User outcome:** Studio matrices and wizard chrome render real paint, not invisible utilities.

**Files:**
- Modify: `apps/design-studio/tailwind.config.ts`

- [ ] Add `../web/src/components/setup/**/*.{ts,tsx}`
- [ ] Add `../web/src/components/layout/presentational/**/*.{ts,tsx}`
- [ ] Add `../../packages/nexus-ui/src/**/*.{ts,tsx}`
- [ ] Smoke: Steps circles, Badge solid fills, Button destructive visible in Studio (light+dark)

### Task 2: Badge Soft/Solid contrast (FB-V1107-001)

**User outcome:** Authors distinguish status by hue in Studio and App.

**Files:**
- Modify: `packages/nexus-ui/src/components/badge.tsx`
- Modify: `DESIGN.md` / `DESIGN.dark.md` if tokens change
- Modify: badge tests (effective styles where feasible)

- [ ] Soft six hues distinct light+dark
- [ ] Solid six fills + AA text
- [ ] Studio matrix smoke (light+dark)

### Task 3: Button destructive + Disabled (FB-V1107-002)

**User outcome:** Destructive actions look dangerous; disabled buttons clearly inactive.

**Files:**
- Modify: `packages/nexus-ui/src/components/button.tsx`
- Modify: DESIGN button tokens if needed
- Verify: Settings Re-run Setup confirm footer

- [ ] Destructive visible light+dark
- [ ] Disabled clearly muted (esp. primary)
- [ ] Components + Settings confirm smoke

### Task 4: Select chevron inset (FB-V1107-003)

**User outcome:** Select chevrons have breathing room from right border.

**Files:**
- Modify: `packages/nexus-ui/src/components/select.tsx`
- Modify: DESIGN `components.select` if needed

- [ ] `appearance-none` + custom chevron with right inset
- [ ] Closed/disabled/invalid Studio smoke
- [ ] Native select only (no Radix)

### Task 5: MainBanner hierarchy (FB-V1107-004)

**User outcome:** Banner title dominates; description reads as secondary.

**Files:**
- Modify: `apps/design-studio/src/fixtures/main-banner-fixtures.tsx`
- Modify: `apps/web/src/components/layout/main-banner.tsx`

- [ ] Description → `text-gray-700`
- [ ] Studio then App match (light+dark)

### Task 6: TopStepIndicator chrome (FB-V1107-005)

**User outcome:** Wizard progress circles, connectors, and labels visible.

**Files:**
- Depends on Task 1; verify `top-step-indicator.tsx` utilities paint
- Fix tokens/classes only if still broken after 000

- [ ] Circles + connectors + labels visible Studio + App

### Task 7: Wizard AgentPicker single column (FB-V1107-006)

**User outcome:** Wizard shows one agent card per row in portrait shell.

**Files:**
- Modify: `apps/web/src/components/setup/agent-picker.tsx`

- [ ] `density=compact` → `grid-cols-1` only
- [ ] Settings default density unchanged

### Task 8: Hide wizard custom launch (FB-V1107-007)

**User outcome:** Wizard Agent step focuses on agent selection only.

**Files:**
- Modify: `apps/web/src/pages/setup-step-agent.tsx`
- Modify: Studio `setup-wizard-chrome-fixtures.tsx`
- Update helper copy

- [ ] `showCustomLaunch={false}` on wizard path
- [ ] Continue requires agent selection only
- [ ] Settings keeps custom launch

### Task 9: Workspace path field reuse Settings (FB-V1107-008)

**User outcome:** Wizard workspace step matches Settings field pattern.

**Files:**
- Add: `apps/web/src/components/setup/workspace-path-field.tsx`
- Modify: `apps/web/src/pages/setup-step-workspace.tsx`, `apps/web/src/pages/settings/settings-workspace-section.tsx`
- Modify: Studio `setup-wizard-chrome-fixtures.tsx` (`WorkspaceBody` imports field)

- [ ] Shared label **Workspace folder** + CTA **Change Folder…**
- [ ] No in-row wizard label chrome
- [ ] a11y label↔input association

### Task 10: AgentPicker outbound links (FB-V1107-009)

**User outcome:** Install/Docs links clearly separated with outbound icon.

**Files:**
- Modify: `apps/web/src/components/setup/agent-picker.tsx` `OutboundLink`

- [ ] Lucide `ArrowUpRight` inside each `<a>`
- [ ] Visible gap between Install and Docs

### Task 11: AgentPicker status dots (FB-V1107-010)

**User outcome:** Selection state visible via green dots on agent cards.

**Files:**
- Modify: `agent-picker.tsx` `StatusDot` if needed after 000

- [ ] Selected: filled green; Unselected installed: hollow green
- [ ] Studio + App smoke

### Task 12: Uninstalled AgentCard chrome (FB-V1107-011)

**User outcome:** Uninstalled agents look muted with badge near title.

**Files:**
- Modify: `agent-picker.tsx` `AgentCard` / `AgentCardIdentity`

- [ ] Remove bottom “Not installed” text
- [ ] Gray soft Badge near title
- [ ] Muted card surface

### Task 13: App adopts package Toast (FB-V1107-012)

**User outcome:** Same Toast implementation in Studio and App; closes `R-V1106P0-001`.

**Files:**
- Modify: `apps/web/src/lib/use-toast.tsx` (thin re-export from `@42ch/nexus-ui`)
- Modify: `apps/web/src/main.tsx`, `apps/web/src/test/test-providers.tsx` (may keep `@/lib/use-toast` import path)
- Modify: `.mstar/iterations/v1.99/specs/component-promotion-boundary.md` (Toast promoted + lucide runtime dep footnote)

- [ ] Delete duplicate toast implementation body; re-export package symbols
- [ ] Tests green via re-export path; Studio + App same package

### Task 14: Sidebar/shell Surfaces SSOT (FB-V1107-013)

**User outcome:** Studio `/surfaces/shell` previews real Creator/Orchestrator IA.

**Files:**
- Add: `apps/web/src/components/layout/presentational/shell-sidebar-chrome.tsx`
- Modify: `apps/web/src/components/layout/sidebar.tsx` (thin wrapper)
- Add Studio alias `@web-layout/*` in `apps/design-studio/{vite.config.ts,vitest.config.ts,tsconfig.json}`
- Replace inline `AppShellFixture` in `apps/design-studio/src/pages/surfaces.tsx`
- Update `apps/design-studio/AGENTS.md` import table + `tooling/check-ui-guardrails.sh` allowlist

- [ ] Creator + Orchestrator IA match App via `ShellSidebarChrome` props
- [ ] Studio `/surfaces/shell` smoke (light+dark)

### Task 15: FooterProfiles + Header/health fixtures (FB-V1107-014)

**User outcome:** Profile switcher and health indicator states previewable in Studio.

**Files:**
- Add: `apps/web/src/components/layout/presentational/footer-profiles-chrome.tsx`
- Add: `apps/web/src/components/layout/presentational/daemon-health-indicator-chrome.tsx`
- Modify: `footer-profiles.tsx`, `daemon-health-indicator.tsx` (thin wrappers)
- Add Studio fixtures importing `@web-layout/*` chrome components

- [ ] Props-driven states: profiles 0/1/N; health `unknown` / `connected` / `offline` (+ remote badge variant)
- [ ] Light+dark smoke

### Task 16: Settings host dedup + ConnectDaemonForm (FB-V1107-015)

**User outcome:** Settings Surfaces show real Connection chrome; smaller fixture file.

**Files:**
- Add: `apps/web/src/components/settings/presentational/connect-daemon-form-chrome.tsx`
- Add: `apps/web/src/components/settings/presentational/settings-setup-section-chrome.tsx`
- Add Studio alias `@web-settings/*` in `apps/design-studio/{vite.config.ts,vitest.config.ts,tsconfig.json}`
- Shrink `apps/design-studio/src/fixtures/settings-host-fixtures.tsx` (import `@web-settings/*`, `@web-setup/workspace-path-field`, `@web-setup/agent-picker`)

- [ ] ConnectDaemonFormChrome four-state matrix: `firstUse`, `reconnectMatch`, `fingerprintMismatch`, `loopbackOnly`
- [ ] Fixture file substantially smaller

### Task 17: Domain badges + author Surfaces backlog (FB-V1107-016)

**User outcome:** Domain badges previewable; next Surfaces pilots have clear triggers.

**Files:**
- Studio `/components` domain badge matrices
- `v1.107/specs/author-surfaces-backlog.md`

- [ ] Status/Chapter/Finding/TaskKind matrices (light+dark)
- [ ] Backlog index with owners/triggers (does not block close)

## Review Gate Summary

**QC mode:** full tri-review (N=3) — SDD
**Initial wave:** Request Changes (2/3) — F-CRIT-001 CI blocker (dead imports + @web-ui/dialog alias leak)
**Fix wave:** commit `71a7a2ea` — dead imports removed, Radix Dialog hoisted, Button snapshot regenerated
**Targeted re-review:** QC1+QC2 Approve; QC3 Approve (unchanged)
**Final verdict:** Approve (3/3)
**Residuals:** R-V1107QC1-W002 (low), R-V1107QC1-ARCH (nit)
**Consolidated report:** `.mstar/sdd/2026-07-10-v1.107-studio-ui-tune/review/qc-consolidated.md`

## QA Gate Summary

**Verdict:** Pass with residuals (see `.mstar/sdd/2026-07-10-v1.107-studio-ui-tune/review/qa-report.md`)

- All automated verification reproduced (nexus-ui 123/123, web 614/614, design-studio 89/89, typecheck/build/guardrails pass).
- Acceptance criteria 1–6 mapped + verified (Tailwind globs, visual FBs, thin Toast re-export, presentational SSOT in fixtures, domain matrices + backlog index, `wire_contracts_changed: false`).
- Checkout alignment verified (branch `plan/v1.107-p0-studio-ui-tune`, range `91b621ae...71a7a2ea` matches QC).
- Coverage: new chrome components exercised via Studio fixtures (no new unit tests added; pre-existing coverage for promoted primitives).
- 2 QC residuals carried (R-V1107QC1-W002 low, R-V1107QC1-ARCH nit) — both open/deferred, no new findings.
- Manual smoke (interactive light+dark + Tauri) flagged as non-headless; Studio build produces usable gallery.

All DoD satisfied. Ready for PM `Done`.

SSOT status: `.mstar/status.json` → `plans[]` / `residual_findings`.
