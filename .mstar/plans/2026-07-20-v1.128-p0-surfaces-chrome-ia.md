# P0 — Surfaces chrome IA

> **Iteration compass:** `.mstar/iterations/v1.128/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.128/specs/surfaces-chrome-ia.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Design Studio Surfaces: left section sidebar for fast switching; **retire Banner gallery (Must)**; **fix Selection Agent dialog fixture so it does not block the page and can be reopened (Must)**.

**Architecture:** Studio-only UI/routing/fixtures (`apps/design-studio`). No daemon, no wire contracts.

**Tech Stack:** React 18 + TypeScript + React Router + Vitest + Testing Library.

**Execution:** mstar-sdd (multi-task)

### Wire contracts

- **Verdict: `wire_contracts_changed: false`**

## Tasks

- [x] **T1 (AC-V1128-1a — Surfaces left sidebar) · Must:** Replace horizontal `SurfacesSectionNav` pills with a persistent left sidebar of `SURFACES_SECTIONS` (minus Banner). Active route highlighted; nested `/surfaces/*` via existing Outlet. Update layout CSS so content sits beside the sidebar.
  - **Verify (author-observable):** Maintainer opens Studio `/surfaces/*`, clicks sidebar links, sees active highlight move and content swap — no horizontal pill strip remains.
  - Files: `apps/design-studio/src/pages/surfaces.tsx`, related layout/CSS, `App.test.tsx`

- [x] **T2 (AC-V1128-1b — Retire Banner) · Must:** Remove Banner from `SURFACES_SECTIONS`, route in `App.tsx`, `SurfacesBannerPage`, `main-banner-fixtures` usage from Surfaces, and Banner assertions in tests. Leave App `main-banner.tsx` unmounted (already V1.125). **Non-negotiable** — Studio must not show ghost Banner after App removed it.
  - **Verify (author-observable):** Maintainer cannot navigate to `/surfaces/banner`; Surfaces nav has no Banner entry; grep Studio Surfaces for Banner = gone.
  - Files: `surfaces.tsx`, `App.tsx`, fixtures, `App.test.tsx`

- [ ] **T3 (AC-V1128-1c — Selection overlay scoped) · Must:** Agent/dialog variant in `selection-submenu-fixtures.tsx` uses a relative host + Open/reopen control (conflict-modal pattern). Remove page-blocking `fixed inset-0` overlay. **Non-negotiable** — maintainer must dogfood all Selection variants on one page.
  - **Verify (author-observable):** Close Agent dialog → other variants visible and clickable → reopen Agent dialog without reload.
  - Files: `apps/design-studio/src/fixtures/selection-submenu-fixtures.tsx`, tests

## Plan self-review (PM Seat 1)

1. Spec coverage: AC-V1128-1a/b/c → T1/T2/T3 — Banner retire + Selection reopen explicitly **Must**
2. No TBD placeholders in tasks
3. Disjoint file families where possible (T2 deletes Banner; T3 fixture-only)
4. User value: fast Surfaces dogfood without ghost Banner or blocking overlay

## Architect lock (Seat 2)

| Item | Lock |
|------|------|
| Scope | Studio-only (`apps/design-studio`); no `@web-*` extracts |
| Selection overlay | Mirror `conflict-modal-fixtures.tsx:84–104` `InlineModalHost`; remove `selection-submenu-fixtures.tsx:177` `fixed inset-0` |
| Wire contracts | `wire_contracts_changed: false` |
| Upstream | None — P0 is first in serial order |

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-20-v1.128-p0-surfaces-chrome-ia/` (gitignored).

## Review Gate Summary

> Filled after QC.

## QA Gate Summary

> Filled after QA.
