# P2 — AgentPicker catalog polish

> **Status:** Draft (Phase 1). Product specify + clarify **done** (§5.1). Architect plan **done** (§5.2). Writing review **done** (§5.3).

| Field | Value |
|-------|-------|
| plan_id | 2026-07-15-v1.119-setup-agent-picker-catalog |
| iteration_refs | V1.119 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.119/specs/setup-agent-picker-catalog.md` |
| iteration_compass | `.mstar/iterations/v1.119/delivery-compass.md` |
| spec_integration_branch | iteration/v1.119 |
| merge_target | iteration/v1.119 |
| working_branch | plan/v1.119-p2-agent-picker-catalog |
| wire_contracts_changed | false |

**Goal:** Curated honest AgentPicker — ACP icons for native, hide ACP dupes, installed chrome, default order (F1–F3). Settings shares picker.

**Depends on:** — (independent of P1; ship after P0 unblocks dogfood preferred)

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors (Setup/Settings) | Calm installed cards; recognizable icons; predictable default list |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Product)

### Specify

- **Problem statement:** Placeholder icons on native agents; Install/Docs on installed cards; `claude-acp`/`codex-acp` dupes in More; thin default grid (F1–F3).
- **User value:** Honest catalog — installed vs uninstalled chrome, curated 12-slot grid, no wrapper dupes.
- **Scope:** Catalog overrides + sort rewrite + AgentCard Install/Docs gating + Settings parity. Primary spec AC-P2-*.
- **Non-goals:** Workspace Continue/path; new install pipelines; HostManager native session registration.
- **Target state:** IC-6, IC-7, IC-8.
- **Draft DoD:** All AC-P2-* pass; catalog unit tests for exclude/sort/icons.

### Clarify

- **Decisions (locked):**
  - Icon overrides: `claude-native` / `codex-native` use ACP CDN URLs from wrapper registry assets.
  - Hard-exclude **only** `claude-acp` and `codex-acp` (not all `*-acp`).
  - Installed cards: hide Install and Docs; show badge + optional version/description.
  - Curated 12 display names → catalog keys table in primary spec (priorities 0–11).
  - Forward-compat: Hermes / Grok Build / Kilo match registry id or case-insensitive name token; skip slot if absent.
  - Pi catalog key: `pi-acp` (display **Pi**).
  - Sort: installed-first, then curated uninstalled order, then remainder; More excludes default grid + hard-excluded ids.
  - Settings uses same `defaultGridEntries` / `moreAgentsEntries` as Setup.
- **Copy (writing §5.3):** Optional native `description` overrides — `claude-native`: *Anthropic's agent for local coding with Claude.*; `codex-native`: *OpenAI's agent for local coding with Codex.* (EN overrides only; residual OK if scan supplies description).
- **Open questions (architect §5.2):**
  - ~~Exact CDN icon URLs to pin in overrides (stable latest path vs versioned).~~ **Resolved** — `/v1/latest/*.svg` URLs (AD-P2).
  - ~~Override schema: `excludeFromPicker` boolean vs hardcoded id filter in TS.~~ **Resolved** — `excludeFromPicker` in overrides JSON (AD-P2).
  - ~~Static description fallback for native agents when scan metadata null.~~ **Resolved** — optional override `description`; residual OK if omitted.

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs Checked:** `agent-catalog-overrides.json` schema v1; `agent-catalog.ts` sort/filter; AgentCard Install/Docs; live CDN icon URLs verified; compass AD-P2 locked.
- **Impactful Ambiguities:** None.
- **Gate Decision:** go

### Plan

- **Option A — Extend overrides + catalog partition rewrite:** `excludeFromPicker`, pinned `iconUrl`, `priority` 0–11, optional `description`. **Trade-off:** JSON edits + sort rewrite in one module.
- **Option B — Hardcoded TS exclude list + runtime registry icon fetch:** **Trade-off:** duplicates registry CDN in runtime; rejected.
- **Selected Approach:** Option A.
- **Long-term Target State:** Honest curated catalog shared by Setup and Settings.
- **Durable Slice for This Batch:** Overrides expansion, catalog filter/sort rewrite, AgentCard `!installed` gating, unit tests + Settings smoke.
- **Module Boundaries:**

  | Module | Change |
  | --- | --- |
  | `apps/web/config/agent-catalog-overrides.json` | Icons, priorities, excludeFromPicker, optional descriptions |
  | `apps/web/src/lib/agent-catalog.ts` | `excludeFromPicker` filter, installed-first sort, curated uninstalled rows |
  | `apps/web/src/components/setup/agent-picker.tsx` | Install/Docs gated on `!installed` |

- **API/Data Contracts:** Consume existing scan DTO — `wire_contracts_changed: false`.
- **Risks and Rollback:** Registry id mismatch for Hermes/Grok/Kilo — forward-compat name tokens + skip empty slots (product AC-P2-7). Rollback: prior thin grid (product rejects).
- **Validation Plan:** AC-P2-* catalog unit tests + Setup/Settings visual smoke.
- **Effort (agent-oriented):** S — 1–2 sessions.

## Scope

### In scope

- Icon overrides for native Claude/Codex
- Hard exclude ACP wrappers from all picker lists
- Install/Docs gated on `!installed` in AgentCard
- Curated priority + installed-first / More rules
- Catalog unit tests + Settings smoke

### Not in scope

- P0 Continue / P1 path sync
- Wire DTO changes unless architect requires native version/description enrichment

## Acceptance Criteria

1. ACP icons + no ACP wrapper rows (AC-P2-1, AC-P2-2).
2. Installed/uninstalled chrome (AC-P2-3, AC-P2-4).
3. Order/groups + Settings parity (AC-P2-5, AC-P2-6).
4. Forward-compat slots when scan matches (AC-P2-7, optional).

## Tasks

- [ ] T1: Overrides — icons, priorities, exclude ACP wrappers, optional native descriptions
- [ ] T2: `defaultGridEntries` / `moreAgentsEntries` sort rewrite
- [ ] T3: AgentCard Install/Docs gating
- [ ] T4: Tests + Setup/Settings verify AC-P2-*

## Roadmap / deferred scope

- Native version/description enrichment if scan stays null — optional follow-up if overrides insufficient (residual OK).
