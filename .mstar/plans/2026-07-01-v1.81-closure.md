# P-last — V1.81 Closure

**plan_id**: `2026-07-01-v1.81-closure`
**Iteration**: V1.81 (Compass: `v1.81-creator-soul-maturation-delivery-compass-v1.md`)
**Status**: Todo
**Owner**: `@project-manager`
**Task category**: docs
**Working branch**: `feature/v1.81-closure` from `iteration/v1.81`
**Merge target**: `iteration/v1.81`
**Depends on**: P0 + P1 Done + QC tri-review + QA Pass.

> Closure track. Compound round + 2 V1.80 low doc residuals folded in +
> compaction + PR + trackers/STRATEGY update. Runs sequentially after P0 + P1
> merge. Note: this plan is the final plan closure; iteration-close (Phase 3
> §3.1–§3.5) still runs as a separate gate after all plans Done.

## 1. Problem statement

After P0 + P1 ship, V1.81 needs the standard closure: knowledge crystallization,
deferred-tracker + STRATEGY update, Profile B compaction, the PR to `main`, and
two carried-forward V1.80 low doc residuals closed as a light companion.

## 2. Scope

- **T1 — Close 2 V1.80-P0 low doc residuals** (companion):
  - `R-V180P0-QC1-001` — document `memory_review_locks` HashMap lifecycle
    ceiling (daemon-lifetime; revisit if multi-creator/session churn).
  - `R-V180P0-QC2-001` — document best-effort `delete_pending_by_id`
    at-least-once semantics (pre-existing V1.33 pattern; accepted under
    local-only/single-creator/small-queue threat model).
  Both are documentation-only (code comments / threat-model notes), matching
  their accepted `decision`.
- **T2 — Compound round** (`mstar-compound`): capture reusable knowledge from
  V1.81 (candidates: the whole-vs-subset SOUL projection model; the
  on-demand-narrative-with-stale-invalidation pattern; any synthesis-seam
  lesson). Update `CONCEPTS.md` if new domain nouns land.
- **T3 — Deferred-tracker + STRATEGY**: BL-10 (growth-curve) shipped; BL-12
  (SOUL viz refinements) shipped minus per-World narrative (deferred); add a
  STRATEGY decision-log entry for V1.81. **Tracker-hygiene subtasks (pre-existing
  carry-forward, flagged by the V1.81 §1.6 writing-specialist):**
  - Move the **DF-49** row (cancelled V1.79, still a strikethrough row in the
    active §2.3 open table) to `shipped-features-tracker.md` cancelled section
    and remove it from the active tracker.
  - Move the **REL-01** row (shipped V1.80, still a strikethrough row in the
    active §2.5 reliability-roadmap table) to the shipped archive and remove it
    from the active tracker.
  - Note: the `creator-memory-soul-lifecycle` spec was renamed in iteration-start
    (commit on `iteration/v1.81`) to drop a `-v1` filename suffix that violated
    `specs/AGENTS.md` ("no version suffix in filenames"); 6 code doc-comments +
    the V1.81 doc references were updated. No further action needed unless the
    title "Specification v1" is also to be normalized.
- **T4 — Profile B compaction**: archive the 4 Done plan rows to
  `.mstar/archived/plans/<id>.json` + `plans-done.json` (string IDs only);
  clear `status.json.plans[]` to non-Done only.
- **T5 — Trackers/README**: update `iterations/README.md` index,
  `knowledge/deferred-features-cross-version-tracker.md` quick-status,
  `status.json` (`latest_active_iteration`, `latest_ship`).
- **T6 — PR**: `iteration/v1.81` → `main` (PR target = `metadata.target_branch`).

## 3. Plan (task list)

- [ ] T1 — Close 2 V1.80 low doc residuals — `@fullstack-dev` (doc comments) / `@project-manager` (status.json)
- [ ] T2 — Compound round — `@project-manager`
- [ ] T3 — Deferred-tracker + STRATEGY decision-log — `@project-manager`
- [ ] T4 — Profile B compaction (4 plans) — `@project-manager`
- [ ] T5 — Trackers/README/status.json — `@project-manager`
- [ ] T6 — PR `iteration/v1.81` → `main` — `@project-manager`

## 4. Acceptance / DoD

- 2 V1.80 low residuals `lifecycle: resolved` in `status.json`.
- Compound round complete (docs created or "无可结晶知识" recorded with reason);
  new knowledge docs registered in `knowledge/README.md`.
- Deferred-tracker + STRATEGY + README updated.
- Profile B compaction verified (`plans-done.json` entries are plain strings).
- PR open against `main`; compass frontmatter `status: completed` + `end_date`
  written at Phase 3 §3.4 (not in this plan — in iteration-close).

## 5. Non-goals

- No new feature work (closure only).
- iteration-close (Phase 3 §3.1–§3.5) is a separate gate after this plan Done —
  this plan does not by itself mark the iteration complete.
