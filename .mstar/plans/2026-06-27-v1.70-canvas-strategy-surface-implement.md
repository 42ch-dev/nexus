# Plan: V1.70 Canvas Strategy Surface Implement (α: read + overlay + Idea-steer)

**Plan ID**: `2026-06-27-v1.70-canvas-strategy-surface-implement`
**Iteration**: V1.70
**Status**: Todo
**Owner**: `@fullstack-dev` (backend A5 if needed) + `@frontend-dev` (React Flow canvas)
**Effort**: L
**Depends on**: P-1 (compass Locked)
**Working branch**: `feature/v1.70-canvas-strategy-read` from `iteration/v1.70`
**Merge target**: `iteration/v1.70`
**Worktree**: `.worktrees/v1.70-canvas/`
**Task category**: `visual` (primary) + `logic` (secondary — graph adapter + A5 read endpoint)
**Superpowers**: (none — single-stream within P0; parallelism is P0∥P1 at plan level, handled by PM dispatch)

## Compass reference

[`v1.70-canvas-strategy-implement-and-ci-optimization-compass-v1.md`](../iterations/v1.70-canvas-strategy-implement-and-ci-optimization-compass-v1.md) §1.1 Track A (A1–A8), §1.3 wire contracts, §3 plan topology, §6 risk notes.

## Spec reference

- [`canvas-strategy-surface.md`](../knowledge/specs/canvas-strategy-surface.md) (V1.69 Draft — the implement input; §3.1 React Flow feasibility, §3.2 Strategy-DAG mapping, §3.3 surfaces, §3.4 interface contracts, §3.5 write boundary [deferred to V1.71], §3.6 token contract, §4 product/UX, §4.4 accessibility)
- [`orchestration-engine.md`](../knowledge/specs/orchestration-engine.md) §3 (graph-of-graphs), §7.2 (preset YAML), §3.3/§4.2 (session state)
- [`web-ui.md`](../knowledge/specs/web-ui.md) §5 (NexusClient transport invariant)
- `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md` (canvas token LEVEL placeholders — A7 fills values)

## Scope (α depth — `wire_contracts_changed: FALSE`)

- **A1** Shared Canvas Shell (React Flow provider, pan/zoom, minimap, selection, command palette read-only, side inspector, validation panel read-only, keyboard shortcuts, screen-reader summary, NexusClient injection, route-split)
- **A2** Strategy graph adapter (preset → React Flow nodes/edges per Draft §3.2 mapping; outer states → top-level nodes; inner_graph → group sub-flows; Converge → join nodes; transitions → edges)
- **A3** Live execution overlay (session state → runtime graph overlay; current node/task + session-level paused/waiting/error/completed status; reuse existing session list/detail endpoints. Completed-path history, child-session progress, and Converge-arrival overlays are **deferred** per compass A3 — they require a future read-contract.)
- **A4** Idea-input affordance + Run/Resume steering (global + contextual Idea input; Steer/Run/Resume/Ask-Nexus-to-revise verbs; reuse existing schedule/orchestration endpoints; visible steering artifacts)
- **A5** Read endpoint verdict (**Phase 2b `@architect` locked option a**): client-side projection from existing endpoints — no new Local API route, no new DTO, no schema/codegen. Strategy graph projection from `GetPresetResponse.yaml`; live overlay bounded to existing `SessionSummary` fields.
- **A6** Browser tab + Tauri WKWebView parity smoke (drag, pan/zoom, wheel/pinch, keyboard focus, clipboard, inspector focus return)
- **A7** Canvas → DESIGN.md token fill (assign concrete light + dark values to 11 LEVEL placeholder tokens; names preserved)
- **A8** Accessibility baseline (keyboard-first traversal, non-spatial alternate view, screen-reader summaries, focus management, motion/zoom discipline)
- **C (residual)** Close `R-V167PSEC-QC1-S-UNMOUNT` (daemon-status-bar.tsx subscription-cleanup race) alongside A3 overlay work

## Non-goals

- No structured node-granular edits (rename state, rewire edge, patch prompt) — V1.71
- No write-boundary operation DTOs — V1.71
- No outline+timeline or World KB canvas surfaces — V1.71+
- No CLI/schema rename of preset → strategy — deferred
- No new schema/codegen/DTO — `wire_contracts_changed: FALSE`

## Tasks (to be detailed at implement dispatch)

- [ ] T1: Add `@xyflow/react` dependency + route-split canvas route skeleton (A1)
- [ ] T2: Shared Canvas Shell component (A1)
- [ ] T3: Strategy graph adapter — preset DTO → nodes/edges projection (A2)
- [ ] T4: Live execution overlay — session state → graph overlay (A3)
- [ ] T5: Idea-input affordance + Run/Resume steering (A4)
- [ ] T6: [not applicable — A5(a) locked; no backend work needed] (was: conditional read endpoint promotion if A5(b))
- [ ] T7: Browser + Tauri WKWebView parity smoke (A6)
- [ ] T8: DESIGN.md + DESIGN.dark.md canvas token values fill (A7)
- [ ] T9: Accessibility baseline (A8)
- [ ] T10: Close R-V167PSEC-QC1-S-UNMOUNT (C)

## Acceptance

- Strategy canvas renders a preset as a state-machine graph with inner-DAG sub-flows + Converge join nodes in browser tab + Tauri shell
- Live execution overlay shows current/completed/paused/error states
- Idea-input → Run/Resume enqueues via existing endpoints; steering artifacts visible
- DESIGN.md canvas tokens have concrete values (light + dark); `pnpm --filter web typecheck` + `pnpm --filter web build` + `pnpm --filter web test` green
- `wire_contracts_changed: FALSE` confirmed (no schemas/codegen/DTO touched)
- `R-V167PSEC-QC1-S-UNMOUNT` closed in SSOT
