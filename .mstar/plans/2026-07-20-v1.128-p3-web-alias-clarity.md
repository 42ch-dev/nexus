# P3 — `@web-*` / `@42ch/nexus-ui` clarity

> **Iteration compass:** `.mstar/iterations/v1.128/delivery-compass.md`
> **Spec:** `.mstar/iterations/v1.128/specs/web-alias-clarity.md`
> **SSOT status:** `.mstar/status.json`

**Goal:** Make the two-tier import story **visible** in Design Studio Surfaces and durable docs — `@web-*` = Studio→`apps/web` presentational extracts; `@42ch/nexus-ui` = promoted primitives. **Labeling and docs are Must; mass promotion is forbidden.**

**Architecture:** Docs + gallery labeling primary. Optional tiny promotion only if criteria already met (record in Promotion list). Runs last so labels match P0–P2 landed surfaces.

**Tech Stack:** Markdown + Studio copy; optional package export if promotion approved.

**Execution:** mstar-sdd (multi-task)

### Wire contracts

- **Verdict: `wire_contracts_changed: false`**

### Promotion list

- **(default empty)** — Review seats may add at most one already-qualified primitive; otherwise zero. Mass migrate = **Non-Goal**.

### Architect lock (Seat 2)

| Item | Lock |
|------|------|
| Runs last | Serial after **P0 → P1 → P2** |
| Extract ownership | Labels only — P1 `@web-canvas/*`, P2 `@web-layout/creator-shell-content`; P0 none |
| Alias policy | No rename; document existing aliases (`vite.config.ts:38–57`) |
| Wire contracts | `wire_contracts_changed: false` |

## Tasks

- [ ] **T1 (AC-V1128-4a/b — Gallery source badges) · Must:** Surfaces pages that cite `@web-*` or `@42ch/nexus-ui` show clear extract-vs-promoted labeling (badge or consistent copy pattern). Cover Shell, Canvas, Selection, and any section touched by P0–P2.
  - **Verify (author-observable):** Maintainer spot-checks Shell/Canvas/Selection — reads “App presentational extract” vs “promoted primitive” without opening tsconfig; smoke test if labels have testids.
  - Files: `apps/design-studio/src/pages/surfaces.tsx` (+ shared helper if needed)

- [ ] **T2 (AC-V1128-4c/d — Docs) · Must:** Update `apps/design-studio/AGENTS.md`, `packages/nexus-ui/AGENTS.md`, and `.mstar/specs/design-studio.md` (or short pointer section) with explicit two-tier table — naming cannot be misread as “all UI is nexus-ui”. State that `@web-*` aliases remain valid; no mass path change required.
  - **Verify (author-observable):** Docs contain two-tier explanation; no contradictory “everything from nexus-ui” claim.
  - Files: listed AGENTS + design-studio spec

- [ ] **T3 (Optional promotion) · Should — default N/A:** If Review locks a single already-qualified promotion, execute it and record in Promotion list; else mark N/A with note. **Do not** use T3 to “fix” alias confusion — T1/T2 are the fix.
  - **Verify:** Promotion list entry + package export + Studio import switch for that one component only.
  - Files: `packages/nexus-ui/*`, Studio imports

## Plan self-review (PM Seat 1)

1. **Labeling/docs over promotion** — T1/T2 are Must; T3 default N/A
2. No mass migrate — success = clarity, not package consolidation
3. Runs last so labels match P0–P2 landed surfaces

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-20-v1.128-p3-web-alias-clarity/` (gitignored).

## Review Gate Summary

> Filled after QC.

## QA Gate Summary

> Filled after QA.
