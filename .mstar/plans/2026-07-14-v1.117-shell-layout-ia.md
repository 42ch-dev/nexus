# P2 - Shell layout + menu IA + status bar

> **For agentic workers:** REQUIRED SUB-SKILL: Use `mstar-sdd`. Steps use checkbox (`- [ ]`) syntax.

| Field | Value |
|-------|-------|
| plan_id | 2026-07-14-v1.117-shell-layout-ia |
| iteration_refs | V1.117 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.117/specs/shell-layout-ia.md` |
| iteration_compass | `.mstar/iterations/v1.117/delivery-compass.md` |
| spec_integration_branch | iteration/v1.117 |
| merge_target | iteration/v1.117 |
| wire_contracts_changed | false |

**Architect (§5.2):** Drill-in when `workId` set; Body → `/chapters`; agent badge
from profile + scan cache; `running` tag locale. See spec AD-P2-*.

**Goal:** Full-height sidebar with Settings+Profiles bottom-aligned; content-only
scroll; menu regroup + work drill-in skeleton; status bar Daemon Running + agent badge.

**Depends on:** P0 T4 (footer Profile switch activates workspace path).

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors (navigation) | Menu groups match Orchestration vs Creation mental model |
| Authors (focus) | Fixed sidebar + status bar; content-only scroll |
| Authors (work context) | Drill-in skeleton instead of full menu tree |
| Authors (desktop) | Glanceable agent badge → Agent settings |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Scope

### In scope

- Sidebar layout: menus stretch; Settings+Profiles bottom section
- Fixed status bar; content scrolls only
- Remove Canvas nav group; Strategy → Orchestration; World KB → Creation
- Work drill-in skeleton (`Back to all` / `返回所有`, `Outline` / `大纲`, `Body` / `正文`)
- Status bar: Running + running tag + state dot + agent badge (name+version / placeholder)

### Not in scope

- Full multi-level hierarchical menus

## Acceptance Criteria

Author-observable AC — full table in
`primary_spec` (AC-P2-1..10). Iteration gate highlights:

1. Settings + Profiles bottom-aligned as one footer section.
2. No Canvas nav group; Strategy → Orchestration; World KB → Creation.
3. Work drill-in skeleton in work context.
4. Status bar: Daemon Running + running tag + agent badge → `/settings/agent`.

## Tasks

- [x] T1: Sidebar chrome — `h-screen` + bottom Settings/Profiles block; content-only scroll in `root-layout`
- [x] T2: Remove Canvas group; Strategy → Orchestration; World KB → Creation; work drill-in skeleton
- [x] T3: Status bar — `running` tag + agent badge (profile + scan version) → `/settings/agent`
- [x] T4: Locales (`nav.drillIn.*`, `daemon.runningTag`) + tests

## Review Gate Summary

**QC mode:** full tri-review (SDD) · **Branch:** `plan/v1.117-p2-shell-layout-ia`
**Range:** `4386cec4..f5f12cc9` (T1–T4 + design-studio regression fix)

| Seat | Focus | Verdict |
|------|-------|---------|
| qc1 | Architecture + maintainability | Approve (0C/0W/4S) |
| qc2 | Security + correctness | Approve (0C/3W-low/ACs met) |
| qc3 | Performance + reliability | Approve (0-blocking/3S) |

**Consolidated verdict: Approve (no fix-wave needed).** AC-P2-1..10 verified Met. Design-studio P1 regression fixed (`f5f12cc9`). Residuals: 2 low/nit registered (R-V1117P2QC2-W001/W002) + suggestion-level cleanup; all post-V1.117.

## QA Gate Summary

**QA gate: mandatory** · **Verdict: Pass** · Report: `.mstar/sdd/2026-07-14-v1.117-shell-layout-ia/review/qa.md`

AC coverage 10/10 met (AC-P2-1..10). Tests: web 1130/1130, design-studio 110/110 — all green; typecheck clean. Design-studio P1 regression fixed. Residuals: 2 low/nit deferred (post-V1.117), no blockers. **Plan ready for Done.**

## Roadmap / deferred scope

- Later: deeper hierarchical menus (multi-level trees)

## SSOT status

`.mstar/status.json` → `plans[]` / `residual_findings`
