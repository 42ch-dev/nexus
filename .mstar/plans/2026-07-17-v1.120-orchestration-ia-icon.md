# P2 — Orchestration IA + sessions + Dock icon

> **Status:** Done (2026-07-17) — merged to `iteration/v1.120` @ `a1b0e5ce`. QC **Approve with residuals**; QA **Pass with residuals** (5/5 ACs + compass bullets).

## QA Gate Summary (durable)

- **QA verdict: Pass with residuals** — AC-P2-1…5 5/5 + compass Orchestration/Desktop bullets; 476 daemon tests, 120 web tests + redirect + capability-endpoint tests, clippy/fmt/typecheck clean; `wire_contracts_changed: false` verified; capability API `GET /v1/daemon/orchestration/capabilities` preserved; corner-alpha independently re-verified (all `[0,0,0,0]`); Dock smoke accepted from user-confirmed 2026-07-17 observation.
- Full report: `.mstar/sdd/2026-07-17-v1.120-orchestration-ia-icon/review/qa.md` (ephemeral bundle).

## Review Gate Summary (durable)

- **SDD:** T1 `5a843feb` (daemon `_system.` session filter + client `filterVisibleSessions` defensive filter; 3→0 phantom rows) → T2 `14539b7e` (Capabilities soft-remove: nav gone desktop+mobile, `/capabilities` → `/sessions` replace redirect; module + APIs preserved) → T3 zero-commit (icon pipeline verified healthy; stale local `icon.icns` root cause; corner-alpha proof all formats; Dock smoke **user-confirmed 2026-07-17**). L2 reviews 3/3 clean.
- **QC tri-review (2026-07-17, range `22c4157d...14539b7e`):** qc1 Approve w/ residuals (0C/0W/3S+baseline note) · qc2 Approve (0C/0W/3S) · qc3 Approve (0C/0W/1S) → **consolidated: Approve with residuals**. No fix wave.
- **Residuals:** 8 registered in `status.json` `residual_findings[2026-07-17-v1.120-orchestration-ia-icon]` (2 low: clippy `--all-targets` baseline, `_system.` preset-name guard; 6 nit). Raw bundle: `.mstar/sdd/2026-07-17-v1.120-orchestration-ia-icon/review/` (ephemeral).

| Field | Value |
|-------|-------|
| plan_id | 2026-07-17-v1.120-orchestration-ia-icon |
| iteration_refs | V1.120 |
| tier | Must |
| primary_spec | `.mstar/iterations/v1.120/specs/orchestration-ia-icon.md` |
| iteration_compass | `.mstar/iterations/v1.120/delivery-compass.md` |
| spec_integration_branch | iteration/v1.120 |
| merge_target | iteration/v1.120 |
| working_branch | plan/v1.120-p2-orchestration-ia-icon |
| wire_contracts_changed | false |

**Goal:** Honest Sessions list; remove Capabilities from Orchestration nav; fix macOS Dock white-square icon.

**Depends on:** —

**Execution:** mstar-sdd

## User value

| Who | Outcome |
| --- | --- |
| Authors | Orchestration nav shows author-useful surfaces; Sessions empty when idle; Dock looks native |

## Prepare gates

- [x] specify
- [x] clarify
- [x] plan

## Prepare Package (Product)

### Specify

- **Problem:** F3 phantom running sessions; F4 Capabilities unnecessary; F7 Dock white square.
- **Scope:** Sessions page filter/query display; sidebar + route redirect; desktop icon compose/generate.
- **Non-goals:** Capability API removal; Sessions live console; non-macOS-specific icon art.
- **Draft DoD:** AC-P2-1…5.

### Clarify (locked — PM §5.1)

- **Sessions (F3):** Active-work monitor only — show `running` / `paused` / `waiting_for_input`; empty when idle; phantom `running` = defect (see primary spec product rule).
- **Capabilities (F4) soft-remove:** remove nav item; `/capabilities` → `/sessions`; **keep** daemon capability APIs (no deletion).
- **Dock (F7):** transparent compose canvas; Dock smoke authoritative per icons README.

## Prepare Package (Architecture)

### Clarify Validation

- **Inputs:** `sessions-page.tsx`, sessions API, `sidebar.tsx`, `App.tsx` routes, `compose-app-icon.mjs`, icons README.
- **Gate Decision:** go

### Plan

- **Selected:** Daemon session list filter + web defensive filter + nav/route redirect + icon pipeline.
- **Module boundaries:**

  | Module | Change |
  | --- | --- |
  | `handlers/orchestration/sessions.rs` | Exclude `_system.*` preset sessions from list (AD-P0-2b) |
  | `apps/web/src/api/queries.ts` | Defensive `useSessions` filter (AD-P0-2c) |
  | `sessions-page.tsx` | Empty state when filtered list empty (AC-P2-1) |
  | `sidebar.tsx`, `root-layout.tsx`, `App.tsx` | Drop Capabilities nav; `/capabilities` → `/sessions` redirect (AD-P2-1) |
  | `apps/desktop` icons | Compose/generate + Dock smoke (AD-P2-2) |

- **Interfaces for implementers:**
  - Daemon filter: `preset_id.starts_with("_system.")` — no new query param (wire unchanged).
  - Redirect: React Router `<Navigate to="/sessions" replace />`.
  - Do **not** delete `capabilities-page.tsx` or daemon capability handlers.
- **Risks:** Author-session orphans after crash — deferred reconcile (AD-P0-2d) only if filter insufficient.
- **Effort:** S–M — 2 SDD tasks + Dock smoke.

## Tasks (refined)

1. **T1 — Sessions honesty:** Daemon `list_sessions` filter + client defensive filter + tests with `_system.*` mock rows (AC-P2-1); document dogfood repro.
2. **T2 — Capabilities soft-remove:** Sidebar + mobile nav + `App.tsx` redirect + route tests (AC-P2-2, AC-P2-3, AC-P2-5).
3. **T3 — Dock icon:** Compose/generate + Dock smoke evidence (AC-P2-4).

## Global Constraints

- Do not delete daemon capability endpoints; `wire_contracts_changed: false`; icon source stays transparent (no opaque fill).
