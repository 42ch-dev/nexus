# Pre-Phase 2 Consolidated Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve all findings from V1.0-phase1 Architecture Review and Product Review, hardening the codebase foundation and filling critical skeleton commands before V1.0-phase2 feature work begins.

**Architecture:** Sequential execution with limited parallelization — P0 bug fixes first, then Foundation Hardening (error strategy, SQLite pooling, schema dedup), then parallel ACP SDK Bridge + Daemon/Manuscript Ops + Codegen Alignment, finally Auth Flow.

**Tech Stack:** Rust (tokio, axum, tower, rusqlite, deadpool-sqlite, agent-client-protocol), TypeScript (codegen), JSON Schema

---

## Inputs

| Document | Path | Relevance |
|----------|------|-----------|
| V1.0-phase1 Architecture Review | `.agents/archived/knowledge/phase1-architecture-review.md` | 36 findings (3 CRITICAL, 7 HIGH, 14 MEDIUM, 12 LOW) |
| V1.0-phase1 Product Review | `.agents/archived/knowledge/phase1-product-review.md` | 32 commands analyzed (24% functional, 43% skeleton, 11% not implemented) |
| V1.0-phase2 Architecture Plan | `.agents/archived/knowledge/phase2-architecture-plan.md` | 5 implementation plans with task breakdowns |
| V1.0-phase2 Product Plan | `.agents/archived/knowledge/phase2-product-plan.md` | P0/P1/P2 feature prioritization, Go/No-Go criteria |
| ACP Client Tech Spec | `.agents/archived/knowledge/acp-client-tech-spec-legacy.md` (archived 2026-04-17; current: `archived/archived/knowledge/acp-client-tech-spec.md`) | ACP SDK integration details |
| Residual Findings | `.agents/status.json` **root** `residual_findings` | 38 open residuals (11 HIGH, 14 MEDIUM, 13 LOW) |

---

## Phase 0: P0 Bug Fixes (PREREQUISITE)

**Status**: In progress on `fix/v1.0-p0-bugs` by @fullstack-dev
**Blocking**: All V1.0-phase2 plans depend on these fixes being merged to `main`

| Bug | Finding | Fix | Verification |
|-----|---------|-----|-------------|
| Workspace `is_initialized()` always returns `false` | CLI-DAEMON-1 [CRITICAL] | Set `workspace_path` via interior mutability in `init_workspace()` | Unit test: init → assert `is_initialized() == true` |
| Missing `POST /v1/local/context/assemble` daemon route | CLI-DAEMON-2 [CRITICAL] | Register context handler in daemon router | Integration test: POST → 200 with valid JSON |
| `daemon start` returns manual instructions instead of spawning | Product Review §4 [CRITICAL] | Auto-spawn `nexus42d` via `std::process::Command` | Manual test: `daemon start` → process running |

**Acceptance**: All 3 bugs fixed, 438+ tests passing, clippy clean, fmt clean.

---

## V1.0-phase2 Plan Breakdown

### Plan A: Foundation Hardening

**ID**: `2026-04-06-foundation-hardening`
**Effort**: M (~2–4 agent sessions)
**Branch**: `feature/v2.0-foundation-hardening` from `main`
**Depends on**: P0 bug fixes merged

Resolves: DEBT-X1..X4, CLI-R9..R15, CC-ERR-1..2, partially SYNC-R4/R10
**15 residuals addressed** (7 HIGH, 4 MEDIUM resolved; 1 HIGH, 3 MEDIUM partially)

#### Task 1: Error Strategy Standardization (CC-ERR-1, CLI-R11)

- [x] Define `NexusApiError` enum in `crates/nexus42d/src/api/errors.rs` with variants: `Uninitialized`, `InvalidInput { field, reason }`, `Internal { code, message }`, `AuthRequired`, `NotFound`
- [x] Implement `IntoResponse` for `NexusApiError` — maps to proper HTTP status codes (400, 401, 404, 409, 500)
- [x] Replace all `Json({ success: false, message })` + HTTP 200 in `crates/nexus42d/src/api/handlers/*.rs` with `Err(NexusApiError::variant)`
- [x] Add `From<anyhow::Error>` impl that captures `.source()` chain (CC-ERR-2)
- [x] Update `DaemonClient` in CLI to parse error responses by status code, not just body
- [x] Add `From<NexusApiError>` for `CliError` in `crates/nexus42/src/errors.rs` — adapted: CLI can't depend on nexus42d, so structured error parsing implemented in `daemon_client.rs` instead
- [x] Test: POST to init with invalid payload → 400, not 200

#### Task 2: SQLite Connection Pooling (DEBT-X1, CLI-R9, CLI-R14)

- [x] Add `deadpool-sqlite` to `crates/nexus42d/Cargo.toml`
- [x] Create `crates/nexus42d/src/db/pool.rs` — `DbPool` wrapper around `deadpool::managed::Pool<SqliteConnManager>`
- [x] Initialize pool in daemon startup with configurable pool size (default: 8)
- [x] Replace `Mutex<Option<Connection>>` with pool `.get()` in `WorkspaceState`
- [x] For CLI: replace `Mutex` with `RwLock` to resolve CLI-R14 race condition (connection-per-command acceptable for single-process CLI) — CLI not modified (open-per-command is fine for single-process); daemon pool resolves CLI-R14 race condition
- [x] Integration test: concurrent handler requests all succeed

#### Task 3: Schema Deduplication (DEBT-X2, CLI-R10)

- [x] Create `crates/nexus42/src/db/schema.rs` — all `CREATE TABLE` and `CREATE INDEX` statements as `const` strings
- [x] Add `.init(db: &Connection)` method to `Schema` struct with schema_version column
- [x] Remove inline CREATE TABLE from `crates/nexus42/src/commands/creator.rs` and `research.rs`
- [x] Remove inline CREATE TABLE from `crates/nexus42d/src/workspace/mod.rs`
- [x] Verify `crates/nexus-sync/src/outbox.rs` schema matches (partially resolves SYNC-R10)
- [x] Test: grep `CREATE TABLE creators` returns exactly 1 non-test location

#### Task 4: Workspace Validation Middleware (DEBT-X4, CLI-R12, CLI-R15)

- [x] Create `crates/nexus42d/src/api/middleware.rs` — `require_workspace` tower middleware layer
- [x] Check `WorkspaceState::is_initialized()` before passing to handler
- [x] Return 409 Conflict with `NexusApiError::Uninitialized` if workspace not initialized
- [x] Apply to all daemon routes except `/v1/local/workspace/init` and `/v1/local/runtime/health`
- [x] Add tracing::info/debug to middleware for request lifecycle observability (CLI-R15)
- [x] Test: unauthenticated/uninitialized requests get proper rejection

#### Task 5: Unsafe Unwrap Cleanup (CLI-R13)

- [x] Audit all `.unwrap()` calls in `crates/nexus42/src/commands/` production code
- [x] Audit all `.unwrap()` calls in `crates/nexus42d/src/api/handlers/` production code
- [x] Replace with `.map_err()?`, `.expect("invariant: ...")`, or proper error propagation
- [x] Add `#[deny(clippy::unwrap_used)]` to command modules (opt-in)
- [x] Test: `cargo test --all` still passes

#### Success Criteria

- [ ] `cargo test --all` passes (445+ tests, no regressions)
- [ ] `cargo clippy --all -- -D warnings` clean
- [ ] Daemon returns HTTP 400 for invalid input, 409 for uninitialized workspace, 500 for internal errors
- [ ] `CREATE TABLE creators` grep returns exactly 1 non-test location
- [ ] No `.unwrap()` in CLI or daemon handler production code paths

---

### Plan B: ACP SDK Bridge Implementation

**ID**: `2026-04-06-acp-sdk-bridge`
**Effort**: M–L (~3–6 agent sessions)
**Branch**: `feature/v2.0-acp-sdk-bridge` from `main` (after Plan A merges)
**Depends on**: Plan A (error strategy)

Resolves: ACP-ARCH-1 (CRITICAL), CLI-DAEMON-5 (HIGH), ACP-ARCH-2..3 (MEDIUM), partially ACP-R4/R7
**10 residuals addressed** (1 CRITICAL, 2 HIGH resolved; 4 MEDIUM, 3 LOW partially)

#### Task 1: LocalSet Bridge Infrastructure

- [ ] Create `crates/nexus42/src/acp/localset_bridge.rs` — `LocalSetBridge` struct
- [ ] Spawn `std::thread` running `LocalSet::new().run_until(async { ... })`
- [ ] Expose `async fn send_request(request) -> Result<Response>` for tokio side
- [ ] Use `tokio::sync::mpsc` (async side) + `std::sync::mpsc` (sync side) for channels
- [ ] Implement `Drop` for graceful shutdown (send shutdown signal, join thread)
- [ ] Unit test: bridge starts, processes request, returns response, shuts down cleanly

#### Task 2: AcpSdkAdapter.initialize() (ACP-ARCH-1, ACP-ARCH-3)

- [ ] Replace stub in `crates/nexus42/src/acp/client.rs` with actual `ClientSideConnection::create()` via bridge
- [ ] Configure `SimpleClientHandler` with Nexus capability set (6 capabilities from tech spec §7)
- [ ] Return real `InitializedSession` with session ID and agent metadata
- [ ] Fix `RegistryClient::default()` panic — return `Result` instead of `expect()` (ACP-ARCH-2)
- [ ] Fix `find_agent("")` — empty query returns `None` (ACP-ARCH-3)
- [ ] Error handling: map SDK errors to `AcpError` variants
- [ ] Integration test: initialize against real ACP agent subprocess

#### Task 3: AcpSdkAdapter.prompt() (ACP-ARCH-1)

- [ ] Replace stub with `ClientSideConnection.send_request(TextMessage { content })` via bridge
- [ ] Stream responses: handle both immediate and streaming responses from agent
- [ ] Update `interactive_prompt_loop` in `crates/nexus42/src/commands/agent.rs`
- [ ] Handle ACP notifications (tool requests, permission requests) — auto-grant with log warning
- [ ] Integration test: send prompt to mock agent, verify response received

#### Task 4: AcpSdkAdapter.subscribe() and Event Handling

- [ ] Implement `subscribe()` to forward ACP notifications to CLI event loop
- [ ] Replace immediately-closed broadcast channel with functional event stream (ACP-ARCH-4)
- [ ] Handle tool permission requests: auto-grant with log warning
- [ ] Graceful session shutdown: send `CloseSession`, wait for ack, then kill subprocess

#### Task 5: AcpError Integration (CLI-DAEMON-5)

- [ ] Add `From<AcpError>` for `CliError` in `crates/nexus42/src/errors.rs`
- [ ] Map ACP errors to user-friendly messages with error codes
- [ ] Test: ACP connection failure produces actionable error, not `CliError::Other(...)`

#### Success Criteria

- [ ] `nexus42 agent run claude-acp` sends user prompt to agent subprocess
- [ ] Agent response received and printed to stdout
- [ ] Session shutdown works cleanly (no zombie processes)
- [ ] `Ctrl+C` during agent run triggers graceful shutdown
- [ ] ACP errors produce structured `CliError`

---

### Plan C: Daemon + Manuscript Operations

**ID**: `2026-04-06-daemon-manuscript-ops`
**Effort**: L (~4–8 agent sessions, 2 milestones)
**Branch**: `feature/v2.0-daemon-manuscript-ops` from `main` (after Plan A merges)
**Depends on**: Plan A (foundation)

Resolves: CLI-DAEMON-8 (MEDIUM), CTX-R4/R5 (HIGH), partially CLI-R6
**9 residuals addressed** (2 CRITICAL, 2 HIGH resolved; 1 HIGH, 1 MEDIUM partially; 3 LOW deferred)

#### Milestone 1: Daemon Auto-Spawn and Lifecycle

##### Task 1: Daemon Stop (complements P0 auto-spawn)

- [x] Implement `stop_daemon()` in `crates/nexus42/src/commands/daemon.rs`
- [x] Read PID from `.nexus42/daemon.pid`, send SIGTERM, wait 2s, then SIGKILL
- [x] Add `--port` and `--foreground` flags to daemon commands
- [x] Test: spawn daemon, verify health check passes, stop daemon

##### Task 2: Daemon Timeout Configuration (CLI-DAEMON-8)

- [x] Add configurable timeout to `DaemonClient` in `crates/nexus42/src/api/daemon_client.rs`
- [x] Default: 10s connect, 30s request
- [x] Replace `reqwest::Client::new()` with `reqwest::Client::builder().timeout(...).build()`
- [x] Test: hung daemon returns timeout error, not infinite hang

#### Milestone 2: Manuscript Operations

##### Task 3: Manuscript File Operations

- [x] Create `crates/nexus42/src/manuscript/manager.rs`
- [x] Implement `manuscript create <title>` — creates `Stories/<title>/` with `manuscript.md` and metadata
- [x] Implement `manuscript edit <title>` — opens in `$EDITOR` or prints content
- [x] Implement `manuscript export <title> --format=markdown|plain`
- [x] Phase management: `manuscript phase <title> <phase>` stores phase in workspace SQLite using domain `ManuscriptState`
- [x] `manuscript status` reads from workspace SQLite (leverages P0 workspace init fix)
- [x] `manuscript promote` validates phase transitions using domain invariants
- [x] `manuscript verify` validates metadata using domain `consistency.rs` rules
- [x] Add clap validator for `--world_id` format `wld_` prefix (CTX-R4)
- [x] Add UTF-8 safety tests for content handling (CTX-R5)

##### Task 4: Sync Status with Real Data (Product P0-3)

- [x] Implement `sync status` using outbox state in `crates/nexus42/src/commands/sync.rs`
- [x] Show: pending bundles count, last successful sync timestamp, unresolved conflicts
- [x] Register daemon route `GET /v1/local/sync/status`
- [x] CLI calls daemon for status (no longer returns "—" for all fields)
- [x] Test: create bundle in outbox, verify `sync status` shows pending count

##### Task 5: Research List/Extract Commands (Product P1-4)

- [x] Implement `research list` — show cached entries from SQLite
- [x] Implement `research extract <id>` — extract text by entry ID
- [x] (P1-4 content extraction deferred to later plan)

#### Success Criteria

- [x] `nexus42 daemon start` spawns daemon in background, returns PID
- [x] `nexus42 daemon stop` terminates daemon cleanly
- [x] `nexus42 manuscript create "My Novel"` creates directory structure
- [x] `nexus42 manuscript phase "My Novel" draft` persists to SQLite
- [x] `nexus42 manuscript status` displays current phase
- [x] `nexus42 sync status` shows real outbox data

---

### Plan D: Auth Flow Completion

**ID**: `2026-04-06-auth-flow`
**Effort**: M (~2–3 agent sessions)
**Branch**: `feature/v2.0-auth-flow` from `main` (after Plan A + C milestone 1)
**Depends on**: Plan A (error strategy), Plan C milestone 1 (daemon infrastructure)

Resolves: ARCH-1 (MEDIUM), SYNC-R6 (HIGH), SYNC-R9 (MEDIUM)
**3 residuals addressed** (1 HIGH, 2 MEDIUM resolved)

#### Task 1: Device Code OAuth Flow

- [ ] Implement `auth login` as RFC 8628 device authorization grant in `crates/nexus42/src/commands/auth.rs`
- [ ] POST to platform `/oauth/device_authorization` → device code + verification URI
- [ ] Display verification URI and user code, poll `/oauth/token`
- [ ] Store tokens in daemon SQLite (not CLI local file)
- [ ] Test: mock platform endpoints, verify full device flow

#### Task 2: Token Lifecycle Management

- [ ] Create `crates/nexus42d/src/auth/token_manager.rs`
- [ ] `TokenManager::refresh_if_expired()` — refresh if < 5min remaining
- [ ] `TokenManager::get_valid_token()` — always returns valid token
- [ ] Store token metadata: `auth_tokens` table (user_id, access_token, refresh_token, expires_at)

#### Task 3: Daemon Auth Middleware

- [ ] Create `crates/nexus42d/src/api/auth_middleware.rs`
- [ ] Extract `Authorization: Bearer <token>` header, validate, inject `AuthenticatedUser`
- [ ] Return 401 if missing/invalid
- [ ] Apply to all routes except health check and workspace init

#### Task 4: CLI Auth Client Update (ARCH-1)

- [ ] CLI `auth status` calls daemon for token state (resolves shared auth types)
- [ ] CLI `auth logout` calls daemon to revoke/clear tokens
- [ ] CLI commands that need auth auto-check and prompt login if needed

#### Success Criteria

- [ ] `nexus42 auth login` completes device code flow (with mock platform)
- [ ] Token refresh works automatically on expiry
- [ ] Daemon returns 401 for unauthenticated requests to protected routes
- [ ] `nexus42 auth status` shows token expiry time

---

### Plan E: Codegen & Contract Alignment

**ID**: `2026-04-06-codegen-alignment`
**Effort**: S (~1–2 agent sessions)
**Branch**: `feature/v2.0-codegen-alignment` from `main` (can run in parallel with all plans)
**Depends on**: None

Resolves: CODEGEN-1/2 (HIGH), CODEGEN-3 (MEDIUM), CTX-R6 (MEDIUM), DM-R5 (MEDIUM)
**5 residuals addressed** (2 HIGH, 3 MEDIUM resolved)

#### Task 1: Fix CommonTypes Codegen (CODEGEN-1)

- [x] Replace hard-coded type lists in `tooling/codegen/ts-generator.ts` `generateCommonTypesFile()` with iteration over `COMMON_DEFINITIONS` map
- [x] Same fix in `tooling/codegen/rust-generator.ts`
- [x] Run `pnpm run codegen` — verify output matches (no regression)
- [x] Test: add new type to `common.schema.json`, run codegen, verify it appears in both TS and Rust

#### Task 2: Generate Context Assembly Types (CODEGEN-2, CTX-R6)

- [x] Fix `isDefinitionsOnly` check in codegen — schemas with only `definitions` should still generate types
- [x] Generate `ContextAssembleRequestV1` and `ContextAssembleResponseV1` in both TS and Rust
- [x] Compare generated types with hand-written versions in `crates/nexus42/src/context/types.rs`
- [x] Replace hand-written types with generated imports
- [x] Run `pnpm run codegen && cargo test --all`

#### Task 3: Registry Manifest Types (CODEGEN-3)

- [x] Generate named types for nested definitions in `schemas/acp-runtime/registry-manifest.schema.json`
- [x] Replace `serde_json::Value` in `crates/nexus42/src/acp/registry.rs` with generated types

#### Success Criteria

- [x] `pnpm run codegen` produces `ContextAssembleRequestV1` in both TS and Rust
- [x] Adding type to `common.schema.json` + codegen produces it without codegen source changes
- [x] `crates/nexus42/src/context/types.rs` imports from `nexus-contracts`
- [x] `cargo test --all` passes after type replacement

---

## Execution Strategy

### Dependency Graph

```
P0 Bug Fixes (fix/v1.0-p0-bugs) ──merge to main──▶
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ Plan A: Foundation Hardening                             │
│ Branch: feature/v2.0-foundation-hardening                │
│ Effort: M (2–4 sessions)                                 │
└────────────┬──────────────┬────────────────┬─────────────┘
             │              │                │
             ▼              ▼                ▼
    ┌─────────────┐ ┌─────────────┐  ┌──────────────┐
    │ Plan B:     │ │ Plan C:     │  │ Plan E:      │
    │ ACP SDK     │ │ Daemon +    │  │ Codegen &    │
    │ Bridge      │ │ Manuscript  │  │ Contract     │
    │             │ │ Ops         │  │ Alignment    │
    │ M–L         │ │ L           │  │ S            │
    └──────┬──────┘ └──────┬──────┘  └──────────────┘
           │               │
           │    ┌──────────┘
           ▼    ▼
    ┌─────────────┐
    │ Plan D:     │
    │ Auth Flow   │
    │ M           │
    └─────────────┘
```

### Parallelization Windows

| Window | Parallel Plans | Notes |
|--------|---------------|-------|
| During P0 fixes | Plan E (Codegen) | No dependency on bug fixes |
| After Plan A | Plans B + C + E | All depend only on Plan A; B and C parallel; E independent |
| After Plan C milestone 1 | Plan D | Auth needs daemon infrastructure |

### Effort Summary

| Plan | ID | Effort | Agent Sessions | Residuals Addressed |
|------|-----|--------|----------------|-------------------|
| **A: Foundation** | `2026-04-06-foundation-hardening` | M | 2–4 | 15 (7 HIGH, 4 MEDIUM resolved) |
| **B: ACP SDK** | `2026-04-06-acp-sdk-bridge` | M–L | 3–6 | 10 (1 CRIT, 2 HIGH resolved) |
| **C: Daemon+Manuscript** | `2026-04-06-daemon-manuscript-ops` | L | 4–8 | 9 (2 CRIT, 2 HIGH resolved) |
| **D: Auth** | `2026-04-06-auth-flow` | M | 2–3 | 3 (1 HIGH, 2 MEDIUM resolved) |
| **E: Codegen** | `2026-04-06-codegen-alignment` | S | 1–2 | 5 (2 HIGH, 3 MEDIUM resolved) |
| **Total (sequential)** | — | — | **12–23 sessions** | **42 residuals addressed** |
| **Total (parallel)** | — | — | **~8–14 sessions** | — |

---

## Findings Coverage Matrix

### Architecture Review (36 findings)

| # | Finding | Severity | Plan | Resolution |
|---|---------|----------|------|------------|
| 1 | ARCH-1 (shared auth crate) | MEDIUM | D (Task 4) | Daemon owns auth state, CLI reads via API |
| 2 | ARCH-2 (domain types unused in CLI) | LOW | — | Accept — CLI uses contract types for now |
| 3 | DM-1 (MembershipPermissionCheck dup) | LOW | — | Defer V1.2 |
| 4 | DM-2 (enum-as-string verbose) | MEDIUM | — | Defer V1.2 |
| 5 | DM-3 (StyleProfile silent drop) | LOW | — | Accept |
| 6 | SYNC-ARCH-1 (SyncConflict loses response) | HIGH | — | Defer V1.2 (sync activation) |
| 7 | SYNC-ARCH-2 (unchecked_transaction) | MEDIUM | A (Task 2) | Document single-writer assumption |
| 8 | SYNC-ARCH-3 (error source chain lost) | MEDIUM | A (Task 1) | Error strategy fixes this |
| 9 | SYNC-ARCH-4 (BundleBuilder ignores command) | LOW | — | Accept |
| 10 | SYNC-ARCH-5 (test DDL diverges) | LOW | A (Task 3) | Schema dedup fixes this |
| 11 | CLI-DAEMON-1 (workspace init bug) | CRITICAL | P0 | Fixed on `fix/v1.0-p0-bugs` |
| 12 | CLI-DAEMON-2 (missing context route) | CRITICAL | P0 | Fixed on `fix/v1.0-p0-bugs` |
| 13 | CLI-DAEMON-3 (always HTTP 200) | HIGH | A (Task 1) | NexusApiError + IntoResponse |
| 14 | CLI-DAEMON-4 (no shared auth types) | HIGH | D (Task 4) | Daemon owns auth state |
| 15 | CLI-DAEMON-5 (no From<AcpError>) | HIGH | B (Task 5) | From<AcpError> for CliError |
| 16 | CLI-DAEMON-6 (workspace init dup) | MEDIUM | A (Task 3) | Schema dedup |
| 17 | CLI-DAEMON-7 (NEXUS_DIR dup) | MEDIUM | A (Task 3) | Schema dedup includes constants |
| 18 | CLI-DAEMON-8 (no daemon timeout) | MEDIUM | C (Task 2) | Timeout configuration |
| 19 | ACP-ARCH-1 (SDK all stubs) | CRITICAL | B (Tasks 1–4) | Full LocalSet bridge |
| 20 | ACP-ARCH-2 (Registry::default panic) | MEDIUM | B (Task 2) | Return Result |
| 21 | ACP-ARCH-3 (empty query match) | MEDIUM | B (Task 2) | Return None |
| 22 | ACP-ARCH-4 (subscribe closes channel) | LOW | B (Task 4) | Functional event stream |
| 23 | CODEGEN-1 (hard-coded CommonTypes) | HIGH | E (Task 1) | Drive from schema |
| 24 | CODEGEN-2 (context types dropped) | HIGH | E (Task 2) | Fix isDefinitionsOnly |
| 25 | CODEGEN-3 (registry types not generated) | MEDIUM | E (Task 3) | Generate named types |
| 26 | CODEGEN-4 (no runtime validation) | MEDIUM | — | Defer V1.2 |
| 27 | CODEGEN-5 (additionalProperties not enforced) | LOW | — | Accept |
| 28 | CODEGEN-6 (two validators) | LOW | — | Accept |
| 29 | CC-ERR-1 (error propagation inconsistent) | HIGH | A (Task 1) | Unified strategy |
| 30 | CC-ERR-2 (From<anyhow> loses chain) | MEDIUM | A (Task 1) | Capture .source() |
| 31 | CC-TEST-1 (daemon tests minimal) | MEDIUM | A (Tasks 1–5) | Integration tests added |
| 32 | CC-TEST-2 (no property-based testing) | LOW | — | Defer V1.2 |
| 33 | CC-DEP-1 (tokio full features) | LOW | — | Accept |
| 34 | CC-DEP-2 (async-broadcast duplicate) | LOW | — | Accept |
| 35 | CC-BUILD-1 (no cached builds) | LOW | — | Defer |
| 36 | CC-BUILD-2 (no release profile) | LOW | — | Defer |

### Product Review Issues

| # | Issue | Severity | Plan | Resolution |
|---|-------|----------|------|------------|
| 1 | Install requires Rust toolchain | Critical | — | Out of scope (ops task, not V1.0-phase2) |
| 2 | Auth device flow requires platform | High | D | Implement device flow |
| 3 | Manuscript commands return skeleton | High | C (Tasks 3) | Real manuscript operations |
| 4 | Daemon manual start required | Critical | P0 + C | Auto-spawn (P0) + stop (C) |
| 5 | Sync not implemented | Critical | C (Task 4) | sync status with real data |
| 6 | Agent prompt loop incomplete | Critical | B | Full SDK bridge |
| 7 | No end-user documentation | High | — | Separate docs plan (P1) |
| 8 | No installation binaries | High | — | Separate ops plan (P1) |
| 9 | No error suggested fixes | Medium | A (Task 1) | Structured error codes |
| 10 | Skeleton messages not actionable | Medium | C (Tasks 3–4) | Replace with real implementations |
| 11 | No onboarding/tutorial | Medium | — | Separate plan (P2) |

---

## Residual Integration Summary

| Category | Count | Notes |
|----------|-------|-------|
| **Resolved in this plan** | 38 | All tracked residuals covered by at least one plan |
| **Fully resolved** | 28 | Residual closed after implementation |
| **Partially resolved** | 8 | Component addressed, full resolution needs platform API |
| **Deferred to V1.2** | 12 | LOW severity, feature gaps |
| **Accept as-is** | 1 | CTX-R3 (local dirs under user control) |

**Net**: 38 open → ~13 deferred/accepted after V1.0-phase2

---

## V1.1 Go/No-Go Criteria

### Must-Have (all plans complete)

- [ ] All CRITICAL findings resolved (CLI-DAEMON-1, CLI-DAEMON-2, ACP-ARCH-1)
- [ ] `nexus42 agent run <agent>` sends prompts and receives responses
- [ ] `nexus42 daemon start` spawns daemon automatically
- [ ] `nexus42 sync status` shows real data (not "—")
- [ ] Manuscript lifecycle commands functional (create, phase, status)
- [ ] All 445+ existing tests pass
- [ ] `cargo clippy --all -- -D warnings` clean
- [ ] `pnpm run codegen` produces no diff

### End-to-End Workflows

1. **First-time setup**: `init workspace` → `daemon start` → `manuscript create` → success
2. **Manuscript lifecycle**: `create` → `phase review` → `status` → correct state
3. **Agent interaction**: `agent run claude-acp` → send prompt → receive response
4. **Sync visibility**: make change → `sync status` → see pending count
5. **Error handling**: invalid input → structured error with fix suggestion

---

## Appendix: Reference Documents

| Document | Path |
|----------|------|
| V1.0-phase1 Architecture Review | `.agents/archived/knowledge/phase1-architecture-review.md` |
| V1.0-phase1 Product Review | `.agents/archived/knowledge/phase1-product-review.md` |
| V1.0-phase2 Architecture Plan | `.agents/archived/knowledge/phase2-architecture-plan.md` |
| V1.0-phase2 Product Plan | `.agents/archived/knowledge/phase2-product-plan.md` |
| ACP Client Tech Spec | `.agents/archived/knowledge/acp-client-tech-spec-legacy.md` (archived 2026-04-17) |
| Plan Status | `.agents/status.json` |
| Project AGENTS.md | `AGENTS.md` |
