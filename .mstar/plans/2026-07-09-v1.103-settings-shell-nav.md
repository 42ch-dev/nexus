# P0 — Settings Shell & Section Nav (S3)

**Goal:** Turn thin `/settings` into a Settings shell with secondary section nav and nested section routes.

**Architecture:** `SetupGate`→`RootLayout` → nested `<Route path="settings" element={<SettingsShellLayout />}>` with index redirect to `agent` and child routes `agent` | `connection` | `setup` (+ `workspace` when P4). Section bodies live in `apps/web/src/pages/settings/`. `/connect` → permanent redirect. Default landing = Agent. **Supersedes** V1.102 no-`/settings/*` lock. Studio-first shell chrome.

**Implementer SSOT:** `.mstar/iterations/v1.103/specs/settings-shell-ia.md` (route tree, module ownership).

**Tech Stack:** React Router, `apps/web` layout, Design Studio fixtures, Vitest.

**Execution:** mstar-sdd

**Tier:** **Must (P0)**

## Prepare Gates

- specify: done
- clarify: done
- plan: done

## Product Framing

**User value:** One Settings place with clear sections instead of a single Agent-only page.

**Iteration gate:** Must complete before P1–P3 bodies ship.

**Non-goals:** Section body product logic (P1–P4); BYOK; execution-mode matrix; Workspace nav before P4; moving FooterProfiles/daemon strip.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| IA | **S3** shell + secondary nav + nested routes |
| Default | Agent |
| Wire | Prefer `wire_contracts_changed: false` |
| Studio-first | Required for shell chrome |

## Acceptance Criteria

1. Author sees Settings section nav (Agent / Connection / Setup); Workspace nav absent unless P4 runs.
2. Nested routes resolve under RootLayout.
3. Default landing is Agent.
4. Studio shell fixtures accepted before App wiring claim.
5. Prefer no schemas change.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.103/specs/settings-shell-ia.md` (shell + nav copy table)
- Process: `.mstar/iterations/v1.103/guides/studio-first-visual-then-app.md`
- Compass: `.mstar/iterations/v1.103/delivery-compass.md`

---

### Task 1: Lock Shell IA Contract

**Files:**
- Refine: `.mstar/iterations/v1.103/specs/settings-shell-ia.md` if review requires

- [x] Confirm routes, nav labels, default Agent, Non-Goals. (architect-locked §5.2; no refine needed)

### Task 2: Studio — Settings Shell Fixture

**Files:**
- Create/Modify: Design Studio Settings shell chrome fixtures

- [x] Visual acceptance of section nav + empty frames.

### Task 3: App — Shell Layout + Nested Routes

**Files:**
- Modify: `apps/web/src/App.tsx` (nested `settings` tree + remove flat `settings` route)
- Create: `apps/web/src/pages/settings/settings-shell-layout.tsx`
- Refactor: `apps/web/src/pages/settings-page.tsx` → `settings-agent-section.tsx` (Agent body; shell extracts nav/outlet)
- Test: settings shell route tests

- [x] Nested routes per shell-ia spec; index redirect to `agent`.
- [x] Vitest coverage for shell navigation + outlet.

### Task 4: Verify

- [x] Web + Studio tests green; no schemas drift.

## Plan self-review (PM before locked)

1. S3 IA explicit; bodies deferred to later plans.
2. Studio-first required.
3. Wire preference false.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-09-v1.103-settings-shell-nav/`


## Review Gate Summary

| Gate | Decision | Notes |
|------|----------|-------|
| QC tri | Approve with residuals | qc1 Approve; qc2 Request Changes → fix d6417f48 → Approve; qc3 Approve |
| QA | Pass with residuals | mandatory — all Must P0 ACs verified; R-V1103P0QC-S001..S005 open |

Open residuals: R-V1103P0QC-S001..S005 in status.json.
