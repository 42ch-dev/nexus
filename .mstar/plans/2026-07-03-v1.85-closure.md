# P-last — V1.85 Closure (QC, QA, Compound, and Closeout)

| Field | Value |
|-------|-------|
| plan_id | `2026-07-03-v1.85-closure` |
| iteration | V1.85 |
| type | closure |
| status | Todo |
| primary_spec | `.mstar/iterations/v1.85/delivery-compass.md` |
| working_branch | n/a (runs on `iteration/v1.85` HEAD) |
| merge_target | `iteration/v1.85` |

## Context

Closure plan for V1.85. After P0 (app icon) and P1 (doc hygiene) merge to `iteration/v1.85`, this plan runs QC tri-review + QA on the integrated HEAD, performs the iteration-close compound round, updates the roadmap, does Profile B compaction, and opens the PR to `main`.

## Specify

- **Problem**: P0 and P1 deliver on parallel branches; the iteration needs integrated review/verification and formal closeout before PR.
- **User value**: Confidence the branded icon + doc hygiene land coherently and the two V1.84 residuals are genuinely closed.
- **Scope**: QC tri-review on `iteration/v1.85` HEAD; QA verification (icon formats resolve + desktop-bundle build + contract invariant + visual dock check); compound round; roadmap update; Profile B compaction; PR to `main`.
- **Non-goals**: new implementation beyond fix-wave responses; reopening scope.
- **DoD (draft)**: QC 3/3 Approve; QA Pass; `R-V184CL-QC1-S001` / `R-V184CL-QC2-S001` resolved; compass `status: completed`; PR open.

## Clarify

- **QC scope** = integrated `iteration/v1.85` HEAD vs `main`. Review range covers P0 + P1.
- **QA scope** = icon formats resolve + `tauri.conf.json` refs + desktop-bundle build (local; CI on push) + `wire_contracts_changed: false` (verify: `git diff --name-only main...iteration/v1.85 | grep -cE '^(schemas/|crates/nexus-contracts/|crates/nexus-daemon|packages/nexus-ui/package.json)'` must be 0) + dock icon visual (capture or user sign-off).
- **Compound candidates**: the `tauri icon` regeneration workflow (if it yields a reusable repo convention) — assess via Q1–Q8; likely skip if general/tooling-only.
- No open high-impact ambiguity.

## Plan

- **QC tri-review**: qc1 (architecture/maintainability — source provenance, LFS scope, README accuracy), qc2 (security/correctness — no bundle/path issue, icon refs resolve), qc3 (performance/reliability — repo/LFS size impact, CI desktop-build). Targeted re-review after any fix-wave.
- **QA**: icon formats present + bundle refs resolve + desktop-bundle build + contract-invariant one-liner + dock icon visual.
- **Compound** (`mstar-compound` Q1–Q8): crystallize only if a project-specific reusable rule emerges.
- **Roadmap**: compass §7 current iteration → `delivered`.
- **Profile B compaction**: archive the 4 V1.85 plan rows.
- **PR**: `iteration/v1.85` → `main`.

## Tasks

- [ ] T1 — Dispatch QC tri-review on `iteration/v1.85` HEAD; address fix-wave findings.
- [ ] T2 — QA verification (icon formats + bundle build + contracts-unchanged + visual).
- [ ] T3 — Compound round (or document "none" with reason).
- [ ] T4 — Compass §7 → `delivered`; §10/§11 filled; frontmatter `status: completed` + `end_date`.
- [ ] T5 — Profile B compaction.
- [ ] T6 — `status.json` residuals `R-V184CL-QC1-S001` / `R-V184CL-QC2-S001` → resolved (P1 owner; verified in QA); open PR to `main`.
- [ ] T7 — PM signoff.

## Acceptance

- QC tri-review 3/3 Approve; QA Pass.
- `R-V184CL-QC1-S001` / `R-V184CL-QC2-S001` resolved; branded icon shipped; `wire_contracts_changed: false`.
- Compass `status: completed` + `end_date`; Profile B done; PR open.
