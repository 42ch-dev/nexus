# V1.44 Multi-Volume Hardening — Plan

**Plan ID**: `2026-06-13-v1.44-multi-volume-hardening`  
**Title**: P2 — Multi-volume completion + supervisor volume context  
**Created**: 2026-06-13  
**Status**: Todo  
**Owner**: `@fullstack-dev`  
**Task category**: `feature`  
**Working branch**: `feature/v1.44-multi-volume-hardening`  
**Merge target**: `iteration/v1.44`  
**Depends on**: `2026-06-13-v1.44-harness-docs-prepare` (merged)  
**Parallel with**: P0, P1  
**Pre-implement gate**: `main` ≥ V1.43 ship  
**Iteration compass**: [v1.44-novel-quality-and-serial-hardening-delivery-compass-v1.md](../iterations/v1.44-novel-quality-and-serial-hardening-delivery-compass-v1.md)  
**Primary specs**:

- [novel-workflow-profile.md](../knowledge/specs/novel-workflow-profile.md) §4.5.4–§6.1
- [creator-workflow.md](../knowledge/specs/creator-workflow.md)

**Residuals**: R-V142P1-QC1-F-002, R-V142P1-QC1-F-004

---

## 1. Problem Statement

V1.42 P1 shipped multi-volume PK, `next_chapter_volume_aware`, and `evaluate_after_persist_volume_aware`. QC flagged two remaining edges:

1. **F-002**: `is_work_completed` in `nexus-local-db` uses flat `current_chapter >= total_planned_chapters` and `list_chapters().len() == total` — fragile when volume-aware chapter numbering repeats across volumes.
2. **F-004**: `supervisor.rs` `ChainAction::NextChapter` logs `next_volume` but calls `enqueue_auto_chain_step(..., Some(next_chapter), ...)` without volume; `WorkFields` / `build_auto_chain_schedule` have no `volume` field, so `novel-writing` preset input loses cross-volume context.

**Out of scope**: R-V142P1-QC1-F-003 (reconcile volume-aware) — **resolved** V1.43 P-last.

---

## 2. Goals

1. Harden `work_chapters::is_work_completed` for multi-volume Works per §6.1 invariants (all rows finalized across volumes; correct progress predicate).
2. Thread `next_volume` through supervisor → `enqueue_auto_chain_step` → `build_auto_chain_schedule` → `WorkFields` → `build_preset_input` (`volume` template var for `novel-writing`).
3. Extend / fix `supervisor_cross_volume.rs` and add `work_chapters` completion regression tests.
4. Close F-002 and F-004 in `status.json` with `target: V1.44 P2`.

---

## 3. Non-goals

- New volume PK schema (V1.42 migration sufficient)
- Re-open F-003 reconcile path
- DF-69 / review-master CLI
- `creator works status` volume column UX (unless required by AC — prefer minimal)

---

## 4. Acceptance Criteria

1. Hermetic test: 2-volume Work completes only when **all** volume rows are `finalized` and progress predicate holds.
2. Hermetic test: after volume boundary, enqueued `novel-writing` schedule `preset.input` includes correct `volume` + `chapter`.
3. Existing `supervisor_cross_volume.rs` tests remain green; add failing-case regression if not already covered.
4. Residual scopes in `status.json` corrected from stale `auto_chain.rs is_work_completed` to `work_chapters.rs`.
5. F-002 and F-004 `lifecycle: resolved` on ship.

---

## 5. Tasks

- [ ] T1: Audit `is_work_completed` vs `next_chapter_volume_aware` / `evaluate_after_persist_volume_aware` interaction.
- [ ] T2: Fix completion predicate (F-002) in `crates/nexus-local-db/src/work_chapters.rs`.
- [ ] T3: Add `volume: Option<i32>` to `WorkFields`; extend `build_auto_chain_schedule` + `enqueue_auto_chain_step` signature; wire supervisor `NextChapter` arm (F-004).
- [ ] T4: Regression tests (`nexus-orchestration/tests/supervisor_cross_volume.rs`, `nexus-local-db` multi-volume completion).
- [ ] T5: Spec amendment only if §6.1 contract wording changes.

---

## 6. Verification

```bash
cargo test -p nexus-orchestration --test supervisor_cross_volume
cargo test -p nexus-local-db --test v142_migration_fixes
rg -n 'next_volume|is_work_completed|WorkFields' crates/nexus-orchestration/src/schedule/supervisor.rs crates/nexus-local-db/src/work_chapters.rs
```
