# P-last — V1.69 Closure — Plan

**Iteration**: V1.69 — Design System Maturation & Canvas Draft
**Plan ID**: `2026-06-27-v1.69-closure`
**Status**: Todo (pending P0 + P1 merge)
**Effort**: S
**Compass**: [v1.69-design-system-maturation-and-canvas-draft-compass-v1.md](../iterations/v1.69-design-system-maturation-and-canvas-draft-compass-v1.md) §3 Wave 3
**Owner**: `@project-manager`
**Branch policy**: direct commit to `iteration/v1.69`
**Wire contracts changed**: FALSE

## Goal

Close V1.69: verify merges, converge residuals in SSOT, promote specs, refresh stale `metadata.tech_debt_summary`, document V1.70 deferrals, Profile B compaction, single iteration PR to `main`.

## Scope (in)

- [ ] Merge verification: `iteration/v1.69` HEAD contains P0 (DESIGN.md Production + DESIGN.dark.md + Canvas Draft + web-ui.md amend) + P1 (4 frontend residuals closed).
- [ ] Residual convergence: mark `R-V167P1-QC1-S1/S2`, `R-V167P1-QC3-S1/S2` as `lifecycle: resolved` in `status.json` root `residual_findings` with `resolution.plan_id` + `resolution.commit`; verify 0 unintended residual changes.
- [ ] Refresh stale `metadata.tech_debt_summary` (V1.67 left it inconsistent): either accurate rollup via `scripts/tech-debt-rollup.sh` or documented retirement.
- [ ] Document V1.70 deferrals: the 21+ open low-severity V1.67-deferred QC-suggestion residuals remain V1.70+ targets; record in `notes.json` timeline + iteration compass §5.
- [ ] Spec promotion: confirm `DESIGN.md` Level 3 tag, `DESIGN.dark.md` present, `canvas-strategy-surface.md` Draft status, `web-ui.md` V1.69 stage section.
- [ ] `metadata.latest_ship` block: V1.69 snapshot (compass, shipped_at, plan_count, plan_ids, wire_contracts_changed: false, profile_b_compaction: done).
- [ ] Profile B compaction: move Done plan rows to `.mstar/archived/plans/<plan-id>.json` + append IDs (strings) to `.mstar/archived/plans-done.json` `plans` array; clear `status.json.plans[]` (only non-Done remain — should be 0).
- [ ] Retire `iteration/v1.69` branch pointer (post-merge, like V1.68 `a36f53b3`).
- [ ] Single iteration PR `iteration/v1.69` → `main` with review trail + CI gate per root `AGENTS.md` merge discipline.

## Non-goals

- No code change in P-last (closure-only).
- No spec content authoring (P0/P1 own content; P-last only flips status fields).

## Acceptance criteria

- All 4 residuals closed in SSOT with resolution metadata.
- `metadata.tech_debt_summary` accurate or retired.
- `metadata.latest_ship` V1.69 block present and accurate.
- Profile B compaction verified: `plans[]` empty; `plans-done.json` `plans` array has 4 new string entries; `.mstar/archived/plans/` has 4 new JSON files.
- PR merged to `main`; `iteration/v1.69` retired.

## Validation

- `python3 -c "import json; d=json.load(open('.mstar/archived/plans-done.json')); assert all(isinstance(p, str) for p in d['plans'])"`
- `jq '.plans | length' .mstar/status.json` → 0
- `jq '.metadata.latest_ship.iteration' .mstar/status.json` → `"V1.69"`
- CI green on the iteration PR.
