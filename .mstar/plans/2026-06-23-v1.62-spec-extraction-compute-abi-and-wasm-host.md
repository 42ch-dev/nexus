---
plan_id: 2026-06-23-v1.62-spec-extraction-compute-abi-and-wasm-host
title: V1.62 P2 — Spec Extraction: compute-module-abi.md + wasm-host.md + 4 Amendments
iteration: V1.62
slot: P2
status: Todo
effort: L
owner: "@writing-specialist"
review: Tri-review (docs-only — may skip QC with explicit reason if PM waives) + mid-QA
wave: 2
dependency: P0 merged to integration
working_branch: feature/v1.62-spec-extraction
merge_target: iteration/v1.62
---

# P2: Spec Extraction — 2 NEW Master Specs + 4 Amendments

Convert V1.61's scattered compute-ABI knowledge (compass + crate AGENTS.md + module README) into durable Master specs under `.mstar/knowledge/specs/`. Amend 4 existing specs to reflect V1.62 changes.

Full task list in compass §1.1 P2 (T1–T6). Key deliverables:

## Tasks

- [ ] T1: Create `.mstar/knowledge/specs/compute-module-abi.md` (NEW Master, Status: Normative — V1.62 Shipped)
  - Header: Status / Document class / Scope / Last updated / Related
  - §1: V1 envelope ABI overview (stateless pure function; ComputeInput → ComputeOutput)
  - §2: Module exports table (`alloc`/`init`/`compute` with signatures)
  - §3: Host import ABI table (`nexus::kb_read`, `nexus::narrative_query` with signatures)
  - §4: `ComputeInput` envelope structure (key_blocks, world_ref, narrative_state, invocation) — content from new `schemas/local-api/compute/compute-input.schema.json`
  - §5: `ComputeOutput` 4-part envelope (state_delta, timeline_events, new_key_blocks, battle_report)
  - §6: Marshalling convention (JSON over linear memory; alloc + ptr/len/cap; error sentinels -1/-2)
  - §7: `manifest.json` contract — required + optional fields + NEW `schemas` block (cross-ref P1)
  - §8: Sandbox model cross-ref (pointer to wasm-host.md)
  - §9: Versioning (nexus_abi_version; additive evolution; V2 deferred items from V1.61 compass)
- [ ] T2: Create `.mstar/knowledge/specs/wasm-host.md` (NEW Master, Status: Normative — V1.62 Shipped)
  - §1: wasmtime runtime overview; crate role
  - §2: Engine lifecycle (single daemon-wide `WasmEngine`; module cache `Arc<RwLock<HashMap>>`)
  - §3: Per-invocation sandbox (compass Q6 — fresh Store + Instance per `compute()`)
  - §4: Sandbox limits (fuel default 10M; memory cap default 64 MiB; wall-time default 30s via epoch interruption; manifest overrides)
  - §5: Wall-time watchdog mechanism (`spawn_watchdog` thread; 25ms step; epoch bump on deadline)
  - §6: Embedded module loading (`build.rs` compile-from-source; `include_dir!`; `MODULE_IDS` registration)
  - §7: User module discovery (`~/.nexus42/modules/` scan)
  - §8: Error taxonomy (`ComputeError` variants including new `ManifestValidationFailed` from P1)
  - §9: Host function implementation (`kb_read` reads `InvocationState`; `narrative_query` passes through `narrative_state`)
- [ ] T3: Amend `.mstar/knowledge/specs/schemas-directory-layout.md` post-P0 — seal the new tree shape in spec language (coordinate with P0 T15; this task is the spec-seal once code is merged)
- [ ] T4: Amend `.mstar/knowledge/specs/entity-scope-model.md` §5.5 — add computable-flag semantics + structured validation mode subsection (closes `R-V161P1-LOW-001`); reference manifest-declared shapes (not deleted entity-attributes/entity-state schemas); coordinate with P1
- [ ] T5: Amend `.mstar/knowledge/specs/orchestration-engine.md` §8 — add `narrative.compute` capability + `combat-engine` preset entries (deferred from V1.61 P3)
- [ ] T6: Update `.mstar/knowledge/specs/README.md` Master index tables — add 2 new specs; update amended-spec statuses to "V1.62 Shipped"

## Done when

- `compute-module-abi.md` exists, header declares `Status: Normative — V1.62 Shipped`, `Document class: Master`, all 9 sections present
- `wasm-host.md` exists, same header shape, all 9 sections present
- `schemas-directory-layout.md` post-P0 tree shape sealed in normative language
- `entity-scope-model.md` §5.5 has the computable-flag + structured-validation amendment; `R-V161P1-LOW-001` closure evidence recorded
- `orchestration-engine.md` §8 has `narrative.compute` + `combat-engine` preset entries
- `specs/README.md` Master index updated; both new specs listed; all amendments reflected
- All cross-references between the 2 new specs + 4 amended specs are bidirectional and correct
- No broken links (verify with `grep` for cited paths)

## Coordination notes

- **Docs-only plan**: QC tri-review may be skipped with explicit PM waiver (per PM routing table — "Tech spec only: @architect, QC may be skipped with explicit reason"). PM default: still run tri-review since 2 NEW Master specs is significant normative content. Re-evaluate at Wave 2 dispatch.
- Coordinate with P0 T15 on `schemas-directory-layout.md` — P0 owns the code-driven rewrite; T3 here is the spec-seal language upgrade.
- Coordinate with P1 on `ManifestValidationFailed` variant name (T2 §8 references it).
- Do NOT touch `schemas/` or `crates/` — code-only territories of P0/P1.
