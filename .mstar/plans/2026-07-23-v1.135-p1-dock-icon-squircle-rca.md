# Dock icon macOS squircle — deep RCA beyond opaque PNG

> **For agentic workers:** REQUIRED SUB-SKILL: `mstar-sdd`. Checkbox steps. **Do not close as Done on “PNG is opaque” or Studio VI-004 alone — author Dock confirm or exhausted RCA + Blocked.**
>
> **Iteration compass:** `.mstar/iterations/v1.135/delivery-compass.md`  
> **Product spec (verify gate locked):** `.mstar/iterations/v1.135/specs/p1-dock-icon-pipeline.md`  
> **Author evidence:** Dock tooltip `nexus-desktop` as sharp square (not squircle) after V1.134 opaque full-bleed  
> **Prior RCA:** `.mstar/iterations/v1.134/guides/p1-app-icon-rca.md` (`DONE_WITH_CONCERNS` — author confirm never obtained)  
> **Carries forward:** `R-V1134P1-001` (closes on **P1G-1** author Dock squircle confirm — see primary spec)

## Metadata

| Field | Value |
|-------|-------|
| plan_id | `2026-07-23-v1.135-p1-dock-icon-squircle-rca` |
| iteration_id | V1.135 |
| priority | **Must** |
| wave | 1 · critical path (parallel-capable with P0) |
| primary_spec | `.mstar/iterations/v1.135/specs/p1-dock-icon-pipeline.md` |
| spec_integration_branch | `iteration/v1.135` |
| working_branch | `plan/v1.135-p1-dock-icon-squircle-rca` |
| merge_target | `iteration/v1.135` |
| wire_contracts_changed | false |

## Problem Statement

**User impact:** For ~6 iterations the macOS Dock tile for Nexus desktop remains a **sharp-edged square**, unlike peer apps’ squircles. V1.134 hypothesized transparent margins and shipped opaque full-bleed compose + docs. Author screenshot **still** shows a raw square `nexus-desktop` Dock icon. Treating opaque PNG as “fixed” without Dock proof was a **process failure** — `R-V1134P1-001` remains open.

### Anti-misread (read before closing plan)

| Misread | Correct interpretation |
|---------|------------------------|
| “Opaque PNG = Done” | **Insufficient** — required baseline, not closure |
| “Studio VI-004 pass = Done” | **Supporting only** — live macOS Dock squircle is the product surface (`P1G-1`) |
| “Preview PNG / opacity metadata = Done” | **Insufficient** — required baseline (H1), not closure |
| “V1.134 P1 DONE_WITH_CONCERNS = shipped” | Author never confirmed; **reopened** in V1.135 |
| “killall Dock optional” | **Mandatory** in author verify ritual before judging |

## User value

Desktop app looks like a normal macOS citizen in the Dock.

## Goal

1. Multi-hypothesis RCA past compose opacity (ordered — see primary spec `P1G-4`):
   - Generated `.icns` / iconset contents (alpha, sizes, mask)
   - `tauri icon` / `bundle.icon` / `Info.plist` / `CFBundleIconFile` / asset catalog
   - Dev path: `cargo run` / `pnpm dev:desktop` vs installed `.app` — which binary Dock actually shows
   - Bundle identity / duplicate apps / LaunchServices icon cache beyond `killall Dock`
   - Whether `logo-primary-square.svg` edge geometry reads as “square plate” even under squircle (vs true mask failure)
2. Implement the winning fix; regenerate icons; document author verify steps.
3. **Hard gate:** Author Dock visual confirm (`P1G-1`) **or** plan stays open / **Blocked** with next candidate named — **never** Approve as Done on Studio/preview/opacity alone.

## Priority

**Must.** No Stretch. Prefer deep RCA over another single-line compose tweak without Dock proof.

## Architecture (architect-locked — normative spec)

**Authority:** `.mstar/iterations/v1.135/specs/p1-dock-icon-pipeline.md` (Architect §5.2 sign-off 2026-07-23).

### Pipeline interfaces

| Stage | Command / file | Output |
|-------|----------------|--------|
| Compose | `pnpm --filter desktop run icons:compose` → `compose-app-icon.mjs` | `source/source-1024.png`, `app-icon-preview-256.png` |
| Generate | `pnpm --filter desktop run icons:generate` | `icon.icns`, `32x32.png`, `128x128.png`, `128x128@2x.png` |
| Bundle | `tauri.conf.json` `bundle.icon[]` + build hooks | `Nexus.app/Contents/Resources/Nexus.icns`, `CFBundleIconFile` |
| Verify | Author ritual (P1G-1) | Live Dock squircle — **not** Studio VI-004 |

### Ordered hypothesis list (implement H1 → H7)

| ID | Hypothesis | First falsification action |
|----|------------|---------------------------|
| H1 | Transparent margins in source (V1.134 baseline) | `sharp` metadata `hasAlpha: false` on `source-1024.png` |
| H2 | Defective / stale `.icns` or iconset | `iconutil` / `sips` on generated `icon.icns` members |
| H3 | Bundle ships stale `Resources/Nexus.icns` | Compare built `.app` icns mtime/hash vs `src-tauri/icons/icon.icns` |
| H4 | Dock shows wrong binary (duplicate/stale `.app`) | Document build path; `ps` / bundle identity `io.nexus42.desktop` |
| H5 | LaunchServices cache beyond `killall Dock` | Remove old `.app`, Trash, reinstall, repeat ritual |
| H6 | Square plate geometry vs mask failure | Squircle overlay on source; compare peer Dock apps |
| H7 | Debug vs release bundle icon path mismatch | Compare `target/debug/bundle` vs `target/release/bundle` |

**Hard gate:** Plan not Done until P1G-1 author Dock confirm **or** Blocked with exhausted H1–H7 + next candidate named.

- Generated artifacts remain gitignored; fix lives in compose/generate pipeline and/or Tauri config + docs
- Evidence in `.mstar/iterations/v1.135/guides/p1-dock-icon-rca.md`

**Execution:** mstar-sdd

## Acceptance Criteria

Plan ACs map to the primary spec **P1G** gates (compass **AC-I3**). Do not close the plan on Studio VI-004 or PNG opacity alone.

| Plan AC | Primary spec | Criterion | Evidence |
|---------|--------------|-----------|----------|
| **AC-1** | **P1G-4** | Written multi-hypothesis RCA with primary root cause selected (not opacity-only) | `guides/p1-dock-icon-rca.md` |
| **AC-2** | — (pipeline) | Fix landed in pipeline and/or bundle config; regenerate path documented | code + `icons/README.md` |
| **AC-3** | H1 baseline | Post-fix proof: opaque full-bleed still holds **if** still required + icns/app icon inspection notes | guide |
| **AC-4** | **P1G-1**, **P1G-5** | **Author Dock squircle confirm** after quit / rebuild / `killall Dock` / relaunch — **or** residual `@author` with explicit “not Done” | author / `R-V1134P1-001` |
| **AC-5** | — (`apps/desktop/AGENTS.md`, `icons/README.md`) | Durable docs state Dock-done ≠ Studio VI-004 / opacity-only; author ritual mandatory | docs |
| **AC-6** | **P1G-5** (anti-pattern) | Plan **not** marked Done if only Studio VI-004 or PNG metadata passed | QC gate |

**P1G cross-reference (product language):** P1G-1 Dock shape · P1G-2 identity · P1G-3 verify ritual · P1G-4 RCA depth · P1G-5 plan closure.

### Author Dock confirm (product language)

1. Quit all Nexus instances.
2. Rebuild/reinstall the `.app` under test (document path).
3. `killall Dock`.
4. Relaunch; inspect Dock tile.
5. **Pass:** macOS squircle rounding. **Fail:** plan continues or Blocked.

## Non-Goals

| Non-goal | Why |
|----------|-----|
| Sidebar create IA | P0 |
| iOS/Android icon polish | Desktop Dock only |
| Signing/notarization | Unrelated |
| Claiming Done from Studio VI-004 alone | Dock is the product surface |
| Closing `R-V1134P1-001` without author Dock eyeball | Residual policy |

## Global Constraints

- Do not commit large generated icon binaries if gitignore forbids; regenerate in CI/dev
- Do not mark plan Done without **AC-4** / **P1G-1** author path or explicit Blocked escalation
- Carry forward `R-V1134P1-001` — close only when **AC-4** / **P1G-1** author Dock squircle confirm passes; `R-V1134P1-002` remains supporting-only

---

### Task 1: Deep RCA · Must

**Read first:** `compose-app-icon.mjs`, `icons/README.md`, `tauri.conf.json`, `.mstar/iterations/v1.134/guides/p1-app-icon-rca.md`, built `Nexus.app/Contents/Info.plist`.

**RCA template sections (required in guide):** executive summary, H1–H7 checklist with pass/fail per check, primary root cause, author confirm block (date/command/outcome).

- [ ] **Step 1:** Execute H1→H7 falsification checks per primary spec; record evidence (commands, file paths, hashes).
- [ ] **Step 2:** Select primary root cause; write `.mstar/iterations/v1.135/guides/p1-dock-icon-rca.md`.
- [ ] **Step 3:** Commit guide + any diagnostic scripts (no secrets).

### Task 2: Implement fix · Must

**Files:** `apps/desktop/src-tauri/icons/source/compose-app-icon.mjs`, `tauri.conf.json` (if needed), `package.json` scripts, generated icons (local only).

- [ ] **Step 1:** Apply pipeline/config fix from RCA winning hypothesis.
- [ ] **Step 2:** `pnpm --filter desktop run icons:generate`; document verify steps in `icons/README.md`.
- [ ] **Step 3:** Commit.

### Task 3: Spec + durable docs · Must

- [x] **Step 1:** Harden `specs/p1-dock-icon-pipeline.md` — **done** (Architect §5.2).
- [ ] **Step 2:** Update `apps/desktop/AGENTS.md` + `icons/README.md` — Dock-done ≠ opacity-only; author ritual mandatory.
- [ ] **Step 3:** Commit. *(Knowledge `{KNOWLEDGE_DIR}` updates optional in separate implementer pass if needed — not architect this turn)*

### Task 4: Author Dock gate · Must

- [ ] **Step 1:** Provide author checklist (quit, rebuild, killall Dock, relaunch) in guide.
- [ ] **Step 2:** Record confirm in RCA guide **and** close `R-V1134P1-001` **or** leave open residual — **do not Done without one**.

## Review Gate Summary

> Filled after QC.

## QA Gate Summary

> Filled after QA.
