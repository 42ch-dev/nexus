# V1.53 Capability Registry Unification — Plan

**Plan ID**: `2026-06-20-v1.53-capability-registry-unification`  
**Title**: P0 — CapabilityRegistry SSOT refactor (adapter-first introduce → cutover → cleanup)  
**Created**: 2026-06-20  
**Status**: Pending  
**Owner**: `@fullstack-dev`  
**Agents**: `@fullstack-dev`, `@qa-engineer`  
**Task category**: `logic`  
**Working branch**: `feature/v1.53-capability-registry-unification`  
**Merge target**: `iteration/v1.53`  
**Iteration compass**: [v1.53-capability-surface-completion-and-skills-cli-cleanup-delivery-compass-v1.md](../iterations/v1.53-capability-surface-completion-and-skills-cli-cleanup-delivery-compass-v1.md)

---

## 1. Scope

Introduce a central `CapabilityRegistry` runtime SSOT with fields id → access → admission → handler → ACP wire → failure mode → test vector. Use an adapter-first migration: introduce registry seam, cut over existing dispatch, then remove stale parallel paths.

## 2. Acceptance

1. Registry row shape includes id, access, admission, handler, ACP wire, failure mode, and handler test vector.
2. Three sub-phase cutover triggers are explicit in the implementation plan/report:
   - Trigger 1: registry introduced behind adapter with parity tests.
   - Trigger 2: all active `nexus.*` dispatch entrypoints route through registry.
   - Trigger 3: old dispatch tables removed or recorded as deferred residuals.
3. No lingering double-path remains at plan close.
4. Any temporary parallel path is recorded in this plan Roadmap / Batch Plan or in `status.json` residual/deferred state.
5. `capability-registry.md` field semantics are filled enough for P1 to add rows.

## 3. Dependencies

- P-1 Done.
- `capability-registry.md` Draft overlay framework exists.

## 4. Branch policy ref

Create `feature/v1.53-capability-registry-unification` from `iteration/v1.53`; merge back to `iteration/v1.53` before P1/P-c PR.

## 5. Review strategy

Tri-review + emphasis. Reviewers should focus on duplicate dispatch risk, failure-mode consistency, and cutover completeness.

## 6. Notes/open sub-grills

- Resolve naming: `nexus.kb_snapshot.read` vs `nexus.kb.keyblock.read` before P1 row creation.
- Do not expand to standalone MCP, cloud assemble, platform publish, or third-party registry.
