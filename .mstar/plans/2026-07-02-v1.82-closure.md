# P-last — V1.82 Closure

**plan_id**: `2026-07-02-v1.82-closure`
**Iteration**: V1.82 (Compass: `v1.82/delivery-compass.md`)
**Status**: Todo
**Owner**: `@project-manager`
**Task category**: docs
**Working branch**: `feature/v1.82-closure` from `iteration/v1.82`
**Merge target**: `iteration/v1.82`
**Depends on**: P0 + P1 Done + QC tri-review + QA Pass.

> Closure track: compound + **7 V1.81 low residual sweep (SP-3)** + compaction +
> PR + trackers/STRATEGY. Runs sequentially after P0 + P1 merge. Note: this plan
> is final plan closure; iteration-close (Phase 3 §3.1–§3.5) still runs as a
> separate gate after all plans Done.

## 1. Problem statement

After P0 + P1 ship, V1.82 needs the standard closure plus a companion sweep of
the 7 open low V1.81 residuals (the deferred QC + Greptile suggestions).

## 2. Scope

- **T1 — Sweep 7 V1.81 low residuals (SP-3)** — dispatch micro-fixes to the
  right owners:
  - `R-V181P0-QC1-S001` (validate-draft step — backend) — `@fullstack-dev`
  - `R-V181P0-QC1-S002` (typed error mapping — backend) — `@fullstack-dev`
  - `R-V181P0-QC1-S005` (narrative length cap — backend) — `@fullstack-dev`
  - `R-V181P0-QC1-S003` (world-selector tracker entry — PM; may be closed by
    P1's worlds-endpoint wiring removing the `simplify:` placeholder)
  - `R-V181P0-GRPT-001` (InsufficientDataState copy — frontend) — `@frontend-dev`
  - `R-V181P0-GRPT-002` (`compute_distinct_keyword_count` doc — backend) — `@fullstack-dev`
  - `R-V181P0-GRPT-003` (double poll timer — frontend; may be closed by P1 T4)
- **T2 — Compound round** (`mstar-compound`): candidates — the per-(creator,
  world) composite-key cache pattern; any greploop/QC lesson. Update CONCEPTS.md
  if new nouns.
- **T3 — Deferred-tracker + STRATEGY**: per-World narrative shipped (BL-12 fully
  closed — V1.81 partial → V1.82 complete); STRATEGY decision-log V1.82 entry.
- **T4 — Profile B compaction**: archive 4 Done plans to `archived/plans/` +
  `plans-done.json` (string IDs); clear `plans[]`.
- **T5 — Trackers/README/status.json**: update `iterations/README.md`,
  `deferred-features-cross-version-tracker.md`, `status.json` (`latest_ship`,
  iteration pointers).
- **T6 — PR**: `iteration/v1.82` → `main`.

## 3. Plan (task list)
- [ ] T1 — Sweep 7 V1.81 residuals — owners per item / `@project-manager`
- [ ] T2 — Compound round — `@project-manager`
- [ ] T3 — Deferred-tracker (BL-12 fully closed) + STRATEGY — `@project-manager`
- [ ] T4 — Profile B compaction (4 plans) — `@project-manager`
- [ ] T5 — Trackers/README/status.json — `@project-manager`
- [ ] T6 — PR `iteration/v1.82` → `main` — `@project-manager`

## 4. Acceptance / DoD
- 7 V1.81 residuals `lifecycle: resolved` in `status.json`.
- Compound round complete (docs or "无可结晶知识"); new docs registered.
- Deferred-tracker (BL-12 closed) + STRATEGY + README updated.
- Profile B compaction verified; PR open against `main`.
- Compass frontmatter `status: completed` + `end_date` at Phase 3 §3.4 (not here).

## 5. Non-goals
- No new feature work (closure + residual sweep only).
- iteration-close (Phase 3 §3.1–§3.5) is a separate gate after this plan Done.
