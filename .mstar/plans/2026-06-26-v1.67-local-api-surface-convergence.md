# Plan — V1.67 P0: Local API Surface Convergence (headline)

**plan_id**: `2026-06-26-v1.67-local-api-surface-convergence`
**Iteration**: V1.67 · **Wave**: 1 · **Track**: A · **Effort**: XL
**Owner**: `@fullstack-dev` (backend) + `@frontend-dev` (UI adaptation)
**Status**: Todo
**Depends on**: P-1 + Phase 2b (§5 #1/#2/#3/#4 LOCKED)
**Working branch**: `feature/v1.67-local-api-surface-convergence`
**Compass**: §1.1 Track A, §1.3 Wire note, §5 #1–#4

## Goal

Converge the Local API surface to one error-envelope + one array-key convention (`items`) + casing discipline + a sort-param contract. The V1.67 headline sweep. This is what makes the Local API consumable and is the natural compaction target before the V1.68 body editor adds a 4th writer.

## Residual coverage (5)

- `R-V164-FE1-ORCH` — orchestration error-envelope consolidation (`schedules.rs` ~1300 LoC + `sessions.rs` + `presets.rs` → canonical `ErrorResponse`/`NexusApiError`).
- `R-V164-QC1-CASING` — error-code casing alignment to `local-api-surface-conventions.md` §3.2.
- `R-V163P3-SURF-003` (F-P3) — response array-key rename → `items` (**pre-1.0 breaking wire change**).
- `R-V163P3-SURF-005` (F-F1) — sort params (additive).
- `R-V164-QC1-S3-P1` — F-P3 UI adaptation (`apps/web` consumers).

## Acceptance criteria

- AC1: All orchestration list/detail handlers return the canonical `ErrorResponse` envelope (no ad-hoc tuples). Verified by handler-level tests.
- AC2: All emitted `ErrorResponse.code` strings match the casing convention. Convention documented + asserted.
- AC3: All list endpoints emit `{ items: [...], pagination: {...} }` (per-resource array keys removed). `pnpm run codegen` regenerates TS + Rust; `check-wire-drift` clean.
- AC4: List endpoints accept the documented `?sort=<grammar>` (additive; server-side sort where cheap, deferred-with-doc where expensive).
- AC5: `apps/web` consumers adapted to `.items`; `@42ch/nexus-contracts` version bumped per `@architect` Phase 2b verdict.
- AC6: `cargo test --all` + `cargo clippy --all -- -D warnings` + `cargo +nightly fmt --all --check` + `pnpm --filter web typecheck/build/test` clean.
- AC7: Regression tests cover error-envelope mapping, casing, `items` shape, sort.

## Tasks (skeleton — `@architect` may recommend P0a/P0b split in Phase 2b)

- T1 (backend): FE1-ORCH — extract shared error-mapping helper + sweep `schedules.rs` + `sessions.rs` + `presets.rs` onto canonical envelope.
- T2 (backend): CASING — align `code` strings; assert convention.
- T3 (backend + contracts): F-P3 — rename array keys → `items`; regenerate; bump contracts.
- T4 (backend): F-F1 — sort-param grammar + server-side sort.
- T5 (frontend): F-P3 UI adaptation — `.items` consumers.
- T6: tests + drift checks + clippy/fmt.

## Out of scope

Frontend scope gaps G1/G2 (P1). Overdue debt (P2). Desktop polish (P-sec). Any canvas work (V1.68). Body editor (rejected direction).

## Dependencies / branch policy

Topic branch `feature/v1.67-local-api-surface-convergence` from `iteration/v1.67`. **Phase 2b §5 #1–#4 LOCKED is a hard prerequisite.** Wave 1 parallel with P-sec (low overlap: daemon-runtime+web vs desktop Tauri). One git worktree per parallel topic branch. Merge target `iteration/v1.67`. `Superpowers: dispatching-parallel-agents` + `using-git-worktrees`.

## Completion Report v2 (fix-wave-1)

**Branch**: `feature/v1.67-p0-fixwave1`  
**Commit**: `24b5914b`  
**Date**: 2026-06-26  
**Executor**: `@fullstack-dev`

### Fixes delivered

1. **Works DTO deduplication** — `crates/nexus-daemon-runtime/src/api/handlers/works.rs` now imports `ListWorksQuery`, `ListWorksResponse`, and `WorkSummary` from `nexus_contracts::local_api::works`, removing the parallel hand-written DTOs flagged by QC.
2. **Sort error-code passthrough** — `crates/nexus-daemon-runtime/src/api/errors.rs` now returns the caller-supplied `*_sort_invalid` codes (`work_sort_invalid`, `schedule_sort_invalid`, `session_sort_invalid`, `capability_sort_invalid`) instead of generic `invalid_input`.
3. **Works sort/pagination push-down** — `crates/nexus-local-db/src/works.rs` gained `WorkListFilters.order_by` and `list_and_count_works`; works are sorted and limited in SQL rather than in-memory.
4. **Schedules sort/pagination push-down** — `crates/nexus-daemon-runtime/src/api/handlers/orchestration/schedules.rs` builds a dynamic `ORDER BY` + `LIMIT/OFFSET` query with a separate `COUNT(*)` for the pagination envelope.
5. **Test coverage** — added unit tests for `parse_sort_terms` and integration tests covering sort + invalid-sort-key for works, schedules, sessions, and capabilities.

### Verification

| Check | Command | Result |
|---|---|---|
| Scoped tests | `cargo test -p nexus-daemon-runtime -p nexus-contracts` | pass |
| Full tests | `cargo test --all` | pass |
| Scoped clippy | `cargo clippy -p nexus-daemon-runtime -p nexus-contracts -- -D warnings` | pass |
| Full clippy | `cargo clippy --all -- -D warnings` | pass |
| Format | `cargo +nightly fmt --all --check` | pass |
| Wire drift | `bash tooling/check-wire-drift.sh` | pass |
| Contracts build | `pnpm --filter @42ch/nexus-contracts run build` | pass |
| Web typecheck | `pnpm --filter web typecheck` | pass |

### Notes / follow-up

- The `gated_preset_without_work_id_is_rejected` test assertion was corrected from `400` to `422` to match the documented `preset_gates_failed` semantics.
- The `list_works_sort_descending_and_pagination` test expectation was corrected: page 2 now asserts the remaining work (`Alpha`) rather than the first page's first item.
- Sessions and capabilities remain in-memory sorted as specified (small bounded registries).
- This wave does **not** complete the full plan; outstanding items (orchestration error-envelope sweep, array-key rename to `items`, frontend adaptation, contract version bump) remain for subsequent waves.

