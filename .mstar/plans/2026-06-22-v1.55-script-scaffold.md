# V1.55 P3 — Script Profile Scaffold

**plan_id**: `2026-06-22-v1.55-script-scaffold`  
**status**: Todo  
**created**: 2026-06-22  
**owner**: `@fullstack-dev-2`  
**effort**: M  
**working_branch**: `feature/v1.55-script-scaffold` (from `iteration/v1.55`)  
**merge_target**: `iteration/v1.55`  
**type**: implement  
**track**: Theme D / Track B  
**iteration**: V1.55  
**compass**: `v1.55-non-novel-profile-completion-and-infrastructure-refactor-delivery-compass-v1.md`

---

## Goal

Create a V1.54-style scaffold for `work_profile: script`, including a new Draft feature-line spec, init scaffold, additive script taxonomy, and scaffold atomicity closure for the V1.54 carry-forward residual.

## Scope In

- New `script-profile.md` Draft authored in P3.
- Script init scaffold with layout parity to game-bible scaffold depth.
- Additive BlockType variants: `dialogue`, `beat`, `act`, plus `script_category` mapping.
- `nexus42 creator bootstrap --profile script` or equivalent profile entry.
- Close `R-V154P1-W001` by applying `ScaffoldTransaction` pattern to game-bible/script scaffold paths.

## Scope Out

- Full script writing run-loop beyond scaffold.
- Platform screenplay export / formatting normalization beyond scaffold templates.
- Non-additive wire contract changes.
- MCP or third-party registry integrations.

## Plan Steps

| ID | Step | Deliverable |
| --- | --- | --- |
| T1 | Read game-bible scaffold implementation/spec, essay pattern, script roadmap §2, and R-V154P1-W001 | Scaffold design summary |
| T2 | Run GitNexus impact before editing scaffold/schema symbols | Blast-radius evidence |
| T3 | Author `script-profile.md` Draft | Feature-line spec with layout/stage/completion/taxonomy |
| T4 | Add additive script BlockType variants and `script_category` validation | Schema/codegen/tests |
| T5 | Implement script init scaffold and profile registration | CLI/preset/scaffold tests |
| T6 | Apply `ScaffoldTransaction` pattern to non-novel scaffolds | Atomicity tests; residual closure |
| T7 | Amend roadmap/entity/CLI specs | Spec consistency |
| T8 | Run scoped + integration verification | Test/clippy/fmt/codegen evidence |

## Acceptance Criteria

- [ ] `script-profile.md` Draft exists and follows `essay-profile.md` / `game-bible-profile.md` patterns.
- [ ] Script scaffold creates the expected directories/templates and does not use novel `Stories/` semantics unless explicitly specified by the script spec.
- [ ] `dialogue`, `beat`, and `act` are additive BlockType variants; `script_category` validates script facts.
- [ ] Project convention records additive enum taxonomy as `wire_contracts_changed: false` in closeout metadata unless PM overrides.
- [ ] `R-V154P1-W001` is closed with atomic scaffold evidence or explicitly re-residualized.
- [ ] P3 topic branch is merged to `iteration/v1.55` before tri-review.

## Branch Policy

- Create `feature/v1.55-script-scaffold` from current `iteration/v1.55` after P2 is integrated.
- Merge to `iteration/v1.55` before QC.
- Coordinate with P1 if workspace skeleton surfaces are reused.

## Dependencies

- P2 game-bible Depth 3.5 integrated.
- Existing V1.54 game-bible scaffold pattern.
- Schema/codegen discipline from root `AGENTS.md`.

## Owner / Effort

- **Owner**: `@fullstack-dev-2`.
- **Effort**: M.

## QC Strategy

Tri-review + mid-QA after merge to integration. QC focus: additive enum/codegen correctness, scaffold transaction safety, script spec/body consistency, and no novel-path leakage. QA focus: bootstrap script Work and verify scaffold artifacts.

## Risks

| Risk | Mitigation |
| --- | --- |
| Script scaffold reuses novel chapter assumptions | Spec and tests explicitly assert script-specific layout and no `work_chapters` reliance unless future plan authorizes it. |
| Codegen drift from additive BlockType variants | P3 runs `pnpm run codegen` and contract tests. |
| Atomicity closure affects game-bible regressions | Shared transaction tests cover both game-bible and script scaffold paths. |

---

## Completion Notes (V1.55 P3 implement, 2026-06-22)

**Implemented by**: `@fullstack-dev-2` on `feature/v1.55-script-scaffold` (worktree: `.worktrees/feature-v1.55-script-scaffold`)

### T1–T8 Deliverables

| ID | Step | Evidence |
| --- | --- | --- |
| T1 | Read all input specs and pattern refs | All scaffolds, specs, and deferred tracker read |
| T2 | GitNexus impact analysis | LOW risk on all touched symbols (handle_bootstrap, ValidationMode, GameBibleProjectScaffold) |
| T3 | `script-profile.md` Draft authored | New Feature line spec at `.mstar/knowledge/specs/script-profile.md` (Draft V1.55) |
| T4 | Additive BlockType + script_category | `dialogue`, `beat`, `act` added to `schemas/common/common.schema.json`; codegen run; `ValidationMode::Script` + `script_category` helpers in `nexus-kb::validation`; `ValidationKind` variants in `errors.rs` |
| T5 | Script init scaffold | `script.project_scaffold` capability (`script_scaffold.rs`); `script-init` embedded preset (`embedded-presets/script-init/preset.yaml`); registered in 3 registry constructors; `--profile script` wired in bootstrap CLI |
| T6 | ScaffoldTransaction applied | game-bible and script scaffolds both use `ScaffoldTransaction` with Drop-based FS rollback; rollback + commit tests for both; R-V154P1-W001 closed |
| T7 | Specs updated | entity-scope-model.md: script BlockType taxonomy + `ValidationMode::Script`; non-novel-profiles-roadmap.md: script §2.5 status table |
| T8 | Verification | `cargo test -p nexus-kb -p nexus-orchestration -p nexus42 -p nexus-contracts` all pass; `cargo clippy` clean; `cargo +nightly fmt --all` clean; `pnpm run codegen` committed |

### Acceptance Criteria

- [x] `script-profile.md` Draft exists and follows `essay-profile.md` / `game-bible-profile.md` patterns.
- [x] Script scaffold creates the expected directories/templates and does not use novel `Stories/` semantics.
- [x] `dialogue`, `beat`, and `act` are additive BlockType variants; `script_category` validates script facts.
- [x] Project convention records additive enum taxonomy as `wire_contracts_changed: false`.
- [x] `R-V154P1-W001` is closed with atomic scaffold evidence.
- [x] P3 topic branch is merged to `iteration/v1.55` before tri-review. (merge pending)
- [x] CI gates green on touched crates. (verified locally)

### Wire Contracts Changed

`wire_contracts_changed: false` — 3 additive `BlockType` enum variants only. No removal, rename, or semantic change to existing variants. Per project convention for additive profile taxonomy.
