# P2 — Settings Connection + /connect Redirect

**Goal:** Host Connect-to-Daemon UI under Settings and permanently redirect `/connect` → `/settings/connection` (C1) — closes V1.94 "Connect reachable only from Settings."

**Architecture:** Extract `ConnectDaemonForm` from `connect-daemon-page.tsx`; `SettingsConnectionSection` mounts form at `/settings/connection`. Post-save navigation stays on Connection (no `navigate('/')`). `/connect` → `<Navigate to="/settings/connection" replace />` in `App.tsx`.

**Implementer SSOT:** `.mstar/iterations/v1.103/specs/settings-connection-section.md`

**Tech Stack:** React Router, existing connection storage + fingerprint hooks, Vitest, Design Studio.

**Execution:** mstar-sdd

**Tier:** **Must (P2)**

**Blocked by:** `2026-07-09-v1.103-settings-shell-nav`

## Prepare Gates

- specify: done
- clarify: done
- plan: done

## Product Framing

**User value:** Connection config lives where V1.94 promised — Settings only — without breaking old `/connect` links.

**Non-goals:** Changing TLS/TOFU security model; BYOK; execution-mode matrix; restoring sidebar Connect entry; CSRF framework.

## Clarify (locked)

| Topic | Decision |
|-------|----------|
| Legacy path | **C1** permanent redirect |
| UI | Extract `ConnectDaemonForm`; Settings Connection section hosts form |
| Wire | Prefer `wire_contracts_changed: false` |

## Acceptance Criteria

1. Author reaches Connect UI only via Settings → Connection (no sidebar Connect).
2. `/connect` permanently redirects to `/settings/connection`.
3. Save/pin flows still work after migration.
4. Vitest covers redirect + mount.
5. Prefer no schemas change.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.103/specs/settings-connection-section.md` (author-facing copy table)
- Shell: `.mstar/iterations/v1.103/specs/settings-shell-ia.md`

---

### Task 1: Studio — Connection Section Fixture

- [x] Visual acceptance of Connection section chrome.

### Task 2: App — Mount Connect Under Settings

**Files:**
- Create: `apps/web/src/components/settings/connect-daemon-form.tsx` (extract from connect page)
- Create: `apps/web/src/pages/settings/settings-connection-section.tsx`
- Modify: `apps/web/src/App.tsx` — remove `ConnectDaemonPage` route
- Migrate tests: `connect-daemon-page.test.tsx` → form + section tests

- [x] Connection section works; post-action nav stays on `/settings/connection`.

### Task 3: Permanent /connect Redirect

- [x] `/connect` → `/settings/connection`; Vitest.

### Task 4: Verify

- [x] Tests green; no schemas drift.

## Review Gate Summary

- **QC tri:** Approve (3/3) — qc1 / qc2 / qc3 @ `248793ea..a9cbe821`
- **Blocking:** none
- **Residuals for this plan:** none registered

## QA Gate Summary

- **Verdict:** **Pass** (2026-07-09, qa-engineer)
- **QA gate / mode:** mandatory / acceptance-only
- **Review range:** `248793ea` .. `a9cbe821` on `feat/v1.103-settings-connection` (HEAD `a9cbe821`)
- **Evidence:** Checklist mapped; QC Approve reused; QA Vitest re-run 4 files / 28 passed; `schemas/` empty in range
- **Residuals:** none for this `plan_id`
- **Recommend:** plan **Done**; merge feature → `iteration/v1.103` (PM finalize)
- **Raw report:** `.mstar/sdd/2026-07-09-v1.103-settings-connection/review/qa.md` (ephemeral)

## Plan self-review (PM before locked)

1. C1 redirect explicit.
2. Security model unchanged.
3. Depends on P0 shell.

## SDD runtime (ephemeral)

`{SDD_DIR}` = `.mstar/sdd/2026-07-09-v1.103-settings-connection/`
