# P2 — Select Component Promotion (Stretch)

**Goal:** Promote presentational `Select` into `@42ch/nexus-ui` under a locked accessibility/composition contract — **only if** P0+P1 automated paths leave capacity.

**Architecture:** Same pattern as V1.100 form fields: contract → Studio fixtures → package → Studio-direct + Web thin wrappers. Package boundary = **presentational only**. Whole plan may defer to V1.102. AgentPicker is **not** part of this plan (stays app-shared in P0).

**Tech Stack:** `@42ch/nexus-ui`, Design Studio, `apps/web` thin wrappers, Vitest.

**Execution:** mstar-sdd

**Tier:** **Stretch (P2)** — **not** Must. Deferral does **not** leave Must incomplete.

## Prepare Gates

- specify: done
- clarify: done
- plan: done

> Product + architect package boundary are coherent for Stretch. Must integrity does not depend on this plan running.

## Product Framing

**User value:** Shared accessible Select primitive for Studio and Web without copying app-local select markup.

**Stretch rule:** If Must work (P0+P1) consumes the iteration, PM defers this plan entirely — **Must completeness is not blocked**. Do not report P2 deferral as a Must residual.

**Non-goals:** Field groups, form frameworks, multi-select product patterns, schema changes, Settings shell (**DF-70**), Must setup work (P0/P1).

## Clarify (product-locked)

| Topic | Decision |
|-------|----------|
| Must vs Stretch | This plan is **Stretch only**. P0 AgentPicker and P1 wizard polish are Must and ship without this plan. |
| Start gate | After P0+P1 automated paths Done (or PM-documented capacity exception). |
| Scope | `Select` only — not field groups or other primitives; **not** AgentPicker. |
| Package boundary | **LOCKED:** presentational only in `@42ch/nexus-ui`; apps own copy/validation/daemon/routing. |
| Deferral | Whole-plan defer to V1.102 allowed; update compass + `status.json` with reason. |
| Smoke | No interactive desktop smoke requirement for P2 automated Done. |
| Wire | `wire_contracts_changed: false`. |

### Architect note

Package presentational boundary locked. Contract a11y details (trigger/value/item) finalize in Execute Task 1 if the plan runs.

## Global Constraints

- Primary spec: `.mstar/iterations/v1.101/specs/select-promotion-contract.md`.
- Process: studio-first guide.
- Starts only after P0+P1 automated paths are Done (or PM explicitly parallelizes with documented capacity).
- Inherits V1.100 guardrails / `cn` SSOT.
- `wire_contracts_changed: false`.

---

### Task 1: Lock Select Contract

**Files:**
- Modify: `.mstar/iterations/v1.101/specs/select-promotion-contract.md`

- [x] Lock trigger/value/item semantics and a11y (keyboard, expanded, labelledby).
- [x] Lock package vs app ownership (no validation/copy/daemon in package).
- [x] Confirm field groups remain out of scope.

### Task 2: Design Studio Fixtures

- [x] Closed/open, disabled, invalid, focus-visible fixtures.
- [x] Visual acceptance before package implementation.

### Task 3: Promote Package Select + Consumers

- [x] Implement/export Select in `@42ch/nexus-ui` with tests.
- [x] Studio direct import; Web thin wrapper/re-export per guardrails.
- [x] No app routing/daemon imports in package.

### Task 4: Deferral Or Done Summary

- [x] ~~If deferred: update compass + `status.json` target V1.102 with reason; confirm Must not marked incomplete.~~ **N/A — Done path** (plan ran through T1–T3; not deferred to V1.102).
- [x] If Done: record QC/QA summary placeholders on this plan (Stretch Done path; QC/QA pending — PM owns gate).

## Plan self-review (PM before locked)

1. Stretch/defer rule explicit; Must integrity preserved.
2. Studio-before-App explicit (when plan runs).
3. Scope = Select only.

## Stretch outcome (implementer)

**Path:** Done (not deferred). Implement Tasks 1–3 complete on `plan/v1.101-p2-select-component-promotion`.

| Task | HEAD (approx) | Outcome |
|------|---------------|---------|
| T1 Contract lock | `5fb3d209` | Native `<select>` a11y + ownership locked |
| T2 Studio fixtures | `cb06e899` | Closed/disabled/invalid/focus/open-manual fixtures + tests |
| T3 Package + consumers | `843033b9` | `@42ch/nexus-ui` Select + Studio direct + Web thin re-export |
| T4 Summary | this commit | Done-path placeholders; plan status remains PM-owned |

Must integrity: P0/P1 were already Done before Stretch ran; deferral was not used.

## Review Gate Summary

| Field | Value |
|-------|--------|
| QC decision | **Approve** (tri-review; qc3 after fix-wave-1 revalidation) |
| Seats | qc1 Approve · qc2 Approve · qc3 Approve |
| Product HEAD | `0205f3ab` (includes README B1 fix) |
| Residuals | `R-V1101P2-001`..`005` (defer); `R-V1100P1QC1-W002` remains open |
| Note | Raw reports: `.mstar/sdd/2026-07-09-v1.101-select-component-promotion/review/` |

## QA Gate Summary

| Field | Value |
|-------|--------|
| QA decision | **Pending** — mandatory QA in flight |
| Product HEAD | `0205f3ab` |
| Note | PM dispatches `qa-engineer` after QC Approve |
