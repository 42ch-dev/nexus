---
plan_id: 2026-06-24-v1.64-local-api-hardening
title: V1.64 P0 — Track B: Local API Data-Layer Hardening (DTO Drift + F-E1 + F-P1 + F-P2)
iteration: V1.64
slot: P0
status: Todo
effort: L
owner: "@fullstack-dev"
review: Tri-review + QA
wave: 1
dependency: P-1 PM signoff
working_branch: feature/v1.64-local-api-hardening
merge_target: iteration/v1.64
---

# P0: Track B — Local API Data-Layer Hardening

Close 4 of the 6 V1.64+ residuals registered by the V1.63 surface audit + handler DTO migration. These are the UI data layer's direct prerequisites (Track A P2 consumes the hardened API). Pure backend/schemas; no UI.

## Tasks

- [ ] T1: **Handler DTO drift closure** (`R-V163-P1-T6`) — audit all Local API handlers in `crates/nexus-daemon-runtime/src/api/handlers/`; ensure each emits shapes matching `generated::local_api::*`. Close remaining `CheckMode::Strict` gaps surfaced by `crates/nexus-contracts/tests/schema_drift_detection.rs`. Verify `./tooling/check-wire-drift.sh` + `cargo test --test schema_drift_detection` clean.
- [ ] T2: **F-E1 — Shared `ErrorResponse` schema** — author `schemas/local-api/common/error-response.schema.json` (`code` stable string, `message` string, optional `details` object). Register in codegen schema-loader. Apply canonical error envelope to Local API handlers (replace ad-hoc `(StatusCode, String)` tuples). Add drift-detection `entry!` (Strict).
- [ ] T3: **F-P1 — Works cursor pagination** — migrate Works list response from offset/limit to cursor-based (matching kb/sessions/capabilities peers). Add `PaginationInfo` (`next_cursor`, `has_more`). **Decouple from F-P3**: keep existing array field names (`works` etc.) — the rename to `items` is deferred (compass §5 item #2). Update handler + DAO + schema + drift entry. This is a wire-visible shape change (safe: first UI consumer ships this iteration).
- [ ] T4: **F-P2 — Findings list endpoint** — implement `/v1/local/works/{work_id}/findings` list route (cursor-based, matching peers) + `schemas/local-api/findings/list-findings-response.schema.json`. Wire handler + DAO (`list_findings` already supports filters post-V1.49/V1.50). Drift entry.
- [ ] T5: Codegen cascade — `pnpm run codegen` → regenerate Rust + TS; verify `@42ch/nexus-contracts` minor-bump exports.
- [ ] T6: `cargo check -p nexus-contracts -p nexus-daemon-runtime` + `cargo clippy -p nexus-contracts -p nexus-daemon-runtime -- -D warnings` + `cargo test -p nexus-contracts -p nexus-daemon-runtime`.
- [ ] T7: Boundary docs — `schemas-directory-layout.md` (add `local-api/common/` + `findings/` subtrees), `schemas-external-consumer-boundary.md` (DTO drift-closure criterion), `schemas/README.md`.
- [ ] T8: Hand convention text to `@architect` for `local-api-surface-conventions.md` (cursor + ErrorResponse + naming) — @architect authors the spec; P0 implementer supplies the empirical patterns.

## Done when

- All 4 residuals (`R-V163-P1-T6`, F-E1, F-P1, F-P2) closed in `status.json` with resolution commits.
- `schema_drift_detection` Strict passes for all local-api schemas (including new ErrorResponse + findings list + changed Works shape).
- Codegen TS types for Works list reflect cursor shape; `@42ch/nexus-contracts` minor-bump.
- Handlers return canonical `ErrorResponse` on error paths.
- Findings list endpoint reachable + drift-clean.

## Notes

- F-P1 is the only wire-breaking change; it lands alongside its first consumer (P2 UI), so no external breakage.
- F-P3 (array rename) + F-F1 (sort) NOT in scope — deferred to V1.65+, UI adapter covers.
- Track A P1 (scaffold) runs in parallel; zero file overlap (P0 = daemon handlers + schemas; P1 = `apps/web/` + workspace config).
