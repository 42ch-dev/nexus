# Plan: V1.71 Canvas Strategy β Write-Boundary (P0)

**Plan ID**: `2026-06-27-v1.71-canvas-strategy-write-boundary`
**Iteration**: V1.71
**Status**: Todo
**Owner**: `@frontend-dev` (canvas UI: inspector / edge / in-node TipTap / conflict modal UX / DESIGN.md write tokens) + `@fullstack-dev` (schemas/codegen + daemon validation + revision persistence + Local API routes + NexusClient + tests)
**Effort**: L
**Depends on**: P-1 (compass Locked)
**Working branch**: `feature/v1.71-canvas-strategy-write-boundary` from `iteration/v1.71`
**Merge target**: `iteration/v1.71`
**Worktree**: `.worktrees/v1.71-canvas/`
**Task category**: `visual` (primary) + `logic` (secondary — graphRevision + conflict policy + Local API)
**Superpowers**: `using-git-worktrees` (parallel with P1; same repo, file-disjoint)

## Compass reference

[`v1.71-canvas-strategy-write-boundary-and-hygiene-compass-v1.md`](../iterations/v1.71-canvas-strategy-write-boundary-and-hygiene-compass-v1.md) §1.1 Track A (A1–A9), §1.3 wire contracts, §3 plan topology, §6 risk notes.

## Spec reference

- [`canvas-strategy-surface.md`](../knowledge/specs/canvas-strategy-surface.md) (V1.70 Shipped α → V1.71 Shipped β): §3.4 interface contracts, §3.5 structured write boundary (Full depth — graphRevision + structured conflict error + UI retry/merge), §3.6 token contract, §4.1 Idea-input, §4.4 accessibility
- [`orchestration-engine.md`](../knowledge/specs/orchestration-engine.md) §3 (graph-of-graphs), §7.2 (preset YAML), §3.3/§4.2 (session state)
- [`local-api-surface-conventions.md`](../knowledge/specs/local-api-surface-conventions.md) (V1.71 extends with patch-route pattern)
- [`web-ui.md`](../knowledge/specs/web-ui.md) §5 (NexusClient transport invariant)
- `apps/web/DESIGN.md` + `apps/web/DESIGN.dark.md` (V1.70 canvas tokens filled; V1.71 adds write-mode tokens)

## Scope (β depth — `wire_contracts_changed: TRUE`)

- **A1 — Schemas + codegen**: New Local API DTOs in `schemas/local-api/canvas/strategy/`:
  - `StrategyPatchStateRequest` / `StrategyPatchTransitionRequest` / `StrategyPatchPromptTemplateRequest`
  - `StrategyPatchResponse`
  - `StrategyConflictError` (extends existing error envelope)
  - `StrategyValidationError`
  - Generate TS (`apps/web/src/api-types/`) and Rust (`crates/nexus-contracts/src/generated/`); commit regenerated output.
- **A2 — graphRevision tracking + persistence (LOCKED)**: Store the canonical graph revision as a `revision:` key in the preset YAML header. Existing preset YAML without `revision:` reads as revision `0`; the first successful patch writes `revision: 1`. On every accepted patch, increment revision exactly once in the same committed YAML image; read endpoints expose the current revision; reject stale `base_revision` with `StrategyConflictError`. Do **not** add a V1.71 `strategy_revisions` DB table; that migration is deferred until audit history / multi-device sync / collaborative edits require it.
- **A3 — Local API routes (LOCKED)**: New `POST` routes following V1.64 conventions and the V1.71 patch-route pattern:
  - `POST /v1/local/strategies/{strategy_id}/states/{state_id}/patch` with `StrategyPatchStateRequest` → `StrategyPatchResponse`
  - `POST /v1/local/strategies/{strategy_id}/transitions/patch` with `StrategyPatchTransitionRequest` → `StrategyPatchResponse`
  - `POST /v1/local/strategies/{strategy_id}/states/{state_id}/prompt/patch` with `StrategyPatchPromptTemplateRequest` → `StrategyPatchResponse`
  - Error envelope: 409 `StrategyConflictError`, 422 `StrategyValidationError`, and standard 400/404/500 envelope codes.
- **A4 — Daemon validation + atomic persistence** (`crates/nexus-daemon-runtime/src/handlers/strategy.rs`):
  - Validate **ID existence**: strategy/preset, state ids, transition handles/source-targets, prompt/template refs; path ids must match duplicated body ids.
  - Validate **reachability / graph integrity**: no orphaned start state, unreachable required states, broken terminal reachability, invalid inner-graph `depends_on`, or invalid Converge structure.
  - Validate **condition syntax**: branch/default/edge conditions parse against the preset condition grammar; unknown variables/capability refs return structured validation paths.
  - Validate **prompt template reference resolution**: target prompt/template exists; TipTap/Markdown patch stays inside allowed prompt subset and node/template scope.
  - Validate **revision precondition**: stale `base_revision` short-circuits as 409 before validation/persistence side effects.
  - Atomic YAML write (temp + rename + fsync) with the incremented YAML `revision:`; failed validation/conflict must not increment revision.
- **A5 — NexusClient methods**: `strategyPatchState`, `strategyPatchTransition`, `strategyPatchPromptTemplate`; track `lastKnownRevision`; refetch canonical preset after patch.
- **A6 — Canvas UI write affordances**:
  - State inspector: edit label/description
  - Edge inspector: edit condition/target
  - In-node TipTap for prompt template editing
  - **Conflict modal UX / copy**:
    - On 409 `StrategyConflictError`, keep the draft patch in memory, refetch the canonical Strategy, and show a modal.
    - **Headline**: "This node changed while you were editing."
    - **Body**: "Nexus updated **{node label}** to revision **{current_revision}** while you were editing **{field}**. Your change is still in the inspector."
    - Show **What changed** (canonical value) and **What you were about to do** (draft value).
    - **Actions**: **Use current** (primary/default), **Reapply my edit**, and **Review side-by-side** (enabled only when draft and canonical changes touch non-overlapping fields; disabled for same-field/path or prompt-template conflicts).
    - **Cancel** returns focus to the originating inspector.
  - **Acceptance (A6)**: conflict modal copy is clear and actionable; the default action is **Use current**; **Review side-by-side** is enabled only when safe; the user's draft is not lost on cancel.
- **A7 — Tauri WKWebView parity smoke**: write affordances + conflict modal + in-node TipTap save work in Tauri shell.
- **A8 — Accessibility**: conflict modal uses an ARIA live region to announce the conflict and current-vs-draft difference; focus moves into the modal and is trapped; closing returns focus to the originating node/inspector; keyboard paths for **Use current**, **Reapply my edit**, **Review side-by-side**, and **Cancel**; `prefers-reduced-motion` honored for modal and graph animations.
- **A9 — Tests**: unit (validation + revision + conflict), integration (3 routes), e2e (canvas edit → save → refetch), regression (concurrent daemon write causes 409).

## Non-goals

- No outline+timeline or World KB canvas surfaces — V1.72+
- No CLI/schema rename `preset` → `strategy` — deferred
- No removal/regression of shipped V1.65 outline editor
- No additional operations beyond the 3 patch operations in Draft §3.5
- No `ci.yml` / desktop-build workflow changes (Track B owns CI/ops)

## Tasks (to be detailed at implement dispatch)

- [ ] T1: Add `schemas/local-api/canvas/strategy/` DTOs + run `pnpm run codegen` + commit generated artifacts (A1)
- [ ] T2: Implement locked graphRevision storage via preset YAML header `revision:` (missing key = `0`; first accepted patch = `1`; no V1.71 DB revision table) (A2)
- [ ] T3: Add 3 locked Local API patch routes + request/response DTO wiring (`StrategyPatchStateRequest`, `StrategyPatchTransitionRequest`, `StrategyPatchPromptTemplateRequest`, `StrategyPatchResponse`) (A3)
- [ ] T4: Implement daemon validation rules: id existence, reachability/graph integrity, condition syntax, prompt-template ref resolution, and revision precondition (A4)
- [ ] T4a: Implement atomic YAML persistence with incremented `revision:` and prove failed validation/conflict paths do not increment revision (A2/A4)
- [ ] T5: NexusClient methods for 3 patch operations (A5)
- [ ] T6: State inspector + edge inspector + in-node TipTap prompt editor (A6)
- [ ] T7: Conflict modal UX with **Use current / Reapply my edit / Review side-by-side** (A6)
- [ ] T7a: Product copy for conflict modal headline/body/button labels, default action, disabled states, and graphRevision freshness indicator (A6)
- [ ] T8: Tauri WKWebView parity smoke for write affordances (A7)
- [ ] T9: DESIGN.md + DESIGN.dark.md canvas-write-mode tokens (A8)
- [ ] T10: Accessibility review of conflict modal (A8)
- [ ] T11: Unit + integration + e2e + regression tests (A9)
- [ ] T12: `pnpm --filter web typecheck` + `pnpm --filter web build` + `pnpm --filter web test` green
- [ ] T13: `cargo clippy --all -- -D warnings` + `cargo test --all` green

## Acceptance

- All 3 Strategy patch routes return correct `StrategyPatchResponse` and increment revision atomically
- graphRevision storage uses the preset YAML header `revision:`; existing missing revisions migrate lazily from `0` to `1` on first successful patch; no separate V1.71 DB table is introduced
- Stale `base_revision` returns `StrategyConflictError` (409) with current revision, node id, and recovery hint
- Validation rejects missing ids, broken reachability, invalid condition syntax, and unresolved prompt-template refs before persistence
- Canvas UI can edit state label/description, edge condition/target, and prompt template; conflict modal offers **Use current**, **Reapply my edit**, and **Review side-by-side**; conflict modal copy is clear and actionable; default action and disabled side-by-side state are implemented
- graphRevision freshness indicator copy is clear and actionable (e.g., "Strategy · revision {revision} · updated {relative time}")
- DESIGN.md canvas-write tokens have concrete light + dark values
- Tests cover validation, conflict detection, concurrent daemon write, and e2e canvas edit→save→refetch
- `wire_contracts_changed: TRUE` confirmed; `@42ch/nexus-contracts` 0.6.0 → 0.7.0 bump documented
- `cargo clippy --all -- -D warnings`, `cargo test --all`, `pnpm --filter web typecheck/build/test` green
