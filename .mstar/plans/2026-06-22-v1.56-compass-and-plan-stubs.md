# V1.56 P-1 — Compass & Plan Stubs

**plan_id**: `2026-06-22-v1.56-compass-and-plan-stubs`  
**status**: Todo  
**created**: 2026-06-22  
**owner**: `@project-manager` (PM whitelist; authored by `@writing-specialist`)  
**effort**: M  
**working_branch**: `feature/v1.56-prepare`  
**merge_target**: `iteration/v1.56`  
**type**: prepare  
**iteration**: V1.56  
**compass**: `v1.56-workspace-and-routing-seam-closure-delivery-compass-v1.md`  
**review**: QC skipped — PM whitelist for Prepare-phase plan authoring

---

## Goal

Create the durable Prepare package for V1.56: compass, seven plan stubs, and deferred tracker activation — encoding PM-locked grill decisions Q1–Q7 verbatim without touching application code.

## Scope In

- Compass: `.mstar/iterations/v1.56-workspace-and-routing-seam-closure-delivery-compass-v1.md`.
- Seven plan stubs under `.mstar/plans/` (P-1 through P-last).
- Deferred-feature tracker V1.56 activation: quick-status, carry-forward index, open feature row updates for DF-29/DF-31/DF-42/DF-56.
- Light status-header/spec-status updates only — no spec body amendments in P-1.

## Scope Out

- No code, schema, migration, or generated contract edits.
- No `status.json` modification — PM territory; PM adds V1.56 metadata + 7 plan rows after reviewing P-1 outputs.
- No `iteration/v1.56` creation — PM creates from `main` after P-1 review.
- No QC/QA dispatch; PM reviews this prepare package.
- No spec body amendments; those happen in P0–P3 implementations and P-last consolidation.

## Plan Steps

| ID | Step | Deliverable |
| --- | --- | --- |
| T1 | Author V1.56 delivery compass from V1.55 template with PM-locked grill decisions | Compass file (§0–§9) |
| T2 | Author seven plan stubs (P-1 through P-last) | Plan stub files |
| T3 | Update deferred tracker for V1.56 activation | Quick-status line, carry-forward index, updated open feature rows |
| T4 | Verify prepare package integrity | Line counts, JSON parse of tracker (info only), git diff scope verification |

## Acceptance Criteria

- [ ] Compass follows V1.55 structure (§0–§9) and includes all 7 PM-locked grill decisions verbatim in §0.
- [ ] Compass §3 lists seven plans, the track-ownership pattern, and the 3-wave dispatch rhythm.
- [ ] Compass §4 states the two-tier branch policy: `iteration/v1.56` + per-plan `feature/v1.56-*` from integration.
- [ ] Seven plan stubs exist, each with Goal, Scope In, Scope Out, Plan Steps, Acceptance Criteria, Branch Policy, Dependencies, Owner, Effort, QC Strategy, and Risks.
- [ ] Deferred tracker quick-status line says V1.56 active, carry-forward index maps all 5 absorbed items to plans, and open feature rows for DF-29/DF-31/DF-42/DF-56 point to V1.56.
- [ ] `status.json` NOT modified (verified by `git diff --name-only` not including `.mstar/status.json`).
- [ ] No files outside `.mstar/**` modified (verified by `git diff --name-only`).

## Branch Policy

- Prepare work lands on `feature/v1.56-prepare` (PM creates this branch from `main` before P-1 starts).
- PM reviews artifacts, then merges `feature/v1.56-prepare` into `iteration/v1.56` (which PM creates from `main` after this review).
- No topic branch is created for P-1; the prepare branch IS the topic branch.

## Dependencies

- PM grill decisions Q1–Q7 are authoritative and locked.
- V1.55 shipped state and `R-V155P2-F002` residual row are the baseline.
- V1.55 compass and plan stubs are the structural template.
- No implement plan can dispatch until PM adds V1.56 metadata + 7 plan rows to `status.json` and flips `metadata.pre_implement_gate` to `go`.

## Owner / Effort

- **Owner**: `@project-manager` (PM whitelist); authored in this assignment by `@writing-specialist`.
- **Effort**: M.

## QC Strategy

QC skipped by PM whitelist. PM review is the gate; no implement dispatch until PM signs off, adds plan rows to `status.json`, and updates the pre-implement gate.

## Risks

| Risk | Mitigation |
| --- | --- |
| Prepare package accidentally starts implementation | Scope Out blocks code/schema/migration edits; verification checks file paths. |
| Plan stubs over-prescribe code before implementers inspect | Stubs define contracts, risks, and acceptance; implementers still run impact analysis before code changes. |
| Deferred tracker drift from compass | Compass §5.1 carry-forward table is canonical; tracker mirrors it. |
