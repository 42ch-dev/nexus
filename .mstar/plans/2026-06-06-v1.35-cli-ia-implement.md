# V1.35 CLI IA Implement — Locked Plan

**Plan ID**: `2026-06-06-v1.35-cli-ia-implement`  
**Title**: P2 — platform sync migration, sync alias, help alignment  
**Created**: 2026-06-06  
**Status**: Todo  
**Owner**: `@project-manager`  
**Agents**: `@fullstack-dev`  
**Task category**: `logic`  
**Working branch**: `feature/v1.35-cli-ia-implement`  
**Merge target**: `iteration/v1.35`  
**Depends on**: P1 spec locked; P0 critical path clear for shared files  
**Spec refs**: [cli-command-ia.md](../knowledge/specs/cli-command-ia.md) §4.1, §5

---

## 1. Problem Statement

Top-level `sync` group contradicts V1.35 IA. Root help promotes `daemon schedule` over `creator run`.

---

## 2. Goals

1. Add `platform sync pull|push|status` (delegate to existing sync handlers).
2. Deprecate top-level `sync` with stderr warning; hidden or alias forwarding.
3. Update root `long_about` and top-level help ordering per IA spec §7.
4. Update shell completion + command_surface_contract tests.

---

## 3. Non-goals

- Hard-delete top-level sync
- creator subcommand reorder (P3)

---

## 4. Tasks

- [ ] T1: Implement platform sync subcommand module
- [ ] T2: Wire deprecated sync alias
- [ ] T3: Update cli.rs long_about + Commands enum docs
- [ ] T4: Update tests + completion
- [ ] T5: gitnexus_impact on touched CLI symbols

---

## 5. Acceptance

1. `nexus42 platform sync --help` works.
2. `nexus42 sync pull` still works with deprecation warning.
3. command_surface_contract tests pass.

---

## 6. Verification

```bash
cargo test -p nexus42 command_surface_contract
nexus42 --help
nexus42 platform sync --help
```
