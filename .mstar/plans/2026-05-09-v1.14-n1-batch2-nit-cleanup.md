# V1.14 N1 Batch 2 Nit Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close deferred nit residual `R-V113-006` by simplifying the nested JSON test helper in `crates/nexus-orchestration/src/tasks/mod.rs` without changing orchestration runtime behavior.

**Architecture:** Keep the cleanup limited to test/helper code in the existing tasks module. Replace the ad hoc nested JSON builder with a small generic helper only if it reduces duplication and preserves existing test expectations; otherwise document that the existing helper is clearer and close the residual with evidence.

**Tech Stack:** Rust workspace crate `nexus-orchestration`, serde_json, cargo test/clippy/fmt, GitNexus impact analysis.

---

## File Structure and Responsibility Lock

- `crates/nexus-orchestration/src/tasks/mod.rs` — owns the nested JSON helper and the tests that consume it (`R-V113-006`).
- `.mstar/status.json` — plan progression and residual closure only after implementation evidence exists.

---

### Task 1: Assess `build_nested_payload` usage and choose the smallest cleanup (`R-V113-006`)

**Files:**
- Modify: `crates/nexus-orchestration/src/tasks/mod.rs`

- [ ] **Step 1: Run required impact analysis before editing `tasks/mod.rs`**

Run: `gitnexus_impact({"target":"build_nested_payload","direction":"upstream","file_path":"crates/nexus-orchestration/src/tasks/mod.rs","repo":"nexus","includeTests":true})`
Expected: risk is LOW or MEDIUM and limited to tests/helper callers. If GitNexus cannot resolve the helper, run impact on `render_core_context_template` and report that the nit helper is test-local before editing.

- [ ] **Step 2: Find all helper call sites**

Run: `rg "build_nested_payload|nested_payload" crates/nexus-orchestration/src/tasks/mod.rs`
Expected: all call sites are in tests or test-support code inside `tasks/mod.rs`.

- [ ] **Step 3: Capture current behavior with focused tests**

Run: `cargo test -p nexus-orchestration nested -- --nocapture`
Expected: PASS, or if no test name contains `nested`, run `cargo test -p nexus-orchestration core_context_template -- --nocapture` and record the focused test names that exercise nested JSON payloads.

- [ ] **Step 4: Select implementation path**

Use the generic helper path only when there are at least two call sites that benefit from it. If there is only one call site, prefer inlining the `serde_json::json!` payload in the test and deleting the helper.

Generic helper target shape:

```rust
fn nested_json_path(path: &[&str], leaf: serde_json::Value) -> serde_json::Value {
    path.iter().rev().fold(leaf, |value, key| {
        serde_json::json!({ *key: value })
    })
}
```

Single-call-site inline target shape:

```rust
let payload = serde_json::json!({
    "world": {
        "series": {
            "title": "Nexus"
        }
    }
});
```

---

### Task 2: Apply cleanup and preserve template behavior

**Files:**
- Modify: `crates/nexus-orchestration/src/tasks/mod.rs`

- [ ] **Step 1: Replace the helper or inline the payload**

If using the generic helper, replace `build_nested_payload` with `nested_json_path` and update tests to call:

```rust
let payload = nested_json_path(
    &["world", "series", "book"],
    serde_json::json!({ "title": "Nexus" }),
);
```

If using inline payloads, delete `build_nested_payload` entirely and use an explicit `serde_json::json!` object in the test.

- [ ] **Step 2: Keep assertions unchanged except variable names**

Existing expected render assertions must remain behaviorally equivalent:

```rust
assert_eq!(rendered, "Nexus");
```

If the current expected value differs, keep the current value and only update helper construction.

- [ ] **Step 3: Run focused tests**

Run:
- `cargo test -p nexus-orchestration nested -- --nocapture`
- `cargo test -p nexus-orchestration core_context_template -- --nocapture`

Expected: PASS. If the first command reports zero matching tests, the second command must still PASS.

- [ ] **Step 4: Run lint for the touched crate**

Run: `cargo clippy -p nexus-orchestration --all-targets -- -D warnings`
Expected: PASS with no new helper-related warnings.

---

### Task 3: Final verification and residual closure handoff

**Files:**
- Modify: `.mstar/status.json`

- [ ] **Step 1: Run workspace verification for the nit cleanup**

Run:
- `cargo +nightly fmt --all -- --check`
- `cargo test -p nexus-orchestration`

Expected: PASS.

- [ ] **Step 2: Run change impact check before commit/review**

Run: `gitnexus_detect_changes({"scope":"all","repo":"nexus"})`
Expected: changed symbols are limited to test/helper code in `crates/nexus-orchestration/src/tasks/mod.rs` and plan/status state.

- [ ] **Step 3: Update plan status and residual only after evidence exists**

Move this plan to `InReview` after verification evidence is recorded. Update `R-V113-006` from `defer` only when the helper cleanup is complete and reviewed; do not modify other residuals.

- [ ] **Step 4: Commit cleanup**

```bash
git add crates/nexus-orchestration/src/tasks/mod.rs .mstar/status.json
git commit -m "test(orchestration): clean nested json payload helper"
```

---

## Gate Alignment

- This plan is deferred batch 2 cleanup and is not required for `G2` or `G3`.
- It should run after Plan A unless the implementer confirms there is no overlap in `crates/nexus-orchestration/src/tasks/mod.rs`.

## Self-Review Checklist

- Spec coverage: `R-V113-006` has a dedicated XS cleanup path with file paths and verification commands.
- Placeholder scan: implementation choices are explicit and bounded to two concrete paths.
- Runtime boundary: no runtime behavior change is intended; assertions must preserve existing template output semantics.
