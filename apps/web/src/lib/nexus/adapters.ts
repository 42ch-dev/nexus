/**
 * Client-side adapters for the F-P3 and F-F1 deferred residuals.
 *
 * These residuals are now closed server-side: all affected list endpoints
 * return the canonical `{ items, pagination }` shape and honor the single
 * `sort` query parameter. This module is intentionally left empty as a
 * placeholder; screen code consumes generated contract types directly.
 */
export {};
