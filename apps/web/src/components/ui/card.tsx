/**
 * Card — DESIGN.md §Component Primitives/Card.
 *
 * Re-exported from @42ch/nexus-ui (V1.99 P0 promotion).
 * The package owns the presentational implementation; this file is a thin
 * re-export wrapper to avoid call-site churn in apps/web.
 * Add app-specific Card behavior here only.
 */
export { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@42ch/nexus-ui';
