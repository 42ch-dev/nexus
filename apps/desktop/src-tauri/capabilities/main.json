{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability for the main Nexus desktop window. Per compass §5 #8 the runtime canonicalize + prefix-check path guard (apps/desktop/src-tauri/src/lib.rs) is AUTHORITATIVE; these opener scopes are defense-in-depth only (static scopes cannot encode the dynamic workspace root).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "opener:allow-reveal-item-in-dir",
      "allow": [{ "path": "**" }]
    },
    "shell:allow-execute"
  ]
}
