# Strategy

## Vision

**Nexus is a local-first narrative-orchestration platform.** Developers get an orchestratable creator-agent service — Harness (preset engine + agent host + capability registry + skills) plus Computable modules they can author and ship. The first-party authoring app is the **reference creator surface** for non-dev authors: Canvas + Timeline remain how humans steer worlds, not a separate product family.

### Three pillars

| Pillar | Means (developer + author) | Maps to in the codebase |
|--------|-------------------------|-------------------------|
| **Harness** | How developers *and* authors harness AI agents: presets, orchestration, capability routing, skills. Developers configure and author this service; authors consume named modes without operator chrome. | `crates/nexus-orchestration/` + `crates/nexus-agent-host/` + `crates/nexus-acp-host/` + capability registry + presets. Surfaced in UI as **Harness** — the user-visible pillar-entry rename **Strategy / Strategies → Harness** landed in V1.156 P3 (closes `DF-V1122-HARNESS-RENAME`); **Preset stays** as the mechanism name under Harness (product lock: [.mstar/iterations/v1.156/specs/product-locks.md](.mstar/iterations/v1.156/specs/product-locks.md) PD-4). Internal identifiers (route `/strategies`, `CanvasSurfaceKind = 'strategy'`, `preset` wire fields, CSS classes, hook names) stay unchanged. Specs: [orchestration-engine.md](.mstar/specs/orchestration-engine.md), [agent-host.md](.mstar/specs/agent-host.md), [capability-registry.md](.mstar/specs/capability-registry.md). |
| **Canvas** | Spatial steering for the reference creator surface — **Timeline-centric World building** as the hero author entry. Timeline is **three layers** — **Brief** / **Narrative** / **Moment** — **World Timeline** = Brief+Narrative (Brief-led); **Work Timeline** = Narrative+Moment (Moment-led peer; Work entry stays Outline). Strategy canvas is the developer explainability surface for daemon-path presets. | `apps/web` React Flow canvas surfaces (`CanvasSurfaceKind = "strategy" \| "outline" \| "world-kb-entities" \| "world-kb-relationships" \| "timeline" \| "work-timeline"`). Timeline is the default **World-entry** surface (V1.122; default layer Brief when data exists from V1.123); Work entry stays Outline (V1.118); Work Timeline is a peer from V1.123. Spec: [canvas-strategy-surface.md](.mstar/specs/canvas-strategy-surface.md). |
| **Computable** | The WASM layer that makes worlds *react* — combat, dice, graph computation, integrator modules. Developers author modules via the official SDK; authors run installed modules (review-then-apply). | Native WASM host (`wasmtime`) + compute-module ABI + combat-engine preset (shipped V1.62; foundation V1.114). Specs: [compute-module-abi.md](.mstar/specs/compute-module-abi.md), [wasm-host.md](.mstar/specs/wasm-host.md). |

**Spine vs projection** (locked product model — both audiences): **World + Timeline + KnowledgeEntry + Fork** are the *spine* (truth of the narrative universe the service stores and Connect serves); **Work + Outline + Manuscript** are *projections* (the authoring plan and prose bound to a World). Timeline is the World's *when* axis at three scales (Brief · Narrative · Moment); Outline is the Work's structural projection; Work Timeline is the Work's when-axis peer (Narrative · Moment). Under the Actor model, KnowledgeEntry stays **one spine primitive with exactly one canonical owner scope** — World, Character, or ActorWorldBinding. **v1.184 shipped** Character and ActorWorldBinding owner scopes alongside World-owned KE; World remains the pre-Actor default ([CONCEPTS.md](CONCEPTS.md) § *KnowledgeEntry*). Dual authoring defaults: **World first for World building (Timeline, Brief-led); Work first for chapter writing (Outline), with Work Timeline for scene precision.** See [CONCEPTS.md](CONCEPTS.md) § *Three Pillars* and § *Brief* / *Narrative* / *Moment*.

### Actors — who can think and act

**Actor** is the cross-cutting narrative identity primitive (accepted product direction, 2026-09-04 product lock; **v1.184 shipped** the Character vertical — **v1.185 maintenance not shipped**): *who can think and act* in a story. It cuts across the three pillars rather than adding a fourth — Harness executes an Actor, Canvas surfaces one, Computable modules react to one. `ActorRef` is a closed v1 sum with exactly two kinds:

- **Creator** — *conducts* the story: the god/orchestrator/narrative driver and operational owner. Carries SOUL + Memory and holds omniscient read over all KnowledgeEntries in every World it owns (including creator-only facts). This is the shipped `creator_id` identity aggregate; its storage and execution remain byte-stable.
- **Character** — *lives* the story: a durable, Creator-owned identity with its own SOUL, Memory, ToM (built additively on the V1.164–V1.166 l5 MindState/belief/observation foundation), and image/persona assets. A Character enters **one or more** Worlds only through explicit `ActorWorldBinding`s — never implicitly, and no World KB is ever copied per Character. Character creation establishes an initial binding atomically; an active Character never has zero active bindings, so removal of its last active binding rejects with zero mutation; transitioning a Character out of active state is a separate later lifecycle contract (explicit archive in v1.185, **not shipped**), never an implicit effect of binding removal.

**Viewpoint** is subordinate execution context paired with an `ActorRef` (`{world_id, optional binding_id/branch_id/event_id}` — it does not repeat an actor id; Character execution requires the binding id, Creator omits it), not identity. One Agent Host / runtime / provider plane serves both kinds; Character sessions execute under the owning Creator's admission boundary with isolated conversation histories. No consumption end changes: still no first-party player (PD-09), no second runtime, no companion app. Outward line: **Nexus Actors are who can think and act — Creators conduct the story; Characters live it.** Durable authority: [actor-product-model.md](.mstar/specs/actor-product-model.md) (accepted product direction — v1.184 stages shipped; v1.185 edit/archive/content/`--remember` not shipped). Vocabulary: [CONCEPTS.md](CONCEPTS.md) § *Actors & Narrative Identity*.

## What we build

Three **consumption ends** (no new first-party apps) plus developer-facing assets:

| Consumption end | Surface | Role |
|-----------------|---------|------|
| **Developers** | `nexus42` CLI + Daemon HTTP API + Connect host | Primary audience. Full operator/config surface, module/strategy authoring DX, capability registry, Run Studio, Develop hub. |
| **Content creators** | `apps/web` + `apps/desktop` (same SPA, **Create** layout) | Reference creator surface: World/Work, Timeline/Outline/KB, findings, memory, guided Run of *installed* modules. Operator chrome hidden. |
| **Third-party users** | `nexus-runtime` + Connect (PD-09 / FL-R) | End users of *integrator* products. Partner UI lives outside this repo. No first-party player. |

| Asset | Role |
|-------|------|
| **`nexus42`** (Rust CLI + daemon) | Producer: CLI, daemon lifecycle, local HTTP API, orchestration, World KB |
| **`web`** (React SPA) | Single SPA, two layout trees (`Create` / `Develop`) selected by User-layer entrance — not a second build |
| **`desktop`** (Tauri v2) | Native shell of the same SPA (file/OS/sidecar); entrance is a setup-wizard step |
| **Official compute-module SDK** | Standalone crates.io-publishable crate + shared manifest helper; scaffold/build/validate/install/run without hand-copied ABI |
| **Strategy / module authoring DX** | CLI groups, validators, embedded skills/demos; strategy canvas as explainability SSOT on the developer layout |
| **Entrance split** | User-layer identity `developer` \| `content-creator`, orthogonal to agent-layer Creator profiles |

Shared: local-first privacy; harness the user's own ACP/native agents; structured narrative spine (timelines, forks, manuscripts, World KB); WASM compute with review-then-apply.

## What we don't build

- **A cloud platform** — this repo is the open-source CLI/daemon (cloud/web platform lives in private `nexus-platform`)
- **A general-purpose note-taking app** — narrative orchestration + reference authoring, not generic notes
- **A competing IDE or editor** — we integrate with the user's tools and agents, not replace them
- **A new first-party player app for third-party users** (PD-09) — partners ship their UI; we ship runtime + Connect
- **A second runtime, companion Character app, NPC swarm, or per-Character copied World KB** (Actor product lock, 2026-09-04 — [actor-product-model.md](.mstar/specs/actor-product-model.md)) — Characters are an Actor kind executed by the one Agent Host under the owning Creator's admission boundary; not a new process plane or player surface (the PD-09 no-first-party-player stance above is unchanged)
- **A general-purpose MCP server** — DF-49 (V1.79) cancelled the standalone
  MCP server; **reversed 2026-08-24 (user decision)** for a scoped
  **tools-only MCP exposure surface**: nexus serves `tools/list` +
  `tools/call` over the single capability-registry spine (builtin `nexus.*`
  + user capabilities + peer tools) via stdio-first MCP so external ACP /
  native-CLI agents can consume nexus tools capability. Not a
  prompts/resources/sampling surface; the CLI remains an ACP client.
- **Full user-authored capability plugin ABI before V1.5+** (DR-10 remainder) — shell/process executors, arbitrary host imports, DF-03 signing/publish, ABI V2. A **minimal** capability extension (DR-10 minimal, V1.172: WASM compute module + descriptor as the capability executor over the existing compute sandbox) is unlocked by decision-lock D6; browsing the registry still ≠ a full plugin ABI

## Guiding Principles

1. **Local-first by default.** Cloud sync is opt-in. The tool works fully offline.
2. **Wire contracts are truth.** JSON Schema is the single source of truth for cross-language types. No parallel DTO sets.
3. **Simplicity over premature abstraction.** Don't abstract before three concrete use cases. Don't add features until the pattern is proven.
4. **Leverage, don't burden.** Use the user's existing local Agent infrastructure (ACP or native) — no extra agents, runtimes, or accounts they didn't ask for.
5. **SDK and interfaces first.** Developers consume official SDKs, CLI, and registries; first-party UI is a projection of those contracts, not a second implementation.

## Technology Direction

| Choice | Rationale |
|--------|-----------|
| **Rust** for CLI + daemon | Performance, memory safety, local-first ecosystem (sqlx, tokio, wasmtime) |
| **ACP** for agent interoperability | Standard protocol; CLI is an ACP client, not a server |
| **JSON Schema → codegen** | Cross-language contracts from one source (TypeScript + Rust) |
| **SQLite** (via sqlx) | Local-first persistence — simple, portable, zero-ops |
| **Native WASM host** (via wasmtime) | Embeddable compute without browser dependency |
| **Standalone module SDK** (crates.io-publishable) | Guest modules cannot depend on the workspace; official crate owns ABI glue, envelopes, sentinels |
| **Axum** for local HTTP API | Type-safe Rust web framework for the local API |
| **React SPA** (`apps/web`) | One daemon-served SPA; Control Room + Setup + dual layout trees |
| **Entrance split = single SPA, two layouts** | No second web build, no second embed; typed entrance registry + route guards |
| **Tauri v2** (`apps/desktop`) | Desktop shell of the same SPA; native OS + persisted entrance |
| **Pre-1.0** | Breaking changes expected; no deprecation period |

## Decision Log

| Decision | Context | Date |
|----------|---------|------|
| Monorepo with `apps/` + `crates/` + `schemas/` | Product surfaces, reusable Rust libs, and wire contracts each have their own lifecycle | Early 2025 |
| `nexus42` binary hosts both CLI and daemon | Single binary for simplicity (no separate `nexus42d`) | V1.68 (June 2026) |
| Web SPA (`apps/web`) as daemon-served Control Room UI | React SPA, served by the daemon over localhost HTTP; reuses generated contract types | V1.64 (June 2026) |
| Desktop shell (`apps/desktop`) via Tauri v2 | Wraps the web SPA for native OS capabilities (file open, reveal in Finder, sidecar lifecycle); no second DTO set | V1.66 (June 2026) |
| JSON Schema as wire truth source | Enables Rust + TypeScript codegen; avoids hand-written DTO drift | V1.0 planning |
| Integration branches via PR only | All merges to `main` go through GitHub PR for review trail and CI gate | V1.39 (June 2026) |
| `.mstar/` harness conventions | Morning Star harness for plan/QC/QA discipline | V1.39 (June 2026) |
| Canvas program complete (V1.67–V1.76) | 4 β/γ surfaces shipped (Strategy, Outline+Timeline, World KB, Relationships) on React Flow with a structured write-boundary (no raw-file edits from the webview); V1.75 retired the V1.65 whole-document editor in favor of node-granular canvas edits | V1.76 (June 2026) |
| Findings-remediation UI closes the quality loop (V1.77) | Control-Room findings view promoted from read-only (V1.64) to a full remediation authoring surface (6-state status transitions + target_executor assignment + inline edit), consuming the PATCH route the backend already ships; last-writer-wins (single-author triage, no OCC) | V1.77 (June 2026) |
| Creator Memory / SOUL review-loop UI closes the author self-loop (V1.78) | Third and final author-in-command loop (capture → review → internalize), parallel to the canvas writing loop (V1.76) and the findings quality loop (V1.77). Published the memory OSS contracts (14 schemas + codegen + normalized hand-written handler DTOs, fixing the daemon-runtime no-hand-written-DTO invariant; `@42ch/nexus-contracts` 0.12.0 → 0.13.0) + Memory review-loop Control-Room surface. Compound captured the reusable contracts-gap-on-shipped-backend pattern | V1.78 (July 2026) |
| Author Reflection — first post-loop-closure iteration (V1.79) | After all three author-in-command loops closed (V1.76–V1.78), V1.79 deepens the author's ability to *reflect on* what the loops produce rather than opening a new loop: manuscript reading surface (Track A, read-only, no write routes, body-ownership invariant preserved) + SOUL personality visualization (Track B, keyword clusters + temporal drift). Additive wire DTO (`memory-fragment-info` + `keywords`/`created_at`; `@42ch/nexus-contracts` 0.13.0 → 0.14.0). **DF-49 (Standalone MCP server) cancelled** — conflicts with the ACP-client product direction (CLI is an ACP client, not a server) + circular-invocation risk. Compound captured the cursor-pagination-without-total count-label pattern | V1.79 (July 2026) |
| Reliability investment iteration (V1.80) | First "stabilize before extending" iteration after four consecutive feature iterations (V1.76–V1.79). Closed REL-01 (`R-V178P0-QC3-003`): rewrote `POST /v1/local/memory/review` from an unbounded synchronous whole-queue loop into a bounded (`REVIEW_BATCH_LIMIT=50`) / per-creator serialized (in-process mutex map on `WorkspaceState`) / deadline-aware (5s partial-progress) synchronous pipeline — deliberately keeping the synchronous request/response shape (no async job infrastructure) as proportional to the local-only / single-active-creator / small-queue threat model. Additive wire DTO (`ReviewResponse` + `has_more`/`processed`; `@42ch/nexus-contracts` 0.14.0 → 0.15.0). Also closed four low V1.79-QC frontend hygiene residuals. Compound captured the bounded-drain-completion-contract pattern (`has_more` must reflect queue advancement, not rows attempted) | V1.80 (July 2026) |
| Creator SOUL Maturation (V1.81) | Returned to the feature cadence after V1.80 stabilization, deepening the V1.79 reflection axis. Headline = Creator-SOUL Narrative — LLM on-demand synthesis of the creator's accumulated fragments into a reflective "who you are becoming" narrative, world-agnostic, persisted with stale-invalidation (no background jobs; consistent with V1.80's synchronous/proportional discipline). Shipped the user-locked "Creator SOUL (whole) vs World projection (subset)" product model: `memory_fragments.world_id` (additive) threads world provenance through the review-promotion seam, enabling a UI world-selector that drills the keyword/drift/growth viz into a world subset (per-World LLM narratives deferred). Plus independent growth-curve (BL-10) + auto-refresh. Synthesis reuses the `SessionDigestSummarizer` trait-seam pattern (`SoulNarrativeSynthesizer` + `AcpSoulNarrativeSynthesizer` via `CapabilityRegistry` → `acp.prompt`). Additive wire (`memory-fragment-info` + `world_id`; new soul-narrative request/response schemas; `@42ch/nexus-contracts` 0.15.0 → 0.16.0). QC tri-review 3/3 Approve after a 3-round fix-wave that surfaced the read-path-cost vs sound-gate tension; compound captured the fingerprint-cached-live-aggregate pattern | V1.81 (July 2026) |
| SOUL Completion (V1.82) | Completes the SOUL track after V1.79 (Track B) → V1.81 (Maturation) — the third consecutive SOUL iteration. Headline = per-World SOUL narrative: extends `POST /v1/local/memory/soul/reflect` with optional `world_id`; `memory_soul_narratives` cache key becomes composite `(creator_id, world_id)` (NULL = Creator-level) via a by-name column-list recreation with a SQLite NULL-PK partial unique-index mitigation (the V1.81 greploop `SELECT *` lesson applied). Per-World ownership enforced server-side via the existing `is_world_owned` (`narrative_worlds.owner_creator_id`); synthesis stays gated behind `force_regenerate=true` (the V1.81 greploop read-path-invariant lesson, negative-path tested per world); distinct-keyword count is threshold-saturated (returns the gate threshold, not an exact-above-threshold value, to bound the expensive recompute). Worlds-endpoint wiring: the frontend consumes the EXISTING `/v1/local/narrative/worlds` (titles) — iteration-start grep discovered the endpoint already existed, so "worlds endpoint" was a frontend-wiring task, not a new build. Additive wire (`soul-narrative-request` + `world_id`; `@42ch/nexus-contracts` 0.16.0 → 0.17.0). P-last swept all 7 V1.81 residuals (validate-draft step, typed error mapping, narrative cap, UI copy, + 3 already-addressed-by-V1.82). QC 3/3 Approve after 1 fix-wave (worlds-shape unwrap + threshold-saturation). Compound updated `fingerprint-cached-live-aggregate` with the threshold-saturated-response-field refinement. BL-12 fully closed (minus realtime push, backlog) | V1.82 (July 2026) |
| Local API Trust-Boundary Hardening (V1.86) | First dedicated security iteration, triggered by three external Cursor reports that compound into one attack chain on the daemon Local API (Guiding Principle #1, "Local-first privacy"). P0 closes the chain: replaced `CorsLayer::permissive()` with an Origin-allowlist `CorsLayer` + a `require_allowed_origin` middleware (reject non-allowlisted `Origin`, allow no-`Origin` non-browser clients, bypass `OPTIONS` preflight — allowlist derived from the daemon's own listening port + Tauri webview origins `tauri://localhost`/`http://tauri.localhost` + Vite dev + `NEXUS_DAEMON_ALLOWED_ORIGINS`); denied `fs/*` host tools unconditionally when no active workspace is configured; replaced the string-prefix path comparison with component-wise `Path::starts_with` via the shared `resolve_guarded_path`. P1 swept the same class + verified five prior "resolved" residuals (2 were insufficient → fresh regression-of-resolution residuals). keyless-localhost kept as default (deprecation deferred); CSRF framework + Unix-socket listener explicitly non-goals. New normative section `daemon-runtime.md §13`. `wire_contracts_changed: false`. QC 3/3 Approve (qc2 deep security lens: all 3 attack paths CLOSED, no bypass). Compound captured the "resolved-residual-is-a-claim" verification protocol | V1.86 (July 2026) |
[…23ln elided…]
| V1.148 — SPOKE `0.6.1→0.8.2` pin + RuleQueryPort production + `orchestrate_check` daemon route + **Connect Host N-C0** (DF-72) | **Context:** PD-09 (2026-08-02) locked FL-R: third parties provide narrative reasoning to ordinary users with their own orchestration/UI; Nexus supplies World KB + Adapter ops over spoke-connect — the third consumption surface alongside Daemon HTTP. **Decision:** pin spoke `=0.8.2` (P0); ship production `RuleQueryPort` (`spoke_rules` table, closes R-V1142P1-001) + Daemon HTTP `POST /v1/daemon/check` via adapter `orchestrate_check` (P1–P2); land **DF-72 N-C0** — opt-in Connect Host (`nexus42 connect start`, cargo feature `connect-host` default off, separate process), signed hello + fail-closed allowlist, honest single-builder `HostCapabilityManifest` (device-id `host_id`, `data-store` role, no `reasoning-complete`), every inbound op refused (`op_unsupported`) (P3); P4 dogfooded N-C0 against a real spoke-connect peer on a seeded workspace (handshake/manifest/refusals/allowlist/token-gate/feature-off all green; 2 defects fixed — device-id double-nesting canonicalization + request_id padding) and the check route (happy/ownership-reject/empty). `wire_contracts_changed: true (P2 additive check DTOs; @42ch/nexus-contracts 0.27.0→0.28.0)`. **N-C1..N-C3 + DF-73** stay on the FL-R roadmap (tracker DF-72/73; N-C1 owner architect, trigger: N-C0 dogfood green + partner demand). | V1.148 (August 2026) |
| V1.151 — FL-L W6 Assembly inspector (DF-76): read-only observation of `assemble_moment` + thin Moment Directive Control Room input | **Context:** V1.149 (DF-74) shipped default-on lore activation and V1.150 (DF-75) shipped the slot engine + Moment Directive + generation-stage gates, but the traces stayed CLI diagnostics — authors could not see in the Control Room which lore fired, where it landed, what it cost in tokens, or whether a directive was active; the V1.150-deferred thin Control Room Moment Directive input was still pending. **Decision:** ship the **Assembly inspector** (DF-76, FL-L W6) — an **enriched inspector packet** as a **separate emission path** (assembled bytes stay byte-identical to V1.150, AC-I6) whose `modules.*` section keeps the spoke assemble-module recipe vocabulary (`placement` = accepted entries; `activation_trace` = full fire/miss — consumer-only, presents the trace, does not clone the spoke assemble wire) plus three additive **product-local** sections (`slot_map` post stage-gate, `budget` chars/4 estimates + cap/remaining, `moment_directive` status/metadata only — the directive body never on the wire, AC-I3). Shared builder relocated to `nexus-moment-context-assembly/src/inspector.rs`; surfaced via daemon route `POST /v1/daemon/inspector/moment` (tier2, ownership-guarded), CLI `assemble-moment --inspect` / `--emit-packet`, and a read-only Control Room panel; the **Moment Directive Control Room input** (thin set/show/clear over the shipped CLI surface via `POST /v1/daemon/moment-directive` + UI) **closes the V1.150-deferred item**. `wire_contracts_changed: true` (`@42ch/nexus-contracts` 0.28.0→0.28.1 — additive daemon inspector/directive DTOs, product-local only; no new spoke wire). P2 dogfood (produce/review/persist, slots + directive + hops) green; zero-residual. Next FL-L wave: DF-77 (Knowledge Pack productization). | V1.151 (August 2026) |
| V1.152 — FL-L W7 (DF-77): Knowledge Pack productization — Control Room Pack panel + daemon routes + CLI conflict-policy completeness | **Context:** V1.146 P3 shipped the Pack transport (CLI `creator world kb pack export\|import`) but deferred the author-facing surface: no daemon HTTP routes, and `rename`/`overwrite` conflict policies were CLI stubs. **Decision:** ship the productization wave — additive ownership-guarded daemon routes (`POST /v1/daemon/worlds/:id/kb/pack/{export,import}`) with all three conflict policies (skip/rename/overwrite), a shared import-orchestration module (`nexus-daemon-runtime/src/pack_import.rs`) reused by CLI + daemon, and a Control Room Pack panel (export download + import upload + results + overwrite confirmation). Completes FL-L W4→W7 (DF-74→DF-77). `wire_contracts_changed: true` (4 new pack DTOs; contracts 0.28.1→0.29.0). **Note:** V1.149/V1.150 Decision Log rows are still missing (backfill-overdue). | V1.152 (Aug 2026) |
| V1.156 — Timeline 3×2 matrix completion + Harness product rename | **Context:** V1.123 locked the three-layer Timeline model (Brief·Narrative·Moment) with World=Brief+Narrative and Work=Narrative+Moment shipped; two matrix cells remained open (World×Moment, Work×Brief). Separately, DF-V1122-HARNESS-RENAME (Strategy→Harness UI rename) had rolled forward since V1.122 — STRATEGY canonized three pillars (Harness·Canvas·Computable) but UI still said "Strategy". **Decision:** complete the 3×2 matrix in two frontend-only plans — P1 World Timeline Moment layer (read/projection of bound Works' Outline scene/beat data; PD-3 Moments Work-owned; fixture-driven, DR-26 tracks the wire extension) + P2 Work Timeline Brief layer (projection of bound World's `block_type=era` via V1.73 `kb/graph`; PD-2 read-only) — and land the Harness rename in P3 (user-visible Strategy/Strategies → Harness; Preset stays; internal identifiers unchanged; zh pillar = English "Harness" proper-noun). All three plans `wire_contracts_changed:false`. Read-only inspectors on projected layers (no `kb.patch_entity` write path). `wire_contracts_changed: false` (all 3 plans). |
| V1.162 — Fork spine completion (DF-V1122-FORK-UI create + read-only lineage) | **Context:** the spine model (World + Timeline + KnowledgeEntry + Fork) had every element surfaced except Fork — `nexus.fork.create` (V1.60 P0) existed in the engine but was unreachable from any author surface, and its lineage was dropped on the read path (`narrative_gateway` hardcoded `is_fork:false`). A scout-grounded direction lock caught the wrong "spine-ready" handoff assumption and redefined the iteration as a full vertical. **Decision:** (P1) carrier B — fork lineage rides the `fork_created` canon marker `extensions_nexus_json.fork_lineage` (no new table/migration; lazy-branch V1.26+ model preserved) + a thin-delegate daemon route `POST /v1/daemon/worlds/:world_id/forks` (owner-gated, fork-point validation) + PD-01 reconciliation (local authoring fork != platform/social fork); (P2) World-Timeline fork-point create + PD-6 post-create landing (enter forked branch via client-side `activeBranchId` threaded into the existing `branch_id` query param; no daemon read change) + read-only lineage chrome (marker-derived badge + parent + fork-point + one-hop parent). fork-merge stays BL-01. `wire_contracts_changed: true` (P1 additive route + DTOs; `@42ch/nexus-contracts` 0.29->0.30). Spec: entity-scope-model §6.6.1-6.6.3. | V1.162 (Aug 2026) |
| V1.163 — Cross-surface event binding + reliability sweep | **Context:** V1.123 P3 shipped surface-level navigation CTAs ("View on World Timeline" / "View in Work Timeline") using only `WorkDetailResponse.world_id` — jumps landed on the other surface's general Narrative layer, not on specific linked events. Separately, the legacy daemon `outbox` table (zero Rust consumers since V1.159 T3) was overdue for DROP per DR-08. **Decision:** (P1) ship event-level read-only deep-links via an additive optional `world_event_id` on Work Outline `timeline_events[]` (carrier); referent = World KB entity `key_block_id` (architect-locked: NOT `narrative_timeline_events.id`/`evt_`); bidirectional Narrative↔Narrative navigation with PD-5 three-state matrix (event bind → `?layer=narrative&event=…`; surface only → V1.123 fallback; no bind → hidden); inbound focus via React Flow node selection (`?event=` param); reverse fan-out capped at N=20 most-recent Works. Read-only — author-explicit binding (write path) deferred to dogfood validation. §3.3.3 V1.163 amendment. (P2) DROP legacy `outbox` table (migration `20260812_drop_legacy_outbox.sql`; `outbox_entries`/`partial_apply_states` untouched); acp-host flake (R-V1151P2-004) unreproduced in 7/7 runs. `DB_SCHEMA_VERSION` bumped 15→16 (Bugbot catch). `wire_contracts_changed: true` (P1 additive `world_event_id`). | V1.163 (Aug 2026) |
| V1.164 — Spoke 0.10.0 L5-mind mental layer (data pipeline + checker + read-only Canvas) | **Context:** spoke 0.10.0 (2026-08-14) shipped the mental-world-tom research pack (MWM arXiv 2607.27201 + OmniToM arXiv 2605.26322) as wire shapes — MindState (l5-mind), `TimelineEvent.modules.observation`, MentalFieldMap/MindDelta, `validate_mind_state` — with engines intentionally product-owned. Nexus stored what happened but not who knew it; false belief and dramatic irony were prompt luck. **Decision:** adopt the wire shapes end-to-end in 3 plans. (P1) spoke 0.9.2→0.10.0 lockstep (Rust×3 + npm×2 + drift-gate pin; single known compile break at the seam literal — actual type is non-Option `HashMap<TimelineEventModulesKey, TimelineEventModulesValue>`) + bidirectional `modules` passthrough at the narrative↔spoke seam (non-lossy; values object|array only; reject-not-drop contract with should_panic tests) + `narrative_timeline_events.modules_json` (v17). (P2) `modules.mental` (nine-field) + `modules.belief` (handbook propositions — holder/truth/access/source, never paper aliases actor/knowledge_access/mental_source) on holder KnowledgeEntry riding `kb_key_blocks.modules_json` zero-migration; `mind_states` derivative table (v18) with the validate gate at the **nexus-spoke-adapter boundary** (sole-consumer layering rule — storage stays pure); orchestrateCheck checker **pair**: `stale_belief_drift` (warning — actor IS observer / no informing event) vs `dramatic_irony_asymmetry` (info — actor absent, the deliberate box/basket thesis), truth-label-driven detection with latest-by-sequence informing-event classification. (P3) `modules` additive on `WorldKbEntityProjection` + `TimelineEventInfo` (codegen; `wire_contracts_changed`), Studio fixture light+dark (AR-6 studio-first), character inspector Mental State section + event inspector Observers line (read-only, graceful degradation, en+zh i18n). Engines (belief revision / ToM inference / observation rendering) explicitly out. Open product decisions: R-V1164P2QC-001 (world-scoped findings attribution — `findings.work_id` FK) + R-V1164P3QC-001 (observation authoring ergonomics). CI hardening: setup-monorepo pnpm cache now conditional on install-deps. | V1.164 (August 2026) |
| V1.165 — Mental-layer pipeline close-out (world findings home + observation authoring) | **Context:** V1.164 shipped the mental layer with two registered deferrals: world-scoped check findings 500 (spoke Finding is entry-scoped, but the only persistence home was the work-scoped `findings` table with NOT NULL `work_id` FK→works) and observation/mental authoring (Canvas reads `modules` that only fixtures could seed; the patch DTO `{title,body,aliases,block_type}` could not carry them). **Decision:** (P1) new `world_findings` table (v19; spoke vocabulary verbatim at persist AND read; FK→narrative_worlds; no creator column) + FindingPort routing via `extensions.nexus` scope discriminator inside the adapter (work_id→legacy byte-identical; world_id→world home; both/neither reject INVALID_INPUT; one batch tx) + checker stamp fix (world_id, the 500 root cause) + `GET /v1/daemon/worlds/:world_id/findings` (ownership-gated; SQL LIMIT probe 501/cap 500 + honest truncated flag — Bugbot-caught unbounded query fixed in-wave 226a7c5a). Legacy work findings + quality loop untouched (whole-table-enumerating consumers made column-widening unsafe). (P2) `modules` additive on `WorldKbEntityPatch` (first-level key upsert merge; omit=preserve; {}=no-op; ModuleMap shape guard at the wire boundary incl. fail-closed promote guard — the conversion seam silently drops non-conforming entries) → mental/belief/observation authoring through the product patch path; CLI `creator world event-add --observer/--access` for the checker-consumed narrative-log carrier. Dual carriers stay dual (no join). Canvas E2E via the product write path (PATCH→graph→inspector Observers line; AR-7 single-referent). Both V1.164 residuals closed by their registered completion definitions. Pattern crystallized: scope-discriminated port persistence (extensions.nexus routing generalizes to any scope-split spoke port output). | V1.165 (August 2026) |
| V1.166 — Quality-loop close-out (DR-64): rules-driven check evaluator + Control Room world-findings/rules surface | **Context:** DR-64 (real run_checker evaluator) was unlocked by V1.165's world findings home; the check callback shipped V1.164 as the mental pair only, `spoke_rules` had no production write path, and world findings were invisible in the product UI. **Decision (grill-me lock):** deterministic structured-constraint evaluator over four families (module_presence/module_absence/required_field/observer_cardinality) with the carrier at `extensions.nexus.constraint` (spoke leaves statement grammar product-owned); world scoping via adapter-crate wrapper `orchestrate_check_world_scoped` adopted by BOTH production callers (daemon + Connect N-C2) with fail-closed foreign/embedded reject — a spoke port split needs a release; CLI-first rule authoring (`creator world rule add/list/deactivate`); read surface at `/worlds/:worldId/findings` (findings panel + rules read-only section, PD-2). NL agent-judge lane deferred (DR-70, dogfood-gated). QC tri caught F-001 (manifest compile guard certifying the pre-wrapper entrypoint) — fixed wave; both plans tri-Approve + QA PASS zero-residual; `wire_contracts_changed: true` (addive world-rules-list-response DTO). | V1.166 (Aug 2026) |
| V1.167 — Dogfood sweep of the V1.164–166 window + local-only creator bootstrap | **Context:** three consecutive iterations (V1.164–166) shipped without real-surface usage; all three next-iteration candidates (DR-70 NL lane, mental-field authoring, rules UI) were dogfood-gated; separately 3 dependabot alerts on Cargo.lock entries. **Decision (grill-me lock):** dogfood-driven fix round over the full V1.164–166 window with rule-based triage (A must-fix / B tracker candidate / C nit; single consolidated scope gate). Dogfood (P0) validated all lanes and produced an evidence-backed findings register; the one confirmed Class-A (DF-A-02) shipped as `creator register --local` — complete local-only bootstrap materializing BOTH stores (persistent `ctr_local*` identity + active-creator config + workspace `creators` row via new `nexus-local-db::ensure_creator_row`), eliminating the platform-auth dead-end under the paused platform integration and the undocumented HTTP-upsert workaround; dependabot alerts dispositioned with evidence (yamux #41 reachable-in-artifact via connect-host; hickory #42/#43 lockfile-only) — deferral accepted, zero lockfile diff. Gate-notes: DR-70 = N (no four-family expressiveness gap observed), mental-field authoring = Y (DF-81), rules UI = Y (DF-82). `wire_contracts_changed: false`. | V1.167 (Aug 2026) |
| V1.168 — Native host provider series: external protocol clients + dsh-native | **Context:** Nexus owned vendor wire parsers for the two native Harness rails (`claude --print` line stream; `codex exec --json` hand-rolled JSONL) — brittle under weekly CLI drift; no first-party DeepSeek Harness provider. **Decision (grill-me lock):** complete replace, not a second stack — `claude-native` internals become `claude-codes` (stream-json) and `codex-native` becomes `codex-codes` (**app-server** JSON-RPC), keeping `ProviderAdapter`/`HostEvent`/provider ids/discovery; decode-drift contract locked (unknown variant skip vs typed-decode/stream-abort → single terminal `OpFailed`; no capability raises); new `dsh-native` provider driven by crates.io `deepseek-harness-sdk` with PATH/`DSH_RUNTIME_BIN` discovery and an honest `dsh_limited` descriptor (no incremental streaming/cancel on the SDK surface). QC caught and fix-waved real reliability defects (provider-global lock across frame reads, 180 s frame-gap timeout, stale turn terminal poisoning the next turn, env-route scan invisibility). ACP-first rail unchanged. `wire_contracts_changed: false`. | V1.168 (Aug 2026) |
| PD-11 — Dual-audience, developer-first pivot | **Context:** 2026-08-20 user-locked direction. Developers are the primary audience; the first-party authoring app is the reference creator surface. Research: pivot product-direction report, technical-feasibility report, and decision lock (2026-08-20 research package; held in the local harness research archive, not tracked in-repo). **Decision:** STRATEGY vision = local-first narrative-orchestration platform; three consumption ends (CLI+Daemon API / Create layout / nexus-runtime+Connect per PD-09); official compute-module SDK + User-layer entrance split (`developer` \| `content-creator`, orthogonal to agent-layer Creator profiles); no new first-party player, no MCP server (DF-49), no user-authored capability ABI before V1.5+ (DR-10); DF-81 deprioritized. First slice = V1.170 A+B. | V1.170 (2026-08-20) |
| DF-49 reversed: tools-only MCP exposure surface | **Context:** ACP wire has no client→agent tool-registration API; native CLI provider adapters pin `structured_tool_calls: false` — the integrator journey (register custom tools → reasoning agents use them) had no viable provider-native face. **Decision (2026-08-24, user):** nexus serves a scoped MCP server (official `rmcp` SDK, stdio transport first; streamable HTTP deferred) exposing the full registry (builtin + user + peer tools) through the single dispatch spine; consumers = any MCP client (ACP `newSession.mcp_servers`, native CLI `--mcp-config`, third-party tools). Supersedes V1.79 DF-49 cancellation rationale for this scope only. | 2026-08-24 (V1.174) |
| First-class Actor product model — Actor as cross-cutting identity primitive | **Context:** the earlier same-day Character Viewpoint-as-identity direction (2026-09-04) encoded Viewpoint as identity and Character as an existing World-scoped `KnowledgeEntry(block_type=character)`; the user rejected that direction — Character must be a durable first-class identity, not lore. **Decision:** lock **Actor** as the cross-cutting narrative identity primitive — *who can think and act* — with `ActorRef = Creator | Character` (closed v1 sum; not a unified actors-table commitment): Creators conduct the story (SOUL+Memory, omniscient KE read over owned Worlds, shipped operational `creator_id` owner); Characters live it (own SOUL+Memory+ToM on the V1.164–166 l5 foundation + image/persona assets; enter **one or more** Worlds via explicit `ActorWorldBinding`s — creation establishes an initial binding atomically, an active Character never has zero active bindings, and last-binding removal must fail or transition the Character out of active state per the later lifecycle contract; one KnowledgeEntry primitive with exactly one canonical owner scope — World-owned KE stays World-local, Character-owned KE is shared explicitly across that Character's bindings without copying, ActorWorldBinding-owned KE is isolated to that World life); Viewpoint demoted to subordinate execution context; one Agent Host/runtime; existing Creator storage byte-stable and existing character KEs remain WorldSheets. Supersedes the same-day Viewpoint-as-identity direction; no fourth pillar, second runtime, companion app, or first-party player. Accepted 2026-09-04; **v1.184 shipped** the identity/binding/knowledge/execution/memory/ToM vertical (PR [#240](https://github.com/42ch-dev/nexus/pull/240)); **v1.185** developer maintenance (edit/archive, WorldSheet, KE content, `--remember`) is **not shipped**. Durable authority: [actor-product-model.md](.mstar/specs/actor-product-model.md). | 2026-09-04; honesty 2026-09-06 |


[Showing lines 1-105 and 129-143 of 143; 23 middle lines (21.6KB) elided. Read artifact://13 for full output]