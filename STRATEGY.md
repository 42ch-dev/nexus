# Strategy

## Vision

**Nexus is the local-first creative-writing tool where a World's Timeline is the central instrument, AI agents are harnessed through Canvas, and Computable modules make worlds react.**

A Nexus Timeline is not one line — it is **three instruments at three scales**: **Brief** (world shape), **Narrative** (events at human pace — the Timeline layer, distinct from prose-craft narrative writing), and **Moment** (scene precision — the Timeline layer, distinct from Moment Context Assembly) — World timelines lead from the Brief; Work timelines lead from the Moments.

Nexus is a **local-first, AI-agent-driven creative writing tool** that uses an infinite canvas to organize ideas and orchestrate the writing workflow — putting authors in full control of their craft and data. After V1.121 the surfaces were functionally rich but narratively scattered: the product read as a feature list, not a thesis. The V1.122 pivot canonizes the three pillars the product has been building toward. V1.123 deepens Timeline into Brief · Narrative · Moment with a World/Work layer split.

### Three pillars

| Pillar | Means (author language) | Maps to in the codebase |
|--------|-------------------------|-------------------------|
| **Harness** | How an author *harnesses* AI agents to execute creative work — control strategy, orchestration, capability routing, presets. | `crates/nexus-orchestration/` + `crates/nexus-agent-host/` + `crates/nexus-acp-host/` + capability registry + presets. Surfaced in UI as **Harness** — the user-visible pillar-entry rename **Strategy / Strategies → Harness** landed in V1.156 P3 (closes `DF-V1122-HARNESS-RENAME`); **Preset stays** as the mechanism name under Harness (product lock: [.mstar/iterations/v1.156/specs/product-locks.md](.mstar/iterations/v1.156/specs/product-locks.md) PD-4). Internal identifiers (route `/strategies`, `CanvasSurfaceKind = 'strategy'`, `preset` wire fields, CSS classes, hook names) stay unchanged. Specs: [orchestration-engine.md](.mstar/specs/orchestration-engine.md), [agent-host.md](.mstar/specs/agent-host.md), [capability-registry.md](.mstar/specs/capability-registry.md). |
| **Canvas** | The spatial steering surface where authors see and shape their world — with **Timeline-centric World building** as the hero entry surface. Timeline is experienced as **three layers** — **Brief** / **Narrative** / **Moment** — with domain split: **World Timeline** = Brief+Narrative (Brief-led); **Work Timeline** = Narrative+Moment (Moment-led peer surface; Work entry stays Outline). | `apps/web` React Flow canvas surfaces (`CanvasSurfaceKind = "strategy" \| "outline" \| "world-kb-entities" \| "world-kb-relationships" \| "timeline" \| "work-timeline"`). Timeline is the default **World-entry** surface (V1.122; default layer Brief when data exists from V1.123); Work entry stays Outline (V1.118); Work Timeline is a peer from V1.123. Spec: [canvas-strategy-surface.md](.mstar/specs/canvas-strategy-surface.md). |
| **Computable** | The WASM layer that makes worlds *react* — combat resolution, dice, relationship-graph computation, user-authored modules. Not just stored prose. | Native WASM host (`wasmtime`) + compute-module ABI + combat-engine preset (shipped V1.62; foundation V1.114). Specs: [compute-module-abi.md](.mstar/specs/compute-module-abi.md), [wasm-host.md](.mstar/specs/wasm-host.md). |

**Spine vs projection** (locked product model): **World + Timeline + KnowledgeEntry + Fork** are the *spine* (the truth of the narrative universe); **Work + Outline + Manuscript** are *projections* (the authoring plan and prose bound to a World). Timeline is the World's *when* axis at three scales (Brief · Narrative · Moment); Outline is the Work's structural projection; Work Timeline is the Work's when-axis peer (Narrative · Moment). Dual entry defaults encode the model — **World first for World building (Timeline, Brief-led); Work first for chapter writing (Outline), with Work Timeline reachable for scene precision.** See [CONCEPTS.md](CONCEPTS.md) § *Three Pillars* and § *Brief* / *Narrative* / *Moment* for the domain-term framing.

## What we build

Three product surfaces, all open-source, targeting creative writers — novelists, worldbuilders, essayists:

| Surface | Tech | Role | What it does |
|---------|------|------|-------------|
| **`nexus42`** | Rust (CLI + daemon) | **Producer** | CLI commands, daemon lifecycle, local HTTP API, orchestration, World KB management |
| **`web`** | TypeScript (React SPA) | **Consumer** | "Control Room + Setup" UI — served by the daemon, provides the infinite canvas and structured writing interface |
| **`desktop`** | TypeScript + Tauri v2 (Rust) | **Consumer** | Native desktop shell — wraps the web SPA, adds OS-level capabilities (file open, reveal in Finder, sidecar lifecycle) |

Shared needs they serve:

- **Local-first privacy** — data stays on their machine by default; cloud sync is optional
- **AI agent orchestration** — leverage the user's own local agents (via ACP or native) to assist in writing, worldbuilding, and narrative structuring, without forcing extra tooling burden
- **Infinite canvas** — visual, non-linear organization of creative material (worlds, outlines, manuscripts, knowledge entries)
- **Structured narrative tools** — timelines, forks, manuscripts, world knowledge bases — beyond what a plain text editor provides

## What we don't build

- **A cloud platform** — this repo is the open-source CLI/daemon only (the cloud/web platform lives in the private `nexus-platform` repo)
- **A general-purpose note-taking app** — focus is creative writing, not generic notes
- **A competing IDE or editor** — we integrate with the user's existing tools and agents, not replace them

## Guiding Principles

1. **Local-first by default.** Cloud sync is an opt-in feature, never a requirement. The tool works fully offline.
2. **Wire contracts are truth.** JSON Schema is the single source of truth for all cross-language types. No parallel DTO sets.
3. **Simplicity over premature abstraction.** Don't abstract before there are three concrete use cases. Don't add features until the pattern is proven.
4. **Leverage, don't burden.** Directly use the user's local existing Agent infrastructure (ACP or native) — do not introduce extra agents, runtimes, or accounts the user didn't ask for.

## Technology Direction

| Choice | Rationale |
|--------|-----------|
| **Rust** for CLI + daemon | Performance, memory safety, strong ecosystem for local-first tools (sqlx, tokio, wasmtime) |
| **ACP** for agent interoperability | Standard protocol over bespoke — aligns with industry direction; CLI is an ACP client, not a server |
| **JSON Schema → codegen** | Cross-language contracts from a single source — generates TypeScript (npm) and Rust types |
| **SQLite** (via sqlx) | Local-first persistence — simple, portable, zero-ops |
| **Native WASM host** (via wasmtime) | Embeddable compute without browser dependency |
| **Axum** for local HTTP API | Modern, type-safe Rust web framework for the local API surface |
| **React SPA** (`apps/web`) | Local-first Control Room + Setup UI — served by the daemon, Tauri-ready |
| **Tauri v2** (`apps/desktop`) | Cross-platform desktop shell wrapping the web SPA with native OS capabilities |
| **Pre-1.0** | Breaking changes expected and allowed; no deprecation period |

## Decision Log

| Decision | Context | Date |
|----------|---------|------|
| Monorepo with `apps/` + `crates/` + `schemas/` | Product surfaces, reusable Rust libs, and wire contracts each have their own lifecycle | Early 2025 |
| `nexus42` binary hosts both CLI and daemon | Single binary for simplicity (no separate `nexus42d`) | V1.68 (June 2026) |
| Web SPA (`apps/web`) as daemon-served Control Room UI | React SPA, served by the daemon over localhost HTTP; reuses generated contract types | V1.64 (June 2026) |
| Desktop shell (`apps/desktop`) via Tauri v2 | Wraps the web SPA for native OS capabilities (file open, reveal in Finder, sidecar lifecycle); no second DTO set | V1.66 (June 2026) |
| JSON Schema as wire truth source | Enables Rust + TypeScript codegen; avoids hand-written DTO drift | V1.0 planning |
| Integration branches via PR only | All merges to `main` go through GitHub PR for review trail and CI gate | V1.39 (June 2026) |
| `.mstar/` harness conventions | Morning Star harness for plan/QC/QA discipline | V1.39 (June 2026) |
| Canvas program complete (V1.67–V1.76) | 4 β/γ surfaces shipped (Strategy, Outline+Timeline, World KB, Relationships) on React Flow with a structured write-boundary (no raw-file edits from the webview); V1.75 retired the V1.65 whole-document editor in favor of node-granular canvas edits | V1.76 (June 2026) |
| Findings-remediation UI closes the quality loop (V1.77) | Control-Room findings view promoted from read-only (V1.64) to a full remediation authoring surface (6-state status transitions + target_executor assignment + inline edit), consuming the PATCH route the backend already ships; last-writer-wins (single-author triage, no OCC) | V1.77 (June 2026) |
| Creator Memory / SOUL review-loop UI closes the author self-loop (V1.78) | Third and final author-in-command loop (capture → review → internalize), parallel to the canvas writing loop (V1.76) and the findings quality loop (V1.77). Published the memory OSS contracts (14 schemas + codegen + normalized hand-written handler DTOs, fixing the daemon-runtime no-hand-written-DTO invariant; `@42ch/nexus-contracts` 0.12.0 → 0.13.0) + Memory review-loop Control-Room surface. Compound captured the reusable contracts-gap-on-shipped-backend pattern | V1.78 (July 2026) |
| Author Reflection — first post-loop-closure iteration (V1.79) | After all three author-in-command loops closed (V1.76–V1.78), V1.79 deepens the author's ability to *reflect on* what the loops produce rather than opening a new loop: manuscript reading surface (Track A, read-only, no write routes, body-ownership invariant preserved) + SOUL personality visualization (Track B, keyword clusters + temporal drift). Additive wire DTO (`memory-fragment-info` + `keywords`/`created_at`; `@42ch/nexus-contracts` 0.13.0 → 0.14.0). **DF-49 (Standalone MCP server) cancelled** — conflicts with the ACP-client product direction (CLI is an ACP client, not a server) + circular-invocation risk. Compound captured the cursor-pagination-without-total count-label pattern | V1.79 (July 2026) |
| Reliability investment iteration (V1.80) | First "stabilize before extending" iteration after four consecutive feature iterations (V1.76–V1.79). Closed REL-01 (`R-V178P0-QC3-003`): rewrote `POST /v1/local/memory/review` from an unbounded synchronous whole-queue loop into a bounded (`REVIEW_BATCH_LIMIT=50`) / per-creator serialized (in-process mutex map on `WorkspaceState`) / deadline-aware (5s partial-progress) synchronous pipeline — deliberately keeping the synchronous request/response shape (no async job infrastructure) as proportional to the local-only / single-active-creator / small-queue threat model. Additive wire DTO (`ReviewResponse` + `has_more`/`processed`; `@42ch/nexus-contracts` 0.14.0 → 0.15.0). Also closed four low V1.79-QC frontend hygiene residuals. Compound captured the bounded-drain-completion-contract pattern (`has_more` must reflect queue advancement, not rows attempted) | V1.80 (July 2026) |
| Creator SOUL Maturation (V1.81) | Returned to the feature cadence after V1.80 stabilization, deepening the V1.79 reflection axis. Headline = Creator-SOUL Narrative — LLM on-demand synthesis of the creator's accumulated fragments into a reflective "who you are becoming" narrative, world-agnostic, persisted with stale-invalidation (no background jobs; consistent with V1.80's synchronous/proportional discipline). Shipped the user-locked "Creator SOUL (whole) vs World projection (subset)" product model: `memory_fragments.world_id` (additive) threads world provenance through the review-promotion seam, enabling a UI world-selector that drills the keyword/drift/growth viz into a world subset (per-World LLM narratives deferred). Plus independent growth-curve (BL-10) + auto-refresh. Synthesis reuses the `SessionDigestSummarizer` trait-seam pattern (`SoulNarrativeSynthesizer` + `AcpSoulNarrativeSynthesizer` via `CapabilityRegistry` → `acp.prompt`). Additive wire (`memory-fragment-info` + `world_id`; new soul-narrative request/response schemas; `@42ch/nexus-contracts` 0.15.0 → 0.16.0). QC tri-review 3/3 Approve after a 3-round fix-wave that surfaced the read-path-cost vs sound-gate tension; compound captured the fingerprint-cached-live-aggregate pattern | V1.81 (July 2026) |
| SOUL Completion (V1.82) | Completes the SOUL track after V1.79 (Track B) → V1.81 (Maturation) — the third consecutive SOUL iteration. Headline = per-World SOUL narrative: extends `POST /v1/local/memory/soul/reflect` with optional `world_id`; `memory_soul_narratives` cache key becomes composite `(creator_id, world_id)` (NULL = Creator-level) via a by-name column-list recreation with a SQLite NULL-PK partial unique-index mitigation (the V1.81 greploop `SELECT *` lesson applied). Per-World ownership enforced server-side via the existing `is_world_owned` (`narrative_worlds.owner_creator_id`); synthesis stays gated behind `force_regenerate=true` (the V1.81 greploop read-path-invariant lesson, negative-path tested per world); distinct-keyword count is threshold-saturated (returns the gate threshold, not an exact-above-threshold value, to bound the expensive recompute). Worlds-endpoint wiring: the frontend consumes the EXISTING `/v1/local/narrative/worlds` (titles) — iteration-start grep discovered the endpoint already existed, so "worlds endpoint" was a frontend-wiring task, not a new build. Additive wire (`soul-narrative-request` + `world_id`; `@42ch/nexus-contracts` 0.16.0 → 0.17.0). P-last swept all 7 V1.81 residuals (validate-draft step, typed error mapping, narrative cap, UI copy, + 3 already-addressed-by-V1.82). QC 3/3 Approve after 1 fix-wave (worlds-shape unwrap + threshold-saturation). Compound updated `fingerprint-cached-live-aggregate` with the threshold-saturated-response-field refinement. BL-12 fully closed (minus realtime push, backlog) | V1.82 (July 2026) |
| Local API Trust-Boundary Hardening (V1.86) | First dedicated security iteration, triggered by three external Cursor reports that compound into one attack chain on the daemon Local API (Guiding Principle #1, "Local-first privacy"). P0 closes the chain: replaced `CorsLayer::permissive()` with an Origin-allowlist `CorsLayer` + a `require_allowed_origin` middleware (reject non-allowlisted `Origin`, allow no-`Origin` non-browser clients, bypass `OPTIONS` preflight — allowlist derived from the daemon's own listening port + Tauri webview origins `tauri://localhost`/`http://tauri.localhost` + Vite dev + `NEXUS_DAEMON_ALLOWED_ORIGINS`); denied `fs/*` host tools unconditionally when no active workspace is configured; replaced the string-prefix path comparison with component-wise `Path::starts_with` via the shared `resolve_guarded_path`. P1 swept the same class + verified five prior "resolved" residuals (2 were insufficient → fresh regression-of-resolution residuals). keyless-localhost kept as default (deprecation deferred); CSRF framework + Unix-socket listener explicitly non-goals. New normative section `daemon-runtime.md §13`. `wire_contracts_changed: false`. QC 3/3 Approve (qc2 deep security lens: all 3 attack paths CLOSED, no bypass). Compound captured the "resolved-residual-is-a-claim" verification protocol | V1.86 (July 2026) |
| Remote-Access Hardening (V1.92) | V1.90 made the daemon opt-in remote-bind capable; V1.92 makes that safe to use — extending the V1.86 localhost trust boundary to the trusted-LAN. Two headlines: (1) **TLS for remote-bind** — daemon auto-generates a self-signed Ed25519 cert (`rcgen`+`rustls`+`axum-server`), stored `~/.nexus42/tls/` (0o700/0o600); non-loopback binds serve HTTPS; the remote-bind gate becomes 3-condition fail-closed (key + flag + usable cert); loopback stays plain HTTP. (2) **Remote Client Connection Model** — web-ui/desktop "Connect to Daemon" setup screen (endpoint + API key + TOFU certificate-fingerprint pinning); `BrowserClient`/`TauriClient` parameterised by base URL + `X-API-Key`; a resume-time `FingerprintGate` blocks the authenticated client until the served fingerprint matches the stored pin (closes a Cursor-found Medium-severity TLS-MITM-on-resume gap). **CSRF is a deliberate non-goal** — the header-key auth model + V1.86 Origin allowlist make a token framework redundant (codified `daemon-runtime.md` §16.3). Companion: sweeps V1.91 residuals 001-004. `@42ch/nexus-contracts` 0.19.1 → 0.20.0 (additive `GET /v1/daemon/runtime/cert-fingerprint`). QC 3/3 Approve after a fix-wave (cert SAN must include the non-loopback bind host); Greptile 5/5. Compound captured the header-key-CSRF-defence + self-signed-TLS-listener-integration patterns. The localhost trust boundary now scales coherently to the LAN without cloud accounts (Principle #1 preserved end-to-end) | V1.92 (July 2026) |
| Remote-Access Polish & Residual Convergence (V1.93) | A stabilization/convergence iteration sweeping all 8 V1.93-targeted V1.92 residuals in one cohesive close — the same pattern the repo follows after every major feature/security iteration (V1.30/V1.44/V1.80). TLS robustness: cert SAN re-validated against `bind_host` on load + regenerate-on-mismatch (R-V192P0-001; production code shipped in V1.92 fix-wave, V1.93 added the IPv6 regression test); TLS `graceful_shutdown` threads `shutdown_grace_ms` (R-V192P0-002; mapping test); §15.1 SAN-vs-bind-host policy precision-tightened as normative (R-V192SPEC-001; the rule was already codified by the V1.92 review chain). Remote-connection polish: `connection-storage.ts` shape validation guards corrupt `localStorage` (R-V192P1-001); honest-limitation copy on the fingerprint-fetch error guiding to the TOFU-capable desktop app (R-V192P1-002); reconnect "pinned fingerprint matches" hint (R-V192UX-001); desktop `connection_config.rs` test coverage verified + gap-filled (R-V192P1-003). Legacy "Local API" naming stragglers swept across 27 spec/doc files (R-V192HYG-001) after a fix-wave correcting bulk-replace stutters + a broken TOC anchor (QC1 W-001/W-002). Dual-track P-1 → P0 ‖ P1 → P-last on disjoint worktrees. `wire_contracts_changed: false` (polish iteration — no new DTOs/schema). QC 3/3 Approve; QA Pass. 3 residuals open going forward, all V1.94: R-V192SEC-001 (medium — resume TOFU FingerprintGate is in-band over the same MITM-able TLS fetch; real fix is desktop Tauri reqwest+rustls transport-layer cert pinning), R-V193PL-001 (low — pre-existing `/v1/local/*` path-literal staleness in ~12 specs), R-V191P1-005 (nit, unchanged). Compound updated two existing docs: `surface-rename-hygiene-checklist` (anchor gate + pre-commit-self-check lesson) and `resolved-residual-verification` (symmetric "deferred-but-already-satisfied" case — 3/8 V1.93 residuals were already code-complete on main from the V1.92 fix-wave). Completes the V1.90→V1.92→V1.93 remote-access arc; the LAN-remote-author workflow is production-grade | V1.93 (July 2026) |
| Desktop App Onboarding & IA Pass (V1.94) | First-impression UX pass for the desktop app, addressing five author-reported issues: setup wizard (workspace selection + daemon ready + ACP agent detection + done; gated by `setup_completed` in `~/.nexus42/config.toml`), per-launch daemon-ready gate (splash screen until first health probe success), two-tab sidebar IA (Creator + Orchestrator) with nested nav + footer profile switcher, restart-icon-only daemon status bar (crash → top banner), and Strategies unification (presets list + strategy canvas detail). Additive daemon API: `POST /v1/daemon/agent-host/scan` (registry list + PATH install status; `@42ch/nexus-contracts` 0.20.0 → 0.21.0). Browser build defaults `setup_completed=true` (desktop-first UX). Mid-Phase-3 user clarification on the contrast rule ("background-driven, not mode-driven") triggered a full audit that found both 4 DESIGN.md token violations + 6 component implementation violations AND the real root cause of the original "primary button not white" complaint: `tailwind-merge` was silently stripping `text-white` because it didn't recognize custom font-size tokens (`text-button-*` / `text-heading-*` / `text-label-*` / `text-copy-*`). Fix: `extendTailwindMerge` registers all custom font-size tokens; corrects both the immediate symptom AND a latent typography regression across 181 call sites in `apps/web/src/`. Compound captured 2 new docs (daemon-ready-gate-pattern + local-environment-scan-safety-boundary) + 1 updated (nexus-brand-token-hierarchy: V1.94 clarified mode-independence of the contrast rule). QC tri-review 3/3 Approve (twice — initial + post-audit revalidation); QA Pass. 9 V1.95-deferred residuals registered. | V1.94 (July 2026) |
| Setup Wizard Bugfix & Iteration (V1.95) | Fixes six real bugs blocking first‑launch of the V1.94 desktop app: (1) ClientProvider immediate TauriClient for desktop (avoids "The string did not match the expected pattern" error); (2) Daemon error surfacing + migration‑mismatch recovery (opt‑in "Reset local database" button); (3) Setup wizard left‑sidebar layout redesign; (4) Workspace path default change to `~/Documents/nexus/default` (brand `nexus/`, system home `~/.nexus42/`); (5) Native directory picker for workspace selection; (6) FingerprintGate `/setup` route bypass. `wire_contracts_changed: false` (no new DTOs/schemas; all fixes in `apps/web` and `apps/desktop/src-tauri`). | V1.95 (July 2026) |
| V1.96–V1.99 — Setup Wizard rework, Desktop first-launch reliability, Design Studio + DESIGN unification | Grouped catch-up (per-iteration detail lives in compasses). V1.96: Setup Wizard rework + daemon diagnostic chain. V1.97: desktop first-launch reliability hardening. V1.98: Design Studio introduced + `DESIGN.md`/`DESIGN.dark.md` unified as the brand SSOT. V1.99: design system deepening. | V1.96–V1.99 (July 2026) |
| V1.100–V1.106 — Desktop first-launch UI completion, settings shell, agent picker, first-launch wizard reshape, studio-first pipeline | Grouped catch-up. UI completion (desktop first-launch + form fields + guardrails); badge tone + thin settings + surfaces polish; Settings Shell deepening (DF-70 remainder) + Settings workspace path (DF-70 W2); first-launch wizard reshape; UI continuity + studio-first pipeline. | V1.100–V1.106 (July 2026) |
| V1.107–V1.113 — Canvas deepening + Frontend i18n + dogfood reliability + tech debt | Grouped catch-up. Studio UI tune; Canvas Outline Spatial + UI Shell SSOT; Canvas deepening (Scene/Beat + Strategy edges + graph scale); V1.110 dogfood fixes (daemon startup latency + agent-scan PATH reliability + AgentPicker UX polish); Frontend Canvas Navigation + Design Studio + consolidation; Frontend i18n foundation + UI migration (common/shell/settings/catalogs); i18n completion + UI normalization + tech debt. | V1.107–V1.113 (July 2026) |
| V1.114–V1.117 — Canvas architecture foundation + Compute module foundation + Desktop UX polish | Grouped catch-up. V1.114: Canvas & Compute Module Foundation (`CanvasSurfaceAdapter` recipe + `useCanvasSurface()` hook + `useAutoLayout()`). V1.115: Canvas & Compute Foundation Completion. V1.116: Stabilization + Canvas/Compute Readiness research. V1.117: Desktop UX Polish (setup agents catalog + token hygiene). | V1.114–V1.117 (July 2026) |
| V1.118–V1.119 — Daemon no-Profile + Creation Canvas shell + Setup unblock | Grouped catch-up. V1.118: daemon no-Profile mode (run without an active Creator profile) + Creation Canvas shell (canvas-first **Work-entry** default = Outline). V1.119: setup unblock + polish. | V1.118–V1.119 (July 2026) |
| V1.120 — Control Room dogfood polish | Control Room polish pass from dogfood feedback. | V1.120 (July 2026) |
| V1.121 — Design system elevation ("The Literary Engine") | Design system elevation pass that established the visual direction V1.122's three-pillar pivot builds on. 15 low/nit residuals deferred (tracked under `DF-V1122-V1121-RES`). | V1.121 (July 2026) |
| V1.122 three-pillar pivot — Harness · Canvas · Computable + Timeline-first World entry | **Context:** after V1.121 the product was functionally rich but narratively scattered — authors could not restate what Nexus *is*. Caller redirected the iteration from the V1.121-roadmapped polish + residual cleanup to a product pivot for PMF. **Decision:** canonize three pillars — **Harness** (control strategy), **Canvas** (spatial steering, with **Timeline-centric World building** as the hero entry surface), and **Computable** (WASM reactivity) — and elevate Timeline from an Outline sub-lane to a peer `CanvasSurfaceKind = "timeline"` that is the default **World-entry** surface. Work entry stays Outline (V1.118, unchanged). One-sentence thesis installed in Vision above. Spine-vs-projection model locked (World/Timeline/KnowledgeEntry = spine; Work/Outline/Manuscript = projection). `STRATEGY.md` decision log refreshed (V1.96–V1.121 catch-up, grouped). `CONCEPTS.md` gained Harness / Computable / Timeline-first World building entries. `wire_contracts_changed: false` for the iteration. UI rename of Strategy → Harness, Computable pillar surfacing, deeper World-building (Fork UI, compute-on-timeline), and V1.121 residual cleanup are deferred with tracker IDs. | V1.122 (July 2026) |
| V1.123 Three-Layer Timeline — Brief · Narrative · Moment | **Context:** V1.122 shipped Timeline as World-entry hero but as a **flat event list**; caller redirected V1.123 from residual/dogfood cleanup to deepen Timeline(World)+Canvas into three zoom layers with domain-differentiated use. **Decision:** canonize **Brief** (world-global), **Narrative** (event-level, shared), **Moment** (scene/beat-precise) as Timeline's three layers; **World Timeline** = Brief+Narrative (Brief-led default); **Work Timeline** = Narrative+Moment as a new peer surface (Work entry stays Outline). Delivery: P0 specs/contracts + CONCEPTS/STRATEGY canonization; P1 World Timeline Brief+Narrative; P2 Work Timeline Narrative+Moment; P3 Timeline-first IA deepening (global entry + shell prominence); P4 three-layer zoom/feel differentiation. Fork UI, compute-on-timeline, World Moment, Work Brief, era taxonomy depth deferred (`DF-V1123-*` / inherited `DF-V1122-*`). Carrier/route/`wire_contracts_changed` locked by architect in iteration `three-layer-architecture.md`. | V1.123 (July 2026) |
| V1.124–V1.126 — Studio-first gap closure + Control Room dogfood + Shell/Canvas deepening + Tech-debt gate | **Context:** three iterations grouped here (V1.124 Studio-first gap closure for Timeline/Layer/Tokens; V1.125 Control Room dogfood + World-first IA; V1.126 Shell selection submenu + Canvas directed axis + Composite Timeline endpoint + status.json compaction) — all closed V1.123-P3 roadmap items + accumulated tech-debt. **Decision:** deliver each iteration as scoped (see per-iteration compasses); no new STRATEGY pivot. `wire_contracts_changed: true` only on V1.126 P2 (composite Timeline endpoint — additive overview DTO; existing routes unchanged). 32 open residuals remained at end of V1.126 (mostly V1.126 P0/P1/P2 nit polish). | V1.124–V1.126 (July 2026) |
| V1.127 — Dogfood-readiness sweep | **Context:** user about to run a manual usage review after V1.127; asked PM to do predictive code scan to surface friction points before testing. **Decision:** scope M=2 business plans around the scan's top P0/P1 candidates — P0 Control Room author-loop fixes (Create World no-op → disabled-tooltip, cursor pagination for >20 worlds, overview error banner, selection-submenu resize listener, stale renamingItem, Setup-Continue residual close); P1 native agent provider registration in daemon HostManager (closed the 11-iteration-roll-forward `R-V1116P0QA-001` architecture-coherence gap). QC tri-review caught a real AC-vs-code mismatch (qc1 C-001: explore scan conflated HostManager registration with `/scan` endpoint behavior) — fix wave reframed AC + manual verification fallback. `wire_contracts_changed: false` for both plans. 3 knowledge docs crystallized (1 new predictive-scan lesson + 2 updated harness conventions). | V1.127 (July 2026) |
| V1.139 — SPOKE adoption + nomenclature alignment | **Context:** after V1.122–V1.127 completion, the `nexus-kb` crate had diverged from the newly adopted SPOKE standard (`42ch-dev/spoke`) for narrative knowledge data. **Decision:** adopt SPOKE (`spoke-schemas` + `spoke-operations` crates) as the truth source for World KB schema and standard lifecycle operations. Merge `nexus-kb` INTO `nexus-knowledge` (P1); rename conceptual term "KeyBlock" → "KnowledgeEntry" throughout documentation, specs, and schema descriptions while retaining frozen wire/DB identifiers (`KeyBlockStatus`, `BlockType`, `kb_key_blocks`, etc.); shift from `KeyBlock + SourceAnchor` crate purpose description to `KnowledgeEntries + Source Anchors` (P2–P3). `wire_contracts_changed: true` (0.24.0 → 0.25.0; additive schemas, bumped version). Three pillars codebase-mapping: `nexus-kb` crate removed; `nexus-knowledge` now owns World KB domain (merged), spoke boundary via `nexus-spoke-adapter`. | V1.139 (July 2026) |
| V1.141 — SPOKE `0.2.0 → 0.4.0` upgrade + adapter-port architecture adoption | **Context:** spoke 0.3.0/0.4.0 shipped a capability-sliced adapter port + injection-orchestration architecture; nexus was pinned at 0.2.0 with only the old pure-helper surface. **Decision:** jump to lockstep `=0.4.0` and adopt the dual-surface architecture (Surface A = existing pure delegates, frozen; Surface B = new port traits + orchestrators, re-exported through `nexus-spoke-adapter`). The boundary crate becomes the single import surface for both surfaces. Production storage behind baseline port families explicitly deferred to next iteration (Roadmap). `wire_contracts_changed: false` for the upgrade. | V1.141 (July 2026) |
| V1.142 — SPOKE `0.4.0 → 0.4.1` upgrade + production port impls + first `orchestrate_promote` cutover | **Context:** V1.141 shipped the spoke 0.4.0 boundary with Surface A pure delegates + Surface B port/orchestration re-exports and a reference in-memory mock; production port impls were deferred (Roadmap). Spoke 0.4.1 (released 2026-07-28) added timeline beat-assist pure helpers. **Decision:** upgrade to lockstep `=0.4.1`, ship the production adapter (V1.146 rename: `NexusAdapter`; six baseline port families: production KnowledgeEntryPort/RelationPort/FindingPort/ScopeQueryPort against nexus-local-db SQLite + honest stubs for RuleQueryPort/HostManifestPort/list_timeline_events) in `nexus-local-db/src/spoke_adapter/` (closes R-V1141P1-001); cut over the `promote_adopt` daemon write path through `orchestrate_promote` — first real Surface B production consumer (closes R-V1139P1-004 on the adopt path). `wire_contracts_changed: false` for all three plans. | V1.142 (July 2026) |
| V1.143 — Deep SPOKE integration (roadmap step 1 of 3) | **Context:** V1.142 shipped the production adapter + first Surface B cutover (`promote_adopt`); three remaining write paths (upsert/relate/check/assemble) and Timeline wire-type unification were deferred. **Decision:** deliver roadmap step 1 of 3 — Timeline wire-type unification (`TimelineEvent` ↔ spoke `TimelineEvent` `From`/`Into` seam; production `get_timeline_ordered()` on both in-memory and SQLite gateways, P0); `patch_entity` upsert cutover through `orchestrate_upsert` — second Surface B production consumer, with C1 merged-terminal behavior-diff accepted and regression-tested (P1); promote_reject/merge Surface A resolution with deliberate per-outcome retention rationale (P2). **Relate cutover DEFERRED to V1.144:** spoke `Relation` lacks a `revision` field, so the OCC-mirror pattern from `KnowledgeEntry` does NOT transfer — each port family's orchestrability must be verified against the spoke wire type's actual fields, not assumed. `wire_contracts_changed: false`. | V1.143 (July 2026) |
| V1.144 — Deep SPOKE integration (roadmap step 2 of 3) | **Context:** V1.143 deferred the `orchestrate_relate` cutover because spoke 0.4.1's `Relation` type lacked a `revision` field, making OCC-based relate impossible. Spoke 0.5.0 resolved this at the protocol level — additive `Relation.revision: Option<u64>`, OCC-aware `RelationPort` trait (`get_relation` + `put_relation(relation, expected_base_revision)`), and `RelationAlreadyExists`/`RelationNotFound` reject codes. The nexus `kb_relationships` table already had a `revision` column (V1.74). **Decision:** deliver roadmap step 2 of 3 — spoke lockstep pin `0.4.1→0.5.0` across Rust and npm with check-wire-drift CI (P0); production `RelationPort` OCC extension in `nexus-local-db` reusing the existing `revision` column with CAS guard and revision-seed=1 convention (P1); `orchestrate_relate` cutover on daemon relation add/update — third Surface B production consumer, closes `R-V1143P2-DEFER-RELATE` (P2; `remove` stays Surface A). `wire_contracts_changed: false`. Roadmap step 3 of 3 (CLI adapter spoke-protocol service interop) remains as V1.145. | V1.144 (July 2026) |
| V1.145 — Spoke consumer alignment: adapter归位 + scope.extensions + timeline port production | **Context:** V1.144 shipped all three orchestrator cutovers (roadmap step 2 of 3), but the adapter was still in `nexus-local-db` despite the durable layering vision — the conversion seam was in `nexus-knowledge` creating a 3-node dep cycle, the timeline port was a stub despite persisted data, and spoke 0.5.0 Scope lacked `extensions` forcing a typed-carrier workaround. **Decision:** (1) rehome the conversion seam (P1a — orphan rule overcome via free functions `world_kb_to_spoke`/`spoke_to_world_kb` + local `WorldKbEntrySpokeExt` trait) and production adapter (P1b) from `nexus-local-db` to `nexus-spoke-adapter`, reversing the dep graph: spoke-adapter = capability aggregation layer, local-db = pure storage; (2) bump spoke `0.5.0→0.6.0` and redo the scope-pushdown on native `scope.extensions["nexus"]` (P2 redo — removes the typed `KbScopeFilters` carrier); (3) replace the `list_timeline_events` stub with production SQLite query into `narrative_timeline_events` (P3 — data was already persisted; V1.142 stub was incorrect); (4) accept direct `spoke-operations` deps in `nexus-local-db` and `nexus-narrative` as standard leaf library usage, deferring narrative-read-via-spoke-adapter to V1.146 (R-V1145P4-001). `wire_contracts_changed: false`. **Spoke-side improvement candidate:** `SpokeRejectCode` 500-class variant (R-V1144P2-INVALIDINPUT-400 — all four ports misclassify storage errors as 400). | V1.145 (July 2026) |
| V1.146 — SPOKE deep integration / full host graduation | **Context:** V1.145 closed consumer-alignment topology but left narrative ordered-read outside the adapter, FullAdapter ports (computable/fork) incomplete, no pack I/O, no activation dialect, and storage faults still mapped as 400-class. **Decision:** (1) pin spoke `0.6.1` with `InternalError` and map storage failures → HTTP 500 (P0); (2) ordered timeline read via adapter facet, drop local-db/narrative direct ordering deps (P1, closes R-V1145P4-001); (3) rename `NexusBaselineAdapter`→`NexusAdapter`, ship ComputablePort + ForkTimelineQueryPort + manifest capabilities (P2); (4) CLI `creator world kb pack export\|import` with skip default + `pack_import` provenance (P3); (5) flag-gated `modules.activation` in MCA + golden OFF + `--emit-packet` (P4); (6) relation `extensions_nexus_json` round-trip + stub re-affirmation + spec/AGENTS truth (P5). `wire_contracts_changed: false`. **Deferred:** R-V1146P0-ACCEPT-01 (constraint→500), R-V1146P2-001 (session TTL). PR [#193](https://github.com/42ch-dev/nexus/pull/193). | V1.146 (July 2026) |
| V1.147 — Computable pillar activation: direct compute lane (invoke/accept) + Run Studio + compute-on-Timeline | **Context:** V1.122 canonized the Computable pillar but deferred all author surfacing (`DF-V1122-COMPUTABLE-UI`, `DF-V1122-COMPUTE-ON-TIMELINE`); V1.146 graduated the WASM host and ComputablePort but no author-facing lane existed — compute was CLI/manual only. **Decision:** ship the first product lane for compute: (P0) world-scoped `POST /worlds/:id/compute/run` direct lane with sandbox-honest errors, `ComputeInputBuilder`, canon-only `compute_result` projection (Accept writes via `append_event_canon_with_extensions_in_tx`), `compute_sessions` migration, accept/discard/list/detail routes, additive wire `0.25.0→0.26.0`; (P1) Run Studio on Settings → Modules (manifest-driven form + result inspector + Runs history, 5 primitives promoted to `@42ch/nexus-ui`, closes `DF-V1122-COMPUTABLE-UI`); (P2) compute on World Timeline (`GET /worlds/:id/timeline/events`, `0.26.0→0.27.0`), canon Compute-result nodes with provenance, ⌘K jumps + `?module=&run=` deep-links, closes `DF-V1122-COMPUTE-ON-TIMELINE`; (P3) dogfood sweep + specs alignment: 422 per-entry validation, build.rs module walker, world-gated terminal-only DELETE `/compute/runs` + Clear UI, **TTL re-assessed → retain-by-default + manual Clear** (closes R-V1146P2-001, R-CLEAR-RUNS, R-V1147P0-001/002). Locked invariants: review-then-apply (Accept required), no Timeline ghost nodes pre-Accept, no auto-TTL purge. PR [#194](https://github.com/42ch-dev/nexus/pull/194) (squash `99d58f4f`). **Deferred:** R-V1147P3-ACENT-CHIP-001 (WCAG contrast chip) to V1.148+. | V1.147 (August 2026) |
| V1.148 — SPOKE `0.6.1→0.8.2` pin + RuleQueryPort production + `orchestrate_check` daemon route + **Connect Host N-C0** (DF-72) | **Context:** PD-09 (2026-08-02) locked FL-R: third parties provide narrative reasoning to ordinary users with their own orchestration/UI; Nexus supplies World KB + Adapter ops over spoke-connect — the third consumption surface alongside Daemon HTTP. **Decision:** pin spoke `=0.8.2` (P0); ship production `RuleQueryPort` (`spoke_rules` table, closes R-V1142P1-001) + Daemon HTTP `POST /v1/daemon/check` via adapter `orchestrate_check` (P1–P2); land **DF-72 N-C0** — opt-in Connect Host (`nexus42 connect start`, cargo feature `connect-host` default off, separate process), signed hello + fail-closed allowlist, honest single-builder `HostCapabilityManifest` (device-id `host_id`, `data-store` role, no `reasoning-complete`), every inbound op refused (`op_unsupported`) (P3); P4 dogfooded N-C0 against a real spoke-connect peer on a seeded workspace (handshake/manifest/refusals/allowlist/token-gate/feature-off all green; 2 defects fixed — device-id double-nesting canonicalization + request_id padding) and the check route (happy/ownership-reject/empty). `wire_contracts_changed: true (P2 additive check DTOs; @42ch/nexus-contracts 0.27.0→0.28.0)`. **N-C1..N-C3 + DF-73** stay on the FL-R roadmap (tracker DF-72/73; N-C1 owner architect, trigger: N-C0 dogfood green + partner demand). | V1.148 (August 2026) |
| V1.151 — FL-L W6 Assembly inspector (DF-76): read-only observation of `assemble_moment` + thin Moment Directive Control Room input | **Context:** V1.149 (DF-74) shipped default-on lore activation and V1.150 (DF-75) shipped the slot engine + Moment Directive + generation-stage gates, but the traces stayed CLI diagnostics — authors could not see in the Control Room which lore fired, where it landed, what it cost in tokens, or whether a directive was active; the V1.150-deferred thin Control Room Moment Directive input was still pending. **Decision:** ship the **Assembly inspector** (DF-76, FL-L W6) — an **enriched inspector packet** as a **separate emission path** (assembled bytes stay byte-identical to V1.150, AC-I6) whose `modules.*` section keeps the spoke assemble-module recipe vocabulary (`placement` = accepted entries; `activation_trace` = full fire/miss — consumer-only, presents the trace, does not clone the spoke assemble wire) plus three additive **product-local** sections (`slot_map` post stage-gate, `budget` chars/4 estimates + cap/remaining, `moment_directive` status/metadata only — the directive body never on the wire, AC-I3). Shared builder relocated to `nexus-moment-context-assembly/src/inspector.rs`; surfaced via daemon route `POST /v1/daemon/inspector/moment` (tier2, ownership-guarded), CLI `assemble-moment --inspect` / `--emit-packet`, and a read-only Control Room panel; the **Moment Directive Control Room input** (thin set/show/clear over the shipped CLI surface via `POST /v1/daemon/moment-directive` + UI) **closes the V1.150-deferred item**. `wire_contracts_changed: true` (`@42ch/nexus-contracts` 0.28.0→0.28.1 — additive daemon inspector/directive DTOs, product-local only; no new spoke wire). P2 dogfood (produce/review/persist, slots + directive + hops) green; zero-residual. Next FL-L wave: DF-77 (Knowledge Pack productization). | V1.151 (August 2026) |
| V1.152 — FL-L W7 (DF-77): Knowledge Pack productization — Control Room Pack panel + daemon routes + CLI conflict-policy completeness | **Context:** V1.146 P3 shipped the Pack transport (CLI `creator world kb pack export\|import`) but deferred the author-facing surface: no daemon HTTP routes, and `rename`/`overwrite` conflict policies were CLI stubs. **Decision:** ship the productization wave — additive ownership-guarded daemon routes (`POST /v1/daemon/worlds/:id/kb/pack/{export,import}`) with all three conflict policies (skip/rename/overwrite), a shared import-orchestration module (`nexus-daemon-runtime/src/pack_import.rs`) reused by CLI + daemon, and a Control Room Pack panel (export download + import upload + results + overwrite confirmation). Completes FL-L W4→W7 (DF-74→DF-77). `wire_contracts_changed: true` (4 new pack DTOs; contracts 0.28.1→0.29.0). **Note:** V1.149/V1.150 Decision Log rows are still missing (backfill-overdue). | V1.152 (Aug 2026) |
| V1.156 — Timeline 3×2 matrix completion + Harness product rename | **Context:** V1.123 locked the three-layer Timeline model (Brief·Narrative·Moment) with World=Brief+Narrative and Work=Narrative+Moment shipped; two matrix cells remained open (World×Moment, Work×Brief). Separately, DF-V1122-HARNESS-RENAME (Strategy→Harness UI rename) had rolled forward since V1.122 — STRATEGY canonized three pillars (Harness·Canvas·Computable) but UI still said "Strategy". **Decision:** complete the 3×2 matrix in two frontend-only plans — P1 World Timeline Moment layer (read/projection of bound Works' Outline scene/beat data; PD-3 Moments Work-owned; fixture-driven, DR-26 tracks the wire extension) + P2 Work Timeline Brief layer (projection of bound World's `block_type=era` via V1.73 `kb/graph`; PD-2 read-only) — and land the Harness rename in P3 (user-visible Strategy/Strategies → Harness; Preset stays; internal identifiers unchanged; zh pillar = English "Harness" proper-noun). All three plans `wire_contracts_changed:false`. Read-only inspectors on projected layers (no `kb.patch_entity` write path). `wire_contracts_changed: false` (all 3 plans). |
| V1.162 — Fork spine completion (DF-V1122-FORK-UI create + read-only lineage) | **Context:** the spine model (World + Timeline + KnowledgeEntry + Fork) had every element surfaced except Fork — `nexus.fork.create` (V1.60 P0) existed in the engine but was unreachable from any author surface, and its lineage was dropped on the read path (`narrative_gateway` hardcoded `is_fork:false`). A scout-grounded direction lock caught the wrong "spine-ready" handoff assumption and redefined the iteration as a full vertical. **Decision:** (P1) carrier B — fork lineage rides the `fork_created` canon marker `extensions_nexus_json.fork_lineage` (no new table/migration; lazy-branch V1.26+ model preserved) + a thin-delegate daemon route `POST /v1/daemon/worlds/:world_id/forks` (owner-gated, fork-point validation) + PD-01 reconciliation (local authoring fork != platform/social fork); (P2) World-Timeline fork-point create + PD-6 post-create landing (enter forked branch via client-side `activeBranchId` threaded into the existing `branch_id` query param; no daemon read change) + read-only lineage chrome (marker-derived badge + parent + fork-point + one-hop parent). fork-merge stays BL-01. `wire_contracts_changed: true` (P1 additive route + DTOs; `@42ch/nexus-contracts` 0.29->0.30). Spec: entity-scope-model §6.6.1-6.6.3. | V1.162 (Aug 2026) |
| V1.163 — Cross-surface event binding + reliability sweep | **Context:** V1.123 P3 shipped surface-level navigation CTAs ("View on World Timeline" / "View in Work Timeline") using only `WorkDetailResponse.world_id` — jumps landed on the other surface's general Narrative layer, not on specific linked events. Separately, the legacy daemon `outbox` table (zero Rust consumers since V1.159 T3) was overdue for DROP per DR-08. **Decision:** (P1) ship event-level read-only deep-links via an additive optional `world_event_id` on Work Outline `timeline_events[]` (carrier); referent = World KB entity `key_block_id` (architect-locked: NOT `narrative_timeline_events.id`/`evt_`); bidirectional Narrative↔Narrative navigation with PD-5 three-state matrix (event bind → `?layer=narrative&event=…`; surface only → V1.123 fallback; no bind → hidden); inbound focus via React Flow node selection (`?event=` param); reverse fan-out capped at N=20 most-recent Works. Read-only — author-explicit binding (write path) deferred to dogfood validation. §3.3.3 V1.163 amendment. (P2) DROP legacy `outbox` table (migration `20260812_drop_legacy_outbox.sql`; `outbox_entries`/`partial_apply_states` untouched); acp-host flake (R-V1151P2-004) unreproduced in 7/7 runs. `DB_SCHEMA_VERSION` bumped 15→16 (Bugbot catch). `wire_contracts_changed: true` (P1 additive `world_event_id`). | V1.163 (Aug 2026) |
| V1.164 — Spoke 0.10.0 L5-mind mental layer (data pipeline + checker + read-only Canvas) | **Context:** spoke 0.10.0 (2026-08-14) shipped the mental-world-tom research pack (MWM arXiv 2607.27201 + OmniToM arXiv 2605.26322) as wire shapes — MindState (l5-mind), `TimelineEvent.modules.observation`, MentalFieldMap/MindDelta, `validate_mind_state` — with engines intentionally product-owned. Nexus stored what happened but not who knew it; false belief and dramatic irony were prompt luck. **Decision:** adopt the wire shapes end-to-end in 3 plans. (P1) spoke 0.9.2→0.10.0 lockstep (Rust×3 + npm×2 + drift-gate pin; single known compile break at the seam literal — actual type is non-Option `HashMap<TimelineEventModulesKey, TimelineEventModulesValue>`) + bidirectional `modules` passthrough at the narrative↔spoke seam (non-lossy; values object|array only; reject-not-drop contract with should_panic tests) + `narrative_timeline_events.modules_json` (v17). (P2) `modules.mental` (nine-field) + `modules.belief` (handbook propositions — holder/truth/access/source, never paper aliases actor/knowledge_access/mental_source) on holder KnowledgeEntry riding `kb_key_blocks.modules_json` zero-migration; `mind_states` derivative table (v18) with the validate gate at the **nexus-spoke-adapter boundary** (sole-consumer layering rule — storage stays pure); orchestrateCheck checker **pair**: `stale_belief_drift` (warning — actor IS observer / no informing event) vs `dramatic_irony_asymmetry` (info — actor absent, the deliberate box/basket thesis), truth-label-driven detection with latest-by-sequence informing-event classification. (P3) `modules` additive on `WorldKbEntityProjection` + `TimelineEventInfo` (codegen; `wire_contracts_changed`), Studio fixture light+dark (AR-6 studio-first), character inspector Mental State section + event inspector Observers line (read-only, graceful degradation, en+zh i18n). Engines (belief revision / ToM inference / observation rendering) explicitly out. Open product decisions: R-V1164P2QC-001 (world-scoped findings attribution — `findings.work_id` FK) + R-V1164P3QC-001 (observation authoring ergonomics). CI hardening: setup-monorepo pnpm cache now conditional on install-deps. | V1.164 (August 2026) |
| V1.165 — Mental-layer pipeline close-out (world findings home + observation authoring) | **Context:** V1.164 shipped the mental layer with two registered deferrals: world-scoped check findings 500 (spoke Finding is entry-scoped, but the only persistence home was the work-scoped `findings` table with NOT NULL `work_id` FK→works) and observation/mental authoring (Canvas reads `modules` that only fixtures could seed; the patch DTO `{title,body,aliases,block_type}` could not carry them). **Decision:** (P1) new `world_findings` table (v19; spoke vocabulary verbatim at persist AND read; FK→narrative_worlds; no creator column) + FindingPort routing via `extensions.nexus` scope discriminator inside the adapter (work_id→legacy byte-identical; world_id→world home; both/neither reject INVALID_INPUT; one batch tx) + checker stamp fix (world_id, the 500 root cause) + `GET /v1/daemon/worlds/:world_id/findings` (ownership-gated; SQL LIMIT probe 501/cap 500 + honest truncated flag — Bugbot-caught unbounded query fixed in-wave 226a7c5a). Legacy work findings + quality loop untouched (whole-table-enumerating consumers made column-widening unsafe). (P2) `modules` additive on `WorldKbEntityPatch` (first-level key upsert merge; omit=preserve; {}=no-op; ModuleMap shape guard at the wire boundary incl. fail-closed promote guard — the conversion seam silently drops non-conforming entries) → mental/belief/observation authoring through the product patch path; CLI `creator world event-add --observer/--access` for the checker-consumed narrative-log carrier. Dual carriers stay dual (no join). Canvas E2E via the product write path (PATCH→graph→inspector Observers line; AR-7 single-referent). Both V1.164 residuals closed by their registered completion definitions. Pattern crystallized: scope-discriminated port persistence (extensions.nexus routing generalizes to any scope-split spoke port output). | V1.165 (August 2026) |
| V1.166 — Quality-loop close-out (DR-64): rules-driven check evaluator + Control Room world-findings/rules surface | **Context:** DR-64 (real run_checker evaluator) was unlocked by V1.165's world findings home; the check callback shipped V1.164 as the mental pair only, `spoke_rules` had no production write path, and world findings were invisible in the product UI. **Decision (grill-me lock):** deterministic structured-constraint evaluator over four families (module_presence/module_absence/required_field/observer_cardinality) with the carrier at `extensions.nexus.constraint` (spoke leaves statement grammar product-owned); world scoping via adapter-crate wrapper `orchestrate_check_world_scoped` adopted by BOTH production callers (daemon + Connect N-C2) with fail-closed foreign/embedded reject — a spoke port split needs a release; CLI-first rule authoring (`creator world rule add/list/deactivate`); read surface at `/worlds/:worldId/findings` (findings panel + rules read-only section, PD-2). NL agent-judge lane deferred (DR-70, dogfood-gated). QC tri caught F-001 (manifest compile guard certifying the pre-wrapper entrypoint) — fixed wave; both plans tri-Approve + QA PASS zero-residual; `wire_contracts_changed: true` (addive world-rules-list-response DTO). | V1.166 (Aug 2026) |
